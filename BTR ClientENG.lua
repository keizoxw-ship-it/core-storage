

if isfile and readfile and isfile("BTR_Client_Ban.json") then
    local _banned = false
    local _banReason = "Access restricted by Owner."
    pcall(function()
        local raw = readfile("BTR_Client_Ban.json")
        local data = game:GetService("HttpService"):JSONDecode(raw)
        if data and data.banned then
            if data.untilTime and data.untilTime > 0 then
                local now = os.time()
                if now < data.untilTime then
                    _banned = true
                    local remaining = math.ceil((data.untilTime - now) / 60)
                    _banReason = (data.reason or "Banned by Hub Owner") .. " (" .. tostring(remaining) .. "m left)"
                else
                    if delfile then delfile("BTR_Client_Ban.json") end
                end
            else
                _banned = true
                _banReason = data.reason or "Permanently banned by Hub Owner."
            end
        end
    end)
    if _banned then
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "BTR CLIENT",
            Text = _banReason,
            Duration = 10
        })
        return
    end
end

local _BTR_GUARD = (function()
    local g = getgenv and getgenv() or _G
    
    
    local _trap = setmetatable({}, {
        __index = function(t, k)
            return function() end
        end,
        __metatable = "BTR_PROTECTED_MEMORY"
    })
    
    
    local function _anti_decompile_junk()
        local _o = 1
        while _o == 2 do
            local a, b, c = 1, 2, 3
            if a == b and b == c then
                return (function() return _trap end)()
            end
        end
    end

    
    local protected_env = setmetatable({}, {
        __index = getfenv and getfenv() or _G,
        __metatable = "LOCKED_ENV"
    })

    return {
        trap = _trap,
        env = protected_env
    }
end)()

pcall(function()
    if _G.BTR_MASTER_CLEANUP and type(_G.BTR_MASTER_CLEANUP) == "function" then
        _G.BTR_MASTER_CLEANUP()
    end
    if _G.AntiSitForceOff and type(_G.AntiSitForceOff) == "function" then
        _G.AntiSitForceOff()
    end
    if _G.BTR_StopRealBlackHole and type(_G.BTR_StopRealBlackHole) == "function" then
        _G.BTR_StopRealBlackHole()
    end
    if _G.BTR_StopLightPlayer and type(_G.BTR_StopLightPlayer) == "function" then
        _G.BTR_StopLightPlayer()
    end
    if _G.BTR_StopVisualSuite and type(_G.BTR_StopVisualSuite) == "function" then
        _G.BTR_StopVisualSuite()
    end
    if _G.BTR_StopBunkerPrison and type(_G.BTR_StopBunkerPrison) == "function" then
        _G.BTR_StopBunkerPrison()
    end
    if _G.BTR_StopApexTitan and type(_G.BTR_StopApexTitan) == "function" then
        _G.BTR_StopApexTitan()
    end
    if _G.BTR_StopGiganticTitan and type(_G.BTR_StopGiganticTitan) == "function" then
        _G.BTR_StopGiganticTitan()
    end
    local lp = game:GetService("Players").LocalPlayer
    local char = lp and lp.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hum then
        hum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
        hum.PlatformStand = false
        if hum.Sit then
            hum.Sit = false
        end
        if hum.WalkSpeed < 16 or hum.WalkSpeed == 0 then hum.WalkSpeed = 16 end
        for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
            pcall(function() track:Stop(0) end)
        end
    end
    if hrp then
        hrp.Anchored = false
        for _, c in ipairs(hrp:GetChildren()) do
            if c:IsA("BodyMover") or c:IsA("BodyVelocity") or c:IsA("BodyGyro") or c:IsA("AlignPosition") or c:IsA("Torque") or c:IsA("LinearVelocity") or c:IsA("VectorForce") then
                pcall(function() c:Destroy() end)
            end
        end
    end
    if char then
        for _, desc in ipairs(char:GetDescendants()) do
            if desc:IsA("BasePart") then
                desc.Anchored = false
            end
        end
    end
    end)

_G.BTR_SESSION_ID = (_G.BTR_SESSION_ID or 0) + 1

_G.BTR_CONNECTIONS = _G.BTR_CONNECTIONS or {}
for _, conn in ipairs(_G.BTR_CONNECTIONS) do
    pcall(function() if conn and conn.Connected then conn:Disconnect() end end)
end
_G.BTR_CONNECTIONS = {}

pcall(function()
    local containers = {}
    pcall(function() table.insert(containers, game:GetService("CoreGui")) end)
    pcall(function() if gethui then table.insert(containers, gethui()) end end)
    pcall(function()
        local lp = game:GetService("Players").LocalPlayer
        if lp and lp:FindFirstChild("PlayerGui") then
            table.insert(containers, lp.PlayerGui)
        end
    end)
    for _, parent in ipairs(containers) do
        for _, child in ipairs(parent:GetChildren()) do
            if child.Name:find("Redz") or child.Name:find("BTR") or child.Name:find("Drip") then
                pcall(function() child:Destroy() end)
            end
        end
    end
    for _, name in ipairs({"BTR_ShootingStars", "BTR_FakeRain", "BTR_ShaderSoundPart"}) do
        local inst = workspace:FindFirstChild(name)
        if inst then pcall(function() inst:Destroy() end) end
    end
    local lighting = game:GetService("Lighting")
    for _, name in ipairs({"BTR_CinematicBlur", "BTR_DarkBrookSky", "BTR_ColorCorrection", "BTR_Bloom", "BTR_SunRays", "BTR_Blur"}) do
        local inst = lighting:FindFirstChild(name)
        if inst then pcall(function() inst:Destroy() end) end
    end
    collectgarbage("collect")
end)

_G.BTR_MASTER_CLEANUP = function()
    
    for _, conn in ipairs(_G.BTR_CONNECTIONS or {}) do
        pcall(function() if conn and conn.Connected then conn:Disconnect() end end)
    end
    _G.BTR_CONNECTIONS = {}

    
    _G.BTR_SESSION_ID = (_G.BTR_SESSION_ID or 0) + 1

    
    pcall(function()
        local containers = {}
        pcall(function() table.insert(containers, game:GetService("CoreGui")) end)
        pcall(function() if gethui then table.insert(containers, gethui()) end end)
        pcall(function()
            local lp = game:GetService("Players").LocalPlayer
            if lp and lp:FindFirstChild("PlayerGui") then
                table.insert(containers, lp.PlayerGui)
            end
        end)
        for _, parent in ipairs(containers) do
            for _, child in ipairs(parent:GetChildren()) do
                if child.Name:find("Redz") or child.Name:find("BTR") or child.Name:find("Drip") then
                    pcall(function() child:Destroy() end)
                end
            end
        end
    end)

    
    pcall(function()
        for _, name in ipairs({"BTR_ShootingStars", "BTR_FakeRain", "BTR_ShaderSoundPart"}) do
            local inst = workspace:FindFirstChild(name)
            if inst then pcall(function() inst:Destroy() end) end
        end
        local lighting = game:GetService("Lighting")
        for _, name in ipairs({"BTR_CinematicBlur", "BTR_DarkBrookSky", "BTR_ColorCorrection", "BTR_Bloom", "BTR_SunRays", "BTR_Blur"}) do
            local inst = lighting:FindFirstChild(name)
            if inst then pcall(function() inst:Destroy() end) end
        end
    end)

    
    if _G.AntiSitForceOff then
        pcall(_G.AntiSitForceOff)
    end
    _G.AntiSitValue = false
    _G.VehicleFlingActive = false
    _G.VehicleFlingCar = nil
    _G.CanoeSpamActive = false
    if getgenv then pcall(function() getgenv().Toggle = false end) end
    if _G.BTR_StopDoorOrbit then
        pcall(_G.BTR_StopDoorOrbit)
    end
    if _G.BTR_StopAstralSovereign then
        pcall(_G.BTR_StopAstralSovereign)
    end
    if _G.BTR_StopRealBlackHole then
        pcall(_G.BTR_StopRealBlackHole)
    end
    if _G.BTR_StopLightPlayer then
        pcall(_G.BTR_StopLightPlayer)
    end
    if _G.BTR_StopVisualSuite then
        pcall(_G.BTR_StopVisualSuite)
    end

    
    pcall(function()
        local lp = game:GetService("Players").LocalPlayer
        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if hum then
            hum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
            if hum.PlatformStand == true then hum.PlatformStand = false end
            if hum.Sit == true then hum.Sit = false end
            if hum.WalkSpeed < 16 then hum.WalkSpeed = 16 end
            for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
                pcall(function() track:Stop(0) end)
            end
        end
        if hrp then
            hrp.Anchored = false
            for _, c in ipairs(hrp:GetChildren()) do
                if c:IsA("BodyMover") or c:IsA("BodyVelocity") or c:IsA("BodyGyro") or c:IsA("AlignPosition") or c:IsA("Torque") then
                    pcall(function() c:Destroy() end)
                end
            end
        end
    end)

    
    pcall(collectgarbage, "collect")
end

-- [LOCKED: WORK PERFECTLY] Helper Anti-Sit pour Carjack/Remove Free

local function withAntiSitDisabled(action)
    
    
    
    
    
    
    if not _G.AntiSitForceOff then
        local waited = 0
        while not _G.AntiSitForceOff and waited < 10 do
            task.wait(0.1)
            waited = waited + 0.1
        end
    end

    
    
    
    local wasActive = (_G.AntiSitValue == true)
    if wasActive then
        
        if _G.AntiSitForceOff then
            pcall(function() _G.AntiSitForceOff() end)
        else
            
            
            _G.AntiSitValue = false
            pcall(function() _G.AntiSitToggle:Set(false) end)
            local lpChar = game.Players.LocalPlayer.Character
            local humFb = lpChar and lpChar:FindFirstChildOfClass("Humanoid")
            if humFb then
                pcall(function() humFb:SetStateEnabled(Enum.HumanoidStateType.Seated, true) end)
            end
        end
        task.wait(0.05)
    end

    
    
    local prevFling = _G.VehicleFlingActive
    _G.VehicleFlingActive = true
    local ok, err = pcall(action)
    _G.VehicleFlingActive = prevFling or false

    
    local lpChar = game.Players.LocalPlayer.Character
    local humOut = lpChar and lpChar:FindFirstChildOfClass("Humanoid")
    local hrpOut = lpChar and lpChar:FindFirstChild("HumanoidRootPart")
    if humOut and (humOut.Sit or humOut.SeatPart ~= nil) then
        pcall(function() humOut.Sit = false end)
        pcall(function() humOut:ChangeState(Enum.HumanoidStateType.GettingUp) end)
        pcall(function() humOut.Jump = true end)
        if hrpOut then
            pcall(function()
                hrpOut.AssemblyLinearVelocity = Vector3.new(0, 60, 0)
                hrpOut.Velocity = Vector3.new(0, 60, 0)
            end)
        end
        task.wait(0.05)
        pcall(function() humOut.Jump = false end)
    end

    if wasActive then
        
        if _G.AntiSitForceOn then
            pcall(function() _G.AntiSitForceOn() end)
        elseif _G.AntiSitToggle then
            pcall(function() _G.AntiSitToggle:Set(true) end)
            _G.AntiSitValue = true
        end
    end
    return ok, err
end
_G.withAntiSitDisabled = withAntiSitDisabled

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
local RunService = game:GetService("RunService")

_G.BTR_UI_READY = false

local BTR_CONFIG_FILE = "BTR_Client_Config.json"

_G.BTR_AUTORUN = {
    AntiSit = true,
    AntiCopy = false,
    AntiAfk = false,
    Vehicles = false,
    Dump = false,
    Canoes = false,
    Jets = false,
    Helis = false,
    Balls = false,
    Doors = false,
    PropPlacement = true 
}
_G.BTR_ANTI_COPY_REACTION = "Load Saved Slot Outfit"
_G.BTR_OUTFIT_SLOT = 1
_G.BTR_COPIED_PLAYERS = _G.BTR_COPIED_PLAYERS or {}
_G.BTR_IS_COPYING_AVATAR = false
_G.BTR_LAST_COPY_TIME = 0

_G.BTR_PROT_TOGGLES = _G.BTR_PROT_TOGGLES or {}
_G.BTR_SETTINGS_TOGGLES = _G.BTR_SETTINGS_TOGGLES or {}
_G.BTR_PROT_CALLBACKS = _G.BTR_PROT_CALLBACKS or {}

_G.BTR_MENU_OPACITY = 100
_G.BTR_CATEGORY_OPACITY = 100
_G.BTR_CATEGORY_COLOR = "White"
_G.BTR_SELECTED_WALLPAPER = "Wallpaper 1"
_G.BTR_CUSTOM_WALLPAPER_ID = ""
_G.BTR_CUSTOM_WALLPAPERS_HISTORY = {}
_G.BTR_ICONS = {
    ["Default"] = "rbxassetid://122646861461158",
    ["Option 1"] = "rbxassetid://125815391535552",
    ["Custom ID"] = "CUSTOM"
}
_G.BTR_SELECTED_ICON = _G.BTR_SELECTED_ICON or "Default"
_G.BTR_CUSTOM_ICON_ID = _G.BTR_CUSTOM_ICON_ID or ""
_G.BTR_HUB_SCALE = 100
_G.BTR_TEXT_COLOR = "Default"
_G.BTR_TITLE_FONT = "GothamBlack"
_G.BTR_TITLE_STYLE = "Cyberpunk (Pink & Cyan)"
_G.BTR_NO_ANIMATION = false
_G.BTR_SHOW_FLOATING_BUTTON = true
_G.BTR_PROP_BYPASS_MODE = "Remote"
_G.BTR_AUTOLOAD_OUTFIT = false
_G.BTR_AUTOLOAD_SLOT = 3

function _G.saveBTRConfig()
    if writefile then
        pcall(function()
            local data = {
                antiCopyReaction = _G.BTR_ANTI_COPY_REACTION,
                selectedOutfitSlot = _G.BTR_OUTFIT_SLOT,
                autoRun = _G.BTR_AUTORUN,
                selectedWallpaper = _G.BTR_SELECTED_WALLPAPER,
                customWallpaperId = _G.BTR_CUSTOM_WALLPAPER_ID,
                customWallpapersHistory = _G.BTR_CUSTOM_WALLPAPERS_HISTORY,
                selectedIcon = _G.BTR_SELECTED_ICON,
                customIconId = _G.BTR_CUSTOM_ICON_ID,
                menuOpacity = _G.BTR_MENU_OPACITY,
                categoryOpacity = _G.BTR_CATEGORY_OPACITY,
                categoryColor = _G.BTR_CATEGORY_COLOR,
                hubScale = _G.BTR_HUB_SCALE,
                textColor = _G.BTR_TEXT_COLOR,
                titleFont = _G.BTR_TITLE_FONT,
                titleColorStyle = _G.BTR_TITLE_STYLE,
                noAnimation = _G.BTR_NO_ANIMATION,
                showFloatingButton = _G.BTR_SHOW_FLOATING_BUTTON,
                autoCorrectTyping = (_G.AutoCorrectEnabled == true),
                autoTranslateOutgoing = (_G.AutoTranslateOutgoingEnabled == true),
                autoTranslateIncoming = (_G.AutoTranslateIncomingEnabled == true),
                outgoingTargetLang = _G.OutgoingTargetLang,
                outgoingFormat = _G.OutgoingFormat,
                incomingTargetLang = _G.IncomingTargetLang,
                propBypassMode = _G.BTR_PROP_BYPASS_MODE or "Remote",
                autoLoadOutfit = (_G.BTR_AUTOLOAD_OUTFIT == true),
                autoLoadSlot = tonumber(_G.BTR_AUTOLOAD_SLOT) or 3,

                ownerUserNotif = (_G.BTR_OWNER_NOTIF_ENABLED ~= false),
rgbNameActive = (_G.BTR_RGB_NAME_ACTIVE == true),
                rgbBioActive = (_G.BTR_RGB_BIO_ACTIVE == true),
                rgbSpeed = tonumber(_G.BTR_RAINBOW_SPEED) or 5,
                nameColorMode = _G.BTR_NAME_COLOR_MODE or "Full Rainbow (RGB)",
                bioColorMode = _G.BTR_BIO_COLOR_MODE or "Full Rainbow (RGB)",
                nameCol1 = _G.BTR_NAME_COL1 or {0, 255, 255},
                nameCol2 = _G.BTR_NAME_COL2 or {170, 0, 255},
                nameCol3 = _G.BTR_NAME_COL3 or {255, 0, 128},
                bioCol1 = _G.BTR_BIO_COL1 or {50, 255, 50},
                bioCol2 = _G.BTR_BIO_COL2 or {255, 215, 0},
                bioCol3 = _G.BTR_BIO_COL3 or {255, 30, 30}
            }
            writefile(BTR_CONFIG_FILE, game:GetService("HttpService"):JSONEncode(data))
        end)
    end
end

function _G.loadBTRConfig()
    if isfile and readfile and isfile(BTR_CONFIG_FILE) then
        pcall(function()
            local raw = readfile(BTR_CONFIG_FILE)
            local data = game:GetService("HttpService"):JSONDecode(raw)
            if data then
                if data.antiCopyReaction then _G.BTR_ANTI_COPY_REACTION = data.antiCopyReaction end
                if data.selectedOutfitSlot then _G.BTR_OUTFIT_SLOT = tonumber(data.selectedOutfitSlot) or 1 end
                if data.autoRun and type(data.autoRun) == "table" then
                    for k, v in pairs(data.autoRun) do
                        _G.BTR_AUTORUN[k] = (v == true)
                    end
                end
                if data.selectedWallpaper then
                    _G.BTR_SELECTED_WALLPAPER = tostring(data.selectedWallpaper)
                end
                if data.customWallpaperId then
                    _G.BTR_CUSTOM_WALLPAPER_ID = tostring(data.customWallpaperId)
                end
                if data.customWallpapersHistory and type(data.customWallpapersHistory) == "table" then
                    _G.BTR_CUSTOM_WALLPAPERS_HISTORY = data.customWallpapersHistory
                end
                if data.selectedIcon then
                    _G.BTR_SELECTED_ICON = tostring(data.selectedIcon)
                end
                if data.customIconId then
                    _G.BTR_CUSTOM_ICON_ID = tostring(data.customIconId)
                end
                if data.menuOpacity then
                    _G.BTR_MENU_OPACITY = tonumber(data.menuOpacity) or 100
                end
                if data.categoryOpacity then
                    _G.BTR_CATEGORY_OPACITY = tonumber(data.categoryOpacity) or 100
                end
                if data.categoryColor then
                    _G.BTR_CATEGORY_COLOR = tostring(data.categoryColor)
                end
                if data.hubScale then
                    _G.BTR_HUB_SCALE = tonumber(data.hubScale) or 150
                else
                    _G.BTR_HUB_SCALE = 100
                end
                if data.textColor then
                    _G.BTR_TEXT_COLOR = tostring(data.textColor)
                end
                if data.titleFont then
                    _G.BTR_TITLE_FONT = tostring(data.titleFont)
                end
                if data.titleColorStyle then
                    _G.BTR_TITLE_STYLE = tostring(data.titleColorStyle)
                end
                if data.noAnimation ~= nil then
                    _G.BTR_NO_ANIMATION = (data.noAnimation == true)
                end
                if data.showFloatingButton ~= nil then
                    _G.BTR_SHOW_FLOATING_BUTTON = (data.showFloatingButton == true)
                end
                if data.autoCorrectTyping ~= nil then
                    _G.AutoCorrectEnabled = (data.autoCorrectTyping == true)
                end
                if data.autoTranslateOutgoing ~= nil then
                    _G.AutoTranslateOutgoingEnabled = (data.autoTranslateOutgoing == true)
                end
                if data.autoTranslateIncoming ~= nil then
                    _G.AutoTranslateIncomingEnabled = (data.autoTranslateIncoming == true)
                end
                if data.outgoingTargetLang then
                    _G.OutgoingTargetLang = tostring(data.outgoingTargetLang)
                end
                if data.outgoingFormat then
                    _G.OutgoingFormat = tostring(data.outgoingFormat)
                end
                if data.incomingTargetLang then
                    _G.IncomingTargetLang = tostring(data.incomingTargetLang)
                end
                if data.propBypassMode then
                    _G.BTR_PROP_BYPASS_MODE = tostring(data.propBypassMode)
                else
                    _G.BTR_PROP_BYPASS_MODE = "Remote"
                end
                if data.autoLoadOutfit ~= nil then
                    _G.BTR_AUTOLOAD_OUTFIT = (data.autoLoadOutfit == true)
                elseif data.autoLoadSlot3 ~= nil then
                    _G.BTR_AUTOLOAD_OUTFIT = (data.autoLoadSlot3 == true)
                end
                if data.autoLoadSlot ~= nil then
                    _G.BTR_AUTOLOAD_SLOT = tonumber(data.autoLoadSlot) or 3
                else
                    _G.BTR_AUTOLOAD_SLOT = 3
                end
                if data.ownerUserNotif ~= nil then
_G.BTR_OWNER_NOTIF_ENABLED = (data.ownerUserNotif == true)
else
_G.BTR_OWNER_NOTIF_ENABLED = true
end
if data.rgbNameActive ~= nil then
                    _G.BTR_RGB_NAME_ACTIVE = (data.rgbNameActive == true)
                end
                if data.rgbBioActive ~= nil then
                    _G.BTR_RGB_BIO_ACTIVE = (data.rgbBioActive == true)
                end
                if data.rgbSpeed ~= nil then
                    _G.BTR_RAINBOW_SPEED = tonumber(data.rgbSpeed) or 5
                end
                if data.nameColorMode ~= nil then
                    _G.BTR_NAME_COLOR_MODE = tostring(data.nameColorMode)
                end
                if data.bioColorMode ~= nil then
                    _G.BTR_BIO_COLOR_MODE = tostring(data.bioColorMode)
                end
                if data.nameCol1 and type(data.nameCol1) == "table" then
                    _G.BTR_NAME_COL1 = data.nameCol1
                end
                if data.nameCol2 and type(data.nameCol2) == "table" then
                    _G.BTR_NAME_COL2 = data.nameCol2
                end
                if data.nameCol3 and type(data.nameCol3) == "table" then
                    _G.BTR_NAME_COL3 = data.nameCol3
                end
                if data.bioCol1 and type(data.bioCol1) == "table" then
                    _G.BTR_BIO_COL1 = data.bioCol1
                end
                if data.bioCol2 and type(data.bioCol2) == "table" then
                    _G.BTR_BIO_COL2 = data.bioCol2
                end
                if data.bioCol3 and type(data.bioCol3) == "table" then
                    _G.BTR_BIO_COL3 = data.bioCol3
                end
            end
        end)
    end
end

_G.loadBTRConfig()

local _btrSyncing = false
function _G.setBTRProtection(key, state, fromWhere)
    
    local cb = _G.BTR_PROT_CALLBACKS and _G.BTR_PROT_CALLBACKS[key]
    if cb then pcall(function() cb(state) end) end

    
    if not _btrSyncing then
        _btrSyncing = true
        if fromWhere ~= "ProtectionTab" then
            local pTg = _G.BTR_PROT_TOGGLES and _G.BTR_PROT_TOGGLES[key]
            if pTg and pTg.Set then pcall(function() pTg:Set(state) end) end
        end
        _btrSyncing = false
    end
end

function _G.setBTRStartupPref(key, state)
    _G.BTR_AUTORUN[key] = (state == true)
    _G.saveBTRConfig()
end

function _G.syncAutoProtections()
    if not _G.BTR_AUTORUN then return end
    for key, enabled in pairs(_G.BTR_AUTORUN) do
        local cb = _G.BTR_PROT_CALLBACKS[key]
        if cb then
            pcall(function() cb(enabled == true) end)
        end
        local tg = _G.BTR_PROT_TOGGLES[key]
        if tg and tg.Set then
            pcall(function() tg:Set(enabled == true) end)
        end
    end
end

local redzlibContent = game:HttpGet("https://raw.githubusercontent.com/realgengar/Library/refs/heads/main/remake.lua", true)

local function plainReplace(str, search, repl)
    local idx = string.find(str, search, 1, true)
    while idx do
        str = string.sub(str, 1, idx - 1) .. repl .. string.sub(str, idx + #search)
        idx = string.find(str, search, idx + #repl, true)
    end
    return str
end

local oldSetToggleBlock = [[if Default then
					CreateTween({Toggle, "Position", UDim2.new(1, 0, 0.5), 0.25})
					CreateTween({Toggle, "BackgroundTransparency", 0, 0.25})
					CreateTween({Toggle, "AnchorPoint", Vector2.new(1, 0.5), 0.25, Wait or false})
				else
					CreateTween({Toggle, "Position", UDim2.new(0, 0, 0.5), 0.25})
					CreateTween({Toggle, "BackgroundTransparency", 0.8, 0.25})
					CreateTween({Toggle, "AnchorPoint", Vector2.new(0, 0.5), 0.25, Wait or false})
				end]]

local newSetToggleBlock = [[if Default then
					Toggle.Position = UDim2.new(1, 0, 0.5)
					Toggle.BackgroundTransparency = 0
					Toggle.AnchorPoint = Vector2.new(1, 0.5)
					CreateTween({Toggle, "Position", UDim2.new(1, 0, 0.5), 0.25})
					CreateTween({Toggle, "BackgroundTransparency", 0, 0.25})
					CreateTween({Toggle, "AnchorPoint", Vector2.new(1, 0.5), 0.25, Wait or false})
				else
					Toggle.Position = UDim2.new(0, 0, 0.5)
					Toggle.BackgroundTransparency = 0.8
					Toggle.AnchorPoint = Vector2.new(0, 0.5)
					CreateTween({Toggle, "Position", UDim2.new(0, 0, 0.5), 0.25})
					CreateTween({Toggle, "BackgroundTransparency", 0.8, 0.25})
					CreateTween({Toggle, "AnchorPoint", Vector2.new(0, 0.5), 0.25, Wait or false})
				end]]
redzlibContent = plainReplace(redzlibContent, oldSetToggleBlock, newSetToggleBlock)

local oldTabAnimBlock = [[Container.Size = UDim2.new(1, 0, 1, 150)
			table.foreach(redzlib.Tabs, function(_,Tab)
				if Tab.Cont ~= Container then
					Tab.func:Disable()
				end
			end)
			CreateTween({Container, "Size", UDim2.new(1, 0, 1, 0), 0.3})]]

local newTabAnimBlock = [[if _G.BTR_NO_ANIMATION then
				Container.Size = UDim2.new(1, 0, 1, 0)
				table.foreach(redzlib.Tabs, function(_,Tab)
					if Tab.Cont ~= Container then
						Tab.func:Disable()
					end
				end)
			else
				Container.Size = UDim2.new(1, 0, 1, 150)
				table.foreach(redzlib.Tabs, function(_,Tab)
					if Tab.Cont ~= Container then
						Tab.func:Disable()
					end
				end)
				CreateTween({Container, "Size", UDim2.new(1, 0, 1, 0), 0.3})
			end
			if _G.BTR_ApplyTextColors then
				task.spawn(_G.BTR_ApplyTextColors)
			end]]
redzlibContent = plainReplace(redzlibContent, oldTabAnimBlock, newTabAnimBlock)

redzlibContent = plainReplace(redzlibContent, 'Image = "rbxassetid://126463403744337"', 'Image = "", ImageTransparency = 1, Visible = false')
redzlibContent = plainReplace(redzlibContent, 'Image = "rbxassetid://75259374779057"', 'Image = "", ImageTransparency = 1, Visible = false')

redzlibContent = plainReplace(redzlibContent, 'TextTransparency = (FirstTab and 0.3) or 0', 'TextTransparency = 0')
redzlibContent = plainReplace(redzlibContent, 'ImageTransparency = (FirstTab and 0.3) or 0', 'ImageTransparency = 0')
redzlibContent = plainReplace(redzlibContent, 'CreateTween({LabelTitle, "TextTransparency", 0.3, 0.35})', 'CreateTween({LabelTitle, "TextTransparency", 0, 0.35})')
redzlibContent = plainReplace(redzlibContent, 'CreateTween({LabelIcon, "ImageTransparency", 0.3, 0.35})', 'CreateTween({LabelIcon, "ImageTransparency", 0, 0.35})')
redzlibContent = plainReplace(redzlibContent, '["Color Text"] = Color3.fromRGB(243, 243, 243)', '["Color Text"] = Color3.fromRGB(255, 255, 255)')

local closeBtnStart = string.find(redzlibContent, "function Window:CloseBtn()", 1, true)
local minBtnStart = string.find(redzlibContent, "function Window:MinimizeBtn()", 1, true)
if closeBtnStart and minBtnStart and closeBtnStart < minBtnStart then
    local btrCloseCode = [[function Window:CloseBtn()
		local Dialog = Window:Dialog({
			Title = "BTR Client",
			Text = "Choose an action: Hide the menu (reopen anytime via the BTR button) or completely Unload BTR Client?",
			Options = {
				{"Hide Menu", function()
					MainFrame.Visible = false
				end},
				{"Unload Script", function()
					if _G.BTR_MASTER_CLEANUP then pcall(_G.BTR_MASTER_CLEANUP) end
					ScreenGui:Destroy()
				end},
				{"Cancel"}
			}
		})
	end
	]]
    redzlibContent = string.sub(redzlibContent, 1, closeBtnStart - 1) .. btrCloseCode .. string.sub(redzlibContent, minBtnStart)
end

redzlibContent = redzlibContent:gsub("Color3%.fromRGB%(179,%s*0,%s*255%)", "Color3.fromRGB(210, 210, 220)")
redzlibContent = redzlibContent:gsub("Color3%.fromRGB%(128,%s*0,%s*255%)", "Color3.fromRGB(160, 160, 170)")
redzlibContent = redzlibContent:gsub("Color3%.fromRGB%(170,%s*0,%s*255%)", "Color3.fromRGB(180, 180, 190)")
redzlibContent = redzlibContent:gsub("Color3%.fromRGB%(27%.5,%s*25,%s*30%)", "Color3.fromRGB(16, 16, 18)")
redzlibContent = redzlibContent:gsub("Color3%.fromRGB%(32%.5,%s*32%.5,%s*32%.5%)", "Color3.fromRGB(22, 22, 25)")
redzlibContent = redzlibContent:gsub("Color3%.fromRGB%(30,%s*30,%s*30%)", "Color3.fromRGB(18, 18, 20)")
redzlibContent = redzlibContent:gsub("Color3%.fromRGB%(40,%s*40,%s*40%)", "Color3.fromRGB(38, 38, 42)")
local redzlib = loadstring(redzlibContent)()
if not redzlib then error("deu red") end
_G.BTR_REDZ_LIB = redzlib

workspace.FallenPartsDestroyHeight = -math.huge

local function startBTRLoader(onFinish)
    local TweenService = game:GetService("TweenService")
    local CoreGui = (gethui and gethui()) or game:GetService("CoreGui")
    local Players = game:GetService("Players")
    local Lighting = game:GetService("Lighting")
    local lp = Players.LocalPlayer

    local blur = Instance.new("BlurEffect")
    blur.Name = "BTR_CinematicBlur"
    blur.Size = 0
    blur.Parent = Lighting
    TweenService:Create(blur, TweenInfo.new(0.35, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Size = 10 }):Play()

    local sg = Instance.new("ScreenGui")
    sg.Name = "BTR_Loader_Screen"
    sg.ResetOnSpawn = false
    sg.IgnoreGuiInset = true
    sg.DisplayOrder = 999999
    pcall(function() sg.Parent = CoreGui end)
    if not sg.Parent then sg.Parent = lp:WaitForChild("PlayerGui") end

    local bgOverlay = Instance.new("Frame")
    bgOverlay.Name = "BackgroundOverlay"
    bgOverlay.Size = UDim2.new(1, 0, 1, 0)
    bgOverlay.BackgroundColor3 = Color3.fromRGB(8, 8, 12)
    bgOverlay.BackgroundTransparency = 0.2
    bgOverlay.BorderSizePixel = 0
    bgOverlay.Parent = sg

    
    local card = Instance.new("Frame")
    card.Name = "LoaderCard"
    card.Size = UDim2.new(0, 440, 0, 175)
    card.Position = UDim2.new(0.5, -220, 0.5, -88)
    card.BackgroundColor3 = Color3.fromRGB(12, 13, 18)
    card.BackgroundTransparency = 0.12
    card.BorderSizePixel = 0
    card.ClipsDescendants = true
    card.Parent = bgOverlay

    local cardCorner = Instance.new("UICorner", card)
    cardCorner.CornerRadius = UDim.new(0, 12)

    local cardStroke = Instance.new("UIStroke", card)
    cardStroke.Color = Color3.fromRGB(0, 200, 255)
    cardStroke.Transparency = 0.35
    cardStroke.Thickness = 1.2
    cardStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border

    
    local glowLine = Instance.new("Frame", card)
    glowLine.Name = "GlowLine"
    glowLine.Size = UDim2.new(1, 0, 0, 2)
    glowLine.Position = UDim2.new(0, 0, 0, 0)
    glowLine.BorderSizePixel = 0
    glowLine.BackgroundColor3 = Color3.fromRGB(0, 220, 255)

    local glowGrad = Instance.new("UIGradient", glowLine)
    glowGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 220, 255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(160, 60, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 128))
    })

    
    local badgeFrame = Instance.new("Frame", card)
    badgeFrame.Name = "BadgeFrame"
    badgeFrame.Size = UDim2.new(0, 95, 0, 20)
    badgeFrame.Position = UDim2.new(0, 25, 0, 20)
    badgeFrame.BackgroundColor3 = Color3.fromRGB(0, 220, 255)
    badgeFrame.BackgroundTransparency = 0.85
    Instance.new("UICorner", badgeFrame).CornerRadius = UDim.new(0, 4)

    local badgeLbl = Instance.new("TextLabel", badgeFrame)
    badgeLbl.Size = UDim2.new(1, 0, 1, 0)
    badgeLbl.BackgroundTransparency = 1
    badgeLbl.Text = "BTR // CLIENT"
    badgeLbl.TextColor3 = Color3.fromRGB(0, 220, 255)
    badgeLbl.Font = Enum.Font.GothamBold
    badgeLbl.TextSize = 10

    local verLbl = Instance.new("TextLabel", card)
    verLbl.Name = "VersionLabel"
    verLbl.Size = UDim2.new(0, 120, 0, 20)
    verLbl.Position = UDim2.new(1, -145, 0, 20)
    verLbl.BackgroundTransparency = 1
    verLbl.Text = "v3.5.0"
    verLbl.TextColor3 = Color3.fromRGB(130, 135, 155)
    verLbl.Font = Enum.Font.GothamMedium
    verLbl.TextSize = 10
    verLbl.TextXAlignment = Enum.TextXAlignment.Right

    
    local title = Instance.new("TextLabel", card)
    title.Name = "Title"
    title.Size = UDim2.new(1, -50, 0, 26)
    title.Position = UDim2.new(0, 25, 0, 48)
    title.BackgroundTransparency = 1
    title.Text = "B T R   C L I E N T"
    title.TextColor3 = Color3.fromRGB(250, 250, 255)
    title.Font = Enum.Font.GothamBlack
    title.TextSize = 18
    title.TextXAlignment = Enum.TextXAlignment.Left

    local subtitle = Instance.new("TextLabel", card)
    subtitle.Name = "Subtitle"
    subtitle.Size = UDim2.new(1, -50, 0, 18)
    subtitle.Position = UDim2.new(0, 25, 0, 75)
    subtitle.BackgroundTransparency = 1
    subtitle.Text = "Brookhaven RP Enhanced Framework"
    subtitle.TextColor3 = Color3.fromRGB(140, 145, 165)
    subtitle.Font = Enum.Font.GothamMedium
    subtitle.TextSize = 11
    subtitle.TextXAlignment = Enum.TextXAlignment.Left

    
    local statusLbl = Instance.new("TextLabel", card)
    statusLbl.Name = "Status"
    statusLbl.Size = UDim2.new(1, -110, 0, 18)
    statusLbl.Position = UDim2.new(0, 25, 0, 114)
    statusLbl.BackgroundTransparency = 1
    statusLbl.Text = "> Connecting to Brookhaven network..."
    statusLbl.TextColor3 = Color3.fromRGB(170, 210, 235)
    statusLbl.Font = Enum.Font.GothamMedium
    statusLbl.TextSize = 11
    statusLbl.TextXAlignment = Enum.TextXAlignment.Left

    local pctLbl = Instance.new("TextLabel", card)
    pctLbl.Name = "Percentage"
    pctLbl.Size = UDim2.new(0, 60, 0, 18)
    pctLbl.Position = UDim2.new(1, -85, 0, 114)
    pctLbl.BackgroundTransparency = 1
    pctLbl.Text = "0%"
    pctLbl.TextColor3 = Color3.fromRGB(0, 220, 255)
    pctLbl.Font = Enum.Font.GothamBold
    pctLbl.TextSize = 11
    pctLbl.TextXAlignment = Enum.TextXAlignment.Right

    
    local barBg = Instance.new("Frame", card)
    barBg.Name = "BarBackground"
    barBg.Size = UDim2.new(1, -50, 0, 4)
    barBg.Position = UDim2.new(0, 25, 0, 138)
    barBg.BackgroundColor3 = Color3.fromRGB(24, 25, 34)
    barBg.BorderSizePixel = 0
    Instance.new("UICorner", barBg).CornerRadius = UDim.new(1, 0)

    local barFill = Instance.new("Frame", barBg)
    barFill.Name = "BarFill"
    barFill.Size = UDim2.new(0, 0, 1, 0)
    barFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    barFill.BorderSizePixel = 0
    Instance.new("UICorner", barFill).CornerRadius = UDim.new(1, 0)

    local barGrad = Instance.new("UIGradient", barFill)
    barGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 220, 255)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(160, 60, 255)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 128))
    })

    card.Position = UDim2.new(0.5, -220, 0.5, -78)
    card.BackgroundTransparency = 1
    title.TextTransparency = 1
    subtitle.TextTransparency = 1
    statusLbl.TextTransparency = 1
    pctLbl.TextTransparency = 1
    badgeFrame.BackgroundTransparency = 1
    badgeLbl.TextTransparency = 1
    verLbl.TextTransparency = 1
    barBg.BackgroundTransparency = 1

    TweenService:Create(card, TweenInfo.new(0.4, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
        Position = UDim2.new(0.5, -220, 0.5, -88),
        BackgroundTransparency = 0.12
    }):Play()
    TweenService:Create(title, TweenInfo.new(0.3), { TextTransparency = 0 }):Play()
    TweenService:Create(subtitle, TweenInfo.new(0.3), { TextTransparency = 0 }):Play()
    TweenService:Create(statusLbl, TweenInfo.new(0.3), { TextTransparency = 0 }):Play()
    TweenService:Create(pctLbl, TweenInfo.new(0.3), { TextTransparency = 0 }):Play()
    TweenService:Create(badgeFrame, TweenInfo.new(0.3), { BackgroundTransparency = 0.85 }):Play()
    TweenService:Create(badgeLbl, TweenInfo.new(0.3), { TextTransparency = 0 }):Play()
    TweenService:Create(verLbl, TweenInfo.new(0.3), { TextTransparency = 0 }):Play()
    TweenService:Create(barBg, TweenInfo.new(0.3), { BackgroundTransparency = 0 }):Play()

    
    task.spawn(function()
        local function setProgress(pct, statusText)
            pctLbl.Text = tostring(math.floor(pct * 100)) .. "%"
            if statusText then statusLbl.Text = "> " .. statusText end
            TweenService:Create(barFill, TweenInfo.new(0.2, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                Size = UDim2.new(math.clamp(pct, 0, 1), 0, 1, 0)
            }):Play()
        end

        setProgress(0.30, "Initializing client...")
        if not game:IsLoaded() then game.Loaded:Wait() end
        task.wait(0.04)

        setProgress(0.65, "Loading modules...")
        local char = lp.Character or lp.CharacterAdded:Wait()
        if not char:FindFirstChild("HumanoidRootPart") then
            char:WaitForChild("HumanoidRootPart", 3)
        end
        task.wait(0.04)

        setProgress(0.88, "Preparing interface...")

        local maxWait = 8.0
        local elapsed = 0
        while not _G.BTR_ALL_LOADED and elapsed < maxWait do
            task.wait(0.04)
            elapsed = elapsed + 0.04
            local dynamicPct = 0.88 + (0.11 * math.min(elapsed / 4, 1))
            setProgress(dynamicPct)
        end

        setProgress(1.0, "Ready!")
        task.wait(0.12)

        TweenService:Create(card, TweenInfo.new(0.35, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
            Position = UDim2.new(0.5, -220, 0.5, -78),
            BackgroundTransparency = 1
        }):Play()
        TweenService:Create(title, TweenInfo.new(0.25), { TextTransparency = 1 }):Play()
        TweenService:Create(subtitle, TweenInfo.new(0.25), { TextTransparency = 1 }):Play()
        TweenService:Create(statusLbl, TweenInfo.new(0.25), { TextTransparency = 1 }):Play()
        TweenService:Create(pctLbl, TweenInfo.new(0.25), { TextTransparency = 1 }):Play()
        TweenService:Create(badgeFrame, TweenInfo.new(0.25), { BackgroundTransparency = 1 }):Play()
        TweenService:Create(badgeLbl, TweenInfo.new(0.25), { TextTransparency = 1 }):Play()
        TweenService:Create(verLbl, TweenInfo.new(0.25), { TextTransparency = 1 }):Play()
        TweenService:Create(barBg, TweenInfo.new(0.25), { BackgroundTransparency = 1 }):Play()
        TweenService:Create(barFill, TweenInfo.new(0.25), { BackgroundTransparency = 1 }):Play()
        TweenService:Create(bgOverlay, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { BackgroundTransparency = 1 }):Play()
        TweenService:Create(blur, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), { Size = 0 }):Play()

        task.wait(0.45)
        pcall(function() blur:Destroy() end)
        pcall(function() sg:Destroy() end)
        if onFinish then onFinish() end
    end)
end

local Window = redzlib:MakeWindow({
    Title = "BTR CLIENT",
    SubTitle = "Brookhaven Edition",
    SaveFolder = "BTR Client.lua"
})

_G.BTR_WALLPAPERS = {
    ["Default"] = "rbxassetid://11424961420",
    ["Wallpaper 1"] = "rbxassetid://8942361465",
    ["Wallpaper 2"] = "rbxassetid://8652665146",
    ["Wallpaper 3"] = "rbxassetid://10552144814",
    ["Wallpaper 4"] = "rbxassetid://6755432181",
    ["Custom ID"] = "CUSTOM",
    ["None"] = ""
}
_G.BTR_SELECTED_WALLPAPER = _G.BTR_SELECTED_WALLPAPER or "Wallpaper 1"
_G.BTR_CUSTOM_WALLPAPER_ID = _G.BTR_CUSTOM_WALLPAPER_ID or ""
_G.BTR_HUB_SCALE = _G.BTR_HUB_SCALE or 100
_G.BTR_MENU_OPACITY = _G.BTR_MENU_OPACITY or 100
_G.BTR_CATEGORY_OPACITY = _G.BTR_CATEGORY_OPACITY or 100
_G.BTR_CATEGORY_COLOR = _G.BTR_CATEGORY_COLOR or "White"
_G.BTR_TEXT_COLOR = _G.BTR_TEXT_COLOR or "Default"
_G.BTR_TITLE_FONT = _G.BTR_TITLE_FONT or "GothamBlack"
_G.BTR_TITLE_STYLE = _G.BTR_TITLE_STYLE or "Cyberpunk (Pink & Cyan)"

_G.BTR_CATEGORY_COLORS = {
    ["AMOLED Black (Pure)"] = Color3.fromRGB(8, 8, 10),
    ["Midnight Obsidian"] = Color3.fromRGB(14, 14, 18),
    ["Deep Charcoal"] = Color3.fromRGB(22, 22, 26),
    ["Stealth Slate"] = Color3.fromRGB(32, 34, 40),
    ["Dark Classic"] = Color3.fromRGB(26, 26, 30),
    ["Blood Dark Crimson"] = Color3.fromRGB(38, 10, 14),
    ["Abyss Dark Navy"] = Color3.fromRGB(12, 18, 30),
    ["Royal Dark Violet"] = Color3.fromRGB(26, 12, 38),
    ["Emerald Dark Forest"] = Color3.fromRGB(10, 30, 18),
    ["Cyber Dark Smoke"] = Color3.fromRGB(18, 28, 32),
    ["Matrix Dark Olive"] = Color3.fromRGB(18, 28, 14),
    ["Dark Copper Brown"] = Color3.fromRGB(34, 20, 14),
    ["White"] = Color3.fromRGB(245, 245, 250),
    ["Neon Cyan"] = Color3.fromRGB(0, 220, 255),
    ["Electric Purple"] = Color3.fromRGB(170, 60, 255),
    ["Hot Pink"] = Color3.fromRGB(255, 40, 140),
    ["Lime Green"] = Color3.fromRGB(40, 230, 80),
    ["Sunset Orange"] = Color3.fromRGB(255, 110, 20),
    ["Golden Yellow"] = Color3.fromRGB(255, 210, 30),
    ["Crimson Red"] = Color3.fromRGB(255, 35, 45),
    ["Rainbow"] = "Rainbow"
}

_G.BTR_TEXT_COLORS = {
    ["Default"] = Color3.fromRGB(240, 240, 245),
    ["Pure White"] = Color3.fromRGB(255, 255, 255),
    ["Silver Slate"] = Color3.fromRGB(195, 200, 210),
    ["Muted Smoke"] = Color3.fromRGB(155, 160, 175),
    ["Stealth Charcoal"] = Color3.fromRGB(110, 115, 125),
    ["Ice Mint"] = Color3.fromRGB(160, 245, 225),
    ["Cyber Cyan"] = Color3.fromRGB(0, 230, 255),
    ["Electric Purple"] = Color3.fromRGB(190, 90, 255),
    ["Hot Pink"] = Color3.fromRGB(255, 50, 160),
    ["Lime Green"] = Color3.fromRGB(60, 255, 100),
    ["Toxic Lime"] = Color3.fromRGB(130, 255, 50),
    ["Sunset Orange"] = Color3.fromRGB(255, 130, 30),
    ["Golden Amber"] = Color3.fromRGB(255, 200, 40),
    ["Crimson Blood"] = Color3.fromRGB(255, 50, 60),
    ["Rainbow"] = "Rainbow"
}

_G.BTR_TITLE_FONTS = {
    ["GothamBlack"] = Enum.Font.GothamBlack,
    ["FredokaOne"] = Enum.Font.FredokaOne,
    ["Arcade"] = Enum.Font.Arcade,
    ["Creepster"] = Enum.Font.Creepster,
    ["PermanentMarker"] = Enum.Font.PermanentMarker,
    ["BuilderSansBold"] = Enum.Font.BuilderSansBold,
    ["SpecialElite"] = Enum.Font.SpecialElite,
    ["SciFi"] = Enum.Font.SciFi
}

_G.BTR_TITLE_STYLES = {
    ["Cyberpunk (Pink & Cyan)"] = {
        format = '<font color="#FF1493"><b>BTR</b></font> <font color="#00FFFF"><b>CLIENT</b></font>',
        sub = '<font color="#A0A0B8">by</font> <font color="#00FFFF"><b>Ash</b></font>'
    },
    ["Stealth Obsidian (Dark Gray & Neon Cyan)"] = {
        format = '<font color="#6B7280"><b>BTR</b></font> <font color="#00F0FF"><b>CLIENT</b></font>',
        sub = '<font color="#9CA3AF">by</font> <font color="#00F0FF"><b>Ash</b></font>'
    },
    ["Midnight Blood (Dark Charcoal & Crimson)"] = {
        format = '<font color="#4B5563"><b>BTR</b></font> <font color="#EF4444"><b>CLIENT</b></font>',
        sub = '<font color="#9CA3AF">by</font> <font color="#EF4444"><b>Ash</b></font>'
    },
    ["Abyss Purple (Dark Smoke & Violet)"] = {
        format = '<font color="#4B5563"><b>BTR</b></font> <font color="#A855F7"><b>CLIENT</b></font>',
        sub = '<font color="#9CA3AF">by</font> <font color="#A855F7"><b>Ash</b></font>'
    },
    ["Phantom Emerald (Dark Carbon & Toxic Green)"] = {
        format = '<font color="#4B5563"><b>BTR</b></font> <font color="#10B981"><b>CLIENT</b></font>',
        sub = '<font color="#9CA3AF">by</font> <font color="#10B981"><b>Ash</b></font>'
    },
    ["Pure Stealth (Charcoal & Ghost White)"] = {
        format = '<font color="#475569"><b>BTR</b></font> <font color="#F8FAFC"><b>CLIENT</b></font>',
        sub = '<font color="#94A3B8">by</font> <font color="#F8FAFC"><b>Ash</b></font>'
    },
    ["Dark Gold Luxury (Dark Bronze & Bright Gold)"] = {
        format = '<font color="#5C4A28"><b>BTR</b></font> <font color="#FFD700"><b>CLIENT</b></font>',
        sub = '<font color="#9CA3AF">by</font> <font color="#FFD700"><b>Ash</b></font>'
    },
    ["Blood Samurai (Crimson & White)"] = {
        format = '<font color="#FF2233"><b>BTR</b></font> <font color="#FFFFFF"><b>CLIENT</b></font>',
        sub = '<font color="#FF6666">by</font> <font color="#FF2233"><b>Ash</b></font>'
    },
    ["Golden Dragon (Gold & Amber)"] = {
        format = '<font color="#FFD700"><b>BTR</b></font> <font color="#FFA500"><b>CLIENT</b></font>',
        sub = '<font color="#FFE4B5">by</font> <font color="#FFD700"><b>Ash</b></font>'
    },
    ["Venom Toxic (Neon Lime & Emerald)"] = {
        format = '<font color="#00FF66"><b>BTR</b></font> <font color="#32CD32"><b>CLIENT</b></font>',
        sub = '<font color="#7CFC00">by</font> <font color="#00FF66"><b>Ash</b></font>'
    },
    ["Shadow Void (Purple & Magenta)"] = {
        format = '<font color="#9933FF"><b>BTR</b></font> <font color="#FF00CC"><b>CLIENT</b></font>',
        sub = '<font color="#BA55D3">by</font> <font color="#DA70D6"><b>Ash</b></font>'
    },
    ["Glacier Ice (Diamond & Pure Cyan)"] = {
        format = '<font color="#E0FFFF"><b>BTR</b></font> <font color="#00BFFF"><b>CLIENT</b></font>',
        sub = '<font color="#AFEEEE">by</font> <font color="#00FFFF"><b>Ash</b></font>'
    },
    ["Monochrome (Pure White & Silver)"] = {
        format = '<font color="#FFFFFF"><b>BTR</b></font> <font color="#DCDCDC"><b>CLIENT</b></font>',
        sub = '<font color="#A0A0A0">by</font> <font color="#FFFFFF"><b>Ash</b></font>'
    },
    ["Rainbow Chroma (Animated Wave)"] = "Rainbow"
}

_G.BTR_GetHubFrame = function()
    local candidates = {}
    pcall(function() table.insert(candidates, game:GetService("CoreGui")) end)
    pcall(function() if gethui then table.insert(candidates, gethui()) end end)
    pcall(function()
        local pGui = game:GetService("Players").LocalPlayer:FindFirstChild("PlayerGui")
        if pGui then table.insert(candidates, pGui) end
    end)

    for _, container in ipairs(candidates) do
        local redzGui = container:FindFirstChild("redz Library V5")
        if redzGui then
            local hub = redzGui:FindFirstChild("Hub")
            if hub and hub:IsA("GuiObject") then
                return hub
            end
        end
        local hub = container:FindFirstChild("Hub", true)
        if hub and hub:IsA("GuiObject") then
            return hub
        end
    end
    return nil
end

_G.BTR_SetHubScale = function(scalePercent)
    _G.BTR_HUB_SCALE = math.clamp(tonumber(scalePercent) or 100, 50, 200)
    local scaleFactor = _G.BTR_HUB_SCALE / 100
    local hub = _G.BTR_GetHubFrame()
    if hub then
        local scaleObj = hub:FindFirstChild("BTR_Hub_Scale")
        if not scaleObj then
            scaleObj = Instance.new("UIScale")
            scaleObj.Name = "BTR_Hub_Scale"
            scaleObj.Scale = scaleFactor
            scaleObj.Parent = hub
        else
            local ts = game:GetService("TweenService")
            ts:Create(scaleObj, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                Scale = scaleFactor
            }):Play()
        end

        local screenGui = hub:FindFirstAncestorOfClass("ScreenGui")
        if screenGui then
            local dropHolder = screenGui:FindFirstChild("Dropdown")
            if dropHolder then
                for _, child in ipairs(dropHolder:GetChildren()) do
                    if child:IsA("GuiObject") then
                        local dScale = child:FindFirstChild("BTR_Hub_Scale")
                        if not dScale then
                            dScale = Instance.new("UIScale")
                            dScale.Name = "BTR_Hub_Scale"
                            dScale.Scale = scaleFactor
                            dScale.Parent = child
                        else
                            local ts = game:GetService("TweenService")
                            ts:Create(dScale, TweenInfo.new(0.25, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
                                Scale = scaleFactor
                            }):Play()
                        end
                    end
                end
            end
        end
    end
end

_G.BTR_SetWallpaper = function(name)
    _G.BTR_SELECTED_WALLPAPER = name
    local tex = _G.BTR_WALLPAPERS[name]
    if name == "Custom ID" or tex == "CUSTOM" then
        local cleanId = tostring(_G.BTR_CUSTOM_WALLPAPER_ID or ""):gsub("%D", "")
        if cleanId:len() > 3 then
            
            tex = "rbxthumb://type=Asset&id=" .. cleanId .. "&w=768&h=432"
        else
            tex = _G.BTR_WALLPAPERS["Default"]
        end
    elseif tex == nil then
        tex = _G.BTR_WALLPAPERS["Default"]
    end

    local hub = _G.BTR_GetHubFrame()
    if hub then
        local bgImg = hub:FindFirstChild("BTR_Background_Image")
        if not bgImg then
            bgImg = Instance.new("ImageLabel")
            bgImg.Name = "BTR_Background_Image"
            bgImg.Size = UDim2.new(1, 0, 1, 0)
            bgImg.Position = UDim2.new(0, 0, 0, 0)
            bgImg.BackgroundTransparency = 1
            bgImg.ScaleType = Enum.ScaleType.Crop
            bgImg.ZIndex = 0

            local corner = Instance.new("UICorner")
            corner.CornerRadius = UDim.new(0, 10)
            corner.Parent = bgImg

            bgImg.Parent = hub
        end

        local menuTrans = 1 - ((_G.BTR_MENU_OPACITY or 100) / 100)

        if tex == "" or name == "None" then
            bgImg.Visible = false
            hub.BackgroundTransparency = menuTrans
        else
            bgImg.Visible = true
            bgImg.Image = tex
            bgImg.ImageTransparency = menuTrans
            hub.BackgroundTransparency = 1
        end

        if _G.BTR_ApplyMenuCustoms then
            _G.BTR_ApplyMenuCustoms()
        end
    end
end

_G.BTR_SetHubIcon = function(name)
    _G.BTR_SELECTED_ICON = name or "Default"
    local tex = _G.BTR_ICONS and _G.BTR_ICONS[_G.BTR_SELECTED_ICON]
    if _G.BTR_SELECTED_ICON == "Custom ID" or tex == "CUSTOM" then
        local cleanId = tostring(_G.BTR_CUSTOM_ICON_ID or ""):gsub("%D", "")
        if cleanId:len() > 3 then
            tex = "rbxthumb://type=Asset&id=" .. cleanId .. "&w=420&h=420"
        else
            tex = _G.BTR_ICONS["Default"]
        end
    elseif not tex or tex == "" then
        tex = _G.BTR_ICONS["Default"]
    end

    
    if _G.BTR_FloatingButton then
        pcall(function()
            _G.BTR_FloatingButton.Image = tex
        end)
    end

    
    pcall(function()
        local hub = _G.BTR_GetHubFrame()
        if hub then
            local topBar = hub:FindFirstChild("TopBar") or hub:FindFirstChild("Top Bar") or hub:FindFirstChild("Header")
            if topBar then
                for _, descendant in ipairs(topBar:GetDescendants()) do
                    if descendant:IsA("ImageLabel") and not descendant:FindFirstAncestor("Buttons") then
                        descendant.Image = tex
                    end
                end
            end
        end
    end)
end

local rainbowCatRunning = false
local rainbowTextRunning = false
local rainbowHeaderRunning = false

_G.BTR_ApplyNoAnimation = function()
    local hub = _G.BTR_GetHubFrame()
    if not hub then return end
    local containers = hub:FindFirstChild("Containers", true)
    if not containers then return end

    local function hookContainer(cont)
        if cont:IsA("ScrollingFrame") and not cont:FindFirstChild("BTR_NoAnimHook") then
            local tag = Instance.new("BoolValue")
            tag.Name = "BTR_NoAnimHook"
            tag.Parent = cont

            cont:GetPropertyChangedSignal("Size"):Connect(function()
                if _G.BTR_NO_ANIMATION and cont.Size ~= UDim2.new(1, 0, 1, 0) then
                    cont.Size = UDim2.new(1, 0, 1, 0)
                end
            end)
        end
    end

    for _, c in ipairs(containers:GetChildren()) do
        hookContainer(c)
        if _G.BTR_NO_ANIMATION and c:IsA("ScrollingFrame") then
            c.Size = UDim2.new(1, 0, 1, 0)
        end
    end

    if not containers:FindFirstChild("BTR_ChildAddedHook") then
        local tag = Instance.new("BoolValue")
        tag.Name = "BTR_ChildAddedHook"
        tag.Parent = containers

        containers.ChildAdded:Connect(function(child)
            hookContainer(child)
            if _G.BTR_NO_ANIMATION and child:IsA("ScrollingFrame") then
                child.Size = UDim2.new(1, 0, 1, 0)
            end
        end)
    end
end

_G.BTR_ApplyMenuCustoms = function()
    pcall(function()
        local hub = _G.BTR_GetHubFrame()
        if not hub then return end

        local menuOpacityVal = math.clamp(_G.BTR_MENU_OPACITY or 100, 5, 100)
        local menuTrans = 1 - (menuOpacityVal / 100)

        
        local bgImg = hub:FindFirstChild("BTR_Background_Image")
        if bgImg and bgImg.Visible and bgImg.Image ~= "" then
            bgImg.ImageTransparency = menuTrans
            hub.BackgroundTransparency = 1
        else
            hub.BackgroundTransparency = menuTrans
        end

        
        if _G.BTR_FloatingButton and _G.BTR_FloatingButton.Parent then
            local fStroke = _G.BTR_FloatingStroke or _G.BTR_FloatingButton:FindFirstChildOfClass("UIStroke")
            local catColNameCurrent = _G.BTR_CATEGORY_COLOR or "White"
            local catColCurrent = _G.BTR_CATEGORY_COLORS[catColNameCurrent] or Color3.fromRGB(240, 240, 245)
            if catColNameCurrent ~= "Rainbow" then
                local lum = (catColCurrent.R * 0.299 + catColCurrent.G * 0.587 + catColCurrent.B * 0.114)
                local glowColor = (catColNameCurrent == "White" or lum < 0.25) and Color3.fromRGB(0, 220, 255) or catColCurrent
                if fStroke then fStroke.Color = glowColor end
            end
        end

        
        local catOpacityVal = math.clamp(_G.BTR_CATEGORY_OPACITY or 100, 0, 100)
        local catTrans = 1 - (catOpacityVal / 100)
        local catColName = _G.BTR_CATEGORY_COLOR or "White"
        local catCol = _G.BTR_CATEGORY_COLORS[catColName] or Color3.fromRGB(240, 240, 245)

        local tabScroll = hub:FindFirstChild("Tab Scroll", true)
        if tabScroll then
            for _, child in ipairs(tabScroll:GetChildren()) do
                if child:IsA("TextButton") or child:IsA("GuiButton") then
                    
                    child.BackgroundTransparency = catTrans
                    if catColName ~= "Rainbow" then
                        rainbowCatRunning = false
                        if catColName == "White" then
                            
                            child.BackgroundColor3 = Color3.fromRGB(245, 245, 250)
                        else
                            child.BackgroundColor3 = catCol
                        end
                    end

                    
                    for _, sub in ipairs(child:GetChildren()) do
                        if sub:IsA("TextLabel") then
                            sub.TextTransparency = catTrans * 0.4
                            if catColName == "White" then
                                
                                sub.TextColor3 = Color3.fromRGB(18, 18, 22)
                                sub.TextStrokeTransparency = 1
                            else
                                sub.TextColor3 = Color3.fromRGB(255, 255, 255)
                                sub.TextStrokeTransparency = 0.5
                                sub.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                            end
                        elseif sub:IsA("ImageLabel") then
                            sub.ImageTransparency = catTrans * 0.4
                            if catColName == "White" then
                                
                                sub.ImageColor3 = Color3.fromRGB(18, 18, 22)
                            else
                                sub.ImageColor3 = Color3.fromRGB(255, 255, 255)
                            end
                        elseif sub:IsA("Frame") then
                            
                            sub.BackgroundTransparency = 1
                            sub.Visible = false
                        end
                    end
                end
            end
        end

        
        if catColName == "Rainbow" then
            if not rainbowCatRunning then
                rainbowCatRunning = true
                task.spawn(function()
                    while _G.BTR_CATEGORY_COLOR == "Rainbow" and hub and hub.Parent do
                        local tScroll = hub:FindFirstChild("Tab Scroll", true)
                        if tScroll then
                            local catOp = math.clamp(_G.BTR_CATEGORY_OPACITY or 100, 0, 100)
                            local cTrans = 1 - (catOp / 100)
                            local idx = 0
                            for _, child in ipairs(tScroll:GetChildren()) do
                                if child:IsA("TextButton") or child:IsA("GuiButton") then
                                    idx = idx + 1
                                    local waveCol = Color3.fromHSV((tick() * 0.35 + idx * 0.08) % 1, 0.85, 1)
                                    child.BackgroundColor3 = waveCol
                                    child.BackgroundTransparency = cTrans
                                    for _, sub in ipairs(child:GetChildren()) do
                                        if sub:IsA("TextLabel") then
                                            sub.TextColor3 = Color3.fromRGB(255, 255, 255)
                                            sub.TextStrokeTransparency = 0.4
                                            sub.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
                                            sub.TextTransparency = cTrans * 0.4
                                        elseif sub:IsA("ImageLabel") then
                                            sub.ImageColor3 = Color3.fromRGB(255, 255, 255)
                                            sub.ImageTransparency = cTrans * 0.4
                                        elseif sub:IsA("Frame") then
                                            sub.BackgroundColor3 = waveCol
                                            sub.BackgroundTransparency = cTrans
                                        end
                                    end
                                end
                            end
                        end
                        if _G.BTR_FloatingButton and _G.BTR_FloatingButton.Parent then
                            local fStroke = _G.BTR_FloatingStroke or _G.BTR_FloatingButton:FindFirstChildOfClass("UIStroke")
                            local rCol = Color3.fromHSV((tick() * 0.25) % 1, 0.85, 1)
                            if fStroke then fStroke.Color = rCol end
                        end
                        task.wait(0.06)
                    end
                    rainbowCatRunning = false
                end)
            end
        end
    end)
end

_G.BTR_ApplyTextColors = function()
    pcall(function()
        local hub = _G.BTR_GetHubFrame()
        if not hub then return end

        local choice = _G.BTR_TEXT_COLOR or "Default"
        local col = _G.BTR_TEXT_COLORS[choice] or Color3.fromRGB(240, 240, 245)

        local function applyToCont(cont)
            if not cont then return end
            for _, v in ipairs(cont:GetDescendants()) do
                if v:IsA("TextLabel") and v.Name ~= "Title" and v.Name ~= "SubTitle" then
                    if choice == "Default" then
                        v.TextColor3 = Color3.fromRGB(240, 240, 245)
                    else
                        v.TextColor3 = col
                    end
                end
            end
        end

        if choice ~= "Rainbow" then
            rainbowTextRunning = false
            local containers = hub:FindFirstChild("Containers", true)
            if containers then applyToCont(containers) end

            local rz = _G.BTR_REDZ_LIB or redzlib
            if rz and rz.Tabs then
                for _, tabData in ipairs(rz.Tabs) do
                    if tabData.Cont then applyToCont(tabData.Cont) end
                end
            end
        else
            if not rainbowTextRunning then
                rainbowTextRunning = true
                task.spawn(function()
                    while _G.BTR_TEXT_COLOR == "Rainbow" and rainbowTextRunning and hub and hub.Parent do
                        local cList = hub:FindFirstChild("Containers", true)
                        if cList then
                            local i = 0
                            for _, v in ipairs(cList:GetDescendants()) do
                                if v:IsA("TextLabel") and v.Name ~= "Title" and v.Name ~= "SubTitle" then
                                    i = i + 1
                                    v.TextColor3 = Color3.fromHSV((tick() * 0.3 + i * 0.03) % 1, 0.85, 1)
                                end
                            end
                        end
                        task.wait(0.08)
                    end
                    rainbowTextRunning = false
                    if _G.BTR_ApplyTextColors then _G.BTR_ApplyTextColors() end
                end)
            end
        end
    end)
end

_G.BTR_ApplyHeaderStyle = function()
    pcall(function()
        local hub = _G.BTR_GetHubFrame()
        if not hub then return end

        local fontName = _G.BTR_TITLE_FONT or "GothamBlack"
        local chosenFont = _G.BTR_TITLE_FONTS[fontName] or Enum.Font.GothamBlack

        local styleName = _G.BTR_TITLE_STYLE or "Cyberpunk (Pink & Cyan)"
        local styleData = _G.BTR_TITLE_STYLES[styleName]

        
        local topBar = hub:FindFirstChild("Top Bar", true) or hub:FindFirstChild("TopBar", true) or hub:FindFirstChild("Header", true)
        local titleLabel = nil
        local subTitleLabel = nil

        if topBar then
            titleLabel = topBar:FindFirstChild("Title") or topBar:FindFirstChildWhichIsA("TextLabel")
            subTitleLabel = (titleLabel and titleLabel:FindFirstChild("SubTitle")) or topBar:FindFirstChild("SubTitle", true)
        else
            for _, desc in ipairs(hub:GetDescendants()) do
                if desc:IsA("TextLabel") and not desc:FindFirstAncestor("Containers") and not desc:FindFirstAncestor("MainScroll") then
                    if desc.Name == "Title" then
                        titleLabel = desc
                    elseif desc.Name == "SubTitle" then
                        subTitleLabel = desc
                    end
                end
            end
        end

        if titleLabel then
            titleLabel.RichText = true
            titleLabel.Font = chosenFont
            titleLabel.TextSize = 20
            titleLabel.TextStrokeTransparency = 0.6
            titleLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
            if type(styleData) == "table" and styleData.format then
                titleLabel.Text = styleData.format
            end
        end

        if subTitleLabel then
            subTitleLabel.RichText = true
            subTitleLabel.Font = chosenFont
            subTitleLabel.TextSize = 13
            if type(styleData) == "table" and styleData.sub then
                subTitleLabel.Text = styleData.sub
            end
        end

        if styleName == "Rainbow Chroma (Animated Wave)" then
            if not rainbowHeaderRunning then
                rainbowHeaderRunning = true
                task.spawn(function()
                    while _G.BTR_TITLE_STYLE == "Rainbow Chroma (Animated Wave)" and hub and hub.Parent do
                        local t1 = Color3.fromHSV(tick() * 0.4 % 1, 0.9, 1):ToHex()
                        local t2 = Color3.fromHSV((tick() * 0.4 + 0.3) % 1, 0.9, 1):ToHex()
                        if titleLabel and titleLabel.Parent then
                            titleLabel.RichText = true
                            titleLabel.Text = '<font color="#' .. t1 .. '"><b>BTR</b></font> <font color="#' .. t2 .. '"><b>CLIENT</b></font>'
                        end
                        if subTitleLabel and subTitleLabel.Parent then
                            subTitleLabel.RichText = true
                            subTitleLabel.Text = '<font color="#' .. t2 .. '">by</font> <font color="#' .. t1 .. '"><b>Ash</b></font>'
                        end
                        task.wait(0.06)
                    end
                    rainbowHeaderRunning = false
                end)
            end
        end
    end)
end

pcall(function()
    task.spawn(function()
        local hubFrame = nil
        for attempt = 1, 60 do
            hubFrame = _G.BTR_GetHubFrame()
            if hubFrame then break end
            task.wait(0.15)
        end

        if hubFrame then
            hubFrame.Visible = false

            
            _G.BTR_SetHubScale(_G.BTR_HUB_SCALE or 100)

            
            _G.BTR_SetWallpaper(_G.BTR_SELECTED_WALLPAPER or "Wallpaper 1")

            
            _G.BTR_ApplyMenuCustoms()

            
            _G.BTR_ApplyTextColors()

            
            _G.BTR_ApplyHeaderStyle()

            
            if _G.BTR_SetHubIcon then
                _G.BTR_SetHubIcon(_G.BTR_SELECTED_ICON or "Default")
            end

            startBTRLoader(function()
                if hubFrame then
                    hubFrame.Visible = true
                end
                if _G.BTR_FloatingButton then
                    _G.BTR_FloatingButton.Visible = (_G.BTR_SHOW_FLOATING_BUTTON ~= false)
                end
            end)
        end
    end)
end)

pcall(function()
    local initialIcon = _G.BTR_ICONS and _G.BTR_ICONS[_G.BTR_SELECTED_ICON or "Default"] or "rbxassetid://122646861461158"
    if (_G.BTR_SELECTED_ICON == "Custom ID" or initialIcon == "CUSTOM") and _G.BTR_CUSTOM_ICON_ID and tostring(_G.BTR_CUSTOM_ICON_ID):len() > 3 then
        initialIcon = "rbxthumb://type=Asset&id=" .. tostring(_G.BTR_CUSTOM_ICON_ID):gsub("%D", "") .. "&w=420&h=420"
    end

    local minimizeObj = Window:AddMinimizeButton({
        Button = {
            Image = initialIcon,
            BackgroundTransparency = 1,
            Visible = false 
        },
        Corner = {
            CornerRadius = UDim.new(0, 4)
        },
        Stroke = {
            Color = Color3.fromRGB(0, 220, 255),
            Transparency = 0.2,
            Thickness = 1.2
        }
    })

    if minimizeObj and minimizeObj.Button then
        local btn = minimizeObj.Button
        btn.Size = UDim2.fromOffset(35, 35)
        btn.Position = UDim2.new(0, 20, 0, 20) 
        btn.Name = "BTR_Floating_Toggle"
        btn.Visible = false

        
        local function toggleHub()
            local hub = _G.BTR_GetHubFrame()
            if hub then
                hub.Visible = not hub.Visible
            elseif Window and Window.Minimize then
                pcall(function() Window:Minimize() end)
            end
        end

        
        btn.Activated:Connect(toggleHub)
        btn.MouseButton1Click:Connect(toggleHub)

        _G.BTR_FloatingButton = btn
        _G.BTR_FloatingStroke = minimizeObj.Stroke
    end

    
    local uis = game:GetService("UserInputService")
    uis.InputBegan:Connect(function(input, gpe)
        if not gpe and (input.KeyCode == Enum.KeyCode.RightControl) then
            local hub = _G.BTR_GetHubFrame()
            if hub then
                hub.Visible = not hub.Visible
            elseif Window and Window.Minimize then
                pcall(function() Window:Minimize() end)
            end
        end
    end)
end)

local Translations = {
    
    ["Info"] = "Info",
    ["Player"] = "Player",
    ["Avatar"] = "Avatar",
    ["Profile"] = "Profile",
    ["Troll"] = "Troll",
    ["Scripts"] = "Morph FE",
    ["Build"] = "Build",
    ["Premium"] = "Premium",
    ["Teleport"] = "Teleport",
    ["Protection"] = "Protection",
    ["Speed"] = "Speed",
    ["JumpPower"] = "Jump Power",
    ["Gravity"] = "Gravity",
    ["Names"] = "Names",
    ["Music"] = "Music",
    ["ESP"] = "ESP",
    ["House"] = "House",
    ["Vehicle"] = "Vehicle",
    ["Graphics"] = "Graphics",
    ["Teleports"] = "Teleports",
    ["Animation"] = "Animations",
    ["Change Fov"] = "FOV Settings",
    ["Copy Character"] = "Copy Character",
    ["Character Clothing"] = "Character Clothing",
    ["Character Editor"] = "Character Editor",
    ["Advanced Character Editor"] = "Advanced Character Editor",
    ["Load Character Parts"] = "Load Character Parts",
    ["Avatar Loader"] = "Avatar Loader",
    ["Select Player"] = "Select Player",
    ["Troll Child"] = "Send Child",
    ["ESP Players"] = "ESP Players",
    ["Troll Options"] = "Troll Options",
    ["Vehicle Speed Unlocker"] = "Vehicle Speed Unlocker",
    ["Limited Items"] = "Limited Items",
    ["Limited Items 2"] = "Limited Items 2",
}

local InfoTab = Window:MakeTab({
    Translations["Info"],
    "rbxassetid://10723415903"
})
local PlayerTab = Window:MakeTab({
    Translations["Player"],
    "rbxassetid://10747373176"
})
local ProfileTab = Window:MakeTab({
    Translations["Profile"],
    "rbxassetid://10734919691"
})
local AvatarTab = Window:MakeTab({
    "Avatar",
    "rbxassetid://10747373176"
})
local TrollTab = Window:MakeTab({
    Translations["Troll"],
    "rbxassetid://10723396107"
})

local TrollV2Tab = Window:MakeTab({
    Translations["Troll"] .. " V2",
    "rbxassetid://10723396107"
})
local MorphFETab = Window:MakeTab({
    Translations["Scripts"],
    "rbxassetid://10734940107"
})
local BuildTab = Window:MakeTab({
    Translations["Build"],
    "rbxassetid://10723405360"
})
local BuildV2Tab = Window:MakeTab({
    Translations["Build"] .. " V2",
    "rbxassetid://10723405360"
})
local PremiumTab = Window:MakeTab({
    "VIP",
    "rbxassetid://10709811110"
})
local HouseTab = Window:MakeTab({
    "House",
    "rbxassetid://107703494735835"
})
local VehicleTab = Window:MakeTab({
    "Vehicle",
    "rbxassetid://10709775894"
})
local AudioTab = Window:MakeTab({
    "Audio",
    "rbxassetid://6031280882"
})

local VisualTab = Window:MakeTab({
    "Visual",
    "rbxassetid://10723415903"
})

local AdminTab = Window:MakeTab({
    "Admin",
    "rbxassetid://10747372167"
})
local ProtectionTab = Window:MakeTab({
    Translations["Protection"],
    "rbxassetid://10734951847"
})
local SettingsTab = Window:MakeTab({
    "Settings",
    "rbxassetid://10734950020"
})

local _infoLP = game:GetService("Players").LocalPlayer

local OwnerTab = nil
if _infoLP and _infoLP.UserId == 10605215828 then
    OwnerTab = Window:MakeTab({
        "Owner",
        "rbxassetid://10723415766"
    })
end
local _executorName = (type(identifyexecutor) == "function" and identifyexecutor()) or "Standard Executor"

InfoTab:AddSection({
    "> Client Info"
})

InfoTab:AddParagraph({
    "BTR Client Information",
    "- Version : 3.5.0\n- Status : Undetected / Safe\n- Environment : " .. tostring(_executorName) .. "\n- Target : Brookhaven RP\n- Edition : Official"
})

InfoTab:AddSection({
    "> User & Session"
})

InfoTab:AddParagraph({
    "User Profile",
    "- Username : " .. tostring(_infoLP.Name) .. "\n- Display Name : " .. tostring(_infoLP.DisplayName) .. "\n- User ID : " .. tostring(_infoLP.UserId) .. "\n- Account Age : " .. tostring(_infoLP.AccountAge) .. " days\n- Membership : " .. tostring(_infoLP.MembershipType):gsub("Enum.MembershipType.", "")
})

InfoTab:AddSection({
    "> Live Performance"
})

local _perfParagraph = InfoTab:AddParagraph({
    "Live Stats",
    "- FPS : Calculating...\n- Ping : Calculating...\n- Session Time : 0s"
})

task.spawn(function()
    local Stats = game:GetService("Stats")
    local sessionStart = tick()
    local thisSession = _G.BTR_SESSION_ID
    while thisSession == _G.BTR_SESSION_ID do
        task.wait(2)
        pcall(function()
            if not _perfParagraph then return end
            local fps = 60
            local ping = 0
            local pingStat = Stats:FindFirstChild("PerformanceStats") and Stats.PerformanceStats:FindFirstChild("Ping")
            if pingStat then
                ping = math.floor(pingStat:GetValue())
            else
                local netStat = Stats:FindFirstChild("Network") and Stats.Network:FindFirstChild("ServerStatsItem") and Stats.Network.ServerStatsItem:FindFirstChild("Data Ping")
                if netStat then
                    ping = math.floor(netStat:GetValue())
                end
            end

            local uptime = math.floor(tick() - sessionStart)
            local hours = math.floor(uptime / 3600)
            local mins = math.floor((uptime % 3600) / 60)
            local secs = uptime % 60
            local timeStr = string.format("%02dh %02dm %02ds", hours, mins, secs)

            local statusText = string.format("- Ping : %d ms\n- Session Time : %s", ping, timeStr)
            if _perfParagraph.Set then
                _perfParagraph:Set("Live Stats", statusText)
            elseif _perfParagraph.SetDesc then
                _perfParagraph:SetDesc(statusText)
            end
        end)
    end
end)

-- Onglet 2 : Player -- [LOCKED: WORK PERFECTLY]
PlayerTab:AddSection({
    "> Movement"
})

local _speedConn = nil
local _customSpeed = 16
PlayerTab:AddSlider({
    Name = "Walk Speed",
    Description = "Safe max: 250 (rollback above ~300 in Brookhaven)",
    Min = 16,
    Max = 250,
    Default = 16,
    Suffix = " sp",
    Callback = function(value)
        _customSpeed = value
        if _speedConn then _speedConn:Disconnect(); _speedConn = nil end
        if value == 16 then
            return
        end
        local lp = game:GetService("Players").LocalPlayer
        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = value end
        _speedConn = game:GetService("RunService").Heartbeat:Connect(function()
            local h = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if h and _customSpeed > 16 and h.WalkSpeed ~= _customSpeed then
                h.WalkSpeed = _customSpeed
            end
        end)
    end
})

local _jumpConn = nil
local _customJump = 50
PlayerTab:AddSlider({
    Name = "Jump Power",
    Description = "Default: 50 - safe max: 250",
    Min = 50,
    Max = 250,
    Default = 50,
    Suffix = " jp",
    Callback = function(value)
        _customJump = value
        if _jumpConn then _jumpConn:Disconnect(); _jumpConn = nil end
        if value == 50 then
            return
        end
        local lp = game:GetService("Players").LocalPlayer
        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.UseJumpPower = true
            hum.JumpPower = value
        end
        _jumpConn = game:GetService("RunService").Heartbeat:Connect(function()
            local h = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if h and _customJump > 50 then
                h.UseJumpPower = true
                if h.JumpPower ~= _customJump then
                    h.JumpPower = _customJump
                end
            end
        end)
    end
})

local _infJumpConn = nil
PlayerTab:AddToggle({
    Name = "Infinite Jump",
    Description = "Jump in the air indefinitely",
    Default = false,
    Callback = function(value)
        if _infJumpConn then _infJumpConn:Disconnect(); _infJumpConn = nil end
        if value then
            _infJumpConn = game:GetService("UserInputService").JumpRequest:Connect(function()
                local lp = game:GetService("Players").LocalPlayer
                local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end
            end)
        end
    end
})

local _flyConn = nil
local _flyBG   = nil
local _flyBV   = nil
PlayerTab:AddToggle({
    Name = "Fly",
    Description = "Hold Space to go up, Shift to go down, WASD to move",
    Default = false,
    Callback = function(value)
        local lp  = game:GetService("Players").LocalPlayer
        local UIS = game:GetService("UserInputService")
        local RS  = game:GetService("RunService")
        if not value then
            if _flyConn then _flyConn:Disconnect(); _flyConn = nil end
            if _flyBG then _flyBG:Destroy(); _flyBG = nil end
            if _flyBV then _flyBV:Destroy(); _flyBV = nil end
            local char = lp.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum then hum.PlatformStand = false end
            if hrp then hrp.AssemblyLinearVelocity = Vector3.new(0,0,0) end
            return
        end
        local char = lp.Character
        local hrp  = char and char:FindFirstChild("HumanoidRootPart")
        local hum  = char and char:FindFirstChildOfClass("Humanoid")
        if not hrp or not hum then return end
        hum.PlatformStand = true

        _flyBG = Instance.new("BodyGyro", hrp)
        _flyBG.P = 9e4; _flyBG.MaxTorque = Vector3.new(9e9,9e9,9e9)
        _flyBG.CFrame = hrp.CFrame

        _flyBV = Instance.new("BodyVelocity", hrp)
        _flyBV.Velocity = Vector3.new(0,0,0)
        _flyBV.MaxForce = Vector3.new(9e9,9e9,9e9)

        _flyConn = RS.Heartbeat:Connect(function()
            local c = lp.Character
            local h = c and c:FindFirstChild("HumanoidRootPart")
            if not h or not _flyBV or not _flyBG then return end
            local cam = workspace.CurrentCamera
            local dir = Vector3.new(0,0,0)
            local spd = 60
            if UIS:IsKeyDown(Enum.KeyCode.W) then dir = dir + cam.CFrame.LookVector end
            if UIS:IsKeyDown(Enum.KeyCode.S) then dir = dir - cam.CFrame.LookVector end
            if UIS:IsKeyDown(Enum.KeyCode.A) then dir = dir - cam.CFrame.RightVector end
            if UIS:IsKeyDown(Enum.KeyCode.D) then dir = dir + cam.CFrame.RightVector end
            if UIS:IsKeyDown(Enum.KeyCode.Space) then dir = dir + Vector3.new(0,1,0) end
            if UIS:IsKeyDown(Enum.KeyCode.LeftShift) then dir = dir - Vector3.new(0,1,0) end
            _flyBV.Velocity = dir.Magnitude > 0 and dir.Unit * spd or Vector3.new(0,0,0)
            _flyBG.CFrame = cam.CFrame
        end)
    end
})

local _noClipConn = nil
local _noClipParts = {}
PlayerTab:AddToggle({
    Name = "NoClip",
    Description = "Walk through walls and objects",
    Default = false,
    Callback = function(value)
        if _noClipConn then
            _noClipConn:Disconnect()
            _noClipConn = nil
        end
        
        local lp = game:GetService("Players").LocalPlayer
        
        if not value then
            for part, _ in pairs(_noClipParts) do
                if part and part.Parent then
                    pcall(function() part.CanCollide = true end)
                end
            end
            _noClipParts = {}
            return
        end
        
        _noClipParts = {}
        _noClipConn = game:GetService("RunService").Stepped:Connect(function()
            local char = lp.Character
            if not char then return end
            for _, p in ipairs(char:GetDescendants()) do
                if p:IsA("BasePart") and p.CanCollide == true and p.Name ~= "HumanoidRootPart" and not p:IsA("Accessory") and p.Parent and not p.Parent:IsA("Accessory") then
                    p.CanCollide = false
                    _noClipParts[p] = true
                end
            end
        end)
    end
})

PlayerTab:AddSection({
    "> Character"
})

PlayerTab:AddButton({
    Name = "Reset Character",
    Description = "Kills and respawns your character instantly",
    Callback = function()
        local lp  = game:GetService("Players").LocalPlayer
        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = 0 end
    end
})

PlayerTab:AddSlider({
    Name = "Field of View",
    Description = "Default: 70 - cinematic max: 120",
    Min = 30,
    Max = 120,
    Default = 70,
    Suffix = " deg",
    Callback = function(value)
        workspace.CurrentCamera.FieldOfView = value
    end
})

do
PlayerTab:AddSection({
    "> Exploits"
})

local InvisActive       = false
local invisToggleRef    = nil
local invisClone        = nil
local invisRealChar     = nil
local invisDiedConn     = nil
local invisSteppedConn  = nil
local invisCamConn      = nil

local DesyncActive      = false
local desyncToggleRef   = nil
local desyncClone       = nil
local desyncHighlight   = nil
local desyncRealChar    = nil
local desyncDiedConn    = nil
local desyncFrozenCF    = nil
local desyncSteppedConn = nil
local desyncCamConn     = nil

local function stopInvisible()
    if invisDiedConn then
        invisDiedConn:Disconnect()
        invisDiedConn = nil
    end
    if invisSteppedConn then
        invisSteppedConn:Disconnect()
        invisSteppedConn = nil
    end
    if invisCamConn then
        invisCamConn:Disconnect()
        invisCamConn = nil
    end

    local lp = game:GetService("Players").LocalPlayer
    local returnCF = nil

    if invisClone and invisClone:FindFirstChild("HumanoidRootPart") then
        returnCF = invisClone.HumanoidRootPart.CFrame
    end

    local char = invisRealChar or lp.Character
    if char and char.Parent then
        char.Parent = workspace
        local root = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if root then
            root.Anchored = false
            if returnCF then
                root.CFrame = returnCF
            end
            root.AssemblyLinearVelocity = Vector3.zero
            root.AssemblyAngularVelocity = Vector3.zero
        end
        for _, p in ipairs(char:GetDescendants()) do
            if p:IsA("BasePart") and p.Name ~= "HumanoidRootPart" and not p:IsA("Accessory") and p.Parent and not p.Parent:IsA("Accessory") then
                p.CanCollide = true
            end
        end
        lp.Character = char
        local cam = workspace.CurrentCamera
        if cam and hum then
            cam.CameraType = Enum.CameraType.Custom
            cam.CameraSubject = hum
        end
        local anim = char:FindFirstChild("Animate")
        if anim then
            anim.Disabled = true
            task.wait(0.05)
            anim.Disabled = false
        end
    end

    if invisClone then
        pcall(function() invisClone:Destroy() end)
        invisClone = nil
    end

    invisRealChar = nil
    InvisActive = false
end

local function stopDesync()
    if desyncDiedConn then
        desyncDiedConn:Disconnect()
        desyncDiedConn = nil
    end
    if desyncSteppedConn then
        desyncSteppedConn:Disconnect()
        desyncSteppedConn = nil
    end
    if desyncCamConn then
        desyncCamConn:Disconnect()
        desyncCamConn = nil
    end

    local lp = game:GetService("Players").LocalPlayer
    local returnCF = desyncFrozenCF

    if desyncClone and desyncClone:FindFirstChild("HumanoidRootPart") then
        returnCF = desyncClone.HumanoidRootPart.CFrame
    end

    if desyncHighlight then
        pcall(function() desyncHighlight:Destroy() end)
        desyncHighlight = nil
    end

    local char = desyncRealChar or lp.Character
    if char and char.Parent then
        local root = char:FindFirstChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")
        if root then
            root.Anchored = false
            if returnCF then
                root.CFrame = returnCF
            end
            root.AssemblyLinearVelocity = Vector3.zero
            root.AssemblyAngularVelocity = Vector3.zero
        end
        for _, p in ipairs(char:GetDescendants()) do
            if p:IsA("BasePart") and p.Name ~= "HumanoidRootPart" and not p:IsA("Accessory") and p.Parent and not p.Parent:IsA("Accessory") then
                p.CanCollide = true
            end
        end
        lp.Character = char
        local cam = workspace.CurrentCamera
        if cam and hum then
            cam.CameraType = Enum.CameraType.Custom
            cam.CameraSubject = hum
        end
        local anim = char:FindFirstChild("Animate")
        if anim then
            anim.Disabled = true
            task.wait(0.05)
            anim.Disabled = false
        end
    end

    if desyncClone then
        pcall(function() desyncClone:Destroy() end)
        desyncClone = nil
    end

    desyncFrozenCF = nil
    desyncRealChar = nil
    DesyncActive = false
end

local lp = game:GetService("Players").LocalPlayer
lp.CharacterAdded:Connect(function(newChar)
    if newChar == invisClone or newChar == desyncClone then
        return
    end
    if InvisActive then
        stopInvisible()
        if invisToggleRef then pcall(function() invisToggleRef:Set(false) end) end
    end
    if DesyncActive then
        stopDesync()
        if desyncToggleRef then pcall(function() desyncToggleRef:Set(false) end) end
    end
end)

invisToggleRef = PlayerTab:AddToggle({
    Name = "Invisible (FE)",
    Description = "Hides your server body in Lighting while you walk freely in ghost mode locally",
    Default = false,
    Callback = function(Value)
        if Value == InvisActive then return end

        if Value then
            if DesyncActive then
                stopDesync()
                if desyncToggleRef then
                    pcall(function() desyncToggleRef:Set(false) end)
                end
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Conflict Resolved", Text = "Desync disabled because Invisible was enabled!", Duration = 3
                })
            end

            local player = game:GetService("Players").LocalPlayer
            local char = player.Character
            if not char then return end
            local root = char:FindFirstChild("HumanoidRootPart")
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not root or not hum then return end

            InvisActive = true
            invisRealChar = char
            local origCF = root.CFrame

            char.Archivable = true
            local clone = char:Clone()
            char.Archivable = false
            clone.Name = ""
            invisClone = clone

            for _, p in ipairs(clone:GetDescendants()) do
                if p:IsA("BasePart") then
                    p.Transparency = (p.Name == "HumanoidRootPart" and 1 or 0.5)
                end
            end

            char:MoveTo(Vector3.new(0, math.pi * 1000000, 0))
            local cam = workspace.CurrentCamera
            if cam then
                cam.CameraType = Enum.CameraType.Scriptable
            end
            task.wait(0.15)
            if cam then
                cam.CameraType = Enum.CameraType.Custom
            end
            char.Parent = game:GetService("Lighting")

            clone.Parent = workspace
            local cloneRoot = clone:FindFirstChild("HumanoidRootPart")
            local cloneHum = clone:FindFirstChildOfClass("Humanoid")
            if cloneRoot then
                cloneRoot.CFrame = origCF
                cloneRoot.Anchored = false
                cloneRoot.AssemblyLinearVelocity = Vector3.zero
                cloneRoot.AssemblyAngularVelocity = Vector3.zero
            end

            player.Character = clone
            if cam and cloneHum then
                cam.CameraType = Enum.CameraType.Custom
                cam.CameraSubject = cloneHum
            end

            local anim = clone:FindFirstChild("Animate")
            if anim then
                anim.Disabled = true
                task.wait(0.05)
                anim.Disabled = false
            end

            invisCamConn = game:GetService("RunService").RenderStepped:Connect(function()
                if not InvisActive or not clone or not clone.Parent or not cloneHum or cloneHum.Health <= 0 then return end
                local currentCam = workspace.CurrentCamera
                if currentCam and currentCam.CameraSubject ~= cloneHum then
                    currentCam.CameraType = Enum.CameraType.Custom
                    currentCam.CameraSubject = cloneHum
                end
            end)

            local voidHeight = workspace.FallenPartsDestroyHeight
            invisSteppedConn = game:GetService("RunService").Stepped:Connect(function()
                pcall(function()
                    if clone and clone.Parent and cloneRoot then
                        if cloneRoot.Position.Y <= (voidHeight + 25) then
                            stopInvisible()
                            if invisToggleRef then pcall(function() invisToggleRef:Set(false) end) end
                        end
                    end
                end)
            end)

            if cloneHum then
                invisDiedConn = cloneHum.Died:Connect(function()
                    stopInvisible()
                    if invisToggleRef then pcall(function() invisToggleRef:Set(false) end) end
                end)
            end

            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Invisible (FE)", Text = "Invisible ON! Server body hidden, camera on ghost.", Duration = 3
            })
        else
            stopInvisible()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Invisible (FE)", Text = "Invisible OFF! Re-synced.", Duration = 3
            })
        end
    end
})

desyncToggleRef = PlayerTab:AddToggle({
    Name = "Desync",
    Description = "Freezes your server body where you stood while you move freely locally",
    Default = false,
    Callback = function(Value)
        if Value == DesyncActive then return end

        if Value then
            if InvisActive then
                stopInvisible()
                if invisToggleRef then
                    pcall(function() invisToggleRef:Set(false) end)
                end
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Conflict Resolved", Text = "Invisible disabled because Desync was enabled!", Duration = 3
                })
            end

            local player = game:GetService("Players").LocalPlayer
            local char = player.Character
            if not char then return end
            local root = char:FindFirstChild("HumanoidRootPart")
            local hum = char:FindFirstChildOfClass("Humanoid")
            if not root or not hum then return end

            DesyncActive = true
            desyncRealChar = char
            desyncFrozenCF = root.CFrame

            root.Anchored = true

            pcall(function()
                local hl = Instance.new("Highlight")
                hl.Name = "BTR_DesyncHighlight"
                hl.FillColor = Color3.fromRGB(255, 30, 30)
                hl.OutlineColor = Color3.fromRGB(255, 255, 255)
                hl.FillTransparency = 0.4
                hl.OutlineTransparency = 0
                hl.Parent = char
                desyncHighlight = hl
            end)

            char.Archivable = true
            local clone = char:Clone()
            char.Archivable = false
            clone.Name = ""
            desyncClone = clone

            for _, p in ipairs(clone:GetDescendants()) do
                if p:IsA("BasePart") then
                    p.Transparency = (p.Name == "HumanoidRootPart" and 1 or 0.5)
                end
            end

            clone.Parent = workspace
            local cloneRoot = clone:FindFirstChild("HumanoidRootPart")
            local cloneHum = clone:FindFirstChildOfClass("Humanoid")
            if cloneRoot then
                cloneRoot.CFrame = desyncFrozenCF
                cloneRoot.Anchored = false
                cloneRoot.AssemblyLinearVelocity = Vector3.zero
                cloneRoot.AssemblyAngularVelocity = Vector3.zero
            end

            player.Character = clone
            local cam = workspace.CurrentCamera
            if cam and cloneHum then
                cam.CameraType = Enum.CameraType.Custom
                cam.CameraSubject = cloneHum
            end

            local anim = clone:FindFirstChild("Animate")
            if anim then
                anim.Disabled = true
                task.wait(0.05)
                anim.Disabled = false
            end

            desyncCamConn = game:GetService("RunService").RenderStepped:Connect(function()
                if not DesyncActive or not clone or not clone.Parent or not cloneHum or cloneHum.Health <= 0 then return end
                local currentCam = workspace.CurrentCamera
                if currentCam and currentCam.CameraSubject ~= cloneHum then
                    currentCam.CameraType = Enum.CameraType.Custom
                    currentCam.CameraSubject = cloneHum
                end
            end)

            local voidHeight = workspace.FallenPartsDestroyHeight
            desyncSteppedConn = game:GetService("RunService").Stepped:Connect(function()
                pcall(function()
                    if clone and clone.Parent and cloneRoot then
                        if cloneRoot.Position.Y <= (voidHeight + 25) then
                            stopDesync()
                            if desyncToggleRef then pcall(function() desyncToggleRef:Set(false) end) end
                        end
                    end
                end)
            end)

            if cloneHum then
                desyncDiedConn = cloneHum.Died:Connect(function()
                    stopDesync()
                    if desyncToggleRef then pcall(function() desyncToggleRef:Set(false) end) end
                end)
            end

            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Desync", Text = "Desync ON! Server body frozen, camera on ghost.", Duration = 3
            })
        else
            stopDesync()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Desync", Text = "Desync OFF! Position re-synced.", Duration = 3
            })
        end
    end
})
end

do
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    local HttpService = game:GetService("HttpService")
    local TextChatService = game:GetService("TextChatService")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local StarterGui = game:GetService("StarterGui")
    local UserInputService = game:GetService("UserInputService")
    local CoreGui = game:GetService("CoreGui")

    _G.AutoTranslateOutgoingEnabled = (_G.AutoTranslateOutgoingEnabled == true)
    _G.AutoCorrectEnabled = (_G.AutoCorrectEnabled == true)
    _G.AutoTranslateIncomingEnabled = (_G.AutoTranslateIncomingEnabled == true)
    _G.OutgoingTargetLang = _G.OutgoingTargetLang or "es"
    _G.IncomingTargetLang = _G.IncomingTargetLang or "fr"
    _G.OutgoingFormat = _G.OutgoingFormat or "Translated Only"

    local isSendingInternal = false
    local isHandlingEnter = false
    local hookedTextBoxes = {}

    local LANG_CODES = {
        ["Spanish"]    = "es",
        ["English"]    = "en",
        ["French"]     = "fr",
        ["German"]     = "de",
        ["Portuguese"] = "pt",
        ["Italian"]    = "it",
        ["Russian"]    = "ru",
        ["Japanese"]   = "ja"
    }

    local LANG_NAMES = {
        ["es"] = "Spanish",
        ["en"] = "English",
        ["fr"] = "French",
        ["de"] = "German",
        ["pt"] = "Portuguese",
        ["it"] = "Italian",
        ["ru"] = "Russian",
        ["ja"] = "Japanese"
    }

    local function notify(title, text)
        pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = 3
            })
        end)
    end

    
    
    
    local function autoCorrectText(text)
        if not text or text == "" then return text end
        local s = " " .. text .. " "

        
        s = s:gsub("(%f[%a])ca va(%f[%A])", "%1ça va%2")
        s = s:gsub("(%f[%a])Ca va(%f[%A])", "%1Ça va%2")
        s = s:gsub("(%f[%a])ca(%f[%A])", "%1ça%2")
        s = s:gsub("(%f[%a])Ca(%f[%A])", "%1Ça%2")
        s = s:gsub("(%f[%a])garcon(%f[%A])", "%1garçon%2")
        s = s:gsub("(%f[%a])garcons(%f[%A])", "%1garçons%2")
        s = s:gsub("(%f[%a])francais(%f[%A])", "%1français%2")
        s = s:gsub("(%f[%a])francaise(%f[%A])", "%1française%2")
        s = s:gsub("(%f[%a])francaises(%f[%A])", "%1françaises%2")
        s = s:gsub("(%f[%a])facon(%f[%A])", "%1façon%2")
        s = s:gsub("(%f[%a])facons(%f[%A])", "%1façons%2")
        s = s:gsub("(%f[%a])lecon(%f[%A])", "%1leçon%2")
        s = s:gsub("(%f[%a])lecons(%f[%A])", "%1leçons%2")
        s = s:gsub("(%f[%a])recu(%f[%A])", "%1reçu%2")
        s = s:gsub("(%f[%a])decu(%f[%A])", "%1déçu%2")
        s = s:gsub("(%f[%a])c est(%f[%A])", "%1c'est%2")
        s = s:gsub("(%f[%a])cest(%f[%A])", "%1c'est%2")
        s = s:gsub("(%f[%a])j ai(%f[%A])", "%1j'ai%2")
        s = s:gsub("(%f[%a])jai(%f[%A])", "%1j'ai%2")
        s = s:gsub("(%f[%a])j espere(%f[%A])", "%1j'espère%2")
        s = s:gsub("(%f[%a])jespere(%f[%A])", "%1j'espère%2")
        s = s:gsub("(%f[%a])j adore(%f[%A])", "%1j'adore%2")
        s = s:gsub("(%f[%a])jadore(%f[%A])", "%1j'adore%2")
        s = s:gsub("(%f[%a])j aime(%f[%A])", "%1j'aime%2")
        s = s:gsub("(%f[%a])jaime(%f[%A])", "%1j'aime%2")
        s = s:gsub("(%f[%a])j arrive(%f[%A])", "%1j'arrive%2")
        s = s:gsub("(%f[%a])jarrive(%f[%A])", "%1j'arrive%2")
        s = s:gsub("(%f[%a])deja(%f[%A])", "%1déjà%2")
        s = s:gsub("(%f[%a])tres(%f[%A])", "%1très%2")
        s = s:gsub("(%f[%a])meme(%f[%A])", "%1même%2")
        s = s:gsub("(%f[%a])apres(%f[%A])", "%1après%2")
        s = s:gsub("(%f[%a])tkt(%f[%A])", "%1t'inquiète%2")
        s = s:gsub("(%f[%a])psk(%f[%A])", "%1parce que%2")
        s = s:gsub("(%f[%a])pck(%f[%A])", "%1parce que%2")
        s = s:gsub("(%f[%a])stp(%f[%A])", "%1s'il te plaît%2")
        s = s:gsub("(%f[%a])svp(%f[%A])", "%1s'il vous plaît%2")
        s = s:gsub("(%f[%a])dsl(%f[%A])", "%1désolé%2")
        s = s:gsub("(%f[%a])voila(%f[%A])", "%1voilà%2")
        s = s:gsub("(%f[%a])aussi(%f[%A])", "%1aussi%2")

        
        s = s:gsub("(%f[%a])dont(%f[%A])", "%1don't%2")
        s = s:gsub("(%f[%a])cant(%f[%A])", "%1can't%2")
        s = s:gsub("(%f[%a])wont(%f[%A])", "%1won't%2")
        s = s:gsub("(%f[%a])didnt(%f[%A])", "%1didn't%2")
        s = s:gsub("(%f[%a])isnt(%f[%A])", "%1isn't%2")
        s = s:gsub("(%f[%a])im(%f[%A])", "%1I'm%2")
        s = s:gsub("(%f[%a])ive(%f[%A])", "%1I've%2")
        s = s:gsub("(%f[%a])youre(%f[%A])", "%1you're%2")
        s = s:gsub("(%f[%a])theyre(%f[%A])", "%1they're%2")
        s = s:gsub("(%f[%a])idk(%f[%A])", "%1I don't know%2")
        s = s:gsub("(%f[%a])tbh(%f[%A])", "%1to be honest%2")
        s = s:gsub("(%f[%a])rn(%f[%A])", "%1right now%2")
        s = s:gsub("(%f[%a])bc(%f[%A])", "%1because%2")
        s = s:gsub("(%f[%a])pls(%f[%A])", "%1please%2")
        s = s:gsub("(%f[%a])thx(%f[%A])", "%1thank you%2")

        s = s:sub(2, -2)
        local firstChar = s:match("^%s*(%a)")
        if firstChar and firstChar:lower() == firstChar then
            s = s:gsub("^%s*" .. firstChar, firstChar:upper(), 1)
        end
        return s
    end

    
    
    
    local function requestHttp(url)
        local req = (syn and syn.request) or (http and http.request) or http_request or (fluxus and fluxus.request) or request
        if req then
            local ok, resp = pcall(function()
                return req({
                    Url = url,
                    Method = "GET",
                    Headers = {
                        ["User-Agent"] = "Mozilla/5.0 (Windows NT 10.0; Win64; x64)"
                    }
                })
            end)
            if ok and resp and (resp.StatusCode == 200 or resp.Status == 200) then
                return resp.Body
            end
        end
        if game.HttpGet then
            local ok, res = pcall(function()
                return game:HttpGet(url)
            end)
            if ok and res then return res end
        end
        return nil
    end

    local function translateText(text, fromLang, toLang)
        if not text or text == "" then return text, fromLang end
        fromLang = fromLang or "auto"
        toLang = toLang or "es"
        if fromLang == toLang and fromLang ~= "auto" then return text, fromLang end

        local encoded = HttpService:UrlEncode(text)

        
        local gUrl = "https://clients5.google.com/translate_a/t?client=dict-chrome-ex&sl=" .. fromLang .. "&tl=" .. toLang .. "&q=" .. encoded
        local gRes = requestHttp(gUrl)
        if gRes then
            local ok, data = pcall(function() return HttpService:JSONDecode(gRes) end)
            if ok and data and type(data) == "table" and data[1] then
                local trans = nil
                local detected = nil
                if type(data[1]) == "table" then
                    trans = tostring(data[1][1] or "")
                    detected = tostring(data[1][2] or "")
                elseif type(data[1]) == "string" then
                    trans = tostring(data[1])
                end
                if trans and trans ~= "" then
                    return trans, detected
                end
            end
        end

        
        local gtxUrl = "https://translate.googleapis.com/translate_a/single?client=gtx&sl=" .. fromLang .. "&tl=" .. toLang .. "&dt=t&q=" .. encoded
        local gtxRes = requestHttp(gtxUrl)
        if gtxRes then
            local ok, data = pcall(function() return HttpService:JSONDecode(gtxRes) end)
            if ok and data and type(data) == "table" and data[1] then
                local str = ""
                for _, seg in ipairs(data[1]) do
                    if type(seg) == "table" and seg[1] then
                        str = str .. tostring(seg[1])
                    end
                end
                if str ~= "" then
                    local det = nil
                    if data[3] and type(data[3]) == "string" then
                        det = data[3]
                    elseif data[2] and type(data[2]) == "string" then
                        det = data[2]
                    end
                    return str, det
                end
            end
        end

        
        local srcPair = (fromLang == "auto" and "fr" or fromLang)
        local memUrl = "https://api.mymemory.translated.net/get?q=" .. encoded .. "&langpair=" .. srcPair .. "|" .. toLang
        local memRes = requestHttp(memUrl)
        if memRes then
            local ok, data = pcall(function() return HttpService:JSONDecode(memRes) end)
            if ok and data and data.responseData and data.responseData.translatedText then
                return tostring(data.responseData.translatedText), srcPair
            end
        end

        return text, fromLang
    end

    local function formatOutgoingMessage(original, translated, toLang)
        if _G.OutgoingFormat == "Translated Only" then
            return translated
        elseif _G.OutgoingFormat == "Trans + Original" then
            return translated .. " (" .. original .. ")"
        elseif _G.OutgoingFormat == "Tagged [LANG]" then
            return "[" .. string.upper(toLang) .. "] " .. translated
        end
        return translated
    end

    
    
    
    local function sendChatMsg(msg)
        if not msg or msg == "" then return end
        isSendingInternal = true

        
        local sent = false
        pcall(function()
            if TextChatService then
                local channels = TextChatService:FindFirstChild("TextChannels")
                local channel = channels and (channels:FindFirstChild("RBXGeneral") or channels:FindFirstChildOfClass("TextChannel"))
                if not channel then
                    local cfg = TextChatService:FindFirstChildOfClass("ChatInputBarConfiguration") or TextChatService:FindFirstChild("ChatInputBarConfiguration")
                    channel = cfg and cfg.TargetTextChannel
                end
                if channel then
                    channel:SendAsync(msg)
                    sent = true
                end
            end
        end)

        
        if not sent then
            pcall(function()
                local defaultChat = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
                local sayMsg = defaultChat and defaultChat:FindFirstChild("SayMessageRequest")
                if sayMsg then
                    sayMsg:FireServer(msg, "All")
                    sent = true
                end
            end)
        end

        
        if not sent then
            pcall(function()
                game:GetService("Chat"):Chat(LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Head") or LocalPlayer.Character, msg, Enum.ChatColor.White)
            end)
        end

        task.wait(0.12)
        isSendingInternal = false
    end

    
    
    
    local recentIncomingCache = {}
    local function isDuplicateIncoming(senderName, rawText)
        local now = os.clock()
        for k, t in pairs(recentIncomingCache) do
            if now - t > 4 then
                recentIncomingCache[k] = nil
            end
        end
        local key = tostring(senderName) .. "::" .. tostring(rawText)
        if recentIncomingCache[key] and (now - recentIncomingCache[key] < 3.0) then
            return true
        end
        recentIncomingCache[key] = now
        return false
    end

    local function displayTranslatedIncoming(senderName, original, translated, detected)
        local tag = string.upper(detected or "TRAD")
        local displayed = false

        
        pcall(function()
            if TextChatService then
                local channels = TextChatService:FindFirstChild("TextChannels")
                local rbxGeneral = channels and channels:FindFirstChild("RBXGeneral")
                if rbxGeneral then
                    rbxGeneral:DisplaySystemMessage(string.format("<font color='#00E5FF'>[%s] %s:</font> %s", tag, senderName, translated))
                    displayed = true
                end
            end
        end)

        
        if not displayed then
            pcall(function()
                StarterGui:SetCore("ChatMakeSystemMessage", {
                    Text = string.format("[%s] %s: %s", tag, senderName, translated),
                    Color = Color3.fromRGB(0, 229, 255),
                    Font = Enum.Font.SourceSansBold,
                    FontSize = Enum.FontSize.Size18
                })
            end)
        end
    end

    local function handleIncomingMessage(senderName, rawText)
        if not _G.AutoTranslateIncomingEnabled then return end
        if not rawText or rawText == "" then return end
        if senderName == LocalPlayer.Name or senderName == LocalPlayer.DisplayName then return end
        if isDuplicateIncoming(senderName, rawText) then return end

        task.spawn(function()
            local translated, detected = translateText(rawText, "auto", _G.IncomingTargetLang)
            if detected and detected:lower() == _G.IncomingTargetLang:lower() then return end
            if translated and translated ~= "" and translated:lower() ~= rawText:lower() then
                displayTranslatedIncoming(senderName, rawText, translated, detected)
            end
        end)
    end

    
    pcall(function()
        if TextChatService then
            TextChatService.MessageReceived:Connect(function(textChatMessage)
                local textSource = textChatMessage.TextSource
                if textSource then
                    local sender = Players:GetPlayerByUserId(textSource.UserId)
                    if sender and sender ~= LocalPlayer then
                        handleIncomingMessage(sender.DisplayName or sender.Name, textChatMessage.Text)
                    end
                end
            end)
        end
    end)

    
    local function hookPlayerLegacyChat(p)
        p.Chatted:Connect(function(msg)
            if p ~= LocalPlayer then
                handleIncomingMessage(p.DisplayName or p.Name, msg)
            end
        end)
    end
    for _, p in ipairs(Players:GetPlayers()) do
        hookPlayerLegacyChat(p)
    end
    Players.PlayerAdded:Connect(hookPlayerLegacyChat)

    
    
    
    
    local function isChatBar(tb)
        if not tb or not tb:IsA("TextBox") then return false end

        
        local p = tb
        while p do
            if p.Name == "RedzLib" or p.Name == "RedzLibHub" or p.Name == "BTR_UI" or p.Name == "AutoTranslateGui" then
                return false
            end
            p = p.Parent
        end

        if tb.Name == "ChatBar" then return true end
        if tb.PlaceholderText and (tb.PlaceholderText:lower():find("chat") or tb.PlaceholderText:find("Message") or tb.PlaceholderText:find("parler") or tb.PlaceholderText:find("discuter")) then
            return true
        end

        local isCore = false
        pcall(function()
            local expChat = CoreGui:FindFirstChild("ExperienceChat")
            if expChat and tb:IsDescendantOf(expChat) then
                isCore = true
            end
        end)
        if isCore then return true end

        if TextChatService then
            local cfg = TextChatService:FindFirstChildOfClass("ChatInputBarConfiguration") or TextChatService:FindFirstChild("ChatInputBarConfiguration")
            if cfg and cfg.TextBox == tb then
                return true
            end
        end

        local pg = LocalPlayer:FindFirstChildOfClass("PlayerGui")
        if pg then
            local chat = pg:FindFirstChild("Chat") or pg:FindFirstChild("ExperienceChat")
            if chat and tb:IsDescendantOf(chat) then
                return true
            end
        end

        return false
    end

    local function bindChatTextBox(tb)
        if not tb or not tb:IsA("TextBox") or hookedTextBoxes[tb] then return end
        hookedTextBoxes[tb] = true

        
        local isUpdatingLive = false
        tb:GetPropertyChangedSignal("Text"):Connect(function()
            if isUpdatingLive then return end
            if not _G.AutoCorrectEnabled then return end

            local current = tb.Text
            if not current or current == "" then return end

            local lastChar = current:sub(-1)
            if lastChar == " " or lastChar == "," or lastChar == "." or lastChar == "!" or lastChar == "?" then
                local corrected = autoCorrectText(current)
                if corrected ~= current then
                    isUpdatingLive = true
                    local oldLen = #current
                    local oldCursor = tb.CursorPosition
                    tb.Text = corrected
                    if oldCursor >= oldLen then
                        tb.CursorPosition = #corrected + 1
                    end
                    isUpdatingLive = false
                end
            end
        end)

        
        tb.FocusLost:Connect(function(enterPressed)
            if enterPressed and not isHandlingEnter then
                local rawText = tb.Text
                if rawText and rawText ~= "" and _G.AutoTranslateOutgoingEnabled then
                    isHandlingEnter = true
                    tb.Text = ""
                    task.spawn(function()
                        local toProcess = rawText
                        if _G.AutoCorrectEnabled then
                            toProcess = autoCorrectText(toProcess)
                        end
                        local trans = translateText(toProcess, "auto", _G.OutgoingTargetLang)
                        local finalMsg = formatOutgoingMessage(rawText, trans, _G.OutgoingTargetLang)
                        notify("Chat Translator (" .. string.upper(_G.OutgoingTargetLang) .. ")", trans)
                        sendChatMsg(finalMsg)
                        task.wait(0.15)
                        isHandlingEnter = false
                    end)
                end
            end
        end)
    end

    
    UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if input.KeyCode == Enum.KeyCode.Return or input.KeyCode == Enum.KeyCode.KeypadEnter then
            if isHandlingEnter then return end

            local focusedTb = UserInputService:GetFocusedTextBox()
            if not focusedTb then
                for tb, _ in pairs(hookedTextBoxes) do
                    if tb and tb.Parent and tb:IsFocused() then
                        focusedTb = tb
                        break
                    end
                end
            end

            if focusedTb and isChatBar(focusedTb) then
                local rawText = focusedTb.Text
                if rawText and rawText ~= "" then
                    if _G.AutoTranslateOutgoingEnabled then
                        isHandlingEnter = true
                        
                        focusedTb.Text = ""
                        pcall(function() focusedTb:ReleaseFocus() end)

                        task.spawn(function()
                            local toProcess = rawText
                            if _G.AutoCorrectEnabled then
                                toProcess = autoCorrectText(toProcess)
                            end

                            local trans = translateText(toProcess, "auto", _G.OutgoingTargetLang)
                            local finalMsg = formatOutgoingMessage(rawText, trans, _G.OutgoingTargetLang)

                            notify("Chat Translator (" .. string.upper(_G.OutgoingTargetLang) .. ")", trans)
                            sendChatMsg(finalMsg)

                            task.wait(0.15)
                            isHandlingEnter = false
                        end)
                    elseif _G.AutoCorrectEnabled then
                        local corrected = autoCorrectText(rawText)
                        if corrected ~= rawText then
                            focusedTb.Text = corrected
                        end
                    end
                end
            end
        end
    end)

    
    local function scanAndBindAll()
        pcall(function()
            local focused = UserInputService:GetFocusedTextBox()
            if focused and isChatBar(focused) then
                bindChatTextBox(focused)
            end
        end)

        pcall(function()
            if TextChatService then
                local cfg = TextChatService:FindFirstChildOfClass("ChatInputBarConfiguration") or TextChatService:FindFirstChild("ChatInputBarConfiguration")
                if cfg and cfg.TextBox and cfg.TextBox:IsA("TextBox") then
                    bindChatTextBox(cfg.TextBox)
                end
            end
        end)

        pcall(function()
            local expChat = CoreGui:FindFirstChild("ExperienceChat")
            if expChat then
                for _, desc in ipairs(expChat:GetDescendants()) do
                    if desc:IsA("TextBox") then
                        bindChatTextBox(desc)
                    end
                end
            end
        end)

        pcall(function()
            local pg = LocalPlayer:FindFirstChildOfClass("PlayerGui")
            if pg then
                for _, desc in ipairs(pg:GetDescendants()) do
                    if desc:IsA("TextBox") and (desc.Name == "ChatBar" or isChatBar(desc)) then
                        bindChatTextBox(desc)
                    end
                end
            end
        end)
    end

    scanAndBindAll()

    pcall(function()
        CoreGui.DescendantAdded:Connect(function(desc)
            if desc:IsA("TextBox") and isChatBar(desc) then
                bindChatTextBox(desc)
            end
        end)
    end)

    pcall(function()
        local pg = LocalPlayer:WaitForChild("PlayerGui", 5)
        if pg then
            pg.DescendantAdded:Connect(function(desc)
                if desc:IsA("TextBox") and isChatBar(desc) then
                    bindChatTextBox(desc)
                end
            end)
        end
    end)

    
    if hookmetamethod and getnamecallmethod and newcclosure then
        pcall(function()
            local oldNamecall
            oldNamecall = hookmetamethod(game, "__namecall", newcclosure(function(self, ...)
                if not (_G.AutoTranslateOutgoingEnabled or _G.AutoCorrectEnabled) or isSendingInternal or checkcaller() then
                    return oldNamecall(self, ...)
                end

                local method = getnamecallmethod()
                local args = {...}

                local shouldProcess = _G.AutoTranslateOutgoingEnabled or _G.AutoCorrectEnabled

                
                if shouldProcess and (method == "SendAsync" or method == "sendAsync") and self:IsA("TextChannel") then
                    local rawText = args[1]
                    if typeof(rawText) == "string" and rawText ~= "" then
                        task.spawn(function()
                            local processedText = rawText
                            if _G.AutoCorrectEnabled then
                                processedText = autoCorrectText(processedText)
                            end
                            if _G.AutoTranslateOutgoingEnabled then
                                local trans = translateText(processedText, "auto", _G.OutgoingTargetLang)
                                processedText = formatOutgoingMessage(rawText, trans, _G.OutgoingTargetLang)
                            end

                            isSendingInternal = true
                            pcall(function() self:SendAsync(processedText) end)
                            task.wait(0.05)
                            isSendingInternal = false
                        end)
                        return nil
                    end
                end

                
                if shouldProcess and (method == "FireServer" or method == "fireServer") and self.Name == "SayMessageRequest" then
                    local rawText = args[1]
                    local channel = args[2] or "All"
                    if typeof(rawText) == "string" and rawText ~= "" then
                        task.spawn(function()
                            local processedText = rawText
                            if _G.AutoCorrectEnabled then
                                processedText = autoCorrectText(processedText)
                            end
                            if _G.AutoTranslateOutgoingEnabled then
                                local trans = translateText(processedText, "auto", _G.OutgoingTargetLang)
                                processedText = formatOutgoingMessage(rawText, trans, _G.OutgoingTargetLang)
                            end

                            isSendingInternal = true
                            pcall(function() self:FireServer(processedText, channel) end)
                            task.wait(0.05)
                            isSendingInternal = false
                        end)
                        return nil
                    end
                end

                return oldNamecall(self, ...)
            end))
        end)
    end

    
    
    

    
    PlayerTab:AddSection({
        "> Chat Translator"
    })

    _G.BTR_AutoTranslateOutgoingToggle = PlayerTab:AddToggle({
        Name = "Auto Translate Outgoing",
        Description = "Translates your chat messages into the target language in real-time",
        Default = (_G.AutoTranslateOutgoingEnabled == true),
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            _G.AutoTranslateOutgoingEnabled = val
            if _G.saveBTRConfig then _G.saveBTRConfig() end
            if val then
                notify("Chat Translator", "Outgoing translation ACTIVE (" .. string.upper(_G.OutgoingTargetLang) .. ")")
            else
                notify("Chat Translator", "Outgoing translation DISABLED")
            end
        end
    })

    PlayerTab:AddDropdown({
        Name = "Outgoing Language",
        Description = "Language your outgoing chat will be translated to",
        Options = { "Spanish", "English", "French", "German", "Portuguese", "Italian", "Russian", "Japanese" },
        Default = LANG_NAMES[_G.OutgoingTargetLang] or "Spanish",
        Callback = function(val)
            _G.OutgoingTargetLang = LANG_CODES[val] or "es"
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end
    })

    PlayerTab:AddDropdown({
        Name = "Outgoing Format",
        Description = "How your translated message appears to others in chat",
        Options = { "Translated Only", "Trans + Original", "Tagged [LANG]" },
        Default = _G.OutgoingFormat or "Translated Only",
        Callback = function(val)
            _G.OutgoingFormat = val
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end
    })

    _G.BTR_AutoTranslateIncomingToggle = PlayerTab:AddToggle({
        Name = "Auto Translate Incoming",
        Description = "Translates messages from other players into your language in chat",
        Default = (_G.AutoTranslateIncomingEnabled == true),
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            _G.AutoTranslateIncomingEnabled = val
            if _G.saveBTRConfig then _G.saveBTRConfig() end
            if val then
                notify("Chat Translator", "Incoming translation ACTIVE (" .. string.upper(_G.IncomingTargetLang) .. ")")
            else
                notify("Chat Translator", "Incoming translation DISABLED")
            end
        end
    })

    PlayerTab:AddDropdown({
        Name = "Incoming Language",
        Description = "Language you want to read other players' messages in",
        Options = { "French", "English", "Spanish" },
        Default = LANG_NAMES[_G.IncomingTargetLang] or "French",
        Callback = function(val)
            _G.IncomingTargetLang = LANG_CODES[val] or "fr"
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end
    })

    
    PlayerTab:AddSection({
        "> Chat Auto-Corrector"
    })

    _G.BTR_AutoCorrectToggle = PlayerTab:AddToggle({
        Name = "Auto Correct Typing",
        Description = "Automatically fixes cédilles, accents, typos and apostrophes (FR & EN)",
        Default = (_G.AutoCorrectEnabled == true),
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            _G.AutoCorrectEnabled = val
            if _G.saveBTRConfig then _G.saveBTRConfig() end
            if val then
                notify("Auto Correct", "Typing correction ACTIVE")
            else
                notify("Auto Correct", "Typing correction DISABLED")
            end
        end
    })
end

-- Onglet 3 : Profile -- [LOCKED: WORK PERFECTLY]
ProfileTab:AddSection({
    "> " .. Translations["Names"]
})

local customRPName = ""
local customRPBio = ""
local selectedPresetName = "『 ѢҬԄ X ҪԒЇԐԊҬ 』"
local selectedPresetBio = "BTR Client | ON TOP"

local function setRPName(text)
    pcall(function()
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        if RE and RE:FindFirstChild("1RPNam1eTex1t") then
            RE["1RPNam1eTex1t"]:FireServer("RolePlayName", text)
        end
    end)
end

local function setRPBio(text)
    pcall(function()
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        if RE and RE:FindFirstChild("1RPNam1eTex1t") then
            RE["1RPNam1eTex1t"]:FireServer("RolePlayBio", text)
        end
    end)
end

ProfileTab:AddTextBox({
    Name = "Custom RP Name",
    Description = "Type your custom RolePlay Name",
    PlaceholderText = "Enter Name here...",
    Callback = function(val)
        customRPName = val
    end
})

ProfileTab:AddButton({
    Name = "Apply RP Name",
    Description = "Sets your RP Name on your character",
    Callback = function()
        if customRPName ~= "" then
            setRPName(customRPName)
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Profile",
                Text = "RP Name updated!",
                Duration = 2
            })
        end
    end
})

ProfileTab:AddTextBox({
    Name = "Custom RP Bio",
    Description = "Type your custom RolePlay Bio",
    PlaceholderText = "Enter Bio here...",
    Callback = function(val)
        customRPBio = val
    end
})

ProfileTab:AddButton({
    Name = "Apply RP Bio",
    Description = "Sets your RP Bio on your character",
    Callback = function()
        if customRPBio ~= "" then
            setRPBio(customRPBio)
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Profile",
                Text = "RP Bio updated!",
                Duration = 2
            })
        end
    end
})

ProfileTab:AddButton({
    Name = "Clear RP Name & Bio",
    Description = "Resets both your RP Name and Bio",
    Callback = function()
        setRPName(" ")
        setRPBio(" ")
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Profile",
            Text = "Name and Bio cleared.",
            Duration = 2
        })
    end
})

ProfileTab:AddSection({
    "> Preset RP Names & Bios"
})

ProfileTab:AddDropdown({
    Name = "Preset RP Names",
    Description = "Select a curated RP Name",
    Options = {
        "『 ѢҬԄ X ҪԒЇԐԊҬ 』",
        "BTR Client | ON TOP",
        "BTR Client",
        "BTR Client V2",
        "Ash",
        "Ash | BTR Client"
    },
    Default = "『 ѢҬԄ X ҪԒЇԐԊҬ 』",
    Callback = function(val)
        selectedPresetName = val
    end
})

ProfileTab:AddButton({
    Name = "Apply Preset Name",
    Description = "Sets the selected preset as your RP Name",
    Callback = function()
        setRPName(selectedPresetName)
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Profile",
            Text = "Applied: " .. (selectedPresetName or ""),
            Duration = 2
        })
    end
})

ProfileTab:AddDropdown({
    Name = "Preset RP Bios",
    Description = "Select a curated RP Bio",
    Options = {
        "BTR Client | ON TOP",
        "BTR Client",
        "Ash",
        "Ash | BTR Client"
    },
    Default = "BTR Client | ON TOP",
    Callback = function(val)
        selectedPresetBio = val
    end
})

ProfileTab:AddButton({
    Name = "Apply Preset Bio",
    Description = "Sets the selected preset as your RP Bio",
    Callback = function()
        setRPBio(selectedPresetBio)
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "Profile",
            Text = "Applied Bio!",
            Duration = 2
        })
    end
})

ProfileTab:AddSection({
    "> Animated Colors (Rainbow & Gradient)"
})

do
local function c3ToTable(c)
    return {math.floor(c.R * 255 + 0.5), math.floor(c.G * 255 + 0.5), math.floor(c.B * 255 + 0.5)}
end
local function tableToC3(t, def)
    if type(t) == "table" and #t >= 3 then
        return Color3.fromRGB(t[1], t[2], t[3])
    end
    return def
end

local isRainbowNameActive = (_G.BTR_RGB_NAME_ACTIVE == true)
local isRainbowBioActive  = (_G.BTR_RGB_BIO_ACTIVE == true)
local rainbowSpeed        = _G.BTR_RAINBOW_SPEED or 5
local nameColorMode       = _G.BTR_NAME_COLOR_MODE or "Full Rainbow (RGB)"
local bioColorMode        = _G.BTR_BIO_COLOR_MODE or "Full Rainbow (RGB)"

local nameCol1 = tableToC3(_G.BTR_NAME_COL1, Color3.fromRGB(0, 255, 255))
local nameCol2 = tableToC3(_G.BTR_NAME_COL2, Color3.fromRGB(170, 0, 255))
local nameCol3 = tableToC3(_G.BTR_NAME_COL3, Color3.fromRGB(255, 0, 128))

local bioCol1  = tableToC3(_G.BTR_BIO_COL1, Color3.fromRGB(50, 255, 50))
local bioCol2  = tableToC3(_G.BTR_BIO_COL2, Color3.fromRGB(255, 215, 0))
local bioCol3  = tableToC3(_G.BTR_BIO_COL3, Color3.fromRGB(255, 30, 30))

local function lerpColorHSV(c1, c2, alpha)
    local h1, s1, v1 = c1:ToHSV()
    local h2, s2, v2 = c2:ToHSV()
    
    local diff = h2 - h1
    if diff > 0.5 then
        diff = diff - 1
    elseif diff < -0.5 then
        diff = diff + 1
    end
    local h = (h1 + diff * alpha) % 1
    local s = s1 + (s2 - s1) * alpha
    local v = v1 + (v2 - v1) * alpha
    return Color3.fromHSV(h, s, v)
end

local function getInterpolatedColor(mode, c1, c2, c3, tProgress)
    if mode == "Full Rainbow (RGB)" then
        return Color3.fromHSV(tProgress % 1, 1, 1)
    elseif mode == "Custom (2 Colors)" then
        local rawSin = math.sin(tProgress * math.pi * 2)
        local alpha = 0.5 + 0.5 * rawSin
        return lerpColorHSV(c1, c2, alpha)
    elseif mode == "Custom (3 Colors)" then
        local p = (tProgress % 1) * 3
        if p < 1 then
            local alpha = (1 - math.cos(p * math.pi)) / 2
            return lerpColorHSV(c1, c2, alpha)
        elseif p < 2 then
            local alpha = (1 - math.cos((p - 1) * math.pi)) / 2
            return lerpColorHSV(c2, c3, alpha)
        else
            local alpha = (1 - math.cos((p - 2) * math.pi)) / 2
            return lerpColorHSV(c3, c1, alpha)
        end
    end
    return Color3.fromHSV(tProgress % 1, 1, 1)
end

local openColorPickerGui = nil
do
    local CoreGui = game:GetService("CoreGui")
    local Players = game:GetService("Players")
    local UserInputService = game:GetService("UserInputService")
    local lp = Players.LocalPlayer

    openColorPickerGui = function(titleText, initialColor, onColorSelected)
        local existing = CoreGui:FindFirstChild("BTR_ColorPickerGui")
        if existing then existing:Destroy() end

        local gui = Instance.new("ScreenGui")
        gui.Name = "BTR_ColorPickerGui"
        gui.ResetOnSpawn = false
        gui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
        pcall(function() gui.Parent = CoreGui end)
        if not gui.Parent or gui.Parent ~= CoreGui then
            gui.Parent = lp:WaitForChild("PlayerGui")
        end

        local mainFrame = Instance.new("Frame")
        mainFrame.Name = "MainFrame"
        mainFrame.Size = UDim2.new(0, 310, 0, 380)
        mainFrame.Position = UDim2.new(0.5, -155, 0.5, -190)
        mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
        mainFrame.BorderSizePixel = 0
        mainFrame.Active = true
        mainFrame.ClipsDescendants = true
        mainFrame.Parent = gui

        local uiCorner = Instance.new("UICorner", mainFrame)
        uiCorner.CornerRadius = UDim.new(0, 12)

        local stroke = Instance.new("UIStroke", mainFrame)
        stroke.Color = Color3.fromRGB(60, 60, 80)
        stroke.Thickness = 1.5

        local headerBar = Instance.new("Frame", mainFrame)
        headerBar.Name = "HeaderBar"
        headerBar.Size = UDim2.new(1, 0, 0, 38)
        headerBar.Position = UDim2.new(0, 0, 0, 0)
        headerBar.BackgroundColor3 = Color3.fromRGB(28, 28, 35)
        headerBar.BorderSizePixel = 0
        headerBar.Active = true

        local headerCorner = Instance.new("UICorner", headerBar)
        headerCorner.CornerRadius = UDim.new(0, 12)

        local titleLabel = Instance.new("TextLabel", headerBar)
        titleLabel.Size = UDim2.new(1, -45, 1, 0)
        titleLabel.Position = UDim2.new(0, 14, 0, 0)
        titleLabel.BackgroundTransparency = 1
        titleLabel.Text = titleText or "Color Picker"
        titleLabel.TextColor3 = Color3.fromRGB(245, 245, 245)
        titleLabel.Font = Enum.Font.GothamBold
        titleLabel.TextSize = 13
        titleLabel.TextXAlignment = Enum.TextXAlignment.Left

        local closeBtn = Instance.new("TextButton", headerBar)
        closeBtn.Name = "CloseButton"
        closeBtn.Size = UDim2.new(0, 26, 0, 26)
        closeBtn.Position = UDim2.new(1, -32, 0, 6)
        closeBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)
        closeBtn.Text = "X"
        closeBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
        closeBtn.Font = Enum.Font.GothamBold
        closeBtn.TextSize = 12
        local closeCorner = Instance.new("UICorner", closeBtn)
        closeCorner.CornerRadius = UDim.new(0, 6)
        closeBtn.MouseButton1Click:Connect(function() gui:Destroy() end)

        local isDraggingWindow = false
        local dragStartPos = nil
        local startGuiPos = nil
        headerBar.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                isDraggingWindow = true
                dragStartPos = input.Position
                startGuiPos = mainFrame.Position
            end
        end)
        UserInputService.InputChanged:Connect(function(input)
            if isDraggingWindow and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
                local delta = input.Position - dragStartPos
                mainFrame.Position = UDim2.new(startGuiPos.X.Scale, startGuiPos.X.Offset + delta.X, startGuiPos.Y.Scale, startGuiPos.Y.Offset + delta.Y)
            end
        end)
        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                isDraggingWindow = false
            end
        end)

        local curHue, curSat, curVal = (initialColor or Color3.fromRGB(0, 255, 255)):ToHSV()

        local svBox = Instance.new("Frame", mainFrame)
        svBox.Name = "SVBox"
        svBox.Size = UDim2.new(0, 230, 0, 230)
        svBox.Position = UDim2.new(0, 14, 0, 52)
        svBox.BackgroundColor3 = Color3.fromHSV(curHue, 1, 1)
        svBox.BorderSizePixel = 0
        local svCorner = Instance.new("UICorner", svBox)
        svCorner.CornerRadius = UDim.new(0, 8)

        local satGrad = Instance.new("UIGradient", svBox)
        satGrad.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
            ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))
        })
        satGrad.Rotation = 0

        local valLayer = Instance.new("Frame", svBox)
        valLayer.Name = "ValLayer"
        valLayer.Size = UDim2.new(1, 0, 1, 0)
        valLayer.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        valLayer.BorderSizePixel = 0
        local valCorner = Instance.new("UICorner", valLayer)
        valCorner.CornerRadius = UDim.new(0, 8)
        local valGrad = Instance.new("UIGradient", valLayer)
        valGrad.Transparency = NumberSequence.new({
            NumberSequenceKeypoint.new(0, 1),
            NumberSequenceKeypoint.new(1, 0)
        })
        valGrad.Rotation = 90

        local svCursor = Instance.new("Frame", svBox)
        svCursor.Name = "SVCursor"
        svCursor.Size = UDim2.new(0, 12, 0, 12)
        svCursor.AnchorPoint = Vector2.new(0.5, 0.5)
        svCursor.Position = UDim2.new(curSat, 0, 1 - curVal, 0)
        svCursor.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        svCursor.BorderSizePixel = 0
        local curCorner = Instance.new("UICorner", svCursor)
        curCorner.CornerRadius = UDim.new(1, 0)
        local curStroke = Instance.new("UIStroke", svCursor)
        curStroke.Color = Color3.fromRGB(0, 0, 0)
        curStroke.Thickness = 1.5

        local hueBar = Instance.new("Frame", mainFrame)
        hueBar.Name = "HueBar"
        hueBar.Size = UDim2.new(0, 36, 0, 230)
        hueBar.Position = UDim2.new(0, 260, 0, 52)
        hueBar.BorderSizePixel = 0
        local hueCorner = Instance.new("UICorner", hueBar)
        hueCorner.CornerRadius = UDim.new(0, 8)

        local hueGrad = Instance.new("UIGradient", hueBar)
        hueGrad.Color = ColorSequence.new({
            ColorSequenceKeypoint.new(0, Color3.fromHSV(0, 1, 1)),
            ColorSequenceKeypoint.new(0.17, Color3.fromHSV(0.17, 1, 1)),
            ColorSequenceKeypoint.new(0.33, Color3.fromHSV(0.33, 1, 1)),
            ColorSequenceKeypoint.new(0.50, Color3.fromHSV(0.50, 1, 1)),
            ColorSequenceKeypoint.new(0.67, Color3.fromHSV(0.67, 1, 1)),
            ColorSequenceKeypoint.new(0.83, Color3.fromHSV(0.83, 1, 1)),
            ColorSequenceKeypoint.new(1, Color3.fromHSV(1, 1, 1))
        })
        hueGrad.Rotation = 90

        local hueCursor = Instance.new("Frame", hueBar)
        hueCursor.Name = "HueCursor"
        hueCursor.Size = UDim2.new(1, 4, 0, 6)
        hueCursor.AnchorPoint = Vector2.new(0.5, 0.5)
        hueCursor.Position = UDim2.new(0.5, 0, curHue, 0)
        hueCursor.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
        hueCursor.BorderSizePixel = 0
        local hueCurCorner = Instance.new("UICorner", hueCursor)
        hueCurCorner.CornerRadius = UDim.new(0, 3)
        local hueStroke = Instance.new("UIStroke", hueCursor)
        hueStroke.Color = Color3.fromRGB(0, 0, 0)
        hueStroke.Thickness = 1

        local previewBox = Instance.new("Frame", mainFrame)
        previewBox.Name = "PreviewBox"
        previewBox.Size = UDim2.new(1, -28, 0, 34)
        previewBox.Position = UDim2.new(0, 14, 0, 296)
        previewBox.BackgroundColor3 = initialColor or Color3.fromRGB(0, 255, 255)
        local prevCorner = Instance.new("UICorner", previewBox)
        prevCorner.CornerRadius = UDim.new(0, 8)

        local previewLabel = Instance.new("TextLabel", previewBox)
        previewLabel.Size = UDim2.new(1, 0, 1, 0)
        previewLabel.BackgroundTransparency = 1
        previewLabel.Text = "Current Color"
        previewLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
        previewLabel.Font = Enum.Font.GothamBold
        previewLabel.TextSize = 11

        local function updateColor()
            local baseCol = Color3.fromHSV(curHue, 1, 1)
            svBox.BackgroundColor3 = baseCol
            satGrad.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 255, 255)),
                ColorSequenceKeypoint.new(1, baseCol)
            })
            local selCol = Color3.fromHSV(curHue, curSat, curVal)
            previewBox.BackgroundColor3 = selCol
        end
        updateColor()

        local isDraggingSV = false
        svBox.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                isDraggingSV = true
                local x = math.clamp((input.Position.X - svBox.AbsolutePosition.X) / svBox.AbsoluteSize.X, 0, 1)
                local y = math.clamp((input.Position.Y - svBox.AbsolutePosition.Y) / svBox.AbsoluteSize.Y, 0, 1)
                curSat = x
                curVal = 1 - y
                svCursor.Position = UDim2.new(x, 0, y, 0)
                updateColor()
            end
        end)

        local isDraggingHue = false
        hueBar.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                isDraggingHue = true
                local y = math.clamp((input.Position.Y - hueBar.AbsolutePosition.Y) / hueBar.AbsoluteSize.Y, 0, 1)
                curHue = y
                hueCursor.Position = UDim2.new(0.5, 0, y, 0)
                updateColor()
            end
        end)

        UserInputService.InputEnded:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                isDraggingSV = false
                isDraggingHue = false
            end
        end)

        UserInputService.InputChanged:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
                if isDraggingSV then
                    local x = math.clamp((input.Position.X - svBox.AbsolutePosition.X) / svBox.AbsoluteSize.X, 0, 1)
                    local y = math.clamp((input.Position.Y - svBox.AbsolutePosition.Y) / svBox.AbsoluteSize.Y, 0, 1)
                    curSat = x
                    curVal = 1 - y
                    svCursor.Position = UDim2.new(x, 0, y, 0)
                    updateColor()
                elseif isDraggingHue then
                    local y = math.clamp((input.Position.Y - hueBar.AbsolutePosition.Y) / hueBar.AbsoluteSize.Y, 0, 1)
                    curHue = y
                    hueCursor.Position = UDim2.new(0.5, 0, y, 0)
                    updateColor()
                end
            end
        end)

        local applyBtn = Instance.new("TextButton", mainFrame)
        applyBtn.Name = "ApplyButton"
        applyBtn.Size = UDim2.new(1, -28, 0, 34)
        applyBtn.Position = UDim2.new(0, 14, 0, 336)
        applyBtn.BackgroundColor3 = Color3.fromRGB(35, 105, 245)
        applyBtn.Text = "Apply Color"
        applyBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        applyBtn.Font = Enum.Font.GothamBold
        applyBtn.TextSize = 13
        local btnCorner = Instance.new("UICorner", applyBtn)
        btnCorner.CornerRadius = UDim.new(0, 8)
        applyBtn.MouseButton1Click:Connect(function()
            local selected = Color3.fromHSV(curHue, curSat, curVal)
            if onColorSelected then onColorSelected(selected) end
            gui:Destroy()
        end)
    end
end

ProfileTab:AddToggle({
    Name = "Rainbow RP Name",
    Description = "Cycles your RP Name color continuously",
    Default = (_G.BTR_RGB_NAME_ACTIVE == true),
    Callback = function(val)
        isRainbowNameActive = val
        _G.BTR_RGB_NAME_ACTIVE = val
        if _G.saveBTRConfig then _G.saveBTRConfig() end
    end
})

ProfileTab:AddToggle({
    Name = "Rainbow RP Bio",
    Description = "Cycles your RP Bio color continuously",
    Default = (_G.BTR_RGB_BIO_ACTIVE == true),
    Callback = function(val)
        isRainbowBioActive = val
        _G.BTR_RGB_BIO_ACTIVE = val
        if _G.saveBTRConfig then _G.saveBTRConfig() end
    end
})

ProfileTab:AddSlider({
    Name = "Color Cycle Speed",
    Description = "Adjusts how fast the colors transition",
    Min = 1,
    Max = 10,
    Default = _G.BTR_RAINBOW_SPEED or 5,
    Suffix = " spd",
    Callback = function(val)
        rainbowSpeed = val
        _G.BTR_RAINBOW_SPEED = val
        if _G.saveBTRConfig then _G.saveBTRConfig() end
    end
})

ProfileTab:AddSection({
    "> RP Name Colors (Custom Picker)"
})

ProfileTab:AddDropdown({
    Name = "Name Mode",
    Description = "Choose between full spectrum RGB or custom 2/3 colors",
    Options = {"Full Rainbow (RGB)", "Custom (2 Colors)", "Custom (3 Colors)"},
    Default = _G.BTR_NAME_COLOR_MODE or "Full Rainbow (RGB)",
    Callback = function(val)
        nameColorMode = val
        _G.BTR_NAME_COLOR_MODE = val
        if _G.saveBTRConfig then _G.saveBTRConfig() end
    end
})

ProfileTab:AddButton({
    Name = "Pick Name Color 1",
    Description = "Opens interactive Color Picker palette",
    Callback = function()
        openColorPickerGui("Pick Name Color 1", nameCol1, function(c)
            nameCol1 = c
            _G.BTR_NAME_COL1 = c3ToTable(c)
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end)
    end
})

ProfileTab:AddButton({
    Name = "Pick Name Color 2",
    Description = "Opens interactive Color Picker palette",
    Callback = function()
        openColorPickerGui("Pick Name Color 2", nameCol2, function(c)
            nameCol2 = c
            _G.BTR_NAME_COL2 = c3ToTable(c)
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end)
    end
})

ProfileTab:AddButton({
    Name = "Pick Name Color 3",
    Description = "Opens interactive Color Picker palette",
    Callback = function()
        openColorPickerGui("Pick Name Color 3", nameCol3, function(c)
            nameCol3 = c
            _G.BTR_NAME_COL3 = c3ToTable(c)
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end)
    end
})

ProfileTab:AddSection({
    "> RP Bio Colors (Custom Picker)"
})

ProfileTab:AddDropdown({
    Name = "Bio Mode",
    Description = "Choose between full spectrum RGB or custom 2/3 colors",
    Options = {"Full Rainbow (RGB)", "Custom (2 Colors)", "Custom (3 Colors)"},
    Default = _G.BTR_BIO_COLOR_MODE or "Full Rainbow (RGB)",
    Callback = function(val)
        bioColorMode = val
        _G.BTR_BIO_COLOR_MODE = val
        if _G.saveBTRConfig then _G.saveBTRConfig() end
    end
})

ProfileTab:AddButton({
    Name = "Pick Bio Color 1",
    Description = "Opens interactive Color Picker palette",
    Callback = function()
        openColorPickerGui("Pick Bio Color 1", bioCol1, function(c)
            bioCol1 = c
            _G.BTR_BIO_COL1 = c3ToTable(c)
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end)
    end
})

ProfileTab:AddButton({
    Name = "Pick Bio Color 2",
    Description = "Opens interactive Color Picker palette",
    Callback = function()
        openColorPickerGui("Pick Bio Color 2", bioCol2, function(c)
            bioCol2 = c
            _G.BTR_BIO_COL2 = c3ToTable(c)
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end)
    end
})

ProfileTab:AddButton({
    Name = "Pick Bio Color 3",
    Description = "Opens interactive Color Picker palette",
    Callback = function()
        openColorPickerGui("Pick Bio Color 3", bioCol3, function(c)
            bioCol3 = c
            _G.BTR_BIO_COL3 = c3ToTable(c)
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end)
    end
})

task.spawn(function()
    local tProg = 0
    while true do
        if isRainbowNameActive or isRainbowBioActive then
            pcall(function()
                local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                local colorRemote = RE and RE:FindFirstChild("1RPNam1eColo1r")
                if colorRemote then
                    tProg = (tProg + (rainbowSpeed * 0.008)) % 1000

                    if isRainbowNameActive then
                        local colName = getInterpolatedColor(nameColorMode, nameCol1, nameCol2, nameCol3, tProg)
                        colorRemote:FireServer("PickingRPNameColor", colName)
                    end

                    if isRainbowBioActive then
                        local colBio = getInterpolatedColor(bioColorMode, bioCol1, bioCol2, bioCol3, tProg)
                        colorRemote:FireServer("PickingRPBioColor", colBio)
                    end
                end
            end)
        end
        task.wait(0.08)
    end
end)
end

TrollTab:AddSection({
    "> Select Player"
})

local PropStateManager = {
    CurrentSession = 0,
    ActiveCategory = nil,
    IsBusy = false,
    LastToggleTime = 0
}

function PropStateManager.newSession(categoryName)
    PropStateManager.CurrentSession = PropStateManager.CurrentSession + 1
    PropStateManager.ActiveCategory = categoryName
    PropStateManager.LastToggleTime = tick()
    return PropStateManager.CurrentSession
end

function PropStateManager.isCurrent(sessionId)
    return PropStateManager.CurrentSession == sessionId
end

function PropStateManager.canToggle()
    local now = tick()
    if now - PropStateManager.LastToggleTime < 0.15 then
        return false 
    end
    PropStateManager.LastToggleTime = now
    return true
end

function PropStateManager.forceGlobalCleanup()
    pcall(function()
        local lp = game:GetService("Players").LocalPlayer
        local char = lp and lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then hum:UnequipTools() end
    end)
    pcall(function()
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        local clr = RE and RE:FindFirstChild("1Clea1rTool1s")
        if clr then
            clr:FireServer("ClearAllTools")
            clr:FireServer("ClosePropMenu")
        end
    end)
end

    
    
    
    
    
    local function cleanupPropMakerTool()
        local lp = game:GetService("Players").LocalPlayer
        if not lp then return end
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        pcall(function()
            local clr = RE and RE:FindFirstChild("1Clea1rTool1s")
            if clr then
                clr:FireServer("ClearAllTools")
                clr:FireServer("ClosePropMenu")
                clr:FireServer("ClosePropMenu")
            end
        end)
        pcall(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum then hum:UnequipTools() end
        end)
        pcall(function()
            local toolRemote = RE and RE:FindFirstChild("1Too1l")
            local tool = (lp.Character and lp.Character:FindFirstChild("PropMaker")) or (lp.Backpack and lp.Backpack:FindFirstChild("PropMaker"))
            if tool then
                if toolRemote then
                    pcall(function() toolRemote:InvokeServer("PickingTools", "PropMaker") end)
                end
                pcall(function() tool:Destroy() end)
            end
        end)
    end

local function getOrFetchPropMaker()
    local lp = game:GetService("Players").LocalPlayer
    if not lp then return nil end
    local backpack = lp:FindFirstChild("Backpack")
    local character = lp.Character
    if not backpack or not character then return nil end
    local tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
    if not tool then
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        local toolRemote = RE and RE:FindFirstChild("1Too1l")
        if toolRemote then
            pcall(function() toolRemote:InvokeServer("PickingTools", "PropMaker") end)
            local start = tick()
            while tick() - start < 1.5 do
                tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
                if tool then break end
                task.wait(0.03)
            end
        end
    end
    if tool and tool.Parent ~= character then
        local hum = character:FindFirstChildOfClass("Humanoid")
        if hum then
            hum:EquipTool(tool)
            local eqStart = tick()
            while tick() - eqStart < 1 do
                if tool.Parent == character then break end
                task.wait(0.03)
            end
        end
    end
    return tool
end

local function duplicateExistingProp(firstProp, countNeeded, delayBetween)
    if not firstProp then return false end
    local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 1.5)
    if not dupRemote then return false end

    for i = 1, countNeeded do
        pcall(function() dupRemote:InvokeServer() end)
        if delayBetween and delayBetween > 0 then
            task.wait(delayBetween)
        end
    end
    return true
end

local function spawnAndDuplicateProps(toolPropMake, spawnPos, totalCount, delayBetween)
    local lp = game:GetService("Players").LocalPlayer
    if not lp or not toolPropMake then return false end
    local myPropName = "Prop" .. lp.Name
    local wc = workspace:FindFirstChild("WorkspaceCom")
    if not wc then return false end

    local existingProps = {}
    for _, folder in pairs(wc:GetChildren()) do
        for _, prop in pairs(folder:GetChildren()) do
            if prop.Name == myPropName then
                existingProps[prop] = true
            end
        end
    end

    pcall(function() toolPropMake:FireServer(workspace.Terrain, spawnPos) end)

    local firstProp = nil
    local t0 = tick()
    while tick() - t0 < 3.0 do
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and not existingProps[prop] then
                    firstProp = prop
                    break
                end
            end
            if firstProp then break end
        end
        if firstProp then break end
        task.wait(0.05)
    end

    if not firstProp then return false end

    if totalCount and totalCount > 1 then
        return duplicateExistingProp(firstProp, totalCount - 1, delayBetween)
    end
    return true
end

-- [LOCKED: WORK PERFECTLY] BringSystem - Predictive Catch Engine

_G.TrollTarget = nil
_G.TrollTargetDest = "---"
_G.SelectedPropName = "FurnitureToilet"
_G.SelectedPropCategory = "Furniture"
_G.AutoLoopTroll = false
_G.BringMethod = "Smart"

_G.BringToiletCount = 1
local bringToiletCount = 1

BringSystem = {}
BringSystem.Active = false
BringSystem.Target = nil
BringSystem.SessionId = 0
BringSystem.StopCounter = 0

function BringSystem.stop()
    BringSystem.StopCounter = BringSystem.StopCounter + 1
    if BringSystem.Stopping then
        
        
        BringSystem.Active = false
        BringSystem.Target = nil
        return
    end
    BringSystem.Stopping = true
    BringSystem.Active = false
    BringSystem.Target = nil
    if BringSystem.CurrentToggle then
        local oldToggle = BringSystem.CurrentToggle
        BringSystem.CurrentToggle = nil
        pcall(function() oldToggle:Set(false) end)
    end
    local eu = game:GetService("Players").LocalPlayer
    if workspace.CurrentCamera and eu.Character and eu.Character:FindFirstChild("Humanoid") then
        workspace.CurrentCamera.CameraSubject = eu.Character.Humanoid
    end
    -- [FIX PROPS] Purge + retrait du carton (pattern LOCKED: ClearAllProps -> wait -> PickingTools)
    local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
    if RE then
        local clearRemote = RE:FindFirstChild("1Clea1rTool1s")
        if clearRemote then pcall(function() clearRemote:FireServer("ClearAllProps") end) end
        task.wait(0.2)
        local toolRemote = RE:FindFirstChild("1Too1l")
        
    end
    
    
    task.wait(0.1)
    local myChar = eu.Character
    local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")
    if myHum then pcall(function() myHum:UnequipTools() end) end
    local myRE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
    if myRE then
        local clr = myRE:FindFirstChild("1Clea1rTool1s")
        if clr then pcall(function() clr:FireServer("ClosePropMenu") end) end
    end
    cleanupPropMakerTool()
    
    if _G.AdminStopAll then pcall(_G.AdminStopAll) end
    BringSystem.Stopping = false
end

function BringSystem.bringPlayer(targetPlayer, actionType, noCamera, callerName, flySpeed)
    
    
    
    local stopCounterAtStart = BringSystem.StopCounter
    local spamGuard = 0
    while BringSystem.Stopping and spamGuard < 40 do
        task.wait(0.05)
        spamGuard = spamGuard + 1
    end
    
    
    if BringSystem.Stopping or BringSystem.StopCounter ~= stopCounterAtStart then return end
    if not targetPlayer or not targetPlayer.Character then return end
    if BringSystem.Active then
        BringSystem.Stopping = true
        BringSystem.Active = false
        BringSystem.Target = nil
        BringSystem.SessionId = BringSystem.SessionId + 1
        local RE2 = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        if RE2 then
            local ct2 = RE2:FindFirstChild("1Too1l")
            
        end
        cleanupPropMakerTool()
        BringSystem.Stopping = false
        task.wait(0.1)
    end
    BringSystem.SessionId = BringSystem.SessionId + 1
    local mySession = BringSystem.SessionId
    BringSystem.Active = true
    BringSystem.Target = targetPlayer
    BringSystem.CurrentAction = actionType
    BringSystem.FlySpeed = (tonumber(flySpeed) and tonumber(flySpeed) > 0) and tonumber(flySpeed) or 65.0

    local eu = game:GetService("Players").LocalPlayer
    local tchar = targetPlayer.Character
    local thum = tchar:FindFirstChild("Humanoid")
    local troot = tchar:FindFirstChild("HumanoidRootPart")
    if not thum or not troot then return end

    if not noCamera and not _G.AutoLoopTroll and actionType ~= "Fly" and workspace.CurrentCamera then
        workspace.CurrentCamera.CameraSubject = thum
    end

    local character = eu.Character or eu.CharacterAdded:Wait()
    local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
    local clearTools = RE:WaitForChild("1Clea1rTool1s")
    
    
    local propsTool = getOrFetchPropMaker()
    if not propsTool then
        BringSystem.stop()
        return
    end

    local humanoid = character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        humanoid:EquipTool(propsTool)
        local startEq = tick()
        while tick() - startEq < 2 do
            if propsTool.Parent == character then break end
            task.wait(0.05)
        end
    end

    pcall(function() clearTools:FireServer("OpenPropMenu") end)
    task.wait(0.2)

    local propName
    local propCategory = "Furniture"
    if actionType == "Fly" then
        propName = "FurnitureBeanBag"
    elseif callerName then
        
        propName = "FurnitureToilet"
        propCategory = "Furniture"
    else
        
        propName = _G.SelectedPropName or "FurnitureToilet"
        propCategory = _G.SelectedPropCategory or "Furniture"
    end
    pcall(function() clearTools:FireServer("RequestingPropName", propName, propCategory) end)
    task.wait(0.3)

    
    local toolPropMake = propsTool:FindFirstChild("Tool_PropMake")
    local waitStart = tick()
    while not toolPropMake and tick() - waitStart < 6 and BringSystem.SessionId == mySession do
        toolPropMake = propsTool:FindFirstChild("Tool_PropMake")
        if not toolPropMake then
            local bpTool = character.Parent and character.Parent:FindFirstChild("Backpack") and character.Parent.Backpack:FindFirstChild("PropMaker")
            if bpTool then toolPropMake = bpTool:FindFirstChild("Tool_PropMake") end
        end
        if not toolPropMake then task.wait(0.1) end
    end
    if toolPropMake then
        local myRoot = character:FindFirstChild("HumanoidRootPart")
        local myPos = myRoot and myRoot.Position or Vector3.new(0,0,0)
        local countToSpawn = math.clamp(_G.BringToiletCount or bringToiletCount or 1, 1, 15)

        local curChar = eu.Character
        local curHum = curChar and curChar:FindFirstChildOfClass("Humanoid")
        if curHum and propsTool.Parent ~= curChar then
            pcall(function() curHum:EquipTool(propsTool) end)
            task.wait(0.1)
        end

        local spawnPos = myPos + Vector3.new(0, 0, -2)
        local spawned = spawnAndDuplicateProps(toolPropMake, spawnPos, countToSpawn, 0)
        if not spawned then
            pcall(function() toolPropMake:FireServer(workspace.Terrain, spawnPos) end)
        end
    else
        return
    end

    
    if BringSystem.SessionId ~= mySession or not BringSystem.Active then return end

    
    task.spawn(function()
        pcall(function() local h = eu.Character and eu.Character:FindFirstChildOfClass("Humanoid"); if h then h:UnequipTools() end end)
        task.wait(1.0)
        pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker") end)
        cleanupPropMakerTool()
    end)

    task.spawn(function()
        local myPropName = "Prop" .. eu.Name
        local loopSession = mySession
        
        
        
        local bringSenders = {} 
        
        
        
        local bringSendHalted = false
        local function bringSendCF(scfRef, cf)
            if not BringSystem.Active or bringSendHalted then return end
            local entry = bringSenders[scfRef]
            if not entry then
                entry = { lastCF = nil, dirty = false }
                entry.thread = task.spawn(function()
                    while BringSystem.Active do
                        task.wait(0.025) 
                        if not BringSystem.Active then return end
                        if bringSendHalted then
                            
                            entry.dirty = false
                            entry.lastCF = nil
                        elseif entry.dirty and entry.lastCF then
                            entry.dirty = false
                            pcall(function() scfRef:InvokeServer(entry.lastCF) end)
                        end
                    end
                end)
                bringSenders[scfRef] = entry
            end
            entry.lastCF = cf
            entry.dirty = true
        end

        
        
        
        
        
        local function antiRollbackHammer(scfRef, destCF)
            local hammerStart = tick()
            while tick() - hammerStart < 0.6 do
                if not BringSystem.Active then return false end
                pcall(function() scfRef:InvokeServer(destCF) end)
                task.wait(0.025)
            end
            return true
        end

        
        
        
        
        local function bringTrackHammer(scfRef, getPosFn, duration)
            local hammerStart = tick()
            while tick() - hammerStart < (duration or 0.6) do
                if not BringSystem.Active then return false end
                local p = getPosFn()
                if p then pcall(function() scfRef:InvokeServer(p) end) end
                task.wait(0.025)
            end
            return true
        end

        
        local predictedVel = Vector3.new(0, 0, 0)
        local lastSamplePos = nil
        local lastSampleTime = tick()
        local phase1Props = nil
        local phase1PropsExpired = 0
        local flyPos = nil
        local flyHeading = nil
        local flyLastTick = nil
        local flySlot1Tool = nil
        local flySlot2Tool = nil
        local hasEverSeatedFly = false
        local flySenderThread = nil
        local flySenderThreadScf = nil
        local flySenderCF = nil

        while BringSystem.Active and BringSystem.Target == targetPlayer and BringSystem.SessionId == loopSession do
            repeat 
            local char = targetPlayer.Character
            local currentTargetRoot = char and char:FindFirstChild("HumanoidRootPart")
            local targetHum = char and char:FindFirstChild("Humanoid")

            if not currentTargetRoot or not targetHum or targetHum.Health <= 0 then
                if _G.AutoLoopTroll and actionType ~= "Fly" and targetPlayer:IsDescendantOf(game:GetService("Players")) then
                    local respawned = false
                    local startTime = os.clock()
                    while os.clock() - startTime < 15 do
                        task.wait(0.1)
                        if not targetPlayer:IsDescendantOf(game:GetService("Players")) then break end
                        if not _G.AutoLoopTroll then break end
                        local c = targetPlayer.Character
                        local h = c and c:FindFirstChild("Humanoid")
                        local r = c and c:FindFirstChild("HumanoidRootPart")
                        if c and h and r and h.Health > 0 then respawned = true break end
                    end
                    if respawned and _G.AutoLoopTroll and BringSystem.CurrentAction then
                        
                        task.wait(0.4)
                        task.spawn(function()
                            BringSystem.bringPlayer(targetPlayer, BringSystem.CurrentAction, noCamera, callerName, flySpeed)
                        end)
                        return
                    end
                else
                    if BringSystem.CurrentToggle then pcall(function() BringSystem.CurrentToggle:Set(false) end) end
                end
                break
            end

            local myRoot = eu.Character and eu.Character:FindFirstChild("HumanoidRootPart")
            if callerName then
                local cp = game:GetService("Players"):FindFirstChild(callerName)
                if cp and cp.Character and cp.Character:FindFirstChild("HumanoidRootPart") then
                    myRoot = cp.Character.HumanoidRootPart
                end
            end
            if not targetHum or not myRoot then break end

            
            if not (targetHum.Sit and targetHum.SeatPart and targetHum.SeatPart:FindFirstAncestor(myPropName)) then
                if actionType == "Fly" and hasEverSeatedFly then
                    pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                    if BringSystem.CurrentToggle then
                        pcall(function() BringSystem.CurrentToggle:Set(false) end)
                    end
                    BringSystem.stop()
                    break
                end
                
                if not phase1Props or phase1PropsExpired < tick() then
                    phase1Props = {}
                    pcall(function()
                        local wc = workspace:FindFirstChild("WorkspaceCom")
                        if wc then
                            for _, folder in pairs(wc:GetChildren()) do
                                for _, prop in pairs(folder:GetChildren()) do
                                    if prop.Name == myPropName then
                                        local scf = prop:FindFirstChild("SetCurrentCFrame")
                                        if scf then table.insert(phase1Props, scf) end
                                    end
                                end
                            end
                        end
                    end)
                    phase1PropsExpired = tick() + 0.5
                end
                local currentProps = phase1Props

                if #currentProps > 0 then
                
                
                
                local nowS = tick()
                if not lastSamplePos then
                    lastSamplePos = currentTargetRoot.Position
                    lastSampleTime = nowS
                else
                    local dtS = nowS - lastSampleTime
                    if dtS >= 0.03 then
                        local measuredVel = (currentTargetRoot.Position - lastSamplePos) / dtS
                        predictedVel = predictedVel:Lerp(measuredVel, 0.6)
                        lastSamplePos = currentTargetRoot.Position
                        lastSampleTime = nowS
                    end
                end
                
                
                
                
                
                local speed = predictedVel.Magnitude
                local leadTime = (speed > 40) and 0.35 or ((speed > 15) and 0.20 or 0.08)
                local feetOffset = 2.5
                if targetHum.HipHeight and targetHum.HipHeight > 0 then
                    feetOffset = targetHum.HipHeight + 0.8
                elseif targetHum.RigType == Enum.HumanoidRigType.R6 then
                    feetOffset = 3.0
                end
                local isAirborne = (targetHum:GetState() == Enum.HumanoidStateType.Jumping or targetHum:GetState() == Enum.HumanoidStateType.Freefall or math.abs(predictedVel.Y) > 5)

                for i, scf in ipairs(currentProps) do
                    local targetCF
                    if targetHum.Sit and targetHum.SeatPart then
                        targetCF = CFrame.new(targetHum.SeatPart.Position + Vector3.new(0, 0.5, 0))
                    elseif _G.SelectedPropName == "FuturisticLargeBed" then
                        
                        local forwardBackSwing = math.sin(tick() * 18) * 1.6
                        local targetLook = currentTargetRoot.CFrame.LookVector
                        local bedPos = currentTargetRoot.Position + (predictedVel * leadTime) + (targetLook * forwardBackSwing)
                        targetCF = CFrame.new(bedPos.X, currentTargetRoot.Position.Y - 0.3, bedPos.Z)
                    elseif #currentProps == 1 then
                        
                        local predicted3DPos = currentTargetRoot.Position + (predictedVel * leadTime)
                        if isAirborne then
                            targetCF = CFrame.new(predicted3DPos.X, predicted3DPos.Y - feetOffset, predicted3DPos.Z)
                        else
                            local rayParams = RaycastParams.new()
                            rayParams.FilterType = Enum.RaycastFilterType.Exclude
                            rayParams.FilterDescendantsInstances = {char}
                            local rayResult = workspace:Raycast(predicted3DPos, Vector3.new(0, -15, 0), rayParams)
                            local finalY = rayResult and (rayResult.Position.Y - 0.2) or (predicted3DPos.Y - feetOffset)
                            targetCF = CFrame.new(predicted3DPos.X, finalY, predicted3DPos.Z)
                        end
                    else
                        
                        local predicted3DPos = currentTargetRoot.Position + (predictedVel * leadTime)
                        local offset = Vector3.new(0, 0, 0)
                        if i > 1 then
                            local total = math.max(#currentProps, 2)
                            local phi = math.acos(1 - 2 * (i - 1) / (total - 1))
                            local theta = math.pi * (1 + math.sqrt(5)) * (i - 1)
                            local radius = 0.8
                            offset = Vector3.new(radius * math.sin(phi) * math.cos(theta), radius * math.cos(phi), radius * math.sin(phi) * math.sin(theta))
                        end
                        targetCF = CFrame.new(predicted3DPos + offset)
                    end
                    
                    
                    
                    bringSendCF(scf, targetCF)
                end
                task.wait(0.03)
                break 
            end
            end
            
            
            if actionType == "Fly" then
                if targetHum.Sit and targetHum.SeatPart then
                    hasEverSeatedFly = true
                    bringSendHalted = true
                    for _, e in pairs(bringSenders) do e.dirty = false end
                    local seat = targetHum.SeatPart
                    local propModel = seat and seat:FindFirstAncestor(myPropName)
                    if propModel then
                        local scf = propModel:FindFirstChild("SetCurrentCFrame")
                        if scf then
                            local now = tick()
                            local dt = math.clamp(now - (flyLastTick or now), 0.01, 0.1)
                            flyLastTick = now

                            if not flyPos then
                                flyPos = currentTargetRoot.Position
                                flyHeading = currentTargetRoot.Orientation.Y
                            end

                            local rawMove = targetHum.MoveDirection
                            local moveHoriz = Vector3.new(rawMove.X, 0, rawMove.Z)
                            local isMoving = moveHoriz.Magnitude > 0.05

                            local targetSpeed = (BringSystem and BringSystem.FlySpeed) or 65.0
                            if isMoving then
                                local moveDir = moveHoriz.Unit
                                flyPos = flyPos + (moveDir * (targetSpeed * dt))
                                flyHeading = math.deg(math.atan2(-moveDir.X, -moveDir.Z))
                            end

                            
                            
                            
                            
                            local bp = targetPlayer:FindFirstChildOfClass("Backpack")
                            local allTools = {}
                            if char then
                                for _, it in ipairs(char:GetChildren()) do
                                    if it:IsA("Tool") then table.insert(allTools, it) end
                                end
                            end
                            if bp then
                                for _, it in ipairs(bp:GetChildren()) do
                                    if it:IsA("Tool") then table.insert(allTools, it) end
                                end
                            end

                            if not flySlot1Tool and allTools[1] then flySlot1Tool = allTools[1] end
                            if not flySlot2Tool and allTools[2] then flySlot2Tool = allTools[2] end

                            local equippedTool = char and char:FindFirstChildOfClass("Tool")
                            local vertSpeed = 0
                            local climbSpeed = targetSpeed * (28.0 / 65.0)
                            if equippedTool then
                                if flySlot1Tool and equippedTool == flySlot1Tool then
                                    vertSpeed = climbSpeed
                                elseif flySlot2Tool and equippedTool == flySlot2Tool then
                                    vertSpeed = -climbSpeed
                                end
                            end

                            flyPos = flyPos + Vector3.new(0, vertSpeed * dt, 0)

                            local targetCF = CFrame.new(flyPos) * CFrame.Angles(0, math.rad(flyHeading + 180), 0)
                            if not flySenderThread or flySenderThreadScf ~= scf then
                                flySenderThreadScf = scf
                                flySenderCF = targetCF
                                flySenderThread = task.spawn(function()
                                    while BringSystem.Active and BringSystem.SessionId == loopSession and flySenderThreadScf == scf do
                                        if flySenderCF then
                                            local cfToSend = flySenderCF
                                            pcall(function() scf:InvokeServer(cfToSend) end)
                                        end
                                        task.wait(0.015)
                                    end
                                end)
                            else
                                flySenderCF = targetCF
                            end
                        end
                    end
                    task.wait(0.015)
                else
                    if hasEverSeatedFly then
                        pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                        if BringSystem.CurrentToggle then
                            pcall(function() BringSystem.CurrentToggle:Set(false) end)
                        end
                        BringSystem.stop()
                        break
                    end
                    flyPos = nil
                    flyHeading = nil
                    flyLastTick = nil
                    flySlot1Tool = nil
                    flySlot2Tool = nil
                end
            elseif targetHum.Sit and targetHum.SeatPart then
                local seat = targetHum.SeatPart
                local propModel = seat:FindFirstAncestor(myPropName)
                if propModel then
                    local scf = propModel:FindFirstChild("SetCurrentCFrame")
                    if scf then
                        if actionType == "Kill" then
                            
                            bringSendHalted = true
                            for _, e in pairs(bringSenders) do e.dirty = false end
                            antiRollbackHammer(scf, CFrame.new(0, -50000, 0))
                            task.wait(0.05)
                            pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                            if _G.AutoLoopTroll and BringSystem.CurrentAction then
                                local savedToggle = BringSystem.CurrentToggle
                                local savedAction = BringSystem.CurrentAction
                                task.spawn(function()
                                    local died = false
                                    for _ = 1, 50 do
                                        
                                        task.wait(0.3)
                                        if not _G.AutoLoopTroll then return end
                                        local ch = targetPlayer.Character
                                        if not ch or not ch:FindFirstChild("HumanoidRootPart") then died = true break end
                                        local h = ch:FindFirstChild("Humanoid")
                                        if h and h.Health <= 0 then died = true break end
                                    end
                                    if died and _G.AutoLoopTroll then
                                        local respawned = false
                                        local startTime = os.clock()
                                        while os.clock() - startTime < 15 do
                                            task.wait(0.1)
                                            if not targetPlayer:IsDescendantOf(game:GetService("Players")) then break end
                                            if not _G.AutoLoopTroll then break end
                                            local c = targetPlayer.Character
                                            local h = c and c:FindFirstChild("Humanoid")
                                            local r = c and c:FindFirstChild("HumanoidRootPart")
                                            if c and h and r and h.Health > 0 then respawned = true break end
                                        end
                                        if respawned and _G.AutoLoopTroll then
                                            local toggleStillOn = true
                                            if savedToggle then
                                                pcall(function() toggleStillOn = savedToggle:GetValue() end)
                                            else
                                                toggleStillOn = _G.AutoLoopTroll
                                            end
                                            if not toggleStillOn then return end
                                            task.wait(0.3)
                                            BringSystem.CurrentToggle = savedToggle
                                            BringSystem.CurrentAction = savedAction
                                            BringSystem.bringPlayer(targetPlayer, savedAction, noCamera, callerName)
                                        end
                                    end
                                end)
                                return 
                            else
                                if BringSystem.CurrentToggle then pcall(function() BringSystem.CurrentToggle:Set(false) end) end
                                break
                            end
                        elseif actionType == "Jail" then
                            
                            bringSendHalted = true
                            for _, e in pairs(bringSenders) do e.dirty = false end
                            antiRollbackHammer(scf, CFrame.new(-582, 10, 57))
                            if not _G.AutoLoopTroll then
                                pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                                BringSystem.Active = false
                                if BringSystem.CurrentToggle then pcall(function() BringSystem.CurrentToggle:Set(false) end) end
                                break
                            end
                        elseif actionType == "Cliff" then
                            
                            bringSendHalted = true
                            for _, e in pairs(bringSenders) do e.dirty = false end
                            antiRollbackHammer(scf, CFrame.new(-1796.89, -30.46, 106.37))
                            if not _G.AutoLoopTroll then
                                pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                                BringSystem.Active = false
                                if BringSystem.CurrentToggle then pcall(function() BringSystem.CurrentToggle:Set(false) end) end
                                break
                            end
                        elseif actionType == "JailV2" then
                            
                            bringSendHalted = true
                            for _, e in pairs(bringSenders) do e.dirty = false end
                            antiRollbackHammer(scf, CFrame.new(498.80, -18.21, 225.37))
                            if not _G.AutoLoopTroll then
                                pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                                BringSystem.Active = false
                                if BringSystem.CurrentToggle then pcall(function() BringSystem.CurrentToggle:Set(false) end) end
                                break
                            end
                        elseif actionType == "Freeze" then
                            
                            bringSendHalted = true
                            for _, e in pairs(bringSenders) do e.dirty = false end
                            antiRollbackHammer(scf, CFrame.new(21.03, 63.05, -479.78))
                            if not _G.AutoLoopTroll then
                                pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                                BringSystem.Active = false
                                if BringSystem.CurrentToggle then pcall(function() BringSystem.CurrentToggle:Set(false) end) end
                                break
                            end
                        elseif actionType == "Exile" then
                            
                            bringSendHalted = true
                            for _, e in pairs(bringSenders) do e.dirty = false end
                            antiRollbackHammer(scf, CFrame.new(999999, 999999, 999999))
                            if not _G.AutoLoopTroll then
                                pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                                BringSystem.Active = false
                                if BringSystem.CurrentToggle then pcall(function() BringSystem.CurrentToggle:Set(false) end) end
                                break
                            end
                        elseif actionType == "VoidGlitch" then
                            bringSendHalted = true
                            for _, e in pairs(bringSenders) do e.dirty = false end
                            antiRollbackHammer(scf, CFrame.new(869658880, 1528186880, 529643328))
                            if not _G.AutoLoopTroll then
                                pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                                BringSystem.Active = false
                                if BringSystem.CurrentToggle then pcall(function() BringSystem.CurrentToggle:Set(false) end) end
                                break
                            end
                        else
                            
                            
                            
                            
                            
                            bringSendHalted = true
                            for _, e in pairs(bringSenders) do e.dirty = false end
                            local targetCF = myRoot.CFrame * CFrame.new(0, 0, -4)
                            pcall(function() scf:InvokeServer(targetCF) end)
                            
                            bringSendHalted = false
                            if (currentTargetRoot.Position - myRoot.Position).Magnitude < 8 then
                                if not _G.AutoLoopTroll then
                                    pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
                                    BringSystem.Active = false
                                    if BringSystem.CurrentToggle then pcall(function() BringSystem.CurrentToggle:Set(false) end) end
                                    break
                                end
                            end
                        end
                    end
                end
            end 
            task.wait(0.015)
            until true 
        end
        BringSystem.stop()
    end)
end

-- [END LOCKED: WORK PERFECTLY] BringSystem

do
    local function getTrollPlayers()
        local list = {}
        for _, p in pairs(game:GetService("Players"):GetPlayers()) do
            if p ~= game:GetService("Players").LocalPlayer then
                table.insert(list, p.Name)
            end
        end
        return list
    end

    local trollDropdown = TrollTab:AddDropdown({
        Name = "Select Target",
        Description = "Choose the player to troll",
        Options = getTrollPlayers(),
        Default = "---",
        Callback = function(value)
            _G.TrollTarget = value
        end
    })

    local destDropdown = TrollTab:AddDropdown({
        Name = "Select TP Destination",
        Description = "Destination for TP actions",
        Options = getTrollPlayers(),
        Default = "---",
        Callback = function(value)
            _G.TrollTargetDest = value
        end
    })

    TrollTab:AddButton({
        Name = "Refresh Player List",
        Callback = function()
            local oldTarget = _G.TrollTarget
            local oldDest = _G.TrollTargetDest
            pcall(function() trollDropdown:Set(getTrollPlayers()) end)
            pcall(function() destDropdown:Set(getTrollPlayers()) end)
            if oldTarget and oldTarget ~= "---" then
                task.wait(0.1)
                pcall(function() trollDropdown:SetValue(oldTarget) end)
            end
            if oldDest and oldDest ~= "---" then
                task.wait(0.1)
                pcall(function() destDropdown:SetValue(oldDest) end)
            end
        end
    })

    
    TrollTab:AddDropdown({
        Name = "Select Prop for Actions",
        Description = "Choose the prop used to grab players",
        Options = {"Toilet", "BeanBag", "Bleachers", "Silver Throne", "Futuristic Large Bed"},
        Default = "Toilet",
        Callback = function(value)
            if value == "Toilet" then
                _G.SelectedPropName = "FurnitureToilet"
                _G.SelectedPropCategory = "Furniture"
            elseif value == "BeanBag" then
                _G.SelectedPropName = "FurnitureBeanBag"
                _G.SelectedPropCategory = "Furniture"
            elseif value == "Bleachers" then
                _G.SelectedPropName = "FurnitureBleachers"
                _G.SelectedPropCategory = "Furniture"
            elseif value == "Silver Throne" then
                _G.SelectedPropName = "FurnitureSilverThrone"
                _G.SelectedPropCategory = "Furniture"
            elseif value == "Futuristic Large Bed" then
                _G.SelectedPropName = "FuturisticLargeBed"
                _G.SelectedPropCategory = "Furniture"
            else
                _G.SelectedPropName = "FurnitureToilet"
                _G.SelectedPropCategory = "Furniture"
            end
        end
    })

    
    local function requireTarget()
        if not _G.TrollTarget or _G.TrollTarget == "---" then
            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Troll", Text="Select a target first!", Duration=3})
            return nil
        end
        local t = game:GetService("Players"):FindFirstChild(_G.TrollTarget)
        if not t then
            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Troll", Text="Target not found!", Duration=3})
            return nil
        end
        return t
    end

    
    
    
    local function stopOtherPropSystems()
        if _G.BTR_StopNoEscape then pcall(_G.BTR_StopNoEscape) end
        if _G.BTR_StopSkyLift then pcall(_G.BTR_StopSkyLift) end
    end

    
    
    
    local function startTrollAction(myToggle, action, targetPlayer, ...)
        if BringSystem.Active and BringSystem.CurrentToggle == myToggle then return end
        
        
        local extraArgs = { ... }
        task.spawn(function()
            if BringSystem.CurrentToggle and BringSystem.CurrentToggle ~= myToggle then
                BringSystem.stop()
            end
            stopOtherPropSystems()
            
            
            
            local guard = 0
            while (_G.BTR_NOESCAPE_STOPPING or _G.BTR_SKYLIFT_STOPPING) and guard < 30 do
                task.wait(0.1)
                guard = guard + 1
            end
            BringSystem.CurrentToggle = myToggle
            BringSystem.CurrentAction = action
            BringSystem.bringPlayer(targetPlayer, action, table.unpack(extraArgs))
        end)
    end

    _G.BringToiletToggle = TrollTab:AddToggle({
        Name = "Bring Target To Me <Prop>",
        Description = "Kidnap the target on a prop and bring them",
        Default = false
    })
    _G.BringToiletToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            local t = requireTarget()
            if not t then pcall(function() _G.BringToiletToggle:Set(false) end) return end
            startTrollAction(_G.BringToiletToggle, "Bring", t)
        else
            BringSystem.stop()
        end
    end)

    TrollTab:AddButton({
        Name = "Teleport Me To Target",
        Description = "Instantly teleports your character to the selected target",
        Callback = function()
            local t = requireTarget()
            if not t then return end
            pcall(function()
                local lp = game:GetService("Players").LocalPlayer
                local char = lp.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                local tHRP = t.Character and t.Character:FindFirstChild("HumanoidRootPart")
                if hrp and tHRP then
                    hrp.CFrame = tHRP.CFrame * CFrame.new(0, 0, 3)
                end
            end)
        end
    })

    _G.TpTargetToDestToggle = TrollTab:AddToggle({
        Name = "Teleport Target To Destination <Prop>",
        Description = "Teleports the target to the chosen destination",
        Default = false
    })
    _G.TpTargetToDestToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            if not _G.TrollTarget or _G.TrollTarget == "---" or not _G.TrollTargetDest or _G.TrollTargetDest == "---" then
                pcall(function() _G.TpTargetToDestToggle:Set(false) end)
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Troll", Text="Select target and destination!", Duration=3})
                return
            end
            local t1 = game:GetService("Players"):FindFirstChild(_G.TrollTarget)
            local t2 = game:GetService("Players"):FindFirstChild(_G.TrollTargetDest)
            if t1 and t2 then
                startTrollAction(_G.TpTargetToDestToggle, "Bring", t1, true, t2.Name)
            else
                pcall(function() _G.TpTargetToDestToggle:Set(false) end)
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Troll", Text="Target or destination not found!", Duration=3})
            end
        else
            BringSystem.stop()
        end
    end)

    _G.KillToiletToggle = TrollTab:AddToggle({
        Name = "Kill Player <Prop>",
        Description = "Force target on a prop and drop them into the void!",
        Default = false
    })
    _G.KillToiletToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            local t = requireTarget()
            if not t then pcall(function() _G.KillToiletToggle:Set(false) end) return end
            startTrollAction(_G.KillToiletToggle, "Kill", t)
        else
            BringSystem.stop()
        end
    end)

    _G.JailToiletToggle = TrollTab:AddToggle({
        Name = "Jail Player <Prop>",
        Description = "Locks the target in the prison cell!",
        Default = false
    })
    _G.JailToiletToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            local t = requireTarget()
            if not t then pcall(function() _G.JailToiletToggle:Set(false) end) return end
            startTrollAction(_G.JailToiletToggle, "Jail", t)
        else
            BringSystem.stop()
        end
    end)

    _G.FreezeToiletToggle = TrollTab:AddToggle({
        Name = "Freeze Player <Prop>",
        Description = "Freezes the target in a glitch spot!",
        Default = false
    })
    _G.FreezeToiletToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            local t = requireTarget()
            if not t then pcall(function() _G.FreezeToiletToggle:Set(false) end) return end
            startTrollAction(_G.FreezeToiletToggle, "Freeze", t)
        else
            BringSystem.stop()
        end
    end)

    _G.CliffToiletToggle = TrollTab:AddToggle({
        Name = "Void Choice <Prop>",
        Description = "Teleports the target to a cliff edge... jump or stay?",
        Default = false
    })
    _G.CliffToiletToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            local t = requireTarget()
            if not t then pcall(function() _G.CliffToiletToggle:Set(false) end) return end
            startTrollAction(_G.CliffToiletToggle, "Cliff", t)
        else
            BringSystem.stop()
        end
    end)

    _G.ExileToiletToggle = TrollTab:AddToggle({
        Name = "Void Player <Prop>",
        Description = "Exile the target to the void!",
        Default = false
    })
    _G.ExileToiletToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            local t = requireTarget()
            if not t then pcall(function() _G.ExileToiletToggle:Set(false) end) return end
            startTrollAction(_G.ExileToiletToggle, "Exile", t)
        else
            BringSystem.stop()
        end
    end)

    _G.VoidGlitchToiletToggle = TrollTab:AddToggle({
        Name = "Void Glitch Player <Prop>",
        Description = "Send the target to the glitch zone where everything vanishes",
        Default = false
    })
    _G.VoidGlitchToiletToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            local t = requireTarget()
            if not t then pcall(function() _G.VoidGlitchToiletToggle:Set(false) end) return end
            startTrollAction(_G.VoidGlitchToiletToggle, "VoidGlitch", t)
        else
            BringSystem.stop()
        end
    end)

    _G.NoclipToiletToggle = TrollTab:AddToggle({
        Name = "Fly Player <BeanBag>",
        Description = "WASD = Move | Slot 1 = Ascend | Slot 2 = Descend | Jump = Exit",
        Default = false
    })
    _G.NoclipToiletToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            local t = requireTarget()
            if not t then pcall(function() _G.NoclipToiletToggle:Set(false) end) return end
            startTrollAction(_G.NoclipToiletToggle, "Fly", t)
        else
            BringSystem.stop()
        end
    end)

    _G.TrollLoopToggle = TrollTab:AddToggle({
        Name = "Loop Actions (Keep Prop & Auto-Catch)",
        Description = "Keeps prop attached permanently; instantly re-attaches if target jumps or respawns",
        Default = false
    })
    _G.TrollLoopToggle:Callback(function(value)
        _G.AutoLoopTroll = value
    end)
end

do
    
    -- [LOCKED: WORK PERFECTLY] No Escape (grille de canapes 5x3) - valide par Keito
    
    local NE = {}
    NE.Active = false
    NE.Stopping = false
    NE.Target = nil
    NE.SessionId = 0
    NE.StopCounter = 0
    NE.Scfs = {}
    local neRGB = false 
    local NE_PROP_NAME = "FurnitureCouchBrown"
    local NE_PROP_CATEGORY = "Furniture"
    local NE_GRID_X = 5
    local NE_GRID_Z = 3
    local NE_SPACING = 6
    local NE_MODE = "Static" 

    local function neLog(m)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {Title = "No Escape", Text = m, Duration = 3})
        end)
    end

    NE.stop = function()
        -- [LOCKED: WORK PERFECTLY] NE.stop: ordre EXACT BringSystem.stop (ClearAllProps -> wait(1) -> PickingTools -> wait(0.1) -> Unequip -> ClosePropMenu -> cleanupPropMakerTool)
        -- [PATTERN BRING LOCKED] anti-double-stop guard (like BringSystem.stop)
        if NE.Stopping then
            NE.StopCounter = NE.StopCounter + 1
            NE.Active = false
            NE.Target = nil
            return
        end
        NE.Stopping = true
        NE.StopCounter = NE.StopCounter + 1
        NE.Active = false
        NE.Target = nil
        NE.Scfs = {}
        if _G.NoEscapeToggle then pcall(function() _G.NoEscapeToggle:Set(false) end) end
        task.spawn(function()
            -- [PATTERN PROPMAKER LOCKED] disable = ClearAllProps (not PickingTools)
            pcall(function()
                local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                local clearRemote = RE and RE:FindFirstChild("1Clea1rTool1s")
                if clearRemote then clearRemote:FireServer("ClearAllProps") end
            end)
            task.wait(1)
                
            local eu = game:GetService("Players").LocalPlayer
            local REb = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            if REb then
                local toolRemote = REb:FindFirstChild("1Too1l")
                
            end
            task.wait(0.1)
            local myChar = eu.Character
            local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")
            if myHum then pcall(function() myHum:UnequipTools() end) end
            local myRE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            if myRE then
                local clr = myRE:FindFirstChild("1Clea1rTool1s")
                if clr then pcall(function() clr:FireServer("ClosePropMenu") end) end
            end
            cleanupPropMakerTool()
            NE.Stopping = false
        end)
    end

    
    
    _G.BTR_StopNoEscape = function()
        
        task.spawn(function()
            _G.BTR_NOESCAPE_STOPPING = true
            NE.stop()
            
            
            local guard = 0
            while NE.Stopping and guard < 40 do
                task.wait(0.1)
                guard = guard + 1
            end
            _G.BTR_NOESCAPE_STOPPING = false
        end)
    end

    NE.start = function(targetPlayer)
        -- [LOCKED: WORK PERFECTLY] NE.start: sequence EXACT du Bring (anti-spam, equip, OpenPropMenu->RequestingPropName, grid autour de soi, retrait carton, senders)
        -- [PATTERN BRING LOCKED] anti-spam: wait for previous stop to finish
        local stopCounterAtStart = NE.StopCounter
        local spamGuard = 0
        while NE.Stopping and spamGuard < 40 do
            task.wait(0.05)
            spamGuard = spamGuard + 1
        end
        if NE.Stopping or NE.StopCounter ~= stopCounterAtStart then return end
        if NE.Active then
            NE.Stopping = true
            NE.Active = false
            NE.Target = nil
            NE.SessionId = NE.SessionId + 1
            NE.Stopping = false
            task.wait(0.15)
        end
        if not targetPlayer or not targetPlayer.Character then return end
        
        if BringSystem.Active and BringSystem.stop then pcall(function() BringSystem.stop() end) end
        NE.Active = true
        NE.Target = targetPlayer
        NE.Scfs = {}
        NE.SessionId = NE.SessionId + 1
        local mySession = NE.SessionId

        local lp = game:GetService("Players").LocalPlayer
        local char = lp.Character or lp.CharacterAdded:Wait()
        local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")

        local propsTool = getOrFetchPropMaker()
        if not propsTool then NE.stop() return end

        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum:EquipTool(propsTool)
            local eq = tick()
            while tick() - eq < 2 do
                if propsTool.Parent == char then break end
                task.wait(0.05)
            end
        end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
    task.wait(0.2)
        pcall(function() clearTools:FireServer("RequestingPropName", NE_PROP_NAME, NE_PROP_CATEGORY) end)
    task.wait(0.3)

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake")
        local ws = tick()
        while not toolPropMake and tick() - ws < 6 and NE.SessionId == mySession do
            toolPropMake = propsTool:FindFirstChild("Tool_PropMake")
            if not toolPropMake then
                local bpTool = char.Parent and char.Parent:FindFirstChild("Backpack") and char.Parent.Backpack:FindFirstChild("PropMaker")
                if bpTool then toolPropMake = bpTool:FindFirstChild("Tool_PropMake") end
            end
            if not toolPropMake then task.wait(0.1) end
        end

        if not toolPropMake then
            -- [RULE 7 LOCKED] silent exit: no notif, no forced stop
            NE.Active = false
            return
        end

        local tRoot = targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")
        if not tRoot then NE.stop() return end
        -- [PATTERN BRING LOCKED] place the grid around OURSELVES (like Bring: myPos + offset)
        
        
        local myRoot = char:FindFirstChild("HumanoidRootPart")
        if not myRoot then NE.stop() return end
        local center = myRoot.Position
        -- [NO ESCAPE LOCKED] ground reference (locked Y): computed BEFORE placing the
        
        local neGroundY = center.Y - 2.5
        do
            local rayParamsInit = RaycastParams.new()
            rayParamsInit.FilterType = Enum.RaycastFilterType.Exclude
            rayParamsInit.FilterDescendantsInstances = { char }
            local rayInit = workspace:Raycast(center + Vector3.new(0, 3, 0), Vector3.new(0, -30, 0), rayParamsInit)
            if rayInit then neGroundY = rayInit.Position.Y end
        end

        local totalCouches = NE_GRID_X * NE_GRID_Z
        local duplicated = spawnAndDuplicateProps(toolPropMake, center, totalCouches, 0)
        if not duplicated then
            for iz = 0, NE_GRID_Z - 1 do
                for ix = 0, NE_GRID_X - 1 do
                    if not NE.Active or NE.SessionId ~= mySession then return end
                    local ox = (ix - (NE_GRID_X - 1) / 2) * NE_SPACING
                    local oz = (iz - (NE_GRID_Z - 1) / 2) * NE_SPACING
                    local pos = center + Vector3.new(ox, 0, oz)
                    pcall(function() toolPropMake:FireServer(workspace.Terrain, pos) end)
                    task.wait(0.85)
                end
            end
        end

        
        task.spawn(function()
            pcall(function() local h = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid"); if h then h:UnequipTools() end end)
            task.wait(0.85)
            pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker") end)
            cleanupPropMakerTool()
        end)

        -- [PATTERN BRING LOCKED] placed prop name: "Prop<PlayerName>"
        local myPropName = "Prop" .. lp.Name

        -- [PATTERN BRING LOCKED] latest-wins senders: 1 thread per prop, 40 req/s
        local senders = {}
        local function neSendCF(scfRef, cf)
            if not NE.Active or NE.SessionId ~= mySession then return end
            local entry = senders[scfRef]
            if not entry then
                entry = { lastCF = nil, dirty = false }
                entry.thread = task.spawn(function()
                    while NE.Active and NE.SessionId == mySession do
                        task.wait(0.025)
                        if not NE.Active or NE.SessionId ~= mySession then return end
                        if entry.dirty and entry.lastCF then
                            entry.dirty = false
                            pcall(function() scfRef:InvokeServer(entry.lastCF) end)
                        end
                    end
                end)
                senders[scfRef] = entry
            end
            entry.lastCF = cf
            entry.dirty = true
        end
        -- [PATTERN BRING LOCKED] EXACT structural copy of BringSystem:
        
        
        local loopSession = mySession
        local predictedVel = Vector3.new(0, 0, 0)
        local lastSamplePos = nil
        local lastSampleTime = tick()
        local phase1Props = nil
        local phase1PropsExpired = 0
        local announced = false

        
        
        
        
        
        
        task.spawn(function()
            while NE.Active and NE.SessionId == mySession do
                task.wait(0.1)
                if not NE.Active or NE.SessionId ~= mySession then return end
                if neRGB then
                    local hue = (tick() * 0.3) % 1
                    local col = Color3.fromHSV(hue, 1, 1)
                    local props = phase1Props or {}
                    for _, e in ipairs(props) do
                        if not NE.Active or NE.SessionId ~= mySession then return end
                        if e.cpc then
                            pcall(function() e.cpc:InvokeServer(col) end)
                        end
                    end
                end
            end
        end)

        local targetPlayerName = targetPlayer.Name
        task.spawn(function()
            while NE.Active and NE.SessionId == loopSession do
                repeat 
                    local Players = game:GetService("Players")
                    local curTarget = Players:FindFirstChild(targetPlayerName)
                    if not curTarget then
                        
                        task.wait(0.5)
                        break
                    end
                    targetPlayer = curTarget
                    NE.Target = curTarget

                    local char = targetPlayer.Character
                    local currentTargetRoot = char and char:FindFirstChild("HumanoidRootPart")
                    local targetHum = char and char:FindFirstChildOfClass("Humanoid")

                    if not currentTargetRoot or not targetHum or targetHum.Health <= 0 then
                        
                        task.wait(0.15)
                        break
                    end

                    -- [NO ESCAPE LOCKED] FREEZE: if the target is sitting on one of OUR
                    
                    if targetHum.Sit and targetHum.SeatPart and targetHum.SeatPart:FindFirstAncestor(myPropName) then
                        task.wait(0.05)
                        break 
                    end

                    -- [PATTERN BRING LOCKED] prop cache: scan WorkspaceCom 2x/s max
                    if not phase1Props or phase1PropsExpired < tick() then
                        phase1Props = {}
                        pcall(function()
                            local wc = workspace:FindFirstChild("WorkspaceCom")
                            if wc then
                                for _, folder in pairs(wc:GetChildren()) do
                                    for _, prop in pairs(folder:GetChildren()) do
                                        if prop.Name == myPropName then
                                            local scf = prop:FindFirstChild("SetCurrentCFrame")
                                            local cpc = prop:FindFirstChild("ChangePropColor")
                                            if scf then table.insert(phase1Props, {scf = scf, cpc = cpc}) end
                                        end
                                    end
                                end
                            end
                        end)
                        phase1PropsExpired = tick() + 0.5
                        if not announced and #phase1Props > 0 then
                            announced = true
                            neLog("Grid of " .. math.min(#phase1Props, NE_GRID_X * NE_GRID_Z) .. " couches active")
                        end
                    end
                    local currentProps = phase1Props

                    if #currentProps > 0 then
                        
                        
                        
                        
                        local tNow = tick()
                        local propCount = math.max(#currentProps, 1)
                        for i, entry in ipairs(currentProps) do
                            if not NE.Active or NE.SessionId ~= loopSession then break end
                            local scf = entry.scf
                            if scf then
                                
                                
                                
                                
                                local px
                                local py
                                local pz
                                if NE_MODE == "Ultra Strong" then
                                    
                                    
                                    
                                    
                                    
                                    
                                    
                                    
                                    local tC = propCount
                                    local padN = math.max(math.floor(tC * 0.6), 1)
                                    local ringN = math.max(math.floor(tC * 0.267), 1)
                                    local lx = currentTargetRoot.Position.X
                                    local lz = currentTargetRoot.Position.Z
                                    local ly = currentTargetRoot.Position.Y
                                    if i <= padN then
                                        local ggx = (i - 1) % 3
                                        local ggz = math.floor((i - 1) / 3)
                                        px = lx + (ggx - 1) * 4.5
                                        pz = lz + (ggz - 1) * 4.5
                                        py = ly - 2.5
                                    elseif i <= padN + ringN then
                                        local dir = i - padN - 1
                                        local ang = dir * (math.pi / 2)
                                        px = lx + math.cos(ang) * 2.2
                                        pz = lz + math.sin(ang) * 2.2
                                        py = ly - 0.5
                                    else
                                        local s = (i - padN - ringN) == 1 and -1 or 1
                                        px = lx + s * 2.8
                                        pz = lz
                                        py = ly + 3.0
                                    end
                                elseif NE_MODE == "Static" then
                                    
                                    
                                    
                                    
                                    
                                    local gx = (i - 1) % NE_GRID_X
                                    local gz = math.floor((i - 1) / NE_GRID_X)
                                    px = currentTargetRoot.Position.X + (gx - (NE_GRID_X - 1) / 2) * 5
                                    pz = currentTargetRoot.Position.Z + (gz - (NE_GRID_Z - 1) / 2) * 5
                                    py = currentTargetRoot.Position.Y - 2.5
                                else
                                    
                                    local aMode = (i / propCount) * (2 * math.pi) + tNow * 6
                                    px = currentTargetRoot.Position.X + math.cos(aMode) * 2.5
                                    pz = currentTargetRoot.Position.Z + math.sin(aMode) * 2.5
                                    py = currentTargetRoot.Position.Y - 2.5
                                end
                                task.spawn(function()
                                    pcall(function() scf:InvokeServer(CFrame.new(px, py, pz)) end)
                                end)
                            end
                        end
                        task.wait(0.08)
                        break 
                    end
                    task.wait(0.015)
                until true
            end
            -- [PATTERN BRING LOCKED] end of loop: clean stop (like BringSystem.stop())
            NE.stop()
        end)
    end

    
    TrollV2Tab:AddSection({"> Troll V2"})

    
    local function getTV2Players()
        local list = {}
        local lp = game:GetService("Players").LocalPlayer
        for _, p in pairs(game:GetService("Players"):GetPlayers()) do
            if p ~= lp then table.insert(list, p.Name) end
        end
        return list
    end
    local tv2Dropdown = TrollV2Tab:AddDropdown({
        Name = "Select Target",
        Description = "Choose the player to trap",
        Options = getTV2Players(),
        Default = "---",
        Callback = function(value)
            _G.TrollTarget = value
        end
    })
    TrollV2Tab:AddButton({
        Name = "Refresh Player List",
        Callback = function()
            local old = _G.TrollTarget
            pcall(function() tv2Dropdown:Set(getTV2Players()) end)
            if old and old ~= "---" then
                task.wait(0.1)
                pcall(function() tv2Dropdown:SetValue(old) end)
            end
        end
    })

    -- [LOCKED: WORK PERFECTLY] NoEscape toggle + callback (valide par Keito)
    _G.NoEscapeToggle = TrollV2Tab:AddToggle({
        Name = "No Escape <Couch>",
        Description = "Grid of 15 couches that sticks to the target: no escape possible",
        Default = false
    })
    _G.NoEscapeToggle:Callback(function(value)
        if not _G.BTR_UI_READY then return end
        if value then
            
            if not _G.TrollTarget or _G.TrollTarget == "---" then
                pcall(function() _G.NoEscapeToggle:Set(false) end)
                game:GetService("StarterGui"):SetCore("SendNotification", { Title = "No Escape", Text = "Select a target first!", Duration = 3 })
                return
            end
            local t = game:GetService("Players"):FindFirstChild(_G.TrollTarget)
            if not t then
                pcall(function() _G.NoEscapeToggle:Set(false) end)
                game:GetService("StarterGui"):SetCore("SendNotification", { Title = "No Escape", Text = "Target not found!", Duration = 3 })
                return
            end
            
            
            if _G.BTR_StopSkyLift then pcall(_G.BTR_StopSkyLift) end

            
            task.spawn(function()
                
                
                if BringSystem.Active then
                    pcall(function() BringSystem.stop() end)
                    local bg = 0
                    while BringSystem.Stopping and bg < 60 do
                        task.wait(0.1)
                        bg = bg + 1
                    end
                end
                NE.start(t)
            end)
        else
            NE.stop()
        end
    end)

    _G.BTR_StartNoEscape = function(targetPlayer)
        if not targetPlayer then return end
        NE_PROP_NAME = "FurnitureCouchBrown"
        neRGB = false
        if _G.BTR_StopSkyLift then pcall(_G.BTR_StopSkyLift) end
        task.spawn(function()
            if BringSystem.Active then
                pcall(function() BringSystem.stop() end)
                local bg = 0
                while BringSystem.Stopping and bg < 60 do
                    task.wait(0.1)
                    bg = bg + 1
                end
            end
            NE.start(targetPlayer)
        end)
    end
    _G.BTR_StopNoEscape = NE.stop

    
    TrollV2Tab:AddDropdown({
        Name = "No Escape Prop",
        Description = "Choose the prop for the No Escape grid (Couch or Bed)",
        Options = {"Couch", "Futuristic Large Bed"},
        Default = "Couch",
        Callback = function(val)
            if val == "Couch" then
                NE_PROP_NAME = "FurnitureCouchBrown"
            elseif val == "Futuristic Large Bed" then
                NE_PROP_NAME = "FuturisticLargeBed"
            end
            warn("[NO ESCAPE] Prop -> " .. tostring(NE_PROP_NAME))
        end
    })

    
    TrollV2Tab:AddDropdown({
        Name = "No Escape Mode",
        Description = "Positioning: Trailing (rotating trail) / Ultra Strong (stacked on him) / Static (fixed formation)",
        Options = {"Trail", "Ultra Strong", "Static"},
        Default = "Static",
        Callback = function(val)
            NE_MODE = val
            warn("[NO ESCAPE] Mode -> " .. tostring(NE_MODE))
        end
    })

    
    TrollV2Tab:AddToggle({
        Name = "RGB",
        Description = "Rainbow colors on the couch grid (cycles HSV)",
        Default = false,
        Callback = function(val)
            neRGB = val
            if val then
                warn("[NO ESCAPE] RGB enabled")
            else
                warn("[NO ESCAPE] RGB disabled")
            end
        end
    })
end

do
    local blindColorMode = "White"
    local blindPropName = "LightsSpotLightWhite"
    local blindColorValue = Color3.fromRGB(255, 255, 255)
    local blindV1Toggle = nil
    local blindV2Toggle = nil
    local blindV3Toggle = nil
    local blindV4Toggle = nil
    local blindV5Toggle = nil
    local blindV6Toggle = nil
    local blindV7Toggle = nil
    local blindRainbowToggle = nil
    local blindRainbowProp = "FuturisticLargeBed"
    local isBlindLoopEnabled = true
    local blindLoopRunning = false
    local blindLockedTarget = nil
    local blindPropEntries = {}

    TrollTab:AddSection({"> Blind & Specials"})

    local blindColorPresets = {
        ["White"] = {prop = "LightsSpotLightWhite", color = Color3.fromRGB(255, 255, 255)},
        ["Black"] = {prop = "LightsSpotLightWhite", color = Color3.fromRGB(0, 0, 0)},
        ["Light Blue"] = {prop = "LightsSpotLightToothpaste", color = Color3.fromRGB(0, 170, 255)},
        ["Red"] = {prop = "LightsSpotLightRed", color = Color3.fromRGB(255, 0, 0)},
        ["Pink"] = {prop = "LightsSpotLightPink", color = Color3.fromRGB(255, 105, 180)},
        ["Green"] = {prop = "LightsSpotLightGreen", color = Color3.fromRGB(0, 255, 0)},
    }

    TrollTab:AddDropdown({
        Name = "Blind Color",
        Description = "Laser/cocoon color (White or Black)",
        Options = {"White", "Black", "Light Blue", "Red", "Pink", "Green", "RGB (Rainbow)"},
        Default = "White",
        Callback = function(value)
            blindColorMode = value
            if value == "RGB (Rainbow)" then
                blindPropName = "RGB"
                blindColorValue = nil
            else
                local preset = blindColorPresets[value] or blindColorPresets["White"]
                blindPropName = preset.prop
                blindColorValue = preset.color
            end
        end
    })

    TrollTab:AddToggle({
        Name = "Blind Loop (Re-lock after rejoin)",
        Description = "If active, waits for the target to come back if they leave",
        Default = true,
        Callback = function(Value)
            isBlindLoopEnabled = Value
        end
    })

    local function blindStart(lockedName, mode)
        local Players = game:GetService("Players")
        local lp = Players.LocalPlayer
        local char = lp.Character
        local humanoid = char and char:FindFirstChildOfClass("Humanoid")
        if not char or not humanoid then return false end

        blindLockedTarget = lockedName
        _G.TrollLockedTarget = lockedName
        blindPropEntries = {}

        local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")

        local propsTool = getOrFetchPropMaker()
        if not propsTool then
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Blind Player", Text = "Could not get PropMaker!", Duration = 4
            })
            return false
        end

        humanoid:EquipTool(propsTool)
        task.wait(0.3)

        local isFurnitureMode = (mode == "RainbowBed")
        local reqProp = isFurnitureMode and blindRainbowProp or blindPropName
        local reqCategory = isFurnitureMode and "Furniture" or "Lights"

        pcall(function()
            clearTools:FireServer("OpenPropMenu")
            task.wait(0.1)
            if not isFurnitureMode and blindPropName == "RGB" then
                
            else
                clearTools:FireServer("RequestingPropName", reqProp, reqCategory)
            end
        end)

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
        if not toolPropMake then return false end 

        local myPos = char.HumanoidRootPart.Position
        local placedPositions = {}
        local MIN_DIST = 4
        local MIN_RADIUS = 3
        local MAX_RADIUS = 9
        local INTERVAL = 0.85

        local function getRandomPos()
            for _ = 1, 20 do
                local angle = math.random() * 2 * math.pi
                local radius = MIN_RADIUS + math.random() * (MAX_RADIUS - MIN_RADIUS)
                local candidate = myPos + Vector3.new(math.cos(angle) * radius, 0, math.sin(angle) * radius)
                local tooClose = false
                for _, pos in pairs(placedPositions) do
                    if (candidate - pos).Magnitude < MIN_DIST then tooClose = true break end
                end
                if not tooClose then return candidate end
            end
            local angle = math.random() * 2 * math.pi
            return myPos + Vector3.new(math.cos(angle) * MAX_RADIUS, 0, math.sin(angle) * MAX_RADIUS)
        end

        local rgbColorsList = {"LightsSpotLightRed", "LightsSpotLightToothpaste", "LightsSpotLightPink", "LightsSpotLightGreen", "LightsSpotLightWhite"}

        if not isFurnitureMode and blindPropName == "RGB" then
            for i = 1, 15 do
                if not propsTool or not propsTool.Parent or propsTool.Parent ~= char then break end
                local rgbProp = rgbColorsList[(i % #rgbColorsList) + 1]
                pcall(function() clearTools:FireServer("RequestingPropName", rgbProp, "Lights") end)
                task.wait(0.05)
                local pos = getRandomPos()
                pcall(function() toolPropMake:FireServer(workspace.Terrain, pos) end)
                table.insert(placedPositions, pos)
                if i < 15 then task.wait(INTERVAL - 0.05) end
            end
        else
            local startPos = getRandomPos()
            local duplicated = spawnAndDuplicateProps(toolPropMake, startPos, 15, 0)
            if not duplicated then
                for i = 1, 15 do
                    if not propsTool or not propsTool.Parent or propsTool.Parent ~= char then break end
                    local pos = getRandomPos()
                    pcall(function() toolPropMake:FireServer(workspace.Terrain, pos) end)
                    table.insert(placedPositions, pos)
                    if i < 15 then task.wait(INTERVAL) end
                end
            else
                local label = isFurnitureMode and ((blindRainbowProp == "FurnitureBleachers") and "Bleachers placed: 15/15" or "Beds placed: 15/15") or "Lights placed: 15/15"
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Blind Player", Text = label, Duration = 2
                })
            end
        end

        
        pcall(function() local h = char and char:FindFirstChildOfClass("Humanoid"); if h then h:UnequipTools() end end)
        task.spawn(function()
            task.wait(1.0)
            pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker") end)
            task.wait(0.1)
            pcall(function() clearTools:FireServer("ClearAllTools") end)
            task.wait(0.1)
            pcall(function() clearTools:FireServer("ClosePropMenu") end)
            cleanupPropMakerTool()
        end)

        
        blindLoopRunning = true
        task.spawn(function()
            local myPropName = "Prop" .. lp.Name
            local t0 = tick()
            local lastColorPropIdx = 0
            while blindLoopRunning do
                local targetPlayer = game:GetService("Players"):FindFirstChild(blindLockedTarget)
                local hrp = targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")

                if not targetPlayer and not isBlindLoopEnabled then
                    
                    
                    blindLoopRunning = false
                    clearBlindProps()
                    break
                end

                local targetPos = nil
                if hrp then
                    targetPos = hrp.Position
                elseif isBlindLoopEnabled then
                    local char = targetPlayer and targetPlayer.Character
                    local hum = char and char:FindFirstChildOfClass("Humanoid")
                    if not char or not hum or hum.Health <= 0 then
                        task.wait(0.5)
                    end
                end

                if targetPos then
                    local currentProps = {}
                    local wc = workspace:FindFirstChild("WorkspaceCom")
                    if wc then
                        for _, folder in pairs(wc:GetChildren()) do
                            for _, prop in pairs(folder:GetChildren()) do
                                if prop.Name == myPropName then
                                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                                    local cpc = prop:FindFirstChild("ChangePropColor")
                                    if scf then
                                        local entry = blindPropEntries[scf]
                                        if not entry then
                                            entry = {
                                                scf = scf,
                                                cpc = cpc,
                                                prop = prop,
                                                scfInFlight = false,
                                                cpcInFlight = false,
                                                colorApplied = false,
                                                lastColorTime = 0
                                            }
                                            blindPropEntries[scf] = entry
                                        end
                                        table.insert(currentProps, entry)
                                    end
                                end
                            end
                        end
                    end

                    local t = tick() - t0
                    local propCount = math.max(#currentProps, 1)

                    for i, entry in pairs(currentProps) do
                        if not blindLoopRunning then break end
                        local targetCF

                        if mode == "V1" then
                            
                            local angle = (i / propCount) * (2 * math.pi)
                            local offset = Vector3.new(math.cos(angle) * 0.5, -2.5, math.sin(angle) * 0.5)
                            targetCF = CFrame.new(targetPos + offset)

                        elseif mode == "V2" then
                            
                            local angle = (i / propCount) * (2 * math.pi)
                            local yOffset = -2.5 + ((i - 1) / math.max(propCount - 1, 1)) * 4.0
                            local offset = Vector3.new(math.cos(angle) * 0.5, yOffset, math.sin(angle) * 0.5)
                            targetCF = CFrame.new(targetPos + offset)

                        elseif mode == "V3" then
                            
                            local phi = math.acos(-1 + (2 * (i - 1)) / propCount)
                            local theta = math.sqrt(propCount * math.pi) * phi + t * 9
                            local r = 0.9 
                            local x = r * math.sin(phi) * math.cos(theta)
                            local y = r * math.sin(phi) * math.sin(theta) + 0.8
                            local z = r * math.cos(phi)
                            targetCF = CFrame.new(targetPos + Vector3.new(x, y, z), targetPos + Vector3.new(0, 0.8, 0))

                        elseif mode == "V4" then
                            
                            local isSecondHelix = (i % 2 == 0)
                            local baseAngle = (i / propCount) * (4 * math.pi) + (isSecondHelix and (t * 12 + math.pi) or (-t * 12))
                            local yProgress = ((i - 1) / math.max(propCount - 1, 1)) * 3.2 - 1.0
                            local r = 1.0 + math.sin(t * 8 + yProgress) * 0.3
                            local offset = Vector3.new(math.cos(baseAngle) * r, yProgress + 0.5, math.sin(baseAngle) * r)
                            targetCF = CFrame.new(targetPos + offset, targetPos + Vector3.new(0, yProgress + 0.5, 0))

                        elseif mode == "V5" then
                            
                            local angle = (i / propCount) * (2 * math.pi) + t * 16
                            local jitterX = math.sin(t * 50 + i) * 0.5
                            local jitterY = math.cos(t * 45 + i) * 0.5
                            local r = 0.7 + math.sin(t * 20 + i) * 0.3
                            local offset = Vector3.new(math.cos(angle) * r + jitterX, 0.8 + jitterY, math.sin(angle) * r)
                            targetCF = CFrame.new(targetPos + offset, targetPos + Vector3.new(0, 0.8, 0))

                        elseif mode == "V6" then
                            
                            
                            local pulse = (math.sin(t * 10) + 1) * 0.5
                            local r = 0.25 + pulse * 3.4
                            local phi = math.acos(-1 + (2 * (i - 1)) / propCount)
                            local theta = (i * 2.39996) + t * 14
                            local x = r * math.sin(phi) * math.cos(theta)
                            local y = r * math.sin(phi) * math.sin(theta) + 0.5
                            local z = r * math.cos(phi)
                            targetCF = CFrame.new(targetPos + Vector3.new(x, y, z), targetPos + Vector3.new(0, 0.5, 0))

                        elseif mode == "V7" then
                            
                            
                            local tier = (i - 1) % 3
                            local tierIdx = math.floor((i - 1) / 3)
                            local tierCount = math.max(math.floor(propCount / 3), 1)
                            local baseAng = (tierIdx / tierCount) * (2 * math.pi)
                            local r = 1.6 + math.sin(t * 6 + tier) * 0.4
                            local posOff = Vector3.new(0, 0.5, 0)
                            if tier == 0 then
                                
                                local ang = baseAng + t * 10
                                posOff = Vector3.new(math.cos(ang) * r, 0.5, math.sin(ang) * r)
                            elseif tier == 1 then
                                
                                local ang = baseAng - t * 8
                                posOff = Vector3.new(0, 0.5 + math.cos(ang) * r, math.sin(ang) * r)
                            else
                                
                                local ang = baseAng + t * 12
                                local diagX = math.cos(ang) * r * 0.707
                                local diagY = math.cos(ang) * r * 0.707 + 0.5
                                local diagZ = math.sin(ang) * r
                                posOff = Vector3.new(diagX, diagY, diagZ)
                            end
                            targetCF = CFrame.new(targetPos + posOff, targetPos + Vector3.new(0, 0.5, 0))

                        elseif mode == "RainbowBed" then
                            
                            
                            local tier = (i - 1) % 3
                            local tierIdx = math.floor((i - 1) / 3)
                            local tierCount = math.max(math.floor(propCount / 3), 1)
                            local baseAng = (tierIdx / tierCount) * (2 * math.pi)

                            local r = (blindRainbowProp == "FurnitureBleachers") and 4.0 or 3.0
                            local yOff = (tier == 0) and 0.5 or ((tier == 1) and 3.2 or -1.8)
                            local rotSpeed = (tier == 1) and -4.5 or 5.2
                            local ang = baseAng + t * rotSpeed

                            local posOff = Vector3.new(math.cos(ang) * r, yOff, math.sin(ang) * r)
                            local targetLook = targetPos + Vector3.new(0, 0.5, 0)
                            targetCF = CFrame.new(targetPos + posOff, targetLook)

                        else
                            targetCF = CFrame.new(targetPos)
                        end

                        if not entry.scfInFlight then
                            entry.scfInFlight = true
                            task.spawn(function()
                                pcall(function() entry.scf:InvokeServer(targetCF) end)
                                entry.scfInFlight = false
                            end)
                        end
                    end

                    
                    if #currentProps > 0 then
                        if mode == "RainbowBed" or blindColorMode == "RGB (Rainbow)" then
                            
                            lastColorPropIdx = (lastColorPropIdx % #currentProps) + 1
                            local colEntry = currentProps[lastColorPropIdx]
                            if colEntry and colEntry.cpc and not colEntry.cpcInFlight then
                                local now = tick()
                                if not colEntry.colorApplied or (now - colEntry.lastColorTime >= 0.25) then
                                    colEntry.cpcInFlight = true
                                    colEntry.lastColorTime = now
                                    colEntry.colorApplied = true
                                    local hue = (t * 0.4 + (lastColorPropIdx / #currentProps)) % 1
                                    local rainbowCol = Color3.fromHSV(hue, 1, 1)
                                    task.spawn(function()
                                        pcall(function() colEntry.cpc:InvokeServer(rainbowCol) end)
                                        colEntry.cpcInFlight = false
                                    end)
                                end
                            end
                        elseif blindColorValue then
                            
                            for _, colEntry in ipairs(currentProps) do
                                if colEntry.cpc and not colEntry.colorApplied and not colEntry.cpcInFlight then
                                    colEntry.cpcInFlight = true
                                    local staticCol = blindColorValue
                                    task.spawn(function()
                                        pcall(function() colEntry.cpc:InvokeServer(staticCol) end)
                                        colEntry.colorApplied = true
                                        colEntry.cpcInFlight = false
                                    end)
                                end
                            end
                        end
                    end
                end

                task.wait(0.08)
            end
        end)
        return true
    end

    
    
    local function clearBlindProps()
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        if RE then
            local clearTools = RE:FindFirstChild("1Clea1rTool1s")
            if clearTools then
                pcall(function() clearTools:FireServer("ClearAllProps") end)
            end
        end
    end

    local function blindStop()
        blindLoopRunning = false
        _G.TrollLockedTarget = nil
        blindPropEntries = {}
        clearBlindProps()
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        if RE then
            local clearTools = RE:FindFirstChild("1Clea1rTool1s")
            local toolRemote = RE:FindFirstChild("1Too1l")
            if clearTools then
                
                task.wait(0.1)
                pcall(function() clearTools:FireServer("ClearAllTools") end)
                task.wait(0.1)
                pcall(function() clearTools:FireServer("ClosePropMenu") end)
            end
        end
        local lp = game:GetService("Players").LocalPlayer
        local char = lp.Character
        local humanoid = char and char:FindFirstChildOfClass("Humanoid")
        if humanoid then pcall(function() humanoid:UnequipTools() end) end
        cleanupPropMakerTool()
    end
    _G.BTR_StartBlind = function(targetName)
        if not targetName then return false end
        return blindStart(targetName, "V1")
    end
    _G.BTR_StopBlind = blindStop

    local function checkOtherBlinds(currentRef)
        local allBlinds = {blindV1Toggle, blindV2Toggle, blindV3Toggle, blindV4Toggle, blindV5Toggle, blindV6Toggle, blindV7Toggle, blindRainbowToggle}
        for _, b in ipairs(allBlinds) do
            if b and b ~= currentRef and b.Value then
                return true
            end
        end
        return false
    end

    blindV1Toggle = TrollTab:AddToggle({
        Name = "Blind Player V1 (Base)",
        Description = "Blinds the selected target with a ring of light",
        Default = false,
        Callback = function(Value)
            if not _G.BTR_UI_READY then return end
            if Value then
                if checkOtherBlinds(blindV1Toggle) then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Disable other Blind modes first!", Duration = 3 })
                    blindV1Toggle:SetValue(false)
                    return
                end
                local targetName = _G.TrollTarget
                if not targetName or targetName == "---" or targetName == "" then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Select a target first!", Duration = 4 })
                    blindV1Toggle:SetValue(false)
                    return
                end
                local ok = blindStart(targetName, "V1")
                if not ok then
                    blindV1Toggle:SetValue(false)
                    blindStop()
                end
            else
                blindStop()
            end
        end
    })

    blindV2Toggle = TrollTab:AddToggle({
        Name = "Blind Player V2 (Cocoon)",
        Description = "Locks the target in a vertical cocoon of light",
        Default = false,
        Callback = function(Value)
            if not _G.BTR_UI_READY then return end
            if Value then
                if checkOtherBlinds(blindV2Toggle) then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Disable other Blind modes first!", Duration = 3 })
                    blindV2Toggle:SetValue(false)
                    return
                end
                local targetName = _G.TrollTarget
                if not targetName or targetName == "---" or targetName == "" then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Select a target first!", Duration = 4 })
                    blindV2Toggle:SetValue(false)
                    return
                end
                local ok = blindStart(targetName, "V2")
                if not ok then
                    blindV2Toggle:SetValue(false)
                    blindStop()
                end
            else
                blindStop()
            end
        end
    })

    blindV3Toggle = TrollTab:AddToggle({
        Name = "Blind Player V3 (3D Sphere)",
        Description = "3D orbiting sphere of pure blinding light around target",
        Default = false,
        Callback = function(Value)
            if not _G.BTR_UI_READY then return end
            if Value then
                if checkOtherBlinds(blindV3Toggle) then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Disable other Blind modes first!", Duration = 3 })
                    blindV3Toggle:SetValue(false)
                    return
                end
                local targetName = _G.TrollTarget
                if not targetName or targetName == "---" or targetName == "" then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Select a target first!", Duration = 4 })
                    blindV3Toggle:SetValue(false)
                    return
                end
                local ok = blindStart(targetName, "V3")
                if not ok then
                    blindV3Toggle:SetValue(false)
                    blindStop()
                end
            else
                blindStop()
            end
        end
    })

    blindV4Toggle = TrollTab:AddToggle({
        Name = "Blind Player V4 (Helix Tornado)",
        Description = "Double helix tornado of lasers surrounding the target",
        Default = false,
        Callback = function(Value)
            if not _G.BTR_UI_READY then return end
            if Value then
                if checkOtherBlinds(blindV4Toggle) then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Disable other Blind modes first!", Duration = 3 })
                    blindV4Toggle:SetValue(false)
                    return
                end
                local targetName = _G.TrollTarget
                if not targetName or targetName == "---" or targetName == "" then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Select a target first!", Duration = 4 })
                    blindV4Toggle:SetValue(false)
                    return
                end
                local ok = blindStart(targetName, "V4")
                if not ok then
                    blindV4Toggle:SetValue(false)
                    blindStop()
                end
            else
                blindStop()
            end
        end
    })

    blindV5Toggle = TrollTab:AddToggle({
        Name = "Blind Player V5 (Strobe Matrix)",
        Description = "High-frequency strobe jitter surrounding the target camera",
        Default = false,
        Callback = function(Value)
            if not _G.BTR_UI_READY then return end
            if Value then
                if checkOtherBlinds(blindV5Toggle) then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Disable other Blind modes first!", Duration = 3 })
                    blindV5Toggle:SetValue(false)
                    return
                end
                local targetName = _G.TrollTarget
                if not targetName or targetName == "---" or targetName == "" then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Select a target first!", Duration = 4 })
                    blindV5Toggle:SetValue(false)
                    return
                end
                local ok = blindStart(targetName, "V5")
                if not ok then
                    blindV5Toggle:SetValue(false)
                    blindStop()
                end
            else
                blindStop()
            end
        end
    })

    blindV6Toggle = TrollTab:AddToggle({
        Name = "Blind Player V6 (Solar Supernova)",
        Description = "Blinding pulsating singularity starburst that detonates right on target's face",
        Default = false,
        Callback = function(Value)
            if not _G.BTR_UI_READY then return end
            if Value then
                if checkOtherBlinds(blindV6Toggle) then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Disable other Blind modes first!", Duration = 3 })
                    blindV6Toggle:SetValue(false)
                    return
                end
                local targetName = _G.TrollTarget
                if not targetName or targetName == "---" or targetName == "" then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Select a target first!", Duration = 4 })
                    blindV6Toggle:SetValue(false)
                    return
                end
                local ok = blindStart(targetName, "V6")
                if not ok then
                    blindV6Toggle:SetValue(false)
                    blindStop()
                end
            else
                if not checkOtherBlinds(blindV6Toggle) then
                    blindStop()
                end
            end
        end
    })

    blindV7Toggle = TrollTab:AddToggle({
        Name = "Blind Player V7 (Quantum Gyro Cage)",
        Description = "3 concentric counter-rotating gyroscopic laser rings locking target's view",
        Default = false,
        Callback = function(Value)
            if not _G.BTR_UI_READY then return end
            if Value then
                if checkOtherBlinds(blindV7Toggle) then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Disable other Blind modes first!", Duration = 3 })
                    blindV7Toggle:SetValue(false)
                    return
                end
                local targetName = _G.TrollTarget
                if not targetName or targetName == "---" or targetName == "" then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Select a target first!", Duration = 4 })
                    blindV7Toggle:SetValue(false)
                    return
                end
                local ok = blindStart(targetName, "V7")
                if not ok then
                    blindV7Toggle:SetValue(false)
                    blindStop()
                end
            else
                if not checkOtherBlinds(blindV7Toggle) then
                    blindStop()
                end
            end
        end
    })

    TrollTab:AddDropdown({
        Name = "Blind Rainbow Prop",
        Description = "Select prop for Rainbow Bed Blind (Beds or Bleachers)",
        Options = {"Beds (Futuristic Large Bed)", "Bleachers (Grandstand)"},
        Default = "Beds (Futuristic Large Bed)",
        Callback = function(value)
            if value:find("Bleachers") then
                blindRainbowProp = "FurnitureBleachers"
            else
                blindRainbowProp = "FuturisticLargeBed"
            end
        end
    })

    blindRainbowToggle = TrollTab:AddToggle({
        Name = "Blind Rainbow (Beds / Bleachers)",
        Description = "Encages and blinds target inside a revolving rainbow vortex of beds or bleachers",
        Default = false,
        Callback = function(Value)
            if not _G.BTR_UI_READY then return end
            if Value then
                if checkOtherBlinds(blindRainbowToggle) then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Disable other Blind modes first!", Duration = 3 })
                    blindRainbowToggle:SetValue(false)
                    return
                end
                local targetName = _G.TrollTarget
                if not targetName or targetName == "---" or targetName == "" then
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Blind Player", Text = "Select a target first!", Duration = 4 })
                    blindRainbowToggle:SetValue(false)
                    return
                end
                local ok = blindStart(targetName, "RainbowBed")
                if not ok then
                    blindRainbowToggle:SetValue(false)
                    blindStop()
                end
            else
                if not checkOtherBlinds(blindRainbowToggle) then
                    blindStop()
                end
            end
        end
    })
end

do
    local bleacherFlyRunning = false

    local function cleanupSkyLift()
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        if RE then
            local clearTools = RE:FindFirstChild("1Clea1rTool1s")
            if clearTools then
                
                pcall(function() clearTools:FireServer("ClearAllProps") end)
                task.wait(0.1)
                pcall(function() clearTools:FireServer("ClearAllTools") end)
                task.wait(0.1)
            end
        end
        cleanupPropMakerTool()
    end

    
    local function stopSkyLift()
        bleacherFlyRunning = false
        
        if skyLiftToggle then pcall(function() skyLiftToggle:Set(false) end) end
        
        
        _G.BTR_SKYLIFT_STOPPING = true
        pcall(cleanupSkyLift)
        _G.BTR_SKYLIFT_STOPPING = false
    end
    _G.BTR_StopSkyLift = stopSkyLift

    local skyLiftToggle = TrollTab:AddToggle({
        Name = "Bleacher Sky Lift (Troll Fly)",
        Description = "Carries the target into the sky on a bleacher platform!",
        Default = false,
        Callback = function(state)
            if not _G.BTR_UI_READY then return end
            bleacherFlyRunning = state
            if state then
                local targetPlayer = nil
                if _G.TrollTarget and _G.TrollTarget ~= "---" then
                    targetPlayer = game:GetService("Players"):FindFirstChild(_G.TrollTarget)
                end

                
                if _G.BTR_StopNoEscape then pcall(_G.BTR_StopNoEscape) end

                if not targetPlayer then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Sky Lift", Text="Player not found!", Duration=3})
                    bleacherFlyRunning = false
                    return
                end

                
                
                task.spawn(function()
                    if BringSystem.Active then
                        pcall(function() BringSystem.stop() end)
                        local bg = 0
                        while BringSystem.Stopping and bg < 60 do
                            task.wait(0.1)
                            bg = bg + 1
                        end
                    end
                    local lp = game:GetService("Players").LocalPlayer
                    local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
                    local clearTools = RE:WaitForChild("1Clea1rTool1s")
                    local myPropName = "Prop" .. lp.Name

                    pcall(function() clearTools:FireServer("ClearAllProps") end)
                    task.wait(0.15)

                    local propsTool = getOrFetchPropMaker()
                    if not propsTool then bleacherFlyRunning = false return end

                    pcall(function() clearTools:FireServer("OpenPropMenu") end)
                    task.wait(0.05)

                    local knownScf = {}
                    for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                        for _, prop in pairs(folder:GetChildren()) do
                            if prop.Name == myPropName then
                                local scf = prop:FindFirstChild("SetCurrentCFrame")
                                if scf then knownScf[scf] = true end
                            end
                        end
                    end

                    
                    pcall(function() clearTools:FireServer("RequestingPropName", "FurnitureBleachers", "Furniture") end)
                    task.wait(0.25)

                    local myRoot = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
                    local spawnPos = myRoot and myRoot.Position or Vector3.new(0, 10, 0)
                    local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
                    if not toolPropMake then bleacherFlyRunning = false return end

                    local flyScfs = {}
                    local duplicated = spawnAndDuplicateProps(toolPropMake, spawnPos, 15, 0)
                    if not duplicated then
                        for i = 1, 15 do
                            if not bleacherFlyRunning then break end
                            pcall(function() toolPropMake:FireServer(workspace.Terrain, spawnPos) end)
                            task.wait(0.85)
                        end
                    end
                    for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                        for _, prop in pairs(folder:GetChildren()) do
                            if prop.Name == "Prop" .. lp.Name then
                                local scf = prop:FindFirstChild("SetCurrentCFrame")
                                if scf and not knownScf[scf] then
                                    knownScf[scf] = true
                                    table.insert(flyScfs, scf)
                                end
                            end
                        end
                    end

                    -- [PATTERN PROPMAKER LOCKED] 1.0s exact delay before removing carton
                    task.spawn(function()
                        task.wait(1.0)
                        pcall(function()
                            game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker")
                        end)
                        pcall(function() clearTools:FireServer("ClosePropMenu") end)
                    end)

                    if #flyScfs == 0 then
                        bleacherFlyRunning = false
                        return
                    end

                    
                    local platformOffsets = {
                        Vector3.new(0, 0, 0),
                        Vector3.new(2.5, 0, 0),
                        Vector3.new(-2.5, 0, 0),
                        Vector3.new(0, 0, 2.5),
                        Vector3.new(0, 0, -2.5),
                        Vector3.new(5, 0, 0),
                        Vector3.new(-5, 0, 0),
                        Vector3.new(0, 0, 5),
                        Vector3.new(0, 0, -5),
                        Vector3.new(3.5, 0, 3.5),
                        Vector3.new(-3.5, 0, 3.5),
                        Vector3.new(3.5, 0, -3.5),
                        Vector3.new(-3.5, 0, -3.5),
                        Vector3.new(5, 0, 2.5),
                        Vector3.new(-5, 0, -2.5),
                    }

                    local tHrpRef = targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                    local platformTargetY = tHrpRef and (tHrpRef.Position.Y - 1.5) or (spawnPos.Y - 1.5)

                    
                    local lastFlyPos = {} 
                    local lastFlyTime = {} 
                    local FLY_MOVE_THRESHOLD = 0.5
                    local FLY_MIN_INTERVAL = 0.1
                    local FLY_MAX_INTERVAL = 0.25
                    local currentLiftY = nil
                    local captured = false

                    while bleacherFlyRunning do
                        if targetPlayer and targetPlayer.Character then
                            local tHrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                            local tHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
                            if tHrp then
                                if not currentLiftY then
                                    currentLiftY = tHrp.Position.Y - 1.2
                                end

                                
                                if tHum and tHum.Sit and tHum.SeatPart and tHum.SeatPart:FindFirstAncestor(myPropName) then
                                    captured = true
                                end

                                
                                currentLiftY = currentLiftY + 1.2
                                local basePos = Vector3.new(tHrp.Position.X, currentLiftY, tHrp.Position.Z)
                                local now = os.clock()

                                for idx, scf in ipairs(flyScfs) do
                                    local off = platformOffsets[idx] or Vector3.new(0, 0, 0)
                                    local targetCF = CFrame.new(basePos + off)
                                    pcall(function() scf:InvokeServer(targetCF) end)
                                end
                            end
                        end
                        task.wait(0.04)
                    end
                end)
            else
                bleacherFlyRunning = false
                cleanupSkyLift()
            end
        end
    })
end

do
    local prisonRunning = false
    local prisonSession = 0
    local prisonToggle = nil
    local senders = {}
    local RATE_LIMIT = 0.025
    local PROP_NAME = "BigGunBunker"
    local PROP_CATEGORY = "Big"
    local PROP_COUNT = 15
    local cageTightness = 4.5
    local spinSpeed = 2.5
    local currentAngle = 0

    local function sendCF(scf, cf, prop)
        if not prisonRunning or not scf then return end

        if prop then
            pcall(function()
                if prop:IsA("Model") then
                    prop:PivotTo(cf)
                elseif prop:IsA("BasePart") then
                    prop.CFrame = cf
                end
            end)
        end

        local entry = senders[scf]
        local now = tick()
        if not entry then
            entry = { lastSent = 0, nextCF = cf, sending = false }
            senders[scf] = entry
        else
            entry.nextCF = cf
        end

        if not entry.sending and (now - entry.lastSent >= RATE_LIMIT) then
            entry.sending = true
            entry.lastSent = now
            local toSend = entry.nextCF
            entry.nextCF = nil
            task.spawn(function()
                pcall(function() scf:InvokeServer(toSend) end)
                entry.sending = false
                if prisonRunning and entry.nextCF and (tick() - entry.lastSent >= RATE_LIMIT) then
                    entry.sending = true
                    entry.lastSent = tick()
                    local nextSend = entry.nextCF
                    entry.nextCF = nil
                    task.spawn(function()
                        pcall(function() scf:InvokeServer(nextSend) end)
                        entry.sending = false
                    end)
                end
            end)
        end
    end

    local function duplicateProps(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 1.5)
        if not dupRemote then return false end

        for i = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
        end
        return true
    end

    local function cleanupPropMaker()
        local lp = game:GetService("Players").LocalPlayer
        local char = lp and lp.Character
        local bp = lp and lp:FindFirstChild("Backpack")
        pcall(function()
            local h = char and char:FindFirstChildOfClass("Humanoid")
            if h then h:UnequipTools() end
        end)
        if char then
            local tool = char:FindFirstChild("PropMaker")
            if tool and bp then tool.Parent = bp end
        end
    end

    local function stopBunkerPrison()
        if not prisonRunning then return end
        prisonRunning = false
        prisonSession = prisonSession + 1

        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        if RE then
            local clearTools = RE:FindFirstChild("1Clea1rTool1s")
            if clearTools then
                pcall(function() clearTools:FireServer("ClearAllProps") end)
            end
        end
        cleanupPropMaker()
        senders = {}

        if prisonToggle then
            pcall(function() prisonToggle:SetValue(false) end)
        end
    end
    _G.BTR_StopBunkerPrison = stopBunkerPrison

    local function getTargetPlayer()
        local targetName = _G.TrollTarget
        if targetName and targetName ~= "---" and targetName ~= "" then
            local p = game:GetService("Players"):FindFirstChild(targetName)
            if p then return p end
        end
        if _G.TrollLockedTarget and _G.TrollLockedTarget ~= "---" and _G.TrollLockedTarget ~= "" then
            local p = game:GetService("Players"):FindFirstChild(_G.TrollLockedTarget)
            if p then return p end
        end
        return nil
    end

    prisonToggle = TrollTab:AddToggle({
        Name = "Bunker Prison (Deep Black)",
        Description = "Encages target in 15 Deep Black Bunkers for total FE camera blackout",
        Default = false,
        Callback = function(state)
            if not _G.BTR_UI_READY then return end
            if not state then
                stopBunkerPrison()
                return
            end

            local targetPlayer = getTargetPlayer()
            if not targetPlayer then
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Bunker Prison",
                    Text = "Please select a target player in Troll tab first!",
                    Duration = 3
                })
                if prisonToggle then pcall(function() prisonToggle:SetValue(false) end) end
                return
            end

            local targetChar = targetPlayer.Character
            local targetRoot = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
            if not targetChar or not targetRoot then
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Bunker Prison",
                    Text = "Target character not found in workspace!",
                    Duration = 3
                })
                if prisonToggle then pcall(function() prisonToggle:SetValue(false) end) end
                return
            end

            stopBunkerPrison()
            task.wait(0.15)

            prisonRunning = true
            prisonSession = prisonSession + 1
            local mySession = prisonSession

            task.spawn(function()
                local Players = game:GetService("Players")
                local lp = Players.LocalPlayer
                local char = lp.Character
                local hum = char and char:FindFirstChildOfClass("Humanoid")
                local myRoot = char and char:FindFirstChild("HumanoidRootPart")
                if not char or not hum or not myRoot then
                    stopBunkerPrison()
                    return
                end

                local spawnPos = myRoot.Position + (myRoot.CFrame.LookVector * 10)
                local propMaker = getOrFetchPropMaker()
                if not propMaker or not prisonRunning or prisonSession ~= mySession then
                    stopBunkerPrison()
                    return
                end

                local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
                local clearTools = RE:WaitForChild("1Clea1rTool1s")
                local Telemetry = game:GetService("ReplicatedStorage"):FindFirstChild("Remotes") and game:GetService("ReplicatedStorage").Remotes:FindFirstChild("TelemetryClientInteraction")

                pcall(function() clearTools:FireServer("ClearAllProps") end)
                task.wait(0.12)

                pcall(function() clearTools:FireServer("OpenPropMenu") end)
                task.wait(0.1)

                if Telemetry then
                    pcall(function() Telemetry:FireServer("filterClick", {name = PROP_CATEGORY, itemType = "Props"}) end)
                    task.wait(0.04)
                    pcall(function() Telemetry:FireServer("filterClick", {name = PROP_NAME, itemType = "Props"}) end)
                    task.wait(0.04)
                end
                pcall(function() clearTools:FireServer("RequestingPropName", PROP_NAME, PROP_CATEGORY) end)
                task.wait(0.18)

                local toolPropMake = propMaker:FindFirstChild("Tool_PropMake") or propMaker:WaitForChild("Tool_PropMake", 3)
                if not toolPropMake or not prisonRunning or prisonSession ~= mySession then
                    return
                end

                local myPropName = "Prop" .. lp.Name
                local wc = workspace:FindFirstChild("WorkspaceCom")
                if not wc or not prisonRunning or prisonSession ~= mySession then
                    stopBunkerPrison()
                    return
                end

                local existingProps = {}
                for _, folder in pairs(wc:GetChildren()) do
                    for _, p in pairs(folder:GetChildren()) do
                        if p.Name == myPropName then existingProps[p] = true end
                    end
                end

                pcall(function() toolPropMake:FireServer(workspace.Terrain, spawnPos) end)

                local firstProp = nil
                local t0 = tick()
                while tick() - t0 < 3.5 do
                    if not prisonRunning or prisonSession ~= mySession or hum.Health <= 0 then
                        stopBunkerPrison()
                        return
                    end
                    for _, folder in pairs(wc:GetChildren()) do
                        for _, p in pairs(folder:GetChildren()) do
                            if p.Name == myPropName and not existingProps[p] then
                                firstProp = p
                                break
                            end
                        end
                        if firstProp then break end
                    end
                    if firstProp then break end
                    task.wait(0.04)
                end

                if not firstProp then
                    stopBunkerPrison()
                    return
                end

                duplicateProps(firstProp, PROP_COUNT - 1)

                -- [PATTERN PROPMAKER LOCKED] 1.0s exact delay before removing carton
                task.spawn(function()
                    task.wait(1.0)
                    pcall(function()
                        game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker")
                    end)
                    cleanupPropMaker()
                end)

                local activeProps = {}
                task.wait(0.25)
                for _, folder in pairs(wc:GetChildren()) do
                    for _, p in pairs(folder:GetChildren()) do
                        if p.Name == myPropName then
                            local scf = p:FindFirstChild("SetCurrentCFrame")
                            local cpc = p:FindFirstChild("ChangePropColor")
                            if scf then
                                table.insert(activeProps, { Prop = p, SCF = scf, CPC = cpc })
                            end
                        end
                    end
                end

                if #activeProps < 1 then
                    stopBunkerPrison()
                    return
                end

                
                local deepBlack = Color3.fromRGB(0, 0, 0)
                for _, item in ipairs(activeProps) do
                    if item.CPC then
                        task.spawn(function()
                            pcall(function() item.CPC:InvokeServer(deepBlack) end)
                        end)
                    end
                    if item.Prop then
                        pcall(function()
                            for _, part in ipairs(item.Prop:GetDescendants()) do
                                if part:IsA("BasePart") then
                                    part.Color = deepBlack
                                end
                            end
                        end)
                    end
                end

                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Bunker Prison",
                    Text = "Target trapped in 15 Deep Black Bunkers!",
                    Duration = 3
                })

                local heartbeatConn
                heartbeatConn = game:GetService("RunService").Heartbeat:Connect(function(dt)
                    if not prisonRunning or prisonSession ~= mySession then
                        if heartbeatConn then heartbeatConn:Disconnect() end
                        return
                    end

                    local curTarget = getTargetPlayer()
                    local curChar = curTarget and curTarget.Character
                    local curRoot = curChar and curChar:FindFirstChild("HumanoidRootPart")
                    local curHum = curChar and curChar:FindFirstChildOfClass("Humanoid")

                    if not curRoot or not curHum or curHum.Health <= 0 then
                        return
                    end

                    currentAngle = (currentAngle + (spinSpeed * dt)) % (math.pi * 2)

                    local targetPos = curRoot.Position
                    local baseCF = CFrame.new(targetPos) * CFrame.Angles(0, currentAngle, 0)
                    local radius = cageTightness

                    for i, item in ipairs(activeProps) do
                        local cf = nil

                        if i <= 8 then
                            local angle = ((i - 1) / 8) * (math.pi * 2)
                            local x = math.cos(angle) * radius
                            local z = math.sin(angle) * radius
                            local wallCF = baseCF * CFrame.new(x, 0, z) * CFrame.Angles(0, -angle + (math.pi / 2), 0)
                            cf = wallCF
                        elseif i <= 10 then
                            local roofOffset = (i == 9) and Vector3.new(0, radius + 1.2, 0) or Vector3.new(0, radius + 2.5, 0)
                            local roofRot = (i == 9) and 0 or (math.pi / 4)
                            cf = baseCF * CFrame.new(roofOffset) * CFrame.Angles(math.pi / 2, roofRot, 0)
                        elseif i <= 12 then
                            local floorOffset = (i == 11) and Vector3.new(0, -(radius + 1.2), 0) or Vector3.new(0, -(radius + 2.5), 0)
                            local floorRot = (i == 11) and 0 or (math.pi / 4)
                            cf = baseCF * CFrame.new(floorOffset) * CFrame.Angles(-math.pi / 2, floorRot, 0)
                        else
                            local coreIndex = i - 12
                            local coreRotY = (coreIndex / 3) * math.pi
                            cf = CFrame.new(targetPos) * CFrame.Angles(0, currentAngle + coreRotY, 0)
                        end

                        sendCF(item.SCF, cf, item.Prop)
                    end
                end)
            end)
        end
    })
end

do
    local parasiteActive = false
    local parasiteTarget = nil
    local heartbeatConn = nil
    local renderConn = nil
    local noclipConn = nil

    local function getTargetTorso(char)
        if not char then return nil end
        return char:FindFirstChild("UpperTorso") or char:FindFirstChild("Torso") or char:FindFirstChild("HumanoidRootPart")
    end

    local function detachParasite()
        parasiteActive = false
        parasiteTarget = nil

        if heartbeatConn then heartbeatConn:Disconnect(); heartbeatConn = nil end
        if renderConn then renderConn:Disconnect(); renderConn = nil end
        if noclipConn then noclipConn:Disconnect(); noclipConn = nil end

        local lp = game:GetService("Players").LocalPlayer
        local char = lp and lp.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.AutoRotate = true
                hum.PlatformStand = false
                pcall(function()
                    hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true)
                    hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
                    hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, true)
                end)
            end
            local animScript = char:FindFirstChild("Animate")
            if animScript and animScript:IsA("LocalScript") then
                animScript.Enabled = true
            end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.AssemblyLinearVelocity = Vector3.zero
                hrp.AssemblyAngularVelocity = Vector3.zero
                for _, item in ipairs(hrp:GetChildren()) do
                    if item.Name == "ParasiteAntiGravity" or item.Name == "ParasiteConstraint" or item.Name == "ParasiteAtt" then
                        item:Destroy()
                    end
                end
            end
        end
    end

    local function startParasite(targetPlayer)
        detachParasite()
        if not targetPlayer then return end

        local lp = game:GetService("Players").LocalPlayer
        local myChar = lp.Character
        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
        if not myChar or not myHrp then return end

        parasiteTarget = targetPlayer
        parasiteActive = true

        
        noclipConn = game:GetService("RunService").Stepped:Connect(function()
            if not parasiteActive then return end
            local c = lp.Character
            if c then
                for _, part in ipairs(c:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.CanCollide = false
                    end
                end
            end
        end)

        
        local myHum = myChar:FindFirstChildOfClass("Humanoid")
        if myHum then
            myHum.AutoRotate = false
            pcall(function()
                myHum:SetStateEnabled(Enum.HumanoidStateType.Freefall, false)
                myHum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
                myHum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
                myHum:ChangeState(Enum.HumanoidStateType.Running)
            end)
            pcall(function()
                for _, track in ipairs(myHum:GetPlayingAnimationTracks()) do
                    track:Stop(0)
                end
            end)
        end

        local animScript = myChar:FindFirstChild("Animate")
        if animScript and animScript:IsA("LocalScript") then
            animScript.Enabled = false
        end

        
        local antiGravity = myHrp:FindFirstChild("ParasiteAntiGravity") or Instance.new("BodyVelocity")
        antiGravity.Name = "ParasiteAntiGravity"
        antiGravity.MaxForce = Vector3.new(1e5, 1e5, 1e5)
        antiGravity.Velocity = Vector3.zero
        antiGravity.Parent = myHrp

        local leadTime = 0.17

        heartbeatConn = game:GetService("RunService").Heartbeat:Connect(function()
            if not parasiteActive or not parasiteTarget or not parasiteTarget.Character then return end
            local targetChar = parasiteTarget.Character
            local targetPart = getTargetTorso(targetChar)
            local currentHrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")

            if targetPart and currentHrp then
                local vel = targetPart.AssemblyLinearVelocity
                local horizontalVel = Vector3.new(vel.X, 0, vel.Z)
                local leadOffset = Vector3.zero
                if horizontalVel.Magnitude > 0.4 then
                    leadOffset = horizontalVel * leadTime
                end

                local targetPos = targetPart.Position + Vector3.new(0, 0.7, 0) + leadOffset
                currentHrp.CFrame = CFrame.new(targetPos, targetPos + targetPart.CFrame.LookVector)
                currentHrp.AssemblyLinearVelocity = targetPart.AssemblyLinearVelocity
                currentHrp.AssemblyAngularVelocity = Vector3.zero
                if antiGravity and antiGravity.Parent then
                    antiGravity.Velocity = targetPart.AssemblyLinearVelocity
                end
            end
        end)

        renderConn = game:GetService("RunService").RenderStepped:Connect(function()
            if not parasiteActive or not parasiteTarget or not parasiteTarget.Character then return end
            local targetChar = parasiteTarget.Character
            local targetPart = getTargetTorso(targetChar)
            local currentHrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")

            if targetPart and currentHrp then
                local vel = targetPart.AssemblyLinearVelocity
                local horizontalVel = Vector3.new(vel.X, 0, vel.Z)
                local leadOffset = Vector3.zero
                if horizontalVel.Magnitude > 0.4 then
                    leadOffset = horizontalVel * leadTime
                end
                local targetPos = targetPart.Position + Vector3.new(0, 0.7, 0) + leadOffset
                currentHrp.CFrame = CFrame.new(targetPos, targetPos + targetPart.CFrame.LookVector)
            end
        end)
    end

    TrollTab:AddSection({
        "> Parasite Chat Impersonator (All-In-One)"
    })

    
    TrollTab:AddButton({
        Name = "Hide Active Name (Stealth Mode)",
        Description = "Clears text over your head so you are 100% anonymous",
        Callback = function()
            pcall(function()
                local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                if RE and RE:FindFirstChild("1RPNam1eTex1t") then
                    RE["1RPNam1eTex1t"]:FireServer("RolePlayName", " ")
                    RE["1RPNam1eTex1t"]:FireServer("RolePlayBio", " ")
                end
            end)
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Stealth Mode",
                Text = "Name and Bio hidden.",
                Duration = 2
            })
        end
    })

    TrollTab:AddButton({
        Name = "Restore BTR Client Tag",
        Description = "Restores official BTR Client tag over your head",
        Callback = function()
            pcall(function()
                local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                if RE and RE:FindFirstChild("1RPNam1eTex1t") then
                    RE["1RPNam1eTex1t"]:FireServer("RolePlayName", "『 ѢҬԄ X ҪԒЇԐԊҬ 』")
                    RE["1RPNam1eTex1t"]:FireServer("RolePlayBio", "• ӇԐӾ ✕ ԒУЯѦ •")
                end
            end)
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Stealth Mode",
                Text = "BTR Client tag restored!",
                Duration = 2
            })
        end
    })

    
    local selectedTrollOutfitSlot = tonumber(_G.BTR_AUTOLOAD_SLOT) or 3
    local defaultSlotStr = "Slot " .. tostring(selectedTrollOutfitSlot)

    TrollTab:AddDropdown({
        Name = "Select Outfit Slot",
        Description = "Choose Brookhaven wardrobe slot (Slot 1, 2 or 3)",
        Options = {"Slot 1", "Slot 2", "Slot 3"},
        Default = defaultSlotStr,
        Callback = function(slot)
            if slot == "Slot 1" then selectedTrollOutfitSlot = 1
            elseif slot == "Slot 2" then selectedTrollOutfitSlot = 2
            elseif slot == "Slot 3" then selectedTrollOutfitSlot = 3
            end
            _G.BTR_AUTOLOAD_SLOT = selectedTrollOutfitSlot
            if _G.saveBTRConfig then _G.saveBTRConfig() end
        end
    })

    TrollTab:AddButton({
        Name = "Save Current Outfit to Slot",
        Description = "Saves your current avatar into the selected slot",
        Callback = function()
            pcall(function()
                local RS = game:GetService("ReplicatedStorage")
                local remotes = RS:FindFirstChild("Remotes")
                local saveRemote = remotes and remotes:FindFirstChild("SaveOutfit")
                if saveRemote then
                    saveRemote:InvokeServer(selectedTrollOutfitSlot, "Slot " .. tostring(selectedTrollOutfitSlot))
                    game:GetService("StarterGui"):SetCore("SendNotification", {
                        Title = "Wardrobe",
                        Text = "Outfit saved to Slot " .. tostring(selectedTrollOutfitSlot) .. "!",
                        Duration = 2
                    })
                end
            end)
        end
    })

    TrollTab:AddButton({
        Name = "Load Outfit from Selected Slot",
        Description = "Loads the saved outfit from chosen slot",
        Callback = function()
            pcall(function()
                local RS = game:GetService("ReplicatedStorage")
                local remotes = RS:FindFirstChild("Remotes")
                local loadRemote = remotes and remotes:FindFirstChild("LoadOutfit")
                if loadRemote then
                    loadRemote:InvokeServer(selectedTrollOutfitSlot)
                    game:GetService("StarterGui"):SetCore("SendNotification", {
                        Title = "Wardrobe",
                        Text = "Slot " .. tostring(selectedTrollOutfitSlot) .. " loaded!",
                        Duration = 2
                    })
                end
            end)
        end
    })

    _G.BTR_AutoLoadOutfitToggle = TrollTab:AddToggle({
        Name = "Auto Outfit for Chat Control",
        Description = "Loads chosen wardrobe slot when activating Parasite / Chat Control",
        Default = (_G.BTR_AUTOLOAD_OUTFIT == true),
        Callback = function(val)
            _G.BTR_AUTOLOAD_OUTFIT = (val == true)
            if _G.saveBTRConfig then
                _G.saveBTRConfig()
            end
        end
    })

    
    local parasiteToggle = TrollTab:AddToggle({
        Name = "Parasite Chat (Lock in Torso)",
        Description = "Locks inside target torso with forward lead and frozen animations",
        Default = false,
        Callback = function(state)
            if not _G.BTR_UI_READY then return end
            if state then
                local t = nil
                if _G.TrollTarget and _G.TrollTarget ~= "---" then
                    t = game:GetService("Players"):FindFirstChild(_G.TrollTarget)
                end
                if not t then
                    game:GetService("StarterGui"):SetCore("SendNotification", {
                        Title = "Parasite Chat",
                        Text = "Select a target first!",
                        Duration = 3
                    })
                    return
                end

                
                if _G.BTR_AUTOLOAD_OUTFIT ~= false then
                    pcall(function()
                        local slotToLoad = tonumber(_G.BTR_AUTOLOAD_SLOT) or selectedTrollOutfitSlot or 3
                        local RS = game:GetService("ReplicatedStorage")
                        local remotes = RS:FindFirstChild("Remotes")
                        local loadRemote = remotes and remotes:FindFirstChild("LoadOutfit")
                        if loadRemote then
                            loadRemote:InvokeServer(slotToLoad)
                        end
                    end)
                end

                startParasite(t)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Parasite Chat",
                    Text = "Locked inside " .. t.DisplayName .. "!",
                    Duration = 2
                })
            else
                detachParasite()
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Parasite Chat",
                    Text = "Detached from target.",
                    Duration = 2
                })
            end
        end
    })

    TrollTab:AddButton({
        Name = "Detach / Stop Parasite",
        Description = "Instantly leaves the target and restores normal character state",
        Callback = function()
            detachParasite()
            if parasiteToggle and parasiteToggle.Set then
                pcall(function() parasiteToggle:Set(false) end)
            end
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Parasite Chat",
                Text = "Detached from target.",
                Duration = 2
            })
        end
    })
end

    

    -- [LOCKED: WORK PERFECTLY] Special Blind V2 - les blinds de la copine
    
    
    do
        local sbMode = "Smoke"
        local sbLockedTarget = nil
        local sbLoopRunning = false
        local sbSession = 0 

        local Players = game:GetService("Players")
        local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")
        local telemetry = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction")
        local sbOffsets = {} 

        -- [PATTERN BLIND LOCKED] selection directe (comme le Blind original : pas de filterClick)
        
        local smokeColors = {"SmokePurple", "SmokeBlue", "SmokeYellow"}

        -- [PATTERN BLIND LOCKED] selection directe (comme le Blind original)
        local function sbSelectProp(name, category, useFeature)
            pcall(function()
                if useFeature then
                    clearTools:FireServer("RequestingPropFeatureName", name, category)
                else
                    clearTools:FireServer("RequestingPropName", name, category)
                end
            end)
            task.wait(0.1)
        end

        local function sbStart(lockedName)
            sbSession = sbSession + 1
            local mySession = sbSession
            local function sbAborted() return sbSession ~= mySession end

                        warn("[SB] sbStart appele! sbMode=" .. tostring(sbMode) .. " | lockedName=" .. tostring(lockedName))
            local lp = Players.LocalPlayer
            local char = lp.Character
            local humanoid = char and char:FindFirstChildOfClass("Humanoid")
            if not char or not humanoid then return false end

            sbLockedTarget = lockedName
            sbOffsets = {}

            local propsTool = getOrFetchPropMaker()
            if not propsTool then
                game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Special Blind", Text = "Could not get PropMaker!", Duration = 4 })
                return false
            end

            humanoid:EquipTool(propsTool)
            task.wait(0.3)

            pcall(function() clearTools:FireServer("OpenPropMenu") end)
            task.wait(0.1)

            local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
            if not toolPropMake then return false end 

            -- [PATTERN BLIND LOCKED] pose AUTOUR DE SOI (getRandomPos identique au Blind)
            local myPos = char.HumanoidRootPart.Position
            local placedPositions = {}
            local MIN_DIST = 4
            local MIN_RADIUS = 3
            local MAX_RADIUS = 9
            local INTERVAL = 0.85

            local function getRandomPos(yOff)
                local yOff = yOff or 0
                for _ = 1, 20 do
                    local angle = math.random() * 2 * math.pi
                    local radius = MIN_RADIUS + math.random() * (MAX_RADIUS - MIN_RADIUS)
                    local candidate = myPos + Vector3.new(math.cos(angle) * radius, yOff, math.sin(angle) * radius)
                    local tooClose = false
                    for _, pos in pairs(placedPositions) do
                        if (candidate - pos).Magnitude < MIN_DIST then tooClose = true break end
                    end
                    if not tooClose then return candidate end
                end
                local angle = math.random() * 2 * math.pi
                return myPos + Vector3.new(math.cos(angle) * MAX_RADIUS, yOff, math.sin(angle) * MAX_RADIUS)
            end

            if sbMode == "Smoke" then
                if #smokeColors == 1 then
                    local c = smokeColors[1]
                    pcall(function() clearTools:FireServer("RequestingPropName", c, "Smoke") end)
                    task.wait(0.05)

                    for i = 1, 15 do
                        local pos = getRandomPos(-3)
                        table.insert(placedPositions, pos)
                        table.insert(sbOffsets, pos - myPos)
                    end

                    local duplicated = spawnAndDuplicateProps(toolPropMake, placedPositions[1], 15, 0)
                    if not duplicated then
                        for i = 1, 15 do
                            if not propsTool or propsTool.Parent ~= char or sbAborted() then break end
                            pcall(function() clearTools:FireServer("RequestingPropName", c, "Smoke") end)
                            task.wait(0.05)
                            pcall(function() toolPropMake:FireServer(workspace.Terrain, placedPositions[i]) end)
                            if i < 15 then task.wait(INTERVAL - 0.05) end
                        end
                    end
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Special Blind", Text = "Smokes placed: 15/15", Duration = 2 })
                else
                    
                    for i = 1, 15 do
                        if not propsTool or not propsTool.Parent then break end
                        if sbAborted() then return false end
                        if propsTool.Parent ~= char then break end
                        local preWait = 0
                        local c = smokeColors[((i - 1) % #smokeColors) + 1]
                        pcall(function() clearTools:FireServer("RequestingPropName", c, "Smoke") end)
                        task.wait(0.05)
                        preWait = 0.05
                        local pos = getRandomPos(-3)
                        pcall(function() toolPropMake:FireServer(workspace.Terrain, pos) end)
                        table.insert(placedPositions, pos)
                        table.insert(sbOffsets, pos - myPos)
                        if i == 5 or i == 10 or i == 15 then
                            game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Special Blind", Text = "Smokes placed: " .. i .. "/15", Duration = 2 })
                        end
                        if i < 15 then task.wait(INTERVAL - preWait) end
                    end
                end
            elseif sbMode == "Flowers" then
                
                pcall(function() clearTools:FireServer("RequestingPropName", "PlantFlowerPurple", "Plant") end)
                task.wait(0.05)
                for i = 1, 8 do
                    local pos = getRandomPos()
                    table.insert(placedPositions, pos)
                    table.insert(sbOffsets, pos - myPos)
                end
                local dup1 = spawnAndDuplicateProps(toolPropMake, placedPositions[1], 8, 0)
                if not dup1 then
                    for i = 1, 8 do
                        if not propsTool or not propsTool.Parent or propsTool.Parent ~= char or sbAborted() then break end
                        pcall(function() clearTools:FireServer("RequestingPropName", "PlantFlowerPurple", "Plant") end)
                        task.wait(0.05)
                        pcall(function() toolPropMake:FireServer(workspace.Terrain, placedPositions[i]) end)
                        if i < 8 then task.wait(INTERVAL - 0.05) end
                    end
                end

                
                pcall(function() clearTools:FireServer("RequestingPropFeatureName", "BonsaiTreeSmallPink", "Plant") end)
                task.wait(0.05)
                for i = 1, 7 do
                    local pos = getRandomPos()
                    table.insert(placedPositions, pos)
                    table.insert(sbOffsets, pos - myPos)
                end
                local dup2 = spawnAndDuplicateProps(toolPropMake, placedPositions[9], 7, 0)
                if not dup2 then
                    for i = 1, 7 do
                        if not propsTool or not propsTool.Parent or propsTool.Parent ~= char or sbAborted() then break end
                        pcall(function() clearTools:FireServer("RequestingPropFeatureName", "BonsaiTreeSmallPink", "Plant") end)
                        task.wait(0.05)
                        pcall(function() toolPropMake:FireServer(workspace.Terrain, placedPositions[8 + i]) end)
                        if i < 7 then task.wait(INTERVAL - 0.05) end
                    end
                end
            elseif sbMode == "Skeleton" then
                task.wait(1.0)
                pcall(function() telemetry:FireServer("filterClick", { name = "Dead", itemType = "Props" }) end)
                task.wait(0.8)
                pcall(function() telemetry:FireServer("filterClick", { name = "DeadSkeletonStandArms", itemType = "Props" }) end)
                task.wait(0.5)
                pcall(function() clearTools:FireServer("RequestingPropName", "DeadSkeletonStandArms", "Dead") end)
                task.wait(0.05)

                for i = 1, 15 do
                    local pos = getRandomPos()
                    table.insert(placedPositions, pos)
                    table.insert(sbOffsets, pos - myPos)
                end

                local duplicated = spawnAndDuplicateProps(toolPropMake, placedPositions[1], 15, 0)
                if not duplicated then
                    for i = 1, 15 do
                        if propsTool and propsTool.Parent == char then
                            pcall(function() clearTools:FireServer("RequestingPropName", "DeadSkeletonStandArms", "Dead") end)
                            task.wait(0.05)
                            pcall(function() toolPropMake:FireServer(workspace.Terrain, placedPositions[i]) end)
                            if i < 15 then task.wait(INTERVAL - 0.05) end
                        else
                            break
                        end
                    end
                end
                game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Special Blind", Text = "Skeletons placed: 15/15", Duration = 2 })
            elseif sbMode == "Carpet" then
                pcall(function() clearTools:FireServer("RequestingPropName", "ArtisanBlueCarpet", "Furniture") end)
                task.wait(0.05)

                for i = 1, 15 do
                    local colAngle = ((i - 1) % 3) * (2 * math.pi / 3)
                    local floorN = math.floor((i - 1) / 3)
                    local pos = myPos + Vector3.new(math.cos(colAngle) * 4, 1 + floorN * 4, math.sin(colAngle) * 4)
                    table.insert(placedPositions, pos)
                    table.insert(sbOffsets, pos - myPos)
                end

                local duplicated = spawnAndDuplicateProps(toolPropMake, placedPositions[1], 15, 0)
                if not duplicated then
                    for i = 1, 15 do
                        if not propsTool or propsTool.Parent ~= char then break end
                        pcall(function() toolPropMake:FireServer(workspace.Terrain, placedPositions[i]) end)
                        if i < 15 then task.wait(INTERVAL - 0.05) end
                    end
                end
            elseif sbMode == "Combo" then
                
                pcall(function() clearTools:FireServer("RequestingPropName", "DeadSkeletonStandArms", "Dead") end)
                task.wait(0.05)
                for i = 1, 8 do
                    local pos = getRandomPos()
                    table.insert(placedPositions, pos)
                    table.insert(sbOffsets, pos - myPos)
                end
                local dupSkel = spawnAndDuplicateProps(toolPropMake, placedPositions[1], 8, 0)
                if not dupSkel then
                    for i = 1, 8 do
                        if not propsTool or not propsTool.Parent or propsTool.Parent ~= char or sbAborted() then break end
                        pcall(function() clearTools:FireServer("RequestingPropName", "DeadSkeletonStandArms", "Dead") end)
                        task.wait(0.05)
                        pcall(function() toolPropMake:FireServer(workspace.Terrain, placedPositions[i]) end)
                        if i < 8 then task.wait(INTERVAL - 0.05) end
                    end
                end

                local smokeTotal = math.min(#smokeColors * 3, 15)
                if #smokeColors == 1 then
                    local c = smokeColors[1]
                    pcall(function() clearTools:FireServer("RequestingPropName", c, "Smoke") end)
                    task.wait(0.05)
                    for i = 1, smokeTotal do
                        local pos = getRandomPos(-3)
                        table.insert(placedPositions, pos)
                        table.insert(sbOffsets, pos - myPos)
                    end
                    local dupSmoke = spawnAndDuplicateProps(toolPropMake, placedPositions[9], smokeTotal, 0)
                    if not dupSmoke then
                        for i = 1, smokeTotal do
                            if not propsTool or not propsTool.Parent or propsTool.Parent ~= char or sbAborted() then break end
                            pcall(function() clearTools:FireServer("RequestingPropName", c, "Smoke") end)
                            task.wait(0.05)
                            pcall(function() toolPropMake:FireServer(workspace.Terrain, placedPositions[8 + i]) end)
                            if i < smokeTotal then task.wait(INTERVAL - 0.05) end
                        end
                    end
                else
                    for i = 1, smokeTotal do
                        if not propsTool or not propsTool.Parent or propsTool.Parent ~= char or sbAborted() then break end
                        local preWait = 0
                        local c = smokeColors[((i - 1) % #smokeColors) + 1]
                        pcall(function() clearTools:FireServer("RequestingPropName", c, "Smoke") end)
                        task.wait(0.05)
                        preWait = 0.05
                        local pos = getRandomPos(-3)
                        pcall(function() toolPropMake:FireServer(workspace.Terrain, pos) end)
                        table.insert(placedPositions, pos)
                        table.insert(sbOffsets, pos - myPos)
                        if i < smokeTotal then task.wait(INTERVAL - preWait) end
                    end
                end
            end

            
            task.spawn(function()
                task.wait(0.85)
                pcall(function() clearTools:FireServer("ClearAllTools") end)
                task.wait(0.1)
                pcall(function() clearTools:FireServer("ClosePropMenu") end)
                cleanupPropMakerTool()
            end)
                        -- [PATTERN BLIND V1 LOCKED] positions stables par index (comme le blind V1)
            
            if sbAborted() then return false end 
            sbLoopRunning = true
            task.spawn(function()
                local myPropName = "Prop" .. lp.Name
                local t0 = tick()
                task.wait(0.5) 
                while sbLoopRunning and not sbAborted() do
                    local targetPlayer = Players:FindFirstChild(sbLockedTarget)
                    local hrp = targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                    if not targetPlayer then sbLoopRunning = false break end
                    local targetPos = hrp and hrp.Position
                    if targetPos then
                        local currentProps = {}
                        for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                            for _, prop in pairs(folder:GetChildren()) do
                                if prop.Name == myPropName then
                                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                                    if scf then table.insert(currentProps, {prop = prop, scf = scf}) end
                                end
                            end
                        end
                        local t = tick() - t0
                        local propCount = math.max(#currentProps, 1)
                        for i, entry in pairs(currentProps) do
                            if not sbLoopRunning then break end
                            local baseAngle = (i / propCount) * (2 * math.pi)
                            local targetCF
                            if sbMode == "Smoke" then
                                
                                local a = baseAngle + t * 8
                                local r = 3
                                targetCF = CFrame.new(targetPos + Vector3.new(math.cos(a) * r, -2.5, math.sin(a) * r))
                            elseif sbMode == "Flowers" then
                                
                                local a = baseAngle + t * 2
                                local r = 4
                                targetCF = CFrame.new(targetPos + Vector3.new(math.cos(a) * r, -2.5, math.sin(a) * r))
                            elseif sbMode == "Skeleton" then
                                
                                local a = baseAngle + t * 8
                                local r = 3
                                
                                
                                targetCF = CFrame.new(targetPos + Vector3.new(math.cos(a) * r, 0, math.sin(a) * r), targetPos)
                            elseif sbMode == "Carpet" then
                                
                                
                                local colAngle = ((i - 1) % 3) * (2 * math.pi / 3)
                                local floorN = math.floor((i - 1) / 3)
                                local r = 4
                                targetCF = CFrame.new(targetPos + Vector3.new(math.cos(colAngle) * r, 1 + floorN * 4, math.sin(colAngle) * r))
                            elseif sbMode == "Combo" then
                                if i <= 8 then
                                    
                                    local a = baseAngle + t * 7
                                    local r = 3
                                    targetCF = CFrame.new(targetPos + Vector3.new(math.cos(a) * r, 0, math.sin(a) * r), targetPos)
                                else
                                    
                                    local a = baseAngle + t * 2.5
                                    local r = 5
                                    targetCF = CFrame.new(targetPos + Vector3.new(math.cos(a) * r, -2.5, math.sin(a) * r))
                                end
                            else
                                targetCF = CFrame.new(targetPos + Vector3.new(0, -2.5, 0))
                            end
                            if targetCF then
                                task.spawn(function() pcall(function() entry.scf:InvokeServer(targetCF) end) end)
                            end
                        end
                    end
                    task.wait(0.08)
                end
            end)
            return true
        end

        local function sbStop()
            sbLoopRunning = false
            sbSession = sbSession + 1 
            local RE2 = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            if RE2 then
                local ct = RE2:FindFirstChild("1Clea1rTool1s")
                if ct then
                    
                    pcall(function() ct:FireServer("ClearAllProps") end)
                    task.wait(0.1)
                    pcall(function() ct:FireServer("ClearAllTools") end)
                    task.wait(0.1)
                    pcall(function() ct:FireServer("ClosePropMenu") end)
                end
            end
            local lp2 = Players.LocalPlayer
            local char2 = lp2.Character
            local hum2 = char2 and char2:FindFirstChildOfClass("Humanoid")
            if hum2 then pcall(function() hum2:UnequipTools() end) end
            cleanupPropMakerTool()
        end

        
        TrollV2Tab:AddSection({"> Special Blind"})
        local sbModeDropdown = TrollV2Tab:AddDropdown({
            Name = "Blind Mode",
            Description = "Choose the special blind",
            Options = {"Smoke", "Flowers", "Skeleton Hug", "Carpet", "Skeleton + Smoke"},
            Default = "Smoke",
        })
        sbModeDropdown:Callback(function(value)
            
            if value == "Skeleton Hug" then
                sbMode = "Skeleton"
            elseif value == "Skeleton + Smoke" then
                sbMode = "Combo"
            else
                sbMode = value
            end
            warn("[SB] sbMode dropdown -> " .. tostring(sbMode))
        end)

        
        
        
        smokeColors = {"SmokePurple", "SmokeBlue", "SmokeYellow"}
        smokeColorDropdown = TrollV2Tab:AddDropdown({
            Name = "Smoke Colors",
            Description = "Choose the smoke colors (also used in Skeleton + Smoke)",
            Options = {"All 3 (Purple + Blue + Yellow)", "Purple + Blue", "Purple + Yellow", "Blue + Yellow", "Purple only", "Blue only", "Yellow only"},
            Default = "All 3 (Purple + Blue + Yellow)",
        })
        smokeColorDropdown:Callback(function(value)
            if value == "Purple + Blue" then smokeColors = {"SmokePurple", "SmokeBlue"}
            elseif value == "Purple + Yellow" then smokeColors = {"SmokePurple", "SmokeYellow"}
            elseif value == "Blue + Yellow" then smokeColors = {"SmokeBlue", "SmokeYellow"}
            elseif value == "Purple only" then smokeColors = {"SmokePurple"}
            elseif value == "Blue only" then smokeColors = {"SmokeBlue"}
            elseif value == "Yellow only" then smokeColors = {"SmokeYellow"}
            else smokeColors = {"SmokePurple", "SmokeBlue", "SmokeYellow"} end
        end)

        _G.SpecialBlindToggle = TrollV2Tab:AddToggle({
            Name = "Special Blind",
            Description = "Activates the selected special blind on the target",
            Default = false
        })
        _G.SpecialBlindToggle:Callback(function(value)
            if not _G.BTR_UI_READY then return end
            if value then
                if not _G.TrollTarget or _G.TrollTarget == "---" then
                    pcall(function() _G.SpecialBlindToggle:Set(false) end)
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Special Blind", Text = "Select a target first!", Duration = 3 })
                    return
                end
                local t = Players:FindFirstChild(_G.TrollTarget)
                if not t then
                    pcall(function() _G.SpecialBlindToggle:Set(false) end)
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Special Blind", Text = "Target not found!", Duration = 3 })
                    return
                end
                sbStart(t.Name)
            else
                sbStop()
            end
        end)
    end

TrollV2Tab:AddSection({
    "> Pirate Ship Tornado"
})

-- [LOCKED: WORK PERFECTLY] PIRATE SHIP TORNADO - VRAI CODE GOAT HUB

do
    local Players = game:GetService("Players")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local RunService = game:GetService("RunService")
    local LocalPlayer = Players.LocalPlayer

    local ShipSystem = {
        tornadoActive = false,
        currentShip = nil,
        antiSitWasActive = false,
        prevFling = false,
        tornadoSession = 0
    }

    
    local tornadoSpot = "Position"

    

    

    

    local tornadoHouseOwner = nil

    
    local tornadoHouseOwner = nil

    local function getHouseOwner(lot)
        local nm = lot.Name or ""
        
        local short = nm:gsub("House$", "")
        local owner = short:match("^(.*)_%d+$")
        if owner and owner ~= "" then return owner end
        
        if short ~= "" then return short end
        return nil
    end

    local function getHouseLot(ownerName)
        local lotsFolder = workspace:FindFirstChild("001_Lots")
        if not lotsFolder then return nil end
        local target = string.lower(ownerName)
        for _, lot in pairs(lotsFolder:GetChildren()) do
            if lot:IsA("Model") then
                local owner = getHouseOwner(lot)
                if owner and string.lower(owner) == target then
                    return lot
                end
            end
        end
        return nil
    end

    local function getHousePosition(ownerName)
        local lot = getHouseLot(ownerName)
        if not lot then return nil end
        
        local spawnPart = lot:FindFirstChild("HouseSpawnPosition") or lot:FindFirstChild("Spawn") or lot:FindFirstChildOfClass("SpawnLocation")
        if spawnPart then
            if spawnPart:IsA("BasePart") then
                return spawnPart.Position
            end
        end
        
        local ok, origin = pcall(function() return lot:GetBoundingBox() end)
        if ok and origin then return origin.Position end
        local pp = lot.PrimaryPart or lot:FindFirstChildOfClass("BasePart")
        if pp then return pp.Position end
        return nil
    end

    local function refreshHouseList()
        local list = {}
        local lotsFolder = workspace:FindFirstChild("001_Lots")
        if lotsFolder then
            for _, lot in pairs(lotsFolder:GetChildren()) do
                if lot:IsA("Model") then
                    local owner = getHouseOwner(lot)
                    if owner and string.find(lot.Name, "House") then
                        local dup = false
                        for _, o in pairs(list) do
                            if string.lower(tostring(o)) == string.lower(owner) then dup = true break end
                        end
                        if not dup then table.insert(list, owner) end
                    end
                end
            end
        end
        table.sort(list)
        return list
    end

    local function spawnPirateShip()
        local character = LocalPlayer.Character
        if not character then return nil end

        local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
        if not humanoidRootPart then return nil end

        humanoidRootPart.CFrame = CFrame.new(1754, -2, 58)
        task.wait(0.5)

        local re = ReplicatedStorage:WaitForChild("RE")
        local carRemote = re:FindFirstChild("1Ca1r")
        if carRemote then
            carRemote:FireServer("PickingBoat", "PirateFree")
        end
        task.wait(1)

        local vehicles = workspace:WaitForChild("Vehicles", 5)
        if vehicles then
            for _ = 1, 15 do
                local ship = vehicles:FindFirstChild(LocalPlayer.Name .. "Car")
                if ship then return ship end
                task.wait(0.2)
            end
        end
        return nil
    end

    local function playShipAudio()
        local audioID = 9068077052
        local args = {workspace, audioID, 1}

        for i = 1, 5 do
            local re = ReplicatedStorage.RE:FindFirstChild("1Gu1nSound1s")
            if re then
                re:FireServer(unpack(args))
            end

            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local sound = Instance.new("Sound")
                sound.SoundId = "rbxassetid://" .. audioID
                sound.Parent = LocalPlayer.Character.HumanoidRootPart
                sound:Play()

                task.wait(1.5)
                sound:Destroy()
            end
        end
    end

    local function startTornado()
        local character = LocalPlayer.Character
        local humanoid = character and character:FindFirstChildOfClass("Humanoid")
        local humanoidRootPart = character and character:FindFirstChild("HumanoidRootPart")
        if not character or not humanoid or not humanoidRootPart then return end

        local targetCenter = humanoidRootPart.Position
        if tornadoSpot == "House" and tornadoHouseOwner then
            local housePos = getHousePosition(tornadoHouseOwner)
            if housePos then
                targetCenter = housePos
            end
        end

        _G.VehicleFlingActive = true
        pcall(function()
            humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
        end)

        ShipSystem.tornadoActive = true
        ShipSystem.currentShip = spawnPirateShip()
        if not ShipSystem.currentShip then
            _G.VehicleFlingActive = false
            ShipSystem.tornadoActive = false
            return
        end

        local seat = ShipSystem.currentShip:FindFirstChild("Body")
        seat = seat and seat:FindFirstChild("VehicleSeat")
        if not seat then
            seat = ShipSystem.currentShip:FindFirstChildWhichIsA("VehicleSeat", true) or ShipSystem.currentShip:FindFirstChildWhichIsA("Seat", true)
        end
        if not seat then
            _G.VehicleFlingActive = false
            ShipSystem.tornadoActive = false
            return
        end

        if not ShipSystem.currentShip.PrimaryPart then
            ShipSystem.currentShip.PrimaryPart = seat
        end

        local sitTimeout = tick() + 6
        repeat
            task.wait(0.1)
            if not ShipSystem.tornadoActive then return end
            humanoidRootPart.CFrame = seat.CFrame * CFrame.new(0, 1, 0)
        until humanoid.SeatPart == seat or tick() > sitTimeout

        if humanoid.SeatPart ~= seat then
            _G.VehicleFlingActive = false
            ShipSystem.tornadoActive = false
            return
        end

        task.spawn(playShipAudio)

        task.delay(4, function()
            if not ShipSystem.tornadoActive then return end
            if humanoid.SeatPart then
                humanoid.Sit = false
            end
            pcall(function() humanoid:ChangeState(Enum.HumanoidStateType.GettingUp) end)
            pcall(function() humanoid.Jump = true end)
            task.wait(0.05)
            pcall(function() humanoid.Jump = false end)
            if humanoidRootPart then
                humanoidRootPart.CFrame = CFrame.new(targetCenter + Vector3.new(0, 3, 0))
            end
            _G.VehicleFlingActive = false
        end)

        task.spawn(function()
            local re = ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Player1sCa1r")
            while ShipSystem.tornadoActive and ShipSystem.currentShip and ShipSystem.currentShip.Parent do
                re:FireServer("Flip")
                task.wait(0.5)
            end
        end)

        local c = targetCenter
        local r = (tornadoSpot == "House") and 5 or 12
        local waypoints = {
            c + Vector3.new(0, 2, -r),
            c + Vector3.new(-r, 2, r),
            c + Vector3.new(r, 2, r),
            c + Vector3.new(r, 2, -r),
            c + Vector3.new(-r, 2, -r)
        }

        ShipSystem.currentShip:SetPrimaryPartCFrame(CFrame.new(waypoints[1]))

        local currentIndex = 1
        local nextIndex = 2
        local moveSpeed = 30
        local rotationSpeed = math.rad(720)
        local progress = 0
        local currentRotation = 0

        local connection
        connection = RunService.Heartbeat:Connect(function(dt)
            if not ShipSystem.tornadoActive or not ShipSystem.currentShip or not ShipSystem.currentShip.PrimaryPart or not ShipSystem.currentShip.Parent then
                connection:Disconnect()
                return
            end

            local startPos = waypoints[currentIndex]
            local endPos = waypoints[nextIndex]

            progress = progress + (moveSpeed * dt) / (startPos - endPos).Magnitude
            if progress >= 1 then
                progress = 0
                currentIndex = nextIndex
                nextIndex = (nextIndex % #waypoints) + 1
            end

            local newPos = startPos:lerp(endPos, progress)
            currentRotation = currentRotation + rotationSpeed * dt

            local cframe = CFrame.new(newPos) * CFrame.Angles(0, currentRotation, 0)
            ShipSystem.currentShip:SetPrimaryPartCFrame(cframe)
        end)
    end

    local function stopTornado()
        ShipSystem.tornadoActive = false
        _G.VehicleFlingActive = false

        task.spawn(function()
            local args = {"DeleteAllVehicles"}
            local re = ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r")
            if re then
                re:FireServer(unpack(args))
            end
        end)
    end

                
    local isTornadoSpotUpdating = false 
    local _tornadoSpotLabel = "Current Position"
    local _tornadoSpotLabels = {}

    local function buildTornadoOptions()
        local opts = {"Current Position"}
        local houses = refreshHouseList()
        _tornadoSpotLabels = {}
        for _, h in pairs(houses) do
            local lbl = "House: " .. h
            table.insert(opts, lbl)
            _tornadoSpotLabels[lbl] = h
        end
        return opts
    end

    local tornadoSpotDropdown = TrollV2Tab:AddDropdown({
        Name = "Tornado Zone",
        Description = "Current Position = the ship circles around you | House: <player> = circles around that player's house",
        Options = buildTornadoOptions(),
        Default = "Current Position",
    })
    tornadoSpotDropdown:Callback(function(value)
        if isTornadoSpotUpdating then return end 
        if type(value) == "string" and string.sub(value, 1, 7) == "House: " then
            tornadoSpot = "House"
            tornadoHouseOwner = string.sub(value, 8)
        else
            tornadoSpot = "Position"
            tornadoHouseOwner = nil
        end
        _tornadoSpotLabel = value
        warn("[TORNADO V2] Spot -> " .. tostring(value))
    end)

    
    local function softRefreshTornadoSpots()
        local prevLabel = _tornadoSpotLabel
        local prevOwner = tornadoHouseOwner
        local opts = buildTornadoOptions()
        
        local stillThere = false
        if prevLabel == "Current Position" then
            stillThere = true
        elseif prevOwner and _tornadoSpotLabels["House: " .. prevOwner] then
            stillThere = true
        end
        isTornadoSpotUpdating = true
        pcall(function() tornadoSpotDropdown:Set(opts) end)
        if stillThere then
            
            
            if prevOwner then
                pcall(function() tornadoSpotDropdown:SetValue("House: " .. prevOwner) end)
            else
                pcall(function() tornadoSpotDropdown:SetValue("Current Position") end)
            end
            tornadoSpot = prevOwner and "House" or "Position"
            tornadoHouseOwner = prevOwner
            _tornadoSpotLabel = prevLabel
        else
            
            pcall(function() tornadoSpotDropdown:SetValue("Current Position") end)
            tornadoSpot = "Position"
            tornadoHouseOwner = nil
            _tornadoSpotLabel = "Current Position"
        end
        isTornadoSpotUpdating = false
    end

    
    task.spawn(function()
        local lotsFolder = workspace:WaitForChild("001_Lots", 10)
        if not lotsFolder then return end
        lotsFolder.ChildAdded:Connect(function()
            task.wait(0.5)
            pcall(softRefreshTornadoSpots)
        end)
        lotsFolder.ChildRemoved:Connect(function()
            task.wait(0.3)
            pcall(softRefreshTornadoSpots)
        end)
    end)

    
    task.spawn(function()
        local lastSig = ""
        while true do
            task.wait(12)
            local opts = buildTornadoOptions()
            local sig = table.concat(opts, "|")
            if sig ~= lastSig then
                lastSig = sig
                pcall(softRefreshTornadoSpots)
            end
        end
    end)
local tornadoLockOwner = false 
    local TornadoToggle = TrollV2Tab:AddToggle({
        Name = "Pirate Ship Tornado",
        Description = "Pulls Pirate Ship to the Square",
        Default = false
    })

    TornadoToggle:Callback(function(Value)
        if Value then
            if _G.TornadoLock then
                pcall(function() game:GetService("StarterGui"):SetCore("SendNotification", {Title="Pirate Ship Tornado", Text="Tornado already active! Turn off the other one first.", Duration=3}) end)
                TornadoToggle:SetValue(false)
                return
            end
            tornadoLockOwner = true
            _G.TornadoLock = true
            task.spawn(function()
                startTornado()
                task.wait(5)
                TornadoToggle:SetValue(false)
            end)
        else
            stopTornado()
            if tornadoLockOwner then
                tornadoLockOwner = false
                _G.TornadoLock = false
            end
        end
    end)
end

-- Real Black Hole (Dense Black Sphere + Universal Gravity Void) [LOCKED: WORK PERFECTLY]

do
    local Players = game:GetService("Players")
    local Workspace = game:GetService("Workspace")
    local StarterGui = game:GetService("StarterGui")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local RunService = game:GetService("RunService")
    local SoundService = game:GetService("SoundService")

    local LocalPlayer = Players.LocalPlayer

    local function bhNotify(title, text, duration)
        pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = title or "Black Hole",
                Text = text or "",
                Duration = duration or 3
            })
        end)
    end

    local BLACK_HOLE_CENTER_POS = Vector3.new(-26, 21, 14)
    local PLAYER_TP_POS = Vector3.new(-27, 10, 1)

    local RE = ReplicatedStorage:WaitForChild("RE", 5)
    local ToolRemote = RE and RE:WaitForChild("1Too1l", 5)
    local ClearRemote = RE and RE:WaitForChild("1Clea1rTool1s", 5)

    local BH_PROP_NAME = "FloorCushion"
    local BH_PROP_CATEGORY = "Furniture"

    local blackHoleRunning = false
    local blackHoleSession = 0
    local blackHolePower = 50000
    local propCount = 15

    local originalWalkSpeed = 16
    local originalJumpPower = 50
    local isMovementLocked = false

    local function lockMovement(lock)
        local char = LocalPlayer.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        local root = char and char:FindFirstChild("HumanoidRootPart")

        if lock then
            isMovementLocked = true
            if hum then
                originalWalkSpeed = hum.WalkSpeed > 0 and hum.WalkSpeed or originalWalkSpeed
                originalJumpPower = hum.JumpPower > 0 and hum.JumpPower or originalJumpPower
                hum.WalkSpeed = 0
                hum.JumpPower = 0
            end
            if root then root.Anchored = true end
        else
            isMovementLocked = false
            if hum then
                hum.WalkSpeed = (originalWalkSpeed and originalWalkSpeed > 0) and originalWalkSpeed or 16
                hum.JumpPower = (originalJumpPower and originalJumpPower > 0) and originalJumpPower or 50
            end
            if root then root.Anchored = false end
        end
    end

    local blackHoleFolder = nil
    local blackHoleAnchorPart = nil
    local blackHoleAtt1 = nil
    local forcedParts = {}
    local forcedVehicles = {}
    local heartbeatConn = nil
    local descendantConn = nil

    local function findVehicleModel(part)
        if not part then return nil end
        local cur = part.Parent
        while cur and cur ~= Workspace do
            if cur:IsA("Model") then
                local vehiclesFolder = Workspace:FindFirstChild("Vehicles")
                if vehiclesFolder and cur.Parent == vehiclesFolder then
                    return cur
                end
                local wc = Workspace:FindFirstChild("WorkspaceCom")
                if wc and cur:IsDescendantOf(wc) then
                    return cur
                end
                for _, d in ipairs(cur:GetChildren()) do
                    if d:IsA("VehicleSeat") then
                        return cur
                    end
                end
                local n = cur.Name:lower()
                if n:find("heli") or n:find("car") or n:find("vehicle") or n:find("boat") or n:find("plane") then
                    return cur
                end
            end
            cur = cur.Parent
        end
        return nil
    end

    local function isBuildingDoor(part)
        local vehiclesFolder = Workspace:FindFirstChild("Vehicles")
        if vehiclesFolder and part:IsDescendantOf(vehiclesFolder) then
            return false
        end
        local wc = Workspace:FindFirstChild("WorkspaceCom")
        local helis = wc and wc:FindFirstChild("001_HeliStorage")
        if helis and part:IsDescendantOf(helis) then
            return false
        end

        local n = part.Name:lower()
        if n:find("door") or n:find("porte") or n:find("gate") then
            return true
        end
        if part.Parent and (part.Parent.Name:lower():find("door") or part.Parent.Name:lower():find("gate")) then
            return true
        end
        return false
    end

    local function cleanupGravityVoid()
        if heartbeatConn then heartbeatConn:Disconnect(); heartbeatConn = nil end
        if descendantConn then descendantConn:Disconnect(); descendantConn = nil end

        for part, data in pairs(forcedParts) do
            if typeof(data) == "table" and data.Part and data.Part.Parent then
                pcall(function()
                    if data.Align then data.Align:Destroy() end
                    if data.Torque then data.Torque:Destroy() end
                    if data.Att then data.Att:Destroy() end
                    data.Part.CanCollide = true
                    if data.Model and data.Model.Parent then
                        for _, d in ipairs(data.Model:GetDescendants()) do
                            if d:IsA("BasePart") then
                                d.CanCollide = true
                                pcall(function() d.Massless = false end)
                            elseif d:IsA("VehicleSeat") or d:IsA("Seat") then
                                d.Disabled = false
                            end
                        end
                    end
                end)
            end
        end
        forcedParts = {}
        forcedVehicles = {}

        if blackHoleFolder then
            pcall(function() blackHoleFolder:Destroy() end)
            blackHoleFolder = nil
            blackHoleAnchorPart = nil
            blackHoleAtt1 = nil
        end
    end

    local function applyGravityToPart(v)
        if not blackHoleRunning or not blackHoleAtt1 then return end
        if not v or not v:IsA("BasePart") or v.Anchored then return end
        if isBuildingDoor(v) then return end

        local myChar = LocalPlayer.Character
        if myChar and v:IsDescendantOf(myChar) then return end
        local myBp = LocalPlayer:FindFirstChild("Backpack")
        if myBp and v:IsDescendantOf(myBp) then return end
        if v == blackHoleAnchorPart then return end

        local resp = math.clamp(150 + (blackHolePower / 50), 150, 1000)
        local spinPwr = math.clamp(5000000 + (blackHolePower * 10000), 5000000, 500000000)

        local vehModel = findVehicleModel(v)
        if vehModel then
            if forcedVehicles[vehModel] then return end
            forcedVehicles[vehModel] = true

            pcall(function()
                local root = vehModel.PrimaryPart or vehModel:FindFirstChild("Body") or vehModel:FindFirstChildWhichIsA("VehicleSeat") or vehModel:FindFirstChildWhichIsA("BasePart")
                if not root or root.Anchored then return end

                for _, d in ipairs(vehModel:GetDescendants()) do
                    if d:IsA("BasePart") then
                        d.CanCollide = false
                        pcall(function() d.Massless = true end)
                        for _, x in ipairs(d:GetChildren()) do
                            if x.Name:find("BH_") then x:Destroy() end
                        end
                    elseif d:IsA("VehicleSeat") or d:IsA("Seat") then
                        d.Disabled = true
                        pcall(function() d.HandBrake = false end)
                    end
                end

                local att0 = Instance.new("Attachment")
                att0.Name = "BH_Att"
                att0.Position = Vector3.new(math.random(-4, 4), math.random(-2, 2), math.random(-4, 4))
                att0.Parent = root

                local align = Instance.new("AlignPosition")
                align.Name = "BH_Align"
                align.Mode = Enum.PositionAlignmentMode.TwoAttachment
                align.Attachment0 = att0
                align.Attachment1 = blackHoleAtt1
                align.MaxForce = math.huge
                align.MaxVelocity = math.huge
                align.Responsiveness = resp
                align.ReactionForceEnabled = false
                align.ApplyAtCenterOfMass = true
                align.Parent = root

                local torque = Instance.new("Torque")
                torque.Name = "BH_Torque"
                torque.Attachment0 = att0
                torque.RelativeTo = Enum.ActuatorRelativeTo.World
                torque.Torque = Vector3.new(
                    math.random(-1, 1) * spinPwr,
                    spinPwr,
                    math.random(-1, 1) * spinPwr
                )
                torque.Parent = root

                forcedParts[root] = {
                    Part = root,
                    Model = vehModel,
                    Align = align,
                    Torque = torque,
                    Att = att0
                }
            end)
            return
        end

        if forcedParts[v] then return end

        pcall(function()
            for _, x in ipairs(v:GetChildren()) do
                if x.Name:find("BH_") then x:Destroy() end
            end

            v.CanCollide = false

            local att0 = Instance.new("Attachment")
            att0.Name = "BH_Att"
            att0.Position = Vector3.new(math.random(-3, 3), math.random(-2, 2), math.random(-3, 3))
            att0.Parent = v

            local align = Instance.new("AlignPosition")
            align.Name = "BH_Align"
            align.Mode = Enum.PositionAlignmentMode.TwoAttachment
            align.Attachment0 = att0
            align.Attachment1 = blackHoleAtt1
            align.MaxForce = math.huge
            align.MaxVelocity = math.huge
            align.Responsiveness = resp
            align.ReactionForceEnabled = false
            align.ApplyAtCenterOfMass = true
            align.Parent = v

            local torque = Instance.new("Torque")
            torque.Name = "BH_Torque"
            torque.Attachment0 = att0
            torque.RelativeTo = Enum.ActuatorRelativeTo.World
            torque.Torque = Vector3.new(
                math.random(-1, 1) * spinPwr,
                spinPwr,
                math.random(-1, 1) * spinPwr
            )
            torque.Parent = v

            forcedParts[v] = {
                Part = v,
                Align = align,
                Torque = torque,
                Att = att0
            }
        end)
    end

    local function scanAllUnanchored()
        local count = 0
        local all = Workspace:GetDescendants()
        for i = 1, #all do
            if not blackHoleRunning then break end
            local obj = all[i]
            if obj:IsA("BasePart") and not obj.Anchored and not isBuildingDoor(obj) then
                applyGravityToPart(obj)
            elseif obj:IsA("VehicleSeat") or obj:IsA("Seat") then
                pcall(function()
                    obj.HandBrake = false
                    obj.Disabled = true
                end)
            end
            count = count + 1
            if count % 250 == 0 then
                task.wait()
            end
        end

        local vehiclesFolder = Workspace:FindFirstChild("Vehicles")
        if vehiclesFolder then
            for _, veh in ipairs(vehiclesFolder:GetChildren()) do
                for _, d in ipairs(veh:GetDescendants()) do
                    if d:IsA("BasePart") and not d.Anchored then
                        applyGravityToPart(d)
                    elseif d:IsA("VehicleSeat") or d:IsA("Seat") then
                        pcall(function()
                            d.HandBrake = false
                            d.Disabled = true
                        end)
                    end
                end
            end
        end

        local wc = Workspace:FindFirstChild("WorkspaceCom")
        local helis = wc and wc:FindFirstChild("001_HeliStorage")
        if helis then
            for _, h in ipairs(helis:GetChildren()) do
                for _, d in ipairs(h:GetDescendants()) do
                    if d:IsA("BasePart") and not d.Anchored then
                        applyGravityToPart(d)
                    end
                end
            end
        end
    end

    local function setupGravityVoid()
        cleanupGravityVoid()

        blackHoleFolder = Instance.new("Folder")
        blackHoleFolder.Name = "BTR_BlackHoleVoid"
        blackHoleFolder.Parent = Workspace

        blackHoleAnchorPart = Instance.new("Part")
        blackHoleAnchorPart.Name = "BH_Center"
        blackHoleAnchorPart.Size = Vector3.new(2, 2, 2)
        blackHoleAnchorPart.CFrame = CFrame.new(BLACK_HOLE_CENTER_POS)
        blackHoleAnchorPart.Anchored = true
        blackHoleAnchorPart.CanCollide = false
        blackHoleAnchorPart.Transparency = 1
        blackHoleAnchorPart.Parent = blackHoleFolder

        blackHoleAtt1 = Instance.new("Attachment")
        blackHoleAtt1.Name = "BH_SingularityAtt"
        blackHoleAtt1.Parent = blackHoleAnchorPart

        pcall(function()
            LocalPlayer.ReplicationFocus = Workspace
            if setscriptable then
                setscriptable(LocalPlayer, "SimulationRadius", true)
                setscriptable(LocalPlayer, "MaximumSimulationRadius", true)
            end
            if sethiddenproperty then
                sethiddenproperty(LocalPlayer, "MaximumSimulationRadius", 1e9)
                sethiddenproperty(LocalPlayer, "SimulationRadius", 1e9)
            end
        end)

        task.spawn(scanAllUnanchored)

        descendantConn = Workspace.DescendantAdded:Connect(function(v)
            if blackHoleRunning then
                task.wait(0.04)
                if v:IsA("BasePart") and not v.Anchored and not isBuildingDoor(v) then
                    applyGravityToPart(v)
                end
            end
        end)

        local lastPeriodicScan = tick()
        heartbeatConn = RunService.Heartbeat:Connect(function()
            if not blackHoleRunning then return end

            pcall(function()
                if sethiddenproperty then
                    sethiddenproperty(LocalPlayer, "MaximumSimulationRadius", 1e9)
                    sethiddenproperty(LocalPlayer, "SimulationRadius", 1e9)
                end
            end)

            local myChar = LocalPlayer.Character
            local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
            local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")
            if myHrp and myHum then
                if myHum.Sit then myHum.Sit = false end
                local toCenter = (BLACK_HOLE_CENTER_POS - myHrp.Position)
                if toCenter.Magnitude < 50 then
                    local vel = myHrp.AssemblyLinearVelocity
                    local dot = vel:Dot(toCenter.Unit)
                    if dot > 0 then
                        myHrp.AssemblyLinearVelocity = vel - (toCenter.Unit * dot)
                    end
                end
            end

            local now = tick()
            if now - lastPeriodicScan > 2.0 then
                lastPeriodicScan = now
                task.spawn(scanAllUnanchored)
            end
        end)
    end

    local function cleanupPropMakerLocal()
        local char = LocalPlayer.Character
        local bp = LocalPlayer:FindFirstChild("Backpack")
        pcall(function()
            local h = char and char:FindFirstChildOfClass("Humanoid")
            if h then h:UnequipTools() end
        end)
        if char then
            local tool = char:FindFirstChild("PropMaker")
            if tool and bp then tool.Parent = bp end
        end
    end

    local function duplicateProps(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
        if not dupRemote then return false end

        for i = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
            task.wait(0.02)
        end
        return true
    end

    local realBlackHoleToggle = nil
    local isStoppingInternally = false

    local function stopRealBlackHole(reason, fromToggle)
        if not blackHoleRunning and not isMovementLocked then return end
        blackHoleRunning = false
        blackHoleSession = blackHoleSession + 1

        cleanupGravityVoid()

        if ClearRemote then
            pcall(function() ClearRemote:FireServer("ClearAllProps") end)
        end
        cleanupPropMakerLocal()

        task.spawn(function()
            local char = LocalPlayer.Character
            local myHrp = char and char:FindFirstChild("HumanoidRootPart")
            local start = tick()
            while tick() - start < 0.6 do
                if myHrp and myHrp.Parent then
                    myHrp.AssemblyLinearVelocity = Vector3.zero
                    myHrp.AssemblyAngularVelocity = Vector3.zero
                end
                RunService.Heartbeat:Wait()
            end
            lockMovement(false)
        end)

        if not fromToggle and realBlackHoleToggle and realBlackHoleToggle.Set and not isStoppingInternally then
            isStoppingInternally = true
            pcall(function() realBlackHoleToggle:Set(false) end)
            task.delay(0.35, function()
                isStoppingInternally = false
            end)
        end

        if reason then
            bhNotify("Real Black Hole", "Black Hole cancelled: " .. tostring(reason), 3)
        else
            bhNotify("Real Black Hole", "Black Hole collapsed.", 2)
        end
    end

    local function startRealBlackHole()
        if blackHoleRunning then
            stopRealBlackHole()
            task.wait(0.3)
        end

        blackHoleRunning = true
        blackHoleSession = blackHoleSession + 1
        local mySession = blackHoleSession

        task.spawn(function()
            local char = LocalPlayer.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local myRoot = char and char:FindFirstChild("HumanoidRootPart")
            if not char or not hum or not myRoot then
                stopRealBlackHole("Character missing")
                return
            end

            myRoot.CFrame = CFrame.new(PLAYER_TP_POS)
            lockMovement(true)
            bhNotify("Real Black Hole", "Initializing smooth singularity...", 2)

            local diedConn = nil
            diedConn = hum.Died:Connect(function()
                if diedConn then diedConn:Disconnect() end
                stopRealBlackHole("Player died")
            end)

            local charConn = nil
            charConn = LocalPlayer.CharacterRemoving:Connect(function()
                if charConn then charConn:Disconnect() end
                stopRealBlackHole("Character removed")
            end)

            local propMaker = getOrFetchPropMaker()
            if not propMaker or not blackHoleRunning or blackHoleSession ~= mySession then
                if diedConn then diedConn:Disconnect() end
                if charConn then charConn:Disconnect() end
                stopRealBlackHole("PropMaker unavailable")
                return
            end

            hum:EquipTool(propMaker)
            local eqStart = tick()
            while tick() - eqStart < 1.5 do
                if propMaker.Parent == char then break end
                task.wait(0.05)
            end

            if ClearRemote then
                pcall(function() ClearRemote:FireServer("OpenPropMenu") end)
                task.wait(0.2)
                pcall(function() ClearRemote:FireServer("RequestingPropName", BH_PROP_NAME, BH_PROP_CATEGORY) end)
                task.wait(0.3)
            end

            local toolPropMake = propMaker:FindFirstChild("Tool_PropMake")
            local wsStart = tick()
            while not toolPropMake and tick() - wsStart < 4 and blackHoleRunning and blackHoleSession == mySession do
                toolPropMake = propMaker:FindFirstChild("Tool_PropMake")
                if not toolPropMake then
                    local bp = LocalPlayer:FindFirstChild("Backpack")
                    local bpTool = bp and bp:FindFirstChild("PropMaker")
                    if bpTool then toolPropMake = bpTool:FindFirstChild("Tool_PropMake") end
                end
                if not toolPropMake then task.wait(0.08) end
            end

            if not toolPropMake then
                if diedConn then diedConn:Disconnect() end
                if charConn then charConn:Disconnect() end
                blackHoleRunning = false
                lockMovement(false)
                return
            end

            local myPropName = "Prop" .. LocalPlayer.Name
            local wc = Workspace:FindFirstChild("WorkspaceCom")
            if not wc or not blackHoleRunning or blackHoleSession ~= mySession then
                if diedConn then diedConn:Disconnect() end
                if charConn then charConn:Disconnect() end
                stopRealBlackHole("WorkspaceCom missing")
                return
            end

            local existingProps = {}
            for _, folder in pairs(wc:GetChildren()) do
                for _, p in pairs(folder:GetChildren()) do
                    if p.Name == myPropName then existingProps[p] = true end
                end
            end

            pcall(function() toolPropMake:FireServer(Workspace.Terrain, BLACK_HOLE_CENTER_POS) end)

            local firstProp = nil
            local t0 = tick()
            while tick() - t0 < 3.5 do
                if not blackHoleRunning or blackHoleSession ~= mySession or hum.Health <= 0 then
                    if diedConn then diedConn:Disconnect() end
                    if charConn then charConn:Disconnect() end
                    stopRealBlackHole("Interrupted during spawn")
                    return
                end
                for _, folder in pairs(wc:GetChildren()) do
                    for _, p in pairs(folder:GetChildren()) do
                        if p.Name == myPropName and not existingProps[p] then
                            firstProp = p
                            break
                        end
                    end
                    if firstProp then break end
                end
                if firstProp then break end
                task.wait(0.04)
            end

            if not firstProp then
                if diedConn then diedConn:Disconnect() end
                if charConn then charConn:Disconnect() end
                stopRealBlackHole("Failed to spawn prop")
                return
            end

            local totalSingularityProps = 15
            duplicateProps(firstProp, totalSingularityProps - 1)

            task.spawn(function()
                task.wait(1.0)
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker")
                end)
                cleanupPropMakerLocal()
            end)

            local activeProps = {}
            task.wait(0.35)
            for _, folder in pairs(wc:GetChildren()) do
                for _, p in pairs(folder:GetChildren()) do
                    if p.Name == myPropName then
                        local scf = p:FindFirstChild("SetCurrentCFrame")
                        local cpc = p:FindFirstChild("ChangePropColor")
                        if scf then
                            table.insert(activeProps, { Prop = p, SCF = scf, CPC = cpc })
                            for _, pt in ipairs(p:GetDescendants()) do
                                if pt:IsA("BasePart") then
                                    pt.CanCollide = false
                                end
                            end
                        end
                    end
                end
            end

            if #activeProps == 0 or not blackHoleRunning or blackHoleSession ~= mySession then
                if diedConn then diedConn:Disconnect() end
                if charConn then charConn:Disconnect() end
                stopRealBlackHole("No props found")
                return
            end

            for _, item in ipairs(activeProps) do
                if item.CPC then
                    pcall(function() item.CPC:InvokeServer(Color3.fromRGB(0, 0, 0)) end)
                end
            end

            setupGravityVoid()
            lockMovement(false)

            if myRoot and myRoot.Parent then
                myRoot.CFrame = CFrame.new(PLAYER_TP_POS)
                myRoot.AssemblyLinearVelocity = Vector3.zero
                myRoot.AssemblyAngularVelocity = Vector3.zero
            end

            bhNotify("Real Black Hole", "Singularity Active! Smooth Black Void Awakened", 3)

            task.spawn(function()
                task.wait(2.0)
                if not blackHoleRunning or blackHoleSession ~= mySession then return end

                local targets = {}
                local vehiclesFolder = Workspace:FindFirstChild("Vehicles")
                if vehiclesFolder then
                    for _, veh in ipairs(vehiclesFolder:GetChildren()) do
                        local seat = veh:FindFirstChildWhichIsA("VehicleSeat", true) or veh:FindFirstChildWhichIsA("BasePart", true)
                        if seat then
                            table.insert(targets, { Model = veh, TargetPart = seat })
                        end
                    end
                end

                local wcRef = Workspace:FindFirstChild("WorkspaceCom")
                local helis = wcRef and wcRef:FindFirstChild("001_HeliStorage")
                if helis then
                    for _, h in ipairs(helis:GetChildren()) do
                        local seat = h:FindFirstChildWhichIsA("VehicleSeat", true) or h:FindFirstChildWhichIsA("BasePart", true)
                        if seat then
                            table.insert(targets, { Model = h, TargetPart = seat })
                        end
                    end
                end

                if #targets > 0 then
                    for _, item in ipairs(targets) do
                        if not blackHoleRunning or blackHoleSession ~= mySession then break end
                        if item.TargetPart and item.TargetPart.Parent and myRoot and myRoot.Parent then
                            myRoot.CFrame = item.TargetPart.CFrame * CFrame.new(0, 3.5, 0)
                            task.wait(0.04)
                        end
                    end

                    task.wait(1.0)
                end

                if myRoot and myRoot.Parent then
                    myRoot.CFrame = CFrame.new(PLAYER_TP_POS)
                    myRoot.AssemblyLinearVelocity = Vector3.zero
                    myRoot.AssemblyAngularVelocity = Vector3.zero
                end
            end)

            local sound = Instance.new("Sound")
            sound.SoundId = "rbxassetid://9069609200"
            sound.Volume = 1
            sound.Parent = SoundService
            sound:Play()

            local senders = {}
            local function sendCFrame(scfRef, cf)
                local entry = senders[scfRef]
                if not entry then
                    entry = { lastCF = nil, dirty = false }
                    entry.thread = task.spawn(function()
                        while blackHoleRunning and blackHoleSession == mySession do
                            task.wait(0.025)
                            if not blackHoleRunning or blackHoleSession ~= mySession then return end
                            if entry.dirty and entry.lastCF then
                                entry.dirty = false
                                pcall(function() scfRef:InvokeServer(entry.lastCF) end)
                            end
                        end
                    end)
                    senders[scfRef] = entry
                end
                entry.lastCF = cf
                entry.dirty = true
            end

            local t = 0
            local totalProps = #activeProps

            while blackHoleRunning and blackHoleSession == mySession do
                if not LocalPlayer.Character or not hum or hum.Health <= 0 then
                    stopRealBlackHole("Player died")
                    break
                end

                local centerPos = BLACK_HOLE_CENTER_POS

                if blackHoleAnchorPart then
                    blackHoleAnchorPart.CFrame = CFrame.new(centerPos)
                end

                t = t + 0.04
                local rotAngle = t * 6

                for i, data in ipairs(activeProps) do
                    if not blackHoleRunning or blackHoleSession ~= mySession then break end

                    local phi = math.acos(-1 + (2 * (i - 1)) / math.max(totalProps - 1, 1))
                    local theta = math.sqrt(totalProps * math.pi) * phi + rotAngle
                    local r = 5.2
                    local x = r * math.sin(phi) * math.cos(theta)
                    local y = r * math.sin(phi) * math.sin(theta)
                    local z = r * math.cos(phi)
                    local pPos = centerPos + Vector3.new(x, y, z)
                    local baseCF = CFrame.new(pPos, centerPos) * CFrame.Angles(0, math.pi / 2, 0)
                    local targetCF = baseCF * CFrame.Angles(0, rotAngle * 2, 0)

                    sendCFrame(data.SCF, targetCF)
                end

                task.wait(0.025)
            end

            if diedConn then diedConn:Disconnect() end
            if charConn then charConn:Disconnect() end
            sound:Destroy()
        end)
    end

    _G.BTR_StopRealBlackHole = stopRealBlackHole

    TrollV2Tab:AddSection({
        "> Real Black Hole V2"
    })

    TrollV2Tab:AddSlider({
        Name = "Attraction Power",
        Description = "Gravity pull strength on server parts and players",
        Min = 500,
        Max = 50000,
        Increase = 500,
        Default = 50000,
        Suffix = " pwr",
        Callback = function(val)
            blackHolePower = val
            local resp = math.clamp(150 + (val / 50), 150, 1000)
            local spinPwr = math.clamp(5000000 + (val * 10000), 5000000, 500000000)
            for part, data in pairs(forcedParts) do
                if typeof(data) == "table" then
                    if data.Align and data.Align.Parent then
                        data.Align.Responsiveness = resp
                    end
                    if data.Torque and data.Torque.Parent then
                        data.Torque.Torque = Vector3.new(
                            math.random(-1, 1) * spinPwr,
                            spinPwr,
                            math.random(-1, 1) * spinPwr
                        )
                    end
                end
            end
        end
    })

    realBlackHoleToggle = TrollV2Tab:AddToggle({
        Name = "Real Black Hole V2",
        Description = "Creates a giant black hole with props and violently pulls all unanchored parts & vehicles to spawn",
        Default = false,
        Callback = function(state)
            if not _G.BTR_UI_READY then return end
            if isStoppingInternally then return end
            if not state and not blackHoleRunning then return end
            if state then
                task.spawn(startRealBlackHole)
            else
                task.spawn(function()
                    stopRealBlackHole(nil, true)
                end)
            end
        end
    })
end

-- END REAL BLACK HOLE [LOCKED: WORK PERFECTLY]

do
    local Players = game:GetService("Players")
    local Workspace = game:GetService("Workspace")
    local StarterGui = game:GetService("StarterGui")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local SoundService = game:GetService("SoundService")

    local LocalPlayer = Players.LocalPlayer

    local function lpNotify(title, text, duration)
        pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = title or "Light Player",
                Text = text or "",
                Duration = duration or 3
            })
        end)
    end

    local RE = ReplicatedStorage:WaitForChild("RE", 5)
    local ToolRemote = RE and RE:WaitForChild("1Too1l", 5)
    local ClearRemote = RE and RE:WaitForChild("1Clea1rTool1s", 5)

    local LIGHT_PROP_NAME = "LightsSpotLightWhite"
    local LIGHT_PROP_CATEGORY = "Lights"

    local lightPlayerRunning = false
    local lightPlayerSession = 0
    local propCount = 15
    local beamHeight = 14.0
    local targetOption = "Self"

    local function cleanupPropMaker()
        local char = LocalPlayer.Character
        local bp = LocalPlayer:FindFirstChild("Backpack")
        pcall(function()
            local h = char and char:FindFirstChildOfClass("Humanoid")
            if h then h:UnequipTools() end
        end)
        if char then
            local tool = char:FindFirstChild("PropMaker")
            if tool and bp then tool.Parent = bp end
        end
    end

    local function getPropMaker()
        local char = LocalPlayer.Character
        local bp = LocalPlayer:FindFirstChild("Backpack")
        if not char or not bp then return nil end

        local tool = char:FindFirstChild("PropMaker") or bp:FindFirstChild("PropMaker")
        if not tool and ToolRemote then
            pcall(function() ToolRemote:InvokeServer("PickingTools", "PropMaker") end)
            local t0 = tick()
            while tick() - t0 < 1.5 do
                tool = char:FindFirstChild("PropMaker") or bp:FindFirstChild("PropMaker")
                if tool then break end
                task.wait(0.03)
            end
        end

        if tool and tool.Parent ~= char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:EquipTool(tool)
                local eqStart = tick()
                while tick() - eqStart < 1 do
                    if tool.Parent == char then break end
                    task.wait(0.03)
                end
            end
        end
        return tool
    end

    local function duplicateProps(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 1.5)
        if not dupRemote then return false end

        for i = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
        end
        return true
    end

    local function stopLightPlayer(reason)
        if not lightPlayerRunning then return end
        lightPlayerRunning = false
        lightPlayerSession = lightPlayerSession + 1

        if ClearRemote then
            pcall(function() ClearRemote:FireServer("ClearAllProps") end)
        end
        cleanupPropMaker()

        if reason then
            lpNotify("Light Player", "Deactivated: " .. tostring(reason), 3)
        else
            lpNotify("Light Player", "Divine Light Beam stopped.", 2)
        end
    end

    local function startLightPlayer()
        if lightPlayerRunning then
            stopLightPlayer()
            task.wait(0.2)
        end

        lightPlayerRunning = true
        lightPlayerSession = lightPlayerSession + 1
        local mySession = lightPlayerSession

        task.spawn(function()
            local char = LocalPlayer.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local myRoot = char and char:FindFirstChild("HumanoidRootPart")
            if not char or not hum or not myRoot then
                stopLightPlayer("Character missing")
                return
            end

            local activeTargetPlayer = nil
            if targetOption == "Target" and _G.TrollTarget and _G.TrollTarget ~= "---" then
                activeTargetPlayer = Players:FindFirstChild(_G.TrollTarget)
            end
            if not activeTargetPlayer then
                activeTargetPlayer = LocalPlayer
            end

            local tChar = activeTargetPlayer.Character
            local tRoot = tChar and tChar:FindFirstChild("HumanoidRootPart")
            local spawnPos = (tRoot and tRoot.Position or myRoot.Position) + Vector3.new(0, beamHeight, 0)

            lpNotify("Light Player", "Summoning Divine Pillar of Light...", 2)

            local propMaker = getPropMaker()
            if not propMaker or not lightPlayerRunning or lightPlayerSession ~= mySession then
                stopLightPlayer("PropMaker unavailable")
                return
            end

            if ClearRemote then
                pcall(function() ClearRemote:FireServer("OpenPropMenu") end)
                task.wait(0.12)
                pcall(function() ClearRemote:FireServer("RequestingPropName", LIGHT_PROP_NAME, LIGHT_PROP_CATEGORY) end)
                task.wait(0.15)
            end

            local toolPropMake = propMaker:FindFirstChild("Tool_PropMake") or propMaker:WaitForChild("Tool_PropMake", 3)
            if not toolPropMake or not lightPlayerRunning or lightPlayerSession ~= mySession then
                stopLightPlayer("Tool_PropMake missing")
                return
            end

            local myPropName = "Prop" .. LocalPlayer.Name
            local wc = Workspace:FindFirstChild("WorkspaceCom")
            if not wc or not lightPlayerRunning or lightPlayerSession ~= mySession then
                stopLightPlayer("WorkspaceCom missing")
                return
            end

            local existingProps = {}
            for _, folder in pairs(wc:GetChildren()) do
                for _, p in pairs(folder:GetChildren()) do
                    if p.Name == myPropName then existingProps[p] = true end
                end
            end

            pcall(function() toolPropMake:FireServer(Workspace.Terrain, spawnPos) end)

            local firstProp = nil
            local t0 = tick()
            while tick() - t0 < 3.0 do
                if not lightPlayerRunning or lightPlayerSession ~= mySession or hum.Health <= 0 then
                    stopLightPlayer("Interrupted during spawn")
                    return
                end
                for _, folder in pairs(wc:GetChildren()) do
                    for _, p in pairs(folder:GetChildren()) do
                        if p.Name == myPropName and not existingProps[p] then
                            firstProp = p
                            break
                        end
                    end
                    if firstProp then break end
                end
                if firstProp then break end
                task.wait(0.04)
            end

            if not firstProp then
                stopLightPlayer("Failed to spawn prop")
                return
            end

            if propCount > 1 then
                duplicateProps(firstProp, propCount - 1)
            end

            -- Retrait du carton apres 1s exacte (Pattern LOCKED)
            task.spawn(function()
                task.wait(1.0)
                if ToolRemote then
                    pcall(function() ToolRemote:InvokeServer("PickingTools", "PropMaker") end)
                end
                cleanupPropMaker()
            end)

            
            local activeProps = {}
            task.wait(0.25)
            for _, folder in pairs(wc:GetChildren()) do
                for _, p in pairs(folder:GetChildren()) do
                    if p.Name == myPropName then
                        local scf = p:FindFirstChild("SetCurrentCFrame")
                        local cpc = p:FindFirstChild("ChangePropColor")
                        if scf then
                            table.insert(activeProps, { Prop = p, SCF = scf, CPC = cpc })
                            if cpc then
                                pcall(function() cpc:InvokeServer(Color3.fromRGB(255, 255, 255)) end)
                            end
                        end
                    end
                end
            end

            if #activeProps == 0 or not lightPlayerRunning or lightPlayerSession ~= mySession then
                stopLightPlayer("No props found")
                return
            end

            lpNotify("Light Player", "Divine Pillar of Light ACTIVE!", 3)

            local sound = Instance.new("Sound")
            sound.SoundId = "rbxassetid://9069609200"
            sound.Volume = 0.8
            sound.Parent = SoundService
            sound:Play()

            
            local senders = {}
            local function sendCFrame(scfRef, cf)
                local entry = senders[scfRef]
                if not entry then
                    entry = { lastCF = nil, dirty = false }
                    entry.thread = task.spawn(function()
                        while lightPlayerRunning and lightPlayerSession == mySession do
                            task.wait(0.025)
                            if not lightPlayerRunning or lightPlayerSession ~= mySession then return end
                            if entry.dirty and entry.lastCF then
                                entry.dirty = false
                                pcall(function() scfRef:InvokeServer(entry.lastCF) end)
                            end
                        end
                    end)
                    senders[scfRef] = entry
                end
                entry.lastCF = cf
                entry.dirty = true
            end

            
            while lightPlayerRunning and lightPlayerSession == mySession do
                if not LocalPlayer.Character or not hum or hum.Health <= 0 then
                    stopLightPlayer("Player died")
                    break
                end

                local curTarget = LocalPlayer
                if targetOption == "Target" and _G.TrollTarget and _G.TrollTarget ~= "---" then
                    curTarget = Players:FindFirstChild(_G.TrollTarget) or LocalPlayer
                end

                local curChar = curTarget.Character
                local curRoot = curChar and curChar:FindFirstChild("HumanoidRootPart")
                local curHum = curChar and curChar:FindFirstChildOfClass("Humanoid")

                if not curRoot or not curHum or curHum.Health <= 0 then
                    task.wait(0.1)
                else
                    local lightPos = curRoot.Position + Vector3.new(0, beamHeight, 0)
                    
                    local targetCF = CFrame.new(lightPos) * CFrame.Angles(math.pi, 0, 0)

                    for _, data in ipairs(activeProps) do
                        if not lightPlayerRunning or lightPlayerSession ~= mySession then break end
                        sendCFrame(data.SCF, targetCF)
                    end
                end

                task.wait(0.02)
            end

            sound:Destroy()
        end)
    end

    TrollV2Tab:AddSection({"> Light Player"})

    TrollV2Tab:AddDropdown({
        Name = "Light Target",
        Description = "Choose who receives the divine pillar of light",
        Options = {"Self", "Target"},
        Default = "Self",
        Callback = function(val)
            targetOption = val
        end
    })

    TrollV2Tab:AddSlider({
        Name = "Beam Height",
        Description = "Height above target head (in studs)",
        Min = 8,
        Max = 25,
        Default = 14,
        Increase = 1,
        Callback = function(val)
            beamHeight = tonumber(val) or 14.0
        end
    })

    TrollV2Tab:AddToggle({
        Name = "Light Player",
        Description = "15 pure white stacked spotlights pointing 180 deg downward creating a divine light pillar",
        Default = false,
        Callback = function(state)
            if not _G.BTR_UI_READY then return end
            if state then
                startLightPlayer()
            else
                stopLightPlayer()
            end
        end
    })
end

do
    local Players = game:GetService("Players")
    local Workspace = game:GetService("Workspace")
    local StarterGui = game:GetService("StarterGui")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local LocalPlayer = Players.LocalPlayer

    local function beNotify(title, text, duration)
        pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = title or "Black Expansion",
                Text = text or "",
                Duration = duration or 3
            })
        end)
    end

    local RE = ReplicatedStorage:WaitForChild("RE", 5)
    local ToolRemote = RE and RE:WaitForChild("1Too1l", 5)
    local ClearRemote = RE and RE:WaitForChild("1Clea1rTool1s", 5)
    local Telemetry = ReplicatedStorage:FindFirstChild("Remotes") and ReplicatedStorage.Remotes:FindFirstChild("TelemetryClientInteraction")

    local PROP_NAME = "BigMilitaryTent"
    local PROP_CATEGORY = "Building"

    local blackExpRunning = false
    local blackExpSession = 0
    local propCount = 15
    local forwardOffset = 0.40
    local heightOffset = -8.0
    local lateralOffset = 0.0
    local rotSpeed = 5.0
    local selectedTargetName = LocalPlayer.Name

    local function getPlayerList()
        local list = {}
        for _, p in ipairs(Players:GetPlayers()) do
            table.insert(list, p.Name)
        end
        return list
    end

    local function cleanupPropMaker()
        local char = LocalPlayer.Character
        local bp = LocalPlayer:FindFirstChild("Backpack")
        pcall(function()
            local h = char and char:FindFirstChildOfClass("Humanoid")
            if h then h:UnequipTools() end
        end)
        if char then
            local tool = char:FindFirstChild("PropMaker")
            if tool and bp then tool.Parent = bp end
        end
    end

    local function getPropMaker()
        local char = LocalPlayer.Character
        local bp = LocalPlayer:FindFirstChild("Backpack")
        if not char or not bp then return nil end

        local tool = char:FindFirstChild("PropMaker") or bp:FindFirstChild("PropMaker")
        if not tool and ToolRemote then
            pcall(function() ToolRemote:InvokeServer("PickingTools", "PropMaker") end)
            local t0 = tick()
            while tick() - t0 < 1.5 do
                tool = char:FindFirstChild("PropMaker") or bp:FindFirstChild("PropMaker")
                if tool then break end
                task.wait(0.03)
            end
        end

        if tool and tool.Parent ~= char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:EquipTool(tool)
                local eqStart = tick()
                while tick() - eqStart < 1 do
                    if tool.Parent == char then break end
                    task.wait(0.03)
                end
            end
        end
        return tool
    end

    local function duplicateProps(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 1.5)
        if not dupRemote then return false end

        for i = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
        end
        return true
    end

    local function stopBlackExpansion(reason)
        if not blackExpRunning then return end
        blackExpRunning = false
        blackExpSession = blackExpSession + 1

        if ClearRemote then
            pcall(function() ClearRemote:FireServer("ClearAllProps") end)
        end
        cleanupPropMaker()

        if reason then
            beNotify("Black Expansion", "Stopped: " .. tostring(reason), 3)
        else
            beNotify("Black Expansion", "Deactivated.", 2)
        end
    end

    local function startBlackExpansion()
        if blackExpRunning then
            stopBlackExpansion()
            task.wait(0.2)
        end

        blackExpRunning = true
        blackExpSession = blackExpSession + 1
        local mySession = blackExpSession

        task.spawn(function()
            local targetPlayer = Players:FindFirstChild(selectedTargetName) or LocalPlayer
            local tChar = targetPlayer.Character
            local tRoot = tChar and tChar:FindFirstChild("HumanoidRootPart")
            local tHum = tChar and tChar:FindFirstChildOfClass("Humanoid")

            local myChar = LocalPlayer.Character
            local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")
            local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")

            if not myChar or not myHum or not myRoot then
                stopBlackExpansion("Character missing")
                return
            end

            local spawnPos = (tRoot and tRoot.Position or myRoot.Position)

            beNotify("Black Expansion", "Deploying Black Zone on " .. targetPlayer.DisplayName .. "...", 2)

            local propMaker = getPropMaker()
            if not propMaker or not blackExpRunning or blackExpSession ~= mySession then
                stopBlackExpansion("PropMaker unavailable")
                return
            end

            if ClearRemote then
                pcall(function() ClearRemote:FireServer("OpenPropMenu") end)
                task.wait(0.12)
                if Telemetry then
                    pcall(function() Telemetry:FireServer("filterClick", {name = PROP_CATEGORY, itemType = "Props"}) end)
                    task.wait(0.04)
                    pcall(function() Telemetry:FireServer("filterClick", {name = PROP_NAME, itemType = "Props"}) end)
                    task.wait(0.04)
                end
                pcall(function() ClearRemote:FireServer("RequestingPropName", PROP_NAME, PROP_CATEGORY) end)
                task.wait(0.18)
            end

            local toolPropMake = propMaker:FindFirstChild("Tool_PropMake") or propMaker:WaitForChild("Tool_PropMake", 3)
            if not toolPropMake or not blackExpRunning or blackExpSession ~= mySession then
                stopBlackExpansion("Tool_PropMake missing")
                return
            end

            local myPropName = "Prop" .. LocalPlayer.Name
            local wc = Workspace:FindFirstChild("WorkspaceCom")
            if not wc or not blackExpRunning or blackExpSession ~= mySession then
                stopBlackExpansion("WorkspaceCom missing")
                return
            end

            local existingProps = {}
            for _, folder in pairs(wc:GetChildren()) do
                for _, p in pairs(folder:GetChildren()) do
                    if p.Name == myPropName then existingProps[p] = true end
                end
            end

            pcall(function() toolPropMake:FireServer(Workspace.Terrain, spawnPos) end)

            local firstProp = nil
            local t0 = tick()
            while tick() - t0 < 3.5 do
                if not blackExpRunning or blackExpSession ~= mySession or myHum.Health <= 0 then
                    stopBlackExpansion("Interrupted during spawn")
                    return
                end
                for _, folder in pairs(wc:GetChildren()) do
                    for _, p in pairs(folder:GetChildren()) do
                        if p.Name == myPropName and not existingProps[p] then
                            firstProp = p
                            break
                        end
                    end
                    if firstProp then break end
                end
                if firstProp then break end
                task.wait(0.04)
            end

            if not firstProp then
                stopBlackExpansion("Failed to spawn prop")
                return
            end

            if propCount > 1 then
                duplicateProps(firstProp, propCount - 1)
            end

            -- Retrait du carton apres 1.0s (Pattern LOCKED)
            task.spawn(function()
                task.wait(1.0)
                if ToolRemote then
                    pcall(function() ToolRemote:InvokeServer("PickingTools", "PropMaker") end)
                end
                cleanupPropMaker()
            end)

            
            local activeProps = {}
            task.wait(0.25)
            for _, folder in pairs(wc:GetChildren()) do
                for _, p in pairs(folder:GetChildren()) do
                    if p.Name == myPropName then
                        local scf = p:FindFirstChild("SetCurrentCFrame")
                        local cpc = p:FindFirstChild("ChangePropColor")
                        if scf then
                            table.insert(activeProps, { Prop = p, SCF = scf, CPC = cpc })
                            if cpc then
                                pcall(function() cpc:InvokeServer(Color3.fromRGB(0, 0, 0)) end)
                            end
                        end
                    end
                end
            end

            if #activeProps == 0 or not blackExpRunning or blackExpSession ~= mySession then
                stopBlackExpansion("No props found")
                return
            end

            beNotify("Black Expansion", "Black Zone ACTIVE!", 3)

            
            local senders = {}
            local function sendCFrame(scfRef, cf)
                local entry = senders[scfRef]
                if not entry then
                    entry = { lastCF = nil, dirty = false }
                    entry.thread = task.spawn(function()
                        while blackExpRunning and blackExpSession == mySession do
                            task.wait(0.025)
                            if not blackExpRunning or blackExpSession ~= mySession then return end
                            if entry.dirty and entry.lastCF then
                                entry.dirty = false
                                pcall(function() scfRef:InvokeServer(entry.lastCF) end)
                            end
                        end
                    end)
                    senders[scfRef] = entry
                end
                entry.lastCF = cf
                entry.dirty = true
            end

            
            local totalTents = #activeProps
            local angleStep = (math.pi * 2) / totalTents
            local currentAngle = 0

            while blackExpRunning and blackExpSession == mySession do
                if not LocalPlayer.Character or not myHum or myHum.Health <= 0 then
                    stopBlackExpansion("Player died")
                    break
                end

                local curTarget = Players:FindFirstChild(selectedTargetName) or LocalPlayer
                local curChar = curTarget.Character
                local curRoot = curChar and curChar:FindFirstChild("HumanoidRootPart")
                local curHum = curChar and curChar:FindFirstChildOfClass("Humanoid")

                if not curRoot or not curHum or curHum.Health <= 0 then
                    task.wait(0.05)
                else
                    currentAngle = currentAngle + (rotSpeed * 0.02)

                    for i, data in ipairs(activeProps) do
                        if not blackExpRunning or blackExpSession ~= mySession then break end
                        local theta = currentAngle + ((i - 1) * angleStep)

                        local tentCF = curRoot.CFrame * CFrame.Angles(0, theta, 0) * CFrame.new(forwardOffset, heightOffset, lateralOffset) * CFrame.Angles(0, math.rad(-90), 0)

                        sendCFrame(data.SCF, tentCF)
                    end
                end

                task.wait(0.02)
            end
        end)
    end

    _G.BTR_StartBlackExpansion = function(tName)
        if tName then selectedTargetName = tName end
        startBlackExpansion()
    end
    _G.BTR_StopBlackExpansion = stopBlackExpansion

    TrollV2Tab:AddSection({"> Black Expansion"})

    local beDropdownRef = nil
    beDropdownRef = TrollV2Tab:AddDropdown({
        Name = "Target Player",
        Description = "Choose target to blind with the black screen zone",
        Options = getPlayerList(),
        Default = LocalPlayer.Name,
        Callback = function(val)
            if val and val ~= "" then
                selectedTargetName = val
            end
        end
    })

    TrollV2Tab:AddButton({
        Name = "Refresh Player List",
        Description = "Update online players in target list",
        Callback = function()
            local updated = getPlayerList()
            if beDropdownRef and beDropdownRef.Set then
                pcall(function() beDropdownRef:Set(updated) end)
            end
            beNotify("Black Expansion", "Player list refreshed!", 2)
        end
    })

    TrollV2Tab:AddToggle({
        Name = "Black Expansion",
        Description = "Blind black zone centered on target, best used to completely blind first person view",
        Default = false,
        Callback = function(state)
            if not _G.BTR_UI_READY then return end
            if state then
                startBlackExpansion()
            else
                stopBlackExpansion()
            end
        end
    })
end

do
    local barrelBotActive = false
    local barrelBotSessionId = 0
    local barrelBotLoopConn = nil
    local barrelBotPartsCache = {}
    local barrelBotOriginCF = nil
    local barrelSideOffset = 5.0
    local barrelForwardOffset = 0.0
    local barrelHeightOffset = 0.0
    local barrelFollowSmooth = 0.28
    local barrelBotScale = 1.0

    
    local stalkActive = false
    local selectedStalkTarget = "[Nearest Player]"
    local stalkFollowMode = "Behind"
    local stalkOrbitAngle = 0.0

    
    local currentCustomEmoteTrack = nil
    local currentCustomEmoteId = "82025361932571"

    local selectedBarrelColor = "Scanned Gray"
    local BARREL_COLORS = {
        ["Scanned Gray"]  = Color3.fromRGB(99, 95, 98),
        ["Toxic Green"]   = Color3.fromRGB(50, 255, 60),
        ["Radioactive Red"]= Color3.fromRGB(255, 35, 45),
        ["Cyber Cyan"]    = Color3.fromRGB(0, 220, 255),
        ["Electric Gold"] = Color3.fromRGB(255, 215, 0),
        ["Void Purple"]   = Color3.fromRGB(160, 40, 255),
        ["Pure White"]    = Color3.fromRGB(255, 255, 255),
        ["Obsidian Black"]= Color3.fromRGB(15, 15, 18),
    }

    local function barrelNotify(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function getBarrelPropMaker()
        local lp = game:GetService("Players").LocalPlayer
        local char = lp.Character
        local bp = lp:FindFirstChild("Backpack")
        if not char and not bp then return nil end

        local tool = (char and char:FindFirstChild("PropMaker")) or (bp and bp:FindFirstChild("PropMaker"))
        if not tool then
            local toolRE = game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l")
            pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
            local t0 = tick()
            while tick() - t0 < 2.5 do
                tool = (char and char:FindFirstChild("PropMaker")) or (bp and bp:FindFirstChild("PropMaker"))
                if tool then break end
                task.wait(0.05)
            end
        end

        if not tool then return nil end

        if char and tool.Parent ~= char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:EquipTool(tool)
                task.wait(0.1)
            end
        end
        return tool
    end

    local function spawnSingleNuclearBarrel(targetPos)
        local tool = getBarrelPropMaker()
        if not tool then return nil end

        local clearTools = game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        pcall(function() clearTools:FireServer("RequestingPropName", "NuclearWasteBarrel", "Building") end)
        task.wait(0.3)

        local lp = game:GetService("Players").LocalPlayer
        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum and tool.Parent ~= char then
            hum:EquipTool(tool)
            task.wait(0.1)
        end

        local tpm = tool:FindFirstChild("Tool_PropMake")
        local t0 = tick()
        while not tpm and tick() - t0 < 3.0 do
            tpm = tool:FindFirstChild("Tool_PropMake")
            if not tpm then
                local bp = lp:FindFirstChild("Backpack") and lp.Backpack:FindFirstChild("PropMaker")
                if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
            end
            if not tpm then task.wait(0.1) end
        end

        -- [PATTERN PROPMAKER LOCKED] Silent exit on missing Tool_PropMake
        if not tpm then return nil end

        local wc = workspace:FindFirstChild("WorkspaceCom")
        local myPropName = "Prop" .. lp.Name
        local beforeList = {}
        if wc then
            for _, folder in ipairs(wc:GetChildren()) do
                for _, prop in ipairs(folder:GetChildren()) do
                    if prop.Name == myPropName then
                        beforeList[prop] = true
                    end
                end
            end
        end

        pcall(function() tpm:FireServer(workspace.Terrain, targetPos) end)

        local spawned = nil
        local tDetect = tick()
        while tick() - tDetect < 3.5 do
            if wc then
                for _, folder in ipairs(wc:GetChildren()) do
                    for _, prop in ipairs(folder:GetChildren()) do
                        if prop.Name == myPropName and not beforeList[prop] then
                            spawned = prop
                            break
                        end
                    end
                    if spawned then break end
                end
            end
            if spawned then break end
            task.wait(0.05)
        end

        return spawned
    end

    local function duplicateNuclearBarrel(firstProp, count)
        if not firstProp or count <= 0 then return end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
        if not dupRemote then return end
        for i = 1, count do
            pcall(function() dupRemote:InvokeServer() end)
            task.wait(0.05)
        end
    end

    local function scanBarrelProps()
        table.clear(barrelBotPartsCache)
        local wc = workspace:FindFirstChild("WorkspaceCom")
        if not wc then return end
        local lp = game:GetService("Players").LocalPlayer
        local myPropName = "Prop" .. lp.Name

        for _, folder in ipairs(wc:GetChildren()) do
            for _, prop in ipairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    local scf = prop:FindFirstChild("SetCurrentCFrame") or prop:WaitForChild("SetCurrentCFrame", 0.5)
                    local cpc = prop:FindFirstChild("ChangePropColor") or prop:FindFirstChild("ChangePropColor", true)
                    if scf then
                        table.insert(barrelBotPartsCache, {
                            prop = prop,
                            remote = scf,
                            colorRemote = cpc
                        })
                    end
                end
            end
        end
    end

    local function applyBarrelColor(item, color)
        if not item then return end
        pcall(function()
            for _, p in ipairs(item.prop:GetDescendants()) do
                if p:IsA("BasePart") then
                    p.Color = color
                end
            end
        end)
        if item.colorRemote then
            task.spawn(function()
                pcall(function() item.colorRemote:InvokeServer(color) end)
            end)
        end
    end

    local function applyColorToAllBarrels()
        local col = BARREL_COLORS[selectedBarrelColor] or BARREL_COLORS["Scanned Gray"]
        for _, item in ipairs(barrelBotPartsCache) do
            applyBarrelColor(item, col)
        end
    end

    local function resolveTargetPlayer()
        local Players = game:GetService("Players")
        local lp = Players.LocalPlayer
        if not stalkActive then
            return lp
        end
        if selectedStalkTarget == "[Nearest Player]" then
            local myChar = lp.Character
            local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
            if not myHrp then return lp end
            local nearestPlr = nil
            local minDist = math.huge
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr ~= lp and plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") then
                    local dist = (plr.Character.HumanoidRootPart.Position - myHrp.Position).Magnitude
                    if dist < minDist then
                        minDist = dist
                        nearestPlr = plr
                    end
                end
            end
            return nearestPlr or lp
        else
            local found = Players:FindFirstChild(selectedStalkTarget)
            if found then return found end
            local lower = string.lower(selectedStalkTarget)
            for _, plr in ipairs(Players:GetPlayers()) do
                if string.find(string.lower(plr.Name), lower, 1, true) or string.find(string.lower(plr.DisplayName), lower, 1, true) then
                    return plr
                end
            end
            return lp
        end
    end

    local function getLimbPart(charModel, index)
        if not charModel then return nil end
        local isR15 = charModel:FindFirstChild("UpperTorso") ~= nil
        if isR15 then
            local rMap = {
                [1]  = "UpperTorso",
                [2]  = "LowerTorso",
                [3]  = "Head",
                [4]  = "LeftUpperArm",
                [5]  = "LeftLowerArm",
                [6]  = "LeftHand",
                [7]  = "RightUpperArm",
                [8]  = "RightLowerArm",
                [9]  = "RightHand",
                [10] = "LeftUpperLeg",
                [11] = "LeftLowerLeg",
                [12] = "LeftFoot",
                [13] = "RightUpperLeg",
                [14] = "RightLowerLeg",
                [15] = "RightFoot",
            }
            return charModel:FindFirstChild(rMap[index] or "UpperTorso")
        else
            local r6Map = {
                [1]  = "Torso",
                [2]  = "Torso",
                [3]  = "Head",
                [4]  = "Left Arm",
                [5]  = "Left Arm",
                [6]  = "Left Arm",
                [7]  = "Right Arm",
                [8]  = "Right Arm",
                [9]  = "Right Arm",
                [10] = "Left Leg",
                [11] = "Left Leg",
                [12] = "Left Leg",
                [13] = "Right Leg",
                [14] = "Right Leg",
                [15] = "Right Leg",
            }
            return charModel:FindFirstChild(r6Map[index] or "Torso")
        end
    end

    local function stopCustomEmote()
        if currentCustomEmoteTrack then
            pcall(function()
                currentCustomEmoteTrack:Stop()
                currentCustomEmoteTrack:Destroy()
            end)
            currentCustomEmoteTrack = nil
            barrelNotify("Custom Emote", "Stopped custom emote.", 2)
        end
    end

    local function playCustomEmote(emoteId)
        if not emoteId or emoteId == "" then return end
        local id = string.match(tostring(emoteId), "%d+")
        if not id then return end

        stopCustomEmote()

        local lp = game:GetService("Players").LocalPlayer
        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if not hum then return end

        task.spawn(function()
            local animObj = nil
            local success, objects = pcall(function()
                return game:GetObjects("rbxassetid://" .. id)
            end)

            if success and objects and #objects > 0 then
                for _, obj in ipairs(objects) do
                    if obj:IsA("Animation") then
                        animObj = obj
                        break
                    end
                    for _, child in ipairs(obj:GetDescendants()) do
                        if child:IsA("Animation") then
                            animObj = child
                            break
                        end
                    end
                    if animObj then break end
                end
            end

            if not animObj then
                local directAnim = Instance.new("Animation")
                directAnim.AnimationId = "rbxassetid://" .. id
                animObj = directAnim
            end

            if animObj then
                local track = hum:LoadAnimation(animObj)
                track.Priority = Enum.AnimationPriority.Action4
                track.Looped = true
                track:Play()
                currentCustomEmoteTrack = track
                barrelNotify("Custom Emote", "Playing custom emote: " .. id, 2)
            end
        end)
    end

    local function stopNuclearBarrelBot()
        barrelBotActive = false
        barrelBotSessionId = barrelBotSessionId + 1

        if barrelBotLoopConn then
            barrelBotLoopConn:Disconnect()
            barrelBotLoopConn = nil
        end

        pcall(function()
            game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps")
        end)

        table.clear(barrelBotPartsCache)
        barrelBotOriginCF = nil
        barrelNotify("Barrel Bot", "Barrel bot deactivated and props cleared.", 2)
    end

    local function startNuclearBarrelBot()
        stopNuclearBarrelBot()
        barrelBotActive = true
        barrelBotSessionId = barrelBotSessionId + 1
        local currentSession = barrelBotSessionId

        task.spawn(function()
            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character or lp.CharacterAdded:Wait()
            local hrp = char:WaitForChild("HumanoidRootPart")

            barrelNotify("Barrel Bot", "Assembling 15-Prop Nuclear Mimic Bot...", 3)

            local clearTools = game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
            local toolRE = game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l")

            pcall(function() clearTools:FireServer("ClearAllProps") end)
            task.wait(0.4)

            local propTool = getBarrelPropMaker()
            if not propTool then
                barrelNotify("Error", "Could not equip PropMaker.", 3)
                return
            end

            pcall(function() clearTools:FireServer("OpenPropMenu") end)
            task.wait(0.2)

            local spawnPos = hrp.Position + (hrp.CFrame.RightVector * barrelSideOffset)
            local baseProp = spawnSingleNuclearBarrel(spawnPos)

            if not baseProp and currentSession == barrelBotSessionId then
                baseProp = spawnSingleNuclearBarrel(spawnPos)
            end

            if not baseProp or currentSession ~= barrelBotSessionId then
                barrelNotify("Error", "Could not spawn base barrel.", 3)
                return
            end

            duplicateNuclearBarrel(baseProp, 14)
            task.wait(0.8)

            -- [PATTERN PROPMAKER LOCKED] 1.0s exact delay before removing carton
            task.wait(1.0)
            pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
            pcall(function() clearTools:FireServer("ClosePropMenu") end)

            task.wait(0.4)
            scanBarrelProps()

            applyColorToAllBarrels()

            barrelNotify("Barrel Bot", string.format("Nuclear Bot Ready! (%d/15 props connected)", #barrelBotPartsCache), 3)

            local nextNetTick = 0
            barrelBotLoopConn = game:GetService("RunService").RenderStepped:Connect(function(dt)
                if not barrelBotActive or currentSession ~= barrelBotSessionId then return end

                local targetPlr = resolveTargetPlayer()
                local curChar = targetPlr and targetPlr.Character
                local curHrp = curChar and (curChar:FindFirstChild("HumanoidRootPart") or curChar:FindFirstChild("Torso"))
                local curHum = curChar and curChar:FindFirstChildOfClass("Humanoid")

                
                if not curHrp or not curHum or curHum.Health <= 0 then
                    curChar = lp.Character
                    curHrp = curChar and curChar:FindFirstChild("HumanoidRootPart")
                    curHum = curChar and curChar:FindFirstChildOfClass("Humanoid")
                end

                if not curHrp or not curHum then return end

                local now = tick()
                local _, yaw, _ = curHrp.CFrame:ToEulerAnglesYXZ()
                local flatPlayerCF = CFrame.new(curHrp.Position) * CFrame.Angles(0, yaw, 0)
                
                local isR15 = curChar:FindFirstChild("UpperTorso") ~= nil

                
                local scaleX = isR15 and 1.80 or 1.90
                local scaleY = isR15 and 2.05 or 2.10
                local scaleZ = isR15 and 1.65 or 1.65

                local groundCompensation = (scaleY - 1.0) * 2.65
                local effectiveHeightOffset = barrelHeightOffset + groundCompensation

                local targetOriginFrame = nil
                if stalkActive and targetPlr ~= lp then
                    if stalkFollowMode == "Behind" then
                        targetOriginFrame = flatPlayerCF * CFrame.new(0, effectiveHeightOffset, 5.5)
                    elseif stalkFollowMode == "Face" then
                        targetOriginFrame = flatPlayerCF * CFrame.new(0, effectiveHeightOffset, -5.5) * CFrame.Angles(0, math.pi, 0)
                    elseif stalkFollowMode == "Orbit" then
                        stalkOrbitAngle = stalkOrbitAngle + (dt or 0.016) * 1.6
                        targetOriginFrame = flatPlayerCF * CFrame.Angles(0, stalkOrbitAngle, 0) * CFrame.new(0, effectiveHeightOffset, -6.5) * CFrame.Angles(0, math.pi, 0)
                    elseif stalkFollowMode == "Left" then
                        targetOriginFrame = flatPlayerCF * CFrame.new(-barrelSideOffset, effectiveHeightOffset, -barrelForwardOffset)
                    else
                        targetOriginFrame = flatPlayerCF * CFrame.new(barrelSideOffset, effectiveHeightOffset, -barrelForwardOffset)
                    end
                else
                    targetOriginFrame = flatPlayerCF * CFrame.new(barrelSideOffset, effectiveHeightOffset, -barrelForwardOffset)
                end

                if not barrelBotOriginCF or (targetOriginFrame.Position - barrelBotOriginCF.Position).Magnitude > 35 then
                    barrelBotOriginCF = targetOriginFrame
                else
                    barrelBotOriginCF = barrelBotOriginCF:Lerp(targetOriginFrame, barrelFollowSmooth)
                end

                local shouldNetSend = now >= nextNetTick
                if shouldNetSend then nextNetTick = now + 0.035 end

                local r6Offsets = {
                    [1]  = CFrame.new(0, 0.8, 0),
                    [2]  = CFrame.new(0, -0.9, 0),
                    [3]  = CFrame.new(0, 0.4, 0),
                    [4]  = CFrame.new(0, 1.0, 0),
                    [5]  = CFrame.new(0, -0.8, 0),
                    [6]  = CFrame.new(0, -2.6, 0),
                    [7]  = CFrame.new(0, 1.0, 0),
                    [8]  = CFrame.new(0, -0.8, 0),
                    [9]  = CFrame.new(0, -2.6, 0),
                    [10] = CFrame.new(0, 0.9, 0),
                    [11] = CFrame.new(0, -1.0, 0),
                    [12] = CFrame.new(0, -2.8, 0),
                    [13] = CFrame.new(0, 0.9, 0),
                    [14] = CFrame.new(0, -1.0, 0),
                    [15] = CFrame.new(0, -2.8, 0),
                }

                local r15Offsets = {
                    [1]  = CFrame.new(0, 0, 0),
                    [2]  = CFrame.new(0, 0, 0),
                    [3]  = CFrame.new(0, 0, 0),
                    [4]  = CFrame.new(0, 0.1, 0),
                    [5]  = CFrame.new(0, -0.1, 0),
                    [6]  = CFrame.new(0, -0.6, 0),
                    [7]  = CFrame.new(0, 0.1, 0),
                    [8]  = CFrame.new(0, -0.1, 0),
                    [9]  = CFrame.new(0, -0.6, 0),
                    [10] = CFrame.new(0, 0.1, 0),
                    [11] = CFrame.new(0, -0.2, 0),
                    [12] = CFrame.new(0, -0.6, 0),
                    [13] = CFrame.new(0, 0.1, 0),
                    [14] = CFrame.new(0, -0.2, 0),
                    [15] = CFrame.new(0, -0.6, 0),
                }

                for idx, item in ipairs(barrelBotPartsCache) do
                    if idx > 15 then break end

                    local sourceLimb = getLimbPart(curChar, idx)
                    if sourceLimb then
                        local relCF = curHrp.CFrame:ToObjectSpace(sourceLimb.CFrame)
                        local scaledPos = Vector3.new(
                            relCF.Position.X * scaleX * barrelBotScale,
                            relCF.Position.Y * scaleY * barrelBotScale,
                            relCF.Position.Z * scaleZ * barrelBotScale
                        )

                        
                        local bodyOffset = Vector3.zero
                        if isR15 then
                            if idx == 1 then
                                bodyOffset = Vector3.new(0, 0.3, 0)
                            elseif idx == 2 then
                                bodyOffset = Vector3.new(0, -0.2, 0)
                            elseif idx == 3 then
                                bodyOffset = Vector3.new(0, 0.75, 0)
                            elseif idx >= 4 and idx <= 6 then
                                bodyOffset = Vector3.new(-0.85, 0.15, 0)
                            elseif idx >= 7 and idx <= 9 then
                                bodyOffset = Vector3.new(0.85, 0.15, 0)
                            elseif idx >= 10 and idx <= 12 then
                                bodyOffset = Vector3.new(-0.60, 0, 0)
                            elseif idx >= 13 and idx <= 15 then
                                bodyOffset = Vector3.new(0.60, 0, 0)
                            end
                        else
                            if idx == 1 then
                                bodyOffset = Vector3.new(0, 0.4, 0)
                            elseif idx == 2 then
                                bodyOffset = Vector3.new(0, -0.4, 0)
                            elseif idx == 3 then
                                bodyOffset = Vector3.new(0, 0.8, 0)
                            elseif idx >= 4 and idx <= 6 then
                                bodyOffset = Vector3.new(-1.15, 0.2, 0)
                            elseif idx >= 7 and idx <= 9 then
                                bodyOffset = Vector3.new(1.15, 0.2, 0)
                            elseif idx >= 10 and idx <= 12 then
                                bodyOffset = Vector3.new(-0.85, 0, 0)
                            elseif idx >= 13 and idx <= 15 then
                                bodyOffset = Vector3.new(0.85, 0, 0)
                            end
                        end

                        local limbRot = relCF.Rotation
                        local fineTune = (isR15 and r15Offsets[idx]) or r6Offsets[idx] or CFrame.new()

                        local finalPropCF = barrelBotOriginCF * CFrame.new(scaledPos + bodyOffset) * limbRot * fineTune

                        pcall(function()
                            if item.prop:IsA("Model") then
                                item.prop:PivotTo(finalPropCF)
                            elseif item.prop:IsA("BasePart") then
                                item.prop.CFrame = finalPropCF
                            end
                        end)

                        if shouldNetSend and item.remote then
                            task.spawn(function()
                                pcall(function() item.remote:InvokeServer(finalPropCF) end)
                            end)
                        end
                    end
                end
            end)
        end)
    end

    _G.BTR_StopNuclearBarrelBot = stopNuclearBarrelBot

    TrollV2Tab:AddSection({"> Nuclear Barrel Bot"})

    TrollV2Tab:AddDropdown({
        Name = "Barrel Color",
        Description = "Select the color of the 15-barrel mimic robot",
        Options = {
            "Scanned Gray",
            "Toxic Green",
            "Radioactive Red",
            "Cyber Cyan",
            "Electric Gold",
            "Void Purple",
            "Pure White",
            "Obsidian Black"
        },
        Default = "Scanned Gray",
        Callback = function(val)
            if val and val ~= "" then
                selectedBarrelColor = val
                applyColorToAllBarrels()
            end
        end
    })

    TrollV2Tab:AddDropdown({
        Name = "Bot Position",
        Description = "Choose side placement relative to your avatar",
        Options = {"Right Side (+5 studs)", "Left Side (-5 studs)", "Behind (-4 studs)"},
        Default = "Right Side (+5 studs)",
        Callback = function(val)
            if val == "Left Side (-5 studs)" then
                barrelSideOffset = -5.0
                barrelForwardOffset = 0.0
            elseif val == "Behind (-4 studs)" then
                barrelSideOffset = 0.0
                barrelForwardOffset = -4.0
            else
                barrelSideOffset = 5.0
                barrelForwardOffset = 0.0
            end
        end
    })

    TrollV2Tab:AddToggle({
        Name = "Nuclear Barrel Bot",
        Description = "15-prop humanoid robot that walks, dances and mimics all your animations beside you",
        Default = false,
        Callback = function(state)
            if not _G.BTR_UI_READY then return end
            if state then
                startNuclearBarrelBot()
            else
                stopNuclearBarrelBot()
            end
        end
    })

    
    local function getBarrelPlayerList()
        local list = {"[Nearest Player]"}
        for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
            if p ~= game:GetService("Players").LocalPlayer then
                table.insert(list, p.Name)
            end
        end
        return list
    end

    local stalkDropdownRef = nil
    stalkDropdownRef = TrollV2Tab:AddDropdown({
        Name = "Stalk Target Player",
        Description = "Choose a player for the bot to stalk and mimic their animations",
        Options = getBarrelPlayerList(),
        Default = "[Nearest Player]",
        Callback = function(val)
            if val and val ~= "" then
                selectedStalkTarget = val
            end
        end
    })

    TrollV2Tab:AddButton({
        Name = "Refresh Stalk Player List",
        Description = "Update online players in stalk target list",
        Callback = function()
            local updated = getBarrelPlayerList()
            if stalkDropdownRef and stalkDropdownRef.Set then
                pcall(function() stalkDropdownRef:Set(updated) end)
            end
            barrelNotify("Barrel Bot", "Player list refreshed!", 2)
        end
    })

    TrollV2Tab:AddDropdown({
        Name = "Stalk Position",
        Description = "Choose how the bot stalks the target player",
        Options = {"Behind Stalker (-5 studs)", "Face-to-Face (+5 studs)", "Orbit 360", "Right (+5 studs)", "Left (-5 studs)"},
        Default = "Behind Stalker (-5 studs)",
        Callback = function(val)
            if val == "Face-to-Face (+5 studs)" then
                stalkFollowMode = "Face"
            elseif val == "Orbit 360" then
                stalkFollowMode = "Orbit"
            elseif val == "Right (+5 studs)" then
                stalkFollowMode = "Right"
            elseif val == "Left (-5 studs)" then
                stalkFollowMode = "Left"
            else
                stalkFollowMode = "Behind"
            end
        end
    })

    TrollV2Tab:AddToggle({
        Name = "Stalk & Mimic Target",
        Description = "Redirect the bot to follow and mirror the chosen player instead of you",
        Default = false,
        Callback = function(state)
            if not _G.BTR_UI_READY then return end
            stalkActive = state
            if state then
                barrelNotify("Barrel Bot", "Stalker Mode Active! Following target player.", 2)
            else
                barrelNotify("Barrel Bot", "Stalker Mode Off. Returning to you.", 2)
            end
        end
    })

    
    TrollV2Tab:AddSection({"> Custom Emotes (Real-Time)"})

    TrollV2Tab:AddTextBox({
        Name = "Custom Emote ID",
        Description = "Enter any Roblox Emote/Animation Catalog Asset ID",
        PlaceholderText = "e.g. 82025361932571",
        Callback = function(val)
            if val and val ~= "" then
                currentCustomEmoteId = val
            end
        end
    })

    TrollV2Tab:AddButton({
        Name = "Play Custom Emote",
        Description = "Play emote with catalog bypass so the bot and avatar mirror it",
        Callback = function()
            playCustomEmote(currentCustomEmoteId)
        end
    })

    TrollV2Tab:AddButton({
        Name = "Stop Custom Emote",
        Description = "Stop current custom emote animation track",
        Callback = function()
            stopCustomEmote()
        end
    })
end

    
    -- [LOCKED: WORK PERFECTLY - FIX FLING v1] Fling Ball (propulsion corrigée)
    -- [LOCKED: WORK PERFECTLY - FIX FLING v1] Fling Ball (propulsion corrigee)
    local function getDripSoccerBall()
        local players = game:GetService("Players")
        local player = players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local backpack = player:WaitForChild("Backpack")
        local serverBalls = workspace:WaitForChild("WorkspaceCom"):WaitForChild("001_SoccerBalls")

        if not backpack:FindFirstChild("SoccerBall") and not character:FindFirstChild("SoccerBall") then
            game:GetService("ReplicatedStorage").RE:FindFirstChild("1Too1l"):InvokeServer("PickingTools", "SoccerBall")
        end
        repeat task.wait() until backpack:FindFirstChild("SoccerBall") or character:FindFirstChild("SoccerBall")
        if backpack:FindFirstChild("SoccerBall") then
            backpack.SoccerBall.Parent = character
        end
        repeat task.wait() until serverBalls:FindFirstChild("Soccer" .. player.Name)
        if character:FindFirstChild("SoccerBall") then
            character.SoccerBall.Parent = backpack
        end
        return serverBalls:FindFirstChild("Soccer" .. player.Name)
    end

    local function prepareDripBall(ball)
        if not ball then return end
        ball.CanCollide = false
        ball.Massless = true
        ball.CustomPhysicalProperties = PhysicalProperties.new(0.0001, 0, 0)

        local oldBv = ball:FindFirstChildWhichIsA("BodyVelocity")
        if oldBv then
            oldBv:Destroy()
        end

        local bv = Instance.new("BodyVelocity")
        bv.Name = "FlingPower"
        bv.Velocity = Vector3.new(9e8, 9e8, 9e8)
        bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
        bv.P = 9e900
        bv.Parent = ball
        return bv
    end

    local isDripFlinging = false
    local dripAuraRunning = false

    local function stopDripSoccerAura()
        dripAuraRunning = false
        pcall(function()
            local player = game:GetService("Players").LocalPlayer
            local serverBalls = workspace:FindFirstChild("WorkspaceCom") and workspace.WorkspaceCom:FindFirstChild("001_SoccerBalls")
            local ball = serverBalls and serverBalls:FindFirstChild("Soccer" .. player.Name)
            if ball then
                local bv = ball:FindFirstChildWhichIsA("BodyVelocity")
                if bv then bv:Destroy() end
            end
            local char = player.Character
            if char and char:FindFirstChild("SoccerBall") then
                char.SoccerBall.Parent = player.Backpack
            end
        end)
    end

    local function executeDripFlingBall(target)
        local players = game:GetService("Players")
        local player = players.LocalPlayer
        local character = player.Character or player.CharacterAdded:Wait()
        local humanoid = character:WaitForChild("Humanoid")
        local serverBalls = workspace.WorkspaceCom:WaitForChild("001_SoccerBalls")

        if isDripFlinging then return end
        isDripFlinging = true

        local ball = serverBalls:FindFirstChild("Soccer" .. player.Name) or getDripSoccerBall()
        if not ball then
            isDripFlinging = false
            return
        end

        local bv = prepareDripBall(ball)

        if target and target ~= player then
            local tchar = target.Character
            if tchar and tchar:FindFirstChild("HumanoidRootPart") and tchar:FindFirstChild("Humanoid") then
                local troot = tchar.HumanoidRootPart
                local thum = tchar.Humanoid

                workspace.CurrentCamera.CameraSubject = thum
                local startTime = os.time()
                repeat
                    if not isDripFlinging then break end
                    if troot.Velocity.Magnitude > 0 then
                        local pos_x = troot.Position.X + (troot.Velocity.X / 1.5)
                        local pos_y = troot.Position.Y + (troot.Velocity.Y / 1.5)
                        local pos_z = troot.Position.Z + (troot.Velocity.Z / 1.5)
                        local position = Vector3.new(pos_x, pos_y, pos_z)
                        ball.CFrame = CFrame.new(position)
                        ball.Orientation = ball.Orientation + Vector3.new(45, 60, 30)
                    else
                        for _, v in pairs(tchar:GetChildren()) do
                            if v:IsA("BasePart") and v.CanCollide and not v.Anchored then
                                ball.CFrame = v.CFrame
                                task.wait(1/6000)
                            end
                        end
                    end
                    task.wait(1/6000)
                until troot.Velocity.Magnitude > 1000 or thum.Health <= 0 or not tchar:IsDescendantOf(workspace) or target.Parent ~= players or (os.time() - startTime > 8)

                if bv and bv.Parent then bv:Destroy() end
                workspace.CurrentCamera.CameraSubject = humanoid
            end
        end

        isDripFlinging = false

        
        if dripAuraRunning then
            task.spawn(function()
                local newBv = prepareDripBall(ball)
            end)
        end
    end

    -- [LOCKED: WORK PERFECTLY - FIX FLING v1] Fling Ball (DRIP CLIENT 1:1)
    TrollTab:AddSection({
        "> SoccerBall Fling & Aura"
    })

    TrollTab:AddButton({
        Name = "Fling Ball (SoccerBall)",
        Description = "Fling target player with SoccerBall (Original Drip Client System)",
        Callback = function()
            if not _G.TrollTarget or _G.TrollTarget == "---" then
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Fling Ball", Text="No target selected!", Duration=3})
                return
            end
            local target = game:GetService("Players"):FindFirstChild(_G.TrollTarget)
            if not target then
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Fling Ball", Text="Target not found!", Duration=3})
                return
            end

            task.spawn(function()
                executeDripFlingBall(target)
            end)
        end
    })
    -- [END LOCKED: WORK PERFECTLY - FIX FLING v1] Fling Ball

    
    
    
    do
        local soccerAuraToggle = TrollTab:AddToggle({
            Name = "SoccerBall Aura Fling",
            Description = "SoccerBall aura shield on your body (Drip Client physics on yourself)",
            Default = false,
            Callback = function(Value)
                if not _G.BTR_UI_READY then return end
                if Value then
                    dripAuraRunning = true
                    task.spawn(function()
                        local players = game:GetService("Players")
                        local player = players.LocalPlayer
                        local serverBalls = workspace:WaitForChild("WorkspaceCom"):WaitForChild("001_SoccerBalls")

                        local ball = serverBalls:FindFirstChild("Soccer" .. player.Name) or getDripSoccerBall()
                        if not ball then
                            dripAuraRunning = false
                            return
                        end

                        local bv = prepareDripBall(ball)

                        while dripAuraRunning do
                            
                            if isDripFlinging then
                                task.wait(0.1)
                            else
                                local character = player.Character
                                local hrp = character and character:FindFirstChild("HumanoidRootPart")
                                local hum = character and character:FindFirstChildOfClass("Humanoid")

                                if character and hrp and hum and hum.Health > 0 then
                                    if not ball or not ball.Parent then
                                        ball = serverBalls:FindFirstChild("Soccer" .. player.Name) or getDripSoccerBall()
                                        if ball then bv = prepareDripBall(ball) end
                                    end

                                    if ball and ball.Parent then
                                        ball.CanCollide = false
                                        ball.Massless = true
                                        ball.CustomPhysicalProperties = PhysicalProperties.new(0.0001, 0, 0)

                                        if not ball:FindFirstChildWhichIsA("BodyVelocity") then
                                            bv = prepareDripBall(ball)
                                        end

                                        ball.CFrame = hrp.CFrame
                                        ball.Orientation = ball.Orientation + Vector3.new(45, 60, 30)
                                    end
                                end
                                task.wait(1/6000)
                            end
                        end
                        stopDripSoccerAura()
                    end)
                else
                    stopDripSoccerAura()
                end
            end
        })
    end

    
    
    
    do
        local vehicleFlingRunning = false
        local vehicleFlingConnection = nil
        local vehicleFlingCar = nil
        local vehicleFlingReturnCF = nil
        _G.FlingVehicle = "Ambulance"

        local function getVehiclePart(car)
            if not car then return nil end
            if car.PrimaryPart then return car.PrimaryPart end
            local body = car:FindFirstChild("Body")
            if body then return body end
            return car:FindFirstChildOfClass("MeshPart") or car:FindFirstChildOfClass("Part")
        end

        -- [LOCKED: WORK PERFECTLY] VEHICLE FLING (Anti-Hacker)
        local vehicleFlingToggleRef = nil
        local isStoppingVehicleFling = false

        local function stopVehicleFling(silent, success)
            if isStoppingVehicleFling then return end
            isStoppingVehicleFling = true

            vehicleFlingRunning = false
            _G.VehicleFlingActive = false
            _G.VehicleFlingCar = nil

            if vehicleFlingConnection then
                vehicleFlingConnection:Disconnect()
                vehicleFlingConnection = nil
            end

            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local hrp = char and char:FindFirstChild("HumanoidRootPart")

            
            if hrp then pcall(function() hrp.Anchored = true end) end

            
            local vPart = getVehiclePart(vehicleFlingCar)
            if vPart then
                pcall(function()
                    vPart.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    vPart.AssemblyAngularVelocity = Vector3.zero
                    vPart.RotVelocity = Vector3.new(0,0,0)
                    vPart.Velocity = Vector3.new(0,0,0)
                end)
            end
            task.wait()

            
            if hum then
                hum.Sit = false
                pcall(function() hum:UnequipTools() end)
            end
            task.wait()

            
            if hrp and hrp.Parent then
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
            end

            
            if vehicleFlingCar then
                pcall(function()
                    vehicleFlingCar:PivotTo(CFrame.new(0, -90000, 0))
                end)
            end
            task.wait(0.1)

            
            local RE2 = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local carRemote = RE2 and RE2:FindFirstChild("1Ca1r")
            if carRemote then pcall(function() carRemote:FireServer("DeleteAllVehicles") end) end
            task.wait(0.15)

            
            if hrp and vehicleFlingReturnCF then
                for _ = 1, 3 do
                    pcall(function() hrp.CFrame = vehicleFlingReturnCF end)
                    task.wait()
                end
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
                vehicleFlingReturnCF = nil
            end

            
            task.spawn(function()
                for _ = 1, 5 do
                    task.wait()
                    if hrp and hrp.Parent then
                        pcall(function()
                            hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                            hrp.Velocity = Vector3.new(0,0,0)
                        end)
                    end
                end
                if hrp and hrp.Parent then pcall(function() hrp.Anchored = false end) end
                isStoppingVehicleFling = false
            end)

            vehicleFlingCar = nil

            
            if vehicleFlingToggleRef then
                pcall(function() vehicleFlingToggleRef:Set(false) end)
            end

            if not silent then
                local notifText = (success == true) and "Target flinged to the void!" or "Attack cancelled."
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Vehicle Fling",
                    Text = notifText,
                    Duration = 3
                })
            end
        end

        TrollTab:AddSection({"> Vehicle Fling"})

        vehicleFlingToggleRef = TrollTab:AddToggle({
            Name = "Vehicle Fling V1",
            Description = "Spawns vehicle V1 and flings the target.",
            Default = false,
            Callback = function(state)
                if state then
                    local lp = game:GetService("Players").LocalPlayer
                    local targetName = _G.TrollTarget
                    local targetPlayer = nil
                    if targetName and targetName ~= "---" and targetName ~= "" then
                        targetPlayer = game:GetService("Players"):FindFirstChild(targetName)
                    end

                    if not targetPlayer then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Select a target first!", Duration=3})
                        if vehicleFlingToggleRef then pcall(function() vehicleFlingToggleRef:Set(false) end) end
                        return
                    end

                    
                    vehicleFlingReturnCF = lp.Character and (lp.Character.PrimaryPart and lp.Character.PrimaryPart.CFrame or (lp.Character:FindFirstChild("HumanoidRootPart") and lp.Character.HumanoidRootPart.CFrame)) or CFrame.new(0, 5, 0)

                    
                    local char = lp.Character
                    local hrp = char and char:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        pcall(function() hrp.CFrame = CFrame.new(-1250.97, 78.58, -1258.14) end)
                    end
                    task.wait(0.2)

                    
                    local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
                    _G.VehicleFlingActive = true
                    pcall(function() RE:WaitForChild("1Ca1r"):FireServer("PickingCar", _G.FlingVehicle) end)
                    task.wait(1.5)

                    
                    local myVehicle = nil
                    local expectedName = lp.Name .. "Car"
                    local vehiclesFolder = workspace:FindFirstChild("Vehicles")
                    local listToSearch = vehiclesFolder and vehiclesFolder:GetChildren() or workspace:GetChildren()

                    
                    for _, v in pairs(listToSearch) do
                        if v:IsA("Model") and v.Name == expectedName then
                            myVehicle = v
                            break
                        end
                    end

                    
                    if not myVehicle then
                        local closestDist = math.huge
                        local charPos = lp.Character and lp.Character.PrimaryPart and lp.Character.PrimaryPart.Position or Vector3.new(0,0,0)
                        for _, v in pairs(listToSearch) do
                            if v:IsA("Model") then
                                local hasSeat = false
                                for _, d in pairs(v:GetDescendants()) do
                                    if d:IsA("VehicleSeat") then hasSeat = true break end
                                end
                                if hasSeat then
                                    local pPart = v.PrimaryPart or v:FindFirstChild("Body") or v:FindFirstChildOfClass("Part") or v:FindFirstChildOfClass("MeshPart")
                                    if pPart then
                                        local dist = (pPart.Position - charPos).Magnitude
                                        if dist < closestDist and dist < 30 then
                                            closestDist = dist
                                            myVehicle = v
                                        end
                                    end
                                end
                            end
                        end
                    end

                    vehicleFlingCar = myVehicle
                    _G.VehicleFlingCar = myVehicle

                    if not myVehicle then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Vehicle not found!", Duration=3})
                        _G.VehicleFlingCar = nil
                        _G.VehicleFlingActive = false
                        if vehicleFlingToggleRef then pcall(function() vehicleFlingToggleRef:Set(false) end) end
                        return
                    end

                    
                    local seat = nil
                    for _, d in pairs(myVehicle:GetDescendants()) do
                        if d:IsA("VehicleSeat") and (d.Name:lower():find("drive") or d.Name:lower():find("main") or not seat) then
                            seat = d
                        end
                    end
                    if not seat then
                        for _, d in pairs(myVehicle:GetDescendants()) do
                            if d:IsA("VehicleSeat") or d:IsA("Seat") then seat = d break end
                        end
                    end

                    if not seat then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Seat not found!", Duration=3})
                        _G.VehicleFlingActive = false
                        _G.VehicleFlingCar = nil
                        if vehicleFlingToggleRef then pcall(function() vehicleFlingToggleRef:Set(false) end) end
                        return
                    end

                    
                    for _, sPart in pairs(myVehicle:GetDescendants()) do
                        if (sPart:IsA("Seat") or sPart:IsA("VehicleSeat")) and sPart ~= seat then
                            pcall(function()
                                sPart.Disabled = true
                                sPart.CanTouch = false
                                sPart.CanCollide = false
                            end)
                        end
                    end

                    pcall(function()
                        myVehicle:PivotTo(lp.Character.PrimaryPart.CFrame * CFrame.new(0, 0, -12))
                    end)
                    task.wait(0.2)

                    local myChar = lp.Character
                    local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")

                    if myHrp and myHum then
                        pcall(function()
                            seat.Disabled = false
                            seat.CanTouch = true
                            myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                        end)
                        myHrp.CFrame = seat.CFrame + Vector3.new(0, 2, 0)
                        task.wait(0.15)
                        pcall(function() seat:Sit(myHum) end)
                        local waitCount = 0
                        while not myHum.Sit and waitCount < 12 do
                            pcall(function()
                                seat.Disabled = false
                                myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                                myHrp.CFrame = seat.CFrame + Vector3.new(0, 1, 0)
                                seat:Sit(myHum)
                            end)
                            task.wait(0.1)
                            waitCount = waitCount + 1
                        end
                        if not myHum.Sit then
                            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="I can't sit down!", Duration=3})
                            _G.VehicleFlingActive = false
                            _G.VehicleFlingCar = nil
                            if vehicleFlingToggleRef then pcall(function() vehicleFlingToggleRef:Set(false) end) end
                            return
                        end
                    end

                    vehicleFlingRunning = true

                    if myHum then
                        pcall(function() myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true) end)
                    end

                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="FLING ACTIVE", Text="Tight orbit - centrifugal impact!", Duration=3})

                    
                    local attackStart = tick()
                    vehicleFlingConnection = game:GetService("RunService").Heartbeat:Connect(function(dt)
                        if not vehicleFlingRunning or not myVehicle or not myVehicle.Parent then
                            if vehicleFlingConnection then vehicleFlingConnection:Disconnect() end
                            return
                        end
                        if not targetPlayer.Character then
                            task.spawn(function() stopVehicleFling(false, false) end)
                            return
                        end

                        local tHrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                        local tHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
                        local primary = myVehicle.PrimaryPart or getVehiclePart(myVehicle) or seat

                        if not tHrp or not primary then return end

                        
                        local t = tick()
                        local r = 2
                        local ox = math.sin(t * 10) * r
                        local oz = math.cos(t * 10) * r

                        local targetCF = CFrame.new(
                            tHrp.Position + Vector3.new(ox, 0.5, oz),
                            tHrp.Position
                        )

                        pcall(function()
                            if myVehicle.PivotTo then
                                myVehicle:PivotTo(targetCF)
                            elseif myVehicle.SetPrimaryPartCFrame then
                                myVehicle:SetPrimaryPartCFrame(targetCF)
                            end
                            
                            local dir = (tHrp.Position - primary.Position)
                            if dir.Magnitude > 0.1 then dir = dir.Unit end
                            primary.AssemblyLinearVelocity  = dir * 25000 + Vector3.new(0, 12000, 0)
                            primary.AssemblyAngularVelocity = Vector3.zero
                            
                            primary.RotVelocity = Vector3.new(0, 8000, 0)
                        end)

                        
                        local elapsed = tick() - attackStart
                        local isTargetFlinged = (tHrp.Position.Y < -20) or (tHrp.AssemblyLinearVelocity.Magnitude > 150) or (tHum and tHum.Health <= 0) or (tHum and tHum.Sit)
                        if elapsed > 2.5 or isTargetFlinged then
                            vehicleFlingRunning = false
                            if vehicleFlingConnection then
                                vehicleFlingConnection:Disconnect()
                                vehicleFlingConnection = nil
                            end
                            task.spawn(function()
                                task.wait(0.2)
                                stopVehicleFling(false, true)
                            end)
                        end
                    end)
                else
                    stopVehicleFling(false, false)
                end
            end
        })

        
        
        
        local vehicleFlingToggleRef2 = nil
        local isStoppingVehicleFling2 = false
        local vehicleFlingRunning2 = false
        local vehicleFlingConnection2 = nil
        local vehicleFlingCar2 = nil
        local vehicleFlingReturnCF2 = nil
        _G.FlingVehicle2 = "CherrypickerTruck"

        local function stopVehicleFling2(silent, success)
            if isStoppingVehicleFling2 then return end
            isStoppingVehicleFling2 = true

            vehicleFlingRunning2 = false
            if vehicleFlingConnection2 then
                vehicleFlingConnection2:Disconnect()
                vehicleFlingConnection2 = nil
            end

            _G.VehicleFlingActive = false
            _G.VehicleFlingCar = nil

            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local hrp = char and char:FindFirstChild("HumanoidRootPart")

            if hrp and hrp.Parent then
                pcall(function() hrp.Anchored = true end)
            end

            if hum then
                hum.Sit = false
                pcall(function() hum:UnequipTools() end)
            end
            task.wait()

            if hrp and hrp.Parent then
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
            end

            if vehicleFlingCar2 then
                pcall(function()
                    vehicleFlingCar2:PivotTo(CFrame.new(0, -90000, 0))
                end)
            end
            task.wait(0.1)

            local RE2 = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local carRemote = RE2 and RE2:FindFirstChild("1Ca1r")
            if carRemote then pcall(function() carRemote:FireServer("DeleteAllVehicles") end) end
            task.wait(0.15)

            if hrp and vehicleFlingReturnCF2 then
                for _ = 1, 3 do
                    pcall(function() hrp.CFrame = vehicleFlingReturnCF2 end)
                    task.wait()
                end
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
                vehicleFlingReturnCF2 = nil
            end

            task.spawn(function()
                for _ = 1, 5 do
                    task.wait()
                    if hrp and hrp.Parent then
                        pcall(function()
                            hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                            hrp.Velocity = Vector3.new(0,0,0)
                        end)
                    end
                end
                if hrp and hrp.Parent then pcall(function() hrp.Anchored = false end) end
                isStoppingVehicleFling2 = false
            end)

            vehicleFlingCar2 = nil

            if vehicleFlingToggleRef2 then
                pcall(function() vehicleFlingToggleRef2:Set(false) end)
            end

            if not silent then
                local notifText = (success == true) and "Target flinged to the void!" or "Attack cancelled."
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Vehicle Fling 2",
                    Text = notifText,
                    Duration = 3
                })
            end
        end

        vehicleFlingToggleRef2 = TrollTab:AddToggle({
            Name = "Vehicle Fling V2",
            Description = "Spawns vehicle V2 and flings the target.",
            Default = false,
            Callback = function(state)
                if state then
                    local lp = game:GetService("Players").LocalPlayer
                    local targetName = _G.TrollTarget
                    local targetPlayer = nil
                    if targetName and targetName ~= "---" and targetName ~= "" then
                        targetPlayer = game:GetService("Players"):FindFirstChild(targetName)
                    end

                    if not targetPlayer then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling 2", Text="Select a target first!", Duration=3})
                        if vehicleFlingToggleRef2 then pcall(function() vehicleFlingToggleRef2:Set(false) end) end
                        return
                    end

                    vehicleFlingReturnCF2 = lp.Character and (lp.Character.PrimaryPart and lp.Character.PrimaryPart.CFrame or (lp.Character:FindFirstChild("HumanoidRootPart") and lp.Character.HumanoidRootPart.CFrame)) or CFrame.new(0, 5, 0)

                    local char = lp.Character
                    local hrp = char and char:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        pcall(function() hrp.CFrame = CFrame.new(-1250.97, 78.58, -1258.14) end)
                    end
                    task.wait(0.2)

                    local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
                    _G.VehicleFlingActive = true
                    pcall(function() RE:WaitForChild("1Ca1r"):FireServer("PickingCar", _G.FlingVehicle2) end)
                    task.wait(1.5)

                    local myVehicle = nil
                    local expectedName = lp.Name .. "Car"
                    local vehiclesFolder = workspace:FindFirstChild("Vehicles")
                    local listToSearch = vehiclesFolder and vehiclesFolder:GetChildren() or workspace:GetChildren()

                    for _, v in pairs(listToSearch) do
                        if v:IsA("Model") and v.Name == expectedName then
                            myVehicle = v
                            break
                        end
                    end

                    if not myVehicle then
                        local closestDist = math.huge
                        local charPos = lp.Character and lp.Character.PrimaryPart and lp.Character.PrimaryPart.Position or Vector3.new(0,0,0)
                        for _, v in pairs(listToSearch) do
                            if v:IsA("Model") then
                                local hasSeat = false
                                for _, d in pairs(v:GetDescendants()) do
                                    if d:IsA("VehicleSeat") then hasSeat = true break end
                                end
                                if hasSeat then
                                    local pPart = v.PrimaryPart or v:FindFirstChild("Body") or v:FindFirstChildOfClass("Part") or v:FindFirstChildOfClass("MeshPart")
                                    if pPart then
                                        local dist = (pPart.Position - charPos).Magnitude
                                        if dist < closestDist and dist < 30 then
                                            closestDist = dist
                                            myVehicle = v
                                        end
                                    end
                                end
                            end
                        end
                    end

                    vehicleFlingCar2 = myVehicle
                    _G.VehicleFlingCar = myVehicle

                    if not myVehicle then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling 2", Text="Vehicle not found!", Duration=3})
                        _G.VehicleFlingCar = nil
                        _G.VehicleFlingActive = false
                        if vehicleFlingToggleRef2 then pcall(function() vehicleFlingToggleRef2:Set(false) end) end
                        return
                    end

                    local seat = nil
                    for _, d in pairs(myVehicle:GetDescendants()) do
                        if d:IsA("VehicleSeat") and (d.Name:lower():find("drive") or d.Name:lower():find("main") or not seat) then
                            seat = d
                        end
                    end
                    if not seat then
                        for _, d in pairs(myVehicle:GetDescendants()) do
                            if d:IsA("VehicleSeat") or d:IsA("Seat") then seat = d break end
                        end
                    end

                    if not seat then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Seat not found!", Duration=3})
                        _G.VehicleFlingActive = false
                        _G.VehicleFlingCar = nil
                        if vehicleFlingToggleRef2 then pcall(function() vehicleFlingToggleRef2:Set(false) end) end
                        return
                    end

                    
                    for _, sPart in pairs(myVehicle:GetDescendants()) do
                        if (sPart:IsA("Seat") or sPart:IsA("VehicleSeat")) and sPart ~= seat then
                            pcall(function()
                                sPart.Disabled = true
                                sPart.CanTouch = false
                                sPart.CanCollide = false
                            end)
                        end
                    end

                    pcall(function()
                        myVehicle:PivotTo(lp.Character.PrimaryPart.CFrame * CFrame.new(0, 0, -12))
                    end)
                    task.wait(0.2)

                    local myChar = lp.Character
                    local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")

                    if myHrp and myHum then
                        pcall(function()
                            seat.Disabled = false
                            seat.CanTouch = true
                            myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                        end)
                        myHrp.CFrame = seat.CFrame + Vector3.new(0, 2, 0)
                        task.wait(0.15)
                        pcall(function() seat:Sit(myHum) end)
                        local waitCount = 0
                        while not myHum.Sit and waitCount < 12 do
                            pcall(function()
                                seat.Disabled = false
                                myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                                myHrp.CFrame = seat.CFrame + Vector3.new(0, 1, 0)
                                seat:Sit(myHum)
                            end)
                            task.wait(0.1)
                            waitCount = waitCount + 1
                        end
                        if not myHum.Sit then
                            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="I can't sit down!", Duration=3})
                            _G.VehicleFlingActive = false
                            _G.VehicleFlingCar = nil
                            if vehicleFlingToggleRef2 then pcall(function() vehicleFlingToggleRef2:Set(false) end) end
                            return
                        end
                    end

                    vehicleFlingRunning2 = true

                    if myHum then
                        pcall(function() myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true) end)
                    end

                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="FLING 2 ACTIVE", Text="Tight orbit - centrifugal impact!", Duration=3})

                    local attackStart = tick()
                    vehicleFlingConnection2 = game:GetService("RunService").Heartbeat:Connect(function(dt)
                        if not vehicleFlingRunning2 or not myVehicle or not myVehicle.Parent then
                            if vehicleFlingConnection2 then vehicleFlingConnection2:Disconnect() end
                            return
                        end
                        if not targetPlayer.Character then
                            task.spawn(function() stopVehicleFling2(false, false) end)
                            return
                        end

                        local tHrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                        local tHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
                        local primary = myVehicle.PrimaryPart or getVehiclePart(myVehicle) or seat

                        if not tHrp or not primary then return end

                        local t = tick()
                        local r = 2
                        local ox = math.sin(t * 10) * r
                        local oz = math.cos(t * 10) * r

                        local targetCF = CFrame.new(
                            tHrp.Position + Vector3.new(ox, 0.5, oz),
                            tHrp.Position
                        )

                        pcall(function()
                            if myVehicle.PivotTo then
                                myVehicle:PivotTo(targetCF)
                            elseif myVehicle.SetPrimaryPartCFrame then
                                myVehicle:SetPrimaryPartCFrame(targetCF)
                            end
                            local dir = (tHrp.Position - primary.Position)
                            if dir.Magnitude > 0.1 then dir = dir.Unit end
                            primary.AssemblyLinearVelocity  = dir * 25000 + Vector3.new(0, 12000, 0)
                            primary.AssemblyAngularVelocity = Vector3.zero
                            primary.RotVelocity = Vector3.new(0, 8000, 0)
                        end)

                        local elapsed = tick() - attackStart
                        local isTargetFlinged = (tHrp.Position.Y < -20) or (tHrp.AssemblyLinearVelocity.Magnitude > 150) or (tHum and tHum.Health <= 0) or (tHum and tHum.Sit)
                        if elapsed > 2.5 or isTargetFlinged then
                            vehicleFlingRunning2 = false
                            if vehicleFlingConnection2 then
                                vehicleFlingConnection2:Disconnect()
                                vehicleFlingConnection2 = nil
                            end
                            task.spawn(function()
                                task.wait(0.2)
                                stopVehicleFling2(false, true)
                            end)
                        end
                    end)
                else
                    stopVehicleFling2(false, false)
                end
            end
        })

        
        
        
        local vehicleFlingToggleRef3 = nil
        local isStoppingVehicleFling3 = false
        local vehicleFlingRunning3 = false
        local vehicleFlingConnection3 = nil
        local vehicleFlingCar3 = nil
        local vehicleFlingReturnCF3 = nil
        _G.FlingVehicle3 = "JettsFoodTruck"

        local function stopVehicleFling3(silent, success)
            if isStoppingVehicleFling3 then return end
            isStoppingVehicleFling3 = true

            vehicleFlingRunning3 = false
            if vehicleFlingConnection3 then
                vehicleFlingConnection3:Disconnect()
                vehicleFlingConnection3 = nil
            end

            _G.VehicleFlingActive = false
            _G.VehicleFlingCar = nil

            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local hrp = char and char:FindFirstChild("HumanoidRootPart")

            if hrp and hrp.Parent then
                pcall(function() hrp.Anchored = true end)
            end

            if hum then
                hum.Sit = false
                pcall(function() hum:UnequipTools() end)
            end
            task.wait()

            if hrp and hrp.Parent then
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
            end

            if vehicleFlingCar3 then
                pcall(function()
                    vehicleFlingCar3:PivotTo(CFrame.new(0, -90000, 0))
                end)
            end
            task.wait(0.1)

            local RE2 = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local carRemote = RE2 and RE2:FindFirstChild("1Ca1r")
            if carRemote then pcall(function() carRemote:FireServer("DeleteAllVehicles") end) end
            task.wait(0.15)

            if hrp and vehicleFlingReturnCF3 then
                for _ = 1, 3 do
                    pcall(function() hrp.CFrame = vehicleFlingReturnCF3 end)
                    task.wait()
                end
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
                vehicleFlingReturnCF3 = nil
            end

            task.spawn(function()
                for _ = 1, 5 do
                    task.wait()
                    if hrp and hrp.Parent then
                        pcall(function()
                            hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                            hrp.Velocity = Vector3.new(0,0,0)
                        end)
                    end
                end
                if hrp and hrp.Parent then pcall(function() hrp.Anchored = false end) end
                isStoppingVehicleFling3 = false
            end)

            vehicleFlingCar3 = nil

            if vehicleFlingToggleRef3 then
                pcall(function() vehicleFlingToggleRef3:Set(false) end)
            end

            if not silent then
                local notifText = (success == true) and "Target flinged to the void!" or "Attack cancelled."
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Vehicle Fling 3",
                    Text = notifText,
                    Duration = 3
                })
            end
        end

        vehicleFlingToggleRef3 = TrollTab:AddToggle({
            Name = "Vehicle Fling V3",
            Description = "Spawns vehicle V3 and flings the target.",
            Default = false,
            Callback = function(state)
                if state then
                    local lp = game:GetService("Players").LocalPlayer
                    local targetName = _G.TrollTarget
                    local targetPlayer = nil
                    if targetName and targetName ~= "---" and targetName ~= "" then
                        targetPlayer = game:GetService("Players"):FindFirstChild(targetName)
                    end

                    if not targetPlayer then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling 3", Text="Select a target first!", Duration=3})
                        if vehicleFlingToggleRef3 then pcall(function() vehicleFlingToggleRef3:Set(false) end) end
                        return
                    end

                    vehicleFlingReturnCF3 = lp.Character and (lp.Character.PrimaryPart and lp.Character.PrimaryPart.CFrame or (lp.Character:FindFirstChild("HumanoidRootPart") and lp.Character.HumanoidRootPart.CFrame)) or CFrame.new(0, 5, 0)

                    local char = lp.Character
                    local hrp = char and char:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        pcall(function() hrp.CFrame = CFrame.new(-1250.97, 78.58, -1258.14) end)
                    end
                    task.wait(0.2)

                    local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
                    _G.VehicleFlingActive = true
                    pcall(function() RE:WaitForChild("1Ca1r"):FireServer("PickingCar", _G.FlingVehicle3) end)
                    task.wait(1.5)

                    local myVehicle = nil
                    local expectedName = lp.Name .. "Car"
                    local vehiclesFolder = workspace:FindFirstChild("Vehicles")
                    local listToSearch = vehiclesFolder and vehiclesFolder:GetChildren() or workspace:GetChildren()

                    for _, v in pairs(listToSearch) do
                        if v:IsA("Model") and v.Name == expectedName then
                            myVehicle = v
                            break
                        end
                    end

                    if not myVehicle then
                        local closestDist = math.huge
                        local charPos = lp.Character and lp.Character.PrimaryPart and lp.Character.PrimaryPart.Position or Vector3.new(0,0,0)
                        for _, v in pairs(listToSearch) do
                            if v:IsA("Model") then
                                local hasSeat = false
                                for _, d in pairs(v:GetDescendants()) do
                                    if d:IsA("VehicleSeat") then hasSeat = true break end
                                end
                                if hasSeat then
                                    local pPart = v.PrimaryPart or v:FindFirstChild("Body") or v:FindFirstChildOfClass("Part") or v:FindFirstChildOfClass("MeshPart")
                                    if pPart then
                                        local dist = (pPart.Position - charPos).Magnitude
                                        if dist < closestDist and dist < 30 then
                                            closestDist = dist
                                            myVehicle = v
                                        end
                                    end
                                end
                            end
                        end
                    end

                    vehicleFlingCar3 = myVehicle
                    _G.VehicleFlingCar = myVehicle

                    if not myVehicle then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling 3", Text="Vehicle not found!", Duration=3})
                        _G.VehicleFlingCar = nil
                        _G.VehicleFlingActive = false
                        if vehicleFlingToggleRef3 then pcall(function() vehicleFlingToggleRef3:Set(false) end) end
                        return
                    end

                    local seat = nil
                    for _, d in pairs(myVehicle:GetDescendants()) do
                        if d:IsA("VehicleSeat") and (d.Name:lower():find("drive") or d.Name:lower():find("main") or not seat) then
                            seat = d
                        end
                    end
                    if not seat then
                        for _, d in pairs(myVehicle:GetDescendants()) do
                            if d:IsA("VehicleSeat") or d:IsA("Seat") then seat = d break end
                        end
                    end

                    if not seat then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Seat not found!", Duration=3})
                        _G.VehicleFlingActive = false
                        _G.VehicleFlingCar = nil
                        if vehicleFlingToggleRef3 then pcall(function() vehicleFlingToggleRef3:Set(false) end) end
                        return
                    end

                    
                    for _, sPart in pairs(myVehicle:GetDescendants()) do
                        if (sPart:IsA("Seat") or sPart:IsA("VehicleSeat")) and sPart ~= seat then
                            pcall(function()
                                sPart.Disabled = true
                                sPart.CanTouch = false
                                sPart.CanCollide = false
                            end)
                        end
                    end

                    pcall(function()
                        myVehicle:PivotTo(lp.Character.PrimaryPart.CFrame * CFrame.new(0, 0, -12))
                    end)
                    task.wait(0.2)

                    local myChar = lp.Character
                    local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")

                    if myHrp and myHum then
                        pcall(function()
                            seat.Disabled = false
                            seat.CanTouch = true
                            myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                        end)
                        myHrp.CFrame = seat.CFrame + Vector3.new(0, 2, 0)
                        task.wait(0.15)
                        pcall(function() seat:Sit(myHum) end)
                        local waitCount = 0
                        while not myHum.Sit and waitCount < 12 do
                            pcall(function()
                                seat.Disabled = false
                                myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                                myHrp.CFrame = seat.CFrame + Vector3.new(0, 1, 0)
                                seat:Sit(myHum)
                            end)
                            task.wait(0.1)
                            waitCount = waitCount + 1
                        end
                        if not myHum.Sit then
                            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="I can't sit down!", Duration=3})
                            _G.VehicleFlingActive = false
                            _G.VehicleFlingCar = nil
                            if vehicleFlingToggleRef3 then pcall(function() vehicleFlingToggleRef3:Set(false) end) end
                            return
                        end
                    end

                    vehicleFlingRunning3 = true

                    if myHum then
                        pcall(function() myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true) end)
                    end

                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="FLING 3 ACTIVE", Text="Tight orbit - centrifugal impact!", Duration=3})

                    local attackStart = tick()
                    vehicleFlingConnection3 = game:GetService("RunService").Heartbeat:Connect(function(dt)
                        if not vehicleFlingRunning3 or not myVehicle or not myVehicle.Parent then
                            if vehicleFlingConnection3 then vehicleFlingConnection3:Disconnect() end
                            return
                        end
                        if not targetPlayer.Character then
                            task.spawn(function() stopVehicleFling3(false, false) end)
                            return
                        end

                        local tHrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                        local tHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
                        local primary = myVehicle.PrimaryPart or getVehiclePart(myVehicle) or seat

                        if not tHrp or not primary then return end

                        local t = tick()
                        local r = 2
                        local ox = math.sin(t * 10) * r
                        local oz = math.cos(t * 10) * r

                        local targetCF = CFrame.new(
                            tHrp.Position + Vector3.new(ox, 0.5, oz),
                            tHrp.Position
                        )

                        pcall(function()
                            if myVehicle.PivotTo then
                                myVehicle:PivotTo(targetCF)
                            elseif myVehicle.SetPrimaryPartCFrame then
                                myVehicle:SetPrimaryPartCFrame(targetCF)
                            end
                            local dir = (tHrp.Position - primary.Position)
                            if dir.Magnitude > 0.1 then dir = dir.Unit end
                            primary.AssemblyLinearVelocity  = dir * 25000 + Vector3.new(0, 12000, 0)
                            primary.AssemblyAngularVelocity = Vector3.zero
                            primary.RotVelocity = Vector3.new(0, 8000, 0)
                        end)

                        local elapsed = tick() - attackStart
                        local isTargetFlinged = (tHrp.Position.Y < -20) or (tHrp.AssemblyLinearVelocity.Magnitude > 150) or (tHum and tHum.Health <= 0) or (tHum and tHum.Sit)
                        if elapsed > 2.5 or isTargetFlinged then
                            vehicleFlingRunning3 = false
                            if vehicleFlingConnection3 then
                                vehicleFlingConnection3:Disconnect()
                                vehicleFlingConnection3 = nil
                            end
                            task.spawn(function()
                                task.wait(0.2)
                                stopVehicleFling3(false, true)
                            end)
                        end
                    end)
                else
                    stopVehicleFling3(false, false)
                end
            end
        })

        
        
        
        local vehicleFlingToggleRef4 = nil
        local isStoppingVehicleFling4 = false
        local vehicleFlingRunning4 = false
        local vehicleFlingConnection4 = nil
        local vehicleFlingCar4 = nil
        local vehicleFlingReturnCF4 = nil
        _G.FlingVehicle4 = "Snowplow"

        local function stopVehicleFling4(silent, success)
            if isStoppingVehicleFling4 then return end
            isStoppingVehicleFling4 = true

            vehicleFlingRunning4 = false
            if vehicleFlingConnection4 then
                vehicleFlingConnection4:Disconnect()
                vehicleFlingConnection4 = nil
            end

            _G.VehicleFlingActive = false
            _G.VehicleFlingCar = nil

            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local hrp = char and char:FindFirstChild("HumanoidRootPart")

            if hrp and hrp.Parent then
                pcall(function() hrp.Anchored = true end)
            end

            if hum then
                hum.Sit = false
                pcall(function() hum:UnequipTools() end)
            end
            task.wait()

            if hrp and hrp.Parent then
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
            end

            if vehicleFlingCar4 then
                pcall(function()
                    vehicleFlingCar4:PivotTo(CFrame.new(0, -90000, 0))
                end)
            end
            task.wait(0.1)

            local RE2 = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local carRemote = RE2 and RE2:FindFirstChild("1Ca1r")
            if carRemote then pcall(function() carRemote:FireServer("DeleteAllVehicles") end) end
            task.wait(0.15)

            if hrp and vehicleFlingReturnCF4 then
                for _ = 1, 3 do
                    pcall(function() hrp.CFrame = vehicleFlingReturnCF4 end)
                    task.wait()
                end
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
                vehicleFlingReturnCF4 = nil
            end

            task.spawn(function()
                for _ = 1, 5 do
                    task.wait()
                    if hrp and hrp.Parent then
                        pcall(function()
                            hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                            hrp.Velocity = Vector3.new(0,0,0)
                        end)
                    end
                end
                if hrp and hrp.Parent then pcall(function() hrp.Anchored = false end) end
                isStoppingVehicleFling4 = false
            end)

            vehicleFlingCar4 = nil

            if vehicleFlingToggleRef4 then
                pcall(function() vehicleFlingToggleRef4:Set(false) end)
            end

            if not silent then
                local notifText = (success == true) and "Target flinged to the void!" or "Attack cancelled."
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Vehicle Fling",
                    Text = notifText,
                    Duration = 3
                })
            end
        end

        vehicleFlingToggleRef4 = TrollTab:AddToggle({
            Name = "Vehicle Fling V4",
            Description = "Spawns vehicle V4 and flings the target.",
            Default = false,
            Callback = function(state)
                if state then
                    local lp = game:GetService("Players").LocalPlayer
                    local targetName = _G.TrollTarget
                    local targetPlayer = nil
                    if targetName and targetName ~= "---" and targetName ~= "" then
                        targetPlayer = game:GetService("Players"):FindFirstChild(targetName)
                    end

                    if not targetPlayer then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Select a target first!", Duration=3})
                        if vehicleFlingToggleRef4 then pcall(function() vehicleFlingToggleRef4:Set(false) end) end
                        return
                    end

                    vehicleFlingReturnCF4 = lp.Character and (lp.Character.PrimaryPart and lp.Character.PrimaryPart.CFrame or (lp.Character:FindFirstChild("HumanoidRootPart") and lp.Character.HumanoidRootPart.CFrame)) or CFrame.new(0, 5, 0)

                    local char = lp.Character
                    local hrp = char and char:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        pcall(function() hrp.CFrame = CFrame.new(-1250.97, 78.58, -1258.14) end)
                    end
                    task.wait(0.2)

                    local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
                    _G.VehicleFlingActive = true
                    pcall(function() RE:WaitForChild("1Ca1r"):FireServer("PickingCar", _G.FlingVehicle4) end)
                    task.wait(1.5)

                    local myVehicle = nil
                    local expectedName = lp.Name .. "Car"
                    local vehiclesFolder = workspace:FindFirstChild("Vehicles")
                    local listToSearch = vehiclesFolder and vehiclesFolder:GetChildren() or workspace:GetChildren()

                    for _, v in pairs(listToSearch) do
                        if v:IsA("Model") and v.Name == expectedName then
                            myVehicle = v
                            break
                        end
                    end

                    if not myVehicle then
                        local closestDist = math.huge
                        local charPos = lp.Character and lp.Character.PrimaryPart and lp.Character.PrimaryPart.Position or Vector3.new(0,0,0)
                        for _, v in pairs(listToSearch) do
                            if v:IsA("Model") then
                                local hasSeat = false
                                for _, d in pairs(v:GetDescendants()) do
                                    if d:IsA("VehicleSeat") then hasSeat = true break end
                                end
                                if hasSeat then
                                    local pPart = v.PrimaryPart or v:FindFirstChild("Body") or v:FindFirstChildOfClass("Part") or v:FindFirstChildOfClass("MeshPart")
                                    if pPart then
                                        local dist = (pPart.Position - charPos).Magnitude
                                        if dist < closestDist and dist < 30 then
                                            closestDist = dist
                                            myVehicle = v
                                        end
                                    end
                                end
                            end
                        end
                    end

                    vehicleFlingCar4 = myVehicle
                    _G.VehicleFlingCar = myVehicle

                    if not myVehicle then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Vehicle not found!", Duration=3})
                        _G.VehicleFlingCar = nil
                        _G.VehicleFlingActive = false
                        if vehicleFlingToggleRef4 then pcall(function() vehicleFlingToggleRef4:Set(false) end) end
                        return
                    end

                    local seat = nil
                    for _, d in pairs(myVehicle:GetDescendants()) do
                        if d:IsA("VehicleSeat") and (d.Name:lower():find("drive") or d.Name:lower():find("main") or not seat) then
                            seat = d
                        end
                    end
                    if not seat then
                        for _, d in pairs(myVehicle:GetDescendants()) do
                            if d:IsA("VehicleSeat") or d:IsA("Seat") then seat = d break end
                        end
                    end

                    if not seat then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Seat not found!", Duration=3})
                        _G.VehicleFlingActive = false
                        _G.VehicleFlingCar = nil
                        if vehicleFlingToggleRef4 then pcall(function() vehicleFlingToggleRef4:Set(false) end) end
                        return
                    end

                    
                    for _, sPart in pairs(myVehicle:GetDescendants()) do
                        if (sPart:IsA("Seat") or sPart:IsA("VehicleSeat")) and sPart ~= seat then
                            pcall(function()
                                sPart.Disabled = true
                                sPart.CanTouch = false
                                sPart.CanCollide = false
                            end)
                        end
                    end

                    pcall(function()
                        myVehicle:PivotTo(lp.Character.PrimaryPart.CFrame * CFrame.new(0, 0, -12))
                    end)
                    task.wait(0.2)

                    local myChar = lp.Character
                    local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")

                    if myHrp and myHum then
                        pcall(function()
                            seat.Disabled = false
                            seat.CanTouch = true
                            myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                        end)
                        myHrp.CFrame = seat.CFrame + Vector3.new(0, 2, 0)
                        task.wait(0.15)
                        pcall(function() seat:Sit(myHum) end)
                        local waitCount = 0
                        while not myHum.Sit and waitCount < 12 do
                            pcall(function()
                                seat.Disabled = false
                                myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                                myHrp.CFrame = seat.CFrame + Vector3.new(0, 1, 0)
                                seat:Sit(myHum)
                            end)
                            task.wait(0.1)
                            waitCount = waitCount + 1
                        end
                        if not myHum.Sit then
                            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="I can't sit down!", Duration=3})
                            _G.VehicleFlingActive = false
                            _G.VehicleFlingCar = nil
                            if vehicleFlingToggleRef4 then pcall(function() vehicleFlingToggleRef4:Set(false) end) end
                            return
                        end
                    end

                    vehicleFlingRunning4 = true

                    if myHum then
                        pcall(function() myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true) end)
                    end

                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="FLING ACTIVE", Text="Snowplow engaged!", Duration=3})

                    local attackStart = tick()
                    vehicleFlingConnection4 = game:GetService("RunService").Heartbeat:Connect(function(dt)
                        if not vehicleFlingRunning4 or not myVehicle or not myVehicle.Parent then
                            if vehicleFlingConnection4 then vehicleFlingConnection4:Disconnect() end
                            return
                        end
                        if not targetPlayer.Character then
                            task.spawn(function() stopVehicleFling4(false, false) end)
                            return
                        end

                        local tHrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                        local tHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
                        local primary = myVehicle.PrimaryPart or getVehiclePart(myVehicle) or seat

                        if not tHrp or not primary then return end

                        local t = tick()
                        local r = 2
                        local ox = math.sin(t * 10) * r
                        local oz = math.cos(t * 10) * r

                        local targetCF = CFrame.new(
                            tHrp.Position + Vector3.new(ox, 0.5, oz),
                            tHrp.Position
                        )

                        pcall(function()
                            if myVehicle.PivotTo then
                                myVehicle:PivotTo(targetCF)
                            elseif myVehicle.SetPrimaryPartCFrame then
                                myVehicle:SetPrimaryPartCFrame(targetCF)
                            end
                            local dir = (tHrp.Position - primary.Position)
                            if dir.Magnitude > 0.1 then dir = dir.Unit end
                            primary.AssemblyLinearVelocity  = dir * 25000 + Vector3.new(0, 12000, 0)
                            primary.AssemblyAngularVelocity = Vector3.zero
                            primary.RotVelocity = Vector3.new(0, 8000, 0)
                        end)

                        local elapsed = tick() - attackStart
                        local isTargetFlinged = (tHrp.Position.Y < -20) or (tHrp.AssemblyLinearVelocity.Magnitude > 150) or (tHum and tHum.Health <= 0) or (tHum and tHum.Sit)
                        if elapsed > 2.5 or isTargetFlinged then
                            vehicleFlingRunning4 = false
                            if vehicleFlingConnection4 then
                                vehicleFlingConnection4:Disconnect()
                                vehicleFlingConnection4 = nil
                            end
                            task.spawn(function()
                                task.wait(0.2)
                                stopVehicleFling4(false, true)
                            end)
                        end
                    end)
                else
                    stopVehicleFling4(false, false)
                end
            end
        })

        
        
        
        local vehicleFlingToggleRef5 = nil
        local isStoppingVehicleFling5 = false
        local vehicleFlingRunning5 = false
        local vehicleFlingConnection5 = nil
        local vehicleFlingCar5 = nil
        local vehicleFlingReturnCF5 = nil
        local flingAntiSitWasActive5 = false
        _G.FlingVehicle5 = "Tractor"

        local function stopVehicleFling5(silent, success)
            if isStoppingVehicleFling5 then return end
            isStoppingVehicleFling5 = true

            vehicleFlingRunning5 = false
            if vehicleFlingConnection5 then
                vehicleFlingConnection5:Disconnect()
                vehicleFlingConnection5 = nil
            end

            _G.VehicleFlingActive = false
            _G.VehicleFlingCar = nil

            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local hrp = char and char:FindFirstChild("HumanoidRootPart")

            if hrp and hrp.Parent then
                pcall(function() hrp.Anchored = true end)
            end

            if hum then
                hum.Sit = false
                pcall(function() hum:UnequipTools() end)
            end
            task.wait()

            if hrp and hrp.Parent then
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
            end

            if vehicleFlingCar5 then
                pcall(function()
                    vehicleFlingCar5:PivotTo(CFrame.new(0, -90000, 0))
                end)
            end
            task.wait(0.1)

            local RE2 = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local carRemote = RE2 and RE2:FindFirstChild("1Ca1r")
            if carRemote then pcall(function() carRemote:FireServer("DeleteAllVehicles") end) end
            task.wait(0.15)

            if hrp and vehicleFlingReturnCF5 then
                for _ = 1, 3 do
                    pcall(function() hrp.CFrame = vehicleFlingReturnCF5 end)
                    task.wait()
                end
                pcall(function()
                    hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                    hrp.RotVelocity = Vector3.new(0,0,0)
                    hrp.Velocity = Vector3.new(0,0,0)
                end)
                vehicleFlingReturnCF5 = nil
            end

            task.spawn(function()
                for _ = 1, 5 do
                    task.wait()
                    if hrp and hrp.Parent then
                        pcall(function()
                            hrp.AssemblyLinearVelocity = Vector3.new(0,0,0)
                            hrp.Velocity = Vector3.new(0,0,0)
                        end)
                    end
                end
                if hrp and hrp.Parent then pcall(function() hrp.Anchored = false end) end
                isStoppingVehicleFling5 = false
            end)

            vehicleFlingCar5 = nil

            if vehicleFlingToggleRef5 then
                pcall(function() vehicleFlingToggleRef5:Set(false) end)
            end

            if not silent then
                local notifText = (success == true) and "Target flinged to the void!" or "Attack cancelled."
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Vehicle Fling",
                    Text = notifText,
                    Duration = 3
                })
            end
        end

        vehicleFlingToggleRef5 = TrollTab:AddToggle({
            Name = "Vehicle Fling V5",
            Description = "Spawns vehicle V5 and flings the target.",
            Default = false,
            Callback = function(state)
                if state then
                    local lp = game:GetService("Players").LocalPlayer
                    local targetName = _G.TrollTarget
                    local targetPlayer = nil
                    if targetName and targetName ~= "---" and targetName ~= "" then
                        targetPlayer = game:GetService("Players"):FindFirstChild(targetName)
                    end

                    if not targetPlayer then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Select a target first!", Duration=3})
                        if vehicleFlingToggleRef5 then pcall(function() vehicleFlingToggleRef5:Set(false) end) end
                        return
                    end

                    vehicleFlingReturnCF5 = lp.Character and (lp.Character.PrimaryPart and lp.Character.PrimaryPart.CFrame or (lp.Character:FindFirstChild("HumanoidRootPart") and lp.Character.HumanoidRootPart.CFrame)) or CFrame.new(0, 5, 0)

                    local char = lp.Character
                    local hrp = char and char:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        pcall(function() hrp.CFrame = CFrame.new(-1250.97, 78.58, -1258.14) end)
                    end
                    task.wait(0.2)

                    local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
                    _G.VehicleFlingActive = true
                    pcall(function() RE:WaitForChild("1Ca1r"):FireServer("PickingCar", _G.FlingVehicle5) end)
                    task.wait(1.5)

                    local myVehicle = nil
                    local expectedName = lp.Name .. "Car"
                    local vehiclesFolder = workspace:FindFirstChild("Vehicles")
                    local listToSearch = vehiclesFolder and vehiclesFolder:GetChildren() or workspace:GetChildren()

                    for _, v in pairs(listToSearch) do
                        if v:IsA("Model") and v.Name == expectedName then
                            myVehicle = v
                            break
                        end
                    end

                    if not myVehicle then
                        local closestDist = math.huge
                        local charPos = lp.Character and lp.Character.PrimaryPart and lp.Character.PrimaryPart.Position or Vector3.new(0,0,0)
                        for _, v in pairs(listToSearch) do
                            if v:IsA("Model") then
                                local hasSeat = false
                                for _, d in pairs(v:GetDescendants()) do
                                    if d:IsA("VehicleSeat") then hasSeat = true break end
                                end
                                if hasSeat then
                                    local pPart = v.PrimaryPart or v:FindFirstChild("Body") or v:FindFirstChildOfClass("Part") or v:FindFirstChildOfClass("MeshPart")
                                    if pPart then
                                        local dist = (pPart.Position - charPos).Magnitude
                                        if dist < closestDist and dist < 30 then
                                            closestDist = dist
                                            myVehicle = v
                                        end
                                    end
                                end
                            end
                        end
                    end

                    vehicleFlingCar5 = myVehicle
                    _G.VehicleFlingCar = myVehicle

                    if not myVehicle then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Vehicle not found!", Duration=3})
                        _G.VehicleFlingCar = nil
                        _G.VehicleFlingActive = false
                        if vehicleFlingToggleRef5 then pcall(function() vehicleFlingToggleRef5:Set(false) end) end
                        return
                    end

                    local seat = nil
                    for _, d in pairs(myVehicle:GetDescendants()) do
                        if d:IsA("VehicleSeat") and (d.Name:lower():find("drive") or d.Name:lower():find("main") or not seat) then
                            seat = d
                        end
                    end
                    if not seat then
                        for _, d in pairs(myVehicle:GetDescendants()) do
                            if d:IsA("VehicleSeat") or d:IsA("Seat") then seat = d break end
                        end
                    end

                    if not seat then
                        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="Seat not found!", Duration=3})
                        _G.VehicleFlingActive = false
                        _G.VehicleFlingCar = nil
                        if vehicleFlingToggleRef then pcall(function() vehicleFlingToggleRef:Set(false) end) end
                        return
                    end

                    
                    for _, sPart in pairs(myVehicle:GetDescendants()) do
                        if (sPart:IsA("Seat") or sPart:IsA("VehicleSeat")) and sPart ~= seat then
                            pcall(function()
                                sPart.Disabled = true
                                sPart.CanTouch = false
                                sPart.CanCollide = false
                            end)
                        end
                    end

                    pcall(function()
                        myVehicle:PivotTo(lp.Character.PrimaryPart.CFrame * CFrame.new(0, 0, -12))
                    end)
                    task.wait(0.2)

                    local myChar = lp.Character
                    local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")

                    if myHrp and myHum then
                        pcall(function()
                            seat.Disabled = false
                            seat.CanTouch = true
                            myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                        end)
                        myHrp.CFrame = seat.CFrame + Vector3.new(0, 2, 0)
                        task.wait(0.15)
                        pcall(function() seat:Sit(myHum) end)
                        local waitCount = 0
                        while not myHum.Sit and waitCount < 12 do
                            pcall(function()
                                seat.Disabled = false
                                myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                                myHrp.CFrame = seat.CFrame + Vector3.new(0, 1, 0)
                                seat:Sit(myHum)
                            end)
                            task.wait(0.1)
                            waitCount = waitCount + 1
                        end
                        if not myHum.Sit then
                            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Vehicle Fling", Text="I can't sit down!", Duration=3})
                            _G.VehicleFlingActive = false
                            _G.VehicleFlingCar = nil
                            if vehicleFlingToggleRef then pcall(function() vehicleFlingToggleRef:Set(false) end) end
                            return
                        end
                    end

                    vehicleFlingRunning5 = true

                    if myHum then
                        pcall(function() myHum:SetStateEnabled(Enum.HumanoidStateType.Seated, true) end)
                    end

                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="FLING ACTIVE", Text="Tractor engaged!", Duration=3})

                    local attackStart = tick()
                    vehicleFlingConnection5 = game:GetService("RunService").Heartbeat:Connect(function(dt)
                        if not vehicleFlingRunning5 or not myVehicle or not myVehicle.Parent then
                            if vehicleFlingConnection5 then vehicleFlingConnection5:Disconnect() end
                            return
                        end
                        if not targetPlayer.Character then
                            task.spawn(function() stopVehicleFling5(false, false) end)
                            return
                        end

                        local tHrp = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
                        local tHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid")
                        local primary = myVehicle.PrimaryPart or getVehiclePart(myVehicle) or seat

                        if not tHrp or not primary then return end

                        local t = tick()
                        local r = 2
                        local ox = math.sin(t * 10) * r
                        local oz = math.cos(t * 10) * r

                        local targetCF = CFrame.new(
                            tHrp.Position + Vector3.new(ox, 0.5, oz),
                            tHrp.Position
                        )

                        pcall(function()
                            if myVehicle.PivotTo then
                                myVehicle:PivotTo(targetCF)
                            elseif myVehicle.SetPrimaryPartCFrame then
                                myVehicle:SetPrimaryPartCFrame(targetCF)
                            end
                            local dir = (tHrp.Position - primary.Position)
                            if dir.Magnitude > 0.1 then dir = dir.Unit end
                            primary.AssemblyLinearVelocity  = dir * 25000 + Vector3.new(0, 12000, 0)
                            primary.AssemblyAngularVelocity = Vector3.zero
                            primary.RotVelocity = Vector3.new(0, 8000, 0)
                        end)

                        local elapsed = tick() - attackStart
                        local isTargetFlinged = (tHrp.Position.Y < -20) or (tHrp.AssemblyLinearVelocity.Magnitude > 150) or (tHum and tHum.Health <= 0) or (tHum and tHum.Sit)
                        if elapsed > 2.5 or isTargetFlinged then
                            vehicleFlingRunning5 = false
                            if vehicleFlingConnection5 then
                                vehicleFlingConnection5:Disconnect()
                                vehicleFlingConnection5 = nil
                            end
                            task.spawn(function()
                                task.wait(0.2)
                                stopVehicleFling5(false, true)
                            end)
                        end
                    end)
                else
                    stopVehicleFling5(false, false)
                end
            end
        })
    end

-- [LOCKED: WORK PERFECTLY] CATEGORIE TROLL COMPLETE

do
    AvatarTab:AddSection({"Copy Avatar System"})

    local CopyAvatarTarget = ""
    local CopyAvatarActive = false
    local CopyColors = false

    local copyPlayerDropdown = AvatarTab:AddDropdown({
        Name = "Target Player",
        Options = {},
        Default = "",
        Callback = function(Value)
            CopyAvatarTarget = Value
        end
    })

    local function refreshCopyPlayers()
        local pList = {}
        for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
            if p ~= game:GetService("Players").LocalPlayer then
                table.insert(pList, p.Name)
            end
        end
        copyPlayerDropdown:Set(pList)
    end

    AvatarTab:AddButton({
        Name = "Refresh Player List",
        Callback = function()
            refreshCopyPlayers()
        end
    })

    refreshCopyPlayers()

    AvatarTab:AddToggle({
        Name = "Copy Item Colors",
        Default = false,
        Callback = function(Value)
            CopyColors = Value
        end
    })

    AvatarTab:AddButton({
        Name = "Copy Selected Avatar",
        Callback = function()
            if CopyAvatarTarget == "" then
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Copy Avatar", Text="Please select a player first!", Duration=3})
                return
            end

            if CopyAvatarActive then
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Copy Avatar", Text="A copy is already in progress...", Duration=3})
                return
            end

            local Players = game:GetService("Players")
            local RS = game:GetService("ReplicatedStorage")
            local Remotes = RS:FindFirstChild("Remotes") or RS:FindFirstChild("RE")
            local wearRemote = Remotes and Remotes:FindFirstChild("Wear")
            local setColorRemote = Remotes and Remotes:FindFirstChild("SetAccessoryColor")

            local TPlayer = Players:FindFirstChild(CopyAvatarTarget)
            if not TPlayer or not TPlayer.Character then
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Copy Avatar", Text="Player or character not found!", Duration=3})
                return
            end

            CopyAvatarActive = true
            _G.BTR_IS_COPYING_AVATAR = true
            _G.BTR_COPIED_PLAYERS = _G.BTR_COPIED_PLAYERS or {}
            _G.BTR_COPIED_PLAYERS[TPlayer.UserId] = true
            _G.BTR_LAST_COPY_TIME = os.clock()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Copy Avatar",
                Text = "Copy Item Colors: " .. (CopyColors and "ON" or "OFF"),
                Duration = 3
            })

            task.spawn(function()
                local THumanoid = TPlayer.Character:FindFirstChildOfClass("Humanoid")
                if not THumanoid then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Copy Avatar", Text="Humanoid not found on target!", Duration=3})
                    CopyAvatarActive = false
                    _G.BTR_IS_COPYING_AVATAR = false
                    return
                end

                local PDesc = nil
                pcall(function() PDesc = THumanoid:GetAppliedDescription() end)
                if not PDesc then
                    pcall(function() PDesc = Players:GetHumanoidDescriptionFromUserId(TPlayer.UserId) end)
                end

                if not PDesc then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Copy Avatar", Text="Unable to read target outfit!", Duration=3})
                    CopyAvatarActive = false
                    _G.BTR_IS_COPYING_AVATAR = false
                    return
                end

                local colorByName = {}
                local colorByMeshId = {}
                
                local function getMeshId(acc)
                    local handle = acc:FindFirstChild("Handle")
                    if not handle then return nil end
                    if handle:IsA("MeshPart") then return handle.MeshId:match("%d+") end
                    local smesh = handle:FindFirstChildOfClass("SpecialMesh")
                    if smesh and smesh.MeshId then return smesh.MeshId:match("%d+") end
                    local fmesh = handle:FindFirstChildOfClass("FileMesh")
                    if fmesh and fmesh.MeshId then return fmesh.MeshId:match("%d+") end
                    return nil
                end

                if TPlayer.Character then
                    for _, child in ipairs(TPlayer.Character:GetChildren()) do
                        if child:IsA("Accessory") then
                            local handle = child:FindFirstChild("Handle")
                            if handle then
                                local surfApp = handle:FindFirstChild("AccessoryColorSurfaceAppearance")
                                if surfApp then
                                    local col = surfApp.Color
                                    colorByName[child.Name] = col
                                    local meshId = getMeshId(child)
                                    if meshId then colorByMeshId[meshId] = col end
                                end
                            end
                        end
                    end
                end

                local wearQueue = {}
                local alreadyQueued = {}

                local function addToQueue(id)
                    if not tonumber(id) or tonumber(id) == 0 then return end
                    local idStr = tostring(math.floor(tonumber(id)))
                    if alreadyQueued[idStr] then return end
                    alreadyQueued[idStr] = true
                    table.insert(wearQueue, tonumber(id))
                end

                local accessories = PDesc:GetAccessories(true)
                for _, acc in ipairs(accessories) do
                    addToQueue(acc.AssetId)
                end

                if TPlayer.Character then
                    for _, child in ipairs(TPlayer.Character:GetChildren()) do
                        if child:IsA("Accessory") then
                            local meshId = getMeshId(child)
                            if meshId then addToQueue(meshId) end
                        end
                    end
                end

                local total = #wearQueue
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Copy Avatar",
                    Text = "Copying " .. total .. " items (Brookhaven cooldown active)...",
                    Duration = 5
                })

                local myChar = Players.LocalPlayer.Character
                if myChar and TPlayer.Character then
                    local myAnimate = myChar:FindFirstChild("Animate")
                    local targetAnimate = TPlayer.Character:FindFirstChild("Animate")
                    if myAnimate and targetAnimate then
                        for _, animFolder in ipairs(targetAnimate:GetChildren()) do
                            local myFolder = myAnimate:FindFirstChild(animFolder.Name)
                            if myFolder then
                                for _, animObj in ipairs(animFolder:GetChildren()) do
                                    if animObj:IsA("Animation") then
                                        local myAnimObj = myFolder:FindFirstChildOfClass("Animation")
                                        if myAnimObj then
                                            myAnimObj.AnimationId = animObj.AnimationId
                                        end
                                    end
                                end
                            end
                        end
                    end
                end

                if CopyAvatarActive and TPlayer.Character then
                    local SkinColor = TPlayer.Character:FindFirstChild("Body Colors")
                    if SkinColor and Remotes and Remotes:FindFirstChild("ChangeBodyColor") then
                        pcall(function() Remotes.ChangeBodyColor:FireServer(tostring(SkinColor.HeadColor)) end)
                        task.wait(0.2)
                    end
                end

                if CopyAvatarActive and Remotes and Remotes:FindFirstChild("IncrementBodySize") then
                    local incrementSizeRemote = Remotes:FindFirstChild("IncrementBodySize")
                    local targetHeight = THumanoid:FindFirstChild("BodyHeightScale")
                    if targetHeight then
                        local attempts = 0
                        while CopyAvatarActive and attempts < 15 do
                            myChar = Players.LocalPlayer.Character
                            local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")
                            if not myHum then break end
                            local myHeight = myHum:FindFirstChild("BodyHeightScale")
                            if not myHeight then break end
                            local diff = targetHeight.Value - myHeight.Value
                            if math.abs(diff) <= 0.05 then break end
                            pcall(function() incrementSizeRemote:FireServer(diff > 0) end)
                            task.wait(0.2)
                            attempts = attempts + 1
                        end
                    end
                end

                for i, id in ipairs(wearQueue) do
                    if not CopyAvatarActive then break end
                    if i == 1 or i % 3 == 0 or i == total then
                        game:GetService("StarterGui"):SetCore("SendNotification", {
                            Title = "Copy Avatar",
                            Text = "Item " .. i .. "/" .. total .. " being applied...",
                            Duration = 2.5
                        })
                    end

                    local myChar = Players.LocalPlayer.Character
                    local existingAccs = {}
                    if myChar and CopyColors then
                        for _, child in ipairs(myChar:GetChildren()) do
                            if child:IsA("Accessory") then
                                existingAccs[child] = true
                            end
                        end
                    end

                    if wearRemote then
                        pcall(function() wearRemote:InvokeServer(id) end)
                    end

                    local detectTime = 0
                    if CopyColors and setColorRemote then
                        local newAcc = nil
                        local attempts = 0
                        while attempts < 35 do
                            task.wait(0.1)
                            detectTime = detectTime + 0.1
                            if myChar then
                                for _, child in ipairs(myChar:GetChildren()) do
                                    if child:IsA("Accessory") and not existingAccs[child] then
                                        newAcc = child
                                        break
                                    end
                                end
                            end
                            if newAcc then break end
                            attempts = attempts + 1
                        end

                        if newAcc then
                            local meshId = getMeshId(newAcc)
                            local col = nil
                            if meshId and colorByMeshId[meshId] then
                                col = colorByMeshId[meshId]
                            elseif colorByName[newAcc.Name] then
                                col = colorByName[newAcc.Name]
                            end

                            if col then
                                pcall(function()
                                    setColorRemote:InvokeServer(id, {
                                        color = { r = col.R, g = col.G, b = col.B }
                                    })
                                end)
                            end
                        end
                    end

                    local waitTime = 2.1 - detectTime
                    if waitTime < 0 then waitTime = 0 end
                    task.wait(waitTime)
                end

                if CopyAvatarActive then
                    local msg = "Avatar copied 100%! (" .. total .. " items)"
                    if CopyColors then
                        msg = "Avatar + RGB colors copied 100%! (" .. total .. " items)"
                    end
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Copy Avatar", Text = msg, Duration = 5 })
                end
                CopyAvatarActive = false
                _G.BTR_LAST_COPY_TIME = os.clock()
                task.spawn(function()
                    task.wait(3)
                    _G.BTR_IS_COPYING_AVATAR = false
                    if _G.BTR_SYNC_ANTI_COPY_BASELINE then
                        _G.BTR_SYNC_ANTI_COPY_BASELINE()
                    end
                end)
            end)
        end
    })

    AvatarTab:AddButton({
        Name = "Stop Copying",
        Callback = function()
            if CopyAvatarActive then
                CopyAvatarActive = false
                _G.BTR_IS_COPYING_AVATAR = false
                _G.BTR_LAST_COPY_TIME = os.clock()
                if _G.BTR_SYNC_ANTI_COPY_BASELINE then
                    _G.BTR_SYNC_ANTI_COPY_BASELINE()
                end
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Copy Avatar", Text="Copy cancelled!", Duration=3})
            else
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Copy Avatar", Text="No copy currently in progress!", Duration=3})
            end
        end
    })
    
    AvatarTab:AddSection({"Giant Mode (FE)"})
    
    local bigBodySaved = nil
    local BIG_IDS = { 81881085814072, 79115019295211, 106740492797177, 81153927159685, 88668275797583, 115379341593655 }
    local giantCooldown = false

    AvatarTab:AddToggle({
        Name = "Giant Mode",
        Description = "Applies a giant body to your character server-side (visible to all).",
        Default = false,
        Callback = function(val)
            if not val and not bigBodySaved then return
            end
            if giantCooldown then return end
            giantCooldown = true
            task.delay(1.5, function() giantCooldown = false end)

            pcall(function()
                local player = game:GetService("Players").LocalPlayer
                local RS = game:GetService("ReplicatedStorage")
                local Remotes = RS:WaitForChild("Remotes", 5)
                if not Remotes then return end
                
                local char = player.Character
                if not char then return end
                local humanoid = char:FindFirstChildOfClass("Humanoid")
                if not humanoid then return end

                if val then if not bigBodySaved then
                        local desc = humanoid:GetAppliedDescription()
                        bigBodySaved = {
                            desc.Torso, desc.RightArm, desc.LeftArm,
                            desc.RightLeg, desc.LeftLeg, desc.Head
                        }
                    end
                    Remotes.ChangeCharacterBody:InvokeServer(unpack({ [1] = BIG_IDS }))
                    game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Giant (FE)", Text = "Giant body applied!", Duration = 3 })
                else
                    if bigBodySaved then Remotes.ChangeCharacterBody:InvokeServer(unpack({ [1] = bigBodySaved }))
                        game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Giant (FE)", Text = "Normal size restored!", Duration = 3 })
                    end
                end
            end)
        end
    })

    
    
    
    AvatarTab:AddSection({"Chakra Armor (FE)"})

    local astralActive = false
    local astralTheme = "Purple"
    local astralScale = 11.0
    local astralIntensity = 1.0
    local astralLoop = nil
    local astralPhysicsLoop = nil
    local astralChest = nil
    local astralTurbines = {}
    local astralSmoothPos = nil
    local astralSmoothSpeed = 18.0
    local astralSenders = {}
    local ASTRAL_RATE = 0.03
    local astralRgbActive = false

    local ASTRAL_THEMES = {
        ["Purple"] = Color3.fromRGB(165, 30, 255),
        ["Blue"]   = Color3.fromRGB(0, 140, 255),
        ["Red"]    = Color3.fromRGB(240, 30, 20),
        ["Cyan"]   = Color3.fromRGB(0, 230, 230),
        ["Green"]  = Color3.fromRGB(30, 240, 80),
        ["Gold"]   = Color3.fromRGB(255, 215, 0),
        ["Black"]  = Color3.fromRGB(20, 15, 30),
        ["White"]  = Color3.fromRGB(255, 255, 255),
    }

    local function sendAstralCF(scf, cf, prop)
        if not astralActive or not scf then return end
        if prop then
            pcall(function()
                if prop:IsA("Model") then
                    prop:PivotTo(cf)
                elseif prop:IsA("BasePart") then
                    prop.CFrame = cf
                end
            end)
        end

        local entry = astralSenders[scf]
        local now = tick()
        if not entry then
            entry = { lastSent = 0, nextCF = cf, sending = false }
            astralSenders[scf] = entry
        else
            entry.nextCF = cf
        end

        if not entry.sending and (now - entry.lastSent >= ASTRAL_RATE) then
            entry.sending = true
            entry.lastSent = now
            local toSend = entry.nextCF
            entry.nextCF = nil
            task.spawn(function()
                pcall(function() scf:InvokeServer(toSend) end)
                entry.sending = false
                if astralActive and entry.nextCF and (tick() - entry.lastSent >= ASTRAL_RATE) then
                    entry.sending = true
                    entry.lastSent = tick()
                    local nextSend = entry.nextCF
                    entry.nextCF = nil
                    task.spawn(function()
                        pcall(function() scf:InvokeServer(nextSend) end)
                        entry.sending = false
                    end)
                end
            end)
        end
    end

    local function applyAstralNeon(prop, col)
        if not prop then return end
        pcall(function()
            for _, p in ipairs(prop:GetDescendants()) do
                if p:IsA("BasePart") then
                    p.Material = Enum.Material.Neon
                    p.Color = col
                    p.CanCollide = false
                    p.CanTouch = false
                    p.CanQuery = false
                    p.Massless = true
                end
            end
        end)
    end

    local function applyAstralColorToPart(prop, cpc, col)
        if prop then
            pcall(function()
                for _, p in ipairs(prop:GetDescendants()) do
                    if p:IsA("BasePart") then
                        p.Material = Enum.Material.Neon
                        p.Color = col
                    end
                end
            end)
        end
        if cpc then
            task.spawn(function()
                pcall(function() cpc:InvokeServer(col) end)
            end)
        end
    end

    local function setAstralTheme(themeName)
        astralTheme = themeName
        if themeName == "Rainbow RGB" then
            astralRgbActive = true
            return
        else
            astralRgbActive = false
        end

        local solidCol = ASTRAL_THEMES[themeName] or Color3.fromRGB(165, 30, 255)
        if astralChest and astralChest.prop then
            applyAstralColorToPart(astralChest.prop, astralChest.cpc, solidCol)
        end
        for _, entry in ipairs(astralTurbines) do
            if entry.prop then
                applyAstralColorToPart(entry.prop, entry.cpc, solidCol)
            end
        end
    end

    local function stopAstralSovereign()
        astralActive = false
        if astralLoop then
            astralLoop:Disconnect()
            astralLoop = nil
        end
        if astralPhysicsLoop then
            astralPhysicsLoop:Disconnect()
            astralPhysicsLoop = nil
        end
        astralSenders = {}
        astralChest = nil
        astralTurbines = {}
        astralSmoothPos = nil
        astralRgbActive = false

        pcall(function()
            local RS = game:GetService("ReplicatedStorage")
            local clearRE = RS:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
            clearRE:FireServer("ClearAllProps")
        end)
    end
    _G.BTR_StopAstralSovereign = stopAstralSovereign

    local function spawnAstralSovereign()
        if astralActive then
            stopAstralSovereign()
            task.wait(0.3)
        end
        astralActive = true
        astralSmoothPos = nil

        local RS = game:GetService("ReplicatedStorage")
        local RE = RS:WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")
        local toolRE = RE:WaitForChild("1Too1l")
        local Telemetry = RS:FindFirstChild("Remotes") and RS.Remotes:FindFirstChild("TelemetryClientInteraction")
        local lp = game:GetService("Players").LocalPlayer
        local myPropName = "Prop" .. lp.Name

        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:WaitForChild("Humanoid")

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.4)

        local function getPropMaker()
            local bp = lp:FindFirstChild("Backpack")
            local c = lp.Character
            if not bp or not c then return nil end
            local tool = c:FindFirstChild("PropMaker") or bp:FindFirstChild("PropMaker")
            if not tool then
                pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
                task.wait(0.3)
                tool = c:FindFirstChild("PropMaker") or bp:FindFirstChild("PropMaker")
            end
            return tool
        end

        local pm = getPropMaker()
        if not pm then astralActive = false return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local wc = workspace:FindFirstChild("WorkspaceCom")
        if not wc then astralActive = false return end

        local existingProps = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    existingProps[prop] = true
                end
            end
        end

        local function getSurfacePos(targetWorldPos)
            local rayParams = RaycastParams.new()
            rayParams.FilterDescendantsInstances = {lp.Character, workspace:FindFirstChild("WorkspaceCom")}
            rayParams.FilterType = Enum.RaycastFilterType.Exclude
            local ray = workspace:Raycast(targetWorldPos + Vector3.new(0, 10, 0), Vector3.new(0, -30, 0), rayParams)
            return (ray and ray.Instance) or workspace.Terrain, (ray and ray.Position) or targetWorldPos
        end

        local function spawnSingleProp(propNameStr, categoryStr, targetInst, targetPos)
            local tool = getPropMaker()
            if not tool then return nil end

            if Telemetry then
                pcall(function() Telemetry:FireServer("filterClick", {name = categoryStr, itemType = "Props"}) end)
                task.wait(0.05)
                pcall(function() Telemetry:FireServer("filterClick", {name = propNameStr, itemType = "Props"}) end)
                task.wait(0.05)
            end

            pcall(function() clearTools:FireServer("RequestingPropName", propNameStr, categoryStr) end)
            task.wait(0.3)

            local currentHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if currentHum and tool.Parent ~= lp.Character then
                currentHum:EquipTool(tool)
                task.wait(0.1)
            end

            local tpm = tool:FindFirstChild("Tool_PropMake")
            local startW = tick()
            while not tpm and tick() - startW < 3.0 and astralActive do
                tpm = tool:FindFirstChild("Tool_PropMake")
                if not tpm then
                    local bp = lp.Backpack:FindFirstChild("PropMaker")
                    if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
                end
                if not tpm then task.wait(0.1) end
            end

            if not tpm then return nil end

            pcall(function() tpm:FireServer(targetInst, targetPos) end)

            local spawned = nil
            local t0 = tick()
            while tick() - t0 < 3.5 and astralActive do
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        if prop.Name == myPropName and not existingProps[prop] then
                            spawned = prop
                            existingProps[prop] = true
                            break
                        end
                    end
                    if spawned then break end
                end
                if spawned then break end
                task.wait(0.05)
            end

            return spawned
        end

        local function duplicateProp(firstProp, countNeeded)
            if not firstProp then return false end
            local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
            if not dupRemote then return false end
            for _ = 1, countNeeded do
                pcall(function() dupRemote:InvokeServer() end)
                task.wait(0.05)
            end
            return true
        end

        
        local bunkerTarget = hrp.Position + (hrp.CFrame.LookVector * 12)
        local bunkerHitInst, bunkerHitPos = getSurfacePos(bunkerTarget)
        local chestProp = spawnSingleProp("BigGunBunker", "Big", bunkerHitInst, bunkerHitPos)
        if not chestProp then
            chestProp = spawnSingleProp("BigGunBunker", "Big", bunkerHitInst, bunkerHitPos)
        end
        if not chestProp or not astralActive then
            stopAstralSovereign()
            return
        end

        
        local turbineTarget = hrp.Position - (hrp.CFrame.LookVector * 12)
        local turbineHitInst, turbineHitPos = getSurfacePos(turbineTarget)
        local firstTurbine = spawnSingleProp("PowerTurbine", "Barrel", turbineHitInst, turbineHitPos)
        if not firstTurbine then
            firstTurbine = spawnSingleProp("PowerTurbine", "001_TrafficCones", turbineHitInst, turbineHitPos)
        end
        if not firstTurbine or not astralActive then
            stopAstralSovereign()
            return
        end

        duplicateProp(firstTurbine, 13)
        task.wait(0.8)

        -- Remove cardboard after 1 second (LOCKED Rule 7)
        task.wait(1.0)
        pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)
        task.wait(0.2)

        
        local function setupPropEntry(prop)
            local scf = prop:FindFirstChild("SetCurrentCFrame") or prop:FindFirstChildWhichIsA("RemoteFunction", true)
            local cpc = prop:FindFirstChild("ChangePropColor") or prop:FindFirstChild("ChangePropColor", true)
            return { prop = prop, scf = scf, cpc = cpc }
        end

        astralChest = nil
        astralTurbines = {}

        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    local entry = setupPropEntry(prop)
                    if entry.scf then
                        local isBunker = false
                        pcall(function()
                            for _, p in ipairs(prop:GetDescendants()) do
                                if p:IsA("BasePart") and p.Size.Magnitude > 25 then
                                    isBunker = true
                                    break
                                end
                            end
                        end)

                        if isBunker and not astralChest then
                            astralChest = entry
                        else
                            table.insert(astralTurbines, entry)
                        end
                    end
                end
            end
        end

        local defaultCol = ASTRAL_THEMES[astralTheme] or Color3.fromRGB(165, 30, 255)
        if astralChest then applyAstralNeon(astralChest.prop, defaultCol) end
        for _, node in ipairs(astralTurbines) do
            applyAstralNeon(node.prop, defaultCol)
        end

        
        local rs = game:GetService("RunService")
        astralPhysicsLoop = rs.Stepped:Connect(function()
            if not astralActive then return end
            local cc = lp.Character
            if cc then
                for _, part in ipairs(cc:GetDescendants()) do
                    if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                        part.CanCollide = false
                        part.CanTouch = false
                    end
                end
            end
            if astralChest and astralChest.prop then
                pcall(function()
                    for _, p in ipairs(astralChest.prop:GetDescendants()) do
                        if p:IsA("BasePart") then
                            p.CanCollide = false
                            p.CanTouch = false
                            p.CanQuery = false
                        end
                    end
                end)
            end
            for _, tData in ipairs(astralTurbines) do
                if tData.prop then
                    pcall(function()
                        for _, p in ipairs(tData.prop:GetDescendants()) do
                            if p:IsA("BasePart") then
                                p.CanCollide = false
                                p.CanTouch = false
                                p.CanQuery = false
                            end
                        end
                    end)
                end
            end
        end)

        setAstralTheme(astralTheme)

        
        local tN = astralTurbines
        local startTime = tick()

        astralLoop = rs.RenderStepped:Connect(function(dt)
            if not astralActive then return end

            local cc = lp.Character
            if not cc then return end
            local hrpNow = cc:FindFirstChild("HumanoidRootPart")
            local humNow = cc:FindFirstChildOfClass("Humanoid")
            if not hrpNow or not humNow then return end

            local hrpCF = hrpNow.CFrame
            local t = tick() - startTime
            local intensity = astralIntensity
            local scale = astralScale

            local breathSlow = math.sin(t * 1.8) * 0.35 * intensity
            local breathFast = math.sin(t * 3.5) * 0.15 * intensity
            local breathCombo = breathSlow + breathFast
            local ribBreathe = math.sin(t * 2.2) * 0.6 * intensity
            local shoulderSway = math.sin(t * 1.4) * 0.3 * intensity
            local haloSpin = t * 22
            local haloPulse = math.sin(t * 4.0) * 0.15 * intensity
            local tailWave = math.sin(t * 2.8) * 0.4 * intensity
            local headDrift = math.sin(t * 1.0) * 2.5 * intensity

            local moveIntensity = 1.0
            if humNow.MoveDirection.Magnitude > 0.05 then
                moveIntensity = 1.5
            end
            for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                if track.IsPlaying and track.Priority == Enum.AnimationPriority.Action4 then
                    moveIntensity = 2.0
                    break
                end
            end

            
            local targetPos = hrpCF.Position + Vector3.new(0, 1.5 + breathCombo * 0.5, 0)
            if not astralSmoothPos then
                astralSmoothPos = targetPos
            else
                local lerpFactor = math.clamp(astralSmoothSpeed * dt, 0, 1)
                astralSmoothPos = astralSmoothPos:Lerp(targetPos, lerpFactor)
            end

            local centerCF = CFrame.new(astralSmoothPos) * (hrpCF - hrpCF.Position)

            
            if astralChest and astralChest.scf then
                local torsoBreath = math.sin(t * 2.0) * 3.0 * intensity
                local torsoSway = math.sin(t * 0.7) * 1.5 * intensity
                local torsoCF = centerCF
                    * CFrame.new(0, -1.0 + breathCombo * 0.5, 0)
                    * CFrame.Angles(
                        math.rad(90 + torsoBreath),
                        math.rad(torsoSway),
                        0
                    )
                sendAstralCF(astralChest.scf, torsoCF, astralChest.prop)
            end

            
            local headPart = cc:FindFirstChild("Head") or hrpNow
            if tN[1] and tN[1].scf then
                local relH = hrpCF:ToObjectSpace(headPart.CFrame)
                local headRot = relH - relH.Position
                local headCF = centerCF
                    * CFrame.new(0, 5.8 * (scale / 11.0), -1.2)
                    * headRot
                    * CFrame.Angles(math.rad(-90 + headDrift), math.rad(math.sin(t * 0.8) * 3), 0)
                sendAstralCF(tN[1].scf, headCF, tN[1].prop)
            end

            
            local lUpperArm = cc:FindFirstChild("LeftUpperArm")
            if tN[2] and tN[2].scf then
                local shoulderBob = math.sin(t * 2.5 + 0.3) * 0.5 * moveIntensity
                local shoulderCF
                if lUpperArm then
                    local rel = hrpCF:ToObjectSpace(lUpperArm.CFrame)
                    local relRot = rel - rel.Position
                    shoulderCF = centerCF
                        * CFrame.new(-6.2 * (scale / 11.0) - 1.5, 4.0 * (scale / 11.0) + shoulderBob + shoulderSway, 0)
                        * relRot
                        * CFrame.Angles(math.rad(90), 0, math.rad(90 + math.sin(t * 1.8) * 8))
                else
                    shoulderCF = centerCF
                        * CFrame.new(-7.5, 4.0 + shoulderBob, 0)
                        * CFrame.Angles(0, 0, math.rad(-30 + math.sin(t * 2) * 10))
                end
                sendAstralCF(tN[2].scf, shoulderCF, tN[2].prop)
            end

            local rUpperArm = cc:FindFirstChild("RightUpperArm")
            if tN[3] and tN[3].scf then
                local shoulderBob = math.sin(t * 2.5 + 1.0) * 0.5 * moveIntensity
                local shoulderCF
                if rUpperArm then
                    local rel = hrpCF:ToObjectSpace(rUpperArm.CFrame)
                    local relRot = rel - rel.Position
                    shoulderCF = centerCF
                        * CFrame.new(6.2 * (scale / 11.0) + 1.5, 4.0 * (scale / 11.0) + shoulderBob + shoulderSway, 0)
                        * relRot
                        * CFrame.Angles(math.rad(90), 0, math.rad(-90 - math.sin(t * 1.8) * 8))
                else
                    shoulderCF = centerCF
                        * CFrame.new(7.5, 4.0 + shoulderBob, 0)
                        * CFrame.Angles(0, 0, math.rad(30 - math.sin(t * 2) * 10))
                end
                sendAstralCF(tN[3].scf, shoulderCF, tN[3].prop)
            end

            
            local lArmParts = {"LeftUpperArm", "LeftLowerArm", "LeftHand"}
            for idx, limbName in ipairs(lArmParts) do
                local node = tN[3 + idx]
                if node and node.scf then
                    local p = cc:FindFirstChild(limbName)
                    if p then
                        local rel = hrpCF:ToObjectSpace(p.CFrame)
                        local relRot = rel - rel.Position
                        local armSpread = -6.2 * (scale / 11.0)
                        local trailStretch = (idx == 3) and (moveIntensity * 0.8) or 0
                        local scaledPos = Vector3.new(
                            (rel.Position.X * scale * 1.15) + armSpread,
                            rel.Position.Y * scale - (idx * 1.2),
                            rel.Position.Z * scale - trailStretch
                        )
                        local armCF = centerCF * CFrame.new(scaledPos) * relRot * CFrame.Angles(math.rad(90), 0, math.rad(90))
                        sendAstralCF(node.scf, armCF, node.prop)
                    end
                end
            end

            
            local rArmParts = {"RightUpperArm", "RightLowerArm", "RightHand"}
            for idx, limbName in ipairs(rArmParts) do
                local node = tN[6 + idx]
                if node and node.scf then
                    local p = cc:FindFirstChild(limbName)
                    if p then
                        local rel = hrpCF:ToObjectSpace(p.CFrame)
                        local relRot = rel - rel.Position
                        local armSpread = 6.2 * (scale / 11.0)
                        local trailStretch = (idx == 3) and (moveIntensity * 0.8) or 0
                        local scaledPos = Vector3.new(
                            (rel.Position.X * scale * 1.15) + armSpread,
                            rel.Position.Y * scale - (idx * 1.2),
                            rel.Position.Z * scale - trailStretch
                        )
                        local armCF = centerCF * CFrame.new(scaledPos) * relRot * CFrame.Angles(math.rad(90), 0, math.rad(-90))
                        sendAstralCF(node.scf, armCF, node.prop)
                    end
                end
            end

            
            if tN[10] and tN[10].scf then
                local ribExpand = ribBreathe * 0.8
                local ribLCF = centerCF
                    * CFrame.new((-3.5 - ribExpand) * (scale / 11.0), -2.0 + breathCombo * 0.3, (-2.8 + ribExpand * 0.3) * (scale / 11.0))
                    * CFrame.Angles(math.rad(25 + math.sin(t * 2.2) * 6), math.rad(55 + math.sin(t * 1.5) * 8 * intensity), math.rad(-25 + ribBreathe * 5))
                sendAstralCF(tN[10].scf, ribLCF, tN[10].prop)
            end
            if tN[11] and tN[11].scf then
                local ribExpand = ribBreathe * 0.8
                local ribRCF = centerCF
                    * CFrame.new((3.5 + ribExpand) * (scale / 11.0), -2.0 + breathCombo * 0.3, (-2.8 + ribExpand * 0.3) * (scale / 11.0))
                    * CFrame.Angles(math.rad(25 + math.sin(t * 2.2) * 6), math.rad(-55 - math.sin(t * 1.5) * 8 * intensity), math.rad(25 - ribBreathe * 5))
                sendAstralCF(tN[11].scf, ribRCF, tN[11].prop)
            end

            
            local lLegPart = cc:FindFirstChild("LeftUpperLeg") or cc:FindFirstChild("LeftLeg")
            if tN[12] and tN[12].scf and lLegPart then
                local rel = hrpCF:ToObjectSpace(lLegPart.CFrame)
                local relRot = rel - rel.Position
                local tailSwing = math.sin(t * 3.0) * 0.3 * moveIntensity
                local legCF = centerCF
                    * CFrame.new(-3.0 * (scale / 11.0) + rel.Position.X * scale * 0.5 + tailWave, -5.8 * (scale / 11.0) + rel.Position.Y * scale * 0.5 + tailSwing, rel.Position.Z * scale * 0.5)
                    * relRot * CFrame.Angles(math.rad(90), 0, math.rad(90 + tailWave * 20))
                sendAstralCF(tN[12].scf, legCF, tN[12].prop)
            end

            local rLegPart = cc:FindFirstChild("RightUpperLeg") or cc:FindFirstChild("RightLeg")
            if tN[13] and tN[13].scf and rLegPart then
                local rel = hrpCF:ToObjectSpace(rLegPart.CFrame)
                local relRot = rel - rel.Position
                local tailSwing = math.sin(t * 3.0 + 1.57) * 0.3 * moveIntensity
                local legCF = centerCF
                    * CFrame.new(3.0 * (scale / 11.0) + rel.Position.X * scale * 0.5 - tailWave, -5.8 * (scale / 11.0) + rel.Position.Y * scale * 0.5 + tailSwing, rel.Position.Z * scale * 0.5)
                    * relRot * CFrame.Angles(math.rad(90), 0, math.rad(-90 - tailWave * 20))
                sendAstralCF(tN[13].scf, legCF, tN[13].prop)
            end

            
            if tN[14] and tN[14].scf then
                local haloBob = math.sin(t * 3.5) * 0.4
                local haloCF = centerCF
                    * CFrame.new(0, 9.0 * (scale / 11.0) + haloBob + haloPulse * 2, 0.5)
                    * CFrame.Angles(math.rad(20 + math.sin(t * 2.0) * 8), math.rad(haloSpin), math.rad(math.sin(t * 3) * 12))
                sendAstralCF(tN[14].scf, haloCF, tN[14].prop)
            end

            
            if astralRgbActive then
                local nowTick = tick()
                local allNodes = {}
                if astralChest then table.insert(allNodes, astralChest) end
                for _, n in ipairs(tN) do table.insert(allNodes, n) end
                for i, node in ipairs(allNodes) do
                    local phase = (i / #allNodes) * 0.7
                    local waveColor = Color3.fromHSV((nowTick * 0.5 + phase + math.sin(nowTick * 2 + i) * 0.1) % 1, 1, 1)
                    if node.prop then
                        pcall(function()
                            for _, p in ipairs(node.prop:GetDescendants()) do
                                if p:IsA("BasePart") then
                                    p.Color = waveColor
                                end
                            end
                        end)
                    end
                    if node.cpc and (nowTick - (node.lastColorSent or 0) >= 0.25) then
                        node.lastColorSent = nowTick
                        task.spawn(function()
                            pcall(function() node.cpc:InvokeServer(waveColor) end)
                        end)
                    end
                end
            end
        end)

        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Chakra Armor",
                Text = "Chakra Armor Activated!",
                Duration = 3
            })
        end)
    end

    AvatarTab:AddToggle({
        Name = "Chakra Armor",
        Description = "Spawns giant chakra aura armor around your character.",
        Default = false,
        Callback = function(val)
            if val then
                task.spawn(spawnAstralSovereign)
            else
                stopAstralSovereign()
            end
        end
    })

    AvatarTab:AddDropdown({
        Name = "Armor Color",
        Options = {"Purple", "Blue", "Red", "Cyan", "Green", "Gold", "Black", "White", "Rainbow RGB"},
        Default = "Purple",
        Callback = function(val)
            setAstralTheme(val)
        end
    })

    AvatarTab:AddSlider({
        Name = "Armor Size",
        Min = 8,
        Max = 16,
        Default = 11,
        Suffix = "x",
        Callback = function(val)
            astralScale = tonumber(val) or 11.0
        end
    })
end

    
    do
        local adminRunning = false
        local adminMode = "Pull"
        local adminLocked = false
        local adminToggle = nil

        local adminWhitelist = { [1] = "", [2] = "", [3] = "" }
        local function isAdminWhitelisted(p)
            if not p then return false end
            local name = p.Name
            local lp = game:GetService("Players").LocalPlayer
            if name == lp.Name then return true end
            for i = 1, 3 do
                if adminWhitelist[i] and adminWhitelist[i] ~= "" and adminWhitelist[i] ~= "---" and adminWhitelist[i] == name then
                    return true
                end
            end
            return false
        end

        local adminNotifHistory = {}
        local function safeAdminNotify(title, text, duration)
            if not text or type(text) ~= "string" or text == "" or text:find("nil") then return end
            local now = os.clock()

            
            for i = #adminNotifHistory, 1, -1 do
                if now - adminNotifHistory[i].time > 3.0 then
                    table.remove(adminNotifHistory, i)
                end
            end

            
            for _, item in ipairs(adminNotifHistory) do
                if item.text == text then return end
            end

            
            if #adminNotifHistory >= 2 then
                return
            end

            table.insert(adminNotifHistory, { text = text, time = now })

            pcall(function()
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = title or "Admin",
                    Text = text,
                    Duration = duration or 3
                })
            end)
        end

        local function adminClearProps()
            local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local cr = RE and RE:FindFirstChild("1Clea1rTool1s")
            if cr then pcall(function() cr:FireServer("ClearAllProps") end) end
        end

        local adminStarting = false

        local function adminStopAll(fromToggle)
            if adminLocked then return end
            
            
            if not adminRunning then return end
            
            
            
            if fromToggle and fromToggle ~= "__force__" and fromToggle ~= adminToggle then return end
            adminLocked = true
            adminRunning = false
            -- [RULE PROPMAKER LOCKED] ClearAllProps d'abord, puis retrait du carton
            adminClearProps()
            task.wait(0.25)
            local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            if RE then
                local toolRemote = RE:FindFirstChild("1Too1l")
                
            end
            
            
            task.wait(0.1)
            local myRE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            if myRE then
                local clr = myRE:FindFirstChild("1Clea1rTool1s")
                if clr then
                    pcall(function() clr:FireServer("ClearAllTools") end)
                    task.wait(0.1)
                    pcall(function() clr:FireServer("ClosePropMenu") end)
                end
            end
            local lpChar = game:GetService("Players").LocalPlayer.Character
            local lpHum = lpChar and lpChar:FindFirstChildOfClass("Humanoid")
            if lpHum then pcall(function() lpHum:UnequipTools() end) end
            cleanupPropMakerTool()
            task.wait(0.1)
            
            
            local forceUncheck = (fromToggle == "__force__")
            if adminToggle and (not adminStarting or forceUncheck) then
                local tg = adminToggle
                adminToggle = nil
                pcall(function() tg:Set(false) end)
            else
                adminToggle = nil
            end
            adminLocked = false
            adminStarting = false
        end
        _G.AdminStopAll = adminStopAll

        local function runAdmin(mode, toggle, callerName)
            adminMode = mode
            adminToggle = toggle
            adminRunning = true
            adminStarting = true
            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character or lp.CharacterAdded:Wait()
            local myRoot = char:FindFirstChild("HumanoidRootPart")

            local allPlayers = game:GetService("Players"):GetPlayers()
            local validPlayers = {}
            for _, p in ipairs(allPlayers) do
                local isFakeAdmin = (fakeAdminEnabled and fakeAdminTarget and p.Name == fakeAdminTarget) or (callerName and p.Name == callerName)
                if p ~= lp and not isFakeAdmin and not isAdminWhitelisted(p) then
                    table.insert(validPlayers, p)
                end
            end
            if #validPlayers == 0 then
                safeAdminNotify("Admin", "No targets found!", 3)
                adminStopAll("__force__")
                return
            end

            local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
            local clearTools = RE:WaitForChild("1Clea1rTool1s")

            local propsTool = getOrFetchPropMaker()
            if not propsTool then
                local fetchStart = tick()
                while not propsTool and tick() - fetchStart < 3.5 do
                    task.wait(0.3)
                    propsTool = getOrFetchPropMaker()
                end
            end
            if not propsTool then
                adminStopAll("__force__")
                return
            end

            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum and propsTool.Parent ~= char then
                pcall(function() hum:EquipTool(propsTool) end)
                task.wait(0.2)
            end

            pcall(function() clearTools:FireServer("OpenPropMenu") end)
            task.wait(0.1)

            -- [LOCKED: WORK PERFECTLY] Admin Mass Actions Prop Selection
            
            local propName = _G.AdminMassProp or "FurnitureToilet"
            if mode == "Fly" or mode == "Noclip" then
                propName = "FurnitureBeanBag"
            elseif callerName then
                propName = "FurnitureToilet"
            end
            local propCategory = "Furniture"
            pcall(function() clearTools:FireServer("RequestingPropName", propName, propCategory) end)
            task.wait(0.1)

            local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
            if not toolPropMake or not myRoot then
                adminStopAll("__force__")
                return
            end

            local myPropName = "Prop" .. lp.Name
            local currentPropsCount = 0
            pcall(function()
                local wc = workspace:FindFirstChild("WorkspaceCom")
                if wc then
                    for _, folder in pairs(wc:GetChildren()) do
                        for _, prop in pairs(folder:GetChildren()) do
                            if prop.Name == myPropName then currentPropsCount = currentPropsCount + 1 end
                        end
                    end
                end
            end)

            local needed = math.min(14, #validPlayers)
            local toSpawn = needed - currentPropsCount
            if toSpawn > 0 then
                local duplicated = spawnAndDuplicateProps(toolPropMake, myRoot.Position, toSpawn, 0)
                if not duplicated then
                    for i = 1, toSpawn do
                        if not adminRunning then break end
                        local curChar = lp.Character
                        local curHum = curChar and curChar:FindFirstChildOfClass("Humanoid")
                        if curHum and propsTool.Parent ~= curChar then
                            pcall(function() curHum:EquipTool(propsTool) end)
                            task.wait(0.1)
                        end
                        local angle = (i / toSpawn) * 2 * math.pi
                        local offset = Vector3.new(math.cos(angle) * 4, 0, math.sin(angle) * 4)
                        pcall(function() toolPropMake:FireServer(workspace.Terrain, myRoot.Position + offset) end)
                        task.wait(0.85)
                    end
                end
            end

            
            pcall(function() local h = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid"); if h then h:UnequipTools() end end)
            task.spawn(function()
                task.wait(0.85)
                pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker") end)
                pcall(function() clearTools:FireServer("ClosePropMenu") end)
                local hNow = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
                if hNow then pcall(function() hNow:UnequipTools() end) end
                cleanupPropMakerTool()
            end)

            adminStarting = false
            task.spawn(function()
                local staticDestPos = nil
                if mode == "Kill" then
                    staticDestPos = Vector3.new(0, workspace.FallenPartsDestroyHeight - 100, 0)
                elseif mode == "Freeze" then
                    staticDestPos = Vector3.new(21.03, 63.05, -479.78)
                elseif mode == "Void" or mode == "Exile" then
                    staticDestPos = Vector3.new(999999, 999999, 999999)
                elseif mode == "Jail" then
                    staticDestPos = Vector3.new(-582, 10, 57)
                elseif mode == "JailV2" then
                    staticDestPos = Vector3.new(498.80, -18.21, 225.37)
                elseif mode == "Cliff" then
                    staticDestPos = Vector3.new(-1796.89, -30.46, 106.37)
                end

                local playerVelocities = {}
                local adminFlyPositions = {}
                local adminFlyHeadings = {}
                local adminFlySlot1Tools = {}
                local adminFlySlot2Tools = {}
                local adminFlyLastTick = tick()
                local adminStartTime = tick()
                local adminDuration = (mode == "Bring") and 10 or 20

                while adminRunning do
                    
                    
                    if callerName and (tick() - adminStartTime >= adminDuration) then
                        adminStopAll()
                        break
                    end

                    local currentProps = {}
                    pcall(function()
                        local wc = workspace:FindFirstChild("WorkspaceCom")
                        if wc then
                            for _, folder in pairs(wc:GetChildren()) do
                                for _, prop in pairs(folder:GetChildren()) do
                                    if prop.Name == myPropName then
                                        local scf = prop:FindFirstChild("SetCurrentCFrame")
                                        if scf then table.insert(currentProps, {scf = scf, model = prop}) end
                                    end
                                end
                            end
                        end
                    end)

                    if #currentProps > 0 then
                    local allTargets = {}
                    for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                        local isFakeAdmin = (fakeAdminEnabled and fakeAdminTarget and p.Name == fakeAdminTarget) or (callerName and p.Name == callerName)
                        if p ~= lp and not isFakeAdmin and not isAdminWhitelisted(p) then
                            if p.Character and p.Character:FindFirstChild("Humanoid") and p.Character:FindFirstChild("HumanoidRootPart") and p.Character.Humanoid.Health > 0 then
                                table.insert(allTargets, p)
                            end
                        end
                    end

                    if #allTargets > 0 then
                    local currentDestPos = staticDestPos
                    local destRoot = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
                    if mode == "Bring" then
                        if callerName then
                            local cp = game:GetService("Players"):FindFirstChild(callerName)
                            if cp and cp.Character and cp.Character:FindFirstChild("HumanoidRootPart") then
                                destRoot = cp.Character.HumanoidRootPart
                            end
                        end
                        if destRoot then
                            currentDestPos = destRoot.Position
                        end
                    end

                    local unseatedTargets = {}
                    local benchStatus = {}

                    for _, p in ipairs(allTargets) do
                        local thum = p.Character.Humanoid
                        local troot = p.Character.HumanoidRootPart
                        local seated = false
                        if thum.Sit and thum.SeatPart then
                            local propModel = thum.SeatPart:FindFirstAncestor(myPropName)
                            if propModel then
                                seated = true
                                for _, pData in ipairs(currentProps) do
                                    if pData.model == propModel then
                                        benchStatus[pData.scf] = p
                                        break
                                    end
                                end
                            end
                        end

                        if not seated then
                            table.insert(unseatedTargets, p)
                        end
                    end

                    
                    for _, pData in ipairs(currentProps) do
                        if not adminRunning then break end
                        local scf = pData.scf
                        local seatedPlayer = benchStatus[scf]

                        if seatedPlayer then
                            if mode == "Kill" then
                                pcall(function() scf:InvokeServer(CFrame.new(0, workspace.FallenPartsDestroyHeight + 2, 0)) end)
                            elseif mode == "Freeze" then
                                pcall(function() scf:InvokeServer(CFrame.new(21.03, 63.05, -479.78)) end)
                            elseif mode == "Void" or mode == "Exile" then
                                pcall(function() scf:InvokeServer(CFrame.new(0, workspace.FallenPartsDestroyHeight + 5, 0)) end)
                            elseif mode == "VoidGlitch" then
                                pcall(function() scf:InvokeServer(CFrame.new(869658880, 1528186880, 529643328)) end)
                            elseif mode == "Jail" then
                                pcall(function() scf:InvokeServer(CFrame.new(-582, 10, 57)) end)
                            elseif mode == "JailV2" then
                                pcall(function() scf:InvokeServer(CFrame.new(498.80, -18.21, 225.37)) end)
                            elseif mode == "Cliff" then
                                pcall(function() scf:InvokeServer(CFrame.new(-1796.89, -30.46, 106.37)) end)
                            elseif mode == "Bring" then
                                if destRoot then
                                    pcall(function() scf:InvokeServer(destRoot.CFrame * CFrame.new(0, 0, -3.5)) end)
                                end
                            elseif mode == "Fly" or mode == "Noclip" then
                                local pChar = seatedPlayer.Character
                                local pRoot = pChar and pChar:FindFirstChild("HumanoidRootPart")
                                local pHum = pChar and pChar:FindFirstChildOfClass("Humanoid")
                                if pRoot and pHum then
                                    local nowA = tick()
                                    local dtA = math.clamp(nowA - (adminFlyLastTick or nowA), 0.01, 0.1)
                                    adminFlyLastTick = nowA

                                    local curPos = adminFlyPositions[scf] or pRoot.Position
                                    local curHeading = adminFlyHeadings[scf] or pRoot.Orientation.Y

                                    local rawMove = pHum.MoveDirection
                                    local moveHoriz = Vector3.new(rawMove.X, 0, rawMove.Z)
                                    if moveHoriz.Magnitude > 0.05 then
                                        local moveDir = moveHoriz.Unit
                                        curPos = curPos + (moveDir * (65.0 * dtA))
                                        curHeading = math.deg(math.atan2(-moveDir.X, -moveDir.Z))
                                    end

                                    local bp = seatedPlayer:FindFirstChildOfClass("Backpack")
                                    local pTools = {}
                                    if pChar then
                                        for _, it in ipairs(pChar:GetChildren()) do
                                            if it:IsA("Tool") then table.insert(pTools, it) end
                                        end
                                    end
                                    if bp then
                                        for _, it in ipairs(bp:GetChildren()) do
                                            if it:IsA("Tool") then table.insert(pTools, it) end
                                        end
                                    end

                                    if not adminFlySlot1Tools[scf] and pTools[1] then adminFlySlot1Tools[scf] = pTools[1] end
                                    if not adminFlySlot2Tools[scf] and pTools[2] then adminFlySlot2Tools[scf] = pTools[2] end

                                    local eqTool = pChar and pChar:FindFirstChildOfClass("Tool")
                                    local vertA = 0
                                    if eqTool then
                                        if adminFlySlot1Tools[scf] and eqTool == adminFlySlot1Tools[scf] then
                                            vertA = 28.0
                                        elseif adminFlySlot2Tools[scf] and eqTool == adminFlySlot2Tools[scf] then
                                            vertA = -28.0
                                        end
                                    end

                                    curPos = curPos + Vector3.new(0, vertA * dtA, 0)

                                    adminFlyPositions[scf] = curPos
                                    adminFlyHeadings[scf] = curHeading

                                    local targetCF = CFrame.new(curPos) * CFrame.Angles(0, math.rad(curHeading + 180), 0)
                                    pcall(function() scf:InvokeServer(targetCF) end)
                                end
                            end
                        else
                            if #unseatedTargets > 0 then
                                local target = table.remove(unseatedTargets, 1)
                                local troot = target.Character and target.Character:FindFirstChild("HumanoidRootPart")
                                local thum = target.Character and target.Character:FindFirstChild("Humanoid")
                                if troot and thum then
                                    task.spawn(function()
                                        local heightOffset = thum.HipHeight and (thum.HipHeight * 0.5) or 1.0
                                        local nowT = tick()
                                        local vEntry = playerVelocities[target]
                                        if not vEntry then
                                            vEntry = { lastPos = troot.Position, lastTime = nowT, vel = Vector3.new(0,0,0) }
                                            playerVelocities[target] = vEntry
                                        else
                                            local dt = nowT - vEntry.lastTime
                                            if dt >= 0.03 then
                                                local curVel = (troot.Position - vEntry.lastPos) / dt
                                                vEntry.vel = vEntry.vel:Lerp(curVel, 0.6)
                                                vEntry.lastPos = troot.Position
                                                vEntry.lastTime = nowT
                                            end
                                        end
                                        local horizontalVel = Vector3.new(vEntry.vel.X, 0, vEntry.vel.Z)
                                        local speed = horizontalVel.Magnitude
                                        local predictionTime = math.clamp(speed * 0.005, 0, 0.15)
                                        local placePos = troot.Position + (horizontalVel * predictionTime) - Vector3.new(0, heightOffset, 0)
                                        pcall(function() scf:InvokeServer(CFrame.new(placePos)) end)
                                    end)
                                end
                            end
                        end
                    end
                    end
                    end

                    task.wait(0.08)
                end
            end)
        end

        AdminTab:AddSection({"> Admin Prop Selection"})

        AdminTab:AddDropdown({
            Name = "Admin Action Prop",
            Description = "Choose prop used for admin actions (Bleachers, BeanBag, etc.)",
            Options = {"Bleachers", "Toilet", "BeanBag", "Silver Throne", "Futuristic Large Bed"},
            Default = "Bleachers",
            Callback = function(value)
                if value == "Toilet" then
                    _G.AdminMassProp = "FurnitureToilet"
                    _G.AdminMassCategory = "Furniture"
                elseif value == "BeanBag" then
                    _G.AdminMassProp = "FurnitureBeanBag"
                    _G.AdminMassCategory = "Furniture"
                elseif value == "Bleachers" then
                    _G.AdminMassProp = "FurnitureBleachers"
                    _G.AdminMassCategory = "Furniture"
                elseif value == "Silver Throne" then
                    _G.AdminMassProp = "FurnitureSilverThrone"
                    _G.AdminMassCategory = "Furniture"
                elseif value == "Futuristic Large Bed" then
                    _G.AdminMassProp = "FuturisticLargeBed"
                    _G.AdminMassCategory = "Furniture"
                end
                safeAdminNotify("Admin Prop", "Admin prop changed to: " .. value, 2)
            end
        })

        AdminTab:AddSection({"> Mass Actions"})

        local adminToggles = {}
        local function makeAdminToggle(name, desc, mode)
            local tg = AdminTab:AddToggle({
                Name = name,
                Description = desc,
                Default = false
            })
            tg:Callback(function(value)
                if not _G.BTR_UI_READY then return end
                if value then
                    for otherMode, otherTg in pairs(adminToggles) do
                        if otherMode ~= mode then
                            pcall(function() otherTg:Set(false) end)
                        end
                    end
                    
                    if BringSystem and BringSystem.Active then BringSystem.stop() end
                    
                    if adminRunning then adminStopAll() end
                    task.wait(0.2)
                    runAdmin(mode, tg)
                else
                    adminStopAll(tg)
                end
            end)
            adminToggles[mode] = tg
            return tg
        end

        
        -- [LOCKED: WORK PERFECTLY] ADMIN MASS ACTIONS (PULL ALL / KILL / FREEZE / VOID)
        
        
        makeAdminToggle("Pull All", "Pulls every player directly to you with bleachers", "Bring")
        makeAdminToggle("Kill All", "Kill every player with bleachers", "Kill")
        makeAdminToggle("Freeze All", "Freeze every player in place", "Freeze")
        makeAdminToggle("Void All", "Exile every player into the void", "Void")
        makeAdminToggle("Void Glitch All", "Exile every player to the glitch zone where everything vanishes", "VoidGlitch")
        makeAdminToggle("Jail All", "Lock every player in the jail cell with beanbags", "Jail")

        
        
        
        
        
        
        
        
        
        
        local bringAllToggle = AdminTab:AddToggle({
            Name = "Bring All",
            Description = "Brings every player to you",
            Default = false,
            Callback = function(val)
                if not _G.BTR_UI_READY then return end
                isBringAllActive = val
                if val then
                    
                    for otherMode, otherTg in pairs(adminToggles) do
                        pcall(function() otherTg:Set(false) end)
                    end
                    if adminRunning then adminStopAll() end
                    if BringSystem and BringSystem.Active then pcall(function() BringSystem.stop() end) end
                    task.wait(0.2)
                    
                    runAdmin("Bring", bringAllToggle, "__BringAll10s__")
                    
                    
                    task.spawn(function()
                        while isBringAllActive and adminRunning do
                            task.wait(0.2)
                        end
                        if isBringAllActive then
                            isBringAllActive = false
                            pcall(function() bringAllToggle:Set(false) end)
                        end
                    end)
                else
                    isBringAllActive = false
                    adminStopAll(bringAllToggle)
                end
            end
        })
        
        

        AdminTab:AddSection({"> Whitelist (3 Exceptions)"})

        local function getAdminPlayersList()
            local list = {"---"}
            for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                if p ~= game:GetService("Players").LocalPlayer then
                    table.insert(list, p.Name)
                end
            end
            return list
        end

        local wlDrop1, wlDrop2, wlDrop3

        wlDrop1 = AdminTab:AddDropdown({
            Name = "Whitelist Slot 1",
            Description = "Player immune to all Admin Mass Trolls",
            Options = getAdminPlayersList(),
            Default = "---",
            Callback = function(val)
                adminWhitelist[1] = (val ~= "---") and val or ""
            end
        })

        wlDrop2 = AdminTab:AddDropdown({
            Name = "Whitelist Slot 2",
            Description = "Player immune to all Admin Mass Trolls",
            Options = getAdminPlayersList(),
            Default = "---",
            Callback = function(val)
                adminWhitelist[2] = (val ~= "---") and val or ""
            end
        })

        wlDrop3 = AdminTab:AddDropdown({
            Name = "Whitelist Slot 3",
            Description = "Player immune to all Admin Mass Trolls",
            Options = getAdminPlayersList(),
            Default = "---",
            Callback = function(val)
                adminWhitelist[3] = (val ~= "---") and val or ""
            end
        })

        AdminTab:AddButton({
            Name = "Refresh / Clear Whitelist",
            Description = "Refreshes online player list and resets whitelist slots",
            Callback = function()
                adminWhitelist = { [1] = "", [2] = "", [3] = "" }
                local updatedList = getAdminPlayersList()
                if wlDrop1 and wlDrop1.Set then pcall(function() wlDrop1:Set(updatedList) end) end
                if wlDrop2 and wlDrop2.Set then pcall(function() wlDrop2:Set(updatedList) end) end
                if wlDrop3 and wlDrop3.Set then pcall(function() wlDrop3:Set(updatedList) end) end
                safeAdminNotify("Admin Whitelist", "Whitelist refreshed & cleared!", 2)
            end
        })

        
        AdminTab:AddSection({"> Give (Fake Admin)"})

        local fakeAdminTarget = "---"
        local fakeAdminEnabled = false
        local lastCmdsTime = 0
        local fakeAdminDropdownRef = nil
        local fakeAdminToggleRef = nil
        local isInternalUpdating = false

        local function getFakeAdminList()
            local list = {"---"}
            for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                if p ~= game:GetService("Players").LocalPlayer then
                    table.insert(list, p.Name)
                end
            end
            return list
        end

        local function resolveTargetPlayer(rawName, excludedSender, allowSelf)
            if not rawName or rawName == "" then return nil end
            local Players = game:GetService("Players")
            local lp = Players.LocalPlayer
            local clean = rawName:lower():match("^%s*(.-)%s*$")
            if not clean or clean == "" then return nil end

            if clean == "me" or clean == "myself" then
                if excludedSender then return excludedSender end
                return lp
            end

            local function isExcluded(p)
                if not allowSelf and excludedSender and p == excludedSender then
                    return true
                end
                return false
            end

            local allPlayers = Players:GetPlayers()

            
            for _, p in ipairs(allPlayers) do
                if not isExcluded(p) and p.Name:lower() == clean then
                    return p
                end
            end

            
            for _, p in ipairs(allPlayers) do
                if not isExcluded(p) and p.DisplayName:lower() == clean then
                    return p
                end
            end

            
            for _, p in ipairs(allPlayers) do
                if not isExcluded(p) and p.Name:lower():sub(1, #clean) == clean then
                    return p
                end
            end

            
            for _, p in ipairs(allPlayers) do
                if not isExcluded(p) and p.DisplayName:lower():sub(1, #clean) == clean then
                    return p
                end
            end

            
            for _, p in ipairs(allPlayers) do
                if not isExcluded(p) and p.Name:lower():find(clean, 1, true) then
                    return p
                end
            end

            
            for _, p in ipairs(allPlayers) do
                if not isExcluded(p) and p.DisplayName:lower():find(clean, 1, true) then
                    return p
                end
            end

            return nil
        end

        local function sendSafeChatMessage(msg)
            local TCS = game:GetService("TextChatService")
            if TCS.ChatVersion == Enum.ChatVersion.TextChatService then
                local ch = TCS.TextChannels:FindFirstChild("RBXGeneral")
                if ch then
                    pcall(function() ch:SendAsync(msg) end)
                    pcall(function() ch:DisplaySystemMessage("<font color='#00d4ff'><b>" .. tostring(msg) .. "</b></font>") end)
                end
            else
                local RS = game:GetService("ReplicatedStorage")
                local sayReq = RS:FindFirstChild("DefaultChatSystemChatEvents") and RS.DefaultChatSystemChatEvents:FindFirstChild("SayMessageRequest")
                if sayReq then pcall(function() sayReq:FireServer(msg, "All") end) end
            end
        end

        local function sendAdminCommandsOutput()
            task.spawn(function()
                sendSafeChatMessage("[Admin] Single: :fly [speed], :bring, :kill, :jail, :freeze, :cliff, :exile, :blind, :ne, :to, :tp [p1] [p2]")
                task.wait(1.1)
                sendSafeChatMessage("[Admin] Mass: :bringall, :killall, :freezeall, :voidall, :tp all [player]")
                task.wait(1.1)
                sendSafeChatMessage("[Admin] Loop: :ljail, :lfreeze, :lkill, :lcliff, :lexile, :lbring | Stop: :stop")
            end)
        end

        local function setFakeAdminState(targetPlayer, enabled)
            if enabled and targetPlayer and targetPlayer.Name then
                if fakeAdminTarget == targetPlayer.Name and fakeAdminEnabled then return end
                fakeAdminTarget = targetPlayer.Name
                fakeAdminEnabled = true
                isInternalUpdating = true
                pcall(function()
                    if fakeAdminDropdownRef and fakeAdminDropdownRef.Set then
                        fakeAdminDropdownRef:Set(targetPlayer.Name)
                    end
                    if fakeAdminToggleRef and fakeAdminToggleRef.Set then
                        fakeAdminToggleRef:Set(true)
                    end
                end)
                isInternalUpdating = false
                safeAdminNotify("Fake Admin", (targetPlayer.DisplayName or targetPlayer.Name) .. " (@" .. targetPlayer.Name .. ") granted fake admin!", 3)
            else
                if fakeAdminTarget == "---" and not fakeAdminEnabled then return end
                fakeAdminTarget = "---"
                fakeAdminEnabled = false
                if BringSystem.Active then BringSystem.stop() end
                if adminRunning then adminStopAll() end
                isInternalUpdating = true
                pcall(function()
                    if fakeAdminDropdownRef and fakeAdminDropdownRef.Set then
                        fakeAdminDropdownRef:Set("---")
                    end
                    if fakeAdminToggleRef and fakeAdminToggleRef.Set then
                        fakeAdminToggleRef:Set(false)
                    end
                end)
                isInternalUpdating = false
                safeAdminNotify("Fake Admin", "Fake admin revoked.", 2)
            end
        end

        local function executeFakeAdminAction(action, targetRaw, targetRaw2, senderPlayer, isLoop, extraParam)
            local Players = game:GetService("Players")
            local lp = Players.LocalPlayer

            local target = resolveTargetPlayer(targetRaw, senderPlayer, (action == "To" or action == "TpMulti" or action == "Bring" or action == "Fly" or action == "Noclip"))
            if not target then
                safeAdminNotify("Fake Admin", "Target '" .. tostring(targetRaw) .. "' not found.", 2)
                return
            end

            
            local isHarmful = (action == "Kill" or action == "Exile" or action == "Cliff" or action == "Freeze" or action == "Jail" or action == "JailV2" or action == "Blind" or action == "NoEscape")
            if isHarmful and target == lp then
                safeAdminNotify("Fake Admin", "Cannot use harmful action on script owner.", 2)
                return
            end

            
            if BringSystem.Active then BringSystem.stop() end
            if adminRunning then adminStopAll() end
            if _G.BTR_StopBlind then pcall(_G.BTR_StopBlind) end
            if _G.BTR_StopNoEscape then pcall(_G.BTR_StopNoEscape) end
            task.wait(0.1)

            if isLoop then
                _G.AutoLoopTroll = true
            end

            local autoStopActions = {
                ["Bring"] = 10,
                ["To"] = 10,
                ["TpMulti"] = 10,
                ["Kill"] = 12
            }

            if not isLoop and autoStopActions[action] then
                local actionDuration = autoStopActions[action]
                task.spawn(function()
                    task.wait(actionDuration)
                    if BringSystem.Active and BringSystem.SessionId == currentActionSession then
                        BringSystem.stop()
                        pcall(function()
                            game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps")
                        end)
                    end
                end)
            end

            task.spawn(function()
                if action == "Kill" then
                    BringSystem.bringPlayer(target, "Kill", true, senderPlayer.Name)
                elseif action == "Bring" then
                    BringSystem.bringPlayer(target, "Bring", true, senderPlayer.Name)
                elseif action == "Jail" then
                    BringSystem.bringPlayer(target, "Jail", true, senderPlayer.Name)
                elseif action == "JailV2" then
                    BringSystem.bringPlayer(target, "JailV2", true, senderPlayer.Name)
                elseif action == "Freeze" then
                    BringSystem.bringPlayer(target, "Freeze", true, senderPlayer.Name)
                elseif action == "Exile" then
                    BringSystem.bringPlayer(target, "Exile", true, senderPlayer.Name)
                elseif action == "Cliff" then
                    BringSystem.bringPlayer(target, "Cliff", true, senderPlayer.Name)
                elseif action == "Fly" or action == "Noclip" then
                    BringSystem.bringPlayer(target, "Fly", true, senderPlayer.Name, extraParam)
                elseif action == "Blind" then
                    if _G.BTR_StartBlind then
                        _G.BTR_StartBlind(target.Name)
                    end
                elseif action == "NoEscape" then
                    if _G.BTR_StartNoEscape then
                        _G.BTR_StartNoEscape(target)
                    end
                elseif action == "To" then
                    if senderPlayer and senderPlayer.Character then
                        BringSystem.bringPlayer(senderPlayer, "Bring", true, target.Name)
                    end
                elseif action == "TpMulti" then
                    local p1IsMe = (targetRaw:lower() == "me" or targetRaw:lower() == "myself")
                    local p2IsMe = (targetRaw2 and (targetRaw2:lower() == "me" or targetRaw2:lower() == "myself"))

                    if p1IsMe then
                        local dest = resolveTargetPlayer(targetRaw2, senderPlayer, true)
                        if dest and senderPlayer and senderPlayer.Character then
                            BringSystem.bringPlayer(senderPlayer, "Bring", true, dest.Name)
                        end
                    elseif p2IsMe then
                        BringSystem.bringPlayer(target, "Bring", true, senderPlayer.Name)
                    else
                        local target2 = resolveTargetPlayer(targetRaw2, senderPlayer, true)
                        if target2 then
                            BringSystem.bringPlayer(target, "Bring", true, target2.Name)
                        end
                    end
                end
            end)
        end

        local processedMessages = {}

        local function processIncomingChat(sender, rawText)
            if not sender or not rawText then return end
            local text = tostring(rawText):match("^%s*(.-)%s*$")
            if not text or text == "" then return end

            local now = os.clock()
            local msgKey = sender.Name .. ":" .. text
            if processedMessages[msgKey] and (now - processedMessages[msgKey] < 0.3) then
                return
            end
            processedMessages[msgKey] = now

            local Players = game:GetService("Players")
            local lp = Players.LocalPlayer

            
            if sender == lp then
                local adminArg = text:match("^[;:!/%.]?[aA][dD][mM][iI][nN]%s+(.+)$")
                if adminArg then
                    adminArg = adminArg:match("^%s*(.-)%s*$")
                    if adminArg and adminArg ~= "" then
                        local target = resolveTargetPlayer(adminArg, nil, true)
                        if target then
                            setFakeAdminState(target, true)
                        else
                            safeAdminNotify("Fake Admin", "Player '" .. tostring(adminArg) .. "' not found.", 2)
                        end
                    end
                    return
                elseif text:lower():match("^[;:!/%.]?[uU][nN][aA][dD][mM][iI][nN]") or text:lower() == ":unadmin" or text:lower() == "unadmin" then
                    setFakeAdminState(nil, false)
                    return
                elseif text:lower():match("^[;:!/%.]?give%s+") or text:lower():match("^[;:!/%.]?giveall") then
                    if _G.BTR_RunVIPAdminCommand then
                        _G.BTR_RunVIPAdminCommand(text)
                    end
                    return
                else
                    local lowerText = text:lower():match("^%s*(.-)%s*$")
                    if lowerText == ":cmds" or lowerText == "cmds" or lowerText == ":help" or lowerText == "help" or lowerText == ":commands" or lowerText == "commands" then
                        sendAdminCommandsOutput()
                        safeAdminNotify("Admin Commands", "Commands displayed in chat.", 2)
                        return
                    end
                end
            end

            
            if not fakeAdminEnabled or not fakeAdminTarget or fakeAdminTarget == "---" then return end
            if sender.Name:lower() ~= fakeAdminTarget:lower() then return end

            
            local cmd, arg = text:match("^[;:!/%.]?%s*([%w_]+)%s*(.*)$")
            if not cmd then return end
            cmd = cmd:lower()
            arg = (arg and arg:match("^%s*(.-)%s*$")) or ""
            local lowerArg = arg:lower()

            
            local isLoop = false
            local loopMap = {
                ["lfreeze"] = "freeze",
                ["ljail"] = "jail",
                ["ljailv2"] = "jailv2",
                ["lkill"] = "kill",
                ["lcliff"] = "cliff",
                ["lexile"] = "exile",
                ["lvoid"] = "void",
                ["lnoclip"] = "noclip",
                ["lfly"] = "fly",
                ["lbring"] = "bring",
                ["lpull"] = "pull"
            }
            if loopMap[cmd] then
                isLoop = true
                cmd = loopMap[cmd]
            end

            
            if cmd == "cmds" or cmd == "help" or cmd == "commands" then
                if now - lastCmdsTime >= 4 then
                    lastCmdsTime = now
                    sendAdminCommandsOutput()
                    safeAdminNotify("Admin Commands", sender.DisplayName .. " requested commands list.", 2)
                end
                return
            end

            
            if cmd == "stop" or cmd == "cancel" or cmd == "unbring" or cmd == "unblind" or cmd == "unfreeze" or cmd == "unjail" or cmd == "unne" or cmd == "unnoescape" or cmd == "unfly" or cmd == "unnoclip" or cmd == "unvoid" or cmd == "unexile" or cmd == "uncliff" then
                _G.AutoLoopTroll = false
                if BringSystem.Active then BringSystem.stop() end
                if adminRunning then adminStopAll() end
                if _G.BTR_StopBlind then pcall(_G.BTR_StopBlind) end
                if _G.BTR_StopNoEscape then pcall(_G.BTR_StopNoEscape) end
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps")
                end)
                safeAdminNotify("Fake Admin", "Stopped all actions by " .. sender.DisplayName, 2)
                return
            end

            
            local isAll = (lowerArg == "all" or lowerArg == "everyone" or lowerArg == "all players" or lowerArg == "everybody" or lowerArg == "allplayers" or lowerArg == "others")

            
            if cmd == "tpall" or ((cmd == "tp" or cmd == "teleport") and lowerArg:match("^all%s+")) then
                local destRaw = lowerArg:match("^all%s+(.+)$") or arg
                if destRaw and destRaw ~= "" then
                    local dest = resolveTargetPlayer(destRaw, sender, true)
                    if dest then
                        task.spawn(function()
                            if BringSystem and BringSystem.Active then BringSystem.stop() end
                            if adminRunning then adminStopAll() end
                            task.wait(0.2)
                            runAdmin("Bring", nil, dest.Name)
                        end)
                    else
                        safeAdminNotify("Fake Admin", "Destination '" .. tostring(destRaw) .. "' not found.", 2)
                    end
                end
                return
            end

            
            if cmd == "bringall" or (cmd == "bring" and isAll) or cmd == "pullall" or (cmd == "pull" and isAll) then
                task.spawn(function()
                    if BringSystem and BringSystem.Active then BringSystem.stop() end
                    if adminRunning then adminStopAll() end
                    task.wait(0.2)
                    runAdmin("Bring", nil, sender.Name)
                end)
                return
            elseif cmd == "killall" or (cmd == "kill" and isAll) then
                task.spawn(function()
                    if BringSystem and BringSystem.Active then BringSystem.stop() end
                    if adminRunning then adminStopAll() end
                    task.wait(0.2)
                    runAdmin("Kill", nil, sender.Name)
                end)
                return
            elseif cmd == "freezeall" or (cmd == "freeze" and isAll) then
                task.spawn(function()
                    if BringSystem and BringSystem.Active then BringSystem.stop() end
                    if adminRunning then adminStopAll() end
                    task.wait(0.2)
                    runAdmin("Freeze", nil, sender.Name)
                end)
                return
            elseif cmd == "voidall" or cmd == "exileall" or (cmd == "void" and isAll) or (cmd == "exile" and isAll) then
                task.spawn(function()
                    if BringSystem and BringSystem.Active then BringSystem.stop() end
                    if adminRunning then adminStopAll() end
                    task.wait(0.2)
                    runAdmin("Void", nil, sender.Name)
                end)
                return
            elseif cmd == "jailall" or (cmd == "jail" and isAll) then
                task.spawn(function()
                    if BringSystem and BringSystem.Active then BringSystem.stop() end
                    if adminRunning then adminStopAll() end
                    task.wait(0.2)
                    runAdmin("Jail", nil, sender.Name)
                end)
                return
            elseif cmd == "cliffall" or (cmd == "cliff" and isAll) then
                task.spawn(function()
                    if BringSystem and BringSystem.Active then BringSystem.stop() end
                    if adminRunning then adminStopAll() end
                    task.wait(0.2)
                    runAdmin("Cliff", nil, sender.Name)
                end)
                return
            end

            
            if cmd == "fly" or cmd == "noclip" then
                local flyTargetRaw = sender.Name
                local flySpeedParam = nil

                if arg and arg ~= "" then
                    local p1, p2 = arg:match("^([^%s]+)%s*(.*)$")
                    p1 = p1 or ""
                    p2 = (p2 and p2:match("^%s*(.-)%s*$")) or ""

                    if tonumber(p1) then
                        
                        flyTargetRaw = sender.Name
                        flySpeedParam = tonumber(p1)
                    elseif p1:lower() == "me" or p1:lower() == "myself" then
                        
                        flyTargetRaw = sender.Name
                        flySpeedParam = tonumber(p2)
                    else
                        
                        flyTargetRaw = p1
                        flySpeedParam = tonumber(p2)
                    end
                end

                local target = resolveTargetPlayer(flyTargetRaw, sender, true)
                if target and BringSystem.Active and BringSystem.CurrentAction == "Fly" and BringSystem.Target == target then
                    local newSpeed = (flySpeedParam and flySpeedParam > 0) and flySpeedParam or 65.0
                    BringSystem.FlySpeed = newSpeed
                    safeAdminNotify("Fake Admin", "Fly speed updated to " .. tostring(newSpeed) .. " for " .. target.DisplayName, 2)
                    return
                end

                executeFakeAdminAction("Fly", flyTargetRaw, nil, sender, isLoop, flySpeedParam)
                return
            end

            
            if arg and arg ~= "" then
                if cmd == "kill" then
                    executeFakeAdminAction("Kill", arg, nil, sender, isLoop)
                elseif cmd == "bring" or cmd == "pull" then
                    executeFakeAdminAction("Bring", arg, nil, sender, isLoop)
                elseif cmd == "jail" then
                    executeFakeAdminAction("Jail", arg, nil, sender, isLoop)
                elseif cmd == "jailv2" then
                    executeFakeAdminAction("JailV2", arg, nil, sender, isLoop)
                elseif cmd == "freeze" then
                    executeFakeAdminAction("Freeze", arg, nil, sender, isLoop)
                elseif cmd == "cliff" then
                    executeFakeAdminAction("Cliff", arg, nil, sender, isLoop)
                elseif cmd == "exile" or cmd == "void" then
                    executeFakeAdminAction("Exile", arg, nil, sender, isLoop)
                elseif cmd == "blind" then
                    executeFakeAdminAction("Blind", arg, nil, sender, false)
                elseif cmd == "ne" or cmd == "noescape" then
                    executeFakeAdminAction("NoEscape", arg, nil, sender, false)
                elseif cmd == "to" or cmd == "goto" then
                    executeFakeAdminAction("To", arg, nil, sender, isLoop)
                elseif cmd == "tp" or cmd == "teleport" then
                    local p1, p2 = arg:match("^([^%s]+)%s+(.+)$")
                    if p1 and p2 then
                        executeFakeAdminAction("TpMulti", p1, p2, sender, isLoop)
                    else
                        executeFakeAdminAction("To", arg, nil, sender, isLoop)
                    end
                end
            end
        end

        local function hookChatListeners()
            if _G.BTR_FakeAdminConns then
                for _, c in ipairs(_G.BTR_FakeAdminConns) do
                    pcall(function() c:Disconnect() end)
                end
            end
            _G.BTR_FakeAdminConns = {}

            local TCS = game:GetService("TextChatService")
            local Players = game:GetService("Players")

            
            if TCS and TCS.MessageReceived then
                local conn = TCS.MessageReceived:Connect(function(msg)
                    if not msg then return end
                    local textContent = msg.Text or msg.RawText
                    if not textContent then return end

                    local sender = nil
                    if msg.TextSource then
                        sender = Players:GetPlayerByUserId(msg.TextSource.UserId)
                    end
                    if not sender and msg.TextChannel then
                        
                        for _, p in ipairs(Players:GetPlayers()) do
                            if textContent:find(p.DisplayName, 1, true) or textContent:find(p.Name, 1, true) then
                                sender = p
                                break
                            end
                        end
                    end
                    if sender then
                        processIncomingChat(sender, textContent)
                    end
                end)
                table.insert(_G.BTR_FakeAdminConns, conn)
            end

            
            if TCS and TCS:FindFirstChild("TextChannels") then
                for _, ch in ipairs(TCS.TextChannels:GetChildren()) do
                    if ch:IsA("TextChannel") then
                        local conn = ch.MessageReceived:Connect(function(msg)
                            if not msg then return end
                            local textContent = msg.Text or msg.RawText
                            if not textContent then return end
                            local sender = nil
                            if msg.TextSource then
                                sender = Players:GetPlayerByUserId(msg.TextSource.UserId)
                            end
                            if sender then
                                processIncomingChat(sender, textContent)
                            end
                        end)
                        table.insert(_G.BTR_FakeAdminConns, conn)
                    end
                end
            end

            
            for _, p in ipairs(Players:GetPlayers()) do
                local conn = p.Chatted:Connect(function(msg)
                    processIncomingChat(p, msg)
                end)
                table.insert(_G.BTR_FakeAdminConns, conn)
            end
            local pAddConn = Players.PlayerAdded:Connect(function(p)
                local conn = p.Chatted:Connect(function(msg)
                    processIncomingChat(p, msg)
                end)
                table.insert(_G.BTR_FakeAdminConns, conn)
            end)
            table.insert(_G.BTR_FakeAdminConns, pAddConn)
        end

        
        hookChatListeners()

        fakeAdminDropdownRef = AdminTab:AddDropdown({
            Name = "Give Admin To",
            Description = "Choose a player to grant fake admin powers",
            Options = getFakeAdminList(),
            Default = "---",
            Callback = function(val)
                if isInternalUpdating then return end
                if type(val) ~= "string" or not val then return end
                if val ~= "---" then
                    local target = game:GetService("Players"):FindFirstChild(val) or resolveTargetPlayer(val)
                    if target then
                        setFakeAdminState(target, true)
                    end
                else
                    setFakeAdminState(nil, false)
                end
            end
        })

        AdminTab:AddButton({
            Name = "Refresh Player List",
            Description = "Update online player list",
            Callback = function()
                local updatedList = getFakeAdminList()
                if fakeAdminDropdownRef and fakeAdminDropdownRef.Set then
                    isInternalUpdating = true
                    pcall(function() fakeAdminDropdownRef:Set(updatedList) end)
                    isInternalUpdating = false
                end
                safeAdminNotify("Fake Admin", "Player list refreshed!", 2)
            end
        })

        fakeAdminToggleRef = AdminTab:AddToggle({
            Name = "Enable Fake Admin",
            Description = "Selected player can execute commands in chat",
            Default = false,
            Callback = function(val)
                if not _G.BTR_UI_READY then return end
                if isInternalUpdating then return end
                if val then
                    if fakeAdminTarget == "---" or not fakeAdminTarget then
                        safeAdminNotify("Fake Admin", "Select a player first!", 3)
                        task.wait(0.05)
                        if fakeAdminToggleRef and fakeAdminToggleRef.Set then
                            isInternalUpdating = true
                            pcall(function() fakeAdminToggleRef:Set(false) end)
                            isInternalUpdating = false
                        end
                    else
                        local target = game:GetService("Players"):FindFirstChild(fakeAdminTarget) or resolveTargetPlayer(fakeAdminTarget)
                        if target then
                            setFakeAdminState(target, true)
                        else
                            fakeAdminEnabled = true
                        end
                    end
                else
                    setFakeAdminState(nil, false)
                end
            end
        })

        AdminTab:AddButton({
            Name = "Show Admin Commands",
            Description = "Display popup with all available commands",
            Callback = function()
                local lp = game:GetService("Players").LocalPlayer
                local pg = lp and lp:FindFirstChild("PlayerGui")
                if not pg then return end

                local old = pg:FindFirstChild("FakeAdminCmdGui")
                if old then pcall(function() old:Destroy() end) end

                local sg = Instance.new("ScreenGui")
                sg.Name = "FakeAdminCmdGui"
                sg.ResetOnSpawn = false
                sg.DisplayOrder = 9999
                sg.Parent = pg

                local frame = Instance.new("Frame")
                frame.Size = UDim2.new(0, 480, 0, 560)
                frame.Position = UDim2.new(0.5, -240, 0.5, -280)
                frame.BackgroundColor3 = Color3.fromRGB(18, 18, 24)
                frame.BorderSizePixel = 0
                frame.Parent = sg

                local corner = Instance.new("UICorner")
                corner.CornerRadius = UDim.new(0, 10)
                corner.Parent = frame

                local stroke = Instance.new("UIStroke")
                stroke.Color = Color3.fromRGB(60, 60, 80)
                stroke.Thickness = 1.5
                stroke.Parent = frame

                local title = Instance.new("TextLabel")
                title.Size = UDim2.new(1, 0, 0, 40)
                title.BackgroundTransparency = 1
                title.Text = "Admin Commands List"
                title.TextColor3 = Color3.fromRGB(255, 255, 255)
                title.TextSize = 16
                title.Font = Enum.Font.GothamBold
                title.Parent = frame

                local cmds = {
                    
                    {":bring [player]", "Brings target to you / admin"},
                    {":kill [player]", "Drops target into the void"},
                    {":jail [player]", "Traps target in Prison V1"},
                    {":jailv2 [player]", "Traps target in Prison V2"},
                    {":freeze [player]", "Freezes target in place"},
                    {":cliff [player]", "Traps target at cliff edge"},
                    {":exile [player]", "Exiles target into the void"},
                    {":noclip [player]", "Noclips target through floor/walls"},
                    {":blind [player]", "Blinds target screen (white cover)"},
                    {":ne [player]", "Traps target in No Escape couch grid"},
                    {":to [player]", "Teleports admin to target"},
                    {":tp [p1] [p2]", "Teleports player 1 to player 2"},

                    
                    {":lkill [player]", "Loop: kills target on every respawn"},
                    {":lfreeze [player]", "Loop: freezes target permanently"},
                    {":ljail [player]", "Loop: locks target in Prison V1 forever"},
                    {":ljailv2 [player]", "Loop: locks target in Prison V2 forever"},
                    {":lcliff [player]", "Loop: traps target at cliff edge forever"},
                    {":lexile [player]", "Loop: keeps target exiled into the void"},
                    {":lnoclip [player]", "Loop: keeps target falling infinitely"},
                    {":lbring [player]", "Loop: keeps target glued to you/admin"},

                    
                    {":bringall", "Brings all players to you / admin"},
                    {":killall", "Kills all players on the server"},
                    {":freezeall", "Freezes all players on the server"},
                    {":voidall", "Exiles all players into the void"},
                    {":noclipall", "Noclips all players on the server"},
                    {":tp all [player]", "Teleports all players to target player"},

                    
                    {":give [p] [item]", "Gives VIP item (Snowboard/Skis/MoneyGun)"},
                    {":giveall [item]", "Gives VIP item to all server players"},
                    {":stop", "Stops current action immediately"},
                    {":cmds", "Shows commands list in chat"}
                }

                local scroll = Instance.new("ScrollingFrame")
                scroll.Size = UDim2.new(1, -20, 1, -95)
                scroll.Position = UDim2.new(0, 10, 0, 45)
                scroll.BackgroundTransparency = 1
                scroll.BorderSizePixel = 0
                scroll.CanvasSize = UDim2.new(0, 0, 0, #cmds * 26 + 15)
                scroll.ScrollBarThickness = 4
                scroll.Parent = frame

                for idx, entry in ipairs(cmds) do
                    local row = Instance.new("Frame")
                    row.Size = UDim2.new(1, 0, 0, 24)
                    row.Position = UDim2.new(0, 0, 0, (idx - 1) * 26)
                    row.BackgroundTransparency = 1
                    row.Parent = scroll

                    local cmdLbl = Instance.new("TextLabel")
                    cmdLbl.Size = UDim2.new(0, 160, 1, 0)
                    cmdLbl.BackgroundTransparency = 1
                    cmdLbl.Text = entry[1]

                    
                    local cmdText = entry[1]:lower()
                    if cmdText:sub(1, 2) == ":l" then
                        cmdLbl.TextColor3 = Color3.fromRGB(120, 255, 170) 
                    elseif cmdText:find("all") then
                        cmdLbl.TextColor3 = Color3.fromRGB(255, 190, 90) 
                    elseif cmdText == ":stop" or cmdText == ":cmds" or cmdText:find("give") then
                        cmdLbl.TextColor3 = Color3.fromRGB(255, 120, 140) 
                    else
                        cmdLbl.TextColor3 = Color3.fromRGB(110, 190, 255) 
                    end

                    cmdLbl.TextSize = 13
                    cmdLbl.Font = Enum.Font.Code
                    cmdLbl.TextXAlignment = Enum.TextXAlignment.Left
                    cmdLbl.Parent = row

                    local descLbl = Instance.new("TextLabel")
                    descLbl.Size = UDim2.new(1, -165, 1, 0)
                    descLbl.Position = UDim2.new(0, 165, 0, 0)
                    descLbl.BackgroundTransparency = 1
                    descLbl.Text = entry[2]
                    descLbl.TextColor3 = Color3.fromRGB(200, 200, 200)
                    descLbl.TextSize = 12
                    descLbl.Font = Enum.Font.Gotham
                    descLbl.TextXAlignment = Enum.TextXAlignment.Left
                    descLbl.Parent = row
                end

                local closeBtn = Instance.new("TextButton")
                closeBtn.Size = UDim2.new(1, -20, 0, 34)
                closeBtn.Position = UDim2.new(0, 10, 1, -42)
                closeBtn.BackgroundColor3 = Color3.fromRGB(35, 35, 45)
        closeBtn.Text = "X"
                closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
                closeBtn.TextSize = 14
                closeBtn.Font = Enum.Font.GothamBold
                closeBtn.Parent = frame

                local btnCorner = Instance.new("UICorner")
                btnCorner.CornerRadius = UDim.new(0, 8)
                btnCorner.Parent = closeBtn

                closeBtn.MouseButton1Click:Connect(function()
                    pcall(function() sg:Destroy() end)
                end)
            end
        })

        
        do
            AdminTab:AddSection({"> Admin Mecha Titan"})

            local titanConfig = {
                size = 11.0,
                heightOffset = 0.0,
                posZ = 0.0,
                posX = 0.0,
                rotY = 0.0,
                walkSpeed = 44.0,
                turnSpeed = 4.8,
                animSpeed = 0.90,
                torsoRotX = 90.0,
                torsoRotY = 0.0,
                torsoRotZ = 0.0,
                torsoOffsetX = 1.0,
                torsoOffsetY = -2.0,
                torsoOffsetZ = -3.0,
                armWidth = 1.03,
                armSpacing = 5.45,
                legWidth = 0.74,
                legOffsetY = 0.0,
                legSpacing = 0.0,
                pelvisOffsetX = -2.00,
                pelvisOffsetY = -3.50,
                pelvisOffsetZ = -1.00,
                pelvisRotX = 0.00,
                pelvisRotY = 85.00,
                pelvisRotZ = 0.00,
            }

            local ANIMS = {
                Idle = "rbxassetid://82598218906950",
                IdleAsset = "rbxassetid://94553259897103",
                Walk = "rbxassetid://82598234841035",
                WalkAsset = "rbxassetid://73356079089912",
                Run = "rbxassetid://82598234841035",
                RunAsset = "rbxassetid://73356079089912",
            }

            local EMOTES = {
                [1] = { id = "72947568152049",  name = "Cute Sit" },
                [2] = { id = "117033010486869", name = "Dia Delicia Dance" },
                [3] = { id = "133139819277127", name = "Dance 3" },
                [4] = { id = "94775564131239",  name = "Best Dance" },
                [5] = { id = "139666592436142", name = "Low Cortisol Dance" },
            }

            local titanActive = false
            local isWaitingForPilot = false
            local pilotBaitProp = nil
            local currentGigOrigin = nil
            local titanWorldPos = Vector3.zero
            local titanHeading = 0
            local titanPartsList = {}
            local titanLoop = nil
            local titanPhysicsLoop = nil

            local ghostRig = nil
            local ghostIdleTrack = nil
            local ghostRunTrack = nil
            local currentEmoteIndex = nil
            local currentEmoteTrack = nil
            local cachedEmoteTracks = {}
            local titanIsMoving = false

            local selectedPilotName = "---"
            local currentPilot = nil

            local titanToggleRef = nil
            local targetPilotDropdownRef = nil
            local forceEmoteDropdownRef = nil

            local rep = game:GetService("ReplicatedStorage")
            local uis = game:GetService("UserInputService")
            local rs = game:GetService("RunService")
            local plrs = game:GetService("Players")
            local lp = plrs.LocalPlayer

            local function notify(title, text, dur)
                safeAdminNotify(title, text, dur or 3)
            end

            local function getPilotPlayersList()
                local list = {"---"}
                for _, p in ipairs(plrs:GetPlayers()) do
                    if p ~= lp then
                        table.insert(list, p.Name)
                    end
                end
                return list
            end

            local function stopTitanEmote()
                if currentEmoteTrack then
                    pcall(function() currentEmoteTrack:Stop(0.15) end)
                    currentEmoteTrack = nil
                end
                currentEmoteIndex = nil
                if titanActive and ghostIdleTrack and not ghostIdleTrack.IsPlaying then
                    pcall(function() ghostIdleTrack:Play(0.2) end)
                end
                if forceEmoteDropdownRef and forceEmoteDropdownRef.Set then
                    pcall(function() forceEmoteDropdownRef:Set("Stop Emote") end)
                end
                notify("Titan Emote", "Emote stopped.", 2)
            end

            local function extractAnimationFromAsset(assetId)
                local objs = nil
                pcall(function()
                    if typeof(getobjects) == "function" then
                        objs = getobjects("rbxassetid://" .. tostring(assetId))
                    end
                end)
                if not objs or #objs == 0 then
                    pcall(function()
                        objs = game:GetObjects("rbxassetid://" .. tostring(assetId))
                    end)
                end
                if not objs or #objs == 0 then
                    pcall(function()
                        local ins = game:GetService("InsertService"):LoadLocalAsset("rbxassetid://" .. tostring(assetId))
                        if ins then
                            objs = typeof(ins) == "table" and ins or { ins }
                        end
                    end)
                end
                if not objs or #objs == 0 then return nil, nil end

                for _, item in ipairs(objs) do
                    local r15 = item:FindFirstChild("R15ArtistIntent") or item:FindFirstChild("R15")
                    if r15 then
                        local a = r15:IsA("Animation") and r15 or r15:FindFirstChildWhichIsA("Animation", true)
                        if a then return a, a.AnimationId end
                    end
                end

                for _, item in ipairs(objs) do
                    if item:IsA("Animation") then return item, item.AnimationId end
                end

                for _, item in ipairs(objs) do
                    local a = item:FindFirstChildWhichIsA("Animation", true)
                    if a then return a, a.AnimationId end
                end

                for _, item in ipairs(objs) do
                    local ks = item:IsA("KeyframeSequence") and item or item:FindFirstChildWhichIsA("KeyframeSequence", true)
                    if ks then
                        local ksp = game:GetService("KeyframeSequenceProvider")
                        local clipUrl = nil
                        pcall(function() clipUrl = ksp:RegisterKeyframeSequence(ks) end)
                        if clipUrl and clipUrl ~= "" then
                            local anim = Instance.new("Animation")
                            anim.AnimationId = clipUrl
                            return anim, clipUrl
                        end
                    end
                end

                return nil, nil
            end

            local function loadEmoteTrack(index)
                local emoteData = EMOTES[index]
                if not emoteData or not ghostRig then return nil end
                if cachedEmoteTracks[index] then return cachedEmoteTracks[index] end

                local hum = ghostRig:FindFirstChildOfClass("Humanoid")
                local animator = hum and (hum:FindFirstChildOfClass("Animator") or hum:FindFirstChildWhichIsA("Animator"))
                if not hum then return nil end
                if not animator then animator = Instance.new("Animator", hum) end

                local track = nil
                local animObj, animId = extractAnimationFromAsset(emoteData.id)

                if animId and animId ~= "" then
                    pcall(function()
                        local pureAnim = Instance.new("Animation")
                        pureAnim.AnimationId = animId
                        pureAnim.Parent = ghostRig
                        track = animator:LoadAnimation(pureAnim)
                    end)
                end

                if not track and animObj then
                    pcall(function()
                        animObj.Parent = ghostRig
                        track = animator:LoadAnimation(animObj)
                    end)
                end

                if not track then
                    pcall(function()
                        local directAnim = Instance.new("Animation")
                        directAnim.AnimationId = "rbxassetid://" .. tostring(emoteData.id)
                        directAnim.Parent = ghostRig
                        track = animator:LoadAnimation(directAnim)
                    end)
                end

                if track then
                    track.Priority = Enum.AnimationPriority.Action4
                    track.Looped = true
                    pcall(function()
                        track:GetPropertyChangedSignal("Looped"):Connect(function() track.Looped = true end)
                    end)
                    track.Stopped:Connect(function()
                        if titanActive and currentEmoteTrack == track and not titanIsMoving then
                            task.defer(function()
                                if titanActive and currentEmoteTrack == track and not titanIsMoving then
                                    track:Play(0.1)
                                end
                            end)
                        end
                    end)
                    cachedEmoteTracks[index] = track
                end

                return track
            end

            local function playTitanEmote(index)
                if not titanActive or not ghostRig then
                    notify("Titan Controller", "Titan must be active to play emotes!", 2)
                    return
                end
                local emoteData = EMOTES[index]
                if not emoteData then return end

                if currentEmoteTrack and currentEmoteTrack.IsPlaying then
                    pcall(function() currentEmoteTrack:Stop(0.1) end)
                end

                local track = loadEmoteTrack(index)
                if track then
                    currentEmoteIndex = index
                    currentEmoteTrack = track
                    if ghostIdleTrack and ghostIdleTrack.IsPlaying then pcall(function() ghostIdleTrack:Stop(0.15) end) end
                    if ghostRunTrack and ghostRunTrack.IsPlaying then pcall(function() ghostRunTrack:Stop(0.15) end) end
                    track:Play(0.15)
                    notify("Titan Emote", string.format("Emote %d (%s) active!", index, emoteData.name), 3)
                else
                    notify("Titan Emote", string.format("Failed to load Emote %d", index), 2)
                end
            end

            local function cleanGhostRig()
                if currentEmoteTrack then pcall(function() currentEmoteTrack:Stop() end) currentEmoteTrack = nil end
                currentEmoteIndex = nil
                cachedEmoteTracks = {}
                if ghostIdleTrack then pcall(function() ghostIdleTrack:Stop() end) ghostIdleTrack = nil end
                if ghostRunTrack  then pcall(function() ghostRunTrack:Stop() end) ghostRunTrack = nil end
                if ghostRig then
                    pcall(function() ghostRig:Destroy() end)
                    ghostRig = nil
                end
            end

            local function setupGhostRig()
                cleanGhostRig()
                local realChar = lp.Character or lp.CharacterAdded:Wait()
                realChar.Archivable = true
                local clone = realChar:Clone()
                realChar.Archivable = false

                clone.Name = "GhostRig_AdminTitan"
                for _, part in ipairs(clone:GetDescendants()) do
                    if part:IsA("BasePart") then
                        part.Transparency = 1
                        part.CanCollide = false
                        part.CanTouch = false
                        part.CanQuery = false
                        part.CastShadow = false
                        part.Massless = true
                    elseif part:IsA("Decal") then
                        part.Transparency = 1
                    end
                end

                local root = clone:FindFirstChild("HumanoidRootPart")
                if root then
                    root.Anchored = true
                    root.CFrame = CFrame.new(0, 5000, 0)
                end

                clone.Parent = workspace
                ghostRig = clone

                local hum = clone:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum.DisplayDistanceType = Enum.HumanoidDisplayDistanceType.None
                    local animator = hum:FindFirstChildOfClass("Animator") or Instance.new("Animator", hum)

                    local function loadGhostAnim(primaryId, containerId, animType)
                        local animTrack = nil

                        if containerId then
                            local obj, clipUrl = extractAnimationFromAsset(containerId)
                            if obj then
                                pcall(function()
                                    obj.Parent = ghostRig
                                    animTrack = animator:LoadAnimation(obj)
                                end)
                            elseif clipUrl then
                                pcall(function()
                                    local a = Instance.new("Animation")
                                    a.AnimationId = clipUrl
                                    a.Parent = ghostRig
                                    animTrack = animator:LoadAnimation(a)
                                end)
                            end
                        end

                        if not animTrack and primaryId then
                            local obj, clipUrl = extractAnimationFromAsset(primaryId)
                            if obj then
                                pcall(function()
                                    obj.Parent = ghostRig
                                    animTrack = animator:LoadAnimation(obj)
                                end)
                            elseif clipUrl then
                                pcall(function()
                                    local a = Instance.new("Animation")
                                    a.AnimationId = clipUrl
                                    a.Parent = ghostRig
                                    animTrack = animator:LoadAnimation(a)
                                end)
                            end
                        end

                        
                        if not animTrack and realChar then
                            local animScript = realChar:FindFirstChild("Animate")
                            if animScript then
                                if animType == "Idle" then
                                    local idleFold = animScript:FindFirstChild("idle")
                                    if idleFold then
                                        for _, ch in ipairs(idleFold:GetChildren()) do
                                            if ch:IsA("Animation") and ch.AnimationId ~= "" then
                                                pcall(function() animTrack = animator:LoadAnimation(ch) end)
                                                if animTrack then break end
                                            end
                                        end
                                    end
                                elseif animType == "Run" or animType == "Walk" then
                                    local runFold = animScript:FindFirstChild("run") or animScript:FindFirstChild("walk")
                                    if runFold then
                                        for _, ch in ipairs(runFold:GetChildren()) do
                                            if ch:IsA("Animation") and ch.AnimationId ~= "" then
                                                pcall(function() animTrack = animator:LoadAnimation(ch) end)
                                                if animTrack then break end
                                            end
                                        end
                                    end
                                end
                            end
                        end

                        
                        if not animTrack then
                            pcall(function()
                                local a = Instance.new("Animation")
                                if animType == "Idle" then
                                    a.AnimationId = "rbxassetid://507766666"
                                else
                                    a.AnimationId = "rbxassetid://913376220"
                                end
                                a.Parent = ghostRig
                                animTrack = animator:LoadAnimation(a)
                            end)
                        end

                        if animTrack then
                            animTrack.Looped = true
                        end
                        return animTrack
                    end

                    ghostRunTrack = loadGhostAnim(ANIMS.Run, ANIMS.RunAsset, "Run")
                    ghostIdleTrack = loadGhostAnim(ANIMS.Idle, ANIMS.IdleAsset, "Idle")

                    if ghostIdleTrack then
                        ghostIdleTrack.Priority = Enum.AnimationPriority.Action
                        ghostIdleTrack.Looped = true
                        ghostIdleTrack:Play(0.1)
                    end
                    if ghostRunTrack then
                        ghostRunTrack.Priority = Enum.AnimationPriority.Action4
                        ghostRunTrack.Looped = true
                    end
                end
                return ghostRig
            end

            local function getGhostLimb(rig, limbName)
                if not rig then return nil end
                local p = rig:FindFirstChild(limbName)
                if p then return p end
                local mapping = {
                    ["LeftUpperArm"]  = "Left Arm",
                    ["LeftLowerArm"]  = "Left Arm",
                    ["LeftHand"]      = "Left Arm",
                    ["RightUpperArm"] = "Right Arm",
                    ["RightLowerArm"] = "Right Arm",
                    ["RightHand"]     = "Right Arm",
                    ["LeftUpperLeg"]  = "Left Leg",
                    ["LeftLowerLeg"]  = "Left Leg",
                    ["LeftFoot"]      = "Left Leg",
                    ["RightUpperLeg"] = "Right Leg",
                    ["RightLowerLeg"] = "Right Leg",
                    ["RightFoot"]     = "Right Leg",
                    ["UpperTorso"]    = "Torso",
                    ["LowerTorso"]    = "Torso",
                    ["Head"]          = "Head",
                }
                local fallbackName = mapping[limbName]
                if fallbackName then return rig:FindFirstChild(fallbackName) end
                return nil
            end

            local senders = {}
            local function sendCF(scf, cf, prop, isPriority)
                if not titanActive or not scf then return end
                if prop then
                    pcall(function()
                        if prop:IsA("Model") then prop:PivotTo(cf)
                        elseif prop:IsA("BasePart") then prop.CFrame = cf end
                    end)
                end

                local entry = senders[scf]
                local now = tick()
                if not entry then
                    entry = { lastSent = 0, nextCF = cf, sending = false }
                    senders[scf] = entry
                else
                    entry.nextCF = cf
                end

                local rateLimit = isPriority and 0.015 or 0.035
                if not entry.sending and (now - entry.lastSent >= rateLimit) then
                    entry.sending = true
                    entry.lastSent = now
                    local toSend = entry.nextCF
                    entry.nextCF = nil
                    task.spawn(function()
                        pcall(function() scf:InvokeServer(toSend) end)
                        entry.sending = false
                    end)
                end
            end

            local myPropName = "PropMaker_" .. lp.Name

            
            local function getOrFetchPropMakerTool()
                local backpack = lp:FindFirstChild("Backpack")
                local character = lp.Character
                if not backpack or not character then return nil end
                local tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
                if not tool then
                    local RE = rep:FindFirstChild("RE")
                    local toolRemote = RE and RE:FindFirstChild("1Too1l")
                    if toolRemote then
                        pcall(function() toolRemote:InvokeServer("PickingTools", "PropMaker") end)
                        local start = tick()
                        while tick() - start < 2.0 do
                            tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
                            if tool then break end
                            task.wait(0.05)
                        end
                    end
                end
                if not tool then return nil end
                if tool.Parent ~= character then
                    local hum = character:FindFirstChildOfClass("Humanoid")
                    if hum then
                        hum:EquipTool(tool)
                        local eqStart = tick()
                        while tick() - eqStart < 2.0 do
                            if tool.Parent == character then break end
                            task.wait(0.05)
                        end
                    end
                end
                return tool
            end

            local function spawnSingleProp(propName, categoryName, targetPos, existingProps)
                local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
                local tool = getOrFetchPropMakerTool()
                if not tool then return nil end

                local Telemetry = rep:FindFirstChild("Remotes") and rep.Remotes:FindFirstChild("TelemetryClientInteraction")
                if Telemetry then
                    pcall(function() Telemetry:FireServer("filterClick", {name = categoryName, itemType = "Props"}) end)
                    task.wait(0.05)
                    pcall(function() Telemetry:FireServer("filterClick", {name = propName, itemType = "Props"}) end)
                    task.wait(0.05)
                end

                pcall(function() clearTools:FireServer("RequestingPropName", propName, categoryName) end)
                task.wait(0.3)

                local currentHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
                if currentHum and tool.Parent ~= lp.Character then
                    currentHum:EquipTool(tool)
                    task.wait(0.1)
                end

                local tpm = tool:FindFirstChild("Tool_PropMake")
                local startW = tick()
                while not tpm and tick() - startW < 4.0 do
                    tpm = tool:FindFirstChild("Tool_PropMake")
                    if not tpm then
                        local bp = lp.Backpack:FindFirstChild("PropMaker")
                        if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
                    end
                    if not tpm then task.wait(0.1) end
                end

                if not tpm then return nil end

                pcall(function() tpm:FireServer(workspace.Terrain, targetPos) end)

                local spawned = nil
                local t0 = tick()
                local wc = workspace:FindFirstChild("WorkspaceCom")
                while tick() - t0 < 4.0 do
                    if wc then
                        for _, folder in pairs(wc:GetChildren()) do
                            for _, prop in pairs(folder:GetChildren()) do
                                if (prop.Name == myPropName or prop.Name:find(lp.Name)) and not existingProps[prop] then
                                    spawned = prop
                                    existingProps[prop] = true
                                    break
                                end
                            end
                            if spawned then break end
                        end
                    end
                    if spawned then break end
                    task.wait(0.05)
                end

                return spawned
            end

            local function duplicateExistingProp(firstProp, countNeeded)
                if not firstProp then return false end
                local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
                if not dupRemote then return false end
                for i = 1, countNeeded do
                    pcall(function() dupRemote:InvokeServer() end)
                    task.wait(0.05)
                end
                return true
            end

            local function applyWhiteTheme()
                for _, entry in ipairs(titanPartsList) do
                    if entry.prop then
                        pcall(function()
                            if entry.cpc then entry.cpc:InvokeServer(Color3.fromRGB(255, 255, 255)) end
                            for _, desc in ipairs(entry.prop:GetDescendants()) do
                                if desc:IsA("BasePart") then desc.Color = Color3.fromRGB(255, 255, 255) end
                            end
                        end)
                    end
                end
            end

            local function stopTitan()
                titanActive = false
                isWaitingForPilot = false
                pilotBaitProp = nil
                currentGigOrigin = nil
                senders = {}
                if titanLoop then titanLoop:Disconnect() titanLoop = nil end
                if titanPhysicsLoop then titanPhysicsLoop:Disconnect() titanPhysicsLoop = nil end

                cleanGhostRig()

                local clearTools = rep:FindFirstChild("RE") and rep.RE:FindFirstChild("1Clea1rTool1s")
                if clearTools then
                    pcall(function()
                        clearTools:FireServer("ClearAllProps")
                        clearTools:FireServer("ClosePropMenu")
                        clearTools:FireServer("ClearAllTools")
                    end)
                end

                pcall(function()
                    local myChar = lp.Character
                    local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")
                    if myHum then myHum:UnequipTools() end
                    local toolRemote = rep:FindFirstChild("RE") and rep.RE:FindFirstChild("1Too1l")
                    local pmTool = (myChar and myChar:FindFirstChild("PropMaker")) or (lp.Backpack and lp.Backpack:FindFirstChild("PropMaker"))
                    if pmTool then
                        if toolRemote then toolRemote:InvokeServer("PickingTools", "PropMaker") end
                        pmTool:Destroy()
                    end
                end)

                titanPartsList = {}

                if titanToggleRef and titanToggleRef.Set then
                    pcall(function() titanToggleRef:Set(false) end)
                end
                if forceEmoteDropdownRef and forceEmoteDropdownRef.Set then
                    pcall(function() forceEmoteDropdownRef:Set("Stop Emote") end)
                end

                notify("Titan Controller", "Mecha Titan dismantled. All props cleared.", 3)
            end

            local function assembleTitanAroundPilot(cockpitBed, pilotPlayer)
                if not cockpitBed or not cockpitBed.Parent then return end
                titanActive = true
                currentPilot = pilotPlayer

                setupGhostRig()

                local wc = workspace:FindFirstChild("WorkspaceCom")
                if not wc then return end

                local existingProps = {}
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        if prop.Name == myPropName or prop.Name:find(lp.Name) then
                            existingProps[prop] = true
                        end
                    end
                end

                duplicateExistingProp(cockpitBed, 9)
                task.wait(0.3)

                local bedProps = { cockpitBed }
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        local matches = (prop.Name == myPropName) or (prop.Name:find(lp.Name) ~= nil)
                        if matches and not existingProps[prop] then
                            existingProps[prop] = true
                            table.insert(bedProps, prop)
                        end
                    end
                end

                local bedPos = cockpitBed.PrimaryPart and cockpitBed.PrimaryPart.Position or (pilotPlayer.Character and pilotPlayer.Character.HumanoidRootPart.Position or Vector3.new())
                local bunkerTarget = bedPos + Vector3.new(0, 5, 0)
                local firstBunker = spawnSingleProp("BigGunBunker", "Big", bunkerTarget, existingProps)
                if not firstBunker then firstBunker = spawnSingleProp("BigGunBunker", "Big", bunkerTarget, existingProps) end
                if not firstBunker then
                    stopTitan()
                    return
                end

                duplicateExistingProp(firstBunker, 4)
                task.wait(0.3)

                local bunkerProps = { firstBunker }
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        local matches = (prop.Name == myPropName) or (prop.Name:find(lp.Name) ~= nil)
                        if matches and not existingProps[prop] then
                            existingProps[prop] = true
                            table.insert(bunkerProps, prop)
                        end
                    end
                end

                
                task.delay(1.0, function()
                    pcall(function()
                        local toolRemote = rep:FindFirstChild("RE") and rep.RE:FindFirstChild("1Too1l")
                        if toolRemote then toolRemote:InvokeServer("PickingTools", "PropMaker") end
                        local pmTool = (lp.Character and lp.Character:FindFirstChild("PropMaker")) or (lp.Backpack and lp.Backpack:FindFirstChild("PropMaker"))
                        if pmTool then pmTool:Destroy() end
                        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
                        if hum then hum:UnequipTools() end
                    end)
                end)

                while #bedProps < 10 do table.insert(bedProps, bedProps[#bedProps]) end
                while #bunkerProps < 5 do table.insert(bunkerProps, bunkerProps[#bunkerProps]) end

                local function getPropRemotes(p)
                    if not p then return nil, nil end
                    local scf = p:FindFirstChild("SetCurrentCFrame") or p:FindFirstChild("SetCurrentCFrame", true)
                    local cpc = p:FindFirstChild("ChangePropColor") or p:FindFirstChild("ChangePropColor", true)
                    return scf, cpc
                end

                local bk1Scf, bk1Cpc = getPropRemotes(bunkerProps[1])
                local bk2Scf, bk2Cpc = getPropRemotes(bunkerProps[2])
                local bk3Scf, bk3Cpc = getPropRemotes(bunkerProps[3])
                local bk4Scf, bk4Cpc = getPropRemotes(bunkerProps[4])
                local bk5Scf, bk5Cpc = getPropRemotes(bunkerProps[5])

                local bd1Scf, bd1Cpc = getPropRemotes(bedProps[1])
                local bd2Scf, bd2Cpc = getPropRemotes(bedProps[2])
                local bd3Scf, bd3Cpc = getPropRemotes(bedProps[3])
                local bd4Scf, bd4Cpc = getPropRemotes(bedProps[4])
                local bd5Scf, bd5Cpc = getPropRemotes(bedProps[5])
                local bd6Scf, bd6Cpc = getPropRemotes(bedProps[6])
                local bd7Scf, bd7Cpc = getPropRemotes(bedProps[7])
                local bd8Scf, bd8Cpc = getPropRemotes(bedProps[8])
                local bd9Scf, bd9Cpc = getPropRemotes(bedProps[9])
                local bd10Scf, bd10Cpc = getPropRemotes(bedProps[10])

                titanPartsList = {
                    { name = "UpperTorso",    scf = bk1Scf, prop = bunkerProps[1], cpc = bk1Cpc, offset = CFrame.new(0, -2.5, 0) },
                    { name = "Head",          scf = bd1Scf, prop = bedProps[1],    cpc = bd1Cpc, offset = CFrame.new(0, 1.5, 0) * CFrame.Angles(math.rad(-90), 0, 0) },
                    { name = "LowerTorso",    scf = bd2Scf, prop = bedProps[2],    cpc = bd2Cpc, offset = CFrame.Angles(0, 0, math.rad(90)) },

                    { name = "LeftUpperArm",  scf = bd3Scf, prop = bedProps[3],    cpc = bd3Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
                    { name = "LeftLowerArm",  scf = bk2Scf, prop = bunkerProps[2], cpc = bk2Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
                    { name = "LeftHand",      scf = bd4Scf, prop = bedProps[4],    cpc = bd4Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },

                    { name = "RightUpperArm", scf = bd5Scf, prop = bedProps[5],    cpc = bd5Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
                    { name = "RightLowerArm", scf = bk3Scf, prop = bunkerProps[3], cpc = bk3Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
                    { name = "RightHand",     scf = bd6Scf, prop = bedProps[6],    cpc = bd6Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },

                    { name = "LeftUpperLeg",  scf = bd7Scf, prop = bedProps[7],    cpc = bd7Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
                    { name = "LeftLowerLeg",  scf = bk4Scf, prop = bunkerProps[4], cpc = bk4Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
                    { name = "LeftFoot",      scf = bd8Scf, prop = bedProps[8],    cpc = bd8Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },

                    { name = "RightUpperLeg", scf = bd9Scf, prop = bedProps[9],    cpc = bd9Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
                    { name = "RightLowerLeg", scf = bk5Scf, prop = bunkerProps[5], cpc = bk5Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
                    { name = "RightFoot",     scf = bd10Scf, prop = bedProps[10],  cpc = bd10Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
                }

                applyWhiteTheme()
                task.delay(0.5, applyWhiteTheme)

                local pChar = pilotPlayer and pilotPlayer.Character
                local pHrp = pChar and pChar:FindFirstChild("HumanoidRootPart")
                local myChar = lp.Character
                local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")

                titanWorldPos = bedPos
                local facingVec = pHrp and pHrp.CFrame.LookVector or (myHrp and myHrp.CFrame.LookVector or Vector3.new(0, 0, -1))
                titanHeading = math.deg(math.atan2(-facingVec.X, -facingVec.Z))

                titanPhysicsLoop = rs.Stepped:Connect(function()
                    if not titanActive then return end
                    for _, entry in ipairs(titanPartsList) do
                        if entry.prop then
                            pcall(function()
                                for _, p in ipairs(entry.prop:GetDescendants()) do
                                    if p:IsA("BasePart") then
                                        p.CanCollide = false
                                        p.CanTouch = false
                                        p.CanQuery = false
                                        p.Massless = true
                                    end
                                end
                            end)
                        end
                    end
                end)

                local rayParams = RaycastParams.new()
                rayParams.FilterType = Enum.RaycastFilterType.Exclude

                local lastTick = tick()
                local spawnStartTick = tick()
                local riseDuration = 1.0

                local smoothedLowestFootY = nil
                local lastCenterY = nil
                local currentWalkSpeed = 0
                local lastMoveDir = nil

                titanLoop = rs.RenderStepped:Connect(function()
                    if not titanActive then return end
                    local now = tick()
                    local dt = math.clamp(now - lastTick, 0.001, 0.1)
                    lastTick = now

                    local activePilot = currentPilot or pilotPlayer
                    if activePilot and activePilot ~= lp then
                        if not activePilot.Parent or not activePilot:IsDescendantOf(plrs) then
                            notify("Titan Controller", (activePilot.DisplayName or "Pilot") .. " left the game. Dismantling Titan...", 3)
                            stopTitan()
                            return
                        end
                    end

                    
                    if cockpitBed and cockpitBed.Parent then
                        for _, desc in ipairs(cockpitBed:GetDescendants()) do
                            if desc:IsA("Seat") or desc:IsA("VehicleSeat") then
                                local occ = desc.Occupant
                                if occ and occ:IsA("Humanoid") and occ.Parent then
                                    local p = plrs:GetPlayerFromCharacter(occ.Parent)
                                    if p and p ~= activePilot then
                                        occ.Sit = false
                                        notify("Titan Controller", (p.DisplayName or "Player") .. " ejected from cockpit!", 2)
                                    end
                                end
                            end
                        end
                    end

                    local pCharNow = activePilot and activePilot.Character
                    local pHum = pCharNow and pCharNow:FindFirstChildOfClass("Humanoid")

                    local moveDir = Vector3.zero
                    local isMoving = false
                    local rawMag = 0

                    local JOYSTICK_DEADZONE = 0.22
                    local EMOTE_CANCEL_DEADZONE = 0.35

                    
                    local isPilotSeated = pHum and (pHum.Sit == true or (pHum.SeatPart and pHum.SeatPart:IsDescendantOf(cockpitBed)))
                    if isPilotSeated then
                        rawMag = pHum.MoveDirection.Magnitude
                        if rawMag > JOYSTICK_DEADZONE then
                            moveDir = pHum.MoveDirection
                            isMoving = true
                        end
                    end

                    titanIsMoving = isMoving

                    if isMoving then
                        if rawMag > EMOTE_CANCEL_DEADZONE then
                            if currentEmoteTrack then
                                pcall(function() currentEmoteTrack:Stop(0.15) end)
                                currentEmoteTrack = nil
                                currentEmoteIndex = nil
                            end
                        end
                        if not currentEmoteTrack then
                            if ghostIdleTrack and ghostIdleTrack.IsPlaying then
                                ghostIdleTrack:Stop(0.15)
                            end
                            if ghostRunTrack and not ghostRunTrack.IsPlaying then
                                ghostRunTrack:Play(0.15)
                                ghostRunTrack:AdjustSpeed(titanConfig.animSpeed)
                            end
                        end
                    else
                        if ghostRunTrack and ghostRunTrack.IsPlaying then
                            ghostRunTrack:Stop(0.15)
                        end
                        if currentEmoteTrack then
                            if not currentEmoteTrack.IsPlaying then
                                pcall(function() currentEmoteTrack:Play(0.1) end)
                            end
                            if ghostIdleTrack and ghostIdleTrack.IsPlaying then
                                ghostIdleTrack:Stop(0.1)
                            end
                        else
                            if ghostIdleTrack and not ghostIdleTrack.IsPlaying then
                                ghostIdleTrack:Play(0.15)
                            end
                        end
                    end

                    local targetSpeed = 0
                    if isMoving then
                        local speedScale = math.clamp((rawMag - JOYSTICK_DEADZONE) / (1.0 - JOYSTICK_DEADZONE), 0.35, 1.0)
                        targetSpeed = titanConfig.walkSpeed * speedScale
                    end

                    local accelRate = isMoving and 7.0 or 12.0
                    currentWalkSpeed = currentWalkSpeed + (targetSpeed - currentWalkSpeed) * math.clamp(dt * accelRate, 0, 1)

                    if currentWalkSpeed > 0.3 then
                        local forwardVec = (isMoving and moveDir.Unit) or lastMoveDir or Vector3.new(0, 0, -1)
                        if isMoving then lastMoveDir = moveDir.Unit end

                        local targetAngle = math.deg(math.atan2(-forwardVec.X, -forwardVec.Z))
                        local diff = (targetAngle - titanHeading + 180) % 360 - 180
                        titanHeading = titanHeading + (diff * math.clamp(dt * titanConfig.turnSpeed, 0, 1))

                        local step = forwardVec * (currentWalkSpeed * dt)
                        titanWorldPos = titanWorldPos + Vector3.new(step.X, 0, step.Z)
                    end

                    local rayFilter = {ghostRig, workspace:FindFirstChild("WorkspaceCom")}
                    for _, plr in ipairs(plrs:GetPlayers()) do
                        if plr.Character then table.insert(rayFilter, plr.Character) end
                    end
                    rayParams.FilterDescendantsInstances = rayFilter

                    local rayOrigin = Vector3.new(titanWorldPos.X, titanWorldPos.Y + 15, titanWorldPos.Z)
                    local ray = workspace:Raycast(rayOrigin, Vector3.new(0, -60, 0), rayParams)
                    local groundY = (ray and ray.Position) and ray.Position.Y or titanWorldPos.Y

                    local ghostRoot = ghostRig and ghostRig:FindFirstChild("HumanoidRootPart")
                    local ghostRootCF = ghostRoot and ghostRoot.CFrame or CFrame.new()

                    local leftFootLimb = getGhostLimb(ghostRig, "LeftFoot")
                    local rightFootLimb = getGhostLimb(ghostRig, "RightFoot")
                    local lowestFootRelY = -2.45 * titanConfig.size

                    if leftFootLimb and rightFootLimb then
                        local lRel = ghostRootCF:ToObjectSpace(leftFootLimb.CFrame)
                        local rRel = ghostRootCF:ToObjectSpace(rightFootLimb.CFrame)
                        local lY = lRel.Position.Y * titanConfig.size + titanConfig.legOffsetY
                        local rY = rRel.Position.Y * titanConfig.size + titanConfig.legOffsetY
                        lowestFootRelY = math.min(lY, rY)

                        if currentEmoteTrack and currentEmoteTrack.IsPlaying then
                            local pelvisLimb = getGhostLimb(ghostRig, "LowerTorso")
                            if pelvisLimb then
                                local pRel = ghostRootCF:ToObjectSpace(pelvisLimb.CFrame)
                                local pY = pRel.Position.Y * titanConfig.size + titanConfig.pelvisOffsetY
                                lowestFootRelY = math.min(lowestFootRelY, pY)
                            end
                        end
                    end

                    
                    if not smoothedLowestFootY then
                        smoothedLowestFootY = lowestFootRelY
                    else
                        smoothedLowestFootY = smoothedLowestFootY + (lowestFootRelY - smoothedLowestFootY) * math.clamp(dt * 8, 0, 1)
                    end

                    local bedHalfThickness = 0.8
                    local targetFinalCenterY = groundY - smoothedLowestFootY - bedHalfThickness + titanConfig.heightOffset
                    if not lastCenterY then
                        lastCenterY = targetFinalCenterY
                    else
                        lastCenterY = lastCenterY + (targetFinalCenterY - lastCenterY) * math.clamp(dt * 12, 0, 1)
                    end

                    local elapsed = now - spawnStartTick
                    local riseAlpha = math.clamp(elapsed / riseDuration, 0, 1)
                    local ease = 1 - math.pow(1 - riseAlpha, 3)
                    local activeCenterY = groundY + ((lastCenterY - groundY) * ease)

                    local targetCenterCF = CFrame.new(titanWorldPos.X, activeCenterY, titanWorldPos.Z) * CFrame.Angles(0, math.rad(titanHeading), 0)
                    if not currentGigOrigin then
                        currentGigOrigin = targetCenterCF
                    else
                        currentGigOrigin = currentGigOrigin:Lerp(targetCenterCF, math.clamp(dt * 18, 0, 1))
                    end
                    local centerCF = currentGigOrigin

                    for _, entry in ipairs(titanPartsList) do
                        if entry.scf then
                            local limbPart = getGhostLimb(ghostRig, entry.name)
                            if limbPart then
                                local rel = ghostRootCF:ToObjectSpace(limbPart.CFrame)
                                local xMult = 1.0
                                if entry.name:find("Arm") or entry.name:find("Hand") then
                                    xMult = titanConfig.armWidth
                                elseif entry.name:find("Leg") or entry.name:find("Foot") then
                                    xMult = titanConfig.legWidth
                                end
                                local scaledPos = Vector3.new(
                                    rel.Position.X * titanConfig.size * xMult,
                                    rel.Position.Y * titanConfig.size,
                                    rel.Position.Z * titanConfig.size
                                )
                                if entry.name:find("LowerArm") then
                                    scaledPos = scaledPos + Vector3.new(0, -titanConfig.armSpacing, 0)
                                elseif entry.name:find("Hand") then
                                    scaledPos = scaledPos + Vector3.new(0, -titanConfig.armSpacing * 2, 0)
                                end
                                if entry.name:find("Leg") or entry.name:find("Foot") then
                                    scaledPos = scaledPos + Vector3.new(0, titanConfig.legOffsetY, 0)
                                    if entry.name:find("LowerLeg") then
                                        scaledPos = scaledPos + Vector3.new(0, titanConfig.legSpacing, 0)
                                    elseif entry.name:find("UpperLeg") then
                                        scaledPos = scaledPos + Vector3.new(0, titanConfig.legSpacing * 2, 0)
                                    end
                                end
                                local relRot = rel - rel.Position
                                local extraOffset = entry.offset or CFrame.new()
                                if entry.name == "UpperTorso" then
                                    extraOffset = CFrame.new(titanConfig.torsoOffsetX, titanConfig.torsoOffsetY, titanConfig.torsoOffsetZ)
                                        * CFrame.Angles(math.rad(titanConfig.torsoRotX), math.rad(titanConfig.torsoRotY), math.rad(titanConfig.torsoRotZ))
                                elseif entry.name == "LowerTorso" then
                                    extraOffset = (entry.offset or CFrame.new())
                                        * CFrame.new(titanConfig.pelvisOffsetX, titanConfig.pelvisOffsetY, titanConfig.pelvisOffsetZ)
                                        * CFrame.Angles(math.rad(titanConfig.pelvisRotX), math.rad(titanConfig.pelvisRotY), math.rad(titanConfig.pelvisRotZ))
                                end
                                local targetCF = centerCF * CFrame.new(scaledPos) * relRot * extraOffset
                                local isCockpit = (entry.prop == cockpitBed) or (entry.name == "Head")
                                sendCF(entry.scf, targetCF, entry.prop, isCockpit)
                            end
                        end
                    end
                end)

                notify("Gigantic Titan", "Robot 6 assembled in Pure White! 15/15 Props firmly grounded.", 4)
            end

            local function deployPilotSeatTrap(targetPlr)
                if not targetPlr then
                    notify("Titan Trap", "Please select a Target Pilot in dropdown first!", 3)
                    if titanToggleRef and titanToggleRef.Set then pcall(function() titanToggleRef:Set(false) end) end
                    return
                end

                if titanActive or isWaitingForPilot then
                    stopTitan()
                    task.wait(0.4)
                end

                local char = lp.Character or lp.CharacterAdded:Wait()
                local hrp = char:WaitForChild("HumanoidRootPart")

                local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
                pcall(function() clearTools:FireServer("ClearAllProps") end)
                task.wait(0.4)

                pcall(function() clearTools:FireServer("OpenPropMenu") end)
                task.wait(0.2)

                local targetChar = targetPlr and targetPlr.Character
                local targetHrp = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
                local spawnPos = targetHrp and (targetHrp.Position + (targetHrp.CFrame.LookVector * 4.5)) or (hrp.Position + (hrp.CFrame.LookVector * 8))

                notify("Titan Trap", "Deploying Cockpit Bed in front of " .. targetPlr.DisplayName .. "...", 3)
                
                local existingProps = {}
                local wc = workspace:FindFirstChild("WorkspaceCom")
                if wc then
                    for _, folder in pairs(wc:GetChildren()) do
                        for _, prop in pairs(folder:GetChildren()) do
                            if prop.Name == myPropName or prop.Name:find(lp.Name) then
                                existingProps[prop] = true
                            end
                        end
                    end
                end

                local firstBed = spawnSingleProp("FuturisticLargeBed", "Furniture", spawnPos, existingProps)
                if not firstBed then firstBed = spawnSingleProp("FuturisticLargeBed", "Furniture", spawnPos, existingProps) end
                if not firstBed then
                    notify("Titan Trap", "Failed to deploy cockpit prop!", 3)
                    if titanToggleRef and titanToggleRef.Set then pcall(function() titanToggleRef:Set(false) end) end
                    return
                end

                
                task.delay(1.0, function()
                    pcall(function()
                        local toolRemote = rep:FindFirstChild("RE") and rep.RE:FindFirstChild("1Too1l")
                        if toolRemote then toolRemote:InvokeServer("PickingTools", "PropMaker") end
                        local pmTool = (lp.Character and lp.Character:FindFirstChild("PropMaker")) or (lp.Backpack and lp.Backpack:FindFirstChild("PropMaker"))
                        if pmTool then pmTool:Destroy() end
                        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
                        if hum then hum:UnequipTools() end
                    end)
                end)

                pcall(function()
                    local cpc = firstBed:FindFirstChild("ChangePropColor") or firstBed:FindFirstChild("ChangePropColor", true)
                    if cpc then cpc:InvokeServer(Color3.fromRGB(255, 255, 255)) end
                    for _, p in ipairs(firstBed:GetDescendants()) do
                        if p:IsA("BasePart") then p.Color = Color3.fromRGB(255, 255, 255) end
                    end
                end)

                pilotBaitProp = firstBed
                isWaitingForPilot = true
                notify("Titan Trap", "Bed deployed! Waiting for " .. targetPlr.DisplayName .. " to sit...", 5)

                task.spawn(function()
                    local timeout = tick() + 120
                    while isWaitingForPilot and tick() < timeout do
                        if not pilotBaitProp or not pilotBaitProp.Parent then break end
                        if targetPlr and (not targetPlr.Parent or not targetPlr:IsDescendantOf(plrs)) then
                            notify("Titan Trap", "Target player left the game. Cancelling...", 3)
                            stopTitan()
                            return
                        end

                        local foundPilot = nil
                        for _, desc in ipairs(pilotBaitProp:GetDescendants()) do
                            if desc:IsA("Seat") or desc:IsA("VehicleSeat") then
                                local occ = desc.Occupant
                                if occ and occ:IsA("Humanoid") and occ.Parent then
                                    local p = plrs:GetPlayerFromCharacter(occ.Parent)
                                    if p then
                                        if p == targetPlr then
                                            foundPilot = p
                                            break
                                        else
                                            
                                            occ.Sit = false
                                            notify("Titan Trap", (p.DisplayName or "Player") .. " ejected! Only " .. targetPlr.DisplayName .. " can pilot!", 2)
                                        end
                                    end
                                end
                            end
                        end

                        if foundPilot then
                            isWaitingForPilot = false
                            currentPilot = foundPilot
                            notify("Gigantic Titan", foundPilot.DisplayName .. " seated in cockpit! Forging Robot 6...", 4)
                            task.spawn(function()
                                assembleTitanAroundPilot(pilotBaitProp, foundPilot)
                            end)
                            return
                        end
                        task.wait(0.1)
                    end

                    if isWaitingForPilot then
                        notify("Titan Trap", "Boarding timeout or cancelled.", 3)
                        isWaitingForPilot = false
                        if titanToggleRef and titanToggleRef.Set then pcall(function() titanToggleRef:Set(false) end) end
                    end
                end)
            end

            
            local function handleChatCommand(senderPlayer, message)
                if not titanActive then return end
                if not senderPlayer or not message then return end

                local senderUserId = senderPlayer.UserId
                local pilotUserId = currentPilot and currentPilot.UserId
                local localUserId = lp and lp.UserId

                local isAllowed = (senderUserId == localUserId) or (pilotUserId and senderUserId == pilotUserId)
                if not isAllowed then return end

                local cleanMsg = message:lower():gsub("^%s+", ""):gsub("%s+$", "")
                local emoteNumStr = cleanMsg:match("[:/]?[e%s]*emote%s*(%d+)")
                    or cleanMsg:match("^:?(%d+)$")

                if emoteNumStr then
                    local num = tonumber(emoteNumStr)
                    if num and num >= 1 and num <= 5 then
                        playTitanEmote(num)
                        return
                    elseif num == 0 then
                        stopTitanEmote()
                        return
                    end
                end

                if cleanMsg:find("stop") and (cleanMsg:find("emote") or cleanMsg:find("^[:/!]?stop") or cleanMsg == "stop") then
                    stopTitanEmote()
                end
            end

            pcall(function()
                local TextChatService = game:GetService("TextChatService")
                TextChatService.MessageReceived:Connect(function(textChatMessage)
                    local sender = nil
                    if textChatMessage.TextSource then
                        sender = plrs:GetPlayerByUserId(textChatMessage.TextSource.UserId)
                    end
                    if sender then handleChatCommand(sender, textChatMessage.Text) end
                end)
            end)

            local function hookPlayerChat(p)
                p.Chatted:Connect(function(msg) handleChatCommand(p, msg) end)
            end
            for _, p in ipairs(plrs:GetPlayers()) do hookPlayerChat(p) end
            plrs.PlayerAdded:Connect(hookPlayerChat)

            plrs.PlayerRemoving:Connect(function(leavingPlr)
                local isTargetPilot = (currentPilot and leavingPlr.UserId == currentPilot.UserId)
                if isTargetPilot then
                    notify("Titan Controller", (leavingPlr.DisplayName or "Pilot") .. " left the game. Dismantling Titan...", 3)
                    stopTitan()
                end
            end)

            
            
            
            targetPilotDropdownRef = AdminTab:AddDropdown({
                Name = "Target Pilot",
                Description = "Select player who will pilot the Titan (or '---')",
                Options = getPilotPlayersList(),
                Default = "---",
                Callback = function(val)
                    selectedPilotName = val
                    if val ~= "---" then
                        currentPilot = plrs:FindFirstChild(val)
                        if currentPilot then
                            notify("Titan Controller", "Selected target pilot: " .. currentPilot.DisplayName, 3)
                        end
                    else
                        currentPilot = nil
                    end
                end
            })

            AdminTab:AddButton({
                Name = "Refresh Pilot List",
                Description = "Refreshes player names in target pilot dropdown",
                Callback = function()
                    local updated = getPilotPlayersList()
                    if targetPilotDropdownRef and targetPilotDropdownRef.Set then
                        pcall(function() targetPilotDropdownRef:Set(updated) end)
                    end
                    notify("Titan Controller", "Pilot list refreshed!", 2)
                end
            })

            
            titanToggleRef = AdminTab:AddToggle({
                Name = "Deploy Cockpit (Build on Sit)",
                Description = "Spawns cockpit bed. When target sits, Titan builds & activates automatically!",
                Default = false,
                Callback = function(val)
                    if val then
                        local target = currentPilot
                        if not target and selectedPilotName ~= "---" then
                            target = plrs:FindFirstChild(selectedPilotName)
                        end
                        if not target then
                            notify("Titan Controller", "Please select a Target Pilot in dropdown first!", 3)
                            if titanToggleRef and titanToggleRef.Set then
                                pcall(function() titanToggleRef:Set(false) end)
                            end
                            return
                        end
                        task.spawn(function()
                            deployPilotSeatTrap(target)
                        end)
                    else
                        if titanActive or isWaitingForPilot then
                            stopTitan()
                        end
                    end
                end
            })

            
            forceEmoteDropdownRef = AdminTab:AddDropdown({
                Name = "Force Titan Emote",
                Description = "Select emote to immediately force on Titan (or Stop)",
                Options = {
                    "Stop Emote",
                    "1: Cute Sit",
                    "2: Dia Delicia Dance",
                    "3: Dance 3",
                    "4: Best Dance",
                    "5: Low Cortisol Dance"
                },
                Default = "Stop Emote",
                Callback = function(val)
                    if val == "Stop Emote" then
                        stopTitanEmote()
                    else
                        local num = tonumber(val:match("^(%d+)"))
                        if num then
                            playTitanEmote(num)
                        end
                    end
                end
            })

            AdminTab:AddButton({
                Name = "Stop Titan / Clean Props",
                Description = "Emergency stop and clean all props",
                Callback = function()
                    stopTitan()
                end
            })
        end

    end

    
    TrollTab:AddSection({"> Auto Loop & Options"})
    
    _G.AutoLoopTroll = false
    TrollTab:AddToggle({
        Name = "Auto-Loop Troll",
        Description = "Re-applies the troll when the target respawns",
        Default = false,
        Callback = function(value)
            _G.AutoLoopTroll = value
        end
    })

    _G.BringMethod = "Smart"
    TrollTab:AddDropdown({
        Name = "Catch Method",
        Description = "Smart: predicts movement. Basic: direct placement.",
        Options = {"Smart", "Basic"},
        Default = "Smart",
        Callback = function(value)
            _G.BringMethod = value
        end
    })

    TrollTab:AddSlider({
        Name = "Prop Count (Spam)",
        Description = "More props = faster catch!",
        Min = 1,
        Max = 15,
        Default = 1,
        Suffix = " props",
        Callback = function(Value)
            bringToiletCount = Value
            _G.BringToiletCount = Value
        end
    })

MorphFETab:AddSection({
    "> Goku (FE)"
})

do
    
    local function _notif(t, m)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {Title = t, Text = m, Duration = 3})
        end)
    end

    
    local function _wearOutfit(outfitIds, name)
        task.spawn(function()
            local RS = game:GetService("ReplicatedStorage")
            local Remotes = RS:FindFirstChild("Remotes")
            local wearRemote = Remotes and Remotes:FindFirstChild("Wear")
            if not wearRemote then
                _notif(name, "Wear remote not found!")
                return
            end
            _notif(name, "Applying outfit... (~" .. math.ceil(#outfitIds * 2.1) .. "s)")
            for i, id in ipairs(outfitIds) do
                pcall(function() wearRemote:InvokeServer(id) end)
                if i < #outfitIds then task.wait(2.1) end 
            end
            _notif(name, "Outfit applied!")
        end)
    end

    
    local function _overrideAnims(ids)
        task.spawn(function()
            local p = game:GetService("Players").LocalPlayer
            local char = p.Character or p.CharacterAdded:Wait()
            local hum = char:WaitForChild("Humanoid")
            local animate = char:WaitForChild("Animate")
            task.wait(0.5)
            pcall(function()
                local i1 = animate.idle:FindFirstChild("Animation1")
                local i2 = animate.idle:FindFirstChild("Animation2")
                local rn = animate.run:FindFirstChild("RunAnim")
                if i1 then i1.AnimationId = "rbxassetid://" .. tostring(ids.idle) end
                if i2 then i2.AnimationId = "rbxassetid://" .. tostring(ids.idle) end
                if rn  then rn.AnimationId  = "rbxassetid://" .. tostring(ids.run)  end
            end)
            animate.Enabled = false
            task.wait(0.05)
            animate.Enabled = true
        end)
    end

    -- Load a catalog emote as a pure Animation (security bypass, LOCKED)
    local function _loadEmoteAsPureAnimation(hum, emoteId)
        local anim = Instance.new("Animation")
        pcall(function()
            local objects = game:GetObjects("rbxassetid://" .. tostring(emoteId))
            if objects and objects[1] and objects[1]:IsA("Animation") then
                anim.AnimationId = objects[1].AnimationId
            end
        end)
        if anim.AnimationId == "" then
            anim.AnimationId = "rbxassetid://" .. tostring(emoteId)
        end
        local track = hum:LoadAnimation(anim)
        track.Priority = Enum.AnimationPriority.Action4
        return track
    end

    do
        local _gokuActive   = false
        local _gokuConns    = {}
        local _ssjConns     = {}

        
        local GOKU_OUTFIT = { 15093053680, 16838399997, 13918623613, 136320123090473 }
        local GOKU_HEAD_ID = 136320123090473  
        local SSJ_HEAD_ID  = 18332240211

        
        local A_IDLE         = 72492959889389
        local A_RUN          = 18537384940
        local A_TP_EMOTE     = "133631846764964"
        local A_SSJ_TRANSFORM= 115344615095212   
        local A_KI_CHARGE    = 70769545504969    
        local A_FLY_SPRINT   = 128582748149019   
        local A_SSJ_SFX      = 18704549552       

        
        local function _lp()  return game:GetService("Players").LocalPlayer end
        local function _char() local p=_lp() return p and p.Character end
        local function _hrp()  local c=_char() return c and c:FindFirstChild("HumanoidRootPart") end
        local function _hum()  local c=_char() return c and c:FindFirstChildOfClass("Humanoid") end
        local function _notif(t, m) pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification",{Title=t,Text=m,Duration=3})
        end) end

        
        local _flying      = false
        local _sprinting   = false
        local _flightSpeed = 55
        local _sprintSpeed = 130
        local _moveState   = {fwd=0,bck=0,lft=0,rgt=0,up=0,dn=0}
        local _vel         = Vector3.new(0,0,0)
        local _currentCF   = nil
        local _flightConns = {}
        local _animTrack   = nil
        local _origGrav    = workspace.Gravity
        local _sprintGui   = nil

        
        local _ssj         = false
        local _ssjExpSfx   = nil

        
        local _kiCharging  = false
        local _kiTrack     = nil

        
        local _origWalk    = 16
        local _origJump    = 7.2

        
        local _tpGui = nil
        local _tping = false

        
        local _origAnims   = {}
        local _animsLoaded = false

        
        local function _stopAllAnims()
            local hum = _hum()
            if not hum then return end
            for _, t in ipairs(hum:GetPlayingAnimationTracks()) do
                pcall(function() t:Stop(0) end)
            end
        end

        local function _playAnim(id, startT, speed, loop)
            local hum = _hum()
            if not hum then return end
            if _animTrack then pcall(function() _animTrack:Stop(0.1) end) _animTrack = nil end
            local a = Instance.new("Animation")
            a.AnimationId = "rbxassetid://" .. tostring(id)
            _animTrack = hum:LoadAnimation(a)
            _animTrack.Looped = loop or false
            _animTrack:Play()
            _animTrack.TimePosition = startT or 0
            _animTrack:AdjustSpeed(speed or 1)
        end

        local function _loadCustomAnims()
            local char = _char(); local hum = _hum()
            if not char or not hum then return end
            local animate = char:FindFirstChild("Animate")
            if not animate then return end
            _stopAllAnims()
            pcall(function()
                local i1 = animate.idle:FindFirstChild("Animation1")
                local i2 = animate.idle:FindFirstChild("Animation2")
                local rn = animate.run:FindFirstChild("RunAnim")
                if i1 then _origAnims.Idle1=i1.AnimationId; i1.AnimationId="rbxassetid://"..A_IDLE end
                if i2 then _origAnims.Idle2=i2.AnimationId; i2.AnimationId="rbxassetid://"..A_IDLE end
                if rn  then _origAnims.Run =rn.AnimationId;  rn.AnimationId ="rbxassetid://"..A_RUN  end
            end)
            animate.Enabled=false; task.wait(0.05); animate.Enabled=true
            _animsLoaded = true
        end

        local function _restoreAnims()
            if not _animsLoaded then return end
            local char = _char()
            if not char then return end
            local animate = char:FindFirstChild("Animate")
            if not animate then return end
            pcall(function()
                if _origAnims.Idle1 then animate.idle.Animation1.AnimationId=_origAnims.Idle1 end
                if _origAnims.Idle2 then animate.idle.Animation2.AnimationId=_origAnims.Idle2 end
                if _origAnims.Run   then animate.run.RunAnim.AnimationId     =_origAnims.Run   end
            end)
            animate.Enabled=false; task.wait(0.05); animate.Enabled=true
            _animsLoaded = false
        end

        
        local function _stopFlight()
            if not _flying then return end
            _flying = false
            _sprinting = false
            for _, c in ipairs(_flightConns) do pcall(function() if c.Connected then c:Disconnect() end end) end
            _flightConns = {}
            _moveState = {fwd=0,bck=0,lft=0,rgt=0,up=0,dn=0}
            workspace.Gravity = _origGrav
            local hum = _hum(); local hrp = _hrp()
            if hum then hum.PlatformStand = false end
            if _animTrack then pcall(function() _animTrack:Stop(0.1) end) _animTrack = nil end
            if hrp then
                if hrp:FindFirstChild("GokuGyro") then hrp.GokuGyro:Destroy() end
                if hrp:FindFirstChild("GokuVel")  then hrp.GokuVel:Destroy()  end
            end
            
            if _sprintGui then pcall(function() _sprintGui:Destroy() end) _sprintGui=nil end
            local animate = _char() and _char():FindFirstChild("Animate")
            if animate then animate.Disabled = false end
        end

        local function _setSprint(on)
            if not _flying then return end
            if on == _sprinting then return end
            _sprinting = on
            if on then
                _playAnim(A_FLY_SPRINT, 15, 1, true)
            else
                
                local fwd = _moveState.fwd - _moveState.bck
                if fwd > 0 then _playAnim(106345021112927,15,1,true)
                elseif fwd < 0 then _playAnim(132783162476851,4,1,true)
                else _playAnim(113420763488155,4,1,true) end
            end
        end

        local function _startFlight()
            if not _gokuActive or _flying then return end
            local hrp=_hrp(); local hum=_hum()
            if not hrp or not hum then return end
            _flying = true
            _sprinting = false
            _origGrav = workspace.Gravity
            workspace.Gravity = 192.2
            hum.PlatformStand = true
            _playAnim(113420763488155, 4, 1, true)

            local gyro = Instance.new("BodyGyro")
            gyro.Name="GokuGyro"; gyro.Parent=hrp
            gyro.P=90000; gyro.MaxTorque=Vector3.new(9e9,9e9,9e9); gyro.CFrame=hrp.CFrame
            local bv = Instance.new("BodyVelocity")
            bv.Name="GokuVel"; bv.Parent=hrp
            bv.MaxForce=Vector3.new(9e9,9e9,9e9); bv.Velocity=Vector3.new(0,0,0)
            _vel = Vector3.new(0,0,0)

            local RS  = game:GetService("RunService")
            local UIS = game:GetService("UserInputService")

            
            local lp2 = _lp()
            local isMobile = UIS.TouchEnabled and not UIS.KeyboardEnabled
            if isMobile then
                _sprintGui = Instance.new("ScreenGui")
                _sprintGui.Name="GokuSprintGui"; _sprintGui.ResetOnSpawn=false
                _sprintGui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
                _sprintGui.Parent=lp2.PlayerGui
                local sprintBtn = Instance.new("TextButton")
                sprintBtn.Size=UDim2.new(0,90,0,90); sprintBtn.Position=UDim2.new(0.88,0,0.72,0)
                sprintBtn.BackgroundColor3=Color3.fromRGB(255,200,0)
                sprintBtn.BackgroundTransparency=0.35
                sprintBtn.Text="SPRINT"; sprintBtn.TextColor3=Color3.fromRGB(255,255,255)
                sprintBtn.Font=Enum.Font.GothamBold; sprintBtn.TextSize=14
                sprintBtn.BorderSizePixel=0; sprintBtn.ZIndex=5
                sprintBtn.Parent=_sprintGui
                Instance.new("UICorner",sprintBtn).CornerRadius=UDim.new(1,0)
                local sprintStroke=Instance.new("UIStroke",sprintBtn)
                sprintStroke.Color=Color3.fromRGB(255,255,255); sprintStroke.Thickness=2
                sprintBtn.MouseButton1Down:Connect(function() _setSprint(true) end)
                sprintBtn.MouseButton1Up:Connect(function() _setSprint(false) end)
            end

            local upd = RS.RenderStepped:Connect(function()
                if not _gokuActive or not _flying then return end
                local h = _hrp(); if not h then return end
                local cam = workspace.CurrentCamera
                local fwd  = _moveState.fwd - _moveState.bck
                local side = _moveState.rgt - _moveState.lft
                local dir  = Vector3.new(0,0,0)
                if fwd  ~= 0 then dir = dir + cam.CFrame.LookVector  * fwd  end
                if side ~= 0 then dir = dir + cam.CFrame.RightVector * side end
                if dir.Magnitude > 0 then dir = dir.Unit end
                local vert = 0
                if _moveState.up > 0 then vert = 1 elseif _moveState.dn > 0 then vert = -1 end
                local spd = _sprinting and _sprintSpeed or _flightSpeed
                local desired = Vector3.new(0,0,0)
                if dir.Magnitude > 0 then desired = desired + dir * spd end
                if vert ~= 0 then desired = desired + Vector3.new(0, vert * spd * 0.6, 0) end
                if desired.Magnitude > 0.1 then
                    _vel = _vel:Lerp(desired, _sprinting and 0.2 or 0.15)
                    local dCF = cam.CFrame
                    if fwd > 0 then dCF = cam.CFrame * CFrame.Angles(0,0,0)
                    elseif fwd < 0 then dCF = cam.CFrame * CFrame.Angles(math.rad(30),0,0) end
                    if _currentCF then _currentCF=_currentCF:Lerp(dCF,0.25) else _currentCF=dCF end
                    gyro.CFrame = _currentCF
                else
                    _vel = _vel:Lerp(Vector3.new(0,0,0), 0.3)
                end
                bv.Velocity = _vel
            end)
            table.insert(_flightConns, upd); table.insert(_gokuConns, upd)

            local ib = UIS.InputBegan:Connect(function(input, gp)
                if gp or not _flying then return end
                local k = input.KeyCode
                if     k==Enum.KeyCode.W           then _moveState.fwd=1
                    if not _sprinting then _playAnim(106345021112927,15,1,true) end
                elseif k==Enum.KeyCode.S           then _moveState.bck=1; _playAnim(132783162476851,4,1,true)
                elseif k==Enum.KeyCode.A           then _moveState.lft=1
                elseif k==Enum.KeyCode.D           then _moveState.rgt=1
                elseif k==Enum.KeyCode.Space       then _moveState.up=1
                elseif k==Enum.KeyCode.LeftControl then _moveState.dn=1
                elseif k==Enum.KeyCode.LeftShift   then _setSprint(true)
                end
            end)
            local ie = UIS.InputEnded:Connect(function(input, gp)
                if not _flying then return end
                local k = input.KeyCode
                if     k==Enum.KeyCode.W           then _moveState.fwd=0
                    if not _sprinting then _playAnim(113420763488155,4,1,true) end
                elseif k==Enum.KeyCode.S           then _moveState.bck=0
                    if not _sprinting then _playAnim(113420763488155,4,1,true) end
                elseif k==Enum.KeyCode.A           then _moveState.lft=0
                elseif k==Enum.KeyCode.D           then _moveState.rgt=0
                elseif k==Enum.KeyCode.Space       then _moveState.up=0
                elseif k==Enum.KeyCode.LeftControl then _moveState.dn=0
                elseif k==Enum.KeyCode.LeftShift   then _setSprint(false)
                end
            end)
            table.insert(_flightConns, ib); table.insert(_gokuConns, ib)
            table.insert(_flightConns, ie); table.insert(_gokuConns, ie)
        end

        
        local function _activateSSJ()
            if not _gokuActive or _ssj then
                _notif("Goku (FE)", "Already Super Saiyan!") return
            end
            local char=_char(); local hrp=_hrp(); local hum=_hum()
            if not char or not hrp or not hum then return end

            _ssj = true
            _origWalk = hum.WalkSpeed; _origJump = hum.JumpHeight
            hum.WalkSpeed=0; hum.JumpHeight=0
            _stopAllAnims()

            
            local SSJ_ENERGY_EMOTE = 73708713364616
            local ssjEmoteTrack    = nil
            local ssjEDesc = hum:FindFirstChildOfClass("HumanoidDescription")
            if ssjEDesc then
                pcall(function()
                    local el = ssjEDesc:GetEmotes()
                    if not el["GokuSSJEmote"] then
                        ssjEDesc:AddEmote("GokuSSJEmote", SSJ_ENERGY_EMOTE)
                    end
                end)
                pcall(function()
                    local ok, tr = pcall(function()
                        return hum:PlayEmoteAndGetAnimTrackById(SSJ_ENERGY_EMOTE)
                    end)
                    if ok and tr then
                        ssjEmoteTrack = tr
                        ssjEmoteTrack.Priority = Enum.AnimationPriority.Action4
                    end
                end)
            end

            
            local TW  = game:GetService("TweenService")
            local cam = workspace.CurrentCamera
            local origCT = cam.CameraType
            cam.CameraType = Enum.CameraType.Scriptable
            TW:Create(cam, TweenInfo.new(1.2,Enum.EasingStyle.Quad,Enum.EasingDirection.Out),
                {CFrame = CFrame.lookAt(
                    hrp.Position + hrp.CFrame.LookVector*16 + Vector3.new(0,4,0),
                    hrp.Position)}):Play()

            
            local ao = Instance.new("Animation")
            ao.AnimationId = "rbxassetid://"..tostring(A_SSJ_TRANSFORM)
            local transTrack = hum:LoadAnimation(ao)
            transTrack.Looped = false; transTrack:Play()
            task.wait(0.6)

            
            local lp2 = game:GetService("Players").LocalPlayer

            
            local flashGui = Instance.new("ScreenGui")
            flashGui.Name="SSJFlash"; flashGui.IgnoreGuiInset=true
            flashGui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
            flashGui.ResetOnSpawn=false; flashGui.Parent=lp2.PlayerGui
            local flashF = Instance.new("Frame")
            flashF.Size=UDim2.new(1,0,1,0)
            flashF.BackgroundColor3=Color3.fromRGB(255,220,0)
            flashF.BackgroundTransparency=1; flashF.BorderSizePixel=0
            flashF.ZIndex=100; flashF.Parent=flashGui

            
            local blast = Instance.new("Part")
            blast.Name="SSJBlast"; blast.Shape=Enum.PartType.Ball
            blast.Material=Enum.Material.Neon
            blast.BrickColor=BrickColor.new("Bright yellow")
            blast.CanCollide=false; blast.Anchored=true
            blast.Size=Vector3.new(1,1,1); blast.Transparency=0.05
            blast.Position=hrp.Position+Vector3.new(0,1,0); blast.Parent=workspace
            local blastLight=Instance.new("PointLight")
            blastLight.Brightness=60; blastLight.Range=90
            blastLight.Color=Color3.fromRGB(255,220,0); blastLight.Parent=blast

            
            local ring = Instance.new("Part")
            ring.Name="SSJRing"; ring.Material=Enum.Material.Neon
            ring.BrickColor=BrickColor.new("Bright yellow")
            ring.CanCollide=false; ring.Anchored=true
            ring.Size=Vector3.new(0.4,1,1); ring.Transparency=0.05
            ring.CFrame=CFrame.new(hrp.Position+Vector3.new(0,0.5,0))*CFrame.Angles(0,0,math.pi/2)
            ring.Parent=workspace

            
            for i=1,6 do
                local pe=Instance.new("ParticleEmitter")
                pe.Texture="rbxassetid://242563111"
                pe.Lifetime=NumberRange.new(0.3,1.2)
                pe.Rate=200; pe.Speed=NumberRange.new(40,90)
                pe.Size=NumberSequence.new({
                    NumberSequenceKeypoint.new(0,3),
                    NumberSequenceKeypoint.new(0.4,4),
                    NumberSequenceKeypoint.new(1,0)
                })
                pe.Color=ColorSequence.new({
                    ColorSequenceKeypoint.new(0,Color3.fromRGB(255,235,0)),
                    ColorSequenceKeypoint.new(1,Color3.fromRGB(255,255,200))
                })
                pe.Parent=blast
            end

            
            _ssjExpSfx=Instance.new("Sound")
            _ssjExpSfx.SoundId="rbxassetid://"..tostring(A_SSJ_SFX)
            _ssjExpSfx.Volume=1.4; _ssjExpSfx.Parent=hrp; _ssjExpSfx:Play()

            
            TW:Create(blast,TweenInfo.new(0.45,Enum.EasingStyle.Quad,Enum.EasingDirection.Out),
                {Size=Vector3.new(14,14,14),Transparency=0.72}):Play()
            TW:Create(ring,TweenInfo.new(0.5,Enum.EasingStyle.Quad,Enum.EasingDirection.Out),
                {Size=Vector3.new(0.15,18,18),Transparency=0.88}):Play()
            TW:Create(flashF,TweenInfo.new(0.12),{BackgroundTransparency=0.2}):Play()

            task.wait(0.12)
            
            TW:Create(flashF,TweenInfo.new(0.55,Enum.EasingStyle.Quad),{BackgroundTransparency=1}):Play()
            TW:Create(blastLight,TweenInfo.new(0.8),{Brightness=5,Range=20}):Play()

            task.wait(1.08)  
            pcall(function() blast:Destroy() end)
            pcall(function() ring:Destroy() end)
            pcall(function() flashGui:Destroy() end)
            pcall(function() _ssjExpSfx:Stop(); _ssjExpSfx:Destroy() end); _ssjExpSfx=nil

            
            if ssjEmoteTrack then
                pcall(function() ssjEmoteTrack:Stop(0) end)
                ssjEmoteTrack = nil
            end
            local hum3 = _hum()
            if hum3 then
                pcall(function() hum3:Move(Vector3.new(1,0,0), false) end)
                task.wait(0.05)
                pcall(function() hum3:Move(Vector3.new(0,0,0), false) end)
            end

            cam.CameraType = origCT

            
            do
                local RS2b  = game:GetService("ReplicatedStorage")
                local Rem2  = RS2b:FindFirstChild("Remotes") or RS2b:FindFirstChild("RE")
                local wR    = Rem2 and Rem2:FindFirstChild("Wear")
                if wR then
                    pcall(function() wR:InvokeServer(GOKU_HEAD_ID) end)
                    task.wait(0.15)
                    pcall(function() wR:InvokeServer(SSJ_HEAD_ID)  end)
                end
            end

            
            local hl = Instance.new("Highlight"); hl.Name="SSJHighlight"; hl.Adornee=char
            hl.FillColor=Color3.fromRGB(255,220,0); hl.OutlineColor=Color3.fromRGB(255,255,150)
            hl.FillTransparency=0.88; hl.OutlineTransparency=0.2; hl.Parent=char

            
            local kl=Instance.new("PointLight"); kl.Name="SSJLight"
            kl.Color=Color3.fromRGB(255,220,0); kl.Brightness=1.5; kl.Range=12; kl.Parent=hrp

            
            local sparks = Instance.new("ParticleEmitter"); sparks.Name="SSJSparks"
            sparks.Texture="rbxassetid://242563111"
            sparks.Lifetime=NumberRange.new(0.1,0.4)
            sparks.Rate=25; sparks.Speed=NumberRange.new(2,8)
            sparks.Size=NumberSequence.new({NumberSequenceKeypoint.new(0,0.4),NumberSequenceKeypoint.new(1,0)})
            sparks.Color=ColorSequence.new(Color3.fromRGB(255,240,100))
            sparks.Parent=hrp

            
            hum.WalkSpeed = 120
            hum.JumpHeight = 45

            local an=char:FindFirstChild("Animate")
            if an then an.Disabled=true; task.wait(0.1); an.Disabled=false end

            _notif("Goku (FE)", "Super Saiyan! Irreversible - Remove Goku to reset.")
        end

        
        local function _startKiCharge()
            if not _gokuActive or _kiCharging then return end
            local hum  = _hum(); if not hum then return end
            local desc = hum:FindFirstChildOfClass("HumanoidDescription")
            if not desc then return end
            _kiCharging = true

            local emoteId   = A_KI_CHARGE
            local emoteName = "GokuKi"

            
            pcall(function()
                local list = desc:GetEmotes()
                if not list[emoteName] then desc:AddEmote(emoteName, emoteId) end
            end)

            
            task.spawn(function()
                while _kiCharging do
                    local hum2 = _hum()
                    if hum2 then
                        pcall(function()
                            local ok, track = pcall(function()
                                return hum2:PlayEmoteAndGetAnimTrackById(emoteId)
                            end)
                            if ok and track then
                                _kiTrack = track
                                _kiTrack.Priority = Enum.AnimationPriority.Action4
                                
                                track.Stopped:Wait()
                            end
                        end)
                    end
                    if _kiCharging then task.wait(0.05) end
                end
            end)

            
            local hrp2 = _hrp()
            if hrp2 then
                local cl = Instance.new("PointLight"); cl.Name="KiChargeLight"
                cl.Color=Color3.fromRGB(100,180,255); cl.Brightness=3; cl.Range=14; cl.Parent=hrp2
            end
        end

        local function _stopKiCharge()
            if not _kiCharging then return end
            _kiCharging = false  
            
            if _kiTrack then
                pcall(function() _kiTrack:Stop(0) end)
                _kiTrack = nil
            end
            local hum2  = _hum()
            local char2 = _char()
            
            if hum2 then
                local desc2 = hum2:FindFirstChildOfClass("HumanoidDescription")
                if desc2 then pcall(function() desc2:RemoveEmote("GokuKi") end) end
            end
            
            if char2 then
                local an = char2:FindFirstChild("Animate")
                if an then
                    an.Disabled = true
                    task.wait(0.05)
                    an.Disabled = false
                end
            end
            
            if hum2 then
                pcall(function() hum2:Move(Vector3.new(1,0,0), false) end)
                task.wait(0.05)
                pcall(function() hum2:Move(Vector3.new(0,0,0), false) end)
            end
            local hrp2 = _hrp()
            if hrp2 and hrp2:FindFirstChild("KiChargeLight") then hrp2.KiChargeLight:Destroy() end
        end

        
        local function _createTpGui()
            if _tpGui then _tpGui:Destroy() end
            local lp  = _lp()
            local TW  = game:GetService("TweenService")
            local PLR = game:GetService("Players")

            _tpGui = Instance.new("ScreenGui")
            _tpGui.Name           = "GokuTpGui"
            _tpGui.ResetOnSpawn   = false
            _tpGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
            _tpGui.IgnoreGuiInset = true
            _tpGui.Parent         = lp.PlayerGui

            
            local _hiddenGuiList = {}
            local _SG = game:GetService("StarterGui")
            for _, sg in ipairs(lp.PlayerGui:GetChildren()) do
                if sg:IsA("ScreenGui") and sg ~= _tpGui and sg.Enabled then
                    pcall(function() sg.Enabled = false end)
                    table.insert(_hiddenGuiList, sg)
                end
            end
            pcall(function() _SG:SetCoreGuiEnabled(Enum.CoreGuiType.Backpack,    false) end)
            pcall(function() _SG:SetCoreGuiEnabled(Enum.CoreGuiType.Chat,         false) end)
            pcall(function() _SG:SetCoreGuiEnabled(Enum.CoreGuiType.PlayerList,   false) end)
            pcall(function() _SG:SetCoreGuiEnabled(Enum.CoreGuiType.Health,       false) end)
            local _guisRestored = false
            local function _restoreAllGuis()
                if _guisRestored then return end
                _guisRestored = true
                for _, sg in ipairs(_hiddenGuiList) do
                    pcall(function() if sg and sg.Parent then sg.Enabled = true end end)
                end
                pcall(function() _SG:SetCoreGuiEnabled(Enum.CoreGuiType.Backpack,    true) end)
                pcall(function() _SG:SetCoreGuiEnabled(Enum.CoreGuiType.Chat,         true) end)
                pcall(function() _SG:SetCoreGuiEnabled(Enum.CoreGuiType.PlayerList,   true) end)
                pcall(function() _SG:SetCoreGuiEnabled(Enum.CoreGuiType.Health,       true) end)
            end
            local _ancestryConn = _tpGui.AncestryChanged:Connect(function(_, parent)
                if parent == nil then _restoreAllGuis() end
            end)

            local blackout = Instance.new("Frame")
            blackout.Size                 = UDim2.new(1,0,1,0)
            blackout.BackgroundColor3     = Color3.fromRGB(0,0,0)
            blackout.BackgroundTransparency = 1
            blackout.BorderSizePixel      = 0
            blackout.ZIndex               = 20
            blackout.Parent               = _tpGui
            TW:Create(blackout, TweenInfo.new(0.65, Enum.EasingStyle.Quart, Enum.EasingDirection.InOut),
                {BackgroundTransparency = 0}):Play()

            task.spawn(function()
                task.wait(0.65)
                if not _tpGui then return end

                local kiVision = Instance.new("Frame")
                kiVision.Size                 = UDim2.new(1,0,1,0)
                kiVision.BackgroundColor3     = Color3.fromRGB(4, 2, 14)
                kiVision.BackgroundTransparency = 1
                kiVision.BorderSizePixel      = 0
                kiVision.ZIndex               = 19
                kiVision.Parent               = _tpGui
                local vigGrad = Instance.new("UIGradient")
                vigGrad.Color = ColorSequence.new({
                    ColorSequenceKeypoint.new(0,    Color3.fromRGB(4,2,14)),
                    ColorSequenceKeypoint.new(0.55, Color3.fromRGB(4,2,14)),
                    ColorSequenceKeypoint.new(1,    Color3.fromRGB(0,0,0))
                })
                vigGrad.Transparency = NumberSequence.new({
                    NumberSequenceKeypoint.new(0,   0),
                    NumberSequenceKeypoint.new(0.6, 0),
                    NumberSequenceKeypoint.new(1,   0.3)
                })
                vigGrad.Rotation = 90; vigGrad.Parent = kiVision
                TW:Create(kiVision, TweenInfo.new(0.35), {BackgroundTransparency = 0}):Play()
                TW:Create(blackout, TweenInfo.new(0.35), {BackgroundTransparency = 1}):Play()

                task.wait(0.25)
                if not _tpGui then return end

                local senseLbl = Instance.new("TextLabel")
                senseLbl.Size               = UDim2.new(0,400,0,28)
                senseLbl.Position           = UDim2.new(0.5,-200,0.5,-14)
                senseLbl.BackgroundTransparency = 1
                senseLbl.Text               = ""
                senseLbl.TextColor3         = Color3.fromRGB(255, 195, 30)
                senseLbl.Font               = Enum.Font.GothamBold
                senseLbl.TextSize           = 15
                senseLbl.TextTransparency   = 0
                senseLbl.TextXAlignment     = Enum.TextXAlignment.Center
                senseLbl.ZIndex             = 21
                senseLbl.Parent             = _tpGui
                local full = "Sensing ki signatures..."
                for i = 1, #full do
                    if not _tpGui then return end
                    senseLbl.Text = full:sub(1,i)
                    task.wait(0.035)
                end
                task.wait(0.35)
                TW:Create(senseLbl, TweenInfo.new(0.3), {TextTransparency = 1}):Play()
                task.wait(0.3)
                if not _tpGui then return end

                local myHRP = _hrp()
                local others = {}
                for _, pl in ipairs(PLR:GetPlayers()) do
                    if pl ~= lp then table.insert(others, pl) end
                end
                if myHRP then
                    table.sort(others, function(a, b)
                        local aHRP = a.Character and a.Character:FindFirstChild("HumanoidRootPart")
                        local bHRP = b.Character and b.Character:FindFirstChild("HumanoidRootPart")
                        local ad = aHRP and (myHRP.Position-aHRP.Position).Magnitude or math.huge
                        local bd = bHRP and (myHRP.Position-bHRP.Position).Magnitude or math.huge
                        return ad < bd
                    end)
                end

                local RS2    = game:GetService("RunService")
                local cam    = workspace.CurrentCamera
                local origCT = cam.CameraType
                cam.CameraType = Enum.CameraType.Scriptable
                local selectedIdx = 1
                local navConn = nil

                local function focusOnPlayer(idx)
                    local tgt  = others[idx]
                    if not tgt or not myHRP then return end
                    local pHRP   = tgt.Character and tgt.Character:FindFirstChild("HumanoidRootPart")
                    local myPos  = myHRP.Position
                    local tgtPos = pHRP and pHRP.Position or (myPos + Vector3.new(0,2,-20))
                    local dir    = (tgtPos - myPos)
                    dir = dir.Magnitude > 0.5 and dir.Unit or Vector3.new(0,0,-1)
                    local camPos = myPos + Vector3.new(0,4.5,0) - dir*9
                    local toCF   = CFrame.lookAt(camPos, tgtPos + Vector3.new(0,2,0))
                    local fromCF = cam.CFrame
                    if navConn then navConn:Disconnect() end
                    local elapsed = 0
                    navConn = RS2.RenderStepped:Connect(function(dt)
                        if not _tpGui then if navConn then navConn:Disconnect() end; return end
                        elapsed = elapsed + dt
                        local t = math.clamp(elapsed/0.55, 0, 1)
                        cam.CFrame = fromCF:Lerp(toCF, 1-(1-t)^3)
                        if elapsed >= 0.55 then navConn:Disconnect(); navConn=nil end
                    end)
                end

                
                local BAR_H = 0.13
                local barTopSel = Instance.new("Frame")
                barTopSel.Size = UDim2.new(1,0,0,0); barTopSel.Position = UDim2.new(0,0,0,0)
                barTopSel.BackgroundColor3 = Color3.fromRGB(0,0,0); barTopSel.BackgroundTransparency = 0
                barTopSel.BorderSizePixel = 0; barTopSel.ZIndex = 30; barTopSel.Parent = _tpGui
                local barBotSel = Instance.new("Frame")
                barBotSel.Size = UDim2.new(1,0,0,0); barBotSel.Position = UDim2.new(0,0,1,0)
                barBotSel.BackgroundColor3 = Color3.fromRGB(0,0,0); barBotSel.BackgroundTransparency = 0
                barBotSel.BorderSizePixel = 0; barBotSel.ZIndex = 30; barBotSel.Parent = _tpGui
                TW:Create(barTopSel, TweenInfo.new(0.4, Enum.EasingStyle.Quint), {Size = UDim2.new(1,0,BAR_H,0)}):Play()
                TW:Create(barBotSel, TweenInfo.new(0.4, Enum.EasingStyle.Quint), {
                    Size = UDim2.new(1,0,BAR_H,0), Position = UDim2.new(0,0,1-BAR_H,0)
                }):Play()
                task.wait(0.4)
                if not _tpGui then cam.CameraType=origCT; return end

                
                local targetNameLbl = Instance.new("TextLabel")
                targetNameLbl.Size = UDim2.new(0,500,0,38)
                targetNameLbl.Position = UDim2.new(0.5,-250,BAR_H+0.04,0)
                targetNameLbl.BackgroundTransparency = 1
                targetNameLbl.Text = ""
                targetNameLbl.TextColor3 = Color3.fromRGB(255,255,255)
                targetNameLbl.Font = Enum.Font.GothamBold
                targetNameLbl.TextSize = 28
                targetNameLbl.TextXAlignment = Enum.TextXAlignment.Center
                targetNameLbl.TextTruncate = Enum.TextTruncate.None
                targetNameLbl.TextTransparency = 0.1
                targetNameLbl.ZIndex = 29
                targetNameLbl.Parent = _tpGui

                local kiBarBg = Instance.new("Frame")
                kiBarBg.Size = UDim2.new(0,250,0,5)
                kiBarBg.Position = UDim2.new(0.5,-125,BAR_H+0.115,0)
                kiBarBg.BackgroundColor3 = Color3.fromRGB(255,255,255)
                kiBarBg.BackgroundTransparency = 0.82
                kiBarBg.BorderSizePixel = 0; kiBarBg.ZIndex = 29; kiBarBg.Parent = _tpGui
                Instance.new("UICorner",kiBarBg).CornerRadius = UDim.new(0,3)

                local kiBarFill = Instance.new("Frame")
                kiBarFill.Size = UDim2.new(0.75,0,1,0)
                kiBarFill.BackgroundColor3 = Color3.fromRGB(100,185,255)
                kiBarFill.BackgroundTransparency = 0.1
                kiBarFill.BorderSizePixel = 0; kiBarFill.ZIndex = 30; kiBarFill.Parent = kiBarBg
                Instance.new("UICorner",kiBarFill).CornerRadius = UDim.new(0,3)

                local kiOverlayLbl = Instance.new("TextLabel")
                kiOverlayLbl.Size = UDim2.new(0,250,0,14)
                kiOverlayLbl.Position = UDim2.new(0.5,-125,BAR_H+0.155,0)
                kiOverlayLbl.BackgroundTransparency = 1; kiOverlayLbl.Text = "KI POWER"
                kiOverlayLbl.TextColor3 = Color3.fromRGB(150,200,255)
                kiOverlayLbl.Font = Enum.Font.Gotham; kiOverlayLbl.TextSize = 10
                kiOverlayLbl.TextXAlignment = Enum.TextXAlignment.Center
                kiOverlayLbl.ZIndex = 29; kiOverlayLbl.Parent = _tpGui

                
                task.spawn(function()
                    while _tpGui do
                        TW:Create(kiBarFill,TweenInfo.new(1.1,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut),{BackgroundTransparency=0.5}):Play()
                        task.wait(1.1)
                        if not _tpGui then break end
                        TW:Create(kiBarFill,TweenInfo.new(1.1,Enum.EasingStyle.Sine,Enum.EasingDirection.InOut),{BackgroundTransparency=0.05}):Play()
                        task.wait(1.1)
                    end
                end)

                
                local accentTop = Instance.new("Frame")
                accentTop.Size=UDim2.new(0,0,0,1); accentTop.Position=UDim2.new(0,0,1,-1)
                accentTop.BackgroundColor3=Color3.fromRGB(255,195,30); accentTop.BackgroundTransparency=0.2
                accentTop.BorderSizePixel=0; accentTop.ZIndex=31; accentTop.Parent=barTopSel
                TW:Create(accentTop,TweenInfo.new(0.55,Enum.EasingStyle.Quad),{Size=UDim2.new(1,0,0,1)}):Play()

                local topTitle = Instance.new("TextLabel")
                topTitle.Size=UDim2.new(0.55,0,0.65,0); topTitle.Position=UDim2.new(0,24,0.1,0)
                topTitle.BackgroundTransparency=1; topTitle.Text="INSTANT TRANSMISSION"
                topTitle.TextColor3=Color3.fromRGB(255,215,70); topTitle.Font=Enum.Font.GothamBold
                topTitle.TextSize=20; topTitle.TextXAlignment=Enum.TextXAlignment.Left
                topTitle.TextYAlignment=Enum.TextYAlignment.Center
                topTitle.TextTransparency=1; topTitle.ZIndex=31; topTitle.Parent=barTopSel
                TW:Create(topTitle,TweenInfo.new(0.35),{TextTransparency=0}):Play()

                local counterLbl = Instance.new("TextLabel")
                counterLbl.Size=UDim2.new(0,200,0.65,0); counterLbl.Position=UDim2.new(1,-220,0.1,0)
                counterLbl.BackgroundTransparency=1; counterLbl.Text=""
                counterLbl.TextColor3=Color3.fromRGB(175,155,255); counterLbl.Font=Enum.Font.Gotham
                counterLbl.TextSize=13; counterLbl.TextXAlignment=Enum.TextXAlignment.Right
                counterLbl.TextYAlignment=Enum.TextYAlignment.Center
                counterLbl.TextTransparency=1; counterLbl.ZIndex=31; counterLbl.Parent=barTopSel
                TW:Create(counterLbl,TweenInfo.new(0.35),{TextTransparency=0}):Play()

                
                local accentBot = Instance.new("Frame")
                accentBot.Size=UDim2.new(0,0,0,1); accentBot.Position=UDim2.new(0,0,0,0)
                accentBot.BackgroundColor3=Color3.fromRGB(255,195,30); accentBot.BackgroundTransparency=0.2
                accentBot.BorderSizePixel=0; accentBot.ZIndex=31; accentBot.Parent=barBotSel
                TW:Create(accentBot,TweenInfo.new(0.55,Enum.EasingStyle.Quad),{Size=UDim2.new(1,0,0,1)}):Play()

                local function makeArrow(sym, xOff)
                    local btn=Instance.new("TextButton")
                    btn.Size=UDim2.new(0,50,0,50); btn.Position=UDim2.new(0.5,xOff,0.5,-25)
                    btn.BackgroundColor3=Color3.fromRGB(255,255,255); btn.BackgroundTransparency=0.88
                    btn.Text=sym; btn.TextColor3=Color3.fromRGB(255,215,70)
                    btn.Font=Enum.Font.GothamBold; btn.TextSize=26
                    btn.AutoButtonColor=false; btn.ZIndex=31; btn.Parent=barBotSel
                    Instance.new("UICorner",btn).CornerRadius=UDim.new(0,10)
                    local st=Instance.new("UIStroke"); st.Color=Color3.fromRGB(255,195,30)
                    st.Thickness=1; st.Transparency=0.5; st.Parent=btn
                    btn.MouseEnter:Connect(function() TW:Create(btn,TweenInfo.new(0.1),{BackgroundTransparency=0.6}):Play() end)
                    btn.MouseLeave:Connect(function() TW:Create(btn,TweenInfo.new(0.1),{BackgroundTransparency=0.88}):Play() end)
                    return btn
                end
                local leftBtn  = makeArrow("\226\151\128", -210)
                local rightBtn = makeArrow("\226\150\182",  160)

                local centerName=Instance.new("TextLabel")
                centerName.Size=UDim2.new(0,280,0,28); centerName.Position=UDim2.new(0.5,-140,0.07,0)
                centerName.BackgroundTransparency=1; centerName.Text=""
                centerName.TextColor3=Color3.fromRGB(235,228,255); centerName.Font=Enum.Font.GothamBold
                centerName.TextSize=17; centerName.TextXAlignment=Enum.TextXAlignment.Center
                centerName.TextTruncate=Enum.TextTruncate.None; centerName.ZIndex=31; centerName.Parent=barBotSel

                local distLblSel=Instance.new("TextLabel")
                distLblSel.Size=UDim2.new(0,280,0,16); distLblSel.Position=UDim2.new(0.5,-140,0.38,0)
                distLblSel.BackgroundTransparency=1; distLblSel.Text=""
                distLblSel.TextColor3=Color3.fromRGB(175,155,255); distLblSel.Font=Enum.Font.Gotham
                distLblSel.TextSize=12; distLblSel.TextXAlignment=Enum.TextXAlignment.Center
                distLblSel.ZIndex=31; distLblSel.Parent=barBotSel

                local tpBtn=Instance.new("TextButton")
                tpBtn.Size=UDim2.new(0,200,0,44); tpBtn.Position=UDim2.new(0.5,-100,1,-54)
                tpBtn.BackgroundColor3=Color3.fromRGB(255,195,30); tpBtn.BackgroundTransparency=0.1
                tpBtn.Text="TELEPORT"; tpBtn.TextColor3=Color3.fromRGB(0,0,0)
                tpBtn.Font=Enum.Font.GothamBold; tpBtn.TextSize=15
                tpBtn.AutoButtonColor=false; tpBtn.ZIndex=31; tpBtn.Parent=barBotSel
                Instance.new("UICorner",tpBtn).CornerRadius=UDim.new(0,10)
                tpBtn.MouseEnter:Connect(function()
                    TW:Create(tpBtn,TweenInfo.new(0.1),{BackgroundTransparency=0,BackgroundColor3=Color3.fromRGB(255,215,40)}):Play()
                end)
                tpBtn.MouseLeave:Connect(function()
                    TW:Create(tpBtn,TweenInfo.new(0.1),{BackgroundTransparency=0.1,BackgroundColor3=Color3.fromRGB(255,195,30)}):Play()
                end)

                local cancelHint=Instance.new("TextLabel")
                cancelHint.Size=UDim2.new(1,0,0,12); cancelHint.Position=UDim2.new(0,0,1,-14)
                cancelHint.BackgroundTransparency=1; cancelHint.Text="Unequip tool to cancel"
                cancelHint.TextColor3=Color3.fromRGB(70,60,90); cancelHint.Font=Enum.Font.Gotham
                cancelHint.TextSize=10; cancelHint.TextXAlignment=Enum.TextXAlignment.Center
                cancelHint.ZIndex=31; cancelHint.Parent=barBotSel

                local function updateNav()
                    if #others == 0 then
                        centerName.Text="No ki signatures detected"; centerName.TextColor3=Color3.fromRGB(120,110,160)
                        distLblSel.Text=""; tpBtn.Visible=false; leftBtn.Visible=false; rightBtn.Visible=false
                        counterLbl.Text="0 KI SIGNATURES"; return
                    end
                    local tgt  = others[selectedIdx]
                    local pHRP = tgt.Character and tgt.Character:FindFirstChild("HumanoidRootPart")
                    local dist = (pHRP and myHRP) and math.floor((myHRP.Position-pHRP.Position).Magnitude) or nil
                    centerName.Text=tgt.Name; centerName.TextColor3=Color3.fromRGB(235,228,255)
                    distLblSel.Text=dist and (dist.." studs away") or "distance unknown"
                    counterLbl.Text=selectedIdx.." / "..#others.."  KI SIGNATURES"
                    
                    targetNameLbl.Text = tgt.Name
                    local kiRatio = 0.45 + (selectedIdx / math.max(#others,1)) * 0.55
                    TW:Create(kiBarFill,TweenInfo.new(0.3),{Size=UDim2.new(kiRatio,0,1,0),BackgroundColor3=(kiRatio>0.75) and Color3.fromRGB(255,215,30) or Color3.fromRGB(100,185,255)}):Play()
                end
                updateNav()
                if #others > 0 then focusOnPlayer(selectedIdx) end

                leftBtn.MouseButton1Click:Connect(function()
                    if #others < 2 then return end
                    selectedIdx=((selectedIdx-2)%#others)+1; updateNav(); focusOnPlayer(selectedIdx)
                end)
                rightBtn.MouseButton1Click:Connect(function()
                    if #others < 2 then return end
                    selectedIdx=(selectedIdx%#others)+1; updateNav(); focusOnPlayer(selectedIdx)
                end)

                
                tpBtn.MouseButton1Click:Connect(function()
                    if _tping or #others == 0 then return end
                    _tping = true
                    if navConn then navConn:Disconnect(); navConn=nil end
                    if _ancestryConn then _ancestryConn:Disconnect(); _ancestryConn=nil end
                    local p = others[selectedIdx]
                    local cGui = nil; local tt = nil

                    local function _safeCleanup()
                        pcall(function() cam.CameraType=origCT end)
                        pcall(function() if tt then tt:Stop() end end)
                        pcall(function() if cGui then cGui:Destroy() end end)
                        _restoreAllGuis()
                        _tping=false
                    end

                    local function moveCam(fromCF, toCF, duration, easeFn)
                        local elapsed=0; local conn
                        conn=RS2.RenderStepped:Connect(function(dt)
                            elapsed=elapsed+dt
                            local raw=math.clamp(elapsed/duration,0,1)
                            local t=easeFn and easeFn(raw) or raw
                            cam.CFrame=fromCF:Lerp(toCF,t)
                            if elapsed>=duration then conn:Disconnect() end
                        end)
                        task.wait(duration)
                        if conn.Connected then conn:Disconnect() end
                    end

                    local function orbitCam(center, radius, startAngle, endAngle, height, duration, easeFn)
                        local elapsed=0; local conn
                        conn=RS2.RenderStepped:Connect(function(dt)
                            elapsed=elapsed+dt
                            local raw=math.clamp(elapsed/duration,0,1)
                            local t=easeFn and easeFn(raw) or raw
                            local angle=startAngle+(endAngle-startAngle)*t
                            local camPos=center+Vector3.new(math.cos(angle)*radius,height,math.sin(angle)*radius)
                            cam.CFrame=CFrame.lookAt(camPos,center+Vector3.new(0,2,0))
                            if elapsed>=duration then conn:Disconnect() end
                        end)
                        task.wait(duration)
                        if conn.Connected then conn:Disconnect() end
                    end

                    local function eSineOut(t)  return math.sin(t*math.pi/2) end
                    local function eExpoIn(t)   return t==0 and 0 or 2^(10*t-10) end
                    local function eExpoOut(t)  return t==1 and 1 or 1-2^(-10*t) end
                    local function eQuintOut(t) return 1-(1-t)^5 end

                    local ok=pcall(function()
                        local lp2=_lp(); local hrp=_hrp(); local hum=_hum(); local char=_char()
                        if not hrp or not hum or not char then return end
                        pcall(function() _tpGui:Destroy() end); _tpGui=nil

                        local ta=Instance.new("Animation")
                        ta.AnimationId="rbxassetid://"..A_TP_EMOTE
                        tt=hum:LoadAnimation(ta); tt:Play()

                        cGui=Instance.new("ScreenGui")
                        cGui.Name="GokuCinematicGui"; cGui.ResetOnSpawn=false
                        cGui.IgnoreGuiInset=true; cGui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
                        cGui.Parent=lp2.PlayerGui

                        local vignette=Instance.new("Frame")
                        vignette.Size=UDim2.new(1,0,1,0); vignette.BackgroundColor3=Color3.fromRGB(0,0,0)
                        vignette.BackgroundTransparency=0.7; vignette.BorderSizePixel=0; vignette.ZIndex=2; vignette.Parent=cGui

                        local auraColor=_ssj and Color3.fromRGB(255,215,0) or Color3.fromRGB(100,180,255)
                        local auraFrame=Instance.new("Frame")
auraFrame.Size=UDim2.new(1,0,1,0)
                        auraFrame.BackgroundColor3=auraColor
auraFrame.BackgroundTransparency=1
                        auraFrame.BorderSizePixel=0
                        auraFrame.ZIndex=4
                        auraFrame.Parent=cGui

                        local flashColor=_ssj and Color3.fromRGB(255,240,100) or Color3.fromRGB(255,255,255)
                        local whiteFlash=Instance.new("Frame")
whiteFlash.Size=UDim2.new(1,0,1,0)
                        whiteFlash.BackgroundColor3=flashColor
whiteFlash.BackgroundTransparency=1
                        whiteFlash.BorderSizePixel=0
                        whiteFlash.ZIndex=5
                        whiteFlash.Parent=cGui

                        local blackCover=Instance.new("Frame")
blackCover.Size=UDim2.new(1,0,1,0)
                        blackCover.BackgroundColor3=Color3.fromRGB(0,0,0)
blackCover.BackgroundTransparency=1
                        blackCover.BorderSizePixel=0
                        blackCover.ZIndex=6
                        blackCover.Parent=cGui

                        local nameLabel=Instance.new("TextLabel")
nameLabel.Size=UDim2.new(1,0,0,38)
                        nameLabel.Position=UDim2.new(0,0,0.5,-19)
                        nameLabel.BackgroundTransparency=1
                        nameLabel.Text=p.DisplayName~="" and p.DisplayName or p.Name
                        nameLabel.TextColor3=_ssj and Color3.fromRGB(255,215,0) or Color3.fromRGB(255,255,255)
nameLabel.Font=Enum.Font.GothamBold
                        nameLabel.TextSize=26
nameLabel.TextTransparency=1
                        nameLabel.ZIndex=7
                        nameLabel.Parent=cGui

                        local barColor=_ssj and Color3.fromRGB(15,10,0) or Color3.fromRGB(0,0,0)
                        local BAR_H2=0.115
                        local barTop2=Instance.new("Frame")
barTop2.Size=UDim2.new(1,0,BAR_H2,0)
                        barTop2.Position=UDim2.new(0,0,-BAR_H2,0)
barTop2.BackgroundColor3=barColor
                        barTop2.BackgroundTransparency=0
barTop2.BorderSizePixel=0
                        barTop2.ZIndex=10
                        barTop2.Parent=cGui
                        local barBot2=Instance.new("Frame")
barBot2.Size=UDim2.new(1,0,BAR_H2,0)
                        barBot2.Position=UDim2.new(0,0,1,0)
barBot2.BackgroundColor3=barColor
                        barBot2.BackgroundTransparency=0
barBot2.BorderSizePixel=0
                        barBot2.ZIndex=10
                        barBot2.Parent=cGui

                        local function tweenUI(obj,props,t,style,dir)
                            style=style or Enum.EasingStyle.Quad; dir=dir or Enum.EasingDirection.Out
                            TW:Create(obj,TweenInfo.new(t,style,dir),props):Play()
                        end

                        cam.CameraType=Enum.CameraType.Scriptable
                        local cPos=hrp.Position; local cLook=hrp.CFrame.LookVector; local cRight=hrp.CFrame.RightVector

                        tweenUI(barTop2,{Position=UDim2.new(0,0,0,0)},        0.35,Enum.EasingStyle.Quint)
                        tweenUI(barBot2,{Position=UDim2.new(0,0,1-BAR_H2,0)}, 0.35,Enum.EasingStyle.Quint)

                        if _ssj then
                            tweenUI(barTop2,{BackgroundColor3=Color3.fromRGB(55,40,0)},0.5)
                            tweenUI(barBot2,{BackgroundColor3=Color3.fromRGB(55,40,0)},0.5)
                            cam.CFrame=CFrame.lookAt(cPos+cLook*4+Vector3.new(0,-2,0),cPos+Vector3.new(0,3,0))
                            task.wait(0.45)
                            local startAngle=math.atan2(-cLook.Z,cLook.X)+math.pi
                            orbitCam(cPos,6.5,startAngle,startAngle+math.pi*2,3.5,2.0,eExpoOut)
                            local ssj_zoomEnd=CFrame.lookAt(cPos+cLook*2.5+cRight*0.3+Vector3.new(0,2.8,0),cPos+Vector3.new(0,2.4,0))
                            moveCam(cam.CFrame,ssj_zoomEnd,0.4,eExpoIn)
                            tweenUI(auraFrame,{BackgroundTransparency=0.4},0.12); task.wait(0.12)
                            tweenUI(auraFrame,{BackgroundTransparency=0.2},0.12); task.wait(0.12)
                            tweenUI(auraFrame,{BackgroundTransparency=0.05},0.12); task.wait(0.12)
                            tweenUI(auraFrame,{BackgroundTransparency=1},0.08)
                            tweenUI(whiteFlash,{BackgroundTransparency=0},0.15); task.wait(0.15)
                            tweenUI(blackCover,{BackgroundTransparency=0},0.08); task.wait(0.08)
                            tweenUI(whiteFlash,{BackgroundTransparency=1},0.1)
                            tweenUI(nameLabel,{TextTransparency=0},0.25)
                        else
                            local n_p1s=CFrame.lookAt(cPos-cLook*9-cRight*4.5+Vector3.new(0,5,0),cPos+Vector3.new(0,2,0))
                            local n_p1e=CFrame.lookAt(cPos-cLook*6-cRight*0.5+Vector3.new(0,4,0),cPos+Vector3.new(0,2,0))
                            cam.CFrame=n_p1s; moveCam(n_p1s,n_p1e,1.2,eSineOut)
                            local n_p2e=CFrame.lookAt(cPos+cRight*4.5-cLook*2.5+Vector3.new(0,2.5,0),cPos+Vector3.new(0,1.5,0))
                            tweenUI(auraFrame,{BackgroundTransparency=0.85},0.3); moveCam(n_p1e,n_p2e,0.7,eSineOut)
                            local n_p3e=CFrame.lookAt(cPos+cLook*2.2+cRight*0.5+Vector3.new(0,2.6,0),cPos+Vector3.new(0,2.2,0))
                            tweenUI(auraFrame,{BackgroundTransparency=0.6},0.3); moveCam(n_p2e,n_p3e,0.45,eExpoIn)
                            task.wait(0.06)
                            local n_p4s=CFrame.lookAt(cPos+Vector3.new(0,-1.5,0)-cLook*3.5,cPos+Vector3.new(0,2.5,0))
                            local n_p4e=CFrame.lookAt(cPos+Vector3.new(0,1.2,0)-cLook*3.5,cPos+Vector3.new(0,2.2,0))
                            moveCam(n_p4s,n_p4e,0.6,eSineOut)
                            tweenUI(auraFrame,{BackgroundTransparency=1},0.15)
                            tweenUI(whiteFlash,{BackgroundTransparency=0.45},0.3); task.wait(0.3)
                            tweenUI(whiteFlash,{BackgroundTransparency=0},0.12); task.wait(0.12)
                            tweenUI(blackCover,{BackgroundTransparency=0},0.08); task.wait(0.08)
                            tweenUI(whiteFlash,{BackgroundTransparency=1},0.1)
                            tweenUI(nameLabel,{TextTransparency=0},0.2)
                        end

                        local savedCF=hrp.CFrame
                        for _,part in ipairs(char:GetDescendants()) do
                            if part:IsA("BasePart") or part:IsA("Decal") then part.Transparency=1 end
                        end
                        hrp.CFrame=CFrame.new(0,10000,0); task.wait(0.55)

                        local tc=p.Character
                        if tc and tc:FindFirstChild("HumanoidRootPart") then
                            hrp.CFrame=tc.HumanoidRootPart.CFrame*CFrame.new(0,0,-5)
                        else hrp.CFrame=savedCF end

                        for _,part in ipairs(char:GetDescendants()) do
                            if part:IsA("BasePart") and part~=hrp then part.Transparency=0
                            elseif part:IsA("Decal") then part.Transparency=0 end
                        end
                        tt:Stop(); tt=nil
                        tweenUI(nameLabel,{TextTransparency=1},0.2)

                        local newPos=hrp.Position; local newLook=hrp.CFrame.LookVector
                        cam.CFrame=CFrame.lookAt(newPos+Vector3.new(0,18,0)-newLook*6,newPos+Vector3.new(0,1,0))
                        tweenUI(blackCover,{BackgroundTransparency=1},0.35,Enum.EasingStyle.Quad)
                        if _ssj then tweenUI(auraFrame,{BackgroundTransparency=0.88},0.2) end
                        local arrMid=CFrame.lookAt(newPos-newLook*10+Vector3.new(0,8,0),newPos+Vector3.new(0,2,0))
                        moveCam(cam.CFrame,arrMid,0.85,eSineOut)
                        if _ssj then tweenUI(auraFrame,{BackgroundTransparency=1},0.3) end
                        local finalCF=CFrame.lookAt(newPos-newLook*6+Vector3.new(0,3,0),newPos+Vector3.new(0,1.5,0))
                        moveCam(arrMid,finalCF,0.6,eQuintOut)
                        tweenUI(barTop2,{Position=UDim2.new(0,0,-BAR_H2,0)},0.35,Enum.EasingStyle.Quint,Enum.EasingDirection.In)
                        tweenUI(barBot2,{Position=UDim2.new(0,0,1,0)},      0.35,Enum.EasingStyle.Quint,Enum.EasingDirection.In)
                        task.wait(0.35)
                        cam.CameraType=origCT
                        pcall(function() cGui:Destroy() end); cGui=nil
                        _restoreAllGuis()
                    end)
                    if _tping then _safeCleanup() end
                end)
            end)
        end

        
        local function _removeGokuTools()
            local lp=_lp(); local bp=lp and lp:FindFirstChild("Backpack"); local char=_char()
            for _, n in ipairs({"GokuFly","GokuTP","GokuSSJ","GokuKi"}) do
                if bp   and bp:FindFirstChild(n)   then bp[n]:Destroy()   end
                if char and char:FindFirstChild(n) then char[n]:Destroy() end
            end
        end

        local function _createGokuTools()
            local lp=_lp(); local bp=lp:WaitForChild("Backpack",5)
            if not bp then return end

            local fly=Instance.new("Tool"); fly.Name="GokuFly"
            fly.TextureId="rbxassetid://138393727967636"; fly.RequiresHandle=false; fly.Parent=bp
            fly.Equipped:Connect(function() if _flying then _stopFlight() else _startFlight() end end)

            local tp=Instance.new("Tool"); tp.Name="GokuTP"
            tp.TextureId="rbxassetid://77738072756366"; tp.RequiresHandle=false; tp.Parent=bp
            tp.Equipped:Connect(function() _createTpGui() end)
            tp.Unequipped:Connect(function() if _tpGui then _tpGui:Destroy() _tpGui=nil end end)

            
            local ssj=Instance.new("Tool"); ssj.Name="GokuSSJ"
            ssj.TextureId="rbxassetid://81438887708519"; ssj.RequiresHandle=false; ssj.Parent=bp
            ssj.Equipped:Connect(function() task.spawn(_activateSSJ) end)

            
            local ki=Instance.new("Tool"); ki.Name="GokuKi"
            ki.TextureId="rbxassetid://6975831171"; ki.RequiresHandle=false; ki.Parent=bp
            ki.Equipped:Connect(function() _startKiCharge() end)
            ki.Unequipped:Connect(function() _stopKiCharge() end)
        end

        
        local function _cleanupGoku()
            _gokuActive=false
            _stopFlight(); _stopKiCharge(); _restoreAnims(); _removeGokuTools()
            if _tpGui then pcall(function() _tpGui:Destroy() end) _tpGui=nil end
            if _sprintGui then pcall(function() _sprintGui:Destroy() end) _sprintGui=nil end
            
            local char2=_char()
            if char2 then
                if char2:FindFirstChild("SSJHighlight") then char2.SSJHighlight:Destroy() end
            end
            local hrp2=_hrp()
            if hrp2 then
                if hrp2:FindFirstChild("SSJLight")    then hrp2.SSJLight:Destroy()    end
                if hrp2:FindFirstChild("SSJSparks")   then hrp2.SSJSparks:Destroy()   end
                if hrp2:FindFirstChild("KiChargeLight") then hrp2.KiChargeLight:Destroy() end
            end
            for _, c in ipairs(_gokuConns) do pcall(function() if c.Connected then c:Disconnect() end end) end
            _gokuConns={}
            _ssj=false; _kiCharging=false; _sprinting=false
            workspace.Gravity=_origGrav
            local hum=_hum()
            if hum then hum.WalkSpeed=16; hum.JumpHeight=7.2; hum.PlatformStand=false end
            _notif("Goku (FE)", "Goku mode removed.")
        end

        local function _startGoku()
            
            local char=_char(); if not char then _notif("Goku (FE)","Character not found.") return end

            
            
            local RS=game:GetService("ReplicatedStorage")
            local Remotes=RS:WaitForChild("Remotes",5)
            local wearRemote = Remotes and Remotes:FindFirstChild("Wear")
            if wearRemote then
                _notif("Goku (FE)", "Applying outfit... ("..#GOKU_OUTFIT.." items, ~"..math.ceil(#GOKU_OUTFIT*2.1).."s)")
                for i, id in ipairs(GOKU_OUTFIT) do
                    pcall(function() wearRemote:InvokeServer(id) end)
                    if i < #GOKU_OUTFIT then task.wait(2.1) end 
                end
                task.wait(0.5) 
            else
                _notif("Goku (FE)", "Wear remote not found - tools only.")
            end

            _gokuActive=true

            
            _createGokuTools()

            
            _loadCustomAnims()

            _notif("Goku (FE)", "Goku activated! Tools: Fly / TP / Super Saiyan / Ki Charge")
        end

        MorphFETab:AddButton({
            Name = "Become Goku (FE)",
            Description = "Equips Goku outfit, then gives Fly + Teleport + Super Saiyan + Ki Charge tools.",
            Callback = function() task.spawn(_startGoku) end
        })

        
    end
    

    
    MorphFETab:AddSection({
        "> Dragon Ball (FE)"
    })

    do
        local function _launchDBZScript(name, url)
            task.spawn(function()
                pcall(function()
                    game:GetService("StarterGui"):SetCore("SendNotification", {
                        Title = name,
                        Text = "Loading full " .. name .. " morph & animations...",
                        Duration = 3
                    })
                end)
                local scriptCode = nil
                local fetchOk, fetchErr = pcall(function()
                    scriptCode = game:HttpGet(url)
                end)
                if not fetchOk or not scriptCode or #scriptCode < 100 then
                    pcall(function()
                        game:GetService("StarterGui"):SetCore("SendNotification", {
                            Title = name,
                            Text = "Failed to fetch script from server!",
                            Duration = 4
                        })
                    end)
                    return
                end

                local fn, compileErr = loadstring(scriptCode)
                if not fn then
                    pcall(function()
                        game:GetService("StarterGui"):SetCore("SendNotification", {
                            Title = name,
                            Text = "Compile error: " .. tostring(compileErr),
                            Duration = 4
                        })
                    end)
                    return
                end

                local execOk, execErr = pcall(fn)
                if not execOk then
                    pcall(function()
                        game:GetService("StarterGui"):SetCore("SendNotification", {
                            Title = name,
                            Text = "Exec error: " .. tostring(execErr),
                            Duration = 4
                        })
                    end)
                else
                    pcall(function()
                        game:GetService("StarterGui"):SetCore("SendNotification", {
                            Title = name,
                            Text = name .. " fully loaded with all animations & moves!",
                            Duration = 3
                        })
                    end)
                end
            end)
        end

        MorphFETab:AddButton({
            Name = "Raditz (FE)",
            Title = "Raditz (FE)",
            Description = "Full FE Raditz morph with complete animations, combat moves, and sound effects.",
            Callback = function()
                _launchDBZScript("Raditz (FE)", "https://pastefy.app/794IbSu4/raw")
            end
        })

        MorphFETab:AddButton({
            Name = "Saibaman (FE)",
            Title = "Saibaman (FE)",
            Description = "Full FE Saibaman morph with custom animations, acid moves, and attacks.",
            Callback = function()
                _launchDBZScript("Saibaman (FE)", "https://pastefy.app/W2ZF4UWI/raw")
            end
        })

        MorphFETab:AddButton({
            Name = "Tenshinhan (FE)",
            Title = "Tenshinhan (FE)",
            Description = "Full FE Tenshinhan morph with all animations, Dodonpa, and combat moves.",
            Callback = function()
                _launchDBZScript("Tenshinhan (FE)", "https://pastefy.app/0MlMSov8/raw")
            end
        })

        MorphFETab:AddButton({
            Name = "Piccolo (FE)",
            Title = "Piccolo (FE)",
            Description = "Full FE Piccolo morph with custom animations, Special Beam Cannon, and attacks.",
            Callback = function()
                _launchDBZScript("Piccolo (FE)", "https://pastefy.app/uaR9iG14/raw")
            end
        })

        MorphFETab:AddButton({
            Name = "Yamcha (FE)",
            Title = "Yamcha (FE)",
            Description = "Full FE Yamcha morph with Wolf Fang Fist, custom animations, and attacks.",
            Callback = function()
                _launchDBZScript("Yamcha (FE)", "https://pastefy.app/KgFLiFxj/raw")
            end
        })
    end
    

    
    
    MorphFETab:AddSection({
        "> Sonic.Exe (FE)"
    })
    MorphFETab:AddButton({
        Name = "Transform Into Sonic.Exe",
        Description = "Sonic.Exe outfit + original headless script",
        Callback = function()
            task.spawn(function()
                local RS = game:GetService("ReplicatedStorage")
                local Remotes = RS:FindFirstChild("Remotes")
                local wearRemote = Remotes and Remotes:FindFirstChild("Wear")
                if not wearRemote then _notif("Sonic.Exe", "Wear remote not found!") return end

                _notif("Sonic.Exe", "Applying outfit... (~7s)")
                for i, id in ipairs({84753470271364, 81802203287969, 15093053680}) do
                    pcall(function() wearRemote:InvokeServer(id) end)
                    if i < 3 then task.wait(2.1) end
                end
                task.wait(2.0) 

                _notif("Sonic.Exe", "Running original script...")
                pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/mxskf/Legal/refs/heads/main/Dboas86"))()
                end)
                _notif("Sonic.Exe", "Sonic.Exe is ready!")
            end)
        end
    })

    
    MorphFETab:AddSection({
        "> Faker (FE)"
    })
    MorphFETab:AddButton({
        Name = "Transform Into Faker",
        Description = "Faker outfit + retro glitch animations",
        Callback = function()
            _wearOutfit({89163989490061, 91151766440767, 14604029285}, "Faker (FE)")
            task.spawn(function()
                local p = game:GetService("Players").LocalPlayer
                local char = p.Character or p.CharacterAdded:Wait()
                local hum = char:WaitForChild("Humanoid")

                local function playRetroTrack(track)
                    if not track then return end
                    if track.IsPlaying then track:Stop() end
                    track:Play()
                    track:AdjustSpeed(3)
                    task.spawn(function()
                        while track.IsPlaying do
                            task.wait(0.1 + math.random() * 0.1)
                            if track.IsPlaying then
                                track:AdjustSpeed(0)
                                task.wait(0.15)
                                track:AdjustSpeed(3)
                            end
                        end
                    end)
                end

                local animator = hum:FindFirstChildOfClass("Animator")
                if not animator then
                    animator = Instance.new("Animator")
                    animator.Parent = hum
                end

                hum.WalkSpeed = 34
                hum.UseJumpPower = true
                hum.JumpPower = 75
                hum.JumpHeight = 7.2

                local function load(id)
                    local a = Instance.new("Animation")
                    a.AnimationId = id
                    return animator:LoadAnimation(a)
                end

                local walkTrack = load("rbxassetid://107806791584829")
                walkTrack.Looped = true
                walkTrack.Priority = Enum.AnimationPriority.Movement
                local idleTrack = load("rbxassetid://120873587634730")
                idleTrack.Looped = true
                idleTrack.Priority = Enum.AnimationPriority.Idle
                local jumpTrack = load("rbxassetid://139390570947836")
                jumpTrack.Looped = true
                jumpTrack.Priority = Enum.AnimationPriority.Action

                hum.Running:Connect(function(speedVal)
                    if hum:GetState() ~= Enum.HumanoidStateType.Jumping then
                        if speedVal > 0 then
                            if idleTrack.IsPlaying then idleTrack:Stop() end
                            if not walkTrack.IsPlaying then walkTrack:Play() end
                        else
                            if walkTrack.IsPlaying then walkTrack:Stop() end
                            if not idleTrack.IsPlaying then playRetroTrack(idleTrack) end
                        end
                    end
                end)
                hum.Jumping:Connect(function(isActive)
                    if isActive then
                        if walkTrack.IsPlaying then walkTrack:Stop() end
                        if idleTrack.IsPlaying then idleTrack:Stop() end
                        jumpTrack:Play()
                    end
                end)
                hum.StateChanged:Connect(function(_, new)
                    if new == Enum.HumanoidStateType.Landed then
                        if jumpTrack.IsPlaying then jumpTrack:Stop() end
                        if hum.MoveDirection and hum.MoveDirection.Magnitude > 0 then
                            if not walkTrack.IsPlaying then walkTrack:Play() end
                        else
                            if not idleTrack.IsPlaying then playRetroTrack(idleTrack) end
                        end
                    end
                end)

                if not idleTrack.IsPlaying then playRetroTrack(idleTrack) end
                _notif("Faker (FE)", "Faker activated!")
            end)
        end
    })

    
    MorphFETab:AddSection({
        "> C00lkid (FE)"
    })
    MorphFETab:AddButton({
        Name = "Transform Into C00lkid",
        Description = "C00lkid outfit + aura + Attack/Swordash tools",
        Callback = function()
            task.spawn(function()
                local p = game:GetService("Players").LocalPlayer
                local char = p.Character or p.CharacterAdded:Wait()
                local hum = char:WaitForChild("Humanoid")
                local root = char:WaitForChild("HumanoidRootPart")

                
                local COOLKID_OUTFIT = {97783522360086, 114654038701845, 12579351328, 137187716343609, 113063794098098, 132853148217154, 101058332011278, 3240543366}
                local RECOLOR_ID = 3240543366
                local DARK_RED = Color3.fromRGB(80, 0, 0)
                task.spawn(function()
                    local RS = game:GetService("ReplicatedStorage")
                    local Remotes = RS:FindFirstChild("Remotes")
                    local wearRemote = Remotes and Remotes:FindFirstChild("Wear")
                    local changeItemColorRemote = Remotes and (Remotes:FindFirstChild("ChangeItemColor") or Remotes:FindFirstChild("ChangeAccessoryColor"))
                    if not wearRemote then return end
                    for i, id in ipairs(COOLKID_OUTFIT) do
                        pcall(function() wearRemote:InvokeServer(id) end)
                        if id == RECOLOR_ID then
                            pcall(function() wearRemote:InvokeServer(id, DARK_RED) end)
                            if changeItemColorRemote then
                                pcall(function() changeItemColorRemote:InvokeServer(id, DARK_RED) end)
                                pcall(function() changeItemColorRemote:InvokeServer(tostring(id), DARK_RED) end)
                            end
                        end
                        if i < #COOLKID_OUTFIT then task.wait(2.1) end
                    end
                end)

                
                local backpack = p:WaitForChild("Backpack")
                for _, tName in ipairs({"Attack", "Swordash"}) do
                    local oldBP = backpack:FindFirstChild(tName)
                    if oldBP then oldBP:Destroy() end
                    local oldChar = char:FindFirstChild(tName)
                    if oldChar then oldChar:Destroy() end
                end

                
                task.delay(1.5, function()
                    local torso = char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
                    if not torso then return end
                    local oldAtt = torso:FindFirstChild("C00lkidAuraAtt")
                    if oldAtt then oldAtt:Destroy() end
                    local att = Instance.new("Attachment")
                    att.Name = "C00lkidAuraAtt"
                    att.Parent = torso
                    local emit1 = Instance.new("ParticleEmitter")
                    emit1.Name = "AuraRed"
                    emit1.Texture = "rbxassetid://16846725471"
                    emit1.Color = ColorSequence.new({
                        ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 30, 30)),
                        ColorSequenceKeypoint.new(1, Color3.fromRGB(120, 0, 0))
                    })
                    emit1.Lifetime = NumberRange.new(0.6, 1.2)
                    emit1.Rate = 12
                    emit1.Rotation = NumberRange.new(-180, 180)
                    emit1.RotSpeed = NumberRange.new(-30, 30)
                    emit1.EmissionDirection = Enum.NormalId.Top
                    emit1.Speed = NumberRange.new(0.2, 0.8)
                    emit1.Size = NumberSequence.new({
                        NumberSequenceKeypoint.new(0, 1.5),
                        NumberSequenceKeypoint.new(0.5, 3.2),
                        NumberSequenceKeypoint.new(1, 0)
                    })
                    emit1.LightEmission = 0.6
                    emit1.Orientation = Enum.ParticleOrientation.FacingCamera
                    emit1.SpreadAngle = Vector2.new(180, 180)
                    emit1.Brightness = 2
                    emit1.Transparency = NumberSequence.new({
                        NumberSequenceKeypoint.new(0, 1),
                        NumberSequenceKeypoint.new(0.3, 0.45),
                        NumberSequenceKeypoint.new(1, 1)
                    })
                    emit1.Parent = att
                end)

                
                local IDS = {
                    AttackIcon = "rbxassetid://131150792336953",
                    SwordashIcon = "rbxassetid://139380868538173",
                    AttackSound = "rbxassetid://12222216",
                    SwordashSound = "rbxassetid://129911400031350"
                }

                local sndAttack = Instance.new("Sound", root)
                sndAttack.SoundId = IDS.AttackSound
                sndAttack.Volume = 2
                local sndSwordash = Instance.new("Sound", root)
                sndSwordash.SoundId = IDS.SwordashSound
                sndSwordash.Volume = 0.79

                local attackTool = Instance.new("Tool")
                attackTool.Name = "Attack"
                attackTool.RequiresHandle = false
                attackTool.TextureId = IDS.AttackIcon
                attackTool.Parent = backpack

                local swordTool = Instance.new("Tool")
                swordTool.Name = "Swordash"
                swordTool.RequiresHandle = false
                swordTool.TextureId = IDS.SwordashIcon
                swordTool.Parent = backpack

                -- [LOCKED: WORK PERFECTLY] Catalog emote -> pure animation bypass
                local trackAttack = _loadEmoteAsPureAnimation(hum, 122240620529815)
                local trackSwordash = _loadEmoteAsPureAnimation(hum, 109998455845225)

                attackTool.Equipped:Connect(function() trackAttack:Play() end)
                attackTool.Activated:Connect(function() trackAttack:Play() sndAttack:Play() end)
                attackTool.Unequipped:Connect(function() trackAttack:Stop() end)
                swordTool.Equipped:Connect(function() trackSwordash:Play() end)
                swordTool.Activated:Connect(function() trackSwordash:Play() sndSwordash:Play() end)
                swordTool.Unequipped:Connect(function() trackSwordash:Stop() end)

                _notif("C00lkid (FE)", "C00lkid activated!")
            end)
        end
    })
end

    
    MorphFETab:AddSection({
        "> Morph Tools Management"
    })
    MorphFETab:AddButton({
        Name = "Remove Tools Morph",
        Description = "Removes all custom morph tools (Goku, C00lkid, etc.) and disables active morph states",
        Callback = function()
            
            pcall(function()
                if _cleanupGoku then _cleanupGoku() end
            end)

            
            local lp = game:GetService("Players").LocalPlayer
            local bp = lp and lp:FindFirstChild("Backpack")
            local char = lp and lp.Character

            local morphToolNames = {"GokuFly", "GokuTP", "GokuSSJ", "GokuKi", "Attack", "Swordash"}
            for _, tName in ipairs(morphToolNames) do
                if bp and bp:FindFirstChild(tName) then
                    pcall(function() bp[tName]:Destroy() end)
                end
                if char and char:FindFirstChild(tName) then
                    pcall(function() char[tName]:Destroy() end)
                end
            end

            
            if char then
                for _, d in ipairs(char:GetDescendants()) do
                    if d.Name == "C00lkidAuraAtt" or d.Name == "SSJHighlight" or d.Name == "SSJLight" or d.Name == "SSJSparks" or d.Name == "KiChargeLight" then
                        pcall(function() d:Destroy() end)
                    end
                end
            end

            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Morph Tools",
                Text = "All morph tools and effects removed successfully!",
                Duration = 3
            })
        end
    })

do
    local robotActive         = false
    local robotLoop           = nil
    local robotChatConn       = nil
    local robotAnchor         = Vector3.new(0, 0, 0)
    local robotAngle          = 0
    local robotAnimT          = 0
    local robotState          = "idle"
    local robotStateTimer     = 0
    local robotMode           = "manual"
    local robotFollowName     = ""
    local robotWaypoint       = nil
    local robotSpeed          = 18
    local robotCooldowns      = {}
    local robotRisingProgress = 0
    local ROBOT_GROUND_Y      = 0
    local robotColorMode      = "Mecha Blue"
    local robotWorkers        = {}
    local robotTarget         = nil
    local robotDriverSeat     = nil

    local function rNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local ROBOT_LAYOUT = {
        {key="core", prop="FurnitureSilverThrone", cat="Furniture",
         off=Vector3.new(0, 16, 0.5), rot=CFrame.new()},

        {key="head", prop="FurnitureTV", cat="Furniture",
         off=Vector3.new(0, 23.5, -1.2), rot=CFrame.Angles(0, math.pi, 0)},

        {key="eyeL", prop="LightsSpotLightRed", cat="Lights",
         off=Vector3.new(-1.2, 24, -2.8), rot=CFrame.Angles(0, math.pi, 0)},

        {key="eyeR", prop="LightsSpotLightRed", cat="Lights",
         off=Vector3.new(1.2, 24, -2.8), rot=CFrame.Angles(0, math.pi, 0)},

        {key="chestArmor", prop="FurnitureBleachers", cat="Furniture",
         off=Vector3.new(0, 18, -3.2), rot=CFrame.Angles(math.rad(15), 0, 0)},

        {key="jetpackL", prop="TorchFire", cat="Torch",
         off=Vector3.new(-3.5, 19, 4), rot=CFrame.Angles(math.rad(160), 0, 0)},

        {key="jetpackR", prop="TorchFire", cat="Torch",
         off=Vector3.new(3.5, 19, 4), rot=CFrame.Angles(math.rad(160), 0, 0)},

        {key="shoulderL", prop="FurnitureBleachers", cat="Furniture",
         off=Vector3.new(-7.5, 20.5, 0), rot=CFrame.Angles(0, 0, math.rad(45))},

        {key="shoulderR", prop="FurnitureBleachers", cat="Furniture",
         off=Vector3.new(7.5, 20.5, 0), rot=CFrame.Angles(0, 0, math.rad(-45))},

        {key="armL_gun", prop="TubeLamp", cat="Lights",
         off=Vector3.new(-9.5, 14, -1), rot=CFrame.Angles(math.rad(90), 0, 0)},

        {key="armR_blade", prop="TubeLamp", cat="Lights",
         off=Vector3.new(9.5, 14, -1), rot=CFrame.Angles(math.rad(90), 0, 0)},

        {key="thighL", prop="FurnitureBleachers", cat="Furniture",
         off=Vector3.new(-4.5, 8.5, 0), rot=CFrame.Angles(0, 0, math.rad(90))},

        {key="thighR", prop="FurnitureBleachers", cat="Furniture",
         off=Vector3.new(4.5, 8.5, 0), rot=CFrame.Angles(0, 0, math.rad(-90))},

        {key="footL", prop="FurnitureTV", cat="Furniture",
         off=Vector3.new(-4.5, 1.5, 0.5), rot=CFrame.Angles(0, 0, 0)},

        {key="footR", prop="FurnitureTV", cat="Furniture",
         off=Vector3.new(4.5, 1.5, 0.5), rot=CFrame.Angles(0, 0, 0)},
    }

    local function canDo(actionKey, cooldownSec)
        local now = tick()
        if robotCooldowns[actionKey] and (now - robotCooldowns[actionKey]) < cooldownSec then
            return false
        end
        robotCooldowns[actionKey] = now
        return true
    end

    local function getPartColor(layout, partIndex, t, state, colorMode)
        local pulse = (math.sin(t * 6) + 1) * 0.5
        local wave = (math.sin(t * 5 + partIndex * 0.45) + 1) * 0.5

        if state == "attack" then
            if layout.key == "armL_gun" or layout.key == "armR_blade" then
                return (math.sin(t * 40) > 0) and Color3.fromRGB(255, 255, 255) or Color3.fromRGB(255, 60, 20)
            end
            return Color3.fromRGB(255, math.floor(40 + pulse * 60), 10)
        elseif state == "roar" then
            return Color3.fromRGB(255, math.floor(180 + pulse * 75), 0)
        elseif state == "shield" then
            return Color3.fromRGB(0, math.floor(200 + pulse * 55), 255)
        end

        if colorMode == "Mecha Blue" then
            if layout.key == "head" or layout.key == "core" then
                return Color3.new(0.3 + pulse * 0.3, 0.8 + pulse * 0.2, 1.0)
            else
                return Color3.new(0.05 + wave * 0.15, 0.4 + wave * 0.35, 0.9 + wave * 0.1)
            end
        elseif colorMode == "Shadow Arise (Violet)" then
            if layout.key == "eyeL" or layout.key == "eyeR" then
                return Color3.fromRGB(160, 0, 255)
            end
            return Color3.new(0.45 + wave * 0.4, 0.02 + wave * 0.08, 0.85 + wave * 0.15)
        elseif colorMode == "Magma Red" then
            return Color3.new(0.5 + pulse * 0.5, 0.05 + pulse * 0.15, 0.0)
        elseif colorMode == "Sacrificial Gold" then
            return Color3.new(0.85 + pulse * 0.15, 0.65 + pulse * 0.25, 0.1 + pulse * 0.2)
        elseif colorMode == "Rainbow" then
            local hue = ((t * 0.2) + (partIndex * 0.065)) % 1
            return Color3.fromHSV(hue, 1, 1)
        elseif colorMode == "Plasma Wave" then
            local phase = ((t * 0.35) + (partIndex * 0.08)) % 1
            if phase < 0.5 then
                return Color3.fromRGB(0, 220, 255):Lerp(Color3.fromRGB(255, 0, 180), phase * 2)
            else
                return Color3.fromRGB(255, 0, 180):Lerp(Color3.fromRGB(0, 220, 255), (phase - 0.5) * 2)
            end
        else
            return Color3.fromRGB(0, 160, 255)
        end
    end

    local function getPartCFrame(layout, t, state, risingProg, partIndex)
        local robotCF = CFrame.new(robotAnchor) * CFrame.Angles(0, robotAngle, 0)
        local baseOff = layout.off
        local baseRot = layout.rot or CFrame.new()
        local animCF = CFrame.new()

        if state == "dormant" then
            if layout.key == "core" then
                animCF = CFrame.new(0, -22, 0)
            elseif layout.key == "armL_gun" or layout.key == "armR_blade" then
                animCF = CFrame.new(0, -10, -8) * CFrame.Angles(-math.pi/1.5, 0, 0)
            else
                animCF = CFrame.new(0, -40, 0)
            end

        elseif state == "idle" then
            local breatheY = math.sin(t * 1.8) * 0.35
            local swayX = math.sin(t * 0.9) * 0.15
            if layout.key == "head" or layout.key == "eyeL" or layout.key == "eyeR" then
                local scan = math.sin(t * 0.8) * 0.25
                animCF = CFrame.new(swayX, breatheY, 0) * CFrame.Angles(0, scan, 0)
            elseif layout.key == "armL_gun" then
                animCF = CFrame.new(0, breatheY * 0.5, 0) * CFrame.Angles(math.sin(t * 1.8) * 0.08, 0, 0)
            elseif layout.key == "armR_blade" then
                animCF = CFrame.new(0, breatheY * 0.5, 0) * CFrame.Angles(-math.sin(t * 1.8) * 0.08, 0, 0)
            elseif layout.key == "thighL" or layout.key == "thighR" or layout.key == "footL" or layout.key == "footR" then
                animCF = CFrame.new()
            else
                animCF = CFrame.new(swayX, breatheY, 0)
            end

        elseif state == "walk" then
            local walkFreq = 3.6
            local walkCycle = t * walkFreq
            local legLift = 2.0
            local legStride = 3.5

            if layout.key == "thighL" then
                local sL = math.sin(walkCycle)
                animCF = CFrame.new(0, math.max(0, -sL) * legLift * 0.6, -sL * legStride * 0.5) * CFrame.Angles(sL * 0.35, 0, 0)
            elseif layout.key == "footL" then
                local sL = math.sin(walkCycle)
                animCF = CFrame.new(0, math.max(0, -sL) * legLift, -sL * legStride) * CFrame.Angles(sL * 0.2, 0, 0)
            elseif layout.key == "thighR" then
                local sR = math.sin(walkCycle + math.pi)
                animCF = CFrame.new(0, math.max(0, -sR) * legLift * 0.6, -sR * legStride * 0.5) * CFrame.Angles(sR * 0.35, 0, 0)
            elseif layout.key == "footR" then
                local sR = math.sin(walkCycle + math.pi)
                animCF = CFrame.new(0, math.max(0, -sR) * legLift, -sR * legStride) * CFrame.Angles(sR * 0.2, 0, 0)
            elseif layout.key == "armL_gun" then
                animCF = CFrame.Angles(math.sin(walkCycle + math.pi) * 0.7, 0, 0)
            elseif layout.key == "armR_blade" then
                animCF = CFrame.Angles(math.sin(walkCycle) * 0.7, 0, 0)
            elseif layout.key == "head" or layout.key == "eyeL" or layout.key == "eyeR" then
                animCF = CFrame.new(math.sin(walkCycle) * 0.4, math.abs(math.sin(walkCycle)) * 0.5, 0) * CFrame.Angles(0, 0, -math.sin(walkCycle) * 0.05)
            else
                animCF = CFrame.new(math.sin(walkCycle) * 0.5, math.abs(math.sin(walkCycle)) * 0.6, 0) * CFrame.Angles(0, 0, -math.sin(walkCycle) * 0.06)
            end

        elseif state == "attack" then
            local recoil = math.sin(t * 45) * 0.45
            if layout.key == "armL_gun" then
                animCF = CFrame.new(0, 0, -4.5 + recoil) * CFrame.Angles(-math.pi/2.2, 0, 0)
            elseif layout.key == "armR_blade" then
                animCF = CFrame.new(0, 0, -4.5 - recoil) * CFrame.Angles(-math.pi/2.2, 0, 0)
            elseif layout.key == "head" or layout.key == "eyeL" or layout.key == "eyeR" then
                animCF = CFrame.Angles(-0.3, 0, 0)
            elseif layout.key == "thighL" or layout.key == "footL" then
                animCF = CFrame.new(-1, 0, 1.5) * CFrame.Angles(0.2, 0, 0)
            elseif layout.key == "thighR" or layout.key == "footR" then
                animCF = CFrame.new(1, 0, -1.5) * CFrame.Angles(-0.2, 0, 0)
            else
                animCF = CFrame.new(0, -0.5, -0.8) * CFrame.Angles(0.25, 0, 0)
            end

        elseif state == "stomp" then
            local p = robotStateTimer
            if layout.key == "thighR" or layout.key == "footR" then
                if p < 1.0 then
                    local lift = math.sin(p * math.pi) * 5
                    animCF = CFrame.new(0, lift, -lift * 0.4) * CFrame.Angles(-0.6, 0, 0)
                else
                    animCF = CFrame.new(0, 0, 0)
                end
            elseif layout.key == "armR_blade" then
                animCF = CFrame.new(0, 1, 0) * CFrame.Angles(-0.5, 0, 0)
            else
                animCF = CFrame.new(0, -math.abs(math.sin(p * 4)) * 1.5, 0)
            end

        elseif state == "roar" then
            local shake = math.sin(t * 50) * 0.25
            if layout.key == "head" or layout.key == "eyeL" or layout.key == "eyeR" then
                animCF = CFrame.new(shake, shake, 0) * CFrame.Angles(0.9, 0, 0)
            elseif layout.key == "armL_gun" then
                animCF = CFrame.new(-1, 2, 0) * CFrame.Angles(0.8, 0, -0.8)
            elseif layout.key == "armR_blade" then
                animCF = CFrame.new(1, 2, 0) * CFrame.Angles(0.8, 0, 0.8)
            else
                animCF = CFrame.new(shake, 0.8 + shake, 0) * CFrame.Angles(-0.15, 0, 0)
            end

        elseif state == "kneel" then
            if layout.key == "thighL" or layout.key == "thighR" then
                animCF = CFrame.new(0, -6, -2) * CFrame.Angles(math.rad(65), 0, 0)
            elseif layout.key == "footL" or layout.key == "footR" then
                animCF = CFrame.new(0, -1, 3) * CFrame.Angles(math.rad(-45), 0, 0)
            elseif layout.key == "core" then
                animCF = CFrame.new(0, -11, 0)
            elseif layout.key == "armL_gun" or layout.key == "armR_blade" then
                animCF = CFrame.new(0, -10, -2) * CFrame.Angles(-math.rad(40), 0, 0)
            else
                animCF = CFrame.new(0, -11, 0)
            end

        elseif state == "shield" then
            local totalParts = #ROBOT_LAYOUT
            local spinAngle = robotAnimT * 8 + partIndex * (math.pi * 2 / totalParts)
            local orbitR = 19
            local spinY = math.sin(robotAnimT * 4 + partIndex * 1.3) * 10

            baseOff = Vector3.new(
                math.cos(spinAngle) * orbitR,
                14 + spinY,
                math.sin(spinAngle) * orbitR
            )
            baseRot = CFrame.new()
            animCF = CFrame.Angles(robotAnimT * 12, robotAnimT * 9, partIndex)
        end

        local smoothProg = math.clamp(risingProg, 0, 1)
        smoothProg = smoothProg * smoothProg * (3 - 2 * smoothProg)
        local spinFactor = (1 - smoothProg)
        local spiralAngle = spinFactor * math.pi * 5 * (partIndex % 2 == 0 and 1 or -1)
        local spiralRadius = 25 * spinFactor
        local undergroundY = -40 * spinFactor

        local vortexOff = Vector3.new(
            math.cos(spiralAngle) * spiralRadius,
            undergroundY,
            math.sin(spiralAngle) * spiralRadius
        )

        local localPos = baseOff + vortexOff
        local worldPos = (robotCF * CFrame.new(localPos)).Position
        local vortexRot = CFrame.Angles(spiralAngle * 0.4, spiralAngle * 0.6, spiralAngle * 0.2)
        local worldCF = CFrame.new(worldPos) * CFrame.Angles(0, robotAngle, 0) * baseRot * animCF * vortexRot

        if risingProg < 1 and risingProg > 0.05 then
            local shake = math.sin(t * 30) * 0.3 * spinFactor
            worldCF = worldCF * CFrame.new(shake, shake, shake)
        end

        return worldCF
    end

    local function startWorkers(w)
        if w.scf then
            task.spawn(function()
                while robotActive and w.scf do
                    local now = tick()
                    if w.targetCF then
                        local cf = w.targetCF
                        local sendCF = false
                        if not w.lastSentCF then
                            sendCF = true
                        else
                            local moved = (cf.Position - w.lastSentCF.Position).Magnitude > 0.35
                            if moved or (now - w.lastSendTimeCF > 0.3) then
                                sendCF = true
                            end
                        end
                        if sendCF then
                            w.lastSentCF = cf
                            w.lastSendTimeCF = now
                            pcall(function() w.scf:InvokeServer(cf) end)
                        end
                    end
                    task.wait(0.04)
                end
            end)
        end

        if w.cpc then
            task.spawn(function()
                while robotActive and w.cpc do
                    local now = tick()
                    if w.targetColor then
                        local col = w.targetColor
                        local sendCol = false
                        if not w.lastSentColor then
                            sendCol = true
                        else
                            if (now - w.lastSendTimeColor >= 0.08) then
                                local dr = math.abs(col.R - w.lastSentColor.R)
                                local dg = math.abs(col.G - w.lastSentColor.G)
                                local db = math.abs(col.B - w.lastSentColor.B)
                                if (dr + dg + db) > 0.035 then
                                    sendCol = true
                                end
                            end
                        end
                        if sendCol then
                            w.lastSentColor = col
                            w.lastSendTimeColor = now
                            pcall(function() w.cpc:InvokeServer(col) end)
                        end
                    end
                    task.wait(0.04)
                end
            end)
        end
    end

    local function startRobotLoop()
        if robotLoop then robotLoop:Disconnect() end
        robotAnimT = 0
        robotRisingProgress = 0

        robotLoop = game:GetService("RunService").Heartbeat:Connect(function(dt)
            if not robotActive then return end
            robotAnimT = robotAnimT + dt
            robotStateTimer = robotStateTimer + dt

            local uis = game:GetService("UserInputService")
            local lp = game:GetService("Players").LocalPlayer
            local myHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            local isRiding = false

            if robotDriverSeat and myHum and robotDriverSeat.Occupant == myHum then
                isRiding = true
            end

            if isRiding then
                local moveDir = Vector3.new(0, 0, 0)
                local turnSpeed = 2.4
                if uis:IsKeyDown(Enum.KeyCode.A) then
                    robotAngle = robotAngle - turnSpeed * dt
                end
                if uis:IsKeyDown(Enum.KeyCode.D) then
                    robotAngle = robotAngle + turnSpeed * dt
                end

                local fwd = Vector3.new(math.sin(robotAngle), 0, math.cos(robotAngle))
                if uis:IsKeyDown(Enum.KeyCode.W) then
                    moveDir = moveDir + fwd
                end
                if uis:IsKeyDown(Enum.KeyCode.S) then
                    moveDir = moveDir - fwd * 0.5
                end

                if moveDir.Magnitude > 0 then
                    robotAnchor = robotAnchor + moveDir.Unit * robotSpeed * dt
                    if robotState ~= "attack" and robotState ~= "stomp" and robotState ~= "roar" then
                        robotState = "walk"
                    end
                else
                    if robotState == "walk" then
                        robotState = "idle"
                    end
                end

            elseif robotState == "attack" then
                local fwd = Vector3.new(math.sin(robotAngle), 0, math.cos(robotAngle))
                robotAnchor = robotAnchor + fwd * 45 * dt

                if robotTarget and robotTarget ~= "---" then
                    local targetPlayer = game:GetService("Players"):FindFirstChild(robotTarget)
                    local targetChar = targetPlayer and targetPlayer.Character or workspace:FindFirstChild(robotTarget)
                    local hrp = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        local dir = (hrp.Position - robotAnchor)
                        dir = Vector3.new(dir.X, 0, dir.Z)
                        if dir.Magnitude > 0 then
                            local targetAngle = math.atan2(dir.X, dir.Z)
                            local angleDiff = (targetAngle - robotAngle + math.pi) % (2 * math.pi) - math.pi
                            robotAngle = robotAngle + math.clamp(angleDiff, -6, 6) * dt
                        end
                    end
                end

            elseif robotState == "walk" then
                if robotMode == "follow" and robotFollowName ~= "" and robotFollowName ~= "---" then
                    local tp = game:GetService("Players"):FindFirstChild(robotFollowName)
                    local hrp = tp and tp.Character and tp.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        local toTarget = hrp.Position - robotAnchor
                        toTarget = Vector3.new(toTarget.X, 0, toTarget.Z)
                        if toTarget.Magnitude > 7 then
                            local targetAngle = math.atan2(toTarget.X, toTarget.Z)
                            local angleDiff = (targetAngle - robotAngle + math.pi) % (2 * math.pi) - math.pi
                            robotAngle = robotAngle + math.clamp(angleDiff, -3.0, 3.0) * dt
                            local dist = toTarget.Magnitude
                            local adaptSpeed = math.clamp(dist * 0.7, robotSpeed, 65)
                            local fwdF = Vector3.new(math.sin(robotAngle), 0, math.cos(robotAngle))
                            robotAnchor = robotAnchor + fwdF * adaptSpeed * dt
                        else
                            robotState = "idle"
                        end
                    end

                elseif robotMode == "free" then
                    if not robotWaypoint then
                        local rAngle = math.random() * 2 * math.pi
                        local rDist  = math.random(40, 120)
                        local nx = math.clamp(robotAnchor.X + math.cos(rAngle) * rDist, -350, 350)
                        local nz = math.clamp(robotAnchor.Z + math.sin(rAngle) * rDist, -350, 350)
                        robotWaypoint = Vector3.new(nx, robotAnchor.Y, nz)
                    end

                    local toWp = robotWaypoint - robotAnchor
                    toWp = Vector3.new(toWp.X, 0, toWp.Z)

                    if toWp.Magnitude < 8 then
                        robotWaypoint = nil
                        robotState = "idle"
                        task.delay(math.random(2, 6), function()
                            if not robotActive or robotMode ~= "free" then return end
                            if robotState == "idle" then robotState = "walk" end
                        end)
                    else
                        local targetAngle = math.atan2(toWp.X, toWp.Z)
                        local angleDiff = (targetAngle - robotAngle + math.pi) % (2 * math.pi) - math.pi
                        local turnRate = math.clamp(math.abs(angleDiff) * 3, 1.8, 5.0)
                        robotAngle = robotAngle + math.clamp(angleDiff, -turnRate, turnRate) * dt
                        local fwdF = Vector3.new(math.sin(robotAngle), 0, math.cos(robotAngle))
                        robotAnchor = robotAnchor + fwdF * robotSpeed * dt
                    end
                else
                    local fwd = Vector3.new(math.sin(robotAngle), 0, math.cos(robotAngle))
                    robotAnchor = robotAnchor + fwd * robotSpeed * dt
                end
            end

            if robotState == "rising" then
                robotRisingProgress = math.min(robotRisingProgress + dt * 0.25, 1)
                if robotRisingProgress >= 1 then
                    robotState = "idle"
                    robotStateTimer = 0
                    rNotif("MECHA ROBOT", "TITAN SYSTEMS 100% ONLINE!", 5)
                end
            end

            if robotState == "attack" and robotStateTimer > 3.5 then
                robotState = "idle" robotStateTimer = 0
            end
            if robotState == "stomp" and robotStateTimer > 2.2 then
                robotState = "idle" robotStateTimer = 0
            end
            if robotState == "roar" and robotStateTimer > 4.5 then
                robotState = "idle" robotStateTimer = 0
            end

            local ignoreList = { workspace:FindFirstChild("WorkspaceCom") }
            for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                if p.Character then table.insert(ignoreList, p.Character) end
            end
            local rayParams = RaycastParams.new()
            rayParams.FilterType = Enum.RaycastFilterType.Exclude
            rayParams.FilterDescendantsInstances = ignoreList

            local rayOrigin = Vector3.new(robotAnchor.X, robotAnchor.Y + 8, robotAnchor.Z)
            local rayResult = workspace:Raycast(rayOrigin, Vector3.new(0, -40, 0), rayParams)
            if rayResult then
                local targetY = rayResult.Position.Y
                local diffY = targetY - robotAnchor.Y
                diffY = math.clamp(diffY, -25 * dt, 14 * dt)
                robotAnchor = Vector3.new(robotAnchor.X, robotAnchor.Y + diffY, robotAnchor.Z)
            end

            for partIndex, layout in ipairs(ROBOT_LAYOUT) do
                local w = robotWorkers[layout.key]
                if w then
                    w.targetCF = getPartCFrame(layout, robotAnimT, robotState, robotRisingProgress, partIndex)
                    w.targetColor = getPartColor(layout, partIndex, robotAnimT, robotState, robotColorMode)
                end
            end
        end)
    end

    local function spawnRobot()
        local lp = game:GetService("Players").LocalPlayer
        local char = lp.Character or lp.CharacterAdded:Wait()
        local myRoot = char:WaitForChild("HumanoidRootPart")

        rNotif("MECHA ROBOT", "Summoning Titan Mecha... (15 parts, ~13s)", 5)

        local RE         = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")
        local toolRE     = RE:WaitForChild("1Too1l")
        local myPropName = "Prop" .. lp.Name
        local wc         = workspace:WaitForChild("WorkspaceCom")
        local Telemetry  = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction")
        local defaultColor = Color3.fromRGB(0, 160, 255)

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.3)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then return end

        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum and propsTool.Parent ~= char then
            hum:EquipTool(propsTool)
            task.wait(0.2)
        end

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
        if not toolPropMake then
            local bpTool = lp.Backpack:FindFirstChild("PropMaker")
            if bpTool then toolPropMake = bpTool:FindFirstChild("Tool_PropMake") end
        end
        if not toolPropMake then return end

        ROBOT_GROUND_Y = myRoot.Position.Y
        robotAnchor    = myRoot.Position + (myRoot.CFrame.LookVector * 15)
        robotAnchor    = Vector3.new(robotAnchor.X, ROBOT_GROUND_Y, robotAnchor.Z)

        robotWorkers = {}
        robotDriverSeat = nil

        local assignedProps = {}

        for idx, layout in ipairs(ROBOT_LAYOUT) do
            if not robotActive then break end

            local playerPos = (lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") and lp.Character.HumanoidRootPart.Position) or myRoot.Position
            local spawnPos = playerPos + Vector3.new(math.cos(idx) * 4, 2, math.sin(idx) * 4)

            
            local currentHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if currentHum and propsTool.Parent ~= lp.Character then
                currentHum:EquipTool(propsTool)
                task.wait(0.1)
            end

            local tpm = propsTool:FindFirstChild("Tool_PropMake")
            if not tpm then
                local bp = lp.Backpack:FindFirstChild("PropMaker")
                if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
            end
            if not tpm then return end

            pcall(function() Telemetry:FireServer("filterClick", {name = layout.cat, itemType = "Props"}) end)
            task.wait(0.05)
            pcall(function() Telemetry:FireServer("filterClick", {name = layout.prop, itemType = "Props"}) end)
            task.wait(0.05)
            pcall(function() clearTools:FireServer("RequestingPropName", layout.prop, layout.cat) end)
            task.wait(0.1)

            
            pcall(function() tpm:FireServer(workspace.Terrain, spawnPos) end)
            task.wait(0.85)

            if not robotActive then break end

            local foundProp = nil
            local scf = nil
            local cpc = nil

            for _, folder in pairs(wc:GetChildren()) do
                if foundProp then break end
                for _, prop in pairs(folder:GetChildren()) do
                    if prop.Name == myPropName and not assignedProps[prop] then
                        local propScf = prop:FindFirstChild("SetCurrentCFrame") or prop:FindFirstChild("SetCurrentCFrame", true) or prop:FindFirstChildWhichIsA("RemoteFunction", true)
                        if propScf then
                            foundProp = prop
                            scf = propScf
                            cpc = prop:FindFirstChild("ChangePropColor") or prop:FindFirstChild("ChangePropColor", true)
                            assignedProps[prop] = true
                            break
                        end
                    end
                end
            end

            if layout.key == "core" and foundProp then
                local seat = foundProp:FindFirstChildWhichIsA("Seat", true)
                if seat then robotDriverSeat = seat end
            end

            local initialCF = getPartCFrame(layout, 0, "dormant", 0, idx)
            local w = {
                key = layout.key, scf = scf, cpc = cpc,
                targetCF = initialCF, targetColor = defaultColor,
                lastSentCF = nil, lastSentColor = nil,
                lastSendTimeCF = 0, lastSendTimeColor = 0
            }
            robotWorkers[layout.key] = w
            if scf then pcall(function() scf:InvokeServer(initialCF) end) end
            if cpc then pcall(function() cpc:InvokeServer(defaultColor) end) end
            startWorkers(w)
        end

        -- Retrait du carton apres 1 seconde (LOCKED Rule 7)
        task.wait(1.0)
        pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        local count = 0
        for _, w in pairs(robotWorkers) do if w.scf then count = count + 1 end end
        rNotif("MECHA ROBOT", "Titan online! " .. count .. "/15 parts assembled. Say 'wake up' to activate!", 6)
    end

    local function setupChatListener()
        if robotChatConn then
            if type(robotChatConn) == "table" then
                for _, c in pairs(robotChatConn) do pcall(function() c:Disconnect() end) end
            else pcall(function() robotChatConn:Disconnect() end) end
        end
        robotChatConn = {}

        local function onChat(player, message)
            if not robotActive or not player or not message then return end
            local isWhitelisted = (player == game:GetService("Players").LocalPlayer)
            if not isWhitelisted then return end

            local text = message:lower():match("^%s*(.-)%s*$")
            if string.find(text, "wake up") or string.find(text, "arise") or string.find(text, "reveil") then
                if robotState == "rising" or robotState == "walk" or robotState == "attack" then return end
                robotState = "rising"
                robotStateTimer = 0
                robotRisingProgress = 0
                rNotif("MECHA ROBOT", "Titan Awakening... WAKE UP!", 6)
            end
        end

        pcall(function()
            local TCS = game:GetService("TextChatService")
            if TCS and TCS.MessageReceived then
                table.insert(robotChatConn, TCS.MessageReceived:Connect(function(msg)
                    if msg and msg.TextSource then
                        local p = game:GetService("Players"):GetPlayerByUserId(msg.TextSource.UserId)
                        if p then onChat(p, msg.Text) end
                    end
                end))
            end
        end)

        for _, p in pairs(game:GetService("Players"):GetPlayers()) do
            table.insert(robotChatConn, p.Chatted:Connect(function(msg) onChat(p, msg) end))
        end
        table.insert(robotChatConn, game:GetService("Players").PlayerAdded:Connect(function(p)
            table.insert(robotChatConn, p.Chatted:Connect(function(msg) onChat(p, msg) end))
        end))
    end

    BuildTab:AddSection({"> Mecha Robot"})

    local robotToggle = BuildTab:AddToggle({
        Name = "Spawn Mecha Robot",
        Description = "Summon a giant 15-prop mecha titan. Say 'wake up' to activate it!",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not robotActive then return end
            robotActive = val
            if val then
                robotState = "dormant"
                robotRisingProgress = 0
                setupChatListener()
                task.spawn(function()
                    spawnRobot()
                    startRobotLoop()
                end)
            else
                if robotChatConn then
                    if type(robotChatConn) == "table" then
                        for _, c in pairs(robotChatConn) do pcall(function() c:Disconnect() end) end
                    else pcall(function() robotChatConn:Disconnect() end) end
                    robotChatConn = nil
                end
                if robotLoop then
                    robotLoop:Disconnect()
                    robotLoop = nil
                end
                robotState = "idle"
                robotWorkers = {}
                robotDriverSeat = nil
                local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                if RE then
                    local clearTools = RE:FindFirstChild("1Clea1rTool1s")
                    if clearTools then
                        pcall(function() clearTools:FireServer("ClearAllProps") end)
                    end
                end
                task.wait(0.1)
                cleanupPropMakerTool()
                rNotif("Mecha Robot", "The mecha returns to the vault.", 3)
            end
        end
    })

    BuildTab:AddDropdown({
        Name = "Robot Color",
        Description = "Choose the dynamic real-time color scheme",
        Options = {"Mecha Blue", "Shadow Arise (Violet)", "Magma Red", "Sacrificial Gold", "Rainbow", "Plasma Wave"},
        Default = "Mecha Blue",
        Callback = function(val)
            robotColorMode = val
        end
    })

    BuildTab:AddDropdown({
        Name = "Target",
        Description = "Select the robot's target",
        Options = (function()
            local names = {"---"}
            for _, p in pairs(game:GetService("Players").LocalPlayer and game:GetService("Players"):GetPlayers() or {}) do
                if p ~= game:GetService("Players").LocalPlayer then
                    table.insert(names, p.Name)
                end
            end
            return names
        end)(),
        Default = "---",
        Callback = function(val)
            robotFollowName = val
            robotTarget = val
        end
    })

    BuildTab:AddButton({
        Name = "Idle",
        Description = "Robot stands in titan stance",
        Callback = function()
            if not robotActive then rNotif("Mecha Robot", "Spawn the robot first!", 2) return end
            robotMode = "manual"
            robotState = "idle"
            robotStateTimer = 0
        end
    })

    BuildTab:AddButton({
        Name = "Follow Target",
        Description = "Robot hunts the selected target",
        Callback = function()
            if not robotActive then rNotif("Mecha Robot", "Spawn the robot first!", 2) return end
            if robotFollowName == "---" or robotFollowName == "" then
                rNotif("Mecha Robot", "Select a target first!", 3) return
            end
            robotMode = "follow"
            robotState = "walk"
            robotWaypoint = nil
            robotStateTimer = 0
            rNotif("Mecha Robot", "Hunting target...", 3)
        end
    })

    BuildTab:AddButton({
        Name = "Follow Me",
        Description = "Robot follows you like an armored bodyguard",
        Callback = function()
            if not robotActive then rNotif("Mecha Robot", "Spawn the robot first!", 2) return end
            robotFollowName = game:GetService("Players").LocalPlayer.Name
            robotTarget = robotFollowName
            robotMode = "follow"
            robotState = "walk"
            robotWaypoint = nil
            robotStateTimer = 0
            rNotif("Mecha Robot", "Titan is following you!", 3)
        end
    })

    BuildTab:AddButton({
        Name = "Roam",
        Description = "Robot patrols the Brookhaven map freely",
        Callback = function()
            if not robotActive then rNotif("Mecha Robot", "Spawn the robot first!", 2) return end
            if robotMode == "free" then return end
            robotMode = "free"
            robotState = "walk"
            robotWaypoint = nil
            robotStateTimer = 0
            rNotif("Mecha Robot", "The titan is on free patrol.", 3)
        end
    })

    BuildTab:AddButton({
        Name = "Attack",
        Description = "Titan locks onto target with double cannons and rapid fire!",
        Callback = function()
            if not robotActive then rNotif("Mecha Robot", "Spawn the robot first!", 2) return end
            if not canDo("attack", 4) then
                rNotif("Mecha Robot", "Cooldown...", 2) return
            end
            robotMode = "manual"
            robotState = "attack"
            robotStateTimer = 0
            rNotif("Mecha Robot", "TITAN ANNIHILATION BARRAGE!", 3)
        end
    })

    BuildTab:AddButton({
        Name = "Stomp",
        Description = "Earthshaker stomp slamming the ground with a massive shockwave",
        Callback = function()
            if not robotActive then rNotif("Mecha Robot", "Spawn the robot first!", 2) return end
            if not canDo("stomp", 3) then
                rNotif("Mecha Robot", "Cooldown...", 2) return
            end
            robotMode = "manual"
            robotState = "stomp"
            robotStateTimer = 0
            rNotif("Mecha Robot", "EARTHSHAKER STOMP!", 2)
        end
    })

    BuildTab:AddButton({
        Name = "Roar",
        Description = "Overdrive titan roar unleashing energy pulses",
        Callback = function()
            if not robotActive then rNotif("Mecha Robot", "Spawn the robot first!", 2) return end
            if not canDo("roar", 5) then
                rNotif("Mecha Robot", "Cooldown...", 2) return
            end
            robotMode = "manual"
            robotState = "roar"
            robotStateTimer = 0
            rNotif("Mecha Robot", "TITAN OVERDRIVE ROAR!", 3)
        end
    })

    BuildTab:AddButton({
        Name = "Kneel (Mount)",
        Description = "Titan kneels down so you can easily sit in the cockpit throne",
        Callback = function()
            if not robotActive then rNotif("Mecha Robot", "Spawn the robot first!", 2) return end
            robotMode = "manual"
            robotState = "kneel"
            robotStateTimer = 0
            rNotif("Mecha Robot", "Titan kneels... climb into the cockpit throne!", 4)
            task.delay(12, function()
                if robotActive and robotState == "kneel" then
                    robotState = "idle"
                    rNotif("Mecha Robot", "The titan stands ready!", 2)
                end
            end)
        end
    })

    BuildTab:AddSection({"> Special Modes"})

    BuildTab:AddButton({
        Name = "Shield Mode",
        Description = "Armor parts detach and form an orbital energy barrier",
        Callback = function()
            if not robotActive then rNotif("Mecha Robot", "Spawn the robot first!", 2) return end
            if robotFollowName == "---" or robotFollowName == "" then
                rNotif("Mecha Robot", "Select a target or yourself first!", 3) return
            end
            robotMode = "shield"
            robotState = "shield"
            robotStateTimer = 0
            rNotif("Mecha Robot", "TITAN AEGIS SHIELD - Orbiting around " .. robotFollowName, 4)
        end
    })

    BuildTab:AddButton({
        Name = "Stop Shield",
        Description = "Stop the shield orbit and reassemble the titan",
        Callback = function()
            if not robotActive then return end
            robotMode = "manual"
            robotState = "idle"
            robotStateTimer = 0
            rNotif("Mecha Robot", "Titan reassembled.", 2)
        end
    })

end

local PropStreamer = {}
PropStreamer.__index = PropStreamer

function PropStreamer.new(rate, deltaThreshold)
    return setmetatable({
        slots = {},
        rate = rate or 0.066,           
        deltaThreshold = deltaThreshold or 0.75, 
        running = false,
        conn = nil,
    }, PropStreamer)
end

function PropStreamer:add(scf, cpc)
    if scf and not self.slots[scf] then
        self.slots[scf] = {
            cf = nil, color = nil,
            dirtyCF = false, dirtyColor = false,
            lastCF = nil, lastSent = 0,
            cpc = cpc,
        }
    end
end

function PropStreamer:setCF(scf, cf)
    local s = self.slots[scf]
    if s then
        s.cf = cf
        local now = tick()
        local moved = (not s.lastCF) or (s.lastCF.Position - cf.Position).Magnitude > self.deltaThreshold
        
        s.dirtyCF = s.dirtyCF or moved or (now - s.lastSent > 0.25)
    end
end

function PropStreamer:setColor(scf, color)
    local s = self.slots[scf]
    if s and s.cpc then
        s.color = color
        s.dirtyColor = true
    end
end

function PropStreamer:start()
    if self.running then return end
    self.running = true
    local rs = game:GetService("RunService")
    self.conn = rs.RenderStepped:Connect(function()
        if not self.running then
            if self.conn then self.conn:Disconnect() self.conn = nil end
            return
        end
        local now = tick()
        for scf, s in pairs(self.slots) do
            if (s.dirtyCF or s.dirtyColor) and (now - s.lastSent >= self.rate) then
                s.lastSent = now
                if s.dirtyCF and s.cf then
                    s.dirtyCF = false
                    local cf = s.cf
                    task.spawn(function() pcall(function() scf:InvokeServer(cf) end) end)
                end
                if s.dirtyColor and s.color and s.cpc then
                    s.dirtyColor = false
                    local c = s.color
                    local cpc = s.cpc
                    task.spawn(function() pcall(function() cpc:InvokeServer(c) end) end)
                end
            end
        end
    end)
end

function PropStreamer:stop()
    self.running = false
    self.slots = {}
end

-- [LOCKED: WORK PERFECTLY] LICORNE GEANTE (porte du hub)

do
    local lp = game:GetService("Players").LocalPlayer
    local unicornActive = false
    local unicornForceReturn = false
    local unicornForceTakeoff = false
    local unicornParts  = {}
    local unicornLoop   = nil
    local UNICORN_HEIGHT = 16 
    local UNICORN_RADIUS = 20
    local uStream = PropStreamer.new(0.066)

    local function uNotif(txt, dur)
        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Unicorn", Text=txt, Duration=dur or 3})
    end

    
    
    
    
    local blueprint = {
        
        {key="selle",     prop="FurnitureSilverThrone", cat="Furniture",
         off=Vector3.new(0, 1.8, -0.8), rot=CFrame.Angles(0,0,0),
         color=Color3.fromRGB(200,200,200),
         anim=function(t) return CFrame.new(0, math.sin(t*7)*0.35, 0) end},

        
        {key="passagers", prop="FurnitureTablePicnic", cat="Furniture",
         off=Vector3.new(0, 1, 0), rot=CFrame.Angles(0,math.rad(90),0),
         color=Color3.fromRGB(255,150,200),
         anim=function(t) return CFrame.new(0, math.sin(t*7)*0.35, 0) end},

        
        {key="corps",     prop="PlantDirtBig",         cat="Plant",
         off=Vector3.new(0, -2, 0), rot=CFrame.Angles(0,0,0),
         color=Color3.fromRGB(255,245,250),
         anim=function(t) return CFrame.new(0, math.sin(t*7)*0.35, 0) end},

        
        {key="cou_1",     prop="PlantDirtSmall",       cat="Plant",
         off=Vector3.new(0, -1, 3.5), rot=CFrame.Angles(0,0,0),
         color=Color3.fromRGB(255,245,250),
         anim=function(t) return CFrame.new(0, math.sin(t*7)*0.35, 0) end},

        
        {key="cou_2",     prop="PlantDirtSmall",       cat="Plant",
         off=Vector3.new(0, 1, 5), rot=CFrame.Angles(0,0,0),
         color=Color3.fromRGB(255,245,250),
         anim=function(t) return CFrame.new(0, math.sin(t*7)*0.35, 0) end},

        
        {key="tete",      prop="PlantDirtSmall",       cat="Plant",
         off=Vector3.new(0, 3, 6.5), rot=CFrame.Angles(0,0,0),
         color=Color3.fromRGB(255,150,200), 
         anim=function(t)
             return CFrame.new(0, math.sin(t*7)*0.35, 0) * CFrame.Angles(math.sin(t*3.5)*0.06, 0, 0)
         end},

        
        {key="corne",     prop="TubeLamp",             cat="Lights",
         off=Vector3.new(0, 4.5, 7.5), rot=CFrame.Angles(math.rad(45), math.rad(90), 0),
         color=Color3.fromRGB(255,215,0), 
         anim=function(t) return CFrame.new(0, math.sin(t*7)*0.35, 0) end},

        
        {key="patte_avG", prop="PillarSmall",          cat="Building",
         off=Vector3.new(-2, -2, 2.5), rot=CFrame.Angles(math.pi,0,0),
         color=Color3.fromRGB(255,245,250),
         anim=function(t) return CFrame.Angles(math.sin(t*7)*0.5, 0, 0) end},

        
        {key="patte_avD", prop="PillarSmall",          cat="Building",
         off=Vector3.new( 2, -2, 2.5), rot=CFrame.Angles(math.pi,0,0),
         color=Color3.fromRGB(255,245,250),
         anim=function(t) return CFrame.Angles(math.sin(t*7)*0.5, 0, 0) end},

        
        {key="patte_arG", prop="PillarSmall",          cat="Building",
         off=Vector3.new(-2, -2, -2.5), rot=CFrame.Angles(math.pi,0,0),
         color=Color3.fromRGB(255,245,250),
         anim=function(t) return CFrame.Angles(math.sin(t*7 + math.pi)*0.5, 0, 0) end},

        
        {key="patte_arD", prop="PillarSmall",          cat="Building",
         off=Vector3.new( 2, -2, -2.5), rot=CFrame.Angles(math.pi,0,0),
         color=Color3.fromRGB(255,245,250),
         anim=function(t) return CFrame.Angles(math.sin(t*7 + math.pi)*0.5, 0, 0) end},

        
        {key="aile_G",    prop="QuarterFloor",         cat="Building",
         off=Vector3.new(-4, 0, 0), rot=CFrame.Angles(0, 0, math.rad(-10)),
         color=Color3.fromRGB(255,255,255),
         anim=function(t) return CFrame.Angles(0, 0, math.sin(t*10)*0.4) * CFrame.new(0, math.sin(t*7)*0.35, 0) end},

        
        {key="aile_D",    prop="QuarterFloor",         cat="Building",
         off=Vector3.new(4, 0, 0), rot=CFrame.Angles(0, 0, math.rad(10)),
         color=Color3.fromRGB(255,255,255),
         anim=function(t) return CFrame.Angles(0, 0, -math.sin(t*10)*0.4) * CFrame.new(0, math.sin(t*7)*0.35, 0) end},

        
        {key="queue1",    prop="PlantDirtSmall",       cat="Plant",
         off=Vector3.new(0, -1, -4.5), rot=CFrame.Angles(math.rad(20), 0, 0),
         color=Color3.fromRGB(230, 100, 220),
         anim=function(t) return CFrame.Angles(math.sin(t*5)*0.3, 0, 0) * CFrame.new(0, math.sin(t*7)*0.35, 0) end},
         
        
        {key="queue2",    prop="PlantDirtSmall",       cat="Plant",
         off=Vector3.new(0, -2.5, -5.5), rot=CFrame.Angles(math.rad(40), 0, 0),
         color=Color3.fromRGB(210, 80, 200),
         anim=function(t) return CFrame.Angles(math.sin(t*5)*0.4, 0, 0) * CFrame.new(0, math.sin(t*7)*0.35, 0) end},
    }

    
    
    
    local function spawnUnicorn()
        local lp = game:GetService("Players").LocalPlayer
        local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools  = RE:WaitForChild("1Clea1rTool1s")
        local toolRemote  = RE:WaitForChild("1Too1l")
        local Telemetry   = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction")

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.3)
        local propsTool = getOrFetchPropMaker()
        if not propsTool then uNotif("Error: PropMaker not found!", 4) return end

        
        local hum = lp.Character and lp.Character:FindFirstChild("Humanoid")
        if hum and propsTool.Parent ~= lp.Character then
            hum:EquipTool(propsTool)
            task.wait(0.2)
        end

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
            if not toolPropMake then return end

        unicornParts = {}
        uNotif("Summoning the unicorn... ("..#blueprint.." parts, ~"..#blueprint.."s)", 5)

        for idx, bp in ipairs(blueprint) do
            if not unicornActive then break end

            local spawnPos = (lp.Character and lp.Character:FindFirstChild("HumanoidRootPart") and lp.Character.HumanoidRootPart.Position) or Vector3.new(0,5,0)
            local offsetSpawn = Vector3.new(math.cos(idx)*4, 3, math.sin(idx)*4)

            
            pcall(function() Telemetry:FireServer("filterClick", {name=bp.cat,  itemType="Props"}) end)
            task.wait(0.05)
            pcall(function() Telemetry:FireServer("filterClick", {name=bp.prop, itemType="Props"}) end)
            task.wait(0.05)
            pcall(function() if bp.prop == "FloorCushion" then clearTools:FireServer("RequestingPropFeatureName", bp.prop, bp.cat) else clearTools:FireServer("RequestingPropName", bp.prop, bp.cat) end end)
            task.wait(0.1)

            
            pcall(function() toolPropMake:FireServer(workspace.Terrain, spawnPos + offsetSpawn) end)
            task.wait(0.85)

            if not unicornActive then break end

            
            local myPropName = "Prop" .. lp.Name
            local found = false
            for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                if found then break end
                for _, prop in pairs(folder:GetChildren()) do
                    if prop.Name == myPropName then
                        local scf = prop:FindFirstChild("SetCurrentCFrame")
                        local cpc = prop:FindFirstChild("ChangePropColor")
                        if scf then
                            local alreadyHave = false
                            for _, p in ipairs(unicornParts) do
                                if p.scf == scf then alreadyHave = true break end
                            end
                            if not alreadyHave then
                                
                                if cpc and bp.color then
                                    pcall(function() cpc:InvokeServer(bp.color) end)
                                end
                                table.insert(unicornParts, {key=bp.key, scf=scf, cpc=cpc, blueprint=bp, propModel=prop})
                                found = true
                                break
                            end
                        end
                    end
                end
            end
        end

        for _, _p in ipairs(unicornParts) do uStream:add(_p.scf, _p.cpc) end
        uStream:start()
        cleanupPropMakerTool()
                uNotif("Unicorn ready! It is waiting for you on the ground. Climb on!", 5)
    end

    local unicornGui = nil
    local isUIUp = false
    local isUIDown = false
    local isUIBoost = false

    local function removeUnicornGUI()
        if unicornGui then
            unicornGui:Destroy()
            unicornGui = nil
        end
        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, true) end
    end

    local function setupUnicornGUI()
        if unicornGui then return end
        
        
        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, false) end
        
        local sg = Instance.new("ScreenGui")
        sg.Name = "UnicornRiderGui"
        sg.Parent = game:GetService("CoreGui")
        unicornGui = sg
        
        local panel = Instance.new("Frame")
        panel.Size = UDim2.new(0, 200, 0, 200)
        panel.Position = UDim2.new(1, -220, 0.5, -100)
        panel.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
        panel.BackgroundTransparency = 0.2
        Instance.new("UICorner", panel).CornerRadius = UDim.new(0, 10)
        panel.Parent = sg
        
        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 30)
        title.BackgroundTransparency = 1
        title.Text = "Unicorn Controls"
        title.TextColor3 = Color3.fromRGB(255, 200, 255)
        title.Font = Enum.Font.GothamBold
        title.TextSize = 16
        title.Parent = panel
        
        local function makeBtn(txt, posY, col, callback)
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1, -20, 0, 35)
            btn.Position = UDim2.new(0, 10, 0, posY)
            btn.BackgroundColor3 = col
            btn.TextColor3 = Color3.new(1,1,1)
            btn.Text = txt
            btn.TextScaled = true
            btn.Font = Enum.Font.GothamBold
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
            btn.Parent = panel
            
            local down = false
            btn.InputBegan:Connect(function(i)
                if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                    down = true
                    callback(true)
                end
            end)
            btn.InputEnded:Connect(function(i)
                if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                    down = false
                    callback(false)
                end
            end)
            return btn
        end
        
        makeBtn("Up (Space)", 40, Color3.fromRGB(80, 150, 200), function(val) isUIUp = val end)
        makeBtn("Down (Ctrl)", 80, Color3.fromRGB(200, 120, 80), function(val) isUIDown = val end)
        makeBtn("Sprint (Shift)", 120, Color3.fromRGB(150, 80, 200), function(val) isUIBoost = val end)
        
        local quitBtn = Instance.new("TextButton")
        quitBtn.Size = UDim2.new(1, -20, 0, 30)
        quitBtn.Position = UDim2.new(0, 10, 0, 160)
        quitBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        quitBtn.TextColor3 = Color3.new(1,1,1)
        quitBtn.Text = "Dismount"
        quitBtn.Font = Enum.Font.GothamBold
        Instance.new("UICorner", quitBtn).CornerRadius = UDim.new(0, 6)
        quitBtn.Parent = panel
        
        quitBtn.MouseButton1Click:Connect(function()
            removeUnicornGUI()
            local h = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if h then 
                h.Sit = false 
            end
        end)
    end

    local function isDriverSitting()
        local h = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if not h then return false end
        for _, p in ipairs(unicornParts) do
            if p.key == "selle" and p.propModel then
                for _, child in pairs(p.propModel:GetDescendants()) do
                    if child:IsA("Seat") and child.Occupant == h then
                        return true
                    end
                end
            end
        end
        return false
    end
    
    local function isAnyoneSitting()
        for _, p in ipairs(unicornParts) do
            if (p.key == "selle" or p.key == "passagers") and p.propModel then
                for _, child in pairs(p.propModel:GetDescendants()) do
                    if child:IsA("Seat") and child.Occupant ~= nil then
                        return true
                    end
                end
            end
        end
        return false
    end
    
    local function getFoldedAnim(bpKey, t2)
        
        if bpKey:sub(1,5) == "patte" then
            if bpKey:sub(7,8) == "av" then
                return CFrame.Angles(math.rad(70), 0, 0)
            else
                return CFrame.Angles(math.rad(-70), 0, 0)
            end
        elseif bpKey:sub(1,4) == "aile" then
            return CFrame.Angles(0, 0, 0)
        elseif bpKey:sub(1,3) == "cou" or bpKey == "tete" or bpKey == "corne" or bpKey == "oeil" then
            return CFrame.new(0, math.sin(t2*2)*0.2 - 2, 0)
        else
            return CFrame.new(0, math.sin(t2*2)*0.2 - 3, 0)
        end
    end

    
    
    
    local function startUnicornLoop()
        local lp  = game:GetService("Players").LocalPlayer
        local RS  = game:GetService("RunService")
        local t   = 0
        local animTime = 0
        local lastUpdate = 0
        
        local rideState = "resting"
        local waitStartTime = 0
        local flightTime = 0
        local flightDistance = 0
        local returnStartCF = nil
        
        local myRoot = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
        local spawnOrigin = myRoot and myRoot.CFrame or CFrame.new(0,5,0)
        spawnOrigin = CFrame.new(spawnOrigin.Position) * CFrame.Angles(0, math.rad(90), 0)
        local orbitCenter = spawnOrigin.Position

        
        local currentVelocity = 0
        local unicornWorldPos = spawnOrigin.Position
        local currentYaw = 0
        local unicornTargetWp = nil
        local currentPitch = 0
        local currentBank = 0
        local uis = game:GetService("UserInputService")
        local ws = game:GetService("Workspace")

        unicornLoop = RS.RenderStepped:Connect(function(dt)
            if not unicornActive then return end
            local _uNow = tick(); if _uNow - lastUpdate < (1/24) then return end; lastUpdate = _uNow 

            t = t + dt
            local unicornCF
            local useFolded = false

            unicornCF = spawnOrigin * CFrame.new(0, 1, 0)

            if rideState == "resting" then
                useFolded = true
                
                if isDriverSitting() then
                    setupUnicornGUI()
                    rideState = "manual_ground"
                    waitStartTime = tick()
                    uNotif("Unicorn tamed! WASD to walk, SHIFT to run, SPACE to take off.", 6)
                    
                    local _, yaw, _ = spawnOrigin:ToEulerAnglesYXZ()
                    currentYaw = yaw
                    currentPitch = 0
                    currentBank = 0
                    currentVelocity = 0
                    unicornWorldPos = unicornCF.Position
                elseif unicornForceTakeoff then
                    unicornForceTakeoff = false
                    rideState = "auto_flight"
                    flightTime = 0
                    flightDistance = 0
                    uNotif("Auto-ride started! Enjoy the view.", 5)
                elseif isAnyoneSitting() then
                    rideState = "waiting"
                    uNotif("Passenger seated! Climb on the throne to drive or click 'Take Off'.", 5)
                end

            elseif rideState == "waiting" then
                useFolded = false 
                
                if isDriverSitting() then
                    setupUnicornGUI()
                    rideState = "manual_ground"
                    waitStartTime = tick()
                    uNotif("Unicorn tamed! WASD to walk, SHIFT to run, SPACE to take off.", 6)
                    
                    local _, yaw, _ = spawnOrigin:ToEulerAnglesYXZ()
                    currentYaw = yaw
                    currentPitch = 0
                    currentBank = 0
                    currentVelocity = 0
                    unicornWorldPos = unicornCF.Position
                elseif unicornForceTakeoff then
                    unicornForceTakeoff = false
                    rideState = "auto_flight"
                    flightTime = 0
                    flightDistance = 0
                    uNotif("Auto-ride started! Enjoy the view.", 5)
                elseif not isAnyoneSitting() then
                    rideState = "resting"
                end

            elseif rideState == "manual_ground" then
                useFolded = false
                
                local moveDir = Vector3.new(0,0,0)
                if uis:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + Vector3.new(0,0,-1) end
                if uis:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir + Vector3.new(0,0,1) end
                if uis:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir + Vector3.new(-1,0,0) end
                if uis:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + Vector3.new(1,0,0) end
                
                local isRunning = uis:IsKeyDown(Enum.KeyCode.LeftShift) or isUIBoost
                local targetSpeed = 0
                
                if moveDir.Magnitude > 0.1 then
                    targetSpeed = isRunning and 140 or 50
                    
                    local cam = ws.CurrentCamera
                    local camLook = cam.CFrame.LookVector
                    local camYaw = (math.atan2 or math.atan)(-camLook.X, -camLook.Z)
                    local localYaw = (math.atan2 or math.atan)(-moveDir.X, -moveDir.Z)
                    local targetYaw = camYaw + localYaw
                    
                    local angleDiff = targetYaw - currentYaw
                    while angleDiff >  math.pi do angleDiff = angleDiff - 2*math.pi end
                    while angleDiff < -math.pi do angleDiff = angleDiff + 2*math.pi end
                    currentYaw = currentYaw + angleDiff * 10 * dt
                end
                
                currentVelocity = currentVelocity + (targetSpeed - currentVelocity) * 8 * dt
                local forwardDir = (CFrame.new(Vector3.new(), Vector3.new(0,0,-1)) * CFrame.Angles(0, currentYaw, 0)).LookVector
                unicornWorldPos = unicornWorldPos + forwardDir * currentVelocity * dt
                
                
                local rayOrigin = unicornWorldPos + Vector3.new(0, 100, 0)
                local rayDirection = Vector3.new(0, -200, 0)
                local rayParams = RaycastParams.new()
                local _ignoreList = {lp.Character}
                for _, pd in ipairs(unicornParts) do if pd.propModel then table.insert(_ignoreList, pd.propModel) end end
                rayParams.FilterDescendantsInstances = _ignoreList
                rayParams.FilterType = Enum.RaycastFilterType.Exclude

                local result = ws:Raycast(rayOrigin, rayDirection, rayParams)
                local groundY = result and result.Position.Y or (unicornWorldPos.Y - 10)
                local targetY = groundY + UNICORN_HEIGHT
                
                local maxCorr = 60 * dt
                unicornWorldPos = Vector3.new(unicornWorldPos.X, unicornWorldPos.Y + math.clamp(targetY - unicornWorldPos.Y, -maxCorr, maxCorr), unicornWorldPos.Z)
                
                local pitchTarget = 0
                if result and currentVelocity > 5 then
                    local fOrigin = rayOrigin + forwardDir * 8
                    local fResult = ws:Raycast(fOrigin, rayDirection, rayParams)
                    if fResult then
                        local slopeDiff = fResult.Position.Y - groundY
                        pitchTarget = (math.atan2 or math.atan)(slopeDiff, 8)
                    end
                end
                currentPitch = currentPitch + (pitchTarget - currentPitch) * 6 * dt
                currentBank = currentBank + (0 - currentBank) * 6 * dt
                
                unicornCF = CFrame.new(unicornWorldPos) * CFrame.Angles(0, currentYaw, 0) * CFrame.Angles(currentPitch, 0, currentBank) * CFrame.Angles(0, math.pi, 0)
                animTime = animTime + dt * (1 + currentVelocity / 30)
                
                if uis:IsKeyDown(Enum.KeyCode.Space) or isUIUp or unicornForceTakeoff then
                    unicornForceTakeoff = false
                    rideState = "manual_flight"
                    flightTime = 0
                    uNotif("Take off! WASD to fly, Space = Ascend, Ctrl = Descend.", 5)
                end
                
                if not isSomeoneSitting() then
                    removeUnicornGUI()
                    rideState = "idle"
                end

            elseif rideState == "idle" then
                useFolded = false
                unicornCF = CFrame.new(unicornWorldPos) * CFrame.Angles(0, currentYaw, 0) * CFrame.Angles(currentPitch, 0, currentBank) * CFrame.Angles(0, math.pi, 0)
                animTime = animTime + dt
                
                if isDriverSitting() then
                    setupUnicornGUI()
                    rideState = "manual_ground"
                    waitStartTime = tick()
                    uNotif("Unicorn tamed! WASD to walk, SHIFT to run, SPACE to take off.", 6)
                elseif unicornForceTakeoff then
                    unicornForceTakeoff = false
                    rideState = "auto_flight"
                    flightTime = 0
                    flightDistance = 0
                    uNotif("Auto-ride started! Enjoy the view.", 5)
                elseif not isAnyoneSitting() then
                    rideState = "resting"
                end

            elseif rideState == "manual_flight" then
                flightTime = flightTime + dt
                
                local moveDir = Vector3.new(0,0,0)
                if uis:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + Vector3.new(0,0,-1) end
                if uis:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir + Vector3.new(0,0,1) end
                if uis:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir + Vector3.new(-1,0,0) end
                if uis:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + Vector3.new(1,0,0) end
                
                local isRunning = uis:IsKeyDown(Enum.KeyCode.LeftShift) or isUIBoost
                local targetSpeed = isRunning and 300 or 120
                
                local bankTarget = 0
                if moveDir.Magnitude > 0.1 then
                    local cam = ws.CurrentCamera
                    local camLook = cam.CFrame.LookVector
                    local camYaw = (math.atan2 or math.atan)(-camLook.X, -camLook.Z)
                    local localYaw = (math.atan2 or math.atan)(-moveDir.X, -moveDir.Z)
                    local targetYaw = camYaw + localYaw
                    
                    local angleDiff = targetYaw - currentYaw
                    while angleDiff >  math.pi do angleDiff = angleDiff - 2*math.pi end
                    while angleDiff < -math.pi do angleDiff = angleDiff + 2*math.pi end
                    currentYaw = currentYaw + angleDiff * 8 * dt
                    
                    currentVelocity = currentVelocity + (targetSpeed - currentVelocity) * 5 * dt
                    bankTarget = -angleDiff * 0.35
                else
                    currentVelocity = currentVelocity + (0 - currentVelocity) * 2 * dt
                end
                
                local vSpeed = 0
                if uis:IsKeyDown(Enum.KeyCode.Space) or isUIUp then vSpeed = 100 end
                if uis:IsKeyDown(Enum.KeyCode.LeftControl) or isUIDown then vSpeed = -100 end
                
                local forwardDir = (CFrame.new(Vector3.new(), Vector3.new(0,0,-1)) * CFrame.Angles(0, currentYaw, 0)).LookVector
                unicornWorldPos = unicornWorldPos + forwardDir * currentVelocity * dt
                unicornWorldPos = unicornWorldPos + Vector3.new(0, vSpeed * dt, 0)
                
                local pitchTarget = (vSpeed / 100) * math.rad(20)
                currentPitch = currentPitch + (pitchTarget - currentPitch) * 5 * dt
                currentBank = currentBank + (bankTarget - currentBank) * 5 * dt
                
                
                local minX, maxX = -1425, 1454
                local minZ, maxZ = -1476, 1403
                if unicornWorldPos.X < minX or unicornWorldPos.X > maxX or unicornWorldPos.Z < minZ or unicornWorldPos.Z > maxZ then
                    local toCenter = Vector3.new(0, 0, 0) - unicornWorldPos
                    unicornWorldPos = unicornWorldPos + toCenter.Unit * currentVelocity * dt
                end
                
                unicornCF = CFrame.new(unicornWorldPos) * CFrame.Angles(0, currentYaw, 0) * CFrame.Angles(currentPitch, 0, currentBank) * CFrame.Angles(0, math.pi, 0)
                animTime = animTime + dt * (1 + currentVelocity / 50)
                
                
                local rayOrigin = unicornWorldPos
                local rayDirection = Vector3.new(0, -UNICORN_HEIGHT - 10, 0)
                local rayParams = RaycastParams.new()
                local _ignoreList2 = {lp.Character}
                for _, pd in ipairs(unicornParts) do if pd.propModel then table.insert(_ignoreList2, pd.propModel) end end
                rayParams.FilterDescendantsInstances = _ignoreList2
                rayParams.FilterType = Enum.RaycastFilterType.Exclude

                local result = ws:Raycast(rayOrigin, rayDirection, rayParams)
                if result and vSpeed < 0 then
                    rideState = "manual_ground"
                    uNotif("Landing successful!", 3)
                end
                
                if unicornForceReturn then
                    removeUnicornGUI()
                    unicornForceReturn = false
                    rideState = "returning"
                    waitStartTime = tick()
                    returnStartCF = unicornCF
                end
                
                if not isDriverSitting() then
                    removeUnicornGUI()
                    rideState = "idle"
                end
                
            elseif rideState == "auto_flight" then
                useFolded = false
                flightTime = flightTime + dt
                
                local currentSpeed = 100 + math.sin(flightTime * 0.5) * 50
                if uis:IsKeyDown(Enum.KeyCode.LeftShift) then
                    currentSpeed = 300
                end
                
                flightDistance = flightDistance + currentSpeed * dt
                local tSpeed = flightDistance / 800
                local maxRadX = 1400
                local maxRadZ = 1200
                
                local targetX = math.sin(tSpeed * 0.8) * maxRadX + math.sin(tSpeed * 1.5) * 200
                local targetZ = (1 - math.cos(tSpeed * 1.1)) * maxRadZ + math.sin(tSpeed * 0.7) * 250
                local heightOffset = math.sin(tSpeed * 2.0) * 80 + math.cos(tSpeed * 0.9) * 40
                
                unicornWorldPos = orbitCenter + Vector3.new(targetX, 140 + heightOffset, targetZ)
                
                local tNext = (flightDistance + 40) / 800
                local nextX = math.sin(tNext * 0.8) * maxRadX + math.sin(tNext * 1.5) * 200
                local nextZ = (1 - math.cos(tNext * 1.1)) * maxRadZ + math.sin(tNext * 0.7) * 250
                
                local dir = Vector3.new(nextX - targetX, 0, nextZ - targetZ).Unit
                
                local barrelRoll = 0
                if math.sin(tSpeed * 3) > 0.95 then
                    barrelRoll = (math.sin(tSpeed * 3) - 0.95) * 20 * math.pi
                end
                
                local bankAngle = math.sin(tSpeed * 1.5) * 0.5
                
                unicornCF = CFrame.lookAt(unicornWorldPos, unicornWorldPos - dir) * CFrame.Angles(0, 0, bankAngle + barrelRoll)
                animTime = animTime + dt * 1.5
                
                if flightTime > 60 or unicornForceReturn then
                    unicornForceReturn = false
                    rideState = "returning"
                    waitStartTime = tick()
                    returnStartCF = unicornCF
                    uNotif("Ride over! Returning to base.", 4)
                end
                
                
                if isDriverSitting() and (uis:IsKeyDown(Enum.KeyCode.W) or uis:IsKeyDown(Enum.KeyCode.A) or uis:IsKeyDown(Enum.KeyCode.S) or uis:IsKeyDown(Enum.KeyCode.D)) then
                    setupUnicornGUI()
                    rideState = "manual_flight"
                    local _, yaw, _ = unicornCF:ToEulerAnglesYXZ()
                    currentYaw = yaw
                    currentPitch = 0
                    currentBank = 0
                    currentVelocity = currentSpeed
                    uNotif("You took back manual controls!", 4)
                end

            elseif rideState == "returning" then
                local elapsed = tick() - waitStartTime
                local returnTime = 10
                local returnProgress = math.min(elapsed / returnTime, 1)

                local currentRoot = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
                local targetPos = currentRoot and currentRoot.Position or orbitCenter
                local targetOrigin = CFrame.new(targetPos + Vector3.new(0,0,-15)) * CFrame.Angles(0, math.rad(90), 0)
                
                local targetCF = targetOrigin * CFrame.new(0, 38, 0)
                unicornCF = returnStartCF:Lerp(targetCF, returnProgress)
                
                if elapsed > returnTime then
                    rideState = "landing"
                    waitStartTime = tick()
                    spawnOrigin = targetOrigin
                end

            elseif rideState == "landing" then
                local elapsed = tick() - waitStartTime
                local landProgress = math.min(elapsed / 4, 1)
                
                local startH = 38
                local currentH = startH - (landProgress * (startH - 1))
                
                unicornCF = spawnOrigin * CFrame.new(0, currentH, 0)
                useFolded = (landProgress > 0.8) 
                
                if elapsed > 4 then
                    unicornForceTakeoff = false
                    unicornForceReturn = false
                    rideState = "resting"
                end
            end

            
                if rideState ~= "flying" then
                    animTime = animTime + dt
                end

            for _, pData in ipairs(unicornParts) do
                local bp = pData.blueprint
                if bp and pData.scf then
                    local animDelta
                    if useFolded then
                        animDelta = getFoldedAnim(bp.key, animTime)
                    elseif bp.anim then
                        animDelta = bp.anim(animTime)
                    else
                        animDelta = CFrame.new()
                    end
                    local finalRot = bp.rot or CFrame.Angles(0,0,0)
                    local partCF = unicornCF * CFrame.new(bp.off) * finalRot * animDelta
                    uStream:setCF(pData.scf, partCF)
                end
            end
        end)
    end

    
    
    
    BuildTab:AddSection({"Attraction: Giant Unicorn"})

    BuildTab:AddToggle({
        Name = "Summon Unicorn",
        Description = "Summons a giant magical unicorn (~15s). Climb on the table!",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not unicornActive then return end  
            unicornActive = val
            unicornForceReturn = false
            if val then
                task.spawn(function()
                    spawnUnicorn()
                    if unicornActive then startUnicornLoop() end
                end)
            else
                if unicornLoop then unicornLoop:Disconnect() unicornLoop = nil end
                uStream:stop()
                unicornParts = {}
                local RE2 = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                local ct2 = RE2 and RE2:FindFirstChild("1Clea1rTool1s")
                if ct2 then pcall(function() ct2:FireServer("ClearAllProps") end) end
                uNotif("The unicorn fades into the stars...", 3)
            end
        end
    })
    
    BuildTab:AddButton({
        Name = "Take Off with Unicorn",
        Callback = function()
            if unicornActive then
                unicornForceTakeoff = true
            end
        end
    })
    
    BuildTab:AddButton({
        Name = "Recall Unicorn",
        Callback = function()
            if unicornActive then
                unicornForceReturn = true
                uNotif("The unicorn heard your call and is returning to base!", 4)
            end
        end
    })

end 

do
    local leviathanActive        = false
    local leviathanWorkers       = {}
    local leviathanLoop          = nil
    local leviathanAnchor        = Vector3.new(0, 0, 0)
    local leviathanAngle         = 0
    local leviathanAnimT         = 0
    local leviathanState         = "idle"
    local leviathanStateTimer    = 0
    local leviathanMode          = "manual"
    local leviathanFollowName    = ""
    local leviathanWaypoint      = nil
    local leviathanSpeed         = 24
    local leviathanCooldowns     = {}
    local leviathanTarget        = nil
    local leviathanColorMode     = "Abyssal Cyan"
    local leviathanDriverSeat    = nil
    local leviathanBreachedNotif = false
    local leviathanSlitherWeight = 0
    local leviathanTurnBank      = 0

    
    local pilotGui               = nil
    local hudFrame               = nil
    local hudStatusLabel         = nil
    local isAscendingHeld        = false
    local isDescendingHeld       = false
    local isSprintToggled        = false
    local inputConn              = nil

    local function lNotif(txt, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Abyssal Leviathan",
                Text = txt,
                Duration = dur or 3
            })
        end)
    end

    local lp = game:GetService("Players").LocalPlayer

    local function canDo(actionKey, cd)
        local now = tick()
        if leviathanCooldowns[actionKey] and (now - leviathanCooldowns[actionKey]) < cd then
            return false
        end
        leviathanCooldowns[actionKey] = now
        return true
    end

    local function triggerBreach()
        if not leviathanActive then lNotif("Summon the Leviathan first!", 2) return end
        if not canDo("breach", 6.5) then lNotif("Breach on cooldown...", 2) return end
        leviathanMode = "manual"
        leviathanState = "breach"
        leviathanStateTimer = 0
        leviathanBreachedNotif = false
        lNotif("THE LEVIATHAN PLUNGES INTO THE ABYSS!", 2.5)
    end

    
    
    local LEVIATHAN_LAYOUT = {
        {key="body_base",   prop="FurnitureBleachers",    cat="Furniture", spineIndex=1,
         off=Vector3.new(0.0,  1.5,   6.0), rot=CFrame.Angles(math.rad(12), 0, 0),
         slitherOff=Vector3.new(0.0, 3.0,  14.0), slitherRot=CFrame.Angles(0, 0, 0)},

        {key="torso_lower", prop="FurnitureBleachers",    cat="Furniture", spineIndex=2,
         off=Vector3.new(0.0,  6.0,   4.2), rot=CFrame.Angles(math.rad(16), 0, 0),
         slitherOff=Vector3.new(0.0, 3.2,   9.5), slitherRot=CFrame.Angles(0, 0, 0)},

        {key="torso_mid",   prop="FurnitureBleachers",    cat="Furniture", spineIndex=3,
         off=Vector3.new(0.0, 10.5,   2.5), rot=CFrame.Angles(math.rad(20), 0, 0),
         slitherOff=Vector3.new(0.0, 3.8,   5.0), slitherRot=CFrame.Angles(math.rad(5), 0, 0)},

        {key="torso_upper", prop="FurnitureBleachers",    cat="Furniture", spineIndex=4,
         off=Vector3.new(0.0, 15.0,   1.0), rot=CFrame.Angles(math.rad(22), 0, 0),
         slitherOff=Vector3.new(0.0, 5.2,   0.5), slitherRot=CFrame.Angles(math.rad(12), 0, 0)},

        {key="neck_lower",  prop="FurnitureBleachers",    cat="Furniture", spineIndex=5,
         off=Vector3.new(0.0, 19.5,  -0.5), rot=CFrame.Angles(math.rad(24), 0, 0),
         slitherOff=Vector3.new(0.0, 7.5,  -3.8), slitherRot=CFrame.Angles(math.rad(18), 0, 0)},

        {key="neck_upper",  prop="FurnitureBleachers",    cat="Furniture", spineIndex=6,
         off=Vector3.new(0.0, 24.0,  -2.2), rot=CFrame.Angles(math.rad(26), 0, 0),
         slitherOff=Vector3.new(0.0, 10.2, -8.0), slitherRot=CFrame.Angles(math.rad(22), 0, 0)},

        {key="neck_arch",   prop="FurnitureBleachers",    cat="Furniture", spineIndex=7,
         off=Vector3.new(0.0, 27.8,  -4.2), rot=CFrame.Angles(math.rad(28), 0, 0),
         slitherOff=Vector3.new(0.0, 12.8, -12.5), slitherRot=CFrame.Angles(math.rad(15), 0, 0)},

        {key="head_upper",  prop="FurnitureBleachers",    cat="Furniture", spineIndex=8,
         off=Vector3.new(0.0, 26.2,  -9.0), rot=CFrame.Angles(math.rad(15), 0, 0),
         slitherOff=Vector3.new(0.0, 10.5, -16.5), slitherRot=CFrame.Angles(0, 0, 0)},

        {key="jaw_lower",   prop="FurnitureBleachers",    cat="Furniture", spineIndex=9,
         off=Vector3.new(0.0, 21.8,  -9.2), rot=CFrame.Angles(math.rad(25), 0, 0),
         slitherOff=Vector3.new(0.0,  8.5, -16.8), slitherRot=CFrame.Angles(math.rad(18), 0, 0)},

        {key="head_crown",  prop="FurnitureBleachers",    cat="Furniture", spineIndex=10,
         off=Vector3.new(0.0, 30.5,  -8.2), rot=CFrame.Angles(-math.rad(65), 0, 0),
         slitherOff=Vector3.new(0.0, 13.5, -15.8), slitherRot=CFrame.Angles(-math.rad(35), 0, 0)},

        {key="breath",      prop="TorchFire",             cat="Torch",     spineIndex=11,
         off=Vector3.new(0.0, 24.0, -10.5), rot=CFrame.Angles(math.rad(90), 0, 0),
         slitherOff=Vector3.new(0.0,  9.5, -18.2), slitherRot=CFrame.Angles(math.rad(90), 0, 0)},

        {key="throne",      prop="FurnitureSilverThrone", cat="Furniture", spineIndex=12,
         off=Vector3.new(0.0, 28.8,  -3.2), rot=CFrame.Angles(0, 0, 0),
         slitherOff=Vector3.new(0.0, 13.8, -11.5), slitherRot=CFrame.Angles(0, 0, 0)},

        {key="tail_stem",   prop="FurnitureBleachers",    cat="Furniture", spineIndex=13,
         off=Vector3.new(0.0,  1.8,  11.8), rot=CFrame.Angles(-math.rad(15), 0, 0),
         slitherOff=Vector3.new(0.0, 3.2,  18.5), slitherRot=CFrame.Angles(0, 0, 0)},

        {key="tail_mid",    prop="FurnitureBleachers",    cat="Furniture", spineIndex=14,
         off=Vector3.new(0.0,  2.2,  17.5), rot=CFrame.Angles(math.rad(12), 0, 0),
         slitherOff=Vector3.new(0.0, 3.8,  23.2), slitherRot=CFrame.Angles(-math.rad(6), 0, 0)},

        {key="tail_fluke",  prop="FurnitureBleachers",    cat="Furniture", spineIndex=15,
         off=Vector3.new(0.0,  3.5,  23.2), rot=CFrame.Angles(math.rad(24), 0, 0),
         slitherOff=Vector3.new(0.0, 4.8,  28.0), slitherRot=CFrame.Angles(-math.rad(18), 0, 0)},
    }

    local function getLeviathanPartColor(layout, partIndex, t, state, colorMode)
        local pulse = (math.sin(t * 6) + 1) * 0.5
        local wave  = (math.sin(t * 4 + partIndex * 0.45) + 1) * 0.5

        if state == "roar" then
            if layout.key == "breath" then
                return Color3.fromRGB(255, 120, 20)
            end
            return Color3.fromRGB(math.floor(20 + pulse * 30), math.floor(160 + pulse * 90), 255)
        elseif state == "strike" then
            if layout.key == "jaw_lower" or layout.key == "head_upper" then
                return Color3.fromRGB(220, 30, 40)
            end
        elseif state == "sprint" then
            if layout.key == "tail_fluke" or layout.key == "head_crown" or layout.key == "breath" then
                local speedPulse = (math.sin(t * 16) + 1) * 0.5
                return Color3.fromRGB(0, math.floor(220 + speedPulse * 35), 255)
            end
        elseif state == "breach" then
            local p = math.clamp(stateTimer / 6.0, 0, 1)
            if p < 0.50 then
                local waveP = (math.sin(t * 16 + partIndex * 0.7) + 1) * 0.5
                if layout.key == "breath" then
                    return Color3.fromRGB(0, 180, 255)
                end
                return Color3.fromRGB(math.floor(5 + waveP * 15), math.floor(60 + waveP * 100), math.floor(140 + waveP * 90))
            elseif p < 0.78 then
                if layout.key == "breath" then
                    return Color3.fromRGB(255, 160, 40)
                elseif layout.key == "head_crown" or layout.key == "head_upper" or layout.key == "neck_arch" then
                    return Color3.fromRGB(210, 250, 255)
                else
                    local breachWave = (math.sin(t * 22 + partIndex * 0.5) + 1) * 0.5
                    return Color3.fromRGB(math.floor(20 + breachWave * 30), math.floor(200 + breachWave * 55), 255)
                end
            else
                local splashH = (p - 0.78) / 0.22
                return Color3.fromRGB(0, math.floor(180 + (1 - splashH) * 60), math.floor(220 + (1 - splashH) * 35))
            end
        end

        if colorMode == "Abyssal Cyan" then
            if layout.key == "head_crown" or layout.key == "neck_arch" or layout.key == "tail_fluke" then
                return Color3.new(0.05 + wave * 0.1, 0.45 + wave * 0.35, 0.75 + wave * 0.2)
            elseif layout.key == "breath" then
                return Color3.fromRGB(0, math.floor(220 + pulse * 35), 255)
            else
                return Color3.new(0.02 + wave * 0.05, 0.25 + wave * 0.2, 0.45 + wave * 0.25)
            end
        elseif colorMode == "Magma Titan" then
            if layout.key == "breath" or layout.key == "head_crown" then
                return Color3.fromRGB(255, math.floor(70 + pulse * 60), 0)
            elseif layout.key == "jaw_lower" or layout.key == "tail_fluke" then
                return Color3.fromRGB(220, math.floor(30 + wave * 40), 10)
            else
                return Color3.fromRGB(math.floor(40 + wave * 25), 10, 10)
            end
        elseif colorMode == "Shadow Void" then
            if layout.key == "breath" then
                return Color3.fromRGB(180, 0, 255)
            elseif layout.key == "head_crown" or layout.key == "tail_fluke" then
                return Color3.fromRGB(100, 0, 180)
            else
                return Color3.fromRGB(math.floor(12 + wave * 10), math.floor(12 + wave * 10), math.floor(20 + wave * 15))
            end
        elseif colorMode == "Rainbow Overdrive" then
            local hue = (t * 0.25 + partIndex * 0.065) % 1
            return Color3.fromHSV(hue, 0.85, 0.95)
        elseif colorMode == "Toxic Emerald" then
            if layout.key == "breath" then
                return Color3.fromRGB(80, 255, 40)
            elseif layout.key == "head_crown" or layout.key == "tail_fluke" then
                return Color3.fromRGB(40, 220, 80)
            else
                return Color3.fromRGB(10, math.floor(45 + wave * 40), 20)
            end
        end
        return Color3.fromRGB(10, 80, 140)
    end

    
    
    
    
    local function getLeviathanPartCFrame(layout, t, state, stateTimer, anchor, angle, slitherW, turnBank)
        local rootCF = CFrame.new(anchor) * CFrame.Angles(0, angle, 0)
        local w = slitherW or 0.0

        local rOff = layout.off
        local sOff = layout.slitherOff or rOff
        local basePos = rOff:Lerp(sOff, w)

        local rRot = layout.rot or CFrame.new()
        local sRot = layout.slitherRot or rRot
        local baseRot = rRot:Lerp(sRot, w)

        local animOff = Vector3.new(0, 0, 0)
        local animRot = CFrame.new()

        local key = layout.key
        local k = layout.spineIndex or 0

        if state == "sprint" or state == "walk" then
            local isSprint = (state == "sprint")
            local freq = isSprint and 6.6 or 3.8
            local lambdaZ = 0.082
            local segZ = basePos.Z
            local phase = t * freq - lambdaZ * (segZ + 16.5)

            
            local curAmp = 0.55
            if segZ <= -12.5 then
                curAmp = 0.55 
            elseif segZ <= 0.0 then
                curAmp = 0.55 + 1.95 * ((segZ + 12.5) / 12.5) 
            elseif segZ <= 14.0 then
                curAmp = 2.5 + 2.0 * (segZ / 14.0) 
            else
                curAmp = 4.5 + 1.7 * ((segZ - 14.0) / 14.0) 
            end

            if not isSprint then
                curAmp = curAmp * 0.65
            end

            
            local waveX = math.sin(phase) * curAmp

            
            local waveY = math.cos(phase * 2) * (isSprint and 0.42 or 0.22)

            
            local slope = curAmp * lambdaZ * math.cos(phase)
            local waveZ = -(slope * slope) * 1.15 + math.cos(phase * 2) * (isSprint and 0.35 or 0.15)

            
            local waveYaw = math.atan(slope) * 1.15

            
            local bankLean = turnBank or 0
            local waveRoll = math.sin(phase) * math.rad(6.5) + bankLean

            
            local wavePitch = math.sin(phase * 2) * math.rad(2.5)

            animOff = Vector3.new(waveX, waveY, waveZ)
            animRot = CFrame.Angles(wavePitch, waveYaw, waveRoll)

            if key == "jaw_lower" then
                local sprintJaw = isSprint and math.rad(18 + math.sin(phase * 2) * 8) or math.rad(6)
                animRot = animRot * CFrame.Angles(sprintJaw, 0, 0)
            end

        elseif state == "idle" then
            local breatheSpeed = 1.8
            local swaySpeed = 1.1

            if k >= 1 and k <= 7 then
                local prog = k / 7
                local bY = math.sin(t * breatheSpeed - prog * 0.8) * (0.2 + prog * 0.5)
                local sX = math.sin(t * swaySpeed - prog * 1.1) * (0.3 + prog * 0.8)
                local pPitch = math.cos(t * breatheSpeed - prog * 0.8) * math.rad(1.5 + prog * 2)
                local pYaw = math.cos(t * swaySpeed - prog * 1.1) * math.rad(2 + prog * 3)
                animOff = Vector3.new(sX, bY, 0)
                animRot = CFrame.Angles(pPitch, pYaw, 0)

            elseif key == "throne" then
                local bY = math.sin(t * breatheSpeed - 0.8) * 0.7
                local sX = math.sin(t * swaySpeed - 1.1) * 1.1
                local pPitch = math.cos(t * breatheSpeed - 0.8) * math.rad(3.5)
                local pYaw = math.cos(t * swaySpeed - 1.1) * math.rad(5)
                animOff = Vector3.new(sX, bY, 0)
                animRot = CFrame.Angles(pPitch, pYaw, 0)

            elseif key == "head_upper" or key == "head_crown" or key == "breath" then
                local bY = math.sin(t * breatheSpeed - 1.0) * 0.75
                local sX = math.sin(t * swaySpeed - 1.3) * 1.2
                local headNod = math.cos(t * breatheSpeed) * math.rad(3)
                local headScan = math.sin(t * 0.8) * math.rad(4.5)
                animOff = Vector3.new(sX, bY, 0)
                animRot = CFrame.Angles(headNod, headScan, 0)

            elseif key == "jaw_lower" then
                local bY = math.sin(t * breatheSpeed - 1.0) * 0.75
                local sX = math.sin(t * swaySpeed - 1.3) * 1.2
                local jawCycle = (math.sin(t * breatheSpeed) + 1) * 0.5
                local jawAngle = math.rad(3 + jawCycle * 9)
                animOff = Vector3.new(sX, bY - jawCycle * 0.6, 0)
                animRot = CFrame.Angles(jawAngle, math.sin(t * 0.8) * math.rad(4.5), 0)

            elseif key == "tail_stem" then
                local wave = math.sin(t * 2.2 - 0.8) * 1.0
                animOff = Vector3.new(wave, 0, 0)
                animRot = CFrame.Angles(0, math.cos(t * 2.2 - 0.8) * math.rad(7), 0)

            elseif key == "tail_mid" then
                local wave = math.sin(t * 2.2 - 1.6) * 1.8
                animOff = Vector3.new(wave, 0, 0)
                animRot = CFrame.Angles(0, math.cos(t * 2.2 - 1.6) * math.rad(14), 0)

            elseif key == "tail_fluke" then
                local wave = math.sin(t * 2.2 - 2.4) * 2.6
                animOff = Vector3.new(wave, 0, 0)
                animRot = CFrame.Angles(0, math.cos(t * 2.2 - 2.4) * math.rad(22), 0)
            end

        elseif state == "roar" then
            local p = math.clamp(stateTimer / 3.5, 0, 1)
            local env = math.sin(p * math.pi)

            if k >= 1 and k <= 7 then
                local prog = k / 7
                local lift = env * prog * 4.0
                local leanBack = env * prog * 3.5
                local pitchBack = -math.rad(env * prog * 16)
                animOff = Vector3.new(0, lift, leanBack)
                animRot = CFrame.Angles(pitchBack, 0, 0)

            elseif key == "throne" then
                local lift = env * 4.0
                local leanBack = env * 3.5
                local pitchBack = -math.rad(env * 16)
                animOff = Vector3.new(0, lift, leanBack)
                animRot = CFrame.Angles(pitchBack, 0, 0)

            elseif key == "head_upper" or key == "head_crown" or key == "breath" then
                local lift = env * 5.0
                local leanBack = env * 4.5
                local pitchBack = -math.rad(env * 32)
                animOff = Vector3.new(0, lift, leanBack)
                animRot = CFrame.Angles(pitchBack, 0, 0)

            elseif key == "jaw_lower" then
                local lift = env * 2.5
                local leanBack = env * 4.0
                local jawOpen = math.rad(env * 42)
                animOff = Vector3.new(0, lift - env * 1.5, leanBack)
                animRot = CFrame.Angles(jawOpen, 0, 0)

            elseif key == "tail_stem" or key == "tail_mid" or key == "tail_fluke" then
                local tK = (key == "tail_stem" and 1) or (key == "tail_mid" and 2) or 3
                animOff = Vector3.new(0, 0, -env * tK * 0.8)
                animRot = CFrame.Angles(math.rad(env * 10), 0, 0)
            end

        elseif state == "strike" then
            local p = math.clamp(stateTimer / 2.2, 0, 1)
            if p < 0.28 then
                local prep = math.sin((p / 0.28) * (math.pi / 2))
                if k >= 4 or key == "head_upper" or key == "jaw_lower" or key == "head_crown" or key == "breath" or key == "throne" then
                    animOff = Vector3.new(0, prep * 2.0, prep * 3.0)
                    animRot = CFrame.Angles(-math.rad(prep * 14), 0, 0)
                end
            elseif p < 0.65 then
                local snapFrac = (p - 0.28) / 0.37
                local snap = math.sin(snapFrac * math.pi)

                if k >= 4 then
                    local prog = (k - 3) / 4
                    local lungeY = -snap * prog * 8.0
                    local lungeZ = -snap * prog * 6.0
                    animOff = Vector3.new(0, lungeY, lungeZ)
                    animRot = CFrame.Angles(math.rad(snap * 18), 0, 0)

                elseif key == "head_upper" or key == "head_crown" or key == "breath" or key == "throne" then
                    local lungeY = -snap * 14.0
                    local lungeZ = -snap * 10.0
                    animOff = Vector3.new(0, lungeY, lungeZ)
                    animRot = CFrame.Angles(math.rad(snap * 26), 0, 0)

                elseif key == "jaw_lower" then
                    local lungeY = -snap * 16.0
                    local lungeZ = -snap * 10.0
                    local jawAngle = (snapFrac > 0.5) and -math.rad(10) or math.rad(34)
                    animOff = Vector3.new(0, lungeY, lungeZ)
                    animRot = CFrame.Angles(math.rad(snap * 26) + jawAngle, 0, 0)
                end
            else
                local rec = 1 - (p - 0.65) / 0.35
                if k >= 4 or key == "head_upper" or key == "jaw_lower" or key == "head_crown" or key == "breath" or key == "throne" then
                    animOff = Vector3.new(0, -rec * 2.0, -rec * 2.0)
                end
            end

        elseif state == "kneel" then
            local p = math.clamp(stateTimer * 1.5, 0, 1)
            local kneelP = p * p * (3 - 2 * p)

            if k >= 3 then
                local prog = (k - 2) / 5
                local bowY = -kneelP * prog * 20.0
                local bowZ = -kneelP * prog * 3.0
                animOff = Vector3.new(0, bowY, bowZ)
                animRot = CFrame.Angles(math.rad(kneelP * prog * 25), 0, 0)

            elseif key == "head_upper" or key == "jaw_lower" or key == "head_crown" or key == "breath" then
                local bowY = -kneelP * 21.5
                animOff = Vector3.new(0, bowY, -kneelP * 4.0)
                animRot = CFrame.Angles(math.rad(kneelP * 30), 0, 0)

            elseif key == "throne" then
                local bowY = -kneelP * 22.0
                animOff = Vector3.new(0, bowY, -kneelP * 3.5)
                animRot = CFrame.Angles(math.rad(kneelP * 15), 0, 0)
            end

        elseif state == "breach" then
            local p = math.clamp(stateTimer / 6.0, 0, 1)
            local bSubY = 0
            local bPitch = 0
            local bJawAngle = 0
            local bShake = 0

            if p < 0.32 then
                local f = p / 0.32
                bSubY = -38 * math.sin(f * (math.pi / 2))
                bPitch = -math.rad(50 * math.sin(f * (math.pi / 2)))
                bJawAngle = math.rad(30 * math.sin(f * math.pi))

            elseif p < 0.50 then
                local f = (p - 0.32) / 0.18
                bSubY = -38 - 4 * math.sin(f * math.pi)
                bPitch = math.rad(-50 + 105 * f)
                bJawAngle = math.rad(15 + 20 * f)

            elseif p < 0.78 then
                local f = (p - 0.50) / 0.28
                bSubY = -38 + 86 * math.sin(f * (math.pi / 2))
                bPitch = math.rad(55 - 25 * f)
                bJawAngle = math.rad(35 + 30 * math.sin(f * math.pi))
                bShake = math.sin(t * 45) * (0.35 * math.sin(f * math.pi))

            else
                local f = (p - 0.78) / 0.22
                local h = 1 - (f * f * (3 - 2 * f))
                bSubY = 48 * h
                bPitch = math.rad(30 * h - 15 * math.sin(f * math.pi))
                bJawAngle = math.rad(8 + 15 * h)
            end

            local shakeOff = (bShake > 0) and Vector3.new(bShake, bShake, bShake) or Vector3.new(0, 0, 0)
            animOff = Vector3.new(0, bSubY, 0) + shakeOff
            animRot = CFrame.Angles(bPitch, 0, 0)

            if key == "jaw_lower" then
                animRot = animRot * CFrame.Angles(bJawAngle, 0, 0)
            end
        end

        return rootCF * CFrame.new(basePos + animOff) * baseRot * animRot
    end

    
    
    
    local function setupPilotHUD()
        if pilotGui then pcall(function() pilotGui:Destroy() end) pilotGui = nil end

        local parent = (gethui and gethui()) or game:GetService("CoreGui") or lp:FindFirstChildOfClass("PlayerGui")
        pilotGui = Instance.new("ScreenGui")
        pilotGui.Name = "BTR_LeviathanHUD"
        pilotGui.ResetOnSpawn = false
        pilotGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

        local frame = Instance.new("Frame")
        frame.Name = "MainFrame"
        frame.AnchorPoint = Vector2.new(0.5, 1)
        frame.Position = UDim2.new(0.5, 0, 1, -15)
        frame.Size = UDim2.new(0, 530, 0, 86)
        frame.BackgroundColor3 = Color3.fromRGB(12, 16, 24)
        frame.BackgroundTransparency = 0.15
        frame.Visible = false
        frame.Parent = pilotGui
        hudFrame = frame

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 12)
        corner.Parent = frame

        local stroke = Instance.new("UIStroke")
        stroke.Color = Color3.fromRGB(0, 200, 255)
        stroke.Thickness = 1.5
        stroke.Transparency = 0.35
        stroke.Parent = frame

        local header = Instance.new("Frame")
        header.Name = "Header"
        header.Size = UDim2.new(1, 0, 0, 24)
        header.BackgroundTransparency = 1
        header.Parent = frame

        local titleLabel = Instance.new("TextLabel")
        titleLabel.Size = UDim2.new(0.48, 0, 1, 0)
        titleLabel.Position = UDim2.new(0, 12, 0, 0)
        titleLabel.BackgroundTransparency = 1
        titleLabel.Text = "LEVIATHAN PILOT CONTROLS"
        titleLabel.TextColor3 = Color3.fromRGB(0, 215, 255)
        titleLabel.Font = Enum.Font.GothamBold
        titleLabel.TextSize = 11
        titleLabel.TextXAlignment = Enum.TextXAlignment.Left
        titleLabel.Parent = header

        hudStatusLabel = Instance.new("TextLabel")
        hudStatusLabel.Size = UDim2.new(0.48, 0, 1, 0)
        hudStatusLabel.Position = UDim2.new(0.5, -12, 0, 0)
        hudStatusLabel.BackgroundTransparency = 1
        hudStatusLabel.Text = "STATUS: HOVERING"
        hudStatusLabel.TextColor3 = Color3.fromRGB(180, 220, 255)
        hudStatusLabel.Font = Enum.Font.GothamSemibold
        hudStatusLabel.TextSize = 10
        hudStatusLabel.TextXAlignment = Enum.TextXAlignment.Right
        hudStatusLabel.Parent = header

        local btnContainer = Instance.new("Frame")
        btnContainer.Name = "ButtonContainer"
        btnContainer.Position = UDim2.new(0, 8, 0, 26)
        btnContainer.Size = UDim2.new(1, -16, 0, 52)
        btnContainer.BackgroundTransparency = 1
        btnContainer.Parent = frame

        local listLayout = Instance.new("UIListLayout")
        listLayout.FillDirection = Enum.FillDirection.Horizontal
        listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
        listLayout.VerticalAlignment = Enum.VerticalAlignment.Center
        listLayout.Padding = UDim.new(0, 6)
        listLayout.Parent = btnContainer

        local function makeBtn(name, keyText, actText, bgColor, width, strokeColor, onClick, onDown, onUp)
            local btn = Instance.new("TextButton")
            btn.Name = name
            btn.Size = UDim2.new(0, width or 64, 0, 46)
            btn.BackgroundColor3 = bgColor
            btn.AutoButtonColor = true
            btn.Text = ""
            btn.Parent = btnContainer

            local bCorner = Instance.new("UICorner")
            bCorner.CornerRadius = UDim.new(0, 8)
            bCorner.Parent = btn

            local bStroke = Instance.new("UIStroke")
            bStroke.Color = strokeColor or Color3.fromRGB(255, 255, 255)
            bStroke.Transparency = strokeColor and 0.25 or 0.8
            bStroke.Thickness = strokeColor and 1.5 or 1
            bStroke.Parent = btn

            local kLbl = Instance.new("TextLabel")
            kLbl.Size = UDim2.new(1, 0, 0.45, 0)
            kLbl.BackgroundTransparency = 1
            kLbl.Text = keyText
            kLbl.TextColor3 = strokeColor or Color3.fromRGB(0, 220, 255)
            kLbl.Font = Enum.Font.GothamBold
            kLbl.TextSize = 11
            kLbl.Parent = btn

            local aLbl = Instance.new("TextLabel")
            aLbl.Size = UDim2.new(1, 0, 0.5, 0)
            aLbl.Position = UDim2.new(0, 0, 0.45, 0)
            aLbl.BackgroundTransparency = 1
            aLbl.Text = actText
            aLbl.TextColor3 = Color3.fromRGB(240, 240, 240)
            aLbl.Font = Enum.Font.GothamMedium
            aLbl.TextSize = 10
            aLbl.Parent = btn

            if onClick then
                btn.Activated:Connect(onClick)
            end
            if onDown then
                btn.MouseButton1Down:Connect(onDown)
                btn.InputBegan:Connect(function(inp)
                    if inp.UserInputType == Enum.UserInputType.Touch then onDown() end
                end)
            end
            if onUp then
                btn.MouseButton1Up:Connect(onUp)
                btn.InputEnded:Connect(function(inp)
                    if inp.UserInputType == Enum.UserInputType.Touch then onUp() end
                end)
            end

            return btn
        end

        makeBtn("AscendBtn", "[Space]", "Ascend", Color3.fromRGB(18, 45, 65), 62, nil, nil,
            function() isAscendingHeld = true end,
            function() isAscendingHeld = false end
        )

        makeBtn("DiveBtn", "[Ctrl]", "Dive", Color3.fromRGB(25, 30, 55), 62, nil, nil,
            function() isDescendingHeld = true end,
            function() isDescendingHeld = false end
        )

        makeBtn("SprintBtn", "[Shift]", "Sprint", Color3.fromRGB(12, 55, 60), 62, nil,
            function()
                isSprintToggled = not isSprintToggled
                lNotif(isSprintToggled and "Serpentine Slither Sprint: ON!" or "Cruising slither restored.", 2)
            end
        )

        makeBtn("BreachBtn", "[R]", "Breach", Color3.fromRGB(8, 70, 115), 78, Color3.fromRGB(0, 240, 255),
            function()
                triggerBreach()
            end
        )

        makeBtn("RoarBtn", "[E]", "Roar", Color3.fromRGB(55, 32, 15), 62, nil,
            function()
                if canDo("roar", 4) then
                    leviathanState = "roar"
                    leviathanStateTimer = 0
                    lNotif("ABYSSAL ROAR!", 2)
                end
            end
        )

        makeBtn("StrikeBtn", "[F]", "Strike", Color3.fromRGB(58, 20, 22), 62, nil,
            function()
                if canDo("strike", 3) then
                    leviathanState = "strike"
                    leviathanStateTimer = 0
                    lNotif("TITANIC STRIKE!", 2)
                end
            end
        )

        makeBtn("DismountBtn", "[Q]", "Exit", Color3.fromRGB(45, 20, 26), 56, nil,
            function()
                local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
                if hum then hum.Sit = false end
            end
        )

        pcall(function() pilotGui.Parent = parent end)

        local UIS = game:GetService("UserInputService")
        if inputConn then pcall(function() inputConn:Disconnect() end) inputConn = nil end
        inputConn = UIS.InputBegan:Connect(function(input, gpe)
            if gpe or not leviathanActive then return end
            local myHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            local isRiding = (leviathanDriverSeat and myHum and leviathanDriverSeat.Occupant == myHum) or (myHum and myHum.Sit)
            if not isRiding then return end

            if input.KeyCode == Enum.KeyCode.E then
                if canDo("roar", 4) then
                    leviathanState = "roar"
                    leviathanStateTimer = 0
                    lNotif("ABYSSAL ROAR!", 2)
                end
            elseif input.KeyCode == Enum.KeyCode.F then
                if canDo("strike", 3) then
                    leviathanState = "strike"
                    leviathanStateTimer = 0
                    lNotif("TITANIC STRIKE!", 2)
                end
            elseif input.KeyCode == Enum.KeyCode.R then
                triggerBreach()
            elseif input.KeyCode == Enum.KeyCode.LeftShift then
                isSprintToggled = not isSprintToggled
                lNotif(isSprintToggled and "Serpentine Slither Sprint: ON (68 studs/s)!" or "Cruising slither restored (30 studs/s).", 1.5)
            elseif input.KeyCode == Enum.KeyCode.Q then
                if myHum and myHum.Sit == true then myHum.Sit = false end
            end
        end)
    end

    
    local function startLeviathanWorkers(w)
        if w.scf then
            task.spawn(function()
                while leviathanActive and w.scf do
                    local now = tick()
                    if w.targetCF then
                        local cf = w.targetCF
                        local sendCF = false
                        if not w.lastSentCF then
                            sendCF = true
                        else
                            local posDiff = (cf.Position - w.lastSentCF.Position).Magnitude
                            local rotDiff = math.abs(cf.LookVector.X - w.lastSentCF.LookVector.X)
                                          + math.abs(cf.LookVector.Y - w.lastSentCF.LookVector.Y)
                                          + math.abs(cf.LookVector.Z - w.lastSentCF.LookVector.Z)
                            if posDiff > 0.06 or rotDiff > 0.04 or (now - w.lastSendTimeCF > 0.12) then
                                sendCF = true
                            end
                        end
                        if sendCF then
                            w.lastSentCF = cf
                            w.lastSendTimeCF = now
                            pcall(function() w.scf:InvokeServer(cf) end)
                        end
                    end
                    task.wait(0.03)
                end
            end)
        end

        if w.cpc then
            task.spawn(function()
                while leviathanActive and w.cpc do
                    local now = tick()
                    if w.targetColor then
                        local col = w.targetColor
                        local sendCol = false
                        if not w.lastSentColor then
                            sendCol = true
                        else
                            local diff = math.abs(col.R - w.lastSentColor.R) + math.abs(col.G - w.lastSentColor.G) + math.abs(col.B - w.lastSentColor.B)
                            if diff > 0.04 or (now - w.lastSendTimeCol > 0.35) then
                                sendCol = true
                            end
                        end
                        if sendCol then
                            w.lastSentColor = col
                            w.lastSendTimeCol = now
                            pcall(function() w.cpc:InvokeServer(col) end)
                        end
                    end
                    task.wait(0.05)
                end
            end)
        end
    end

    
    local function spawnLeviathan()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")

        lNotif("Summoning the 30-Stud Abyssal Leviathan...", 4)

        local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")
        local myPropName = "Prop" .. lp.Name

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then
            lNotif("Error: PropMaker not found!", 4)
            return
        end

        local humanoid = char:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid:EquipTool(propsTool) end
        task.wait(0.4)

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
        if not toolPropMake then return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        leviathanAnchor = hrp.Position + (hrp.CFrame.LookVector * 18) + Vector3.new(0, 2, 0)
        leviathanAngle = math.atan2(-hrp.CFrame.LookVector.X, -hrp.CFrame.LookVector.Z)
        leviathanWorkers = {}
        leviathanDriverSeat = nil
        leviathanAnimT = 0
        leviathanState = "idle"
        leviathanMode = "manual"
        leviathanSlitherWeight = 0

        local knownScf = {}
        for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                    if scf then knownScf[scf] = true end
                end
            end
        end

        local hitInst = workspace.Terrain
        local Telemetry = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction")

        for i, layout in ipairs(LEVIATHAN_LAYOUT) do
            if not leviathanActive then break end

            local targetCF = getLeviathanPartCFrame(layout, 0, "idle", 0, leviathanAnchor, leviathanAngle, 0)
            local defaultColor = getLeviathanPartColor(layout, i, 0, "idle", leviathanColorMode)

            pcall(function() Telemetry:FireServer("filterClick", {name=layout.cat,  itemType="Props"}) end)
            task.wait(0.04)
            pcall(function() Telemetry:FireServer("filterClick", {name=layout.prop, itemType="Props"}) end)
            task.wait(0.04)
            pcall(function() clearTools:FireServer("RequestingPropName", layout.prop, layout.cat) end)
            task.wait(0.18)

            pcall(function() toolPropMake:FireServer(hitInst, leviathanAnchor) end)
            task.wait(0.55)

            local foundProp, foundScf, foundCpc, foundSeat = nil, nil, nil, nil
            for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                for _, prop in pairs(folder:GetChildren()) do
                    if prop.Name == myPropName then
                        local scf = prop:FindFirstChild("SetCurrentCFrame")
                        local cpc = prop:FindFirstChild("ChangePropColor")
                        if scf and not knownScf[scf] then
                            foundProp = prop
                            foundScf = scf
                            foundCpc = cpc
                            knownScf[scf] = true
                            if layout.key == "throne" then
                                foundSeat = prop:FindFirstChildWhichIsA("Seat", true) or prop:FindFirstChildWhichIsA("VehicleSeat", true)
                            end
                            break
                        end
                    end
                end
                if foundScf then break end
            end

            if foundSeat then
                leviathanDriverSeat = foundSeat
            end

            local w = {
                key = layout.key,
                layout = layout,
                prop = foundProp,
                scf = foundScf,
                cpc = foundCpc,
                partIndex = i,
                targetCF = targetCF,
                lastSentCF = nil,
                lastSendTimeCF = 0,
                targetColor = defaultColor,
                lastSentColor = nil,
                lastSendTimeCol = 0
            }

            leviathanWorkers[layout.key] = w

            if foundScf then
                pcall(function() foundScf:InvokeServer(targetCF) end)
            end
            if foundCpc then
                pcall(function() foundCpc:InvokeServer(defaultColor) end)
            end

            startLeviathanWorkers(w)
        end

        task.wait(1)
        pcall(function()
            game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker")
        end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        pcall(function()
            local b = lp:FindFirstChildOfClass("Backpack")
            if b then
                for _, t in pairs(b:GetChildren()) do
                    if t:IsA("Tool") and t.Name == "PropMaker" then t:Destroy() end
                end
            end
            if char then
                for _, t in pairs(char:GetChildren()) do
                    if t:IsA("Tool") and t.Name == "PropMaker" then t:Destroy() end
                end
            end
        end)

        setupPilotHUD()
        lNotif("30-Stud Leviathan Titan Assembled and Online!", 5)
    end

    local function startLeviathanLoop()
        local RS = game:GetService("RunService")
        local UIS = game:GetService("UserInputService")
        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        leviathanLoop = RS.Heartbeat:Connect(function(dt)
            if not leviathanActive then return end

            leviathanAnimT = leviathanAnimT + dt
            leviathanStateTimer = leviathanStateTimer + dt

            local myHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            local isRiding = (leviathanDriverSeat and myHum and leviathanDriverSeat.Occupant == myHum) or (myHum and myHum.Sit)

            if leviathanState == "roar" and leviathanStateTimer > 3.5 then
                leviathanState = "idle"
                leviathanStateTimer = 0
            elseif leviathanState == "strike" and leviathanStateTimer > 2.2 then
                leviathanState = "idle"
                leviathanStateTimer = 0
            elseif leviathanState == "breach" then
                if leviathanStateTimer >= 3.0 and not leviathanBreachedNotif then
                    leviathanBreachedNotif = true
                    lNotif("TITANIC ABYSSAL BREACH! ERUPTING INTO THE HEAVENS!", 3)
                end
                if leviathanStateTimer > 6.0 then
                    leviathanState = "idle"
                    leviathanStateTimer = 0
                    leviathanBreachedNotif = false
                    lNotif("The Leviathan levels off gracefully.", 2)
                end
            end

            
            local targetSlither = (leviathanState == "sprint" or leviathanState == "walk") and 1.0 or 0.0
            leviathanSlitherWeight = leviathanSlitherWeight + (targetSlither - leviathanSlitherWeight) * math.clamp(dt * 3.5, 0, 1)

            local ignores = {workspace:FindFirstChild("WorkspaceCom")}
            for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
                if p.Character then table.insert(ignores, p.Character) end
            end
            rayParams.FilterDescendantsInstances = ignores
            local hit = workspace:Raycast(leviathanAnchor + Vector3.new(0, 10, 0), Vector3.new(0, -40, 0), rayParams)
            local groundY = hit and hit.Position.Y or leviathanAnchor.Y

            if isRiding then
                if hudFrame and not hudFrame.Visible then
                    hudFrame.Visible = true
                end

                local turnSpeed = 2.6
                local moveDir = Vector3.new(0, 0, 0)
                local vertDir = 0

                if UIS:IsKeyDown(Enum.KeyCode.Space) or isAscendingHeld then
                    vertDir = vertDir + 1
                end
                if UIS:IsKeyDown(Enum.KeyCode.LeftControl) or UIS:IsKeyDown(Enum.KeyCode.C) or isDescendingHeld then
                    vertDir = vertDir - 1
                end

                local isSprinting = UIS:IsKeyDown(Enum.KeyCode.LeftShift) or isSprintToggled
                local curSpeed = isSprinting and 68 or 30

                if UIS:IsKeyDown(Enum.KeyCode.A) then
                    leviathanAngle = leviathanAngle + turnSpeed * dt
                end
                if UIS:IsKeyDown(Enum.KeyCode.D) then
                    leviathanAngle = leviathanAngle - turnSpeed * dt
                end

                if myHum and myHum.MoveDirection.Magnitude > 0.05 then
                    local md = myHum.MoveDirection
                    local targetYaw = math.atan2(-md.X, -md.Z)
                    local diffAngle = (targetYaw - leviathanAngle + math.pi) % (2 * math.pi) - math.pi
                    leviathanAngle = leviathanAngle + math.clamp(diffAngle, -turnSpeed * 1.5, turnSpeed * 1.5) * dt
                    moveDir = moveDir + (CFrame.Angles(0, leviathanAngle, 0)).LookVector
                end

                local curRot = CFrame.Angles(0, leviathanAngle, 0)
                local fwd = curRot.LookVector
                if UIS:IsKeyDown(Enum.KeyCode.W) then
                    moveDir = moveDir + fwd
                end
                if UIS:IsKeyDown(Enum.KeyCode.S) then
                    moveDir = moveDir - fwd * 0.5
                end

                if leviathanState == "breach" then
                    local p = math.clamp(leviathanStateTimer / 6.0, 0, 1)
                    local breachFwdSpeed = 40
                    if p < 0.32 then
                        breachFwdSpeed = 38
                    elseif p < 0.50 then
                        breachFwdSpeed = 62
                    elseif p < 0.78 then
                        breachFwdSpeed = 46
                    else
                        breachFwdSpeed = 24
                    end
                    leviathanAnchor = leviathanAnchor + (curRot.LookVector * breachFwdSpeed * dt)
                end

                if moveDir.Magnitude > 0 then
                    leviathanAnchor = leviathanAnchor + moveDir.Unit * curSpeed * dt
                    if leviathanState ~= "roar" and leviathanState ~= "strike" and leviathanState ~= "kneel" and leviathanState ~= "breach" then
                        if isSprinting then
                            leviathanState = "sprint"
                        else
                            leviathanState = "walk"
                        end
                    end
                else
                    if isSprinting and (leviathanState == "walk" or leviathanState == "idle") then
                        leviathanState = "idle"
                    elseif leviathanState == "walk" or leviathanState == "sprint" then
                        leviathanState = "idle"
                    end
                end

                local vertSpeed = isSprinting and 32 or 22
                if vertDir ~= 0 then
                    leviathanAnchor = leviathanAnchor + Vector3.new(0, vertDir * vertSpeed * dt, 0)
                else
                    local diffY = groundY - leviathanAnchor.Y
                    if diffY > 0 then
                        leviathanAnchor = Vector3.new(leviathanAnchor.X, leviathanAnchor.Y + math.clamp(diffY, 0, 25 * dt), leviathanAnchor.Z)
                    end
                end

                if hudStatusLabel then
                    if leviathanState == "breach" then
                        local p = math.clamp(leviathanStateTimer / 6.0, 0, 1)
                        if p < 0.32 then
                            hudStatusLabel.Text = "STATUS: ABYSSAL PLUNGE (DIVING)"
                            hudStatusLabel.TextColor3 = Color3.fromRGB(130, 80, 255)
                        elseif p < 0.50 then
                            hudStatusLabel.Text = "STATUS: SUBTERRANEAN SURGE"
                            hudStatusLabel.TextColor3 = Color3.fromRGB(0, 200, 255)
                        elseif p < 0.78 then
                            hudStatusLabel.Text = "STATUS: CELESTIAL BREACH (ERUPTING!)"
                            hudStatusLabel.TextColor3 = Color3.fromRGB(255, 220, 50)
                        else
                            hudStatusLabel.Text = "STATUS: APEX CREST & SPLASHDOWN"
                            hudStatusLabel.TextColor3 = Color3.fromRGB(0, 255, 200)
                        end
                    elseif isSprinting and moveDir.Magnitude > 0 then
                        hudStatusLabel.Text = "STATUS: SERPENT SPRINT (68 STUDS/S)"
                        hudStatusLabel.TextColor3 = Color3.fromRGB(0, 255, 230)
                    elseif isSprinting and moveDir.Magnitude == 0 then
                        hudStatusLabel.Text = "STATUS: SPRINT READY (PRESS W)"
                        hudStatusLabel.TextColor3 = Color3.fromRGB(0, 230, 255)
                    elseif vertDir > 0 then
                        hudStatusLabel.Text = "STATUS: ASCENDING (FLYING)"
                        hudStatusLabel.TextColor3 = Color3.fromRGB(80, 200, 255)
                    elseif vertDir < 0 then
                        hudStatusLabel.Text = "STATUS: DIVING (SUBMERGING)"
                        hudStatusLabel.TextColor3 = Color3.fromRGB(150, 100, 255)
                    elseif moveDir.Magnitude > 0 then
                        hudStatusLabel.Text = "STATUS: CRUISING SLITHER (30 STUDS/S)"
                        hudStatusLabel.TextColor3 = Color3.fromRGB(100, 255, 120)
                    else
                        hudStatusLabel.Text = "STATUS: HOVERING"
                        hudStatusLabel.TextColor3 = Color3.fromRGB(200, 220, 255)
                    end
                end

            else
                if hudFrame and hudFrame.Visible then
                    hudFrame.Visible = false
                    isAscendingHeld = false
                    isDescendingHeld = false
                    isSprintToggled = false
                end

                if leviathanMode == "follow" then
                    local targetChar = nil
                    if leviathanFollowName == lp.Name then
                        targetChar = lp.Character
                    else
                        local targetPlr = game:GetService("Players"):FindFirstChild(leviathanFollowName)
                        targetChar = targetPlr and targetPlr.Character
                    end

                    local targetHrp = targetChar and targetChar:FindFirstChild("HumanoidRootPart")
                    if targetHrp then
                        local toTarget = (targetHrp.Position - leviathanAnchor)
                        local dist = Vector3.new(toTarget.X, 0, toTarget.Z).Magnitude
                        if dist > 14 then
                            local dir = Vector3.new(toTarget.X, 0, toTarget.Z).Unit
                            local isFast = (dist > 32)
                            local fSpeed = isFast and 68 or 30
                            leviathanAnchor = leviathanAnchor + (dir * fSpeed * dt)
                            leviathanAngle = math.atan2(-dir.X, -dir.Z)
                            leviathanState = isFast and "sprint" or "walk"
                        else
                            leviathanState = "idle"
                        end
                    else
                        leviathanState = "idle"
                    end

                    local diffY = math.clamp(groundY - leviathanAnchor.Y, -25 * dt, 15 * dt)
                    leviathanAnchor = Vector3.new(leviathanAnchor.X, leviathanAnchor.Y + diffY, leviathanAnchor.Z)

                elseif leviathanMode == "free" then
                    if not leviathanWaypoint or (Vector3.new(leviathanWaypoint.X - leviathanAnchor.X, 0, leviathanWaypoint.Z - leviathanAnchor.Z).Magnitude < 8) then
                        local randomOff = Vector3.new(math.random(-100, 100), 0, math.random(-100, 100))
                        leviathanWaypoint = leviathanAnchor + randomOff
                    end
                    local toWp = leviathanWaypoint - leviathanAnchor
                    local dir = Vector3.new(toWp.X, 0, toWp.Z).Unit
                    leviathanAnchor = leviathanAnchor + (dir * 30 * dt)
                    leviathanAngle = math.atan2(-dir.X, -dir.Z)
                    leviathanState = "walk"

                    local diffY = math.clamp(groundY - leviathanAnchor.Y, -25 * dt, 15 * dt)
                    leviathanAnchor = Vector3.new(leviathanAnchor.X, leviathanAnchor.Y + diffY, leviathanAnchor.Z)
                else
                    local diffY = math.clamp(groundY - leviathanAnchor.Y, -25 * dt, 15 * dt)
                    leviathanAnchor = Vector3.new(leviathanAnchor.X, leviathanAnchor.Y + diffY, leviathanAnchor.Z)
                end
            end

            
            local targetBank = 0
            if isRiding then
                if UIS:IsKeyDown(Enum.KeyCode.A) then
                    targetBank = targetBank + math.rad(14)
                end
                if UIS:IsKeyDown(Enum.KeyCode.D) then
                    targetBank = targetBank - math.rad(14)
                end
                if myHum and myHum.MoveDirection.Magnitude > 0.05 then
                    local md = myHum.MoveDirection
                    local targetYaw = math.atan2(-md.X, -md.Z)
                    local diffAngle = (targetYaw - leviathanAngle + math.pi) % (2 * math.pi) - math.pi
                    targetBank = targetBank + math.clamp(diffAngle * 0.8, -math.rad(16), math.rad(16))
                end
            end
            leviathanTurnBank = leviathanTurnBank + (targetBank - leviathanTurnBank) * math.clamp(dt * 5.0, 0, 1)

            
            for _, w in pairs(leviathanWorkers) do
                w.targetCF = getLeviathanPartCFrame(w.layout, leviathanAnimT, leviathanState, leviathanStateTimer, leviathanAnchor, leviathanAngle, leviathanSlitherWeight, leviathanTurnBank)
                w.targetColor = getLeviathanPartColor(w.layout, w.partIndex, leviathanAnimT, leviathanState, leviathanColorMode)
            end
        end)
    end

    
    
    
    BuildTab:AddSection({"> Abyssal Leviathan"})

    local leviathanToggle = BuildTab:AddToggle({
        Name = "Spawn Leviathan",
        Description = "Summon a giant 15-prop serpent titan",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not leviathanActive then return end
            leviathanActive = val
            if val then
                task.spawn(function()
                    spawnLeviathan()
                    if leviathanActive then startLeviathanLoop() end
                end)
            else
                if leviathanLoop then leviathanLoop:Disconnect() leviathanLoop = nil end
                pcall(function()
                    local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
                    if hum then hum.Sit = false end
                end)
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps")
                end)
                leviathanWorkers = {}
                leviathanDriverSeat = nil
                if pilotGui then pcall(function() pilotGui:Destroy() end) pilotGui = nil hudFrame = nil end
                if inputConn then pcall(function() inputConn:Disconnect() end) inputConn = nil end
                lNotif("Leviathan dismissed.", 2)
            end
        end
    })

    BuildTab:AddDropdown({
        Name = "Leviathan Color",
        Description = "Choose the dynamic real-time color scheme",
        Options = {"Abyssal Cyan", "Magma Titan", "Shadow Void", "Rainbow Overdrive", "Toxic Emerald"},
        Default = "Abyssal Cyan",
        Callback = function(choice)
            leviathanColorMode = choice
            lNotif("Color: " .. choice, 2)
        end
    })

    BuildTab:AddDropdown({
        Name = "Target",
        Description = "Select the leviathan's target",
        Options = (function()
            local names = {"---"}
            for _, p in pairs(game:GetService("Players").LocalPlayer and game:GetService("Players"):GetPlayers() or {}) do
                if p ~= game:GetService("Players").LocalPlayer then
                    table.insert(names, p.Name)
                end
            end
            return names
        end)(),
        Default = "---",
        Callback = function(choice)
            leviathanFollowName = choice
            leviathanTarget = choice
        end
    })

    BuildTab:AddButton({
        Name = "Stand (Idle)",
        Description = "Leviathan stands in towering stance",
        Callback = function()
            if not leviathanActive then lNotif("Spawn the Leviathan first!", 2) return end
            leviathanMode = "manual"
            leviathanState = "idle"
            leviathanStateTimer = 0
            lNotif("Leviathan is idle.", 2)
        end
    })

    BuildTab:AddButton({
        Name = "Hunt Target",
        Description = "Leviathan hunts the selected target",
        Callback = function()
            if not leviathanActive then lNotif("Spawn the Leviathan first!", 2) return end
            if leviathanFollowName == "---" or leviathanFollowName == "" then
                lNotif("Select a target first!", 2) return
            end
            leviathanMode = "follow"
            leviathanState = "walk"
            leviathanStateTimer = 0
            lNotif("Hunting target...", 2)
        end
    })

    BuildTab:AddButton({
        Name = "Follow Me",
        Description = "Leviathan follows you like an armored guardian",
        Callback = function()
            if not leviathanActive then lNotif("Spawn the Leviathan first!", 2) return end
            leviathanFollowName = lp.Name
            leviathanTarget = leviathanFollowName
            leviathanMode = "follow"
            leviathanState = "walk"
            leviathanStateTimer = 0
            lNotif("Leviathan is following you!", 2)
        end
    })

    BuildTab:AddButton({
        Name = "Patrol (Free)",
        Description = "Leviathan patrols the Brookhaven map freely",
        Callback = function()
            if not leviathanActive then lNotif("Spawn the Leviathan first!", 2) return end
            leviathanMode = "free"
            leviathanState = "walk"
            leviathanWaypoint = nil
            leviathanStateTimer = 0
            lNotif("The leviathan is on free patrol.", 2)
        end
    })

    BuildTab:AddButton({
        Name = "Strike",
        Description = "Strikes downward with a snapping bite",
        Callback = function()
            if not leviathanActive then lNotif("Spawn the Leviathan first!", 2) return end
            if not canDo("strike", 3) then lNotif("Cooldown...", 2) return end
            leviathanMode = "manual"
            leviathanState = "strike"
            leviathanStateTimer = 0
            lNotif("TITANIC STRIKE!", 2)
        end
    })

    BuildTab:AddButton({
        Name = "Roar",
        Description = "Unleashes a massive roar with fire breath",
        Callback = function()
            if not leviathanActive then lNotif("Spawn the Leviathan first!", 2) return end
            if not canDo("roar", 4) then lNotif("Cooldown...", 2) return end
            leviathanMode = "manual"
            leviathanState = "roar"
            leviathanStateTimer = 0
            lNotif("TITANIC ROAR!", 2)
        end
    })

    BuildTab:AddButton({
        Name = "Breach (Dive)",
        Description = "Dives underground and erupts into the sky",
        Callback = function()
            if not leviathanActive then lNotif("Spawn the Leviathan first!", 2) return end
            triggerBreach()
        end
    })

    BuildTab:AddButton({
        Name = "Mount Throne",
        Description = "Mounts or dismounts the pilot throne at the apex",
        Callback = function()
            if not leviathanActive then lNotif("Spawn the Leviathan first!", 2) return end
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if not hum or not hrp then return end

            if hum.Sit then
                hum.Sit = false
                lNotif("Dismounted from throne.", 2)
                return
            end

            if leviathanDriverSeat then
                hrp.CFrame = leviathanDriverSeat.CFrame + Vector3.new(0, 1.5, 0)
                task.wait(0.1)
                pcall(function() leviathanDriverSeat:Sit(hum) end)
                lNotif("Mounted throne! Pilot HUD active.", 2)
            else
                local throneW = leviathanWorkers["throne"]
                if throneW and throneW.targetCF then
                    hrp.CFrame = throneW.targetCF + Vector3.new(0, 2, 0)
                    lNotif("Mounted throne.", 2)
                end
            end
        end
    })

end 

do
    local lp = game:GetService("Players").LocalPlayer
    local creatureActive = false
    local creatureParts  = {}
    local creatureLoop   = nil
    local creatureSitConn = nil
    local cStream = PropStreamer.new(0.066)
    local CREATURE_HEIGHT = 9

    local creatureForceTakeoff = false

    local function cNotif(txt, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Mythic Creature",
                Text = txt,
                Duration = dur or 3
            })
        end)
    end

    local blueprint = {
        {key="selle",      prop="FurnitureSilverThrone", cat="Furniture", off=Vector3.new(0, 3.5, -0.5), rot=CFrame.Angles(0, 0, 0), color=Color3.fromRGB(160, 10, 10)},
        {key="passagers",  prop="FurnitureTablePicnic",  cat="Furniture", off=Vector3.new(0, 3.2, -2.8), rot=CFrame.Angles(0, math.rad(90), 0), color=Color3.fromRGB(90, 15, 15)},
        {key="corps",      prop="PlantDirtBig",           cat="Plant",     off=Vector3.new(0, 0, 0), rot=CFrame.Angles(0, 0, 0), color=Color3.fromRGB(35, 15, 55)},
        {key="cou",        prop="PlantDirtSmall",         cat="Plant",     off=Vector3.new(0, 1.5, 3.2), rot=CFrame.Angles(0, 0, 0), color=Color3.fromRGB(35, 15, 55)},
        {key="tete",       prop="PlantDirtSmall",         cat="Plant",     off=Vector3.new(0, 2.5, 5.5), rot=CFrame.Angles(0, 0, 0), color=Color3.fromRGB(40, 18, 62)},
        {key="oreille_G",  prop="QuarterFloor",           cat="Building",  off=Vector3.new(-5.5, 3.5, 4.5), rot=CFrame.Angles(0, 0, math.rad(-55)), color=Color3.fromRGB(120, 15, 30)},
        {key="oreille_D",  prop="QuarterFloor",           cat="Building",  off=Vector3.new(5.5, 3.5, 4.5), rot=CFrame.Angles(0, 0, math.rad(55)), color=Color3.fromRGB(120, 15, 30)},
        {key="defense_G",  prop="TubeLamp",               cat="Lights",    off=Vector3.new(-1.2, 1.2, 7.2), rot=CFrame.Angles(math.rad(90), 0, math.rad(-12)), color=Color3.fromRGB(255, 195, 30)},
        {key="defense_D",  prop="TubeLamp",               cat="Lights",    off=Vector3.new(1.2, 1.2, 7.2), rot=CFrame.Angles(math.rad(90), 0, math.rad(12)), color=Color3.fromRGB(255, 195, 30)},
        {key="trompe_1",   prop="TubeLamp",               cat="Lights",    off=Vector3.new(0, 0.5, 7.5), rot=CFrame.Angles(math.rad(130), 0, 0), color=Color3.fromRGB(50, 10, 80)},
        {key="trompe_2",   prop="PlantDirtSmall",         cat="Plant",     off=Vector3.new(0, -1.5, 9), rot=CFrame.Angles(0, 0, 0), color=Color3.fromRGB(50, 10, 80)},
        {key="patte_avG",  prop="PillarSmall",            cat="Building",  off=Vector3.new(-3.5, -2, 2.5), rot=CFrame.Angles(math.pi, 0, 0), color=Color3.fromRGB(18, 8, 30)},
        {key="patte_avD",  prop="PillarSmall",            cat="Building",  off=Vector3.new(3.5, -2, 2.5), rot=CFrame.Angles(math.pi, 0, 0), color=Color3.fromRGB(18, 8, 30)},
        {key="patte_milG", prop="PillarSmall",            cat="Building",  off=Vector3.new(-3.5, -2, 0), rot=CFrame.Angles(math.pi, 0, 0), color=Color3.fromRGB(18, 8, 30)},
        {key="patte_milD", prop="PillarSmall",            cat="Building",  off=Vector3.new(3.5, -2, 0), rot=CFrame.Angles(math.pi, 0, 0), color=Color3.fromRGB(18, 8, 30)},
        {key="patte_arG",  prop="PillarSmall",            cat="Building",  off=Vector3.new(-3.5, -2, -2.5), rot=CFrame.Angles(math.pi, 0, 0), color=Color3.fromRGB(18, 8, 30)},
        {key="patte_arD",  prop="PillarSmall",            cat="Building",  off=Vector3.new(3.5, -2, -2.5), rot=CFrame.Angles(math.pi, 0, 0), color=Color3.fromRGB(18, 8, 30)},
    }

    local function getPartAnimDelta(bpKey, animTime, currentVelocity, rideState)
        local walkSpeed = math.min(currentVelocity / 50, 2)
        local walkPhase = animTime * (6 + walkSpeed * 4)
        local idlePhase = animTime * 2.5

        if bpKey:sub(1,5) == "patte" then
            local isFront = bpKey:find("av") ~= nil
            local isMid   = bpKey:find("mil") ~= nil
            local isLeft  = bpKey:find("G") ~= nil

            if rideState == "manual_flight" then
                local foldAngle = math.rad(-25) + math.sin(animTime * 2) * math.rad(3)
                return CFrame.Angles(foldAngle, 0, 0)
            else
                local phaseShift = 0
                if isFront then phaseShift = isLeft and 0 or math.pi
                elseif isMid then phaseShift = isLeft and math.pi or 0
                else phaseShift = isLeft and 0 or math.pi end
                local swing = math.sin(walkPhase + phaseShift) * (0.15 + walkSpeed * 0.25)
                local lift  = math.max(0, math.cos(walkPhase + phaseShift)) * (0.2 + walkSpeed * 0.3)
                return CFrame.new(0, lift, 0) * CFrame.Angles(swing, 0, 0)
            end
        elseif bpKey:sub(1,7) == "oreille" then
            local isLeft = bpKey:find("G") ~= nil
            local flapFreq = (rideState == "manual_flight") and 5 or 2.5
            local flapAmp  = (rideState == "manual_flight") and math.rad(22) or math.rad(8)
            local flap = math.sin(animTime * flapFreq) * flapAmp
            local dir = isLeft and -1 or 1
            return CFrame.Angles(0, 0, flap * dir)
        elseif bpKey:sub(1,6) == "trompe" then
            local isTip = bpKey:find("2") ~= nil
            local wave  = math.sin(idlePhase + (isTip and 1.5 or 0)) * math.rad(10)
            return CFrame.Angles(wave, 0, 0)
        elseif bpKey == "tete" or bpKey == "cou" then
            local breathe = math.sin(idlePhase) * 0.08
            return CFrame.new(0, breathe, 0) * CFrame.Angles(breathe * 0.5, 0, 0)
        end
        return CFrame.new(0, math.sin(idlePhase) * 0.05, 0)
    end

    local function isDriverSitting()
        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if not hum then return false end
        for _, p in ipairs(creatureParts) do
            if p.key == "selle" and p.propModel then
                for _, child in pairs(p.propModel:GetDescendants()) do
                    if child:IsA("Seat") and child.Occupant ~= nil then
                        if child.Occupant == hum or (child.Occupant.Parent and child.Occupant.Parent.Name == lp.Name) then
                            return true
                        end
                    end
                end
            end
        end
        return false
    end

    local function isAnyoneSitting()
        for _, p in ipairs(creatureParts) do
            if (p.key == "selle" or p.key == "passagers") and p.propModel then
                for _, child in pairs(p.propModel:GetDescendants()) do
                    if child:IsA("Seat") and child.Occupant ~= nil then
                        return true
                    end
                end
            end
        end
        return false
    end

    
    local riderGui = nil
    local isUIUp    = false
    local isUIDown  = false
    local isUIBoost = false

    local function removeCreatureGUI()
        if riderGui then
            riderGui:Destroy()
            riderGui = nil
        end
        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, true) end
        isUIUp = false; isUIDown = false; isUIBoost = false
    end

    local function setupCreatureGUI()
        if riderGui then return end
        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, false) end

        local sg = Instance.new("ScreenGui")
        sg.Name = "MythicCreatureRiderGui"
        sg.ResetOnSpawn = false
        sg.Parent = (gethui and gethui()) or lp:FindFirstChild("PlayerGui") or game:GetService("CoreGui")
        riderGui = sg

        local panel = Instance.new("Frame")
        panel.Size = UDim2.new(0, 210, 0, 250)
        panel.Position = UDim2.new(1, -230, 0.5, -125)
        panel.BackgroundColor3 = Color3.fromRGB(15, 12, 22)
        panel.BackgroundTransparency = 0.1
        panel.BorderSizePixel = 0
        panel.Parent = sg
        Instance.new("UICorner", panel).CornerRadius = UDim.new(0, 12)

        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 34)
        title.BackgroundColor3 = Color3.fromRGB(35, 15, 55)
        title.BorderSizePixel = 0
        title.Text = "Mythic Creature"
        title.TextColor3 = Color3.fromRGB(255, 195, 30)
        title.Font = Enum.Font.GothamBold
        title.TextSize = 14
        title.Parent = panel
        Instance.new("UICorner", title).CornerRadius = UDim.new(0, 12)

        local function makeRiderBtn(txt, posY, col, callback)
            local btn = Instance.new("TextButton")
            btn.Size = UDim2.new(1, -20, 0, 34)
            btn.Position = UDim2.new(0, 10, 0, posY)
            btn.BackgroundColor3 = col
            btn.TextColor3 = Color3.new(1,1,1)
            btn.Text = txt
            btn.TextScaled = true
            btn.Font = Enum.Font.GothamBold
            btn.BorderSizePixel = 0
            btn.Parent = panel
            Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 6)
            btn.InputBegan:Connect(function(i)
                if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                    callback(true)
                end
            end)
            btn.InputEnded:Connect(function(i)
                if i.UserInputType == Enum.UserInputType.MouseButton1 or i.UserInputType == Enum.UserInputType.Touch then
                    callback(false)
                end
            end)
            return btn
        end

        makeRiderBtn("Ascend (Space)",   40,  Color3.fromRGB(40, 80, 160),  function(v) isUIUp    = v end)
        makeRiderBtn("Descend (Ctrl)",  80,  Color3.fromRGB(160, 60, 40),  function(v) isUIDown  = v end)
        makeRiderBtn("Sprint (Shift)",  120, Color3.fromRGB(100, 40, 160), function(v) isUIBoost = v end)

        local infoBtn = Instance.new("TextButton")
        infoBtn.Size = UDim2.new(1, -20, 0, 30)
        infoBtn.Position = UDim2.new(0, 10, 0, 160)
        infoBtn.BackgroundColor3 = Color3.fromRGB(30, 90, 80)
        infoBtn.TextColor3 = Color3.new(1,1,1)
        infoBtn.Text = "Controls Guide "
        infoBtn.Font = Enum.Font.GothamBold
        infoBtn.BorderSizePixel = 0
        infoBtn.Parent = panel
        Instance.new("UICorner", infoBtn).CornerRadius = UDim.new(0, 6)

        local infoOverlay = nil
        infoBtn.MouseButton1Click:Connect(function()
            if infoOverlay then
                infoOverlay:Destroy()
                infoOverlay = nil
            else
                local card = Instance.new("Frame")
                card.Size = UDim2.new(0, 210, 0, 150)
                card.Position = UDim2.new(1, -450, 0.5, -75)
                card.BackgroundColor3 = Color3.fromRGB(12, 10, 18)
                card.BackgroundTransparency = 0.05
                card.BorderSizePixel = 0
                card.Parent = sg
                Instance.new("UICorner", card).CornerRadius = UDim.new(0, 10)
                infoOverlay = card

                local lbl = Instance.new("TextLabel")
                lbl.Size = UDim2.new(1, -16, 1, -16)
                lbl.Position = UDim2.new(0, 8, 0, 8)
                lbl.BackgroundTransparency = 1
                lbl.TextColor3 = Color3.fromRGB(220, 220, 240)
                lbl.Font = Enum.Font.GothamMedium
                lbl.TextSize = 12
                lbl.TextXAlignment = Enum.TextXAlignment.Left
                lbl.TextYAlignment = Enum.TextYAlignment.Top
                lbl.TextWrapped = true
                lbl.Text = " MYTHIC CONTROLS:\n\n- WASD: Move / Steering\n- Space: Ascend into Air\n- Left Ctrl: Descend\n- Left Shift: Turbo Speed (300)\n- Touch Ground: Auto Land"
                lbl.Parent = card
            end
        end)

        local quitBtn = Instance.new("TextButton")
        quitBtn.Size = UDim2.new(1, -20, 0, 28)
        quitBtn.Position = UDim2.new(0, 10, 0, 196)
        quitBtn.BackgroundColor3 = Color3.fromRGB(100, 35, 35)
        quitBtn.TextColor3 = Color3.new(1,1,1)
        quitBtn.Text = "Dismount"
        quitBtn.Font = Enum.Font.GothamBold
        quitBtn.BorderSizePixel = 0
        quitBtn.Parent = panel
        Instance.new("UICorner", quitBtn).CornerRadius = UDim.new(0, 6)
        quitBtn.MouseButton1Click:Connect(function()
            
            local h = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if h then h.Sit = false end
        end)
    end

    local function spawnCreature()
        local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")
        local myPropName = "Prop" .. lp.Name

        cNotif("Summoning Mythic Creature...", 5)

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then cNotif("PropMaker not found!", 4) return end

        local humanoid = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid:EquipTool(propsTool) end
        task.wait(0.4)

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
        if not toolPropMake then return end

        task.wait(0.15)

        local hrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
        local spawnAnchor
        if hrp then
            local fwd = hrp.CFrame.LookVector
            spawnAnchor = hrp.Position + Vector3.new(fwd.X * 8, 0, fwd.Z * 8)
        else
            spawnAnchor = Vector3.new(0, 0, 0)
        end

        creatureParts = {}
        local knownScf = {}

        for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                    if scf then knownScf[scf] = true end
                end
            end
        end

        for idx, bp in ipairs(blueprint) do
            if not creatureActive then break end

            pcall(function() if bp.prop == "FloorCushion" then clearTools:FireServer("RequestingPropFeatureName", bp.prop, bp.cat) else clearTools:FireServer("RequestingPropName", bp.prop, bp.cat) end end)
            task.wait(0.2)

            pcall(function() toolPropMake:FireServer(workspace.Terrain, spawnAnchor) end)
            task.wait(0.85)

            local foundScf = nil
            local foundCpc = nil
            local foundProp = nil
            for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                for _, prop in pairs(folder:GetChildren()) do
                    if prop.Name == myPropName then
                        local scf = prop:FindFirstChild("SetCurrentCFrame")
                        local cpc = prop:FindFirstChild("ChangePropColor")
                        if scf and not knownScf[scf] then
                            foundScf = scf
                            foundCpc = cpc
                            foundProp = prop
                            knownScf[scf] = true
                        end
                    end
                end
            end

            if foundScf then
                if foundCpc and bp.color then
                    pcall(function() foundCpc:InvokeServer(bp.color) end)
                end
                table.insert(creatureParts, {key=bp.key, scf=foundScf, cpc=foundCpc, blueprint=bp, propModel=foundProp})
            end
        end

        task.wait(1)
        pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        for _, _p in ipairs(creatureParts) do cStream:add(_p.scf, _p.cpc) end
        cStream:start()
        cleanupPropMakerTool()

        cNotif("Mythic Creature ready! ("..#creatureParts.."/17 parts)", 5)
    end

    local function startCreatureLoop()
        local RS  = game:GetService("RunService")
        local uis = game:GetService("UserInputService")
        local ws  = game:GetService("Workspace")
        local t   = 0
        local animTime = 0
        local lastUpdate = 0

        if creatureSitConn then creatureSitConn:Disconnect() end
        local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
        if hum then
            creatureSitConn = hum:GetPropertyChangedSignal("Sit"):Connect(function()
                if not hum.Sit and creatureActive then
                    local root = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
                    if root then root.CFrame = root.CFrame + Vector3.new(0, 15, 0) end
                end
            end)
        end

        local rideState = "resting"

        local myRoot = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
        local spawnOrigin = myRoot and myRoot.CFrame or CFrame.new(0,5,0)
        spawnOrigin = CFrame.new(spawnOrigin.Position) * CFrame.Angles(0, math.rad(90), 0)

        local currentVelocity = 0
        local creatureWorldPos = spawnOrigin.Position
        local currentYaw = 0
        local currentPitch = 0
        local currentBank = 0

        creatureLoop = RS.RenderStepped:Connect(function(dt)
            if not creatureActive then return end
            local _now = tick()
            if _now - lastUpdate < (1/24) then return end
            lastUpdate = _now

            t = t + dt
            local creatureCF
            creatureCF = spawnOrigin * CFrame.new(0, 1, 0)

            if rideState == "resting" then
                if isDriverSitting() then
                    setupCreatureGUI()
                    pcall(function() lp.Character:FindFirstChildOfClass("Humanoid"):SetStateEnabled(Enum.HumanoidStateType.Jumping, false) end)
                    rideState = "manual_ground"
                    local _, yaw, _ = spawnOrigin:ToEulerAnglesYXZ()
                    currentYaw = yaw
                    currentPitch = 0; currentBank = 0; currentVelocity = 0
                    creatureWorldPos = creatureCF.Position
                    cNotif("Mythic Creature tamed! WASD = Move, Shift = Sprint, Space = Fly ", 6)
                elseif isAnyoneSitting() then
                    rideState = "waiting"
                end

            elseif rideState == "waiting" then
                if isDriverSitting() then
                    setupCreatureGUI()
                    pcall(function() lp.Character:FindFirstChildOfClass("Humanoid"):SetStateEnabled(Enum.HumanoidStateType.Jumping, false) end)
                    rideState = "manual_ground"
                    local _, yaw, _ = spawnOrigin:ToEulerAnglesYXZ()
                    currentYaw = yaw
                    currentPitch = 0; currentBank = 0; currentVelocity = 0
                    creatureWorldPos = creatureCF.Position
                    cNotif("Mythic Creature tamed! WASD = Move, Shift = Sprint, Space = Fly ", 6)
                elseif not isAnyoneSitting() then
                    rideState = "resting"
                end

            elseif rideState == "manual_ground" then
                local moveDir = Vector3.new(0,0,0)
                if uis:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + Vector3.new(0,0,-1) end
                if uis:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir + Vector3.new(0,0,1) end
                if uis:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir + Vector3.new(-1,0,0) end
                if uis:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + Vector3.new(1,0,0) end

                local isRunning = uis:IsKeyDown(Enum.KeyCode.LeftShift) or isUIBoost
                local targetSpeed = 0

                if moveDir.Magnitude > 0.1 then
                    targetSpeed = isRunning and 140 or 50
                    local cam = ws.CurrentCamera
                    local camYaw = math.atan2(-cam.CFrame.LookVector.X, -cam.CFrame.LookVector.Z)
                    local localYaw = math.atan2(-moveDir.X, -moveDir.Z)
                    local targetYaw = camYaw + localYaw
                    local angleDiff = targetYaw - currentYaw
                    while angleDiff >  math.pi do angleDiff = angleDiff - 2*math.pi end
                    while angleDiff < -math.pi do angleDiff = angleDiff + 2*math.pi end
                    currentYaw = currentYaw + angleDiff * 10 * dt
                end

                currentVelocity = currentVelocity + (targetSpeed - currentVelocity) * 8 * dt
                local forwardDir = (CFrame.new(Vector3.new(), Vector3.new(0,0,-1)) * CFrame.Angles(0, currentYaw, 0)).LookVector
                creatureWorldPos = creatureWorldPos + forwardDir * currentVelocity * dt

                local rayParams = RaycastParams.new()
                local _ignoreList = {lp.Character}
                for _, pd in ipairs(creatureParts) do if pd.propModel then table.insert(_ignoreList, pd.propModel) end end
                rayParams.FilterDescendantsInstances = _ignoreList
                rayParams.FilterType = Enum.RaycastFilterType.Exclude

                local result = ws:Raycast(creatureWorldPos + Vector3.new(0, 100, 0), Vector3.new(0, -200, 0), rayParams)
                local groundY = result and result.Position.Y or (creatureWorldPos.Y - CREATURE_HEIGHT)
                local targetY = groundY + CREATURE_HEIGHT
                creatureWorldPos = Vector3.new(creatureWorldPos.X,
                    creatureWorldPos.Y + math.clamp(targetY - creatureWorldPos.Y, -60*dt, 60*dt),
                    creatureWorldPos.Z)

                local pitchTarget = 0
                if result and currentVelocity > 5 then
                    local fResult = ws:Raycast(creatureWorldPos + Vector3.new(0,100,0) + forwardDir*8, Vector3.new(0,-200,0), rayParams)
                    if fResult then pitchTarget = math.atan2(fResult.Position.Y - groundY, 8) end
                end
                currentPitch = currentPitch + (pitchTarget - currentPitch) * 6 * dt
                currentBank  = currentBank  + (0 - currentBank) * 6 * dt

                creatureCF = CFrame.new(creatureWorldPos) * CFrame.Angles(0, currentYaw, 0) * CFrame.Angles(currentPitch, 0, currentBank) * CFrame.Angles(0, math.pi, 0)
                animTime = animTime + dt * (1 + currentVelocity / 30)

                if uis:IsKeyDown(Enum.KeyCode.Space) or isUIUp or creatureForceTakeoff then
                    creatureForceTakeoff = false
                    rideState = "manual_flight"
                    cNotif("In Flight! WASD = Fly | Space = Ascend | Ctrl = Descend | Shift = Turbo", 5)
                end

                if not isDriverSitting() and not isAnyoneSitting() then
                    removeCreatureGUI()
                    pcall(function() lp.Character:FindFirstChildOfClass("Humanoid"):SetStateEnabled(Enum.HumanoidStateType.Jumping, true) end)
                    
                    spawnOrigin = CFrame.new(creatureWorldPos) * CFrame.Angles(0, currentYaw, 0) * CFrame.Angles(0, math.pi, 0)
                    rideState = "resting"
                end

            elseif rideState == "manual_flight" then
                local moveDir = Vector3.new(0,0,0)
                if uis:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + Vector3.new(0,0,-1) end
                if uis:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir + Vector3.new(0,0,1) end
                if uis:IsKeyDown(Enum.KeyCode.A) then moveDir = moveDir + Vector3.new(-1,0,0) end
                if uis:IsKeyDown(Enum.KeyCode.D) then moveDir = moveDir + Vector3.new(1,0,0) end

                local isRunning = uis:IsKeyDown(Enum.KeyCode.LeftShift) or isUIBoost
                local targetSpeed = isRunning and 300 or 120

                local bankTarget = 0
                if moveDir.Magnitude > 0.1 then
                    local cam = ws.CurrentCamera
                    local camYaw = math.atan2(-cam.CFrame.LookVector.X, -cam.CFrame.LookVector.Z)
                    local localYaw = math.atan2(-moveDir.X, -moveDir.Z)
                    local targetYaw = camYaw + localYaw
                    local angleDiff = targetYaw - currentYaw
                    while angleDiff >  math.pi do angleDiff = angleDiff - 2*math.pi end
                    while angleDiff < -math.pi do angleDiff = angleDiff + 2*math.pi end
                    currentYaw = currentYaw + angleDiff * 8 * dt
                    currentVelocity = currentVelocity + (targetSpeed - currentVelocity) * 5 * dt
                    bankTarget = -angleDiff * 0.35
                else
                    currentVelocity = currentVelocity + (0 - currentVelocity) * 2 * dt
                end

                local vSpeed = 0
                if uis:IsKeyDown(Enum.KeyCode.Space) or isUIUp then vSpeed = 100 end
                if uis:IsKeyDown(Enum.KeyCode.LeftControl) or isUIDown then vSpeed = -100 end

                local forwardDir = (CFrame.new(Vector3.new(), Vector3.new(0,0,-1)) * CFrame.Angles(0, currentYaw, 0)).LookVector
                creatureWorldPos = creatureWorldPos + forwardDir * currentVelocity * dt
                creatureWorldPos = creatureWorldPos + Vector3.new(0, vSpeed * dt, 0)

                local pitchTarget = (vSpeed / 100) * math.rad(20)
                currentPitch = currentPitch + (pitchTarget - currentPitch) * 5 * dt
                currentBank  = currentBank  + (bankTarget  - currentBank)  * 5 * dt

                local minX, maxX = -1425, 1454
                local minZ, maxZ = -1476, 1403
                if creatureWorldPos.X < minX or creatureWorldPos.X > maxX or creatureWorldPos.Z < minZ or creatureWorldPos.Z > maxZ then
                    local toCenter = Vector3.new(0,0,0) - creatureWorldPos
                    creatureWorldPos = creatureWorldPos + toCenter.Unit * currentVelocity * dt
                end

                creatureCF = CFrame.new(creatureWorldPos) * CFrame.Angles(0, currentYaw, 0) * CFrame.Angles(currentPitch, 0, currentBank) * CFrame.Angles(0, math.pi, 0)
                animTime = animTime + dt * (1 + currentVelocity / 50)

                local rayParams2 = RaycastParams.new()
                local _il2 = {lp.Character}
                for _, pd in ipairs(creatureParts) do if pd.propModel then table.insert(_il2, pd.propModel) end end
                rayParams2.FilterDescendantsInstances = _il2
                rayParams2.FilterType = Enum.RaycastFilterType.Exclude
                local result2 = ws:Raycast(creatureWorldPos, Vector3.new(0, -CREATURE_HEIGHT - 10, 0), rayParams2)
                if result2 and vSpeed < 0 then
                    rideState = "manual_ground"
                    spawnOrigin = CFrame.new(creatureWorldPos) * CFrame.Angles(0, currentYaw, 0) * CFrame.Angles(0, math.pi, 0)
                    cNotif("Landing successful!", 3)
                end

                if not isDriverSitting() and not isAnyoneSitting() then
                    removeCreatureGUI()
                    pcall(function() lp.Character:FindFirstChildOfClass("Humanoid"):SetStateEnabled(Enum.HumanoidStateType.Jumping, true) end)
                    
                    local rpLand = RaycastParams.new()
                    local _ilLand = {lp.Character}
                    for _, pd in ipairs(creatureParts) do if pd.propModel then table.insert(_ilLand, pd.propModel) end end
                    rpLand.FilterDescendantsInstances = _ilLand
                    rpLand.FilterType = Enum.RaycastFilterType.Exclude
                    local rLand = ws:Raycast(creatureWorldPos + Vector3.new(0,200,0), Vector3.new(0,-400,0), rpLand)
                    if rLand then
                        creatureWorldPos = Vector3.new(creatureWorldPos.X, rLand.Position.Y + CREATURE_HEIGHT, creatureWorldPos.Z)
                    end
                    spawnOrigin = CFrame.new(creatureWorldPos) * CFrame.Angles(0, currentYaw, 0) * CFrame.Angles(0, math.pi, 0)
                    rideState = "resting"
                end
            end

            animTime = animTime + dt

            for _, pData in ipairs(creatureParts) do
                local bp = pData.blueprint
                if bp and pData.scf then
                    local animDelta = getPartAnimDelta(bp.key, animTime, currentVelocity, rideState)
                    local finalRot  = bp.rot or CFrame.Angles(0,0,0)
                    local partCF    = creatureCF * CFrame.new(bp.off) * finalRot * animDelta
                    cStream:setCF(pData.scf, partCF)
                end
            end
        end)
    end

    
    BuildTab:AddSection({"Attraction: Mythic Creature"})

    BuildTab:AddToggle({
        Name = "Summon Mythic Creature",
        Description = "Summons a controllable mythic flying creature mount",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not creatureActive then return end
            creatureActive = val
            if val then
                task.spawn(function()
                    spawnCreature()
                    if creatureActive then startCreatureLoop() end
                end)
            else
                if creatureLoop then creatureLoop:Disconnect() creatureLoop = nil end
                cStream:stop()
                if creatureSitConn then creatureSitConn:Disconnect() creatureSitConn = nil end
                removeCreatureGUI()
                pcall(function() lp.Character:FindFirstChildOfClass("Humanoid"):SetStateEnabled(Enum.HumanoidStateType.Jumping, true) end)
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps")
                end)
                creatureParts = {}
                cNotif("The Mythic Creature returns to the void...", 3)
            end
        end
    })

end 

do
BuildTab:AddSection({"Build AK-47"})

local BuildSystem = {
    Active = false,
    CurrentShape = "AK-47",
    CurrentMaterial = "Mix (For AK-47)",
    Scale = 3
}

local function getPoints(shapeName)
    local points = {}
    if shapeName == "AK-47" then
        
        
        points = {
            
            {x=0,    y=0,   prop="CaseBlackBriefcase", cat="Case", rz=0},
            {x=1.2,  y=0,   prop="CaseBlackBriefcase", cat="Case", rz=0},
            {x=2.4,  y=0,   prop="CaseBlackBriefcase", cat="Case", rz=0},
            
            
            {x=1.2,  y=-1.2, prop="CaseBlackBriefcase", cat="Case", rz=15},
            {x=1.5,  y=-2.2, prop="CaseBlackBriefcase", cat="Case", rz=30},
            {x=2.0,  y=-3.0, prop="CaseBlackBriefcase", cat="Case", rz=45},
            
            
            {x=-0.2, y=-1.3, prop="CaseBlackBriefcase", cat="Case", rz=75},
            
            
            {x=-1.0, y=-0.2, prop="BoxOne", cat="Box", rz=0},
            {x=-2.2, y=-0.5, prop="BoxOne", cat="Box", rz=0},
            {x=-3.4, y=-0.8, prop="BoxOne", cat="Box", rz=0},
            {x=-3.2, y=0.5,  prop="BoxOne", cat="Box", rz=0}, 
            
            
            {x=3.6,  y=0,    prop="BoxOne", cat="Box", rz=0},
            
            
            {x=4.5,  y=0,    prop="TubeLamp", cat="Lights", rz=-90}, 
            {x=6.5,  y=0,    prop="TubeLamp", cat="Lights", rz=-90}, 
            {x=4.5,  y=0.6,  prop="TubeLamp", cat="Lights", rz=-90}, 
        }
    elseif shapeName == "AK47 HD" then
        points = {
            {x=0.0,  y=0.0,  z=0.0,  prop="CaseBlackBriefcase", cat="Case", rx=0, ry=0, rz=0},
            {x=1.3,  y=0.0,  z=0.0,  prop="CaseBlackBriefcase", cat="Case", rx=0, ry=0, rz=0},
            {x=2.6,  y=0.05, z=0.0,  prop="CaseBlackBriefcase", cat="Case", rx=0, ry=0, rz=0},
            {x=1.1,  y=-1.3, z=0.0,  prop="CaseBlackBriefcase", cat="Case", rx=0, ry=0, rz=18},
            {x=1.6,  y=-2.4, z=0.0,  prop="CaseBlackBriefcase", cat="Case", rx=0, ry=0, rz=36},
            {x=2.2,  y=-3.3, z=0.0,  prop="CaseBlackBriefcase", cat="Case", rx=0, ry=0, rz=52},
            {x=-0.4, y=-1.3, z=0.0,  prop="CaseBlackBriefcase", cat="Case", rx=0, ry=0, rz=72},
            {x=-1.2, y=-0.2, z=0.0,  prop="BoxOne", cat="Box", rx=0, ry=0, rz=0},
            {x=-2.4, y=-0.5, z=0.0,  prop="BoxOne", cat="Box", rx=0, ry=0, rz=0},
            {x=-3.6, y=-0.8, z=0.0,  prop="BoxOne", cat="Box", rx=0, ry=0, rz=0},
            {x=-3.2, y=0.45, z=0.0,  prop="BoxOne", cat="Box", rx=0, ry=0, rz=0},
            {x=3.8,  y=0.05, z=0.0,  prop="BoxOne", cat="Box", rx=0, ry=0, rz=0},
            {x=4.8,  y=0.0,  z=0.0,  prop="TubeLamp", cat="Lights", rx=0, ry=0, rz=-90},
            {x=6.8,  y=0.0,  z=0.0,  prop="TubeLamp", cat="Lights", rx=0, ry=0, rz=-90},
            {x=8.2,  y=0.0,  z=0.0,  prop="TubeLamp", cat="Lights", rx=0, ry=0, rz=-90},
        }
    elseif shapeName == "Zizi" then
        
        points = {
            
            {x=-1.2, y=0.0, prop="BoxOne", cat="Box", rz=0},
            {x=-1.8, y=0.8, prop="BoxOne", cat="Box", rz=0},
            {x=1.2,  y=0.0, prop="BoxOne", cat="Box", rz=0},
            {x=1.8,  y=0.8, prop="BoxOne", cat="Box", rz=0},
            
            
            {x=0, y=0.0, prop="BoxOne", cat="Box", rz=0},
            {x=0, y=1.5, prop="BoxOne", cat="Box", rz=0},
            {x=0, y=3.0, prop="BoxOne", cat="Box", rz=0},
            {x=0, y=4.5, prop="BoxOne", cat="Box", rz=0},
            {x=0, y=6.0, prop="BoxOne", cat="Box", rz=0},
            
            
            {x=0,    y=7.6, prop="BoxOne", cat="Box", rz=0},
            {x=-0.5, y=6.8, prop="BoxOne", cat="Box", rz=0},
            {x=0.5,  y=6.8, prop="BoxOne", cat="Box", rz=0},
        }
    end
    return points
end

BuildTab:AddToggle({
    Name = "Build (Toggle to clear)",
    Description = "Starts the build. Toggle OFF to clear.",
    Default = false,
    Callback = function(Value)
        if not _G.BTR_UI_READY then return end
        BuildSystem.Active = Value
        local lp = game:GetService("Players").LocalPlayer
        local hrp = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
        local clearTools = game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s")

        if Value then
            if not hrp then BuildSystem.Active = false return end
            if hrp then hrp.Anchored = true end

            task.spawn(function()
                
                local tool = getOrFetchPropMaker()
                
                if tool and hrp and hrp.Parent then
                    local hum = hrp.Parent:FindFirstChild("Humanoid")
                    if hum then 
                        hum:EquipTool(tool) 
                        
                        lp.Character:WaitForChild("PropMaker", 2)
                    end
                end
                task.wait(0.5)
                pcall(function() clearTools:FireServer("OpenPropMenu") end)
                task.wait(0.2)

                
                local startCFrame = hrp.CFrame * CFrame.new(0, 2, -12)
                local points = getPoints(BuildSystem.CurrentShape)
                local lastProp = ""
                local lastCat = ""
                
                
                local spawnedProps = {}
                local connection
                local myPropName = "Prop" .. lp.Name
                connection = workspace:WaitForChild("WorkspaceCom").DescendantAdded:Connect(function(desc)
                    if desc:IsA("Model") and desc.Name == myPropName then
                        table.insert(spawnedProps, desc)
                    end
                end)

                local allSameProp = true
                local firstP = points[1]
                for _, pt in ipairs(points) do
                    if pt.prop ~= firstP.prop or pt.cat ~= firstP.cat then
                        allSameProp = false
                        break
                    end
                end

                if allSameProp and #points > 1 then
                    pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction"):FireServer("filterClick", {name = firstP.cat, itemType = "Props"}) end)
                    task.wait(0.1)
                    pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction"):FireServer("filterClick", {name = firstP.prop, itemType = "Props"}) end)
                    task.wait(0.1)
                    pcall(function() clearTools:FireServer("RequestingPropName", firstP.prop, firstP.cat) end)
                    task.wait(0.3)

                    local currentTool = lp.Character:FindFirstChild("PropMaker")
                    local propMake = currentTool and currentTool:FindFirstChild("Tool_PropMake")
                    if propMake then
                        local dup = spawnAndDuplicateProps(propMake, startCFrame.Position, #points, 0)
                        if not dup then
                            for i, pt in ipairs(points) do
                                if not BuildSystem.Active then break end
                                local actualScale = (BuildSystem.CurrentShape == "AK-47" or BuildSystem.CurrentShape == "AK47 HD" or BuildSystem.CurrentShape == "Zizi") and 1.5 or BuildSystem.Scale
                                local targetPos = (startCFrame * CFrame.new(pt.x * actualScale, pt.y * actualScale, (pt.z or 0) * actualScale)).Position
                                pcall(function() propMake:FireServer(workspace.Terrain, targetPos) end)
                                task.wait(0.85)
                            end
                        end
                    end
                else
                    for i, pt in ipairs(points) do
                        if not BuildSystem.Active then break end
                        local useProp = pt.prop
                        local useCat  = pt.cat

                        if useProp ~= lastProp or useCat ~= lastCat then
                            pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction"):FireServer("filterClick", {name = useCat, itemType = "Props"}) end)
                            task.wait(0.1)
                            pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction"):FireServer("filterClick", {name = useProp, itemType = "Props"}) end)
                            task.wait(0.1)
                            pcall(function() clearTools:FireServer("RequestingPropName", useProp, useCat) end)
                            task.wait(0.3)
                            lastProp = useProp
                            lastCat  = useCat
                        end

                        local actualScale = (BuildSystem.CurrentShape == "AK-47" or BuildSystem.CurrentShape == "AK47 HD" or BuildSystem.CurrentShape == "Zizi") and 1.5 or BuildSystem.Scale
                        local targetPos = (startCFrame * CFrame.new(pt.x * actualScale, pt.y * actualScale, (pt.z or 0) * actualScale)).Position
                        local currentTool = lp.Character:FindFirstChild("PropMaker")
                        if currentTool then
                            local propMake = currentTool:FindFirstChild("Tool_PropMake")
                            if propMake then
                                propMake:FireServer(workspace.Terrain, targetPos)
                            end
                        end
                        task.wait(0.85)
                    end
                end
                
                if connection then connection:Disconnect() end
                
                
                task.wait(0.5)
                for i, pt in ipairs(points) do
                    if not BuildSystem.Active then break end
                    local propModel = spawnedProps[i]
                    if propModel then
                        local scf = propModel:FindFirstChild("SetCurrentCFrame")
                        if scf then
                            local rx = pt.rx and math.rad(pt.rx) or 0
                            local ry = pt.ry and math.rad(pt.ry) or 0
                            local rz = pt.rz and math.rad(pt.rz) or 0
                            local actualScale = (BuildSystem.CurrentShape == "AK-47" or BuildSystem.CurrentShape == "AK47 HD" or BuildSystem.CurrentShape == "Zizi") and 1.5 or BuildSystem.Scale
                            local targetCF = startCFrame * CFrame.new(pt.x * actualScale, pt.y * actualScale, (pt.z or 0) * actualScale) * CFrame.Angles(rx, ry, rz)
                            task.spawn(function() pcall(function() scf:InvokeServer(targetCF) end) end)
                        end
                    end
                end

                task.wait(1)
                pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker") end)
                local humanoid = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
                if humanoid then pcall(function() humanoid:UnequipTools() end) end
                pcall(function() clearTools:FireServer("ClosePropMenu") end)
                cleanupPropMakerTool()
                if hrp then hrp.Anchored = false end
                BuildSystem.Active = false
            end)
        else
            if hrp then hrp.Anchored = false end
            pcall(function() clearTools:FireServer("ClearAllProps") end)
        end
    end
})

BuildTab:AddDropdown({
    Name = "Shape",
    Options = {"AK-47", "AK47 HD", "Zizi"},
    Default = "AK-47",
    Callback = function(value)
        BuildSystem.CurrentShape = value
    end
})

    BuildTab:AddSection({"Attraction: RGB Orbit Aura"})

    local orbitActive = false
    local orbitRGB = true
    local orbitLoopConnection = nil
    local orbitProps = {}
    local orbitSpeed = 1.5     
    local orbitRadius = 6      
    local selectedOrbitProp = "FurnitureBleachers"
    local selectedAuraShape = "Roller Coaster"
    local rideState = "WAITING"
    local rideTimer = 0
    local rideDuration = 20
    local rideIntensity = "Sky Rider"
    local rideBoardingTime = 5
    local rideCooldownTime = 3
    local rideArrivalTimer = 0
    local cityRideRecallRequested = false 
    local rideAnchor = Vector3.new(0, 0, 0) 
    local aStream = PropStreamer.new(0.066)

    BuildTab:AddDropdown({
        Name = "Aura Shape",
        Options = {"Dragon Tail", "Ferris Wheel", "Tornado", "Triple Ring", "Black Hole", "DNA Helix", "Meteor Shower", "Dragon Spirit", "Roller Coaster", "View the city"},
        Default = "Roller Coaster",
        Callback = function(val)
            selectedAuraShape = val
            rideState = "WAITING"
        end
    })

    BuildTab:AddSlider({
        Name = "Ride Duration (sec)",
        Min = 10,
        Max = 180,
        Default = 60,
        Color = Color3.fromRGB(255, 255, 255),
        Increment = 10,
        Callback = function(Value)
            rideDuration = Value
        end
    })

    BuildTab:AddSlider({
        Name = "Departure Time (sec)",
        Min = 1,
        Max = 15,
        Default = 5,
        Color = Color3.fromRGB(255, 255, 255),
        Increment = 1,
        Callback = function(Value)
            rideBoardingTime = Value
        end
    })

    BuildTab:AddSlider({
        Name = "Station Pause (sec)",
        Min = 0,
        Max = 15,
        Default = 3,
        Color = Color3.fromRGB(255, 255, 255),
        Increment = 1,
        Callback = function(Value)
            rideCooldownTime = Value
        end
    })

    BuildTab:AddDropdown({
        Name = "Coaster Intensity",
        Options = {"Sky Rider", "Grand Tour", "Orbital Strike"},
        Default = "Sky Rider",
        Callback = function(val)
            rideIntensity = val
        end
    })

    BuildTab:AddDropdown({
        Name = "Aura Type",
        Options = {"Bleachers", "Chairs", "Futuristic Large Bed"},
        Default = "Bleachers",
        Callback = function(val)
            if val == "Bleachers" then selectedOrbitProp = "FurnitureBleachers"
            elseif val == "Chairs" then selectedOrbitProp = "FurnitureChairBlackBig"
            elseif val == "Futuristic Large Bed" then selectedOrbitProp = "FuturisticLargeBed"
            end
        end
    })

    BuildTab:AddToggle({
        Name = "RGB Colors",
        Default = true,
        Callback = function(val)
            orbitRGB = val
        end
    })

    BuildTab:AddButton({
        Name = "Recall View the city",
        Callback = function()
            if selectedAuraShape ~= "View the city" then
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="View the city", Text="Select 'View the city' first in Aura Shape!", Duration=4})
                return
            end
            if rideState == "RIDING" then
                cityRideRecallRequested = true
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="View the city", Text="Return requested! The ride is coming back slowly...", Duration=4})
            elseif rideState == "WAITING" or rideState == "BOARDING" then
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="View the city", Text="The attraction is already at the station!", Duration=3})
            else
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="View the city", Text="Already returning...", Duration=3})
            end
        end
    })

    BuildTab:AddToggle({
        Name = "RGB Orbit Aura",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            orbitActive = val
            local lp = game.Players.LocalPlayer
            local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
            local clearTools = RE:WaitForChild("1Clea1rTool1s")
            local toolRemote = RE:WaitForChild("1Too1l")

            
            if not orbitActive then
                if orbitLoopConnection then orbitLoopConnection:Disconnect() orbitLoopConnection = nil end
                aStream:stop()
                pcall(function() clearTools:FireServer("ClearAllProps") end)
                pcall(function() clearTools:FireServer("ClosePropMenu") end)
                orbitProps = {}
                cleanupPropMakerTool()
                return
            end

            
            
            pcall(function() clearTools:FireServer("ClearAllProps") end)
            task.wait(0.3)
            
            local propsTool = getOrFetchPropMaker()
            
            local humanoid = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if propsTool and humanoid then
                humanoid:EquipTool(propsTool)
                task.wait(0.3)
            end
            local toolPropMake = propsTool and propsTool:FindFirstChild("Tool_PropMake")
            if not toolPropMake then return end
            
            pcall(function() clearTools:FireServer("OpenPropMenu") end)
            task.wait(0.1)

            
            local category = (selectedOrbitProp == "LightsSpotLightWhite") and "Lights" or "Furniture"
            pcall(function() clearTools:FireServer("RequestingPropName", selectedOrbitProp, category) end)
            task.wait(0.1)

            orbitProps = {}

            
            local runService = game:GetService("RunService")
            local t = 0
            local lastColorUpdate = 0
            local lastMoveUpdate = 0
            
            orbitLoopConnection = runService.RenderStepped:Connect(function(dt)
                if not orbitActive then return end
                t = t + dt * orbitSpeed

                
                local updateMove = false
                if tick() - lastMoveUpdate > 0.05 then
                    updateMove = true
                    lastMoveUpdate = tick()
                end

                local updateColor = false
                if orbitRGB and (tick() - lastColorUpdate > 0.1) then
                    updateColor = true
                    lastColorUpdate = tick()
                end
                
                if not updateMove and not updateColor then return end 

                local myRoot = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
                if not myRoot then return end
                local center = myRoot.Position + Vector3.new(0, 5, 0) 

                local hue = (t * 0.6) % 1
                local rgbColor = Color3.fromHSV(hue, 1, 1)
                
                local totalProps = #orbitProps
                if totalProps == 0 then return end

                for i, pData in ipairs(orbitProps) do
                    local finalPos, finalCF
                    
                    if selectedAuraShape == "Ferris Wheel" then
                        
                        local baseAngle = (i / totalProps) * (2 * math.pi) + t * 0.7
                        local radius = orbitRadius + 4
                        local lookVector = myRoot.CFrame.LookVector
                        local rightVector = myRoot.CFrame.RightVector
                        
                        local rimX = math.cos(baseAngle) * radius
                        local rimY = math.sin(baseAngle) * radius
                        finalPos = center + (rightVector * rimX) + Vector3.new(0, rimY, 0)
                        
                        finalCF = CFrame.fromMatrix(finalPos, lookVector, Vector3.new(0, 1, 0))

                    elseif selectedAuraShape == "Black Hole" then
                        
                        
                        
                        local r = 2.5 + ((i - 1) / math.max(totalProps - 1, 1)) * 8
                        local keplerSpeed = 6 / math.sqrt(r + 0.5)
                        local ang = t * keplerSpeed + (i * 2.399)
                        local tilt = 0.6
                        local lx = math.cos(ang) * r
                        local lz = math.sin(ang) * r
                        local ly = math.sin(ang) * r * tilt
                        local shiver = (1 - r / 10.5) * math.sin(t * 9 + i * 1.7) * 0.35
                        finalPos = myRoot.Position + Vector3.new(lx, 3 + ly + shiver, lz)
                        
                        finalCF = CFrame.new(finalPos, finalPos + Vector3.new(-math.sin(ang) * r, math.cos(ang) * r * tilt, math.cos(ang) * r))

                    elseif selectedAuraShape == "Dragon Tail" then
                        
                        local dist = i * 2.4
                        local lookVector = myRoot.CFrame.LookVector
                        local rightVector = myRoot.CFrame.RightVector
                        
                        local wiggleH = math.sin(t * 2.8 - i * 0.42) * (3.5 + math.sin(t * 0.9) * 1.8)
                        local wiggleV = math.sin(t * 2.2 - i * 0.31) * 1.6
                        local archDown = i * 0.1 
                        finalPos = myRoot.Position - (lookVector * dist) + (rightVector * wiggleH) + Vector3.new(0, 2.5 + wiggleV - archDown, 0)
                        
                        local fwdBias = (-lookVector + rightVector * (math.cos(t * 2.8 - i * 0.42) * 0.5)).Unit
                        finalCF = CFrame.new(finalPos, finalPos + fwdBias)

                    elseif selectedAuraShape == "Tornado" then
                        
                        local heightPct = (i - 1) / math.max(totalProps - 1, 1)
                        local height = -3 + heightPct * 28
                        local radius = 1.2 + heightPct * 13 
                        local spinSpeed = 2.5 + heightPct * 9  
                        local spinAngle = t * spinSpeed + (i / totalProps) * 2 * math.pi
                        local lx = math.cos(spinAngle) * radius
                        local lz = math.sin(spinAngle) * radius
                        finalPos = myRoot.Position + Vector3.new(lx, height, lz)
                        
                        finalCF = CFrame.new(finalPos, finalPos + Vector3.new(-math.sin(spinAngle), 0.2 * heightPct, math.cos(spinAngle)))

                    elseif selectedAuraShape == "Meteor Shower" then
                        
                        
                        
                        local ph = ((t * 1.4 + (i / totalProps)) % 1)
                        local height = 32 * (1 - ph * ph) - 2
                        local radius = math.sin(ph * math.pi) * 5
                        local spin = ph * math.pi * 6 + i * 2.4
                        local lx = math.cos(spin) * radius
                        local lz = math.sin(spin) * radius
                        finalPos = myRoot.Position + Vector3.new(lx, height, lz)
                        
                        finalCF = CFrame.new(finalPos, finalPos + Vector3.new(lx * 0.2, -2, lz * 0.2))

                    elseif selectedAuraShape == "Dragon Spirit" then
                        
                        
                        
                        
                        local bodyPct = (i - 1) / math.max(totalProps - 1, 1)
                        local ang = t * 1.9 + bodyPct * 2 * math.pi * 3
                        local radius = 6 + math.sin(t * 2.1 + bodyPct * math.pi * 2) * 1.3
                        local yWave = math.sin(t * 2.6 - bodyPct * math.pi * 4) * 2.8
                        local lx = math.cos(ang) * radius
                        local lz = math.sin(ang) * radius
                        finalPos = myRoot.Position + Vector3.new(lx, 3 + yWave, lz)
                        
                        local nextAng = ang + 0.12
                        local nextRadius = 6 + math.sin(t * 2.1 + (bodyPct + 0.02) * math.pi * 2) * 1.3
                        local nextPos = myRoot.Position + Vector3.new(math.cos(nextAng) * nextRadius, 3 + math.sin(t * 2.6 - (bodyPct + 0.02) * math.pi * 4) * 2.8, math.sin(nextAng) * nextRadius)
                        finalCF = CFrame.new(finalPos, nextPos)

                    elseif selectedAuraShape == "DNA Helix" then
                        
                        local isStrand2 = (i % 2 == 0)
                        local strandIdx = math.ceil(i / 2)
                        local phase = isStrand2 and math.pi or 0
                        local totalHeight = 20
                        local scrollSpeed = 1.8
                        local halfProps = math.max(math.ceil(totalProps / 2), 1)
                        
                        local yOffset = ((strandIdx / halfProps) * totalHeight + t * scrollSpeed) % totalHeight - 5
                        
                        local spinAngle = (strandIdx / halfProps) * 2 * math.pi * 3 + t * 2.2 + phase
                        local lx = math.cos(spinAngle) * 3.8
                        local lz = math.sin(spinAngle) * 3.8
                        finalPos = myRoot.Position + Vector3.new(lx, yOffset, lz)
                        finalCF = CFrame.new(finalPos, myRoot.Position + Vector3.new(0, yOffset, 0))

                    elseif selectedAuraShape == "Roller Coaster" or selectedAuraShape == "View the city" then
                        
                        local now = tick()
                        local parkAngSpeed = (selectedAuraShape == "View the city") and 0.4 or 2.0

                        local playersSeated = 0
                        for _, player in pairs(game.Players:GetPlayers()) do
                            local h = player.Character and player.Character:FindFirstChild("Humanoid")
                            if h and h.Sit and h.SeatPart then
                                for _, pData in ipairs(orbitProps) do
                                    if pData.scf and pData.scf.Parent and h.SeatPart:IsDescendantOf(pData.scf.Parent) then
                                        playersSeated = playersSeated + 1
                                        break
                                    end
                                end
                            end
                        end

                        if rideState == "WAITING" then
                            if playersSeated > 0 and #orbitProps == totalProps then
                                if now - rideArrivalTimer > rideCooldownTime then
                                    rideState = "BOARDING"
                                    rideTimer = now
                                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Coaster Mode", Text="Departing in "..rideBoardingTime.."s!", Duration=math.min(rideBoardingTime, 5)})
                                end
                            end
                        elseif rideState == "BOARDING" then
                            if playersSeated == 0 then
                                rideState = "WAITING"
                            elseif now - rideTimer > rideBoardingTime then
                                rideState = "RIDING"
                                rideTimer = now
                                
                                
                                rideAnchor = myRoot.Position
                                game:GetService("StarterGui"):SetCore("SendNotification", {Title=rideIntensity, Text="Hold on!! ("..rideDuration.."s)", Duration=3})
                            end
                        elseif rideState == "RIDING" then
                            if now - rideTimer > rideDuration or (selectedAuraShape == "View the city" and cityRideRecallRequested) then
                                rideState = "LANDING"
                                rideTimer = now
                                cityRideRecallRequested = false
                                game:GetService("StarterGui"):SetCore("SendNotification", {Title="View the city", Text="Returning smoothly...", Duration=3})
                            end
                        elseif rideState == "LANDING" then
                            local parkCycleTime = (2 * math.pi) / parkAngSpeed
                            local minWait = totalProps * 0.15 + 1
                            local maxLandTime = math.ceil(minWait / parkCycleTime) * parkCycleTime
                            if now - rideTimer > maxLandTime then
                                rideState = "WAITING"
                                rideArrivalTimer = now
                                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Coaster Mode", Text="All clear!", Duration=3})
                            end
                        end

                        
                        local function getTrackPoint(tTrack)
                            local parkRadius = 15
                            local parkHeight = -4.5
                            local rideDurationSafe = math.max(rideDuration, 1)
                            local spinBase = parkAngSpeed * rideDurationSafe
                            local minSpins = math.ceil(spinBase / (2 * math.pi)) + 2
                            local finalAng = minSpins * 2 * math.pi
                            local spinFast = (finalAng - spinBase) / 2

                            if tTrack <= 0 then
                                return Vector3.new(math.cos(tTrack * parkAngSpeed)*parkRadius, parkHeight, math.sin(tTrack * parkAngSpeed)*parkRadius)
                            end
                            if tTrack >= rideDurationSafe then
                                local ang = finalAng + (tTrack - rideDurationSafe) * parkAngSpeed
                                return Vector3.new(math.cos(ang)*parkRadius, parkHeight, math.sin(ang)*parkRadius)
                            end

                            local pt = tTrack / rideDurationSafe
                            local pArc = math.sin(pt * math.pi) 

                            
                            local baseAng = spinBase * pt + spinFast * (1 - math.cos(pt * math.pi))
                            local baseX = math.cos(baseAng) * parkRadius
                            local baseZ = math.sin(baseAng) * parkRadius

                            
                            local shapeX, shapeY, shapeZ = 0, 0, 0

                            if selectedAuraShape == "View the city" then
                                
                                
                                
                                
                                

                                
                                local cityMaxRadius = 400 
                                local cityMaxHeight = 200 
                                local spiralTurns  = 2    
                                local cruiseTurns  = 1.0  

                                
                                if pt < 0.30 then
                                    
                                    
                                    local ph = pt / 0.30
                                    local easePh = ph * ph * (3 - 2*ph)  
                                    local spiralAng = easePh * spiralTurns * 2 * math.pi
                                    local r = easePh * cityMaxRadius
                                    
                                    local hillWave = math.sin(spiralAng * 2.1 + 0.5) * 30 * easePh
                                    shapeX = math.cos(spiralAng) * r
                                    shapeZ = math.sin(spiralAng) * r
                                    shapeY = easePh * cityMaxHeight + hillWave

                                elseif pt < 0.70 then
                                    
                                    
                                    local ph = (pt - 0.30) / 0.40
                                    local tourAng = ph * cruiseTurns * 2 * math.pi

                                    
                                    local rBase = cityMaxRadius
                                    local rWobble = math.sin(tourAng * 1.7) * 150 + math.cos(tourAng * 0.9) * 80
                                    local r = rBase + rWobble

                                    shapeX = math.cos(tourAng) * r
                                    shapeZ = math.sin(tourAng) * r

                                    
                                    local altBase   = cityMaxHeight
                                    local altWave1  = math.sin(tourAng * 3.1)  * 55  
                                    local altWave2  = math.sin(tourAng * 7.3 + 1.2) * 20  
                                    local altWave3  = math.cos(tourAng * 1.8 + 0.7) * 35  
                                    shapeY = altBase + altWave1 + altWave2 + altWave3

                                else
                                    
                                    local ph = (pt - 0.70) / 0.30
                                    local easePh = 1 - ph * ph * (3 - 2*ph)  
                                    local spiralAng = (1-ph) * spiralTurns * 2 * math.pi
                                    local r = easePh * cityMaxRadius
                                    local hillWave = math.sin(spiralAng * 2.1 + 0.5) * 30 * easePh
                                    shapeX = math.cos(spiralAng) * r
                                    shapeZ = math.sin(spiralAng) * r
                                    shapeY = easePh * cityMaxHeight + hillWave
                                end

                                
                                baseX = 0
                                baseZ = 0
                            elseif rideIntensity == "Sky Rider" then
                                
                                
                                
                                
                                
                                
                                
                                if pt < 0.15 then
                                    local ph = pt / 0.15
                                    
                                    shapeY = ph * ph * 170
                                    shapeX = math.sin(ph * math.pi) * 20
                                elseif pt < 0.55 then
                                    local ph = (pt - 0.15) / 0.40
                                    
                                    local loopAng = ph * 3 * 2 * math.pi
                                    shapeY = 170 + math.sin(loopAng) * 100
                                    
                                    shapeX = math.cos(loopAng * 0.4) * 75 * math.sin(ph * math.pi)
                                    shapeZ = math.sin(loopAng * 0.3) * 55 * math.sin(ph * math.pi)
                                elseif pt < 0.80 then
                                    local ph = (pt - 0.55) / 0.25
                                    
                                    local rawY = 170 - ph * 195 
                                    shapeY = math.max(2, rawY) 
                                    shapeX = math.sin(ph * math.pi * 2) * 90 * (1 - ph)
                                    shapeZ = math.cos(ph * math.pi * 3) * 60 * (1 - ph)
                                else
                                    local ph = (pt - 0.80) / 0.20
                                    
                                    shapeY = (1 - ph) * (1 - ph) * 18
                                end

                            elseif rideIntensity == "Grand Tour" then
                                
                                
                                
                                
                                
                                local bigRadius = 275 
                                
                                
                                
                                shapeX = math.cos(baseAng) * bigRadius * pArc
                                shapeZ = math.sin(baseAng) * bigRadius * pArc
                                
                                shapeY = pArc * 12 + math.sin(pt * math.pi * 5) * 4 * pArc

                            else 
                                
                                
                                
                                
                                
                                
                                if pt < 0.25 then
                                    local ph = pt / 0.25
                                    shapeY = ph * ph * 290
                                    shapeX = math.sin(ph * math.pi * 3) * 4 * ph
                                    shapeZ = math.cos(ph * math.pi * 3) * 4 * ph
                                elseif pt < 0.60 then
                                    local ph = (pt - 0.25) / 0.35
                                    shapeY = 290 - ph * 60
                                    local orbitR = 45 * math.sin(ph * math.pi)
                                    shapeX = math.cos(ph * math.pi * 4) * orbitR
                                    shapeZ = math.sin(ph * math.pi * 4) * orbitR
                                else
                                    local ph = (pt - 0.60) / 0.40
                                    shapeY = (1 - ph) * (1 - ph) * 230
                                    local vrille = ph * math.pi * 10
                                    local vrRadius = (1 - ph) * 30
                                    shapeX = math.cos(vrille) * vrRadius
                                    shapeZ = math.sin(vrille) * vrRadius
                                end
                            end

                            return Vector3.new(baseX + shapeX, parkHeight + shapeY, baseZ + shapeZ)
                        end

                        local pTime = 0
                        if rideState == "RIDING" then
                            pTime = now - rideTimer
                        elseif rideState == "LANDING" then
                            pTime = rideDuration + (now - rideTimer)
                        end

                        local carTrackTime
                        if selectedAuraShape == "View the city" then
                            
                            local rowIdx = math.ceil(i / 2) - 1
                            carTrackTime = pTime - (rowIdx * 0.35) 
                        else
                            carTrackTime = pTime - (i * 0.15)
                        end

                        local currentPos = getTrackPoint(carTrackTime)
                        local nextPos    = getTrackPoint(carTrackTime + 0.1)

                        local lookVector = myRoot.CFrame.LookVector
                        
                        local anchor = (rideState == "RIDING" or rideState == "LANDING") and rideAnchor or myRoot.Position
                        local worldPos  = anchor + currentPos
                        local worldLook = anchor + nextPos

                        if (worldLook - worldPos).Magnitude > 0.01 then
                            finalCF = CFrame.new(worldPos, worldLook)
                        else
                            finalCF = myRoot.CFrame
                        end
                          
                        local finalColor = rgbColor 

                        
                        if selectedAuraShape == "View the city" then
                            
                            local isRightSeat = (i % 2 == 0)
                            local xOffset = isRightSeat and 2.5 or -2.5
                            finalCF = finalCF * CFrame.new(xOffset, 0, 0)
                        end

                        
                        if rideState == "RIDING" then
                            local pt   = math.clamp(carTrackTime / rideDuration, 0, 1)
                            local pArc = math.sin(pt * math.pi)
                            local roll = 0

                            if selectedAuraShape == "View the city" then
                                
                                local nextNextPos = getTrackPoint(carTrackTime + 0.5)
                                local futureWorld = anchor + nextNextPos
                                local toFuture = (futureWorld - worldPos)
                                if toFuture.Magnitude > 0.01 then
                                    local rightVec = finalCF.RightVector
                                    local lateralDiff = toFuture:Dot(rightVec)
                                    roll = math.clamp(lateralDiff * 0.003, -0.25, 0.25)
                                end
                            elseif rideIntensity == "Sky Rider" then
                                
                                if pt > 0.15 and pt < 0.55 then
                                    local ph = (pt - 0.15) / 0.40
                                    roll = math.cos(ph * 3 * 2 * math.pi) * 2.2 * pArc
                                elseif pt > 0.55 and pt < 0.80 then
                                    
                                    roll = math.sin(pt * math.pi * 8) * 1.5 * pArc
                                end

                            elseif rideIntensity == "Grand Tour" then
                                
                                roll = math.sin(pt * 2 * math.pi) * 0.35 * pArc

                            else 
                                if pt > 0.60 then
                                    roll = ((pt - 0.60) / 0.40) * math.pi * 5
                                end
                            end

                            finalCF = finalCF * CFrame.Angles(0, 0, roll)
                        end
                        
                    else
                        
                        
                        local propsPerRing = 5
                        local ringIdx = math.floor((i - 1) / propsPerRing) + 1
                        if ringIdx > 3 then ringIdx = 3 end
                        local j = ((i - 1) % propsPerRing) + 1
                        local r = orbitRadius + 1
                        local speeds = {0.9, 1.6, 2.5} 
                        local ang = (j / propsPerRing) * 2 * math.pi + t * speeds[ringIdx]
                        local pivot = myRoot.Position + Vector3.new(0, 2, 0)
                        if ringIdx == 1 then
                            finalPos = pivot + Vector3.new(math.cos(ang)*r, 0, math.sin(ang)*r)
                        elseif ringIdx == 2 then
                            finalPos = pivot + Vector3.new(math.cos(ang)*r, math.sin(ang)*r, 0)
                        else
                            finalPos = pivot + Vector3.new(0, math.sin(ang)*r, math.cos(ang)*r)
                        end
                        finalCF = CFrame.new(finalPos, pivot)
                    end
                    
                    
                    if selectedOrbitProp == "FurnitureBleachers" then
                        if selectedAuraShape == "Dragon Tail" then
                            finalCF = finalCF * CFrame.Angles(0, math.pi/2, 0)
                        elseif selectedAuraShape == "Ferris Wheel" or selectedAuraShape == "Roller Coaster" then
                            finalCF = finalCF * CFrame.Angles(0, math.pi/2, 0)
                        elseif selectedAuraShape == "View the city" then
                            
                            finalCF = finalCF * CFrame.Angles(0, math.pi, 0)
                        end
                    end

                    if updateMove then
                        aStream:setCF(pData.scf, finalCF)
                    end
                    if updateColor and pData.cpc then
                        aStream:setColor(pData.scf, finalColor or rgbColor)
                    end
                end
            end)

            
            task.spawn(function()
                local spawnPos = lp.Character.HumanoidRootPart.Position
                local duplicated = spawnAndDuplicateProps(toolPropMake, spawnPos, 15, 0)
                if not duplicated then
                    for i = 1, 15 do
                        if not orbitActive then break end
                        pcall(function() toolPropMake:FireServer(workspace.Terrain, spawnPos) end)
                        task.wait(0.85) 
                        if not orbitActive then break end
                        
                        
                        local myPropName = "Prop" .. lp.Name
                        for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                            for _, prop in pairs(folder:GetChildren()) do
                                if prop.Name == myPropName then
                                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                                    local cpc = prop:FindFirstChild("ChangePropColor")
                                    if scf then
                                        local alreadyHave = false
                                        for _, p in ipairs(orbitProps) do
                                            if p.scf == scf then alreadyHave = true break end
                                        end
                                        if not alreadyHave then
                                            table.insert(orbitProps, {scf = scf, cpc = cpc})
                                            aStream:add(scf, cpc)
                                        end
                                    end
                                end
                            end
                        end
                    end
                else
                    task.wait(0.2)
                    local myPropName = "Prop" .. lp.Name
                    for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                        for _, prop in pairs(folder:GetChildren()) do
                            if prop.Name == myPropName then
                                local scf = prop:FindFirstChild("SetCurrentCFrame")
                                local cpc = prop:FindFirstChild("ChangePropColor")
                                if scf then
                                    local alreadyHave = false
                                    for _, p in ipairs(orbitProps) do
                                        if p.scf == scf then alreadyHave = true break end
                                    end
                                    if not alreadyHave then
                                        table.insert(orbitProps, {scf = scf, cpc = cpc})
                                        aStream:add(scf, cpc)
                                    end
                                end
                            end
                        end
                    end
                end
                
                if orbitActive then
                    aStream:start()
                    task.wait(1)
                    pcall(function() game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker") end)
                    pcall(function() clearTools:FireServer("ClosePropMenu") end)
                    if humanoid then pcall(function() humanoid:UnequipTools() end) end
                    cleanupPropMakerTool()
                end
            end)
        end
    })
end

do
    local lp = game:GetService("Players").LocalPlayer
    local spiderActive = false
    local spiderLoop = nil
    local spiderAnchor = Vector3.new(0, 0, 0)
    local spiderAngle = 0
    local spiderAnimT = 0
    local spiderState = "idle"
    local spiderStance = "Ride (WASD Steering)"
    local spiderSpeed = 50
    local spiderWorkers = {}
    local spiderDriverSeat = nil
    local isSpiderMounted = false
    local spiderColorMode = "Black Widow"
    local spiderTarget = "---"
    local spiderScarePhase = "none"
    local spiderScareTarget = nil
    local spiderScareTimer = 0
    local spiderSlopePitch = 0
    local spiderSlopeRoll = 0
    local spiderFallVelocity = 0
    local roamState = "prowl"
    local roamTimer = 0
    local roamCenter = Vector3.new(0, 0, 0)
    local roamInvestigatePlayer = nil
    local roamTurnDir = 3.2
    local roamTargetAngle = 0
    local roamLidarTimer = 0
    local roamCornerStuckCount = 0

    local function sNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function getSpiderPlayers()
        local list = {"---"}
        for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
            if p ~= lp then
                table.insert(list, p.Name)
            end
        end
        return list
    end

    local SPIDER_LAYOUT = {
        {key = "core", prop = "PlantDirtSmall", cat = "Plant",
         hip = Vector3.new(0, 2.6, 0.0), reach = Vector3.new(0, 2.6, 0.0)},
        {key = "head", prop = "PlantDirtSmall", cat = "Plant",
         hip = Vector3.new(0, 2.8, 2.0), reach = Vector3.new(0, 2.8, 2.0)},
        {key = "saddle", prop = "FloorCushion", cat = "Furniture",
         hip = Vector3.new(0, 2.8, 0.0), reach = Vector3.new(0, 2.8, 0.0)},
        {key = "abdomen", prop = "PlantDirtBig", cat = "Plant",
         hip = Vector3.new(0, 3.4, -3.4), reach = Vector3.new(0, 3.4, -3.4)},
        {key = "spinneret", prop = "PlantDirtSmall", cat = "Plant",
         hip = Vector3.new(0, 2.4, -6.0), reach = Vector3.new(0, 2.4, -6.0)},
        {key = "fangL", prop = "PlantDirtSmall", cat = "Plant",
         hip = Vector3.new(-0.65, 2.2, 2.8), reach = Vector3.new(-0.65, 2.2, 2.8)},
        {key = "fangR", prop = "PlantDirtSmall", cat = "Plant",
         hip = Vector3.new(0.65, 2.2, 2.8), reach = Vector3.new(0.65, 2.2, 2.8)},
        {key = "legL1", prop = "PillarSmall", cat = "Building", side = -1, legIdx = 1,
         hip = Vector3.new(-2.2, 2.6, 2.2), footBase = Vector3.new(-11.5, 0, 8.5)},
        {key = "legR1", prop = "PillarSmall", cat = "Building", side = 1, legIdx = 1,
         hip = Vector3.new(2.2, 2.6, 2.2), footBase = Vector3.new(11.5, 0, 8.5)},
        {key = "legL2", prop = "PillarSmall", cat = "Building", side = -1, legIdx = 2,
         hip = Vector3.new(-2.6, 2.6, 0.7), footBase = Vector3.new(-13.5, 0, 2.8)},
        {key = "legR2", prop = "PillarSmall", cat = "Building", side = 1, legIdx = 2,
         hip = Vector3.new(2.6, 2.6, 0.7), footBase = Vector3.new(13.5, 0, 2.8)},
        {key = "legL3", prop = "PillarSmall", cat = "Building", side = -1, legIdx = 3,
         hip = Vector3.new(-2.6, 2.6, -0.7), footBase = Vector3.new(-13.5, 0, -2.8)},
        {key = "legR3", prop = "PillarSmall", cat = "Building", side = 1, legIdx = 3,
         hip = Vector3.new(2.6, 2.6, -0.7), footBase = Vector3.new(13.5, 0, -2.8)},
        {key = "legL4", prop = "PillarSmall", cat = "Building", side = -1, legIdx = 4,
         hip = Vector3.new(-2.2, 2.6, -2.2), footBase = Vector3.new(-11.5, 0, -8.5)},
        {key = "legR4", prop = "PillarSmall", cat = "Building", side = 1, legIdx = 4,
         hip = Vector3.new(2.2, 2.6, -2.2), footBase = Vector3.new(11.5, 0, -8.5)},
    }

    local function getSpiderPartColor(layout, partIdx, t, state, colorMode)
        if state == "scare" then
            
            if layout.key == "fangL" or layout.key == "fangR" then
                return Color3.fromRGB(12, 12, 14)
            elseif layout.key == "head" then
                return Color3.fromRGB(14, 14, 16)
            end
        end

        if colorMode == "Black Widow" then
            if layout.key == "saddle" then
                return Color3.fromRGB(22, 22, 26)
            elseif layout.key == "fangL" or layout.key == "fangR" then
                return Color3.fromRGB(18, 18, 22)
            elseif layout.key == "abdomen" then
                return Color3.fromRGB(10, 10, 12)
            else
                return Color3.fromRGB(14, 14, 16)
            end
        elseif colorMode == "Acid Broodmother" then
            if layout.key == "fangL" or layout.key == "fangR" then
                return Color3.fromRGB(40, 255, 50)
            elseif layout.key == "abdomen" then
                return Color3.fromRGB(12, 35, 16)
            else
                return Color3.fromRGB(15, 45, 20)
            end
        elseif colorMode == "Tarantula Gold" then
            if layout.key == "fangL" or layout.key == "fangR" then
                return Color3.fromRGB(190, 130, 30)
            elseif layout.key == "abdomen" then
                return Color3.fromRGB(48, 28, 14)
            elseif layout.legIdx then
                return (layout.legIdx % 2 == 0) and Color3.fromRGB(210, 140, 45) or Color3.fromRGB(65, 40, 20)
            else
                return Color3.fromRGB(55, 32, 18)
            end
        elseif colorMode == "Shadow Void" then
            if layout.key == "fangL" or layout.key == "fangR" then
                return Color3.fromRGB(210, 50, 255)
            elseif layout.key == "abdomen" then
                return Color3.fromRGB(25, 10, 42)
            else
                return Color3.fromRGB(30, 12, 50)
            end
        elseif colorMode == "Glacial Weaver" then
            if layout.key == "fangL" or layout.key == "fangR" then
                return Color3.fromRGB(0, 220, 255)
            elseif layout.key == "abdomen" then
                return Color3.fromRGB(210, 240, 255)
            else
                return Color3.fromRGB(175, 225, 250)
            end
        elseif colorMode == "Rainbow Venom" then
            return Color3.fromHSV((t * 0.5 + partIdx * 0.065) % 1, 0.9, 1)
        end

        return Color3.fromRGB(14, 14, 16)
    end

    local function getSpiderPartCFrame(layout, t, state, centerPos, yaw, strideSpeed, slopePitch, slopeRoll)
        local baseCF = CFrame.new(centerPos) * CFrame.Angles(0, yaw, 0) * CFrame.Angles(slopePitch or 0, 0, slopeRoll or 0)

        local bodyLift = 0
        local bodyPitch = 0
        local bodyRoll = 0
        local fangPinch = math.sin(t * 5) * 0.18

        if state == "scare" then
            bodyLift = 1.8 + math.sin(t * 7.0) * 0.9
            bodyPitch = math.rad(-14) + math.sin(t * 7.0) * math.rad(8)
            fangPinch = math.sin(t * 14.0) * 0.55
        elseif state == "walk" then
            local freq = 6.0 + (strideSpeed / 26.0) * 3.0
            bodyLift = math.abs(math.cos(t * freq)) * 0.28
            bodyRoll = math.sin(t * freq) * 0.03
            fangPinch = math.sin(t * 8) * 0.25
        else
            bodyLift = math.sin(t * 2.0) * 0.10
        end

        local rootCF = baseCF * CFrame.new(0, bodyLift, 0) * CFrame.Angles(bodyPitch, 0, bodyRoll)

        if layout.key == "core" then
            return rootCF * CFrame.new(layout.hip)
        elseif layout.key == "head" then
            local headTwitch = (state == "idle") and math.sin(t * 1.5) * 0.04 or 0
            return rootCF * CFrame.new(layout.hip) * CFrame.Angles(headTwitch, 0, 0)
        elseif layout.key == "saddle" then
            return rootCF * CFrame.new(layout.hip) * CFrame.Angles(0, math.pi, 0)
        elseif layout.key == "abdomen" then
            local abdoSway = (state == "walk") and math.sin(t * 6) * 0.06 or math.sin(t * 2.0) * 0.03
            local abdoPitch = math.sin(t * 2.5) * 0.04 + math.rad(5)
            return rootCF * CFrame.new(layout.hip) * CFrame.Angles(abdoPitch, abdoSway, 0)
        elseif layout.key == "spinneret" then
            local spinSway = (state == "walk") and math.sin(t * 6) * 0.08 or math.sin(t * 2.0) * 0.03
            return rootCF * CFrame.new(layout.hip) * CFrame.Angles(math.rad(10), spinSway, 0)
        elseif layout.key == "fangL" then
            local pinch = (state == "scare") and (math.sin(t * 14.0) * 0.25) or (math.sin(t * 2.0) * 0.08)
            return rootCF * CFrame.new(layout.hip) * CFrame.Angles(math.rad(15), math.rad(12) - pinch, 0)
        elseif layout.key == "fangR" then
            local pinch = (state == "scare") and (math.sin(t * 14.0) * 0.25) or (math.sin(t * 2.0) * 0.08)
            return rootCF * CFrame.new(layout.hip) * CFrame.Angles(math.rad(15), math.rad(-12) + pinch, 0)
        elseif layout.legIdx then
            local groupA = (layout.side == -1 and (layout.legIdx == 1 or layout.legIdx == 3)) or (layout.side == 1 and (layout.legIdx == 2 or layout.legIdx == 4))
            local freq = 6.0 + (strideSpeed / 26.0) * 3.0
            local phase = groupA and (t * freq) or (t * freq + math.pi)
            phase = phase % (2 * math.pi)

            local footX = layout.footBase.X
            local footY = layout.footBase.Y
            local footZ = layout.footBase.Z

            if state == "scare" then
                local freq = 7.0
                if layout.legIdx == 1 or layout.legIdx == 2 then
                    
                    local phase = t * freq + (layout.side * 1.2) + (layout.legIdx * 0.8)
                    footY = 3.2 + math.sin(phase) * 2.0
                    footZ = layout.footBase.Z + 4.2 + math.cos(phase) * 1.8
                    footX = layout.footBase.X * 0.75 + math.sin(phase * 0.5) * 1.0
                else
                    
                    local phase = t * freq + (layout.side * 1.8) + (layout.legIdx * 0.8)
                    footY = math.max(0.0, math.sin(phase) * 1.4)
                    footZ = layout.footBase.Z + math.cos(phase) * 1.0
                    footX = layout.footBase.X
                end
            elseif state == "walk" then
                local cycle = phase / (2 * math.pi)
                local strideLen = 3.6
                local liftHeight = 2.6

                if cycle < 0.35 then
                    local swingT = cycle / 0.35
                    footY = math.sin(swingT * math.pi) * liftHeight
                    footZ = layout.footBase.Z + (strideLen * (swingT * 2.0 - 1.0))
                    footX = layout.footBase.X + math.sin(swingT * math.pi) * (layout.side * 0.7)
                else
                    local stanceT = (cycle - 0.35) / 0.65
                    footY = 0.0
                    footZ = layout.footBase.Z + (strideLen * (1.0 - stanceT * 2.0))
                    footX = layout.footBase.X
                end
            else
                local twitch = math.sin(t * 2.0 + layout.legIdx * 1.2 + layout.side) * 0.2
                footY = 0.0
                footZ = layout.footBase.Z + twitch
            end

            local hipWorld = rootCF:PointToWorldSpace(layout.hip)
            local footWorld = baseCF:PointToWorldSpace(Vector3.new(footX, footY, footZ))
            local midWorld = (hipWorld + footWorld) * 0.5

            local legRot = CFrame.lookAt(midWorld, footWorld) * CFrame.Angles(math.rad(90), 0, 0)
            return legRot
        end

        return rootCF
    end

    local function isDriverSitting()
        if not spiderDriverSeat or not isSpiderMounted then return false end
        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not hum or not hrp then
            isSpiderMounted = false
            return false
        end
        if not hum.Sit then
            isSpiderMounted = false
            return false
        end
        if (hrp.Position - spiderAnchor).Magnitude > 8.0 then
            isSpiderMounted = false
            return false
        end
        if hum.SeatPart == spiderDriverSeat or (spiderDriverSeat:IsA("Seat") and spiderDriverSeat.Occupant == hum) then
            return true
        end
        isSpiderMounted = false
        return false
    end

    local function startSpiderWorkers(w)
        task.spawn(function()
            while spiderActive and w.scf do
                local now = tick()
                if w.targetCF then
                    local cf = w.targetCF
                    local send = false
                    if not w.lastSentCF then
                        send = true
                    else
                        local pDiff = (cf.Position - w.lastSentCF.Position).Magnitude
                        if pDiff > 0.04 or (now - w.lastSendTimeCF > 0.055) then
                            send = true
                        end
                    end
                    if send then
                        w.lastSentCF = cf
                        w.lastSendTimeCF = now
                        pcall(function() w.scf:InvokeServer(cf) end)
                    end
                end
                task.wait(0.035)
            end
        end)

        task.spawn(function()
            while spiderActive and w.cpc do
                local now = tick()
                if w.targetColor then
                    local col = w.targetColor
                    local sendCol = false
                    if not w.lastSentColor then
                        sendCol = true
                    else
                        local diff = math.abs(col.R - w.lastSentColor.R) + math.abs(col.G - w.lastSentColor.G) + math.abs(col.B - w.lastSentColor.B)
                        if diff > 0.04 or (now - w.lastSendTimeCol > 0.35) then
                            sendCol = true
                        end
                    end
                    if sendCol then
                        w.lastSentColor = col
                        w.lastSendTimeCol = now
                        pcall(function() w.cpc:InvokeServer(col) end)
                    end
                end
                task.wait(0.08)
            end
        end)
    end

    local function spawnSpider()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")

        sNotif("Giant Spider", "Summoning the Arachnid Titan (15 parts)...", 4)

        local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")
        local myPropName = "Prop" .. lp.Name

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then
            sNotif("Giant Spider", "PropMaker not found!", 3)
            return
        end

        local humanoid = char:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid:EquipTool(propsTool) end
        task.wait(0.4)

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
        if not toolPropMake then return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local rayOrigin = hrp.Position + (hrp.CFrame.LookVector * 16) + Vector3.new(0, 10, 0)
        local rayParams = RaycastParams.new()
        rayParams.FilterDescendantsInstances = {char}
        rayParams.FilterType = Enum.RaycastFilterType.Exclude
        local groundRay = workspace:Raycast(rayOrigin, Vector3.new(0, -50, 0), rayParams)
        local groundY = groundRay and groundRay.Position.Y or (hrp.Position.Y - 2.5)

        spiderAnchor = Vector3.new(rayOrigin.X, groundY, rayOrigin.Z)
        spiderAngle = math.atan2(hrp.CFrame.LookVector.X, hrp.CFrame.LookVector.Z)
        spiderWorkers = {}
        spiderDriverSeat = nil
        isSpiderMounted = false
        spiderAnimT = 0
        spiderState = "idle"
        spiderFallVelocity = 0
        spiderSlopePitch = 0
        roamCenter = spiderAnchor
        roamState = "prowl"
        roamTimer = 0
        spiderSlopeRoll = 0
        spiderScarePhase = "none"
        spiderScareTarget = nil
        spiderScareTimer = 0

        local knownScf = {}
        for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                    if scf then knownScf[scf] = true end
                end
            end
        end

        local hitInst = workspace.Terrain
        local Telemetry = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction")

        for i, layout in ipairs(SPIDER_LAYOUT) do
            if not spiderActive then break end

            local targetCF = getSpiderPartCFrame(layout, 0, "idle", spiderAnchor, spiderAngle, 0, 0, 0)
            local defaultColor = getSpiderPartColor(layout, i, 0, "idle", spiderColorMode)

            pcall(function() Telemetry:FireServer("filterClick", {name=layout.cat, itemType="Props"}) end)
            task.wait(0.04)
            pcall(function() Telemetry:FireServer("filterClick", {name=layout.prop, itemType="Props"}) end)
            task.wait(0.04)
            pcall(function()
                if layout.prop == "FloorCushion" then
                    clearTools:FireServer("RequestingPropFeatureName", layout.prop, layout.cat)
                else
                    clearTools:FireServer("RequestingPropName", layout.prop, layout.cat)
                end
            end)
            task.wait(0.18)

            pcall(function() toolPropMake:FireServer(hitInst, spiderAnchor) end)
            task.wait(0.55)

            local foundProp, foundScf, foundCpc, foundSeat = nil, nil, nil, nil
            for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                for _, prop in pairs(folder:GetChildren()) do
                    if prop.Name == myPropName then
                        local scf = prop:FindFirstChild("SetCurrentCFrame")
                        local cpc = prop:FindFirstChild("ChangePropColor")
                        if scf and not knownScf[scf] then
                            foundProp = prop
                            foundScf = scf
                            foundCpc = cpc
                            knownScf[scf] = true
                            if layout.key == "saddle" then
                                foundSeat = prop:FindFirstChildWhichIsA("Seat", true) or prop:FindFirstChildWhichIsA("VehicleSeat", true)
                            end
                            break
                        end
                    end
                end
                if foundScf then break end
            end

            if foundProp then
                local function killLight(d)
                    if d:IsA("Light") then
                        d.Enabled = false
                        d.Brightness = 0
                        d.Range = 0
                        pcall(function() d:Destroy() end)
                    end
                end
                for _, desc in ipairs(foundProp:GetDescendants()) do
                    killLight(desc)
                end
                foundProp.DescendantAdded:Connect(killLight)
            end

            if foundSeat then
                spiderDriverSeat = foundSeat
                pcall(function()
                    foundSeat.CanCollide = false
                end)
            end

            local w = {
                key = layout.key,
                layout = layout,
                prop = foundProp,
                scf = foundScf,
                cpc = foundCpc,
                partIndex = i,
                targetCF = targetCF,
                lastSentCF = nil,
                lastSendTimeCF = 0,
                targetColor = defaultColor,
                lastSentColor = nil,
                lastSendTimeCol = 0
            }

            spiderWorkers[layout.key] = w

            if foundScf then
                pcall(function() foundScf:InvokeServer(targetCF) end)
            end
            if foundCpc then
                pcall(function() foundCpc:InvokeServer(defaultColor) end)
            end

            startSpiderWorkers(w)
        end

        task.wait(1)
        pcall(function()
            game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker")
        end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        pcall(function()
            local b = lp:FindFirstChildOfClass("Backpack")
            if b then
                local pm = b:FindFirstChild("PropMaker")
                if pm then pm:Destroy() end
            end
            local c = lp.Character
            if c then
                local pm = c:FindFirstChild("PropMaker")
                if pm then pm:Destroy() end
            end
        end)

        sNotif("Giant Spider", "The Arachnid Titan is fully materialized!", 4)
    end

    local function tryMoveSpider(currentPos, moveDelta, rayParams)
        local moveDist = moveDelta.Magnitude
        if moveDist < 0.001 then return currentPos, nil end
        local moveDir = moveDelta.Unit

        
        
        
        local sideVec = Vector3.new(-moveDir.Z, 0, moveDir.X).Unit
        local testPoints = {
            Vector3.new(0, 2.0, 0),
            Vector3.new(0, 1.0, 0),
            Vector3.new(0, 3.2, 0),
            Vector3.new(sideVec.X * 3.2, 2.0, sideVec.Z * 3.2),
            Vector3.new(-sideVec.X * 3.2, 2.0, -sideVec.Z * 3.2)
        }

        local isBlocked = false
        local blockNormal = nil
        local checkDist = moveDist + 2.8

        for _, pt in ipairs(testPoints) do
            local rayOrigin = currentPos + pt
            local hit = workspace:Raycast(rayOrigin, moveDir * checkDist, rayParams)
            if hit and hit.Normal.Y < 0.65 then
                isBlocked = true
                blockNormal = hit.Normal
                break
            end
        end

        if not isBlocked then
            return currentPos + moveDelta, nil
        else
            
            local slideDir = moveDir - blockNormal * moveDir:Dot(blockNormal)
            if slideDir.Magnitude > 0.08 then
                local slideDelta = slideDir.Unit * (moveDist * 0.72)
                
                local slideHit = workspace:Raycast(currentPos + Vector3.new(0, 2.0, 0), slideDelta.Unit * (slideDelta.Magnitude + 2.2), rayParams)
                if not slideHit or slideHit.Normal.Y >= 0.65 then
                    return currentPos + slideDelta, blockNormal
                end
            end
            
            return currentPos, blockNormal
        end
    end

    local function scanLidarCorridor(pos, curAngle, rayParams)
        local numRays = 12
        local bestScore = -9999
        local bestAngle = curAngle
        local rayOrigin = pos + Vector3.new(0, 2.0, 0)

        for i = 0, numRays - 1 do
            local a = (i / numRays) * (2 * math.pi)
            local d = Vector3.new(math.sin(a), 0, math.cos(a))
            local hit = workspace:Raycast(rayOrigin, d * 35.0, rayParams)
            local dist = hit and (hit.Position - rayOrigin).Magnitude or 35.0

            
            local fwdDot = math.cos(a - curAngle)
            local score = dist + (fwdDot * 7.5)

            if score > bestScore then
                bestScore = score
                bestAngle = a
            end
        end
        return bestAngle
    end

    local function startSpiderLoop()
        local RS = game:GetService("RunService")
        local uis = game:GetService("UserInputService")
        local lastUpdate = 0

        spiderLoop = RS.RenderStepped:Connect(function(dt)
            if not spiderActive then return end

            local now = tick()
            if now - lastUpdate < (1 / 45) then return end
            lastUpdate = now

            spiderAnimT = spiderAnimT + dt
            local isDriver = isDriverSitting()

            
            local rayOrigin = Vector3.new(spiderAnchor.X, spiderAnchor.Y + 8, spiderAnchor.Z)
            local rayDir = Vector3.new(0, -50, 0)
            local rayParams = RaycastParams.new()
            local ignoreList = {}
            for _, p in pairs(game:GetService("Players"):GetPlayers()) do
                if p.Character then table.insert(ignoreList, p.Character) end
            end
            local wcom = workspace:FindFirstChild("WorkspaceCom")
            if wcom then table.insert(ignoreList, wcom) end
            for _, w in pairs(spiderWorkers) do
                if w.prop then table.insert(ignoreList, w.prop) end
            end
            rayParams.FilterDescendantsInstances = ignoreList
            rayParams.FilterType = Enum.RaycastFilterType.Exclude

            local groundHit = workspace:Raycast(rayOrigin, rayDir, rayParams)
            if groundHit then
                local targetY = groundHit.Position.Y
                spiderFallVelocity = 0
                local deltaY = math.clamp(targetY - spiderAnchor.Y, -dt * 30, dt * 14)
                spiderAnchor = Vector3.new(spiderAnchor.X, spiderAnchor.Y + deltaY, spiderAnchor.Z)

                
                local norm = groundHit.Normal
                local fwd = Vector3.new(math.sin(spiderAngle), 0, math.cos(spiderAngle))
                local right = Vector3.new(math.cos(spiderAngle), 0, -math.sin(spiderAngle))
                local pitchTarget = math.clamp(math.asin(math.clamp(fwd:Dot(norm), -0.45, 0.45)), -0.45, 0.45)
                local rollTarget = math.clamp(-math.asin(math.clamp(right:Dot(norm), -0.45, 0.45)), -0.45, 0.45)
                spiderSlopePitch = spiderSlopePitch + (pitchTarget - spiderSlopePitch) * math.min(1, dt * 10)
                spiderSlopeRoll = spiderSlopeRoll + (rollTarget - spiderSlopeRoll) * math.min(1, dt * 10)
            else
                spiderFallVelocity = spiderFallVelocity + (workspace.Gravity * 1.5 * dt)
                spiderAnchor = Vector3.new(spiderAnchor.X, spiderAnchor.Y - spiderFallVelocity * dt, spiderAnchor.Z)
                spiderSlopePitch = spiderSlopePitch * (1 - dt * 5)
                spiderSlopeRoll = spiderSlopeRoll * (1 - dt * 5)
            end

            
            local isSprinting = isDriver and uis:IsKeyDown(Enum.KeyCode.LeftShift)
            local currentSpd = isSprinting and (spiderSpeed * 1.9) or spiderSpeed

            if spiderScarePhase == "charge" then
                local tHrp = spiderScareTarget and spiderScareTarget.Character and spiderScareTarget.Character:FindFirstChild("HumanoidRootPart")
                if not tHrp then
                    spiderScarePhase = "none"
                    spiderState = "idle"
                    sNotif("Giant Spider", "Target disappeared!", 2)
                else
                    local diff = tHrp.Position - spiderAnchor
                    local flatDiff = Vector3.new(diff.X, 0, diff.Z)
                    local dist = flatDiff.Magnitude
                    local dir = flatDiff.Unit
                    spiderAngle = math.atan2(dir.X, dir.Z)
                    spiderState = "walk"
                    currentSpd = math.max(spiderSpeed * 1.6, 78)

                    
                    local obsRay = workspace:Raycast(spiderAnchor + Vector3.new(0, 1.8, 0), dir * 4.0, rayParams)
                    if obsRay and obsRay.Normal.Y < 0.45 then
                        local slide = dir - obsRay.Normal * dir:Dot(obsRay.Normal)
                        spiderAnchor = spiderAnchor + (slide.Unit * (currentSpd * dt))
                    else
                        spiderAnchor = spiderAnchor + (dir * (currentSpd * dt))
                    end

                    if dist <= 4.8 then
                        spiderScarePhase = "jumpscare"
                        spiderScareTimer = 0
                        sNotif("Giant Spider", "JUMPSCARE! Terrifying " .. spiderTarget .. "!", 3)
                    end
                end
            elseif spiderScarePhase == "jumpscare" then
                spiderScareTimer = spiderScareTimer + dt
                local tHrp = spiderScareTarget and spiderScareTarget.Character and spiderScareTarget.Character:FindFirstChild("HumanoidRootPart")
                if not tHrp or spiderScareTimer >= 4.5 then
                    spiderScarePhase = "none"
                    spiderState = "idle"
                    sNotif("Giant Spider", spiderTarget .. " was thoroughly terrified!", 3)
                else
                    spiderState = "scare"
                    local tLook = tHrp.CFrame.LookVector
                    local idealPos = tHrp.Position + (tLook * 3.4)
                    local diff = Vector3.new(idealPos.X - spiderAnchor.X, 0, idealPos.Z - spiderAnchor.Z)
                    spiderAnchor = spiderAnchor + (diff * math.min(1, dt * 14))
                    local faceTarget = (tHrp.Position - spiderAnchor)
                    spiderAngle = math.atan2(faceTarget.X, faceTarget.Z)
                end
            elseif isDriver and spiderStance == "Ride (WASD Steering)" then
                local moveDir = 0
                if uis:IsKeyDown(Enum.KeyCode.W) then moveDir = moveDir + 1 end
                if uis:IsKeyDown(Enum.KeyCode.S) then moveDir = moveDir - 1 end

                
                if uis:IsKeyDown(Enum.KeyCode.A) then
                    spiderAngle = spiderAngle - (dt * 3.4)
                end
                if uis:IsKeyDown(Enum.KeyCode.D) then
                    spiderAngle = spiderAngle + (dt * 3.4)
                end

                if math.abs(moveDir) > 0.1 then
                    spiderState = "walk"
                    local forwardVec = Vector3.new(math.sin(spiderAngle), 0, math.cos(spiderAngle))
                    local stepVec = forwardVec * (moveDir * currentSpd * dt)

                    local newPos, _ = tryMoveSpider(spiderAnchor, stepVec, rayParams)
                    spiderAnchor = newPos
                else
                    spiderState = "idle"
                end
            elseif spiderStance == "Guardian (Follow Me)" then
                local myChar = lp.Character
                local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                if myHrp then
                    local diff = myHrp.Position - spiderAnchor
                    local flatDiff = Vector3.new(diff.X, 0, diff.Z)
                    local dist = flatDiff.Magnitude
                    local dir = flatDiff.Unit

                    spiderAngle = math.atan2(dir.X, dir.Z)

                    if dist > 10.5 then
                        spiderState = "walk"
                        local moveDelta = dir * (spiderSpeed * 1.15 * dt)
                        local newPos, _ = tryMoveSpider(spiderAnchor, moveDelta, rayParams)
                        spiderAnchor = newPos
                    else
                        spiderState = "idle"
                    end
                else
                    spiderState = "idle"
                end
            elseif spiderStance == "Roam (Autonomous Predator AI)" then
                roamTimer = roamTimer + dt
                roamLidarTimer = roamLidarTimer + dt

                
                if roamLidarTimer > 0.45 then
                    roamLidarTimer = 0
                    roamTargetAngle = scanLidarCorridor(spiderAnchor, spiderAngle, rayParams)
                end

                
                local fromCenter = Vector3.new(spiderAnchor.X - roamCenter.X, 0, spiderAnchor.Z - roamCenter.Z)
                if fromCenter.Magnitude > 115 then
                    local homeAngle = math.atan2(-fromCenter.X, -fromCenter.Z)
                    roamTargetAngle = homeAngle
                end

                if roamState == "prowl" then
                    spiderState = "walk"
                    currentSpd = 34

                    
                    local angleDiff = (roamTargetAngle - spiderAngle + math.pi) % (2 * math.pi) - math.pi
                    spiderAngle = spiderAngle + math.clamp(angleDiff, -dt * 3.6, dt * 3.6)

                    local moveDelta = Vector3.new(math.sin(spiderAngle), 0, math.cos(spiderAngle)) * (currentSpd * dt)
                    local newPos, blockNorm = tryMoveSpider(spiderAnchor, moveDelta, rayParams)
                    spiderAnchor = newPos

                    if blockNorm then
                        roamCornerStuckCount = roamCornerStuckCount + 1
                        
                        roamLidarTimer = 999
                        if roamCornerStuckCount >= 4 then
                            
                            spiderAnchor = spiderAnchor - (moveDelta.Unit * 1.8)
                            spiderAngle = spiderAngle + math.pi
                            roamTargetAngle = spiderAngle
                            roamCornerStuckCount = 0
                        end
                    else
                        roamCornerStuckCount = 0
                    end

                    
                    if roamTimer > 10.5 then
                        roamState = "pause"
                        roamTimer = 0
                    end

                elseif roamState == "pause" then
                    spiderState = "idle"
                    currentSpd = 0
                    
                    spiderAngle = spiderAngle + (math.sin(roamTimer * 2.0) * 0.22 * dt)

                    if roamTimer > 2.8 then
                        roamState = "prowl"
                        roamTimer = 0
                        roamLidarTimer = 999 
                    end
                end
            else
                spiderState = "idle"
            end

            
            for _, w in pairs(spiderWorkers) do
                w.targetCF = getSpiderPartCFrame(w.layout, spiderAnimT, spiderState, spiderAnchor, spiderAngle, currentSpd, spiderSlopePitch, spiderSlopeRoll)
                w.targetColor = getSpiderPartColor(w.layout, w.partIndex, spiderAnimT, spiderState, spiderColorMode)
            end
        end)
    end

    
    
    
    BuildV2Tab:AddSection({"> Giant Spider Titan"})

    local spiderToggle = BuildV2Tab:AddToggle({
        Name = "Spawn Giant Spider",
        Description = "Summon an ultra-realistic 15-prop arachnid titan with mountable beanbag",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not spiderActive then return end
            if val then
                spiderActive = true
                task.spawn(function()
                    spawnSpider()
                    if spiderActive then startSpiderLoop() end
                end)
            else
                if not spiderActive then return end
                spiderActive = false
                if spiderLoop then spiderLoop:Disconnect() spiderLoop = nil end
                pcall(function()
                    local hum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
                    if hum and hum.Sit == true then hum.Sit = false end
                end)
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps")
                end)
                spiderWorkers = {}
                spiderDriverSeat = nil
                isSpiderMounted = false
                spiderScarePhase = "none"
                sNotif("Giant Spider", "The Arachnid Titan retreats into the shadows.", 2)
            end
        end
    })

    BuildV2Tab:AddButton({
        Name = "Mount / Dismount Beanbag",
        Description = "Mount the discreet beanbag cushion to drive the spider with WASD",
        Callback = function()
            if not spiderActive then sNotif("Giant Spider", "Spawn the spider first!", 2) return end
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not hum then return end

            if isDriverSitting() then
                isSpiderMounted = false
                hum.Sit = false
                hum:ChangeState(Enum.HumanoidStateType.GettingUp)
                sNotif("Giant Spider", "Dismounted.", 2)
            else
                if spiderDriverSeat then
                    pcall(function()
                        spiderDriverSeat.CanCollide = false
                        isSpiderMounted = true
                        spiderDriverSeat:Sit(hum)
                    end)
                    sNotif("Giant Spider", "Mounted! Use WASD to crawl, SHIFT to sprint!", 3)
                else
                    sNotif("Giant Spider", "Beanbag cushion not found!", 2)
                end
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Spider Mode",
        Description = "Select spider behavior (Steer, Follow, or Autonomous AI)",
        Options = {"Ride (WASD Steering)", "Guardian (Follow Me)", "Roam (Autonomous Predator AI)"},
        Default = "Ride (WASD Steering)",
        Callback = function(choice)
            spiderStance = choice
            if choice == "Roam (Autonomous Predator AI)" then
                roamCenter = spiderAnchor
                roamState = "prowl"
                roamTimer = 0
                sNotif("Predator AI", "Autonomous Roam AI activated! Stalking territory...", 3)
            else
                sNotif("Spider Mode", choice, 2)
            end
        end
    })

    local targetDropdown = BuildV2Tab:AddDropdown({
        Name = "Target Player",
        Description = "Select a player to terrify with the giant spider",
        Options = getSpiderPlayers(),
        Default = "---",
        Callback = function(choice)
            spiderTarget = choice
        end
    })

    BuildV2Tab:AddButton({
        Name = "Terrify Target (Scare Player)",
        Description = "Charge across map and perform a terrifying face-to-face jumpscare lunge",
        Callback = function()
            if not spiderActive then
                sNotif("Giant Spider", "Spawn the spider first!", 2)
                return
            end

            if not spiderTarget or spiderTarget == "---" or spiderTarget == "" then
                sNotif("Giant Spider", "Please select a target player in the list first!", 3)
                return
            end

            local tPlr = game:GetService("Players"):FindFirstChild(spiderTarget)
            local tChar = tPlr and tPlr.Character
            local tHrp = tChar and tChar:FindFirstChild("HumanoidRootPart")
            if not tHrp then
                sNotif("Giant Spider", "Target character not found in game!", 2)
                return
            end

            spiderScarePhase = "charge"
            spiderScareTarget = tPlr
            spiderScareTimer = 0
            sNotif("Giant Spider", "Stalking & charging at " .. spiderTarget .. "!", 3)
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Spider Color",
        Description = "Choose the dynamic real-time carapace and venom theme",
        Options = {"Black Widow", "Tarantula Gold", "Acid Broodmother", "Shadow Void", "Glacial Weaver", "Rainbow Venom"},
        Default = "Black Widow",
        Callback = function(choice)
            spiderColorMode = choice
            sNotif("Spider Color", "Theme: " .. choice, 2)
        end
    })

    BuildV2Tab:AddSlider({
        Name = "Crawl Speed",
        Description = "Adjust the crawling speed of the spider",
        Min = 25,
        Max = 120,
        Increase = 2,
        Default = 50,
        Callback = function(val)
            spiderSpeed = val
        end
    })
end

-- [LOCKED: WORK PERFECTLY] Natural Rig Posture + Walking/Breathing Animation + 4 Sizes + Gravity + Master Color Themes

do
    local lp = game:GetService("Players").LocalPlayer
    local stickmanActive = false
    local stickmanSpawnThread = nil
    local stickmanAnchor = Vector3.new(0, 0, 0)
    local stickmanAngle = 0
    local stickmanWorkers = {}
    local stickmanSize = "Medium"
    local stickmanStyle = "Samurai"
    local stickmanColorTheme = "Black"
    local stickmanFollow = false
    local animTime = 0
    local walkCycle = 0
    local groundClearance = 1.25

    local function sNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function getOrFetchPropMaker()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local pm = char:FindFirstChild("PropMaker") or lp.Backpack:FindFirstChild("PropMaker")
        if not pm then
            pcall(function()
                game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker")
            end)
            task.wait(0.4)
            pm = char:FindFirstChild("PropMaker") or lp.Backpack:FindFirstChild("PropMaker")
        end
        return pm
    end

    
    -- [LOCKED: WORK PERFECTLY] MASTER COLOR THEMES ENGINE
    
    local function getStickmanPartColor(theme, key, style, partIdx)
        if theme == "Black" or theme == "Shadow Void" then
            return Color3.fromRGB(15, 15, 15)
        elseif theme == "Rainbow" then
            local pIdx = partIdx or 1
            return Color3.fromHSV((tick() * 0.4 + pIdx * 0.08) % 1, 1, 1)
        elseif theme == "White" or theme == "Ghost Phantom" then
            if key == "halo" then
                return Color3.fromRGB(255, 255, 255)
            end
            return Color3.fromRGB(245, 248, 255)
        elseif theme == "Samurai" or theme == "Realistic" then
            if style == "Samurai" and stickmanSize ~= "Ultra Giant" then
                if key == "hat" then
                    return Color3.fromRGB(195, 155, 100) 
                elseif key == "sword" then
                    return Color3.fromRGB(215, 220, 225) 
                elseif key == "halo" then
                    return Color3.fromRGB(255, 215, 30) 
                elseif key == "wingL" or key == "wingR" then
                    return Color3.fromRGB(165, 25, 25) 
                elseif key == "head" then
                    return Color3.fromRGB(20, 20, 25) 
                elseif key:find("torso") then
                    return Color3.fromRGB(25, 25, 30) 
                else
                    return Color3.fromRGB(35, 35, 40) 
                end
            else
                return Color3.fromRGB(15, 15, 15)
            end
        elseif theme == "Red" or theme == "Blood Demon" then
            if key == "wingL" or key == "wingR" or key == "sword" or key == "halo" or key == "head" then
                return Color3.fromRGB(225, 20, 25)
            elseif key == "hat" then
                return Color3.fromRGB(180, 15, 20)
            elseif key:find("arm") or key:find("leg") then
                return Color3.fromRGB(180, 20, 25)
            else
                return Color3.fromRGB(25, 10, 12)
            end
        elseif theme == "Yellow" or theme == "Gold" or theme == "Divine Gold" then
            if key == "halo" or key == "hat" or key == "head" then
                return Color3.fromRGB(255, 225, 50)
            elseif key == "wingL" or key == "wingR" or key == "sword" then
                return Color3.fromRGB(255, 200, 35)
            elseif key:find("arm") or key:find("leg") then
                return Color3.fromRGB(240, 190, 40)
            else
                return Color3.fromRGB(210, 160, 25)
            end
        elseif theme == "Blue" or theme == "Cyan" or theme == "Cyber Neon" then
            if key == "wingL" or key == "wingR" or key == "sword" or key == "halo" or key == "head" then
                return Color3.fromRGB(0, 240, 255)
            elseif key == "hat" then
                return Color3.fromRGB(20, 20, 25)
            elseif key:find("arm") or key:find("leg") then
                return Color3.fromRGB(0, 210, 255)
            else
                return Color3.fromRGB(15, 18, 26)
            end
        elseif theme == "Green" or theme == "Toxic Venom" then
            if key == "wingL" or key == "wingR" or key == "sword" or key == "halo" or key == "head" then
                return Color3.fromRGB(40, 250, 70)
            elseif key == "hat" then
                return Color3.fromRGB(20, 25, 20)
            elseif key:find("arm") or key:find("leg") then
                return Color3.fromRGB(40, 230, 65)
            else
                return Color3.fromRGB(15, 28, 18)
            end
        elseif theme == "Purple" or theme == "Amethyst Void" then
            if key == "wingL" or key == "wingR" or key == "sword" or key == "halo" or key == "head" then
                return Color3.fromRGB(165, 40, 255)
            elseif key == "hat" then
                return Color3.fromRGB(115, 25, 180)
            elseif key:find("arm") or key:find("leg") then
                return Color3.fromRGB(150, 35, 235)
            else
                return Color3.fromRGB(22, 14, 30)
            end
        elseif theme == "Orange" or theme == "Inferno Flame" then
            if key == "wingL" or key == "wingR" or key == "sword" or key == "halo" or key == "head" then
                return Color3.fromRGB(255, 95, 0)
            elseif key == "hat" then
                return Color3.fromRGB(255, 140, 10)
            elseif key:find("arm") or key:find("leg") then
                return Color3.fromRGB(240, 85, 0)
            else
                return Color3.fromRGB(28, 18, 16)
            end
        end
        return Color3.fromRGB(15, 15, 15)
    end

    local rainbowRunning = false
    local function applyStickmanColors()
        if stickmanColorTheme == "Rainbow" then
            if not rainbowRunning then
                rainbowRunning = true
                task.spawn(function()
                    while stickmanActive and stickmanColorTheme == "Rainbow" do
                        local idx = 0
                        for key, w in pairs(stickmanWorkers) do
                            idx = idx + 1
                            if w then
                                if not w.cpc and w.prop then
                                    w.cpc = w.prop:FindFirstChild("ChangePropColor")
                                end
                                if w.cpc then
                                    local col = Color3.fromHSV((tick() * 0.4 + idx * 0.08) % 1, 1, 1)
                                    pcall(function() w.cpc:InvokeServer(col) end)
                                end
                            end
                        end
                        task.wait(0.1)
                    end
                    rainbowRunning = false
                end)
            end
        else
            task.spawn(function()
                for key, w in pairs(stickmanWorkers) do
                    if w then
                        if not w.cpc and w.prop then
                            w.cpc = w.prop:FindFirstChild("ChangePropColor")
                        end
                        if w.cpc then
                            local col = getStickmanPartColor(stickmanColorTheme, key, stickmanStyle)
                            pcall(function() w.cpc:InvokeServer(col) end)
                        end
                    end
                end
            end)
        end
    end

    
    -- [LOCKED: WORK PERFECTLY] RIG BLUEPRINT GENERATOR
    
    local function getStickmanBlueprint(size, style, t, walkC, isWalk)
        local breath = math.sin(t * 1.8)
        local chestLift = isWalk and (math.abs(math.cos(walkC * 2)) * 0.3) or (breath * 0.15)
        local shoulderLift = isWalk and (math.abs(math.cos(walkC * 2)) * 0.2) or (breath * 0.22)
        local armBreath = breath * 2.0
        local headBob = isWalk and (math.sin(walkC * 2) * 0.2) or (breath * 0.18)
        local wingFlap = math.sin(t * 1.6) * 6.0

        local legPitchL = isWalk and (math.sin(walkC) * 22.0) or 0
        local legPitchR = isWalk and (-math.sin(walkC) * 22.0) or 0
        local legLiftL = isWalk and (math.max(0, math.sin(walkC)) * 1.2) or 0
        local legLiftR = isWalk and (math.max(0, -math.sin(walkC)) * 1.2) or 0

        local armPitchL = isWalk and (-math.sin(walkC) * 18.0) or 0
        local armPitchR = isWalk and (math.sin(walkC) * 18.0) or 0

        local parts = {}

        if size == "Medium" then
            
            table.insert(parts, {
                key = "legL", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-2.0, -1.25 + legLiftL, 0), rot = Vector3.new(legPitchL, 0, 0)
            })
            table.insert(parts, {
                key = "legR", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(2.0, -1.25 + legLiftR, 0), rot = Vector3.new(legPitchR, 0, 0)
            })
            table.insert(parts, {
                key = "torso", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(0, 7.5 + chestLift, 0), rot = Vector3.new(0, 0, 0)
            })
            table.insert(parts, {
                key = "armL", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-4.2, 7.0 + shoulderLift, 0), rot = Vector3.new(armPitchL, 0, -18.0 - armBreath)
            })
            table.insert(parts, {
                key = "armR", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(4.2, 7.0 + shoulderLift, 0), rot = Vector3.new(armPitchR, 0, 18.0 + armBreath)
            })
            table.insert(parts, {
                key = "head", prop = "FlatCylinder", cat = "Building",
                pos = Vector3.new(0, 22.0 + headBob, -0.4), rot = Vector3.new(90, 0, 0)
            })

            if style == "Samurai" then
                table.insert(parts, {
                    key = "hat", prop = "Pyramid", cat = "Building",
                    pos = Vector3.new(0, 24.8 + headBob, -0.5), rot = Vector3.new(12, 0, 0)
                })
                table.insert(parts, {
                    key = "halo", prop = "FullArch", cat = "Building",
                    pos = Vector3.new(0, 22.0 + headBob, 0.8), rot = Vector3.new(90, 0, 0)
                })
                table.insert(parts, {
                    key = "wingL", prop = "PillarSmall", cat = "Building",
                    pos = Vector3.new(-5.5, 16.5 + shoulderLift, 1.4), rot = Vector3.new(15, 25 + wingFlap, -45)
                })
                table.insert(parts, {
                    key = "wingR", prop = "PillarSmall", cat = "Building",
                    pos = Vector3.new(5.5, 16.5 + shoulderLift, 1.4), rot = Vector3.new(15, -25 - wingFlap, 45)
                })
                table.insert(parts, {
                    key = "sword", prop = "PillarSmall", cat = "Building",
                    pos = Vector3.new(0, 11.0 + chestLift, 0.7), rot = Vector3.new(0, 0, 38)
                })
            end

        elseif size == "Large" then
            
            table.insert(parts, {
                key = "legL", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-2.8, -1.25 + legLiftL, 0), rot = Vector3.new(legPitchL, 0, -4.0)
            })
            table.insert(parts, {
                key = "legR", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(2.8, -1.25 + legLiftR, 0), rot = Vector3.new(legPitchR, 0, 4.0)
            })
            table.insert(parts, {
                key = "torsoLower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(0, 6.5 + chestLift * 0.7, 0), rot = Vector3.new(0, 0, 0)
            })
            table.insert(parts, {
                key = "torsoUpper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(0, 16.0 + chestLift, 0), rot = Vector3.new(0, 0, 0)
            })
            table.insert(parts, {
                key = "armL", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-5.2, 14.5 + shoulderLift, 0), rot = Vector3.new(armPitchL, 0, -20.0 - armBreath)
            })
            table.insert(parts, {
                key = "armR", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(5.2, 14.5 + shoulderLift, 0), rot = Vector3.new(armPitchR, 0, 20.0 + armBreath)
            })
            table.insert(parts, {
                key = "head", prop = "FlatCylinder", cat = "Building",
                pos = Vector3.new(0, 28.0 + headBob, -0.4), rot = Vector3.new(90, 0, 0)
            })

            if style == "Samurai" then
                table.insert(parts, {
                    key = "hat", prop = "Pyramid", cat = "Building",
                    pos = Vector3.new(0, 31.0 + headBob, -0.6), rot = Vector3.new(12, 0, 0)
                })
                table.insert(parts, {
                    key = "halo", prop = "FullArch", cat = "Building",
                    pos = Vector3.new(0, 28.0 + headBob, 0.9), rot = Vector3.new(90, 0, 0)
                })
                table.insert(parts, {
                    key = "wingL", prop = "PillarSmall", cat = "Building",
                    pos = Vector3.new(-6.8, 22.0 + shoulderLift, 1.6), rot = Vector3.new(15, 25 + wingFlap, -48)
                })
                table.insert(parts, {
                    key = "wingR", prop = "PillarSmall", cat = "Building",
                    pos = Vector3.new(6.8, 22.0 + shoulderLift, 1.6), rot = Vector3.new(15, -25 - wingFlap, 48)
                })
                table.insert(parts, {
                    key = "sword", prop = "PillarSmall", cat = "Building",
                    pos = Vector3.new(0, 16.5 + chestLift, 0.8), rot = Vector3.new(0, 0, 38)
                })
            end

        elseif size == "Giant" then
            
            table.insert(parts, {
                key = "legL_Lower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-4.2, -1.25 + legLiftL, 0), rot = Vector3.new(legPitchL, 0, -6.0)
            })
            table.insert(parts, {
                key = "legL_Upper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-3.0, 8.5 + legLiftL * 0.5, 0), rot = Vector3.new(legPitchL * 0.7, 0, -4.0)
            })
            table.insert(parts, {
                key = "legR_Lower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(4.2, -1.25 + legLiftR, 0), rot = Vector3.new(legPitchR, 0, 6.0)
            })
            table.insert(parts, {
                key = "legR_Upper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(3.0, 8.5 + legLiftR * 0.5, 0), rot = Vector3.new(legPitchR * 0.7, 0, 4.0)
            })
            table.insert(parts, {
                key = "torsoLower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(0, 15.0 + chestLift * 0.7, 0), rot = Vector3.new(0, 0, 0)
            })
            table.insert(parts, {
                key = "torsoUpper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(0, 25.0 + chestLift, 0), rot = Vector3.new(0, 0, 0)
            })
            table.insert(parts, {
                key = "armL_Upper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-6.2, 23.5 + shoulderLift, 0), rot = Vector3.new(armPitchL, 0, -18.0 - armBreath)
            })
            table.insert(parts, {
                key = "armL_Lower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-8.2, 14.5 + shoulderLift, 0), rot = Vector3.new(armPitchL * 1.1, 0, -22.0 - armBreath)
            })
            table.insert(parts, {
                key = "armR_Upper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(6.2, 23.5 + shoulderLift, 0), rot = Vector3.new(armPitchR, 0, 18.0 + armBreath)
            })
            table.insert(parts, {
                key = "armR_Lower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(8.2, 14.5 + shoulderLift, 0), rot = Vector3.new(armPitchR * 1.1, 0, 22.0 + armBreath)
            })
            table.insert(parts, {
                key = "head", prop = "FlatCylinder", cat = "Building",
                pos = Vector3.new(0, 37.0 + headBob, -0.4), rot = Vector3.new(90, 0, 0)
            })

            if style == "Samurai" then
                table.insert(parts, {
                    key = "hat", prop = "Pyramid", cat = "Building",
                    pos = Vector3.new(0, 40.2 + headBob, -0.6), rot = Vector3.new(12, 0, 0)
                })
                table.insert(parts, {
                    key = "halo", prop = "FullArch", cat = "Building",
                    pos = Vector3.new(0, 37.0 + headBob, 1.2), rot = Vector3.new(90, 0, 0)
                })
                table.insert(parts, {
                    key = "wingL", prop = "PillarSmall", cat = "Building",
                    pos = Vector3.new(-8.5, 30.5 + shoulderLift, 2.0), rot = Vector3.new(16, 26 + wingFlap, -46)
                })
                table.insert(parts, {
                    key = "wingR", prop = "PillarSmall", cat = "Building",
                    pos = Vector3.new(8.5, 30.5 + shoulderLift, 2.0), rot = Vector3.new(16, -26 - wingFlap, 46)
                })
            end

        elseif size == "Ultra Giant" then
            -- [LOCKED: WORK PERFECTLY] 15-Prop Monolithic Pure Stickman (Zero gap, 3 articulated segments per limb)
            
            table.insert(parts, {
                key = "legL_Lower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-4.5, -1.25 + legLiftL, 0), rot = Vector3.new(legPitchL * 1.0, 0, -3.0)
            })
            table.insert(parts, {
                key = "legL_Mid", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-3.8, 8.0 + legLiftL * 0.7, 0), rot = Vector3.new(legPitchL * 0.85, 0, -2.0)
            })
            table.insert(parts, {
                key = "legL_Upper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-3.0, 17.25 + legLiftL * 0.4, 0), rot = Vector3.new(legPitchL * 0.7, 0, -1.5)
            })

            
            table.insert(parts, {
                key = "legR_Lower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(4.5, -1.25 + legLiftR, 0), rot = Vector3.new(legPitchR * 1.0, 0, 3.0)
            })
            table.insert(parts, {
                key = "legR_Mid", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(3.8, 8.0 + legLiftR * 0.7, 0), rot = Vector3.new(legPitchR * 0.85, 0, 2.0)
            })
            table.insert(parts, {
                key = "legR_Upper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(3.0, 17.25 + legLiftR * 0.4, 0), rot = Vector3.new(legPitchR * 0.7, 0, 1.5)
            })

            
            table.insert(parts, {
                key = "torsoLower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(0, 25.0 + chestLift * 0.7, 0), rot = Vector3.new(0, 0, 0)
            })
            table.insert(parts, {
                key = "torsoUpper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(0, 34.0 + chestLift, 0), rot = Vector3.new(0, 0, 0)
            })

            
            table.insert(parts, {
                key = "armL_Upper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-7.0, 32.5 + shoulderLift, 0), rot = Vector3.new(armPitchL * 0.8, 0, -18.0 - armBreath)
            })
            table.insert(parts, {
                key = "armL_Mid", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-9.8, 23.5 + shoulderLift, 0), rot = Vector3.new(armPitchL * 1.0, 0, -20.0 - armBreath)
            })
            table.insert(parts, {
                key = "armL_Lower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(-12.6, 14.5 + shoulderLift, 0), rot = Vector3.new(armPitchL * 1.2, 0, -22.0 - armBreath)
            })

            
            table.insert(parts, {
                key = "armR_Upper", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(7.0, 32.5 + shoulderLift, 0), rot = Vector3.new(armPitchR * 0.8, 0, 18.0 + armBreath)
            })
            table.insert(parts, {
                key = "armR_Mid", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(9.8, 23.5 + shoulderLift, 0), rot = Vector3.new(armPitchR * 1.0, 0, 20.0 + armBreath)
            })
            table.insert(parts, {
                key = "armR_Lower", prop = "PillarSmall", cat = "Building",
                pos = Vector3.new(12.6, 14.5 + shoulderLift, 0), rot = Vector3.new(armPitchR * 1.2, 0, 22.0 + armBreath)
            })

            
            table.insert(parts, {
                key = "head", prop = "FlatCylinder", cat = "Building",
                pos = Vector3.new(0, 48.5 + headBob, -0.4), rot = Vector3.new(90, 0, 0)
            })
        end

        return parts
    end

    local function computeStickmanPartCFrames(blueprint, centerPos, yaw)
        local rootPos = centerPos + Vector3.new(0, groundClearance, 0)
        local baseCF = CFrame.new(rootPos) * CFrame.Angles(0, yaw, 0)

        local cfs = {}
        for _, part in ipairs(blueprint) do
            local offsetCF = CFrame.new(part.pos) * CFrame.Angles(
                math.rad(part.rot.X),
                math.rad(part.rot.Y),
                math.rad(part.rot.Z)
            )
            cfs[part.key] = baseCF * offsetCF
        end
        return cfs
    end

    
    
    
    local function startStickmanGravityAndAnimationLoop()
        task.spawn(function()
            while stickmanActive do
                animTime = animTime + 0.04
                local char = lp.Character
                local hrp = char and char:FindFirstChild("HumanoidRootPart")
                local isWalk = false

                
                if stickmanFollow and hrp then
                    local fwdDist = (stickmanSize == "Ultra Giant") and 30 or ((stickmanSize == "Giant") and 24 or ((stickmanSize == "Large") and 18 or 14))
                    local targetPos = hrp.Position + (hrp.CFrame.LookVector * fwdDist)
                    stickmanAngle = math.atan2(hrp.CFrame.LookVector.X, hrp.CFrame.LookVector.Z)
                    local moveDir = (targetPos - stickmanAnchor)
                    local dist = Vector2.new(moveDir.X, moveDir.Z).Magnitude
                    if dist > 2.0 then
                        isWalk = true
                        walkCycle = walkCycle + 0.18
                        local moveStep = moveDir.Unit * math.min(dist, 16 * 0.05)
                        stickmanAnchor = stickmanAnchor + Vector3.new(moveStep.X, 0, moveStep.Z)
                    end
                end

                
                local ignoreList = {char}
                local wc = workspace:FindFirstChild("WorkspaceCom")
                if wc then table.insert(ignoreList, wc) end

                local rayParams = RaycastParams.new()
                rayParams.FilterDescendantsInstances = ignoreList
                rayParams.FilterType = Enum.RaycastFilterType.Exclude

                local rayOrigin = Vector3.new(stickmanAnchor.X, stickmanAnchor.Y + 35, stickmanAnchor.Z)
                local groundRay = workspace:Raycast(rayOrigin, Vector3.new(0, -80, 0), rayParams)
                if groundRay then
                    local targetGroundY = groundRay.Position.Y
                    stickmanAnchor = Vector3.new(stickmanAnchor.X, targetGroundY, stickmanAnchor.Z)
                else
                    stickmanAnchor = Vector3.new(stickmanAnchor.X, stickmanAnchor.Y - 1.5, stickmanAnchor.Z)
                end

                
                local bp = getStickmanBlueprint(stickmanSize, stickmanStyle, animTime, walkCycle, isWalk)
                local cfs = computeStickmanPartCFrames(bp, stickmanAnchor, stickmanAngle)
                for _, part in ipairs(bp) do
                    local w = stickmanWorkers[part.key]
                    if w and cfs[part.key] then
                        w.targetCF = cfs[part.key]
                    end
                end

                task.wait(0.04)
            end
        end)
    end

    local function startStickmanWorkerSync(w)
        task.spawn(function()
            while stickmanActive and w.scf do
                if w.targetCF then
                    local cf = w.targetCF
                    local send = false
                    if not w.lastSentCF then
                        send = true
                    else
                        local pDiff = (cf.Position - w.lastSentCF.Position).Magnitude
                        if pDiff > 0.03 then send = true end
                    end
                    if send then
                        w.lastSentCF = cf
                        pcall(function() w.scf:InvokeServer(cf) end)
                    end
                end
                task.wait(0.05)
            end
        end)
    end

    
    
    
    local function spawnStickmanTitan()
        stickmanSpawnThread = coroutine.running()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")

        local modeLabel = (stickmanSize == "Ultra Giant") and "Ultra Giant Titan (15 Props Pure)" or (stickmanSize .. " " .. stickmanStyle)
        sNotif("Stickman Titan", "Summoning " .. modeLabel .. " (" .. stickmanColorTheme .. ")...", 4)

        local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")
        local toolRE = RE:WaitForChild("1Too1l")
        local myPropName = "Prop" .. lp.Name

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then
            sNotif("Stickman Titan", "PropMaker tool not found!", 3)
            return
        end

        local humanoid = char:FindFirstChildOfClass("Humanoid")
        if humanoid then humanoid:EquipTool(propsTool) end
        task.wait(0.4)

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake") or propsTool:WaitForChild("Tool_PropMake", 3)
        if not toolPropMake then return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local ignoreList = {char}
        local wc = workspace:FindFirstChild("WorkspaceCom")
        if wc then table.insert(ignoreList, wc) end

        local fwdDist = (stickmanSize == "Ultra Giant") and 30 or ((stickmanSize == "Giant") and 24 or ((stickmanSize == "Large") and 18 or 14))
        local forwardPos = hrp.Position + (hrp.CFrame.LookVector * fwdDist)
        local rayParams = RaycastParams.new()
        rayParams.FilterDescendantsInstances = ignoreList
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        local gRay = workspace:Raycast(forwardPos + Vector3.new(0, 35, 0), Vector3.new(0, -80, 0), rayParams)
        local groundY = gRay and gRay.Position.Y or (hrp.Position.Y - 2.5)
        local hitInst = gRay and gRay.Instance or workspace.Terrain
        local groundHitPos = gRay and gRay.Position or Vector3.new(forwardPos.X, groundY, forwardPos.Z)

        stickmanAnchor = Vector3.new(forwardPos.X, groundY, forwardPos.Z)
        stickmanAngle = math.atan2(hrp.CFrame.LookVector.X, hrp.CFrame.LookVector.Z)
        stickmanWorkers = {}

        local stagingPos = stickmanAnchor + Vector3.new(0, 35, 0)

        local knownScf = {}
        if wc then
            for _, folder in pairs(wc:GetChildren()) do
                for _, prop in pairs(folder:GetChildren()) do
                    if prop.Name == myPropName then
                        local scf = prop:FindFirstChild("SetCurrentCFrame")
                        if scf then knownScf[scf] = true end
                    end
                end
            end
        end

        local Telemetry = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction")
        local blueprint = getStickmanBlueprint(stickmanSize, stickmanStyle, 0, 0, false)

        
        for i, part in ipairs(blueprint) do
            if not stickmanActive then break end

            pcall(function() Telemetry:FireServer("filterClick", {name=part.cat, itemType="Props"}) end)
            task.wait(0.05)
            pcall(function() Telemetry:FireServer("filterClick", {name=part.prop, itemType="Props"}) end)
            task.wait(0.05)
            pcall(function() clearTools:FireServer("RequestingPropName", part.prop, part.cat) end)
            task.wait(0.20)

            pcall(function() toolPropMake:FireServer(hitInst, groundHitPos) end)
            task.wait(0.85) 

            local foundProp, foundScf, foundCpc = nil, nil, nil
            if wc then
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        if prop.Name == myPropName then
                            local scf = prop:FindFirstChild("SetCurrentCFrame")
                            local cpc = prop:FindFirstChild("ChangePropColor")
                            if scf and not knownScf[scf] then
                                foundProp = prop
                                foundScf = scf
                                foundCpc = cpc
                                knownScf[scf] = true
                                break
                            end
                        end
                    end
                    if foundScf then break end
                end
            end

            if foundProp then
                for _, d in ipairs(foundProp:GetDescendants()) do
                    if d:IsA("Light") then
                        d.Enabled = false
                        d.Brightness = 0
                        pcall(function() d:Destroy() end)
                    end
                end
            end

            local w = {
                key = part.key,
                prop = foundProp,
                scf = foundScf,
                cpc = foundCpc,
                targetCF = nil,
                lastSentCF = nil
            }
            stickmanWorkers[part.key] = w

            if foundScf then
                local floatCF = CFrame.new(stagingPos + Vector3.new(0, i * 2, 0))
                pcall(function() foundScf:InvokeServer(floatCF) end)
            end
            if foundCpc then
                local partColor = getStickmanPartColor(stickmanColorTheme, part.key, stickmanStyle, i)
                pcall(function() foundCpc:InvokeServer(partColor) end)
            end

            startStickmanWorkerSync(w)
        end

        
        task.wait(1)
        pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)
        task.wait(0.3)

        
        local initialCFs = computeStickmanPartCFrames(blueprint, stickmanAnchor, stickmanAngle)
        for _, part in ipairs(blueprint) do
            local w = stickmanWorkers[part.key]
            if w then
                if initialCFs[part.key] then
                    w.targetCF = initialCFs[part.key]
                    if w.scf then
                        pcall(function() w.scf:InvokeServer(initialCFs[part.key]) end)
                    end
                end
            end
        end
        applyStickmanColors()

        task.wait(0.2)
        
        startStickmanGravityAndAnimationLoop()
        sNotif("Stickman Titan", modeLabel .. " online!", 4)
    end

    local function stopStickmanTitan()
        stickmanActive = false
        if stickmanSpawnThread then
            pcall(function() task.cancel(stickmanSpawnThread) end)
            stickmanSpawnThread = nil
        end
        stickmanWorkers = {}
        pcall(function()
            local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            if RE then
                local ct = RE:FindFirstChild("1Clea1rTool1s")
                if ct then
                    ct:FireServer("ClearAllProps")
                    task.wait(0.05)
                    ct:FireServer("ClearAllTools")
                    ct:FireServer("ClosePropMenu")
                end
            end
        end)
        pcall(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum then hum:UnequipTools() end
            cleanupPropMakerTool()
        end)
        sNotif("Stickman Titan", "Stickman Titan dismantled.", 2)
    end

    
    
    
    BuildV2Tab:AddSection({"Stickman Titan"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Stickman",
        Default = false,
        Callback = function(v)
            if not _G.BTR_UI_READY then return end
            if v then
                stickmanActive = true
                task.spawn(spawnStickmanTitan)
            else
                stopStickmanTitan()
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Size",
        Default = "Medium",
        Options = {"Medium", "Large", "Giant", "Ultra Giant"},
        Callback = function(choice)
            stickmanSize = choice
            if stickmanActive then
                task.spawn(spawnStickmanTitan)
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Style",
        Default = "Samurai",
        Options = {"Original", "Samurai"},
        Callback = function(choice)
            stickmanStyle = choice
            if stickmanActive and stickmanSize ~= "Ultra Giant" then
                task.spawn(spawnStickmanTitan)
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Color Theme",
        Default = "Black",
        Options = {"Black", "Rainbow", "Samurai", "Red", "Blue", "Green", "Purple", "Orange", "Yellow", "White"},
        Callback = function(choice)
            stickmanColorTheme = choice
            if stickmanActive then
                applyStickmanColors()
            end
        end
    })

    BuildV2Tab:AddToggle({
        Name = "Follow Player",
        Default = false,
        Callback = function(v)
            stickmanFollow = v
        end
    })

    BuildV2Tab:AddButton({
        Name = "Teleport In Front",
        Callback = function()
            local char = lp.Character or lp.CharacterAdded:Wait()
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp and stickmanActive then
                local ignoreList = {char}
                local wc = workspace:FindFirstChild("WorkspaceCom")
                if wc then table.insert(ignoreList, wc) end

                local fwdDist = (stickmanSize == "Ultra Giant") and 30 or ((stickmanSize == "Giant") and 24 or ((stickmanSize == "Large") and 18 or 14))
                local forwardPos = hrp.Position + (hrp.CFrame.LookVector * fwdDist)
                local rayParams = RaycastParams.new()
                rayParams.FilterDescendantsInstances = ignoreList
                rayParams.FilterType = Enum.RaycastFilterType.Exclude

                local gRay = workspace:Raycast(forwardPos + Vector3.new(0, 35, 0), Vector3.new(0, -80, 0), rayParams)
                local groundY = gRay and gRay.Position.Y or (hrp.Position.Y - 2.5)

                stickmanAnchor = Vector3.new(forwardPos.X, groundY, forwardPos.Z)
                stickmanAngle = math.atan2(hrp.CFrame.LookVector.X, hrp.CFrame.LookVector.Z)
                sNotif("Stickman Titan", "Repositioned in front of you!", 2)
            end
        end
    })
end

do
    local lp = game:GetService("Players").LocalPlayer
    local catActive = false
    local catAnchor = Vector3.new(0, 0, 0)
    local catAngle = 0
    local catWorkers = {}
    local catAnimTime = 0
    local catSleepAlpha = 0
    local catTargetSleepAlpha = 0
    local catIsSleeping = false
    local catFollowActive = false
    local catMoveAlpha = 0
    local catRunStride = 0
    local catSeat = nil
    local catLoop = nil

    local CAT_BREEDS = {
        ["Bombay (Black)"]        = Color3.fromRGB(25, 25, 28),
        ["Ginger Tabby (Orange)"] = Color3.fromRGB(225, 120, 38),
        ["Snow Angora (White)"]   = Color3.fromRGB(248, 248, 252),
        ["Russian Blue (Grey)"]   = Color3.fromRGB(115, 125, 138),
        ["Siamese (Cream)"]       = Color3.fromRGB(215, 192, 158),
        ["Caramel (Golden)"]      = Color3.fromRGB(195, 142, 78)
    }

    local currentBreed = "Bombay (Black)"
    local currentCatColor = CAT_BREEDS[currentBreed]

    local CAT_BASE_BLUEPRINT = {
        { key = "body",   prop = "Block2",           cat = "Building" },
        { key = "head",   prop = "Block1",           cat = "Building" },
        { key = "ear_L",  prop = "CornerWedgeSmall", cat = "Building" },
        { key = "ear_R",  prop = "CornerWedgeSmall", cat = "Building" },
        { key = "leg_FL", prop = "TubeLamp",          cat = "Lights" },
        { key = "leg_FR", prop = "TubeLamp",          cat = "Lights" },
        { key = "leg_RL", prop = "TubeLamp",          cat = "Lights" },
        { key = "leg_RR", prop = "TubeLamp",          cat = "Lights" },
        { key = "tail",   prop = "TubeLamp",          cat = "Lights" },
        { key = "saddle", prop = "FloorCushion",     cat = "Furniture" }
    }

    local function cNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function getOrFetchPropMaker()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local pm = char:FindFirstChild("PropMaker") or lp.Backpack:FindFirstChild("PropMaker")
        if not pm then
            pcall(function()
                game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker")
            end)
            task.wait(0.4)
            pm = char:FindFirstChild("PropMaker") or lp.Backpack:FindFirstChild("PropMaker")
        end
        return pm
    end

    local function lerp(a, b, t)
        return a + (b - a) * t
    end

    local function lerpVec3(v1, v2, t)
        return Vector3.new(lerp(v1.X, v2.X, t), lerp(v1.Y, v2.Y, t), lerp(v1.Z, v2.Z, t))
    end

    local function computeDynamicPartPose(key, t, sAlpha, mAlpha, stridePhase)
        sAlpha = sAlpha or 0
        mAlpha = mAlpha or 0
        stridePhase = stridePhase or 0

        local smoothS = sAlpha * sAlpha * (3 - 2 * sAlpha)

        
        local breathSpeed = lerp(1.6, 0.85, smoothS)
        local breathAmp = lerp(0.045, 0.065, smoothS)
        local breathY = math.sin(t * breathSpeed) * breathAmp
        local breathPitch = math.sin(t * breathSpeed + 0.3) * lerp(0.8, 0.4, smoothS)

        
        local gazeYaw = math.sin(t * 0.45) * 5.0 * (1 - smoothS) * (1 - mAlpha * 0.7)
        local gazeRoll = math.cos(t * 0.35) * 2.2 * (1 - smoothS) * (1 - mAlpha * 0.7)
        local sniffPitch = math.sin(t * 1.8) * 1.2 * (1 - smoothS) * (1 - mAlpha * 0.7)

        
        local tailBaseSway = math.sin(t * 1.4) * 16.0
        local tailTipFlick = math.sin(t * 2.8 + 1.2) * 8.0
        local tailDynamicSway = (tailBaseSway + tailTipFlick) * (1 - smoothS * 0.85)
        local tailPitchWave = math.sin(t * 1.4) * 3.5 * (1 - smoothS)

        
        local runBounce = math.sin(stridePhase * 2) * 0.035 * mAlpha

        
        if key == "body" then
            local pStand = Vector3.new(0, 2.45 + breathY + runBounce, 0)
            local pSleep = Vector3.new(0, 1.25 + breathY, 0)
            local bodyY = lerp(pStand.Y, pSleep.Y, smoothS)
            local bodyPitch = lerp(breathPitch, 0, smoothS)
            return Vector3.new(0, bodyY, 0), Vector3.new(bodyPitch, 90, lerp(0, -3.5, smoothS))

        
        elseif key == "head" then
            local pStand = Vector3.new(0, 3.05 + breathY * 0.6 + runBounce * 0.7, 4.2)
            local pSleep = Vector3.new(-0.15, 1.55 + breathY * 0.6, 4.10)
            local pos = lerpVec3(pStand, pSleep, smoothS)
            local rotX = lerp(sniffPitch, 4.0, smoothS)
            local rotY = lerp(gazeYaw, -8.0, smoothS)
            local rotZ = lerp(gazeRoll, -14.0, smoothS)
            return pos, Vector3.new(rotX, rotY, rotZ)

        
        elseif key == "leg_FL" then
            local legZ = math.sin(stridePhase) * 0.35 * mAlpha
            local legLift = math.max(0, -math.cos(stridePhase)) * 0.16 * mAlpha
            local legRotX = math.sin(stridePhase) * 12 * mAlpha

            local pStand = Vector3.new(-0.82, -1.60 + legLift, 2.35 + legZ)
            local pSleep = Vector3.new(-0.82, -3.40, 2.20)
            local pos = lerpVec3(pStand, pSleep, smoothS)
            return pos, Vector3.new(legRotX * (1 - smoothS), 0, 0)

        
        elseif key == "leg_FR" then
            local legZ = math.sin(stridePhase + math.pi) * 0.35 * mAlpha
            local legLift = math.max(0, -math.cos(stridePhase + math.pi)) * 0.16 * mAlpha
            local legRotX = math.sin(stridePhase + math.pi) * 12 * mAlpha

            local pStand = Vector3.new(0.82, -1.60 + legLift, 2.35 + legZ)
            local pSleep = Vector3.new(0.82, -3.40, 2.20)
            local pos = lerpVec3(pStand, pSleep, smoothS)
            return pos, Vector3.new(legRotX * (1 - smoothS), 0, 0)

        
        elseif key == "leg_RL" then
            local legZ = math.sin(stridePhase + math.pi) * 0.35 * mAlpha
            local legLift = math.max(0, -math.cos(stridePhase + math.pi)) * 0.16 * mAlpha
            local legRotX = math.sin(stridePhase + math.pi) * 12 * mAlpha

            local pStand = Vector3.new(-0.82, -1.60 + legLift, -2.35 + legZ)
            local pSleep = Vector3.new(-0.82, -3.40, -2.15)
            local pos = lerpVec3(pStand, pSleep, smoothS)
            return pos, Vector3.new(legRotX * (1 - smoothS), 0, 0)

        
        elseif key == "leg_RR" then
            local legZ = math.sin(stridePhase) * 0.35 * mAlpha
            local legLift = math.max(0, -math.cos(stridePhase)) * 0.16 * mAlpha
            local legRotX = math.sin(stridePhase) * 12 * mAlpha

            local pStand = Vector3.new(0.82, -1.60 + legLift, -2.35 + legZ)
            local pSleep = Vector3.new(0.82, -3.40, -2.15)
            local pos = lerpVec3(pStand, pSleep, smoothS)
            return pos, Vector3.new(legRotX * (1 - smoothS), 0, 0)

        
        elseif key == "tail" then
            local pStand = Vector3.new(0, 2.10 + breathY * 0.7 + runBounce * 0.5, -3.80)
            local pSleep = Vector3.new(0.35, 0.35 + breathY * 0.4, -3.75)
            local pos = lerpVec3(pStand, pSleep, smoothS)
            local rotX = lerp(-52 + tailPitchWave, -78, smoothS)
            local rotY = lerp(tailDynamicSway + (math.sin(stridePhase) * 8 * mAlpha), 35 + math.sin(t * 0.9) * 2.5, smoothS)
            return pos, Vector3.new(rotX, rotY, 0)

        
        elseif key == "saddle" then
            local pStand = Vector3.new(0, 4.30 + breathY + runBounce, -0.40)
            local pSleep = Vector3.new(0, 2.90 + breathY, -0.40)
            local pos = lerpVec3(pStand, pSleep, smoothS)
            return pos, Vector3.new(0, 180, 0)
        end

        return Vector3.new(0, 0, 0), Vector3.new(0, 0, 0)
    end

    local function computeDynamicCFrame(key, anchor, yaw, t, sAlpha, mAlpha, stridePhase)
        local baseCF = CFrame.new(anchor) * CFrame.Angles(0, yaw, 0)

        
        if key == "ear_L" or key == "ear_R" then
            local headPos, headRot = computeDynamicPartPose("head", t, sAlpha, mAlpha, stridePhase)
            local headCF = baseCF * CFrame.new(headPos) * CFrame.Angles(
                math.rad(headRot.X),
                math.rad(headRot.Y),
                math.rad(headRot.Z)
            )

            local twitchWaveL = math.clamp(math.sin(t * 0.38) - 0.88, 0, 1) * 9.0 * math.sin(t * 22) * (1 - (sAlpha or 0) * 0.8)
            local twitchWaveR = math.clamp(math.cos(t * 0.42) - 0.88, 0, 1) * 9.0 * math.sin(t * 24) * (1 - (sAlpha or 0) * 0.8)

            if key == "ear_L" then
                
                local localOffset = CFrame.new(0.14, 0.63, 0)
                local localRot = CFrame.Angles(0, math.rad(-90), 0) * CFrame.Angles(math.rad(twitchWaveL), 0, 0)
                return headCF * localOffset * localRot
            else
                local localOffset = CFrame.new(-0.04, 0.63, 0)
                local localRot = CFrame.Angles(0, 0, 0) * CFrame.Angles(math.rad(twitchWaveR), 0, 0)
                return headCF * localOffset * localRot
            end
        end

        local pos, rot = computeDynamicPartPose(key, t, sAlpha, mAlpha, stridePhase)
        local offsetCF = CFrame.new(pos) * CFrame.Angles(
            math.rad(rot.X),
            math.rad(rot.Y),
            math.rad(rot.Z)
        )
        return baseCF * offsetCF
    end

    local function startCatWorker(w)
        task.spawn(function()
            while catActive and w.scf do
                local now = tick()
                if w.targetCF then
                    local cf = w.targetCF
                    local send = false
                    if not w.lastSentCF then
                        send = true
                    else
                        local pDiff = (cf.Position - w.lastSentCF.Position).Magnitude
                        if pDiff > 0.02 or (now - w.lastSendTimeCF > 0.08) then
                            send = true
                        end
                    end
                    if send then
                        w.lastSentCF = cf
                        w.lastSendTimeCF = now
                        pcall(function() w.scf:InvokeServer(cf) end)
                        if (now - (w.lastNeonCheck or 0) > 0.8) and w.prop then
                            w.lastNeonCheck = now
                            for _, d in ipairs(w.prop:GetDescendants()) do
                                if d:IsA("BasePart") and (d.Material == Enum.Material.Neon or d.Color ~= currentCatColor) then
                                    pcall(function()
                                        d.Material = Enum.Material.SmoothPlastic
                                        if currentCatColor then d.Color = currentCatColor end
                                    end)
                                elseif d:IsA("Light") and d.Enabled then
                                    d.Enabled = false
                                    d.Brightness = 0
                                end
                            end
                        end
                    end
                end
                task.wait(0.04)
            end
        end)
    end

    local function updateCatColor(newColor)
        currentCatColor = newColor
        for _, w in pairs(catWorkers) do
            if w.cpc then
                pcall(function() w.cpc:InvokeServer(newColor) end)
            end
            if w.prop then
                for _, d in ipairs(w.prop:GetDescendants()) do
                    if d:IsA("BasePart") then
                        pcall(function()
                            d.Material = Enum.Material.SmoothPlastic
                            d.Color = newColor
                        end)
                    end
                end
            end
        end
    end

    local function spawnCatCompanion()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")

        local RE = game:GetService("ReplicatedStorage"):WaitForChild("RE")
        local clearTools = RE:WaitForChild("1Clea1rTool1s")
        local toolRE = RE:WaitForChild("1Too1l")
        local Telemetry = game:GetService("ReplicatedStorage"):WaitForChild("Remotes"):WaitForChild("TelemetryClientInteraction")

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local pm = getOrFetchPropMaker()
        if not pm then
            cNotif("Cat Companion", "PropMaker not found!", 3)
            return
        end

        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum:EquipTool(pm) end
        task.wait(0.4)

        local toolPropMake = pm:FindFirstChild("Tool_PropMake") or pm:WaitForChild("Tool_PropMake", 3)
        if not toolPropMake then return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local fwd = hrp.CFrame.LookVector
        local forwardPos = hrp.Position + (fwd * 12) + Vector3.new(0, 10, 0)
        local rayParams = RaycastParams.new()
        rayParams.FilterDescendantsInstances = {char}
        rayParams.FilterType = Enum.RaycastFilterType.Exclude
        local groundRay = workspace:Raycast(forwardPos, Vector3.new(0, -40, 0), rayParams)
        local groundY = groundRay and groundRay.Position.Y or (hrp.Position.Y - 2.5)

        catAnchor = Vector3.new(forwardPos.X, groundY, forwardPos.Z)
        catAngle = math.atan2(fwd.X, fwd.Z)
        catWorkers = {}
        catSleepAlpha = 0
        catTargetSleepAlpha = 0
        catIsSleeping = false

        local myPropName = "Prop" .. lp.Name
        local knownScf = {}
        for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                    if scf then knownScf[scf] = true end
                end
            end
        end

        local hitInst = workspace.Terrain

        for i, part in ipairs(CAT_BASE_BLUEPRINT) do
            if not catActive then break end

            local initialCF = computeDynamicCFrame(part.key, catAnchor, catAngle, 0, 0, 0, 0)

            pcall(function() Telemetry:FireServer("filterClick", {name = part.cat, itemType = "Props"}) end)
            task.wait(0.04)
            pcall(function() Telemetry:FireServer("filterClick", {name = part.prop, itemType = "Props"}) end)
            task.wait(0.04)
            pcall(function()
                if part.prop == "FloorCushion" then
                    clearTools:FireServer("RequestingPropFeatureName", part.prop, part.cat)
                else
                    clearTools:FireServer("RequestingPropName", part.prop, part.cat)
                end
            end)
            task.wait(0.18)

            pcall(function() toolPropMake:FireServer(hitInst, catAnchor) end)
            task.wait(0.65)

            local foundProp, foundScf, foundCpc = nil, nil, nil
            for _, folder in pairs(workspace:WaitForChild("WorkspaceCom"):GetChildren()) do
                for _, prop in pairs(folder:GetChildren()) do
                    if prop.Name == myPropName then
                        local scf = prop:FindFirstChild("SetCurrentCFrame")
                        local cpc = prop:FindFirstChild("ChangePropColor")
                        if scf and not knownScf[scf] then
                            foundProp = prop
                            foundScf = scf
                            foundCpc = cpc
                            knownScf[scf] = true
                            break
                        end
                    end
                end
                if foundScf then break end
            end

            if foundProp then
                for _, d in ipairs(foundProp:GetDescendants()) do
                    if d:IsA("Seat") or d:IsA("VehicleSeat") then
                        pcall(function() d.CanCollide = false end)
                        if part.key == "saddle" then
                            catSeat = d
                        end
                    elseif d:IsA("Light") then
                        d.Enabled = false
                        d.Brightness = 0
                        pcall(function() d:Destroy() end)
                    elseif d:IsA("BasePart") then
                        pcall(function()
                            d.Material = Enum.Material.SmoothPlastic
                            if part.key == "saddle" then
                                d.Color = Color3.fromRGB(42, 34, 30)
                                d.CanCollide = false
                            elseif currentCatColor then
                                d.Color = currentCatColor
                            end
                        end)
                    end
                end
            end

            local w = {
                key = part.key,
                prop = foundProp,
                scf = foundScf,
                cpc = foundCpc,
                targetCF = initialCF,
                lastSentCF = nil,
                lastSendTimeCF = 0
            }
            catWorkers[part.key] = w

            if foundScf then
                pcall(function() foundScf:InvokeServer(initialCF) end)
            end
            if foundCpc then
                pcall(function() foundCpc:InvokeServer(currentCatColor) end)
            end

            startCatWorker(w)
        end

        task.wait(1.0)
        pcall(function()
            toolRE:InvokeServer("PickingTools", "PropMaker")
        end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        cNotif("Cat Companion", "Feline Companion spawned!", 3)

        if catLoop then catLoop:Disconnect() catLoop = nil end
        catLoop = game:GetService("RunService").Heartbeat:Connect(function(dt)
            if not catActive then return end
            catAnimTime = catAnimTime + dt

            if catSleepAlpha < catTargetSleepAlpha then
                catSleepAlpha = math.min(catTargetSleepAlpha, catSleepAlpha + dt * 1.5)
            elseif catSleepAlpha > catTargetSleepAlpha then
                catSleepAlpha = math.max(catTargetSleepAlpha, catSleepAlpha - dt * 1.5)
            end

            
            local char = lp.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if catFollowActive and hrp and catSleepAlpha < 0.2 then
                local wsCom = workspace:FindFirstChild("WorkspaceCom")
                local ignoreList = {char}
                if wsCom then table.insert(ignoreList, wsCom) end
                local rayParams = RaycastParams.new()
                rayParams.FilterDescendantsInstances = ignoreList
                rayParams.FilterType = Enum.RaycastFilterType.Exclude

                local function getSafeGroundY(pos)
                    local gRay = workspace:Raycast(Vector3.new(pos.X, pos.Y + 3.5, pos.Z), Vector3.new(0, -14, 0), rayParams)
                    if gRay and gRay.Instance and gRay.Instance.CanCollide then
                        return gRay.Position.Y
                    end
                    return hrp.Position.Y - 2.85
                end

                local targetFollowPos = hrp.Position - (hrp.CFrame.LookVector * 4.8) + (hrp.CFrame.RightVector * 2.2)
                local targetGroundY = getSafeGroundY(targetFollowPos)
                local targetVec = Vector3.new(targetFollowPos.X, targetGroundY, targetFollowPos.Z)

                local toTarget = Vector3.new(targetVec.X - catAnchor.X, 0, targetVec.Z - catAnchor.Z)
                local hDist = toTarget.Magnitude

                if hDist > 90 then
                    catAnchor = targetVec
                    catAngle = math.atan2(hrp.CFrame.LookVector.X, hrp.CFrame.LookVector.Z)
                    catMoveAlpha = 0
                elseif hDist > 2.8 then
                    local hDir = toTarget.Unit
                    local targetAngle = math.atan2(hDir.X, hDir.Z)
                    local diffAngle = (targetAngle - catAngle + math.pi) % (math.pi * 2) - math.pi
                    catAngle = catAngle + diffAngle * math.clamp(dt * 7.0, 0, 1)

                    local catSpeed = math.clamp(hDist * 2.6 + 6, 12, 22)
                    local stepDist = math.min(hDist - 2.0, catSpeed * dt)
                    local step = hDir * stepDist

                    local newX = catAnchor.X + step.X
                    local newZ = catAnchor.Z + step.Z

                    
                    local groundY = getSafeGroundY(Vector3.new(newX, catAnchor.Y, newZ))
                    local smoothY = catAnchor.Y + (groundY - catAnchor.Y) * math.clamp(dt * 10, 0, 1)

                    catAnchor = Vector3.new(newX, smoothY, newZ)
                    catMoveAlpha = math.min(1, catMoveAlpha + dt * 4.0)
                    catRunStride = catRunStride + dt * (catSpeed * 0.45)
                else
                    catMoveAlpha = math.max(0, catMoveAlpha - dt * 3.5)
                    local groundY = getSafeGroundY(catAnchor)
                    local smoothY = catAnchor.Y + (groundY - catAnchor.Y) * math.clamp(dt * 8, 0, 1)
                    catAnchor = Vector3.new(catAnchor.X, smoothY, catAnchor.Z)

                    if catMoveAlpha < 0.2 then
                        local toPlayer = Vector3.new(hrp.Position.X - catAnchor.X, 0, hrp.Position.Z - catAnchor.Z)
                        if toPlayer.Magnitude > 0.5 then
                            local lookAngle = math.atan2(toPlayer.X, toPlayer.Z)
                            local diffAngle = (lookAngle - catAngle + math.pi) % (math.pi * 2) - math.pi
                            catAngle = catAngle + diffAngle * math.clamp(dt * 4.5, 0, 1)
                        end
                    end
                end
            else
                catMoveAlpha = math.max(0, catMoveAlpha - dt * 3.5)
            end

            for key, w in pairs(catWorkers) do
                w.targetCF = computeDynamicCFrame(key, catAnchor, catAngle, catAnimTime, catSleepAlpha, catMoveAlpha, catRunStride)
            end
        end)
    end

    
    BuildV2Tab:AddSection({"> Feline Companion"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Cat Companion",
        Description = "Summon an animated realistic 10-prop feline companion with mountable beanbag cushion",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not catActive then return end
            if val then
                catActive = true
                task.spawn(spawnCatCompanion)
            else
                if not catActive then return end
                catActive = false
                catSeat = nil
                if catLoop then catLoop:Disconnect() catLoop = nil end
                pcall(function()
                    game:GetService("ReplicatedStorage"):WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps")
                end)
                catWorkers = {}
                catIsSleeping = false
                catFollowActive = false
                catMoveAlpha = 0
                catRunStride = 0
                catTargetSleepAlpha = 0
                cNotif("Cat Companion", "Cat dismissed.", 2)
            end
        end
    })

    BuildV2Tab:AddButton({
        Name = "Mount / Dismount Cat",
        Description = "Sit comfortably on the beanbag cushion for a fun stroll",
        Callback = function()
            if not catActive then
                cNotif("Cat Companion", "Spawn the cat first!", 2)
                return
            end
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not hum then return end

            if hum.Sit then
                hum.Sit = false
                hum:ChangeState(Enum.HumanoidStateType.GettingUp)
                cNotif("Cat Companion", "Dismounted.", 2)
            else
                if catSeat then
                    pcall(function()
                        catSeat.CanCollide = false
                        catSeat:Sit(hum)
                    end)
                    cNotif("Cat Companion", "Mounted! Enjoy the stroll!", 2)
                else
                    cNotif("Cat Companion", "Beanbag cushion not found!", 2)
                end
            end
        end
    })

    BuildV2Tab:AddButton({
        Name = "Toggle Follow Me",
        Description = "Cat follows you with realistic running strides and stops when you stop",
        Callback = function()
            if not catActive then
                cNotif("Cat Companion", "Spawn the cat first!", 2)
                return
            end
            catFollowActive = not catFollowActive
            if catFollowActive then
                catIsSleeping = false
                catTargetSleepAlpha = 0
            end
            cNotif("Cat Companion", catFollowActive and "Cat is now following you!" or "Cat stopped following.", 2)
        end
    })

    BuildV2Tab:AddButton({
        Name = "Toggle Sleep / Stand",
        Description = "Smoothly toggle posture between standing on all 4 legs and sleeping flat",
        Callback = function()
            if not catActive then
                cNotif("Cat Companion", "Spawn the cat first!", 2)
                return
            end
            catIsSleeping = not catIsSleeping
            catTargetSleepAlpha = catIsSleeping and 1 or 0
            cNotif("Cat Companion", catIsSleeping and "Cat is laying down flat to sleep..." or "Cat is rising to its feet...", 2)
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Cat Breed",
        Description = "Choose the cat breed and coat color theme",
        Options = {"Bombay (Black)", "Ginger Tabby (Orange)", "Snow Angora (White)", "Russian Blue (Grey)", "Siamese (Cream)", "Caramel (Golden)"},
        Default = "Bombay (Black)",
        Callback = function(choice)
            currentBreed = choice
            local col = CAT_BREEDS[choice]
            if col then
                updateCatColor(col)
                cNotif("Cat Breed", "Coat changed to " .. choice, 2)
            end
        end
    })

    BuildV2Tab:AddButton({
        Name = "Teleport In Front",
        Description = "Reposition the cat directly in front of you",
        Callback = function()
            local char = lp.Character or lp.CharacterAdded:Wait()
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp and catActive then
                local forwardPos = hrp.Position + (hrp.CFrame.LookVector * 12)
                local rayParams = RaycastParams.new()
                rayParams.FilterDescendantsInstances = {char}
                rayParams.FilterType = Enum.RaycastFilterType.Exclude

                local gRay = workspace:Raycast(forwardPos + Vector3.new(0, 35, 0), Vector3.new(0, -80, 0), rayParams)
                local groundY = gRay and gRay.Position.Y or (hrp.Position.Y - 2.5)

                catAnchor = Vector3.new(forwardPos.X, groundY, forwardPos.Z)
                catAngle = math.atan2(hrp.CFrame.LookVector.X, hrp.CFrame.LookVector.Z)
                cNotif("Cat Companion", "Repositioned in front of you!", 2)
            end
        end
    })
end

-- [LOCKED: WORK PERFECTLY] Titan Scorpion Ride & Drive Mount
do
    local lp = game:GetService("Players").LocalPlayer
    local uis = game:GetService("UserInputService")
    local rs = game:GetService("RunService")
    local rep = game:GetService("ReplicatedStorage")
    local ws = game:GetService("Workspace")

    local scorpionActive = false
    local scorpionPos = Vector3.new(0, 0, 0)
    local scorpionYaw = 0
    local scorpionWorkers = {}
    local scorpionSeat = nil
    local scorpionLoop = nil
    local isRiding = false
    local isStriking = false
    local strikeProgress = 0
    local walkCycle = 0
    local moveSpeed = 0
    local animTime = 0
    local currentTheme = "Realistic (Default)"
    local isRainbowMode = false
    local rainbowHue = 0
    local lastRainbowSync = 0
    local rainbowIndex = 1
    local strikeThread = nil

    local function sNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local SCORPION_PARTS = {
        ["Torso"]   = { off = Vector3.new(0, 3.8, 0),       rot = Vector3.new(0, 90, 0) },
        ["Saddle"]  = { off = Vector3.new(0, 3.4, 0.2),     rot = Vector3.new(0, 0, 0) },
        ["Arm_L"]   = { off = Vector3.new(-4.5, 3.6, -6.5), rot = Vector3.new(0, -45, 15) },
        ["Arm_R"]   = { off = Vector3.new(4.5, 3.6, -6.5),  rot = Vector3.new(0, 45, -15) },
        ["Leg_FL"]  = { off = Vector3.new(-4.5, 2.0, -2.0), rot = Vector3.new(10, 160, -28) },
        ["Leg_ML"]  = { off = Vector3.new(-4.8, 2.0, 0.4),  rot = Vector3.new(0, 180, -28) },
        ["Leg_BL"]  = { off = Vector3.new(-4.5, 2.0, 2.8),  rot = Vector3.new(-10, 200, -28) },
        ["Leg_FR"]  = { off = Vector3.new(4.5, 2.0, -2.0),  rot = Vector3.new(10, 20, -28) },
        ["Leg_MR"]  = { off = Vector3.new(4.8, 2.0, 0.4),   rot = Vector3.new(0, 0, -28) },
        ["Leg_BR"]  = { off = Vector3.new(4.5, 2.0, 2.8),   rot = Vector3.new(-10, -20, -28) },
        ["Tail_1"]  = { off = Vector3.new(0, 5.0, 4.5),     rot = Vector3.new(-38, 0, 0) },
        ["Tail_2"]  = { off = Vector3.new(0, 7.5, 4.0),     rot = Vector3.new(-85, 0, 0) },
        ["Stinger"] = { off = Vector3.new(0, 8.5, 1.8),     rot = Vector3.new(-145, 0, 0) }
    }

    local SCORPION_BLUEPRINT = {
        { key = "Torso",   prop = "Block2",             cat = "Building" },
        { key = "Saddle",  prop = "FloorCushion",       cat = "Furniture" },
        { key = "Arm_L",   prop = "Block3",             cat = "Building" },
        { key = "Claw_L",  prop = "VerticalWedgeSmall", cat = "Building" },
        { key = "Arm_R",   prop = "Block3",             cat = "Building" },
        { key = "Claw_R",  prop = "VerticalWedgeSmall", cat = "Building" },
        { key = "Leg_FL",  prop = "Block3",             cat = "Building" },
        { key = "Leg_ML",  prop = "Block3",             cat = "Building" },
        { key = "Leg_BL",  prop = "Block3",             cat = "Building" },
        { key = "Leg_FR",  prop = "Block3",             cat = "Building" },
        { key = "Leg_MR",  prop = "Block3",             cat = "Building" },
        { key = "Leg_BR",  prop = "Block3",             cat = "Building" },
        { key = "Tail_1",  prop = "Block1",             cat = "Building" },
        { key = "Tail_2",  prop = "Block1",             cat = "Building" },
        { key = "Stinger", prop = "VerticalWedgeSmall", cat = "Building" }
    }

    local SCORPION_THEMES = {
        ["Realistic (Default)"] = {
            Torso   = Color3.fromRGB(52, 34, 22),
            Legs    = Color3.fromRGB(52, 34, 22),
            Claws   = Color3.fromRGB(52, 34, 22),
            Stinger = Color3.fromRGB(52, 34, 22)
        },
        ["Full Black"] = {
            Torso   = Color3.fromRGB(12, 12, 14),
            Legs    = Color3.fromRGB(12, 12, 14),
            Claws   = Color3.fromRGB(12, 12, 14),
            Stinger = Color3.fromRGB(12, 12, 14)
        },
        ["Desert"] = {
            Torso   = Color3.fromRGB(210, 180, 110),
            Legs    = Color3.fromRGB(225, 200, 130),
            Claws   = Color3.fromRGB(190, 160, 95),
            Stinger = Color3.fromRGB(190, 160, 95)
        },
        ["Red & Black"] = {
            Torso   = Color3.fromRGB(18, 18, 20),
            Legs    = Color3.fromRGB(18, 18, 20),
            Claws   = Color3.fromRGB(180, 20, 25),
            Stinger = Color3.fromRGB(180, 20, 25)
        },
        ["Toxic Green"] = {
            Torso   = Color3.fromRGB(20, 22, 20),
            Legs    = Color3.fromRGB(25, 28, 25),
            Claws   = Color3.fromRGB(40, 210, 60),
            Stinger = Color3.fromRGB(40, 210, 60)
        },
        ["Purple"] = {
            Torso   = Color3.fromRGB(50, 20, 75),
            Legs    = Color3.fromRGB(25, 15, 35),
            Claws   = Color3.fromRGB(120, 40, 170),
            Stinger = Color3.fromRGB(140, 50, 190)
        },
        ["Rainbow"] = {}
    }

    local RAINBOW_COLORS = {
        Color3.fromRGB(255, 40, 40),
        Color3.fromRGB(255, 140, 0),
        Color3.fromRGB(255, 220, 0),
        Color3.fromRGB(40, 230, 50),
        Color3.fromRGB(0, 220, 255),
        Color3.fromRGB(40, 100, 255),
        Color3.fromRGB(160, 40, 255),
        Color3.fromRGB(255, 40, 180)
    }

    local RAINBOW_BRICKS = {
        "Bright red", "Bright orange", "Bright yellow", "Lime green", "Cyan", "Bright blue", "Royal purple", "Hot pink"
    }

    local function getPartColorForTheme(themeName, key)
        if themeName == "Realistic (Default)" then
            return Color3.fromRGB(52, 34, 22)
        end
        if themeName == "Full Black" then
            return Color3.fromRGB(12, 12, 14)
        end
        local theme = SCORPION_THEMES[themeName] or SCORPION_THEMES["Realistic (Default)"]
        if key == "Torso" or key == "Tail_1" or key == "Tail_2" then
            return theme.Torso or Color3.fromRGB(52, 34, 22)
        elseif key:sub(1, 4) == "Leg_" then
            return theme.Legs or Color3.fromRGB(52, 34, 22)
        elseif key:sub(1, 4) == "Arm_" or key:sub(1, 4) == "Claw" then
            return theme.Claws or Color3.fromRGB(52, 34, 22)
        elseif key == "Stinger" then
            return theme.Stinger or (theme.Torso or Color3.fromRGB(52, 34, 22))
        end
        return Color3.fromRGB(52, 34, 22)
    end

    local function paintScorpionWorker(w, col)
        if not w or w.key == "Saddle" or not col then return end
        if w.cpc then
            pcall(function() w.cpc:InvokeServer(col) end)
        end
        if w.prop then
            for _, d in ipairs(w.prop:GetDescendants()) do
                if d:IsA("BasePart") then
                    pcall(function()
                        d.Material = Enum.Material.SmoothPlastic
                        d.Color = col
                    end)
                end
            end
        end
    end

    local function applyTheme(themeName)
        currentTheme = themeName
        isRainbowMode = (themeName == "Rainbow")
        if isRainbowMode then
            sNotif("Titan Scorpion", "Rainbow Mode On!", 2)
            return
        end

        for key, w in pairs(scorpionWorkers) do
            if w.key ~= "Saddle" then
                local col = getPartColorForTheme(themeName, key)
                paintScorpionWorker(w, col)
            end
        end
        sNotif("Titan Scorpion", "Color: " .. themeName, 2)
    end

    local function computeScorpionPartCFrame(key, anchorCF, t, strikeAlpha, stride, isSprint, isMoving)
        local base = SCORPION_PARTS[key]
        local curOff = base and base.off or Vector3.new(0, 0, 0)
        local curRot = base and base.rot or Vector3.new(0, 0, 0)

        local breatheY = math.sin(t * 2.0) * 0.10
        if isSprint and isMoving then
            breatheY = breatheY - 0.35
        end

        if key == "Torso" or key == "Saddle" then
            curOff = curOff + Vector3.new(0, breatheY, 0)
        elseif key == "Arm_L" then
            local wave = math.sin(t * 1.8) * 0.20
            curOff = curOff + Vector3.new(wave, breatheY, 0)
        elseif key == "Arm_R" then
            local wave = math.cos(t * 1.8) * 0.20
            curOff = curOff + Vector3.new(-wave, breatheY, 0)
        elseif key == "Claw_L" then
            local armBase = SCORPION_PARTS["Arm_L"]
            local wave = math.sin(t * 1.8) * 0.20
            local armCF = anchorCF * CFrame.new(armBase.off + Vector3.new(wave, breatheY, 0)) * CFrame.Angles(
                math.rad(armBase.rot.X),
                math.rad(armBase.rot.Y),
                math.rad(armBase.rot.Z)
            )
            local pinch = math.sin(t * 2.5) * 4
            return armCF * CFrame.new(-4.85, 0, -0.3) * CFrame.Angles(0, math.rad(65 + pinch), 0)
        elseif key == "Claw_R" then
            local armBase = SCORPION_PARTS["Arm_R"]
            local wave = math.cos(t * 1.8) * 0.20
            local armCF = anchorCF * CFrame.new(armBase.off + Vector3.new(-wave, breatheY, 0)) * CFrame.Angles(
                math.rad(armBase.rot.X),
                math.rad(armBase.rot.Y),
                math.rad(armBase.rot.Z)
            )
            local pinch = math.sin(t * 2.5) * 4
            return armCF * CFrame.new(4.85, 0, -0.3) * CFrame.Angles(0, math.rad(-65 - pinch), 0)
        elseif key:sub(1, 4) == "Leg_" then
            if isMoving then
                local phase = 0
                if key == "Leg_FR" or key == "Leg_ML" or key == "Leg_BR" then
                    phase = math.pi
                end
                local p = stride + phase
                local liftMult = isSprint and 1.1 or 0.65
                local sweepMult = isSprint and 16 or 11

                local lift = math.max(0, math.sin(p)) * liftMult
                local sweep = math.cos(p) * sweepMult

                curOff = curOff + Vector3.new(0, lift, 0)
                curRot = curRot + Vector3.new(0, sweep, 0)
            end
        end

        if strikeAlpha and strikeAlpha ~= 0 then
            local s = strikeAlpha
            if s > 0 then
                if key == "Tail_1" then
                    curOff = curOff + Vector3.new(0, 0.5 * s, -2.8 * s)
                    curRot = curRot + Vector3.new(62 * s, 0, 0)
                elseif key == "Tail_2" then
                    curOff = curOff + Vector3.new(0, -1.2 * s, -8.8 * s)
                    curRot = curRot + Vector3.new(155 * s, 0, 0)
                elseif key == "Stinger" then
                    curOff = curOff + Vector3.new(0, -6.5 * s, -12.5 * s)
                    curRot = curRot + Vector3.new(-75 * s, 0, 0)
                end
            else
                local w = -s
                if key == "Tail_1" then
                    curOff = curOff + Vector3.new(0, 0.3 * w, 0.8 * w)
                    curRot = curRot + Vector3.new(-18 * w, 0, 0)
                elseif key == "Tail_2" then
                    curOff = curOff + Vector3.new(0, 0.8 * w, 1.2 * w)
                    curRot = curRot + Vector3.new(-25 * w, 0, 0)
                elseif key == "Stinger" then
                    curOff = curOff + Vector3.new(0, 1.5 * w, 1.0 * w)
                    curRot = curRot + Vector3.new(15 * w, 0, 0)
                end
            end
        end

        local cf = anchorCF * CFrame.new(curOff)
        cf = cf * CFrame.Angles(
            math.rad(curRot.X),
            math.rad(curRot.Y),
            math.rad(curRot.Z)
        )
        return cf
    end

    local function getGroundPos(pos)
        local char = lp.Character
        local wsCom = ws:FindFirstChild("WorkspaceCom")
        local ignoreList = {}
        if char then table.insert(ignoreList, char) end
        if wsCom then table.insert(ignoreList, wsCom) end

        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude
        rayParams.FilterDescendantsInstances = ignoreList

        local ray = ws:Raycast(Vector3.new(pos.X, pos.Y + 4.0, pos.Z), Vector3.new(0, -24.0, 0), rayParams)
        if ray and ray.Instance and ray.Instance.CanCollide then
            return Vector3.new(pos.X, ray.Position.Y, pos.Z)
        end

        local deepRay = ws:Raycast(Vector3.new(pos.X, pos.Y + 12.0, pos.Z), Vector3.new(0, -50.0, 0), rayParams)
        if deepRay and deepRay.Instance and deepRay.Instance.CanCollide then
            return Vector3.new(pos.X, deepRay.Position.Y, pos.Z)
        end

        return Vector3.new(pos.X, pos.Y, pos.Z)
    end

    local function getPropMakerTool()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local backpack = lp:WaitForChild("Backpack")
        local pm = char:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
        if not pm then
            local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
            pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
            local start = tick()
            while tick() - start < 3 do
                pm = char:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
                if pm then break end
                task.wait(0.1)
            end
        end
        return pm
    end

    local function startWorker(w)
        task.spawn(function()
            while scorpionActive and w.scf and w.scf.Parent do
                local now = tick()
                if w.targetCF then
                    local cf = w.targetCF
                    local send = false
                    if not w.lastSentCF then
                        send = true
                    else
                        local pDiff = (cf.Position - w.lastSentCF.Position).Magnitude
                        if pDiff > 0.03 or (now - w.lastSendTimeCF > 0.08) then
                            send = true
                        end
                    end
                    if send then
                        w.lastSentCF = cf
                        w.lastSendTimeCF = now
                        task.spawn(function()
                            pcall(function()
                                w.scf:InvokeServer(cf)
                            end)
                        end)
                        if not isRainbowMode and w.prop and (now - (w.lastColorCheck or 0) > 0.6) then
                            w.lastColorCheck = now
                            local targetCol = getPartColorForTheme(currentTheme, w.key)
                            for _, d in ipairs(w.prop:GetDescendants()) do
                                if d:IsA("BasePart") and w.key ~= "Saddle" and (d.Material ~= Enum.Material.SmoothPlastic or d.Color ~= targetCol) then
                                    pcall(function()
                                        d.Material = Enum.Material.SmoothPlastic
                                        d.Color = targetCol
                                    end)
                                end
                            end
                        end
                    end
                end
                task.wait(0.03)
            end
        end)
    end

    local function triggerStingerStrike()
        if not scorpionActive then
            sNotif("Titan Scorpion", "Spawn the scorpion first!", 2)
            return
        end
        if isStriking then return end
        isStriking = true

        if strikeThread then task.cancel(strikeThread) end
        strikeThread = task.spawn(function()
            local t0 = tick()
            while tick() - t0 < 0.12 do
                local a = (tick() - t0) / 0.12
                strikeProgress = -0.35 * math.sin(a * math.pi * 0.5)
                task.wait(0.02)
            end

            local t1 = tick()
            while tick() - t1 < 0.09 do
                local a = (tick() - t1) / 0.09
                local easeOut = 1 - (1 - a) * (1 - a) * (1 - a)
                strikeProgress = -0.35 + (1.0 - (-0.35)) * easeOut
                task.wait(0.02)
            end
            strikeProgress = 1.0

            task.wait(0.10)

            local t2 = tick()
            while tick() - t2 < 0.18 do
                local a = (tick() - t2) / 0.18
                local easeInOut = a < 0.5 and 2 * a * a or 1 - math.pow(-2 * a + 2, 2) / 2
                strikeProgress = 1.0 * (1 - easeInOut)
                task.wait(0.02)
            end

            strikeProgress = 0
            isStriking = false
        end)
    end

    local function spawnTitanScorpion()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")

        local propsTool = getPropMakerTool()
        if not propsTool then
            sNotif("Titan Scorpion", "PropMaker not found!", 3)
            return
        end

        if hum then
            hum:EquipTool(propsTool)
            local eq = tick()
            while tick() - eq < 2 do
                if propsTool.Parent == char then break end
                task.wait(0.05)
            end
        end

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake")
        local wsWait = tick()
        while not toolPropMake and tick() - wsWait < 5 do
            toolPropMake = propsTool:FindFirstChild("Tool_PropMake")
            task.wait(0.1)
        end
        if not toolPropMake then
            return
        end

        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        local wc = ws:WaitForChild("WorkspaceCom")
        local myPropName = "Prop" .. lp.Name

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.4)

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local forwardVec = hrp.CFrame.LookVector
        local flatFwd = Vector3.new(forwardVec.X, 0, forwardVec.Z).Unit
        local spawnPos = hrp.Position + flatFwd * 18.0
        local groundVec = getGroundPos(spawnPos)

        scorpionPos = groundVec
        scorpionYaw = math.atan2(-flatFwd.X, -flatFwd.Z)
        scorpionActive = true
        scorpionWorkers = {}
        scorpionSeat = nil

        local knownScf = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                    if scf then knownScf[scf] = true end
                end
            end
        end

        for _, bp in ipairs(SCORPION_BLUEPRINT) do
            if not scorpionActive then break end

            local currentAnchorCF = CFrame.new(scorpionPos) * CFrame.Angles(0, scorpionYaw, 0)
            local initialCF = computeScorpionPartCFrame(bp.key, currentAnchorCF, 0, 0, 0, false, false)

            pcall(function()
                if bp.prop == "FloorCushion" then
                    clearTools:FireServer("RequestingPropFeatureName", bp.prop, bp.cat)
                else
                    clearTools:FireServer("RequestingPropName", bp.prop, bp.cat)
                end
            end)
            task.wait(0.18)

            pcall(function() toolPropMake:FireServer(ws.Terrain, scorpionPos) end)
            task.wait(0.65)

            local foundProp, foundScf, foundCpc = nil, nil, nil
            for _, folder in pairs(wc:GetChildren()) do
                for _, prop in pairs(folder:GetChildren()) do
                    if prop.Name == myPropName then
                        local scf = prop:FindFirstChild("SetCurrentCFrame")
                        local cpc = prop:FindFirstChild("ChangePropColor")
                        if scf and not knownScf[scf] then
                            foundProp = prop
                            foundScf = scf
                            foundCpc = cpc
                            knownScf[scf] = true
                            break
                        end
                    end
                end
                if foundScf then break end
            end

            if foundProp and bp.key == "Saddle" then
                for _, d in ipairs(foundProp:GetDescendants()) do
                    if d:IsA("Seat") or d:IsA("VehicleSeat") then
                        pcall(function() d.CanCollide = false end)
                        scorpionSeat = d
                    elseif d:IsA("BasePart") then
                        pcall(function()
                            d.Transparency = 1
                            d.CanCollide = false
                        end)
                    end
                end
            end

            local worker = {
                key = bp.key,
                prop = foundProp,
                scf = foundScf,
                cpc = foundCpc,
                targetCF = initialCF,
                lastSentCF = nil,
                lastSendTimeCF = 0
            }
            scorpionWorkers[bp.key] = worker

            if foundScf then
                pcall(function() foundScf:InvokeServer(initialCF) end)
            end
            if bp.key ~= "Saddle" then
                local initialCol = getPartColorForTheme(currentTheme, bp.key)
                paintScorpionWorker(worker, initialCol)
            end

            startWorker(worker)
        end

        task.wait(1.0)
        applyTheme(currentTheme)
        pcall(function()
            toolRE:InvokeServer("PickingTools", "PropMaker")
        end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        if scorpionLoop then scorpionLoop:Disconnect() scorpionLoop = nil end
        scorpionLoop = rs.Heartbeat:Connect(function(dt)
            if not scorpionActive then return end

            animTime = animTime + dt
            local myChar = lp.Character
            local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")
            isRiding = myHum and myHum.Sit and scorpionSeat and (myHum.SeatPart == scorpionSeat)

            local isMoving = false
            local isSprint = false

            if isRiding then
                local isFwd = uis:IsKeyDown(Enum.KeyCode.Z) or uis:IsKeyDown(Enum.KeyCode.W) or uis:IsKeyDown(Enum.KeyCode.Up)
                local isBwd = uis:IsKeyDown(Enum.KeyCode.S) or uis:IsKeyDown(Enum.KeyCode.Down)
                local isLeft = uis:IsKeyDown(Enum.KeyCode.Q) or uis:IsKeyDown(Enum.KeyCode.A) or uis:IsKeyDown(Enum.KeyCode.Left)
                local isRight = uis:IsKeyDown(Enum.KeyCode.D) or uis:IsKeyDown(Enum.KeyCode.Right)
                isSprint = uis:IsKeyDown(Enum.KeyCode.LeftShift) or uis:IsKeyDown(Enum.KeyCode.RightShift)

                if isLeft then
                    scorpionYaw = scorpionYaw + 2.8 * dt
                end
                if isRight then
                    scorpionYaw = scorpionYaw - 2.8 * dt
                end

                local targetSpeed = 0
                if isFwd then
                    targetSpeed = isSprint and 32 or 16
                elseif isBwd then
                    targetSpeed = -(isSprint and 18 or 10)
                end

                moveSpeed = moveSpeed + (targetSpeed - moveSpeed) * math.clamp(dt * 9, 0, 1)

                if math.abs(moveSpeed) > 0.4 then
                    isMoving = true
                    local lookDir = Vector3.new(-math.sin(scorpionYaw), 0, -math.cos(scorpionYaw))
                    local nextPos = scorpionPos + lookDir * (moveSpeed * dt)
                    local groundPos = getGroundPos(nextPos)
                    local diffY = groundPos.Y - scorpionPos.Y
                    local stepSpeed = diffY < 0 and 22 or 12
                    local smoothY = scorpionPos.Y + math.clamp(diffY * math.clamp(dt * stepSpeed, 0, 1), -30 * dt, 15 * dt)
                    scorpionPos = Vector3.new(nextPos.X, smoothY, nextPos.Z)

                    local strideRate = (isSprint and 10 or 6) * (moveSpeed > 0 and 1 or -1)
                    walkCycle = walkCycle + dt * strideRate
                else
                    local groundPos = getGroundPos(scorpionPos)
                    local diffY = groundPos.Y - scorpionPos.Y
                    local stepSpeed = diffY < 0 and 22 or 12
                    local smoothY = scorpionPos.Y + math.clamp(diffY * math.clamp(dt * stepSpeed, 0, 1), -30 * dt, 15 * dt)
                    scorpionPos = Vector3.new(scorpionPos.X, smoothY, scorpionPos.Z)
                end

                if uis:IsKeyDown(Enum.KeyCode.Space) or uis:IsKeyDown(Enum.KeyCode.F) then
                    triggerStingerStrike()
                end
            else
                local groundPos = getGroundPos(scorpionPos)
                local diffY = groundPos.Y - scorpionPos.Y
                local stepSpeed = diffY < 0 and 22 or 12
                local smoothY = scorpionPos.Y + math.clamp(diffY * math.clamp(dt * stepSpeed, 0, 1), -30 * dt, 15 * dt)
                scorpionPos = Vector3.new(scorpionPos.X, smoothY, scorpionPos.Z)
            end

            if isRainbowMode then
                rainbowHue = (rainbowHue + dt * 0.45) % 1
                for idx, bp in ipairs(SCORPION_BLUEPRINT) do
                    local w = scorpionWorkers[bp.key]
                    if w and w.prop and bp.key ~= "Saddle" then
                        local partHue = (rainbowHue + (idx / #SCORPION_BLUEPRINT)) % 1
                        local col = Color3.fromHSV(partHue, 0.95, 1)
                        for _, d in ipairs(w.prop:GetDescendants()) do
                            if d:IsA("BasePart") then
                                pcall(function() d.Color = col end)
                            end
                        end
                    end
                end

                if tick() - lastRainbowSync > 0.35 then
                    lastRainbowSync = tick()
                    rainbowIndex = (rainbowIndex % #RAINBOW_BRICKS) + 1
                    local brick = RAINBOW_BRICKS[rainbowIndex]
                    for _, w in pairs(scorpionWorkers) do
                        if w.cpc and w.key ~= "Saddle" then
                            pcall(function() w.cpc:InvokeServer(brick) end)
                        end
                    end
                end
            end

            local anchorCF = CFrame.new(scorpionPos) * CFrame.Angles(0, scorpionYaw, 0)
            for _, bp in ipairs(SCORPION_BLUEPRINT) do
                local w = scorpionWorkers[bp.key]
                if w and w.scf then
                    w.targetCF = computeScorpionPartCFrame(bp.key, anchorCF, animTime, strikeProgress, walkCycle, isSprint, isMoving)
                end
            end
        end)

        sNotif("Titan Scorpion", "Titan Scorpion assembled! Ready to ride.", 3)
    end

    local function clearTitanScorpion()
        scorpionActive = false
        isStriking = false
        strikeProgress = 0
        scorpionSeat = nil
        scorpionWorkers = {}
        if scorpionLoop then
            scorpionLoop:Disconnect()
            scorpionLoop = nil
        end
        pcall(function()
            rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps")
        end)
        sNotif("Titan Scorpion", "Scorpion dismissed.", 2)
    end

    
    BuildV2Tab:AddSection({"> Titan Scorpion Mount"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Titan Scorpion",
        Description = "Summon an animated colossal scorpion mount with rideable saddle",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not scorpionActive then return end
            if val then
                task.spawn(spawnTitanScorpion)
            else
                clearTitanScorpion()
            end
        end
    })

    BuildV2Tab:AddButton({
        Name = "Mount / Dismount",
        Description = "Climb onto or hop off the scorpion saddle",
        Callback = function()
            if not scorpionActive then
                sNotif("Titan Scorpion", "Spawn the scorpion first!", 2)
                return
            end
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not hum then return end

            if hum.Sit then
                hum.Sit = false
                hum:ChangeState(Enum.HumanoidStateType.GettingUp)
                sNotif("Titan Scorpion", "Dismounted.", 2)
            else
                if scorpionSeat then
                    pcall(function()
                        scorpionSeat.CanCollide = false
                        scorpionSeat:Sit(hum)
                    end)
                    sNotif("Titan Scorpion", "Mounted! Use ZQSD / WASD to drive, Shift to sprint, Space to attack.", 3)
                else
                    sNotif("Titan Scorpion", "Saddle not found!", 2)
                end
            end
        end
    })

    BuildV2Tab:AddButton({
        Name = "Stinger Strike",
        Description = "Unleash a high-speed poisonous tail strike (or press Space/F)",
        Callback = function()
            triggerStingerStrike()
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Scorpion Color",
        Description = "Choose scorpion color theme or animated rainbow",
        Options = {
            "Realistic (Default)",
            "Desert",
            "Red & Black",
            "Toxic Green",
            "Purple",
            "Full Black",
            "Rainbow"
        },
        Default = "Realistic (Default)",
        Callback = function(choice)
            applyTheme(choice)
        end
    })

    BuildV2Tab:AddButton({
        Name = "Teleport In Front",
        Description = "Reposition the scorpion directly in front of you",
        Callback = function()
            local char = lp.Character or lp.CharacterAdded:Wait()
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp and scorpionActive then
                local forwardVec = hrp.CFrame.LookVector
                local flatFwd = Vector3.new(forwardVec.X, 0, forwardVec.Z).Unit
                local forwardPos = hrp.Position + flatFwd * 18.0
                scorpionPos = getGroundPos(forwardPos)
                scorpionYaw = math.atan2(-flatFwd.X, -flatFwd.Z)
                sNotif("Titan Scorpion", "Repositioned in front of you!", 2)
            end
        end
    })
end

do
    local lp = game:GetService("Players").LocalPlayer
    local uis = game:GetService("UserInputService")
    local rs = game:GetService("RunService")
    local rep = game:GetService("ReplicatedStorage")
    local ws = game:GetService("Workspace")

    local gorillaActive = false
    local gorillaPos = Vector3.new(0, 0, 0)
    local gorillaYaw = 0
    local gorillaWorkers = {}
    local gorillaLoop = nil
    local gorillaFollowActive = false
    local gorillaFallVelocity = 0
    local currentTheme = "Realistic (Default)"
    local isRainbowMode = false
    local rainbowHue = 0
    local lastRainbowSync = 0
    local rainbowIndex = 1
    local animTime = 0

    local function gNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local GORILLA_BLUEPRINT = {
        { key = "Chest_Upper", prop = "Block2", cat = "Building" },
        { key = "Chest_Lower", prop = "Block2", cat = "Building" },
        { key = "Back_Slope",  prop = "Block2", cat = "Building" },
        { key = "Hips",        prop = "Block1", cat = "Building" },
        { key = "Head",        prop = "Block1", cat = "Building" },

        { key = "Arm_L_Upper", prop = "Block1", cat = "Building" },
        { key = "Arm_L_Lower", prop = "Block1", cat = "Building" },
        { key = "Hand_L",      prop = "Block1", cat = "Building" },

        { key = "Arm_R_Upper", prop = "Block1", cat = "Building" },
        { key = "Arm_R_Lower", prop = "Block1", cat = "Building" },
        { key = "Hand_R",      prop = "Block1", cat = "Building" },

        { key = "Leg_L",       prop = "Block1", cat = "Building" },
        { key = "Foot_L",      prop = "Block1", cat = "Building" },

        { key = "Leg_R",       prop = "Block1", cat = "Building" },
        { key = "Foot_R",      prop = "Block1", cat = "Building" }
    }

    local GORILLA_THEMES = {
        ["Realistic (Default)"] = {
            Silver = Color3.fromRGB(132, 138, 148),
            Hips   = Color3.fromRGB(38, 40, 46),
            Skin   = Color3.fromRGB(24, 25, 28),
            Fur    = Color3.fromRGB(15, 15, 17)
        },
        ["Full Black"] = {
            Silver = Color3.fromRGB(12, 12, 14),
            Hips   = Color3.fromRGB(12, 12, 14),
            Skin   = Color3.fromRGB(12, 12, 14),
            Fur    = Color3.fromRGB(12, 12, 14)
        },
        ["Dark Brown"] = {
            Silver = Color3.fromRGB(115, 90, 70),
            Hips   = Color3.fromRGB(45, 30, 20),
            Skin   = Color3.fromRGB(32, 22, 14),
            Fur    = Color3.fromRGB(50, 34, 22)
        },
        ["Albino"] = {
            Silver = Color3.fromRGB(255, 255, 255),
            Hips   = Color3.fromRGB(245, 245, 250),
            Skin   = Color3.fromRGB(225, 200, 200),
            Fur    = Color3.fromRGB(240, 240, 245)
        },
        ["Red & Black"] = {
            Silver = Color3.fromRGB(205, 30, 35),
            Hips   = Color3.fromRGB(35, 15, 18),
            Skin   = Color3.fromRGB(160, 20, 25),
            Fur    = Color3.fromRGB(18, 18, 22)
        },
        ["Rainbow"] = {}
    }

    local RAINBOW_COLORS = {
        Color3.fromRGB(255, 40, 40),
        Color3.fromRGB(255, 140, 0),
        Color3.fromRGB(255, 220, 0),
        Color3.fromRGB(40, 230, 50),
        Color3.fromRGB(0, 220, 255),
        Color3.fromRGB(40, 100, 255),
        Color3.fromRGB(160, 40, 255),
        Color3.fromRGB(255, 40, 180)
    }

    local RAINBOW_BRICKS = {
        "Bright red", "Bright orange", "Bright yellow", "Lime green", "Cyan", "Bright blue", "Royal purple", "Hot pink"
    }

    local function getPartColor(themeName, key)
        if themeName == "Full Black" then
            return Color3.fromRGB(12, 12, 14)
        end
        local theme = GORILLA_THEMES[themeName] or GORILLA_THEMES["Realistic (Default)"]
        if key == "Chest_Lower" then
            return theme.Silver or Color3.fromRGB(132, 138, 148)
        elseif key == "Chest_Upper" or key == "Back_Slope" then
            return theme.Fur or Color3.fromRGB(15, 15, 17)
        elseif key == "Hips" then
            return theme.Hips or Color3.fromRGB(38, 40, 46)
        elseif key == "Head" or key == "Hand_L" or key == "Hand_R" or key == "Foot_L" or key == "Foot_R" then
            return theme.Skin or Color3.fromRGB(24, 25, 28)
        end
        return theme.Fur or Color3.fromRGB(15, 15, 17)
    end

    local function paintGorillaWorker(w, col)
        if not w or not w.prop then return end
        if w.prop:IsA("BasePart") then
            pcall(function()
                w.prop.Material = Enum.Material.SmoothPlastic
                w.prop.Color = col
            end)
        end
        for _, d in ipairs(w.prop:GetDescendants()) do
            if d:IsA("BasePart") then
                pcall(function()
                    d.Material = Enum.Material.SmoothPlastic
                    d.Color = col
                end)
            end
        end
        if w.cpc then
            pcall(function() w.cpc:InvokeServer(col) end)
        end
    end

    local function applyTheme(themeName)
        currentTheme = themeName
        isRainbowMode = (themeName == "Rainbow")
        if isRainbowMode then
            gNotif("Colossal Gorilla", "Rainbow Mode On!", 2)
            return
        end
        for key, w in pairs(gorillaWorkers) do
            local col = getPartColor(themeName, key)
            paintGorillaWorker(w, col)
        end
        gNotif("Colossal Gorilla", "Color: " .. themeName, 2)
    end

    local function computeGorillaPartCFrame(key, anchorCF, t)
        local breathe = math.sin(t * 2.0) * 0.10

        
        local chestPos = Vector3.new(0, 6.6 + breathe, -0.6)
        local chestCF = anchorCF * CFrame.new(chestPos) * CFrame.Angles(math.rad(15), 0, 0)
        if key == "Chest_Upper" then
            return chestCF
        end

        
        local chestLowerCF = chestCF * CFrame.new(0, -1.8, 0.4) * CFrame.Angles(math.rad(8), 0, 0)
        if key == "Chest_Lower" then
            return chestLowerCF
        end

        
        local backCF = chestCF * CFrame.new(0, -1.2, 2.2) * CFrame.Angles(math.rad(28), 0, 0)
        if key == "Back_Slope" then
            return backCF
        end

        
        local hipsCF = backCF * CFrame.new(0, -1.3, 1.7) * CFrame.Angles(math.rad(-22), 0, 0)
        if key == "Hips" then
            return hipsCF
        end

        
        local headCF = chestCF * CFrame.new(0, 1.4, -0.8) * CFrame.Angles(math.rad(-12), 0, 0)
        if key == "Head" then
            return headCF
        end

        
        local shoulderL_CF = chestCF * CFrame.new(-3.7, -0.1, -0.1)
            * CFrame.Angles(math.rad(10), math.rad(-5), math.rad(6))

        local armL1_CF = shoulderL_CF * CFrame.new(0, -2.3, 0)
        if key == "Arm_L_Upper" then return armL1_CF end

        local armL2_CF = shoulderL_CF * CFrame.new(0, -4.8, -0.1) * CFrame.Angles(math.rad(4), 0, 0)
        if key == "Arm_L_Lower" then return armL2_CF end

        local handL_CF = shoulderL_CF * CFrame.new(0, -7.3, -0.2) * CFrame.Angles(math.rad(-10), 0, 0)
        if key == "Hand_L" then return handL_CF end

        
        local shoulderR_CF = chestCF * CFrame.new(3.7, -0.1, -0.1)
            * CFrame.Angles(math.rad(10), math.rad(5), math.rad(-6))

        local armR1_CF = shoulderR_CF * CFrame.new(0, -2.3, 0)
        if key == "Arm_R_Upper" then return armR1_CF end

        local armR2_CF = shoulderR_CF * CFrame.new(0, -4.8, -0.1) * CFrame.Angles(math.rad(4), 0, 0)
        if key == "Arm_R_Lower" then return armR2_CF end

        local handR_CF = shoulderR_CF * CFrame.new(0, -7.3, -0.2) * CFrame.Angles(math.rad(-10), 0, 0)
        if key == "Hand_R" then return handR_CF end

        
        local hipL_CF = hipsCF * CFrame.new(-1.9, -0.4, 0.1)
            * CFrame.Angles(math.rad(-12), math.rad(-8), math.rad(10))

        local legL_CF = hipL_CF * CFrame.new(0, -1.1, 0)
        if key == "Leg_L" then return legL_CF end

        local footL_CF = hipL_CF * CFrame.new(0, -2.6, 0.25) * CFrame.Angles(math.rad(16), 0, 0)
        if key == "Foot_L" then return footL_CF end

        local hipR_CF = hipsCF * CFrame.new(1.9, -0.4, 0.1)
            * CFrame.Angles(math.rad(-12), math.rad(8), math.rad(-10))

        local legR_CF = hipR_CF * CFrame.new(0, -1.1, 0)
        if key == "Leg_R" then return legR_CF end

        local footR_CF = hipR_CF * CFrame.new(0, -2.6, 0.25) * CFrame.Angles(math.rad(16), 0, 0)
        if key == "Foot_R" then return footR_CF end

        return anchorCF
    end

    local function getSafeGround(pos)
        local char = lp.Character
        local wsCom = ws:FindFirstChild("WorkspaceCom")
        local ignore = {}
        if char then table.insert(ignore, char) end
        if wsCom then table.insert(ignore, wsCom) end

        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude
        rayParams.FilterDescendantsInstances = ignore

        local ray = ws:Raycast(Vector3.new(pos.X, pos.Y + 4.0, pos.Z), Vector3.new(0, -25.0, 0), rayParams)
        if ray and ray.Instance and ray.Instance.CanCollide then
            return Vector3.new(pos.X, ray.Position.Y, pos.Z), ray.Instance
        end

        local deepRay = ws:Raycast(Vector3.new(pos.X, pos.Y + 12.0, pos.Z), Vector3.new(0, -60.0, 0), rayParams)
        if deepRay and deepRay.Instance and deepRay.Instance.CanCollide then
            return Vector3.new(pos.X, deepRay.Position.Y, pos.Z), deepRay.Instance
        end

        return Vector3.new(pos.X, pos.Y, pos.Z), ws.Terrain
    end

    local function getGroundAnchor()
        local char = lp.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not hrp then return CFrame.new(), ws.Terrain end

        local fwd = hrp.CFrame.LookVector
        local flatFwd = Vector3.new(fwd.X, 0, fwd.Z).Unit
        local spawnPos = hrp.Position + flatFwd * 16.0
        local groundVec, groundInst = getSafeGround(spawnPos)
        local yaw = math.atan2(flatFwd.X, flatFwd.Z)

        return CFrame.new(groundVec) * CFrame.Angles(0, yaw, 0), (groundInst or ws.Terrain)
    end

    local function getPropMakerTool()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local backpack = lp:WaitForChild("Backpack")
        local pm = char:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
        if not pm then
            local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
            pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
            local start = tick()
            while tick() - start < 3 do
                pm = char:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
                if pm then break end
                task.wait(0.1)
            end
        end
        return pm
    end

    local function startWorker(w)
        task.spawn(function()
            while gorillaActive and w.scf and w.scf.Parent do
                local now = tick()
                if w.targetCF then
                    local cf = w.targetCF
                    local send = false
                    if not w.lastSentCF then
                        send = true
                    else
                        local p0 = w.lastSentCF.Position
                        local p1 = cf.Position
                        local posDiff = (p1 - p0).Magnitude
                        if posDiff > 0.05 or (now - w.lastSendTimeCF > 0.35) then
                            send = true
                        end
                    end
                    if send then
                        w.lastSentCF = cf
                        w.lastSendTimeCF = now
                        task.spawn(function()
                            pcall(function()
                                w.scf:InvokeServer(cf)
                            end)
                        end)
                        if not isRainbowMode and w.prop and (now - (w.lastColorCheck or 0) > 0.6) then
                            w.lastColorCheck = now
                            local targetCol = getPartColor(currentTheme, w.key)
                            for _, d in ipairs(w.prop:GetDescendants()) do
                                if d:IsA("BasePart") and (d.Material ~= Enum.Material.SmoothPlastic or d.Color ~= targetCol) then
                                    pcall(function()
                                        d.Material = Enum.Material.SmoothPlastic
                                        d.Color = targetCol
                                    end)
                                end
                            end
                        end
                    end
                end
                task.wait(0.03)
            end
        end)
    end

    local function spawnGorilla()
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:FindFirstChildOfClass("Humanoid")

        local propsTool = getPropMakerTool()
        if not propsTool then
            gNotif("Colossal Gorilla", "PropMaker not found!", 3)
            return
        end

        if hum then
            hum:EquipTool(propsTool)
            local eq = tick()
            while tick() - eq < 2 do
                if propsTool.Parent == char then break end
                task.wait(0.05)
            end
        end

        local toolPropMake = propsTool:FindFirstChild("Tool_PropMake")
        local wsWait = tick()
        while not toolPropMake and tick() - wsWait < 5 do
            toolPropMake = propsTool:FindFirstChild("Tool_PropMake")
            task.wait(0.1)
        end
        if not toolPropMake then
            return
        end

        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        local wc = ws:WaitForChild("WorkspaceCom")
        local myPropName = "Prop" .. lp.Name

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.4)

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local anchorCF, groundInst = getGroundAnchor()
        gorillaPos = anchorCF.Position
        gorillaYaw = math.atan2(-anchorCF.LookVector.X, -anchorCF.LookVector.Z)
        gorillaActive = true
        gorillaWorkers = {}

        local Telemetry = rep:FindFirstChild("Remotes") and rep.Remotes:FindFirstChild("TelemetryClientInteraction")

        local knownScf = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                    if scf then knownScf[scf] = true end
                end
            end
        end

        for idx, bp in ipairs(GORILLA_BLUEPRINT) do
            if not gorillaActive then break end

            local currentAnchorCF = CFrame.new(gorillaPos) * CFrame.Angles(0, gorillaYaw, 0)
            local initialCF = computeGorillaPartCFrame(bp.key, currentAnchorCF, 0)

            local foundProp, foundScf, foundCpc = nil, nil, nil

            for attempt = 1, 3 do
                pcall(function()
                    if Telemetry then
                        Telemetry:FireServer("filterClick", {name = bp.cat, itemType = "Props"})
                        task.wait(0.04)
                        Telemetry:FireServer("filterClick", {name = bp.prop, itemType = "Props"})
                        task.wait(0.04)
                    end
                    clearTools:FireServer("RequestingPropName", bp.prop, bp.cat)
                end)
                task.wait(0.22)

                pcall(function() toolPropMake:FireServer(groundInst or ws.Terrain, gorillaPos) end)

                local searchStart = tick()
                while tick() - searchStart < 1.3 do
                    for _, folder in pairs(wc:GetChildren()) do
                        for _, prop in pairs(folder:GetChildren()) do
                            if prop.Name == myPropName then
                                local scf = prop:FindFirstChild("SetCurrentCFrame")
                                local cpc = prop:FindFirstChild("ChangePropColor")
                                if scf and not knownScf[scf] then
                                    foundProp = prop
                                    foundScf = scf
                                    foundCpc = cpc
                                    break
                                end
                            end
                        end
                        if foundScf then break end
                    end
                    if foundScf then break end
                    task.wait(0.08)
                end

                if foundScf then
                    knownScf[foundScf] = true
                    break
                else
                    task.wait(0.4)
                end
            end

            local worker = {
                key = bp.key,
                prop = foundProp,
                scf = foundScf,
                cpc = foundCpc,
                targetCF = initialCF,
                lastSentCF = nil,
                lastSendTimeCF = 0
            }
            gorillaWorkers[bp.key] = worker

            if foundScf then
                pcall(function() foundScf:InvokeServer(initialCF) end)
            end
            local initialCol = getPartColor(currentTheme, bp.key)
            paintGorillaWorker(worker, initialCol)

            startWorker(worker)
            if idx < #GORILLA_BLUEPRINT then
                task.wait(0.2)
            end
        end

        task.wait(1.0)
        pcall(function()
            toolRE:InvokeServer("PickingTools", "PropMaker")
        end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        if gorillaLoop then gorillaLoop:Disconnect() gorillaLoop = nil end
        gorillaLoop = rs.Heartbeat:Connect(function(dt)
            if not gorillaActive then return end
            animTime = animTime + dt

            if isRainbowMode then
                rainbowHue = (rainbowHue + dt * 0.4) % 1
                local now = tick()
                if now - lastRainbowSync > 0.08 then
                    lastRainbowSync = now
                    rainbowIndex = (rainbowIndex % #RAINBOW_COLORS) + 1
                    local brickName = RAINBOW_BRICKS[rainbowIndex]
                    for idx, bp in ipairs(GORILLA_BLUEPRINT) do
                        local w = gorillaWorkers[bp.key]
                        if w and w.prop then
                            local partHue = (rainbowHue + (idx / #GORILLA_BLUEPRINT)) % 1
                            local rainbowPartCol = Color3.fromHSV(partHue, 0.9, 1.0)
                            for _, d in ipairs(w.prop:GetDescendants()) do
                                if d:IsA("BasePart") then
                                    pcall(function()
                                        d.Material = Enum.Material.Neon
                                        d.Color = rainbowPartCol
                                    end)
                                end
                            end
                            if w.cpc then
                                pcall(function() w.cpc:InvokeServer(brickName) end)
                            end
                        end
                    end
                end
            end

            if gorillaFollowActive then
                local cc = lp.Character
                local hrp = cc and cc:FindFirstChild("HumanoidRootPart")
                if hrp then
                    local targetPos = hrp.Position
                    local diff = targetPos - gorillaPos
                    local dist = Vector3.new(diff.X, 0, diff.Z).Magnitude
                    local minFollowDist = 12.0
                    if dist > minFollowDist then
                        local dir = Vector3.new(diff.X, 0, diff.Z).Unit
                        local targetYaw = math.atan2(-dir.X, -dir.Z)
                        local diffYaw = (targetYaw - gorillaYaw + math.pi) % (2 * math.pi) - math.pi
                        gorillaYaw = gorillaYaw + diffYaw * math.clamp(dt * 3.5, 0, 1)

                        local moveSpeed = 18.0
                        gorillaPos = gorillaPos + dir * math.min(moveSpeed * dt, dist - minFollowDist)
                    end
                end
            end

            
            local safeGroundVec, _ = getSafeGround(gorillaPos)
            local targetGroundY = safeGroundVec.Y
            local diffY = targetGroundY - gorillaPos.Y

            if diffY < -0.1 then
                
                gorillaFallVelocity = gorillaFallVelocity + (workspace.Gravity * dt)
                local fallStep = gorillaFallVelocity * dt
                if -diffY <= fallStep then
                    gorillaPos = Vector3.new(gorillaPos.X, targetGroundY, gorillaPos.Z)
                    gorillaFallVelocity = 0
                else
                    gorillaPos = Vector3.new(gorillaPos.X, gorillaPos.Y - fallStep, gorillaPos.Z)
                end
            elseif diffY > 0.1 then
                
                gorillaFallVelocity = 0
                local stepUp = math.clamp(diffY * math.clamp(dt * 12, 0, 1), 0, 25 * dt)
                gorillaPos = Vector3.new(gorillaPos.X, gorillaPos.Y + stepUp, gorillaPos.Z)
            else
                gorillaFallVelocity = 0
                gorillaPos = Vector3.new(gorillaPos.X, targetGroundY, gorillaPos.Z)
            end

            local anchorCF = CFrame.new(gorillaPos) * CFrame.Angles(0, gorillaYaw, 0)
            for _, bp in ipairs(GORILLA_BLUEPRINT) do
                local w = gorillaWorkers[bp.key]
                if w then
                    w.targetCF = computeGorillaPartCFrame(bp.key, anchorCF, animTime)
                end
            end
        end)

        gNotif("Colossal Gorilla", "Silverback Gorilla Assembled!", 3)
    end

    local function clearGorilla()
        gorillaActive = false
        gorillaFollowActive = false
        gorillaFallVelocity = 0
        gorillaWorkers = {}
        if gorillaLoop then
            gorillaLoop:Disconnect()
            gorillaLoop = nil
        end
        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        pcall(function()
            clearTools:FireServer("ClearAllProps")
        end)
        gNotif("Colossal Gorilla", "Gorilla dismissed.", 2)
    end

    
    BuildV2Tab:AddSection({"> Colossal Silverback Gorilla"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Silverback Gorilla",
        Description = "Summon an imposing 15-props Silverback Gorilla with realistic anatomy",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not gorillaActive then return end
            if val then
                task.spawn(spawnGorilla)
            else
                clearGorilla()
            end
        end
    })

    BuildV2Tab:AddToggle({
        Name = "Follow Me",
        Description = "Colossal gorilla stalks and follows your footsteps smoothly",
        Default = false,
        Callback = function(val)
            gorillaFollowActive = val
            gNotif("Colossal Gorilla", val and "Gorilla is now following you!" or "Gorilla stopped following.", 2)
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Gorilla Color",
        Description = "Choose gorilla color theme or animated rainbow",
        Options = {
            "Realistic (Default)",
            "Full Black",
            "Dark Brown",
            "Albino",
            "Red & Black",
            "Rainbow"
        },
        Default = "Realistic (Default)",
        Callback = function(choice)
            applyTheme(choice)
        end
    })

    BuildV2Tab:AddButton({
        Name = "Teleport In Front",
        Description = "Reposition the gorilla directly in front of you",
        Callback = function()
            if gorillaActive then
                local anchorCF = getGroundAnchor()
                gorillaPos = anchorCF.Position
                gorillaYaw = math.atan2(-anchorCF.LookVector.X, -anchorCF.LookVector.Z)
                gNotif("Colossal Gorilla", "Repositioned in front of you!", 2)
            end
        end
    })
end

do
    local lp = game:GetService("Players").LocalPlayer
    local rs = game:GetService("RunService")
    local rep = game:GetService("ReplicatedStorage")

    local mechaActive = false
    local mechaLoop = nil
    local currentMechOrigin = nil
    local myPropName = "Prop" .. lp.Name

    local savedHipHeight = 2.0
    local savedWalkSpeed = 16
    local savedJumpPower = 50
    local savedUseJumpPower = false
    local currentHipHeightVal = 62.0
    local savedTransparencies = {}

    local mechaColorTheme = "Default"
    local mechaPartsList = {}

    -- [LOCKED: WORK PERFECTLY - VALIDATED BY KEITO]
    local mechaConfig = {
        size = 11.0,
        height = 1.0,
        playerLift = 62.0,
        posZ = 0.0,
        posX = 0.0,
        rotY = 0.0,
        walkSpeed = 40.0,
        animSpeed = 0.85,
        torsoRotX = 90.0,
        torsoRotY = 0.0,
        torsoRotZ = 0.0,
        torsoOffsetX = 1.0,
        torsoOffsetY = -2.0,
        torsoOffsetZ = -3.0,
        armWidth = 1.03,
        armSpacing = 5.45,
        legWidth = 0.74,
        legOffsetY = 0.0,
        legSpacing = 0.0,
        pelvisOffsetX = -2.00,
        pelvisOffsetY = -3.50,
        pelvisOffsetZ = -1.00,
        pelvisRotX = 0.00,
        pelvisRotY = 85.00,
        pelvisRotZ = 0.00,
    }

    local MECHA_THEMES = {
        ["Obsidian"] = Color3.fromRGB(15, 15, 18),
        ["Gold"] = Color3.fromRGB(255, 215, 0),
        ["Crimson"] = Color3.fromRGB(220, 20, 35),
        ["Violet"] = Color3.fromRGB(150, 40, 240),
        ["Acid"] = Color3.fromRGB(50, 255, 60),
        ["Ice"] = Color3.fromRGB(0, 230, 255),
        ["Magma"] = Color3.fromRGB(255, 115, 0),
    }

    local function mNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function applyPlayerLift(newVal)
        local char = lp.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hum and hrp then
                hum.HipHeight = newVal
                if newVal > 3.0 then
                    hrp.CFrame = hrp.CFrame + Vector3.new(0, 0.5, 0)
                end
            end
        end
    end

    local function smoothClimbTo(targetVal, step)
        step = step or 2.0
        task.spawn(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local current = (hum and hum.HipHeight) or 2.0
            if current < targetVal then
                for v = current + step, targetVal, step do
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                applyPlayerLift(targetVal)
            else
                for v = current - step, targetVal, -step do
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                applyPlayerLift(targetVal)
            end
        end)
    end

    local function setCharacterHidden(hidden)
        local char = lp.Character
        if not char then return end
        if hidden then
            savedTransparencies = {}
            for _, part in pairs(char:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("Decal") then
                    savedTransparencies[part] = part.Transparency
                    part.Transparency = 1
                end
            end
        else
            for part, origVal in pairs(savedTransparencies) do
                if part and part.Parent then
                    part.Transparency = origVal
                end
            end
            savedTransparencies = {}
        end
    end

    local function getOrFetchPropMaker()
        local backpack = lp:FindFirstChild("Backpack")
        local character = lp.Character
        if not backpack or not character then return nil end
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        local tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
        if not tool then
            pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
            local start = tick()
            while tick() - start < 2.0 do
                tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
                if tool then break end
                task.wait(0.05)
            end
        end
        if not tool then return nil end

        if tool.Parent ~= character then
            local hum = character:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:EquipTool(tool)
                local eqStart = tick()
                while tick() - eqStart < 2.0 do
                    if tool.Parent == character then break end
                    task.wait(0.05)
                end
            end
        end
        return tool
    end

    local function duplicateExistingProp(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
        if not dupRemote then return false end

        for i = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
            task.wait(0.05)
        end
        return true
    end

    
    local senders = {}
    local RATE_LIMIT = 0.03

    local function sendCF(scf, cf, prop)
        if not mechaActive or not scf then return end

        if prop then
            pcall(function()
                if prop:IsA("Model") then
                    prop:PivotTo(cf)
                elseif prop:IsA("BasePart") then
                    prop.CFrame = cf
                end
            end)
        end

        local entry = senders[scf]
        local now = tick()
        if not entry then
            entry = { lastSent = 0, nextCF = cf, sending = false }
            senders[scf] = entry
        else
            entry.nextCF = cf
        end

        if not entry.sending and (now - entry.lastSent >= RATE_LIMIT) then
            entry.sending = true
            entry.lastSent = now
            local toSend = entry.nextCF
            entry.nextCF = nil
            task.spawn(function()
                pcall(function() scf:InvokeServer(toSend) end)
                entry.sending = false
                if mechaActive and entry.nextCF and (tick() - entry.lastSent >= RATE_LIMIT) then
                    entry.sending = true
                    entry.lastSent = tick()
                    local nextSend = entry.nextCF
                    entry.nextCF = nil
                    task.spawn(function()
                        pcall(function() scf:InvokeServer(nextSend) end)
                        entry.sending = false
                    end)
                end
            end)
        end
    end

    local function applyThemeToPart(prop, cpc, color)
        if prop then
            pcall(function()
                for _, p in ipairs(prop:GetDescendants()) do
                    if p:IsA("BasePart") then
                        p.Color = color
                    end
                end
            end)
        end
        if cpc then
            task.spawn(function()
                pcall(function() cpc:InvokeServer(color) end)
            end)
        end
    end

    local function applyMechaTheme(themeName)
        mechaColorTheme = themeName
        if not mechaActive or #mechaPartsList == 0 then return end

        if themeName == "Default" then
            for _, entry in ipairs(mechaPartsList) do
                local col = (entry.name == "UpperTorso" or entry.name == "LowerTorso")
                    and Color3.fromRGB(115, 120, 130)
                    or Color3.fromRGB(145, 150, 160)
                applyThemeToPart(entry.prop, entry.cpc, col)
            end
            mNotif("Mecha Robot", "Theme: Default", 2)
            return
        elseif themeName == "Rainbow" then
            mNotif("Mecha Robot", "Theme: Rainbow", 2)
            return
        elseif themeName == "Cyberpunk" then
            for i, entry in ipairs(mechaPartsList) do
                local col = (i % 2 == 0) and Color3.fromRGB(0, 235, 255) or Color3.fromRGB(255, 30, 150)
                applyThemeToPart(entry.prop, entry.cpc, col)
            end
            mNotif("Mecha Robot", "Theme: Cyberpunk", 2)
            return
        end

        local solidCol = MECHA_THEMES[themeName] or Color3.fromRGB(145, 150, 160)
        for _, entry in ipairs(mechaPartsList) do
            applyThemeToPart(entry.prop, entry.cpc, solidCol)
        end
        mNotif("Mecha Robot", "Theme: " .. themeName, 2)
    end

    local function stopMecha()
        if not mechaActive then return end
        mechaActive = false
        if mechaLoop then
            mechaLoop:Disconnect()
            mechaLoop = nil
        end
        currentMechOrigin = nil
        senders = {}
        mechaPartsList = {}

        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.WalkSpeed = savedWalkSpeed or 16
            if savedJumpPower then hum.JumpPower = savedJumpPower end
            if savedUseJumpPower ~= nil then hum.UseJumpPower = savedUseJumpPower end
            pcall(function() hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true) end)
            for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
                if track.Priority == Enum.AnimationPriority.Action4 or track.Name:lower():find("run") then
                    pcall(function() track:Stop(0.1) end)
                end
            end
            pcall(function() hum:ChangeState(Enum.HumanoidStateType.Landed) end)
        end

        smoothClimbTo(savedHipHeight or 2.0, 4.0)
        setCharacterHidden(false)

        task.wait(0.15)
        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.3)
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        mNotif("Mecha Robot", "Mecha Robot dismantled.", 2)
    end

    local function getTargetSurface(targetWorldPos)
        local street = workspace:FindFirstChild("Model") and workspace.Model:FindFirstChild("Street") and workspace.Model.Street:FindFirstChild("Street")
        local rayParams = RaycastParams.new()
        rayParams.FilterDescendantsInstances = {lp.Character, workspace:FindFirstChild("WorkspaceCom")}
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        local ray = workspace:Raycast(targetWorldPos + Vector3.new(0, 10, 0), Vector3.new(0, -30, 0), rayParams)
        local hitInst = street or (ray and ray.Instance) or workspace.Terrain
        local hitPos = ray and ray.Position or targetWorldPos

        return hitInst, hitPos
    end

    local function spawnMecha()
        if mechaActive then stopMecha() task.wait(0.5) end
        mechaActive = true
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:WaitForChild("Humanoid")

        savedHipHeight = hum.HipHeight
        savedWalkSpeed = hum.WalkSpeed
        savedJumpPower = hum.JumpPower
        savedUseJumpPower = hum.UseJumpPower
        hum.WalkSpeed = mechaConfig.walkSpeed
        hum.UseJumpPower = true
        hum.JumpPower = 110
        pcall(function() hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true) end)
        mNotif("Mecha Robot", "Assembling Mecha Robot...", 4)

        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then mNotif("Mecha Robot", "PropMaker missing!", 3) return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local wc = workspace:FindFirstChild("WorkspaceCom")
        if not wc then mNotif("Mecha Robot", "WorkspaceCom missing!", 3) return end

        local bunkerTarget = hrp.Position + (hrp.CFrame.LookVector * 14)
        local turbineTarget = hrp.Position - (hrp.CFrame.LookVector * 10)

        local bunkerHitInst, bunkerHitPos = getTargetSurface(bunkerTarget)
        local turbineHitInst, turbineHitPos = getTargetSurface(turbineTarget)

        local Telemetry = rep:FindFirstChild("Remotes") and rep.Remotes:FindFirstChild("TelemetryClientInteraction")
        local existingProps = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    existingProps[prop] = true
                end
            end
        end

        local function spawnSingleProp(propName, categoryName, targetInst, targetPos)
            local tool = getOrFetchPropMaker()
            if not tool then return nil end

            if Telemetry then
                pcall(function() Telemetry:FireServer("filterClick", {name = categoryName, itemType = "Props"}) end)
                task.wait(0.05)
                pcall(function() Telemetry:FireServer("filterClick", {name = propName, itemType = "Props"}) end)
                task.wait(0.05)
            end

            pcall(function() clearTools:FireServer("RequestingPropName", propName, categoryName) end)
            task.wait(0.3)

            local currentHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if currentHum and tool.Parent ~= lp.Character then
                currentHum:EquipTool(tool)
                task.wait(0.1)
            end

            local tpm = tool:FindFirstChild("Tool_PropMake")
            local startW = tick()
            while not tpm and tick() - startW < 4.0 and mechaActive do
                tpm = tool:FindFirstChild("Tool_PropMake")
                if not tpm then
                    local bp = lp.Backpack:FindFirstChild("PropMaker")
                    if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
                end
                if not tpm then task.wait(0.1) end
            end

            if not tpm then return nil end

            local useInst = targetInst or bunkerHitInst
            local usePos = targetPos or bunkerHitPos
            pcall(function() tpm:FireServer(useInst, usePos) end)

            local spawned = nil
            local t0 = tick()
            while tick() - t0 < 4.0 and mechaActive do
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        if prop.Name == myPropName and not existingProps[prop] then
                            spawned = prop
                            existingProps[prop] = true
                            break
                        end
                    end
                    if spawned then break end
                end
                if spawned then break end
                task.wait(0.05)
            end

            return spawned
        end

        
        local chestProp = spawnSingleProp("BigGunBunker", "Big", bunkerHitInst, bunkerHitPos)
        if not chestProp then
            chestProp = spawnSingleProp("BigGunBunker", "Big", bunkerHitInst, bunkerHitPos)
        end
        if not chestProp then mNotif("Mecha Robot", "Torso prop failed!", 3) return end

        
        local firstTurbine = spawnSingleProp("PowerTurbine", "Barrel", turbineHitInst, turbineHitPos)
        if not firstTurbine then
            firstTurbine = spawnSingleProp("PowerTurbine", "001_TrafficCones", turbineHitInst, turbineHitPos)
        end
        if not firstTurbine then
            firstTurbine = spawnSingleProp("PowerTurbine", "Barrel", turbineHitInst, turbineHitPos)
        end
        if not firstTurbine then mNotif("Mecha Robot", "Turbine prop failed!", 3) return end

        
        duplicateExistingProp(firstTurbine, 13)
        task.wait(0.8)

        -- Retrait du carton apres 1 seconde (LOCKED)
        task.wait(1.0)
        pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        
        task.wait(0.3)
        local turbineProps = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and prop ~= chestProp then
                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                    local cpc = prop:FindFirstChild("ChangePropColor")
                    if scf then
                        table.insert(turbineProps, {prop = prop, scf = scf, cpc = cpc})
                    end
                end
            end
        end

        local chestScf = chestProp:FindFirstChild("SetCurrentCFrame") or chestProp:FindFirstChild("SetCurrentCFrame", true) or chestProp:FindFirstChildWhichIsA("RemoteFunction", true)
        local chestCpc = chestProp:FindFirstChild("ChangePropColor") or chestProp:FindFirstChild("ChangePropColor", true)
        if not chestScf then mNotif("Mecha Robot", "Torso SCF missing!", 3) return end

        
        task.wait(0.3)
        smoothClimbTo(mechaConfig.playerLift, 2.0)

        
        setCharacterHidden(true)

        
        local MECHA_PARTS = {
            { name = "UpperTorso", scf = chestScf, prop = chestProp, cpc = chestCpc, offset = CFrame.new(0, -2.5, 0) },
            { name = "Head", scf = turbineProps[1] and turbineProps[1].scf, prop = turbineProps[1] and turbineProps[1].prop, cpc = turbineProps[1] and turbineProps[1].cpc, offset = CFrame.new(0, 1.5, 0) * CFrame.Angles(math.rad(-90), 0, 0) },
            { name = "LowerTorso", scf = turbineProps[2] and turbineProps[2].scf, prop = turbineProps[2] and turbineProps[2].prop, cpc = turbineProps[2] and turbineProps[2].cpc, offset = CFrame.Angles(0, 0, math.rad(90)) },

            { name = "LeftUpperArm", scf = turbineProps[3] and turbineProps[3].scf, prop = turbineProps[3] and turbineProps[3].prop, cpc = turbineProps[3] and turbineProps[3].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftLowerArm", scf = turbineProps[4] and turbineProps[4].scf, prop = turbineProps[4] and turbineProps[4].prop, cpc = turbineProps[4] and turbineProps[4].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftHand",     scf = turbineProps[5] and turbineProps[5].scf, prop = turbineProps[5] and turbineProps[5].prop, cpc = turbineProps[5] and turbineProps[5].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },

            { name = "RightUpperArm", scf = turbineProps[6] and turbineProps[6].scf, prop = turbineProps[6] and turbineProps[6].prop, cpc = turbineProps[6] and turbineProps[6].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightLowerArm", scf = turbineProps[7] and turbineProps[7].scf, prop = turbineProps[7] and turbineProps[7].prop, cpc = turbineProps[7] and turbineProps[7].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightHand",     scf = turbineProps[8] and turbineProps[8].scf, prop = turbineProps[8] and turbineProps[8].prop, cpc = turbineProps[8] and turbineProps[8].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },

            { name = "LeftUpperLeg", scf = turbineProps[9] and turbineProps[9].scf, prop = turbineProps[9] and turbineProps[9].prop, cpc = turbineProps[9] and turbineProps[9].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftLowerLeg", scf = turbineProps[10] and turbineProps[10].scf, prop = turbineProps[10] and turbineProps[10].prop, cpc = turbineProps[10] and turbineProps[10].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftFoot",     scf = turbineProps[11] and turbineProps[11].scf, prop = turbineProps[11] and turbineProps[11].prop, cpc = turbineProps[11] and turbineProps[11].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },

            { name = "RightUpperLeg", scf = turbineProps[12] and turbineProps[12].scf, prop = turbineProps[12] and turbineProps[12].prop, cpc = turbineProps[12] and turbineProps[12].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightLowerLeg", scf = turbineProps[13] and turbineProps[13].scf, prop = turbineProps[13] and turbineProps[13].prop, cpc = turbineProps[13] and turbineProps[13].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightFoot",     scf = turbineProps[14] and turbineProps[14].scf, prop = turbineProps[14] and turbineProps[14].prop, cpc = turbineProps[14] and turbineProps[14].cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
        }
        mechaPartsList = MECHA_PARTS

        applyMechaTheme(mechaColorTheme)
        mNotif("Mecha Robot", "Mecha Robot ready! Pilote active.", 3)

        if mechaLoop then mechaLoop:Disconnect() mechaLoop = nil end
        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        mechaLoop = rs.RenderStepped:Connect(function()
            if not mechaActive then return end

            local cc = lp.Character
            if not cc then return end
            local hrpNow = cc:FindFirstChild("HumanoidRootPart")
            if not hrpNow then return end
            local humNow = cc:FindFirstChildOfClass("Humanoid")

            
            
            local rayFilter = {cc, workspace:FindFirstChild("WorkspaceCom")}
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr.Character and plr.Character ~= cc then
                    table.insert(rayFilter, plr.Character)
                end
            end
            rayParams.FilterDescendantsInstances = rayFilter
            local ray = workspace:Raycast(hrpNow.Position + Vector3.new(0, 10, 0), Vector3.new(0, -250, 0), rayParams)
            local groundY = ray and ray.Position.Y
            local minFloorY = groundY and (groundY + currentHipHeightVal) or nil

            if minFloorY and hrpNow.Position.Y < minFloorY then
                local vel = hrpNow.AssemblyLinearVelocity
                if vel.Y < 0 then
                    hrpNow.AssemblyLinearVelocity = Vector3.new(vel.X, 0, vel.Z)
                end
                hrpNow.CFrame = CFrame.new(hrpNow.Position.X, minFloorY, hrpNow.Position.Z) * hrpNow.CFrame.Rotation
                if humNow and humNow:GetState() == Enum.HumanoidStateType.Freefall then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Landed) end)
                end
            end

            local clampedY = hrpNow.Position.Y
            if minFloorY and clampedY < minFloorY then
                clampedY = minFloorY
            end
            local clampedHrpCF = CFrame.new(hrpNow.Position.X, clampedY, hrpNow.Position.Z) * hrpNow.CFrame.Rotation
            local targetCenterCF = clampedHrpCF * CFrame.new(mechaConfig.posX, mechaConfig.height, mechaConfig.posZ) * CFrame.Angles(0, math.rad(mechaConfig.rotY), 0)
            if not currentMechOrigin then
                currentMechOrigin = targetCenterCF
            else
                currentMechOrigin = currentMechOrigin:Lerp(targetCenterCF, 0.22)
            end
            local centerCF = currentMechOrigin

            for _, entry in ipairs(MECHA_PARTS) do
                if entry.scf then
                    local part = cc:FindFirstChild(entry.name)
                    if not part and entry.name == "UpperTorso" then
                        part = cc:FindFirstChild("Torso") or hrpNow
                    end
                    if part then
                        local rel = clampedHrpCF:ToObjectSpace(part.CFrame)

                        local xMult = 1.0
                        if entry.name:find("Arm") or entry.name:find("Hand") then
                            xMult = mechaConfig.armWidth
                        elseif entry.name:find("Leg") or entry.name:find("Foot") then
                            xMult = mechaConfig.legWidth
                        end

                        local scaledPos = Vector3.new(rel.Position.X * mechaConfig.size * xMult, rel.Position.Y * mechaConfig.size, rel.Position.Z * mechaConfig.size)

                        if entry.name:find("LowerArm") then
                            scaledPos = scaledPos + Vector3.new(0, -mechaConfig.armSpacing, 0)
                        elseif entry.name:find("Hand") then
                            scaledPos = scaledPos + Vector3.new(0, -mechaConfig.armSpacing * 2, 0)
                        end

                        if entry.name:find("Leg") or entry.name:find("Foot") then
                            scaledPos = scaledPos + Vector3.new(0, mechaConfig.legOffsetY, 0)
                            if entry.name:find("LowerLeg") then
                                scaledPos = scaledPos + Vector3.new(0, mechaConfig.legSpacing, 0)
                            elseif entry.name:find("UpperLeg") then
                                scaledPos = scaledPos + Vector3.new(0, mechaConfig.legSpacing * 2, 0)
                            end
                        end

                        local relRot = rel - rel.Position

                        local extraOffset = entry.offset or CFrame.new()
                        if entry.name == "UpperTorso" then
                            extraOffset = CFrame.new(mechaConfig.torsoOffsetX, mechaConfig.torsoOffsetY, mechaConfig.torsoOffsetZ)
                                * CFrame.Angles(math.rad(mechaConfig.torsoRotX), math.rad(mechaConfig.torsoRotY), math.rad(mechaConfig.torsoRotZ))
                        elseif entry.name == "LowerTorso" then
                            extraOffset = (entry.offset or CFrame.new())
                                * CFrame.new(mechaConfig.pelvisOffsetX, mechaConfig.pelvisOffsetY, mechaConfig.pelvisOffsetZ)
                                * CFrame.Angles(math.rad(mechaConfig.pelvisRotX), math.rad(mechaConfig.pelvisRotY), math.rad(mechaConfig.pelvisRotZ))
                        end

                        local targetCF = centerCF * CFrame.new(scaledPos) * relRot * extraOffset

                        sendCF(entry.scf, targetCF, entry.prop)
                    end
                end
            end

            
            if humNow and humNow.MoveDirection.Magnitude > 0.05 then
                if humNow:GetState() ~= Enum.HumanoidStateType.Running then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Running) end)
                end
                if humNow.WalkSpeed < mechaConfig.walkSpeed then
                    humNow.WalkSpeed = mechaConfig.walkSpeed
                end

                local hasRun = false
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("idle") or animName:find("walk") then
                        track:Stop(0.05)
                    elseif animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        hasRun = true
                        track:AdjustSpeed(mechaConfig.animSpeed)
                    end
                end

                if not hasRun then
                    local animScript = cc:FindFirstChild("Animate")
                    local runAnim = animScript and animScript:FindFirstChild("run") and animScript.run:FindFirstChildOfClass("Animation")
                    if runAnim then
                        pcall(function()
                            local runTrack = humNow:LoadAnimation(runAnim)
                            runTrack.Priority = Enum.AnimationPriority.Action4
                            runTrack:Play(0.05)
                            runTrack:AdjustSpeed(mechaConfig.animSpeed)
                        end)
                    end
                end
            else
                
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        track:Stop(0.1)
                    end
                end
            end

            
            if humNow and humNow.HipHeight ~= mechaConfig.playerLift and currentHipHeightVal >= mechaConfig.playerLift then
                humNow.HipHeight = mechaConfig.playerLift
            end

            
            if mechaColorTheme == "Rainbow" then
                local nowTick = tick()
                for i, entry in ipairs(MECHA_PARTS) do
                    local waveColor = Color3.fromHSV((nowTick * 0.4 + (i / #MECHA_PARTS)) % 1, 1, 1)

                    if entry.prop then
                        pcall(function()
                            for _, p in ipairs(entry.prop:GetDescendants()) do
                                if p:IsA("BasePart") then
                                    p.Color = waveColor
                                end
                            end
                        end)
                    end

                    if entry.cpc and (nowTick - (entry.lastColorSent or 0) >= 0.25) then
                        entry.lastColorSent = nowTick
                        task.spawn(function()
                            pcall(function() entry.cpc:InvokeServer(waveColor) end)
                        end)
                    end
                end
            end
        end)
    end

    
    
    
    BuildV2Tab:AddSection({"> Mecha Robot"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Mecha Robot",
        Description = "Pilote a giant 15-prop humanoid mecha with real-time R15 motion replication",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not mechaActive then return end
            if val then
                task.spawn(spawnMecha)
            else
                stopMecha()
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Mecha Color Theme",
        Description = "Select robot color or animated wave",
        Options = {
            "Default",
            "Rainbow",
            "Cyberpunk",
            "Obsidian",
            "Gold",
            "Crimson",
            "Violet",
            "Acid",
            "Ice",
            "Magma"
        },
        Default = "Default",
        Callback = function(choice)
            applyMechaTheme(choice)
        end
    })
end

do
    local lp = game:GetService("Players").LocalPlayer
    local rs = game:GetService("RunService")
    local rep = game:GetService("ReplicatedStorage")

    local titanActive = false
    local titanLoop = nil
    local titanPhysicsConn = nil
    local myPropName = "Prop" .. lp.Name

    local savedHipHeight = 2.0
    local savedWalkSpeed = 16
    local currentHipHeightVal = 62.0
    local savedTransparencies = {}

    local titanColorTheme = "Default"
    local titanPartsList = {}

    local titanConfig = {
        propName = "BigOutToilet",
        category = "Building",
        size = 6.8,
        height = 0.0,
        posZ = 0.0,
        posX = 0.0,
        rotY = 0.0,
        walkSpeed = 40.0,
        animSpeed = 0.85,
        armWidth = 0.90,
        armSpacing = 0.0,
        legWidth = 0.72,
        legOffsetY = 0.0,
        legSpacing = 0.0,
    }

    local TITAN_THEMES = {
        ["Obsidian"] = Color3.fromRGB(15, 15, 18),
        ["Gold"] = Color3.fromRGB(255, 215, 0),
        ["Crimson"] = Color3.fromRGB(220, 20, 35),
        ["Violet"] = Color3.fromRGB(150, 40, 240),
        ["Acid"] = Color3.fromRGB(50, 255, 60),
        ["Ice"] = Color3.fromRGB(0, 230, 255),
        ["Magma"] = Color3.fromRGB(255, 115, 0),
    }

    local function tNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function applyPlayerLift(newVal)
        local char = lp.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hum and hrp then
                hum.HipHeight = newVal
                if newVal > 3.0 then
                    hrp.CFrame = hrp.CFrame + Vector3.new(0, 0.5, 0)
                end
            end
        end
    end

    local function smoothClimbTo(targetVal, step)
        step = step or 3.0
        task.spawn(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local current = (hum and hum.HipHeight) or 2.0
            if current < targetVal then
                for v = current + step, targetVal, step do
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                applyPlayerLift(targetVal)
            else
                for v = current - step, targetVal, -step do
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                applyPlayerLift(targetVal)
            end
        end)
    end

    local function setCharacterHidden(hidden)
        local char = lp.Character
        if not char then return end
        if hidden then
            for _, part in pairs(char:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("Decal") then
                    if not savedTransparencies[part] then
                        savedTransparencies[part] = part.Transparency
                    end
                    part.Transparency = 1
                end
            end
        else
            for part, origVal in pairs(savedTransparencies) do
                if part and part.Parent then
                    pcall(function() part.Transparency = origVal end)
                end
            end
            savedTransparencies = {}
        end
    end

    local function getOrFetchPropMaker()
        local backpack = lp:FindFirstChild("Backpack")
        local character = lp.Character
        if not backpack or not character then return nil end
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        local tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
        if not tool then
            pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
            local start = tick()
            while tick() - start < 2.0 do
                tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
                if tool then break end
                task.wait(0.05)
            end
        end
        if not tool then return nil end

        if tool.Parent ~= character then
            local hum = character:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:EquipTool(tool)
                local eqStart = tick()
                while tick() - eqStart < 2.0 do
                    if tool.Parent == character then break end
                    task.wait(0.05)
                end
            end
        end
        return tool
    end

    local function duplicateExistingProp(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
        if not dupRemote then return false end

        for i = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
            task.wait(0.05)
        end
        return true
    end

    local senders = {}
    local RATE_LIMIT = 0.03

    local function sendCF(scf, cf, prop)
        if not titanActive or not scf then return end

        if prop then
            pcall(function()
                if prop:IsA("Model") then
                    prop:PivotTo(cf)
                elseif prop:IsA("BasePart") then
                    prop.CFrame = cf
                end
            end)
        end

        local entry = senders[scf]
        local now = tick()
        if not entry then
            entry = { lastSent = 0, nextCF = cf, sending = false }
            senders[scf] = entry
        else
            entry.nextCF = cf
        end

        if not entry.sending and (now - entry.lastSent >= RATE_LIMIT) then
            entry.sending = true
            entry.lastSent = now
            local toSend = entry.nextCF
            entry.nextCF = nil
            task.spawn(function()
                pcall(function() scf:InvokeServer(toSend) end)
                entry.sending = false
                if titanActive and entry.nextCF and (tick() - entry.lastSent >= RATE_LIMIT) then
                    entry.sending = true
                    entry.lastSent = tick()
                    local nextSend = entry.nextCF
                    entry.nextCF = nil
                    task.spawn(function()
                        pcall(function() scf:InvokeServer(nextSend) end)
                        entry.sending = false
                    end)
                end
            end)
        end
    end

    local function applyThemeToPart(prop, cpc, color)
        if prop then
            pcall(function()
                for _, p in ipairs(prop:GetDescendants()) do
                    if p:IsA("BasePart") then
                        p.Color = color
                    end
                end
            end)
        end
        if cpc then
            task.spawn(function()
                pcall(function() cpc:InvokeServer(color) end)
            end)
        end
    end

    local function applyToiletTitanTheme(themeName)
        titanColorTheme = themeName
        if not titanActive or #titanPartsList == 0 then return end

        if themeName == "Default" then
            for _, entry in ipairs(titanPartsList) do
                local col = (entry.name == "UpperTorso" or entry.name == "LowerTorso")
                    and Color3.fromRGB(163, 162, 165)
                    or Color3.fromRGB(180, 180, 185)
                applyThemeToPart(entry.prop, entry.cpc, col)
            end
            tNotif("Toilet Titan", "Theme: Default", 2)
            return
        elseif themeName == "Rainbow" then
            tNotif("Toilet Titan", "Theme: Rainbow", 2)
            return
        elseif themeName == "Cyberpunk" then
            for i, entry in ipairs(titanPartsList) do
                local col = (i % 2 == 0) and Color3.fromRGB(0, 235, 255) or Color3.fromRGB(255, 30, 150)
                applyThemeToPart(entry.prop, entry.cpc, col)
            end
            tNotif("Toilet Titan", "Theme: Cyberpunk", 2)
            return
        end

        local solidCol = TITAN_THEMES[themeName] or Color3.fromRGB(163, 162, 165)
        for _, entry in ipairs(titanPartsList) do
            applyThemeToPart(entry.prop, entry.cpc, solidCol)
        end
        tNotif("Toilet Titan", "Theme: " .. themeName, 2)
    end

    local function stopToiletTitan()
        if not titanActive then return end
        titanActive = false
        if titanLoop then
            titanLoop:Disconnect()
            titanLoop = nil
        end
        if titanPhysicsConn then
            titanPhysicsConn:Disconnect()
            titanPhysicsConn = nil
        end
        senders = {}
        titanPartsList = {}

        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.WalkSpeed = savedWalkSpeed or 16
            pcall(function()
                hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true)
                hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
                hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, true)
            end)
            for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
                if track.Priority == Enum.AnimationPriority.Action4 or track.Name:lower():find("run") then
                    pcall(function() track:Stop(0.1) end)
                end
            end
            pcall(function() hum:ChangeState(Enum.HumanoidStateType.Landed) end)
        end

        smoothClimbTo(savedHipHeight or 2.0, 4.0)
        setCharacterHidden(false)

        task.wait(0.15)
        pcall(function() rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
        task.wait(0.3)
        pcall(function() rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
        tNotif("Toilet Titan", "Titan offline and props cleared.", 3)
    end

    local function getTargetSurface(targetWorldPos)
        local street = workspace:FindFirstChild("Model") and workspace.Model:FindFirstChild("Street") and workspace.Model.Street:FindFirstChild("Street")

        local rayParams = RaycastParams.new()
        rayParams.FilterDescendantsInstances = {lp.Character, workspace:FindFirstChild("WorkspaceCom")}
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        local ray = workspace:Raycast(targetWorldPos + Vector3.new(0, 10, 0), Vector3.new(0, -30, 0), rayParams)
        local hitInst = street or (ray and ray.Instance) or workspace.Terrain
        local hitPos = ray and ray.Position or targetWorldPos

        return hitInst, hitPos
    end

    local function spawnToiletTitan()
        if titanActive then stopToiletTitan() task.wait(0.5) end
        titanActive = true
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:WaitForChild("Humanoid")
        savedHipHeight = hum.HipHeight
        savedWalkSpeed = hum.WalkSpeed
        hum.WalkSpeed = titanConfig.walkSpeed
        pcall(function()
            hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
        end)
        tNotif("Toilet Titan", "Spawning Toilet Titan Colossus...", 3)

        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        local Telemetry = rep:FindFirstChild("Remotes") and rep.Remotes:FindFirstChild("TelemetryClientInteraction")

        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then tNotif("Toilet Titan", "PropMaker missing!", 3) return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local wc = workspace:FindFirstChild("WorkspaceCom")
        if not wc then tNotif("Toilet Titan", "WorkspaceCom missing!", 3) return end

        local targetWorld = hrp.Position + (hrp.CFrame.LookVector * 14)
        local hitInst, hitPos = getTargetSurface(targetWorld)

        local existingProps = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    existingProps[prop] = true
                end
            end
        end

        local function spawnSingleProp(propName, categoryName, targetI, targetP)
            local tool = getOrFetchPropMaker()
            if not tool then return nil end

            if Telemetry then
                pcall(function() Telemetry:FireServer("filterClick", {name = categoryName, itemType = "Props"}) end)
                task.wait(0.05)
                pcall(function() Telemetry:FireServer("filterClick", {name = propName, itemType = "Props"}) end)
                task.wait(0.05)
            end

            pcall(function() clearTools:FireServer("RequestingPropName", propName, categoryName) end)
            task.wait(0.3)

            local currentHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if currentHum and tool.Parent ~= lp.Character then
                currentHum:EquipTool(tool)
                task.wait(0.1)
            end

            local tpm = tool:FindFirstChild("Tool_PropMake")
            local startW = tick()
            while not tpm and tick() - startW < 4.0 and titanActive do
                tpm = tool:FindFirstChild("Tool_PropMake")
                if not tpm then
                    local bp = lp.Backpack:FindFirstChild("PropMaker")
                    if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
                end
                if not tpm then task.wait(0.1) end
            end

            if not tpm then return nil end

            local useInst = targetI or hitInst
            local usePos = targetP or hitPos
            pcall(function() tpm:FireServer(useInst, usePos) end)

            local spawned = nil
            local t0 = tick()
            while tick() - t0 < 4.0 and titanActive do
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        if prop.Name == myPropName and not existingProps[prop] then
                            spawned = prop
                            existingProps[prop] = true
                            break
                        end
                    end
                    if spawned then break end
                end
                if spawned then break end
                task.wait(0.05)
            end

            return spawned
        end

        
        local firstToilet = spawnSingleProp(titanConfig.propName, titanConfig.category, hitInst, hitPos)
        if not firstToilet then
            firstToilet = spawnSingleProp(titanConfig.propName, titanConfig.category, hitInst, hitPos)
        end
        if not firstToilet then tNotif("Toilet Titan", "BigOutToilet spawn failed!", 3) return end

        
        duplicateExistingProp(firstToilet, 14)
        task.wait(0.8)

        -- 3. Retrait du carton apres 1 seconde (Pattern Brookhaven LOCKED)
        task.wait(1.0)
        pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        
        task.wait(0.3)
        local allToilets = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    local scf = prop:FindFirstChild("SetCurrentCFrame")
                    local cpc = prop:FindFirstChild("ChangePropColor")
                    if scf then
                        table.insert(allToilets, {prop = prop, scf = scf, cpc = cpc})
                        pcall(function()
                            for _, p in ipairs(prop:GetDescendants()) do
                                if p:IsA("BasePart") then
                                    p.CanCollide = false
                                    p.CanTouch = false
                                    p.CanQuery = false
                                    p.Massless = true
                                end
                            end
                        end)
                    end
                end
            end
        end

        if #allToilets < 15 then
            tNotif("Toilet Titan", string.format("Warning: %d/15 toilets ready, continuing...", #allToilets), 3)
        end

        
        hum.HipHeight = currentHipHeightVal
        hrp.CFrame = hrp.CFrame + Vector3.new(0, currentHipHeightVal - 2.0, 0)
        setCharacterHidden(true)
        task.wait(0.3)

        
        titanPhysicsConn = rs.Stepped:Connect(function()
            if not titanActive then return end
            local cc = lp.Character
            if cc then
                for _, part in ipairs(cc:GetDescendants()) do
                    if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                        part.CanCollide = false
                        part.CanTouch = false
                    end
                end
            end
            for _, propData in ipairs(allToilets) do
                if propData.prop then
                    for _, p in ipairs(propData.prop:GetDescendants()) do
                        if p:IsA("BasePart") then
                            p.CanCollide = false
                            p.CanTouch = false
                            p.CanQuery = false
                        end
                    end
                end
            end
        end)

        tNotif("Toilet Titan", string.format("Toilet Titan Active! (%d props)", #allToilets), 3)

        
        
        
        local MECHA_PARTS = {
            { name = "UpperTorso",   scf = allToilets[1] and allToilets[1].scf,   prop = allToilets[1] and allToilets[1].prop,   cpc = allToilets[1] and allToilets[1].cpc,   offset = CFrame.new(0, 0, 0) },
            { name = "Head",         scf = allToilets[2] and allToilets[2].scf,   prop = allToilets[2] and allToilets[2].prop,   cpc = allToilets[2] and allToilets[2].cpc,   offset = CFrame.new(0, 1.2, -0.2) * CFrame.Angles(math.rad(-8), 0, 0) },
            { name = "LowerTorso",   scf = allToilets[3] and allToilets[3].scf,   prop = allToilets[3] and allToilets[3].prop,   cpc = allToilets[3] and allToilets[3].cpc,   offset = CFrame.new(0, -1.0, 0) },

            { name = "LeftUpperArm", scf = allToilets[4] and allToilets[4].scf,   prop = allToilets[4] and allToilets[4].prop,   cpc = allToilets[4] and allToilets[4].cpc,   offset = CFrame.new(-0.8, 0.4, 0) * CFrame.Angles(0, math.rad(15), math.rad(22)) },
            { name = "LeftLowerArm", scf = allToilets[5] and allToilets[5].scf,   prop = allToilets[5] and allToilets[5].prop,   cpc = allToilets[5] and allToilets[5].cpc,   offset = CFrame.new(-0.6, -0.4, -0.4) * CFrame.Angles(math.rad(-15), math.rad(10), math.rad(12)) },
            { name = "LeftHand",     scf = allToilets[6] and allToilets[6].scf,   prop = allToilets[6] and allToilets[6].prop,   cpc = allToilets[6] and allToilets[6].cpc,   offset = CFrame.new(-0.4, -0.8, -0.8) * CFrame.Angles(math.rad(-20), 0, math.rad(8)) },

            { name = "RightUpperArm",scf = allToilets[7] and allToilets[7].scf,   prop = allToilets[7] and allToilets[7].prop,   cpc = allToilets[7] and allToilets[7].cpc,   offset = CFrame.new(0.8, 0.4, 0) * CFrame.Angles(0, math.rad(-15), math.rad(-22)) },
            { name = "RightLowerArm",scf = allToilets[8] and allToilets[8].scf,   prop = allToilets[8] and allToilets[8].prop,   cpc = allToilets[8] and allToilets[8].cpc,   offset = CFrame.new(0.6, -0.4, -0.4) * CFrame.Angles(math.rad(-15), math.rad(-10), math.rad(-12)) },
            { name = "RightHand",    scf = allToilets[9] and allToilets[9].scf,   prop = allToilets[9] and allToilets[9].prop,   cpc = allToilets[9] and allToilets[9].cpc,   offset = CFrame.new(0.4, -0.8, -0.8) * CFrame.Angles(math.rad(-20), 0, math.rad(-8)) },

            { name = "LeftUpperLeg", scf = allToilets[10] and allToilets[10].scf, prop = allToilets[10] and allToilets[10].prop, cpc = allToilets[10] and allToilets[10].cpc, offset = CFrame.new(-0.4, -0.4, 0) * CFrame.Angles(0, math.rad(5), math.rad(4)) },
            { name = "LeftLowerLeg", scf = allToilets[11] and allToilets[11].scf, prop = allToilets[11] and allToilets[11].prop, cpc = allToilets[11] and allToilets[11].cpc, offset = CFrame.new(-0.3, -0.6, 0) * CFrame.Angles(0, math.rad(5), math.rad(2)) },
            { name = "LeftFoot",     scf = allToilets[12] and allToilets[12].scf, prop = allToilets[12] and allToilets[12].prop, cpc = allToilets[12] and allToilets[12].cpc, offset = CFrame.new(-0.2, -0.8, 0.4) * CFrame.Angles(math.rad(6), 0, 0) },

            { name = "RightUpperLeg",scf = allToilets[13] and allToilets[13].scf, prop = allToilets[13] and allToilets[13].prop, cpc = allToilets[13] and allToilets[13].cpc, offset = CFrame.new(0.4, -0.4, 0) * CFrame.Angles(0, math.rad(-5), math.rad(-4)) },
            { name = "RightLowerLeg",scf = allToilets[14] and allToilets[14].scf, prop = allToilets[14] and allToilets[14].prop, cpc = allToilets[14] and allToilets[14].cpc, offset = CFrame.new(0.3, -0.6, 0) * CFrame.Angles(0, math.rad(-5), math.rad(-2)) },
            { name = "RightFoot",    scf = allToilets[15] and allToilets[15].scf, prop = allToilets[15] and allToilets[15].prop, cpc = allToilets[15] and allToilets[15].cpc, offset = CFrame.new(0.2, -0.8, 0.4) * CFrame.Angles(math.rad(6), 0, 0) },
        }

        titanPartsList = MECHA_PARTS

        
        if titanColorTheme ~= "Default" then
            applyToiletTitanTheme(titanColorTheme)
        end

        local floorRayParams = RaycastParams.new()
        floorRayParams.FilterDescendantsInstances = {lp.Character, wc}
        floorRayParams.FilterType = Enum.RaycastFilterType.Exclude

        titanLoop = rs.RenderStepped:Connect(function()
            if not titanActive then return end

            local cc = lp.Character
            if not cc then return end
            local hrpNow = cc:FindFirstChild("HumanoidRootPart")
            local humNow = cc:FindFirstChildOfClass("Humanoid")
            if not hrpNow or not humNow then return end

            local hrpCF = hrpNow.CFrame
            local forwardFlat = (hrpCF.LookVector * Vector3.new(1, 0, 1)).Unit
            if forwardFlat.Magnitude < 0.001 then forwardFlat = hrpCF.LookVector end

            local groundRay = workspace:Raycast(hrpNow.Position, Vector3.new(0, -120, 0), floorRayParams)
            local groundY = groundRay and groundRay.Position.Y or (hrpNow.Position.Y - humNow.HipHeight - 1.5)

            local jumpDelta = math.max(0, (hrpNow.Position.Y - groundY) - humNow.HipHeight - 1.5)
            local baseHeight = 25.5 * (titanConfig.size / 6.8)
            local torsoCenterY = groundY + baseHeight + titanConfig.height + jumpDelta

            local centerCF = CFrame.new(hrpCF.Position.X, torsoCenterY, hrpCF.Position.Z)
                * CFrame.new(titanConfig.posX, 0, titanConfig.posZ)
                * CFrame.Angles(0, math.rad(titanConfig.rotY), 0)
                * (hrpCF - hrpCF.Position)

            
            for _, p in pairs(cc:GetDescendants()) do
                if (p:IsA("BasePart") or p:IsA("Decal")) and p.Transparency < 1 then
                    if not savedTransparencies[p] then
                        savedTransparencies[p] = p.Transparency
                    end
                    p.Transparency = 1
                end
            end

            
            for _, entry in ipairs(MECHA_PARTS) do
                if entry.scf then
                    local part = cc:FindFirstChild(entry.name)
                    if not part and entry.name == "UpperTorso" then
                        part = cc:FindFirstChild("Torso") or hrpNow
                    end

                    if part then
                        local rel = hrpCF:ToObjectSpace(part.CFrame)

                        local xMult = 1.0
                        if entry.name:find("Arm") or entry.name:find("Hand") then
                            xMult = titanConfig.armWidth
                        elseif entry.name:find("Leg") or entry.name:find("Foot") then
                            xMult = titanConfig.legWidth
                        end

                        local scaledPos = Vector3.new(
                            rel.Position.X * titanConfig.size * xMult,
                            rel.Position.Y * titanConfig.size,
                            rel.Position.Z * titanConfig.size
                        )

                        if entry.name:find("LowerArm") then
                            scaledPos = scaledPos + Vector3.new(0, -titanConfig.armSpacing, 0)
                        elseif entry.name:find("Hand") then
                            scaledPos = scaledPos + Vector3.new(0, -titanConfig.armSpacing * 2, 0)
                        end

                        if entry.name:find("Leg") or entry.name:find("Foot") then
                            scaledPos = scaledPos + Vector3.new(0, titanConfig.legOffsetY, 0)
                            if entry.name:find("LowerLeg") then
                                scaledPos = scaledPos + Vector3.new(0, titanConfig.legSpacing, 0)
                            elseif entry.name:find("Foot") then
                                scaledPos = scaledPos + Vector3.new(0, titanConfig.legSpacing * 2, 0)
                            end
                        end

                        local relRot = rel - rel.Position
                        local extraOffset = entry.offset or CFrame.new()

                        local targetCF = centerCF * CFrame.new(scaledPos) * relRot * extraOffset

                        sendCF(entry.scf, targetCF, entry.prop)
                    end
                end
            end

            
            if humNow and humNow.MoveDirection.Magnitude > 0.05 then
                if humNow:GetState() ~= Enum.HumanoidStateType.Running then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Running) end)
                end
                if humNow.WalkSpeed < titanConfig.walkSpeed then
                    humNow.WalkSpeed = titanConfig.walkSpeed
                end

                local hasRun = false
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("idle") or animName:find("walk") then
                        track:Stop(0.05)
                    elseif animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        hasRun = true
                        track:AdjustSpeed(titanConfig.animSpeed)
                    end
                end

                if not hasRun then
                    local animScript = cc:FindFirstChild("Animate")
                    local runAnim = animScript and animScript:FindFirstChild("run") and animScript.run:FindFirstChildOfClass("Animation")
                    if runAnim then
                        pcall(function()
                            local runTrack = humNow:LoadAnimation(runAnim)
                            runTrack.Priority = Enum.AnimationPriority.Action4
                            runTrack:Play(0.05)
                            runTrack:AdjustSpeed(titanConfig.animSpeed)
                        end)
                    end
                end
            else
                
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        track:Stop(0.1)
                    end
                end
            end

            
            if titanColorTheme == "Rainbow" then
                local nowTick = tick()
                for i, entry in ipairs(MECHA_PARTS) do
                    local waveColor = Color3.fromHSV((nowTick * 0.4 + (i / #MECHA_PARTS)) % 1, 1, 1)

                    if entry.prop then
                        pcall(function()
                            for _, p in ipairs(entry.prop:GetDescendants()) do
                                if p:IsA("BasePart") then
                                    p.Color = waveColor
                                end
                            end
                        end)
                    end

                    if entry.cpc and (nowTick - (entry.lastColorSent or 0) >= 0.25) then
                        entry.lastColorSent = nowTick
                        task.spawn(function()
                            pcall(function() entry.cpc:InvokeServer(waveColor) end)
                        end)
                    end
                end
            end
        end)
    end

    
    
    
    BuildV2Tab:AddSection({"> Toilet Titan Robot"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Toilet Titan",
        Description = "Pilote a giant 15-prop toilet colossus with real-time R15 motion replication",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not titanActive then return end
            if val then
                task.spawn(spawnToiletTitan)
            else
                stopToiletTitan()
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Toilet Titan Color Theme",
        Description = "Select robot color or animated wave",
        Options = {
            "Default",
            "Rainbow",
            "Cyberpunk",
            "Obsidian",
            "Gold",
            "Crimson",
            "Violet",
            "Acid",
            "Ice",
            "Magma"
        },
        Default = "Default",
        Callback = function(choice)
            applyToiletTitanTheme(choice)
        end
    })
end

do
    local lp = game:GetService("Players").LocalPlayer
    local rs = game:GetService("RunService")
    local rep = game:GetService("ReplicatedStorage")

    local genActive = false
    local genLoop = nil
    local genPhysicsConn = nil
    local myPropName = "Prop" .. lp.Name

    local savedHipHeight = 2.0
    local savedWalkSpeed = 16
    local currentHipHeightVal = 62.0
    local savedTransparencies = {}

    local genColorTheme = "Default"
    local genPartsList = {}

    local mechaConfig = {
        size = 11.0,
        height = 1.0,
        posZ = 0.0,
        posX = 0.0,
        rotY = 0.0,
        walkSpeed = 40.0,
        animSpeed = 0.85,
        torsoRotX = 90.0,
        torsoRotY = 0.0,
        torsoRotZ = 0.0,
        torsoOffsetX = 1.0,
        torsoOffsetY = -2.0,
        torsoOffsetZ = -3.0,
        armWidth = 1.03,
        armSpacing = 5.45,
        legWidth = 0.74,
        legOffsetY = 0.0,
        legSpacing = 0.0,
        pelvisOffsetX = -2.00,
        pelvisOffsetY = -3.50,
        pelvisOffsetZ = -1.00,
        pelvisRotX = 0.00,
        pelvisRotY = 85.00,
        pelvisRotZ = 0.00,
    }

    local limbRot = {
        armPitch = 0,
        armYaw = 33,
        armRoll = 0,
        legPitch = 0,
        legYaw = 33,
        legRoll = 0,
        headPitch = 0,
        headYaw = 0,
        headRoll = 0,
        pelvisPitch = 0,
        pelvisYaw = 0,
        pelvisRoll = 0,
    }

    local GEN_THEMES = {
        ["Obsidian"] = Color3.fromRGB(15, 15, 18),
        ["Gold"]     = Color3.fromRGB(255, 215, 0),
        ["Crimson"]  = Color3.fromRGB(220, 20, 35),
        ["Violet"]   = Color3.fromRGB(150, 40, 240),
        ["Acid"]     = Color3.fromRGB(50, 255, 60),
        ["Ice"]      = Color3.fromRGB(0, 230, 255),
        ["Magma"]    = Color3.fromRGB(255, 115, 0),
    }

    local function gNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function applyPlayerLift(newVal)
        local char = lp.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hum and hrp then
                hum.HipHeight = newVal
                if newVal > 3.0 then
                    hrp.CFrame = hrp.CFrame + Vector3.new(0, 0.5, 0)
                end
            end
        end
    end

    local function smoothClimbTo(targetVal, step)
        step = step or 2.0
        task.spawn(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local current = (hum and hum.HipHeight) or 2.0
            if current < targetVal then
                for v = current + step, targetVal, step do
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                applyPlayerLift(targetVal)
            else
                for v = current - step, targetVal, -step do
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                applyPlayerLift(targetVal)
            end
        end)
    end

    local function setCharacterHidden(hidden)
        local char = lp.Character
        if not char then return end
        if hidden then
            for _, part in pairs(char:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("Decal") then
                    if not savedTransparencies[part] then
                        savedTransparencies[part] = part.Transparency
                    end
                    part.Transparency = 1
                end
            end
        else
            for part, origVal in pairs(savedTransparencies) do
                if part and part.Parent then
                    pcall(function() part.Transparency = origVal end)
                end
            end
            savedTransparencies = {}
        end
    end

    local function getOrFetchPropMaker()
        local backpack = lp:FindFirstChild("Backpack")
        local character = lp.Character
        if not backpack or not character then return nil end
        local tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
        if not tool then
            pcall(function()
                rep:WaitForChild("RE"):WaitForChild("1Too1l"):InvokeServer("PickingTools", "PropMaker")
            end)
            local start = tick()
            while tick() - start < 2.0 do
                tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
                if tool then break end
                task.wait(0.05)
            end
        end
        if tool and tool.Parent ~= character then
            local hum = character:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:EquipTool(tool)
                local eqStart = tick()
                while tick() - eqStart < 2.0 do
                    if tool.Parent == character then break end
                    task.wait(0.05)
                end
            end
        end
        return tool
    end

    local function duplicateExistingProp(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
        if not dupRemote then return false end

        for _ = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
            task.wait(0.05)
        end
        return true
    end

    local senders = {}
    local RATE_LIMIT = 0.03

    local function sendCF(scf, cf, prop)
        if not genActive or not scf then return end

        if prop then
            pcall(function()
                if prop:IsA("Model") then
                    prop:PivotTo(cf)
                elseif prop:IsA("BasePart") then
                    prop.CFrame = cf
                end
            end)
        end

        local entry = senders[scf]
        local now = tick()
        if not entry then
            entry = { lastSent = 0, nextCF = cf, sending = false }
            senders[scf] = entry
        else
            entry.nextCF = cf
        end

        if not entry.sending and (now - entry.lastSent >= RATE_LIMIT) then
            entry.sending = true
            entry.lastSent = now
            local toSend = entry.nextCF
            entry.nextCF = nil
            task.spawn(function()
                pcall(function() scf:InvokeServer(toSend) end)
                entry.sending = false
                if genActive and entry.nextCF and (tick() - entry.lastSent >= RATE_LIMIT) then
                    entry.sending = true
                    entry.lastSent = tick()
                    local nextSend = entry.nextCF
                    entry.nextCF = nil
                    task.spawn(function()
                        pcall(function() scf:InvokeServer(nextSend) end)
                        entry.sending = false
                    end)
                end
            end)
        end
    end

    local function applyThemeToPart(prop, cpc, col)
        if prop then
            pcall(function()
                for _, p in ipairs(prop:GetDescendants()) do
                    if p:IsA("BasePart") then
                        p.Color = col
                    end
                end
            end)
        end
        if cpc then
            task.spawn(function()
                pcall(function() cpc:InvokeServer(col) end)
            end)
        end
    end

    local function applyGeneratorTitanTheme(themeName)
        genColorTheme = themeName
        if not genActive or #genPartsList == 0 then return end

        if themeName == "Default" then
            for _, entry in ipairs(genPartsList) do
                applyThemeToPart(entry.prop, entry.cpc, Color3.fromRGB(163, 162, 165))
            end
            gNotif("Generator Titan", "Theme: Default", 2)
            return
        elseif themeName == "Rainbow" then
            gNotif("Generator Titan", "Theme: Rainbow Wave", 2)
            return
        elseif themeName == "Cyberpunk" then
            for i, entry in ipairs(genPartsList) do
                local col = (i % 2 == 0) and Color3.fromRGB(0, 235, 255) or Color3.fromRGB(255, 30, 150)
                applyThemeToPart(entry.prop, entry.cpc, col)
            end
            gNotif("Generator Titan", "Theme: Cyberpunk", 2)
            return
        end

        local solidCol = GEN_THEMES[themeName] or Color3.fromRGB(163, 162, 165)
        for _, entry in ipairs(genPartsList) do
            applyThemeToPart(entry.prop, entry.cpc, solidCol)
        end
        gNotif("Generator Titan", "Theme: " .. themeName, 2)
    end

    local function stopGeneratorTitan()
        if not genActive then return end
        genActive = false
        if genLoop then
            genLoop:Disconnect()
            genLoop = nil
        end
        if genPhysicsConn then
            genPhysicsConn:Disconnect()
            genPhysicsConn = nil
        end
        senders = {}
        genPartsList = {}

        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.WalkSpeed = savedWalkSpeed or 16
            pcall(function()
                hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true)
                hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
                hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, true)
            end)
            for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
                if track.Priority == Enum.AnimationPriority.Action4 or track.Name:lower():find("run") then
                    pcall(function() track:Stop(0.1) end)
                end
            end
            pcall(function() hum:ChangeState(Enum.HumanoidStateType.Landed) end)
        end

        smoothClimbTo(savedHipHeight or 2.0, 4.0)
        setCharacterHidden(false)

        task.wait(0.15)
        pcall(function() rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
        task.wait(0.3)
        pcall(function() rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s"):FireServer("ClearAllProps") end)
        gNotif("Generator Titan", "Titan offline and props cleared.", 3)
    end

    local function getTargetSurface(targetWorldPos)
        local street = workspace:FindFirstChild("Model") and workspace.Model:FindFirstChild("Street") and workspace.Model.Street:FindFirstChild("Street")

        local rayParams = RaycastParams.new()
        rayParams.FilterDescendantsInstances = {lp.Character, workspace:FindFirstChild("WorkspaceCom")}
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        local ray = workspace:Raycast(targetWorldPos + Vector3.new(0, 10, 0), Vector3.new(0, -30, 0), rayParams)
        local hitInst = street or (ray and ray.Instance) or workspace.Terrain
        local hitPos = ray and ray.Position or targetWorldPos

        return hitInst, hitPos
    end

    local function spawnGeneratorTitan()
        if genActive then stopGeneratorTitan() task.wait(0.5) end
        genActive = true

        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:WaitForChild("Humanoid")
        savedHipHeight = hum.HipHeight
        savedWalkSpeed = hum.WalkSpeed
        hum.WalkSpeed = mechaConfig.walkSpeed

        pcall(function()
            hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
        end)

        gNotif("Generator Titan", "Assembling 15-prop Titan...", 3)

        local clearRE = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        local Telemetry = rep:FindFirstChild("Remotes") and rep.Remotes:FindFirstChild("TelemetryClientInteraction")

        pcall(function() clearRE:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then gNotif("Generator Titan", "PropMaker tool unavailable!", 3) return end

        pcall(function() clearRE:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local wc = workspace:FindFirstChild("WorkspaceCom")
        if not wc then gNotif("Generator Titan", "WorkspaceCom missing!", 3) return end

        local bunkerTarget = hrp.Position + (hrp.CFrame.LookVector * 14)
        local generatorTarget = hrp.Position - (hrp.CFrame.LookVector * 10)

        local bunkerHitInst, bunkerHitPos = getTargetSurface(bunkerTarget)
        local generatorHitInst, generatorHitPos = getTargetSurface(generatorTarget)

        local existingProps = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    existingProps[prop] = true
                end
            end
        end

        local function spawnSingleProp(propName, categoryName, targetInst, targetPos)
            local tool = getOrFetchPropMaker()
            if not tool then return nil end

            if Telemetry then
                pcall(function() Telemetry:FireServer("filterClick", {name = categoryName, itemType = "Props"}) end)
                task.wait(0.05)
                pcall(function() Telemetry:FireServer("filterClick", {name = propName, itemType = "Props"}) end)
                task.wait(0.05)
            end

            pcall(function() clearRE:FireServer("RequestingPropName", propName, categoryName) end)
            task.wait(0.3)

            local currentHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if currentHum and tool.Parent ~= lp.Character then
                currentHum:EquipTool(tool)
                task.wait(0.1)
            end

            local tpm = tool:FindFirstChild("Tool_PropMake")
            local startW = tick()
            while not tpm and tick() - startW < 4.0 and genActive do
                tpm = tool:FindFirstChild("Tool_PropMake")
                if not tpm then
                    local bp = lp.Backpack:FindFirstChild("PropMaker")
                    if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
                end
                if not tpm then task.wait(0.1) end
            end

            if not tpm then return nil end

            local useInst = targetInst or bunkerHitInst
            local usePos = targetPos or bunkerHitPos
            pcall(function() tpm:FireServer(useInst, usePos) end)

            local spawned = nil
            local t0 = tick()
            while tick() - t0 < 4.0 and genActive do
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        if prop.Name == myPropName and not existingProps[prop] then
                            spawned = prop
                            existingProps[prop] = true
                            break
                        end
                    end
                    if spawned then break end
                end
                if spawned then break end
                task.wait(0.05)
            end

            return spawned
        end

        
        local chestProp = spawnSingleProp("BigGunBunker", "Big", bunkerHitInst, bunkerHitPos)
        if not chestProp then
            chestProp = spawnSingleProp("BigGunBunker", "Big", bunkerHitInst, bunkerHitPos)
        end
        if not chestProp then gNotif("Generator Titan", "Chest prop failed!", 3) return end

        
        local firstGen = spawnSingleProp("BigGenerator", "Building", generatorHitInst, generatorHitPos)
        if not firstGen then
            firstGen = spawnSingleProp("BigGenerator", "Building", generatorHitInst, generatorHitPos)
        end
        if not firstGen then gNotif("Generator Titan", "BigGenerator failed!", 3) return end

        
        duplicateExistingProp(firstGen, 13)
        task.wait(0.8)

        -- Retrait du carton apres 1 seconde exacte (LOCKED Rule 7)
        task.wait(1.0)
        pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearRE:FireServer("ClosePropMenu") end)

        task.wait(0.3)
        local generatorProps = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and prop ~= chestProp then
                    local scf = prop:FindFirstChild("SetCurrentCFrame") or prop:FindFirstChildWhichIsA("RemoteFunction", true)
                    local cpc = prop:FindFirstChild("ChangePropColor") or prop:FindFirstChild("ChangePropColor", true)
                    if scf then
                        table.insert(generatorProps, {prop = prop, scf = scf, cpc = cpc})
                        pcall(function()
                            for _, p in ipairs(prop:GetDescendants()) do
                                if p:IsA("BasePart") then
                                    p.CanCollide = false
                                    p.CanTouch = false
                                    p.CanQuery = false
                                    p.Massless = true
                                end
                            end
                        end)
                    end
                end
            end
        end

        local chestScf = chestProp:FindFirstChild("SetCurrentCFrame") or chestProp:FindFirstChild("SetCurrentCFrame", true) or chestProp:FindFirstChildWhichIsA("RemoteFunction", true)
        local chestCpc = chestProp:FindFirstChild("ChangePropColor") or chestProp:FindFirstChild("ChangePropColor", true)
        if not chestScf then gNotif("Generator Titan", "Chest SetCurrentCFrame missing!", 3) return end

        pcall(function()
            for _, p in ipairs(chestProp:GetDescendants()) do
                if p:IsA("BasePart") then
                    p.CanCollide = false
                    p.CanTouch = false
                    p.CanQuery = false
                    p.Massless = true
                end
            end
        end)

        task.wait(0.3)
        smoothClimbTo(currentHipHeightVal, 2.0)
        setCharacterHidden(true)

        
        genPhysicsConn = rs.Stepped:Connect(function()
            if not genActive then return end
            local cc = lp.Character
            if cc then
                for _, part in ipairs(cc:GetDescendants()) do
                    if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                        part.CanCollide = false
                        part.CanTouch = false
                    end
                end
            end
            if chestProp then
                for _, p in ipairs(chestProp:GetDescendants()) do
                    if p:IsA("BasePart") then
                        p.CanCollide = false
                        p.CanTouch = false
                        p.CanQuery = false
                    end
                end
            end
            for _, genData in ipairs(generatorProps) do
                if genData.prop then
                    for _, p in ipairs(genData.prop:GetDescendants()) do
                        if p:IsA("BasePart") then
                            p.CanCollide = false
                            p.CanTouch = false
                            p.CanQuery = false
                        end
                    end
                end
            end
        end)

        gNotif("Generator Titan", string.format("Generator Titan Active! (%d props)", #generatorProps + 1), 3)

        
        
        
        local MECHA_PARTS = {
            { name = "UpperTorso", scf = chestScf, prop = chestProp, cpc = chestCpc, offset = CFrame.new(0, -2.5, 0) },
            { name = "Head", scf = generatorProps[1] and generatorProps[1].scf, prop = generatorProps[1] and generatorProps[1].prop, cpc = generatorProps[1] and generatorProps[1].cpc, isHead = true },
            { name = "LowerTorso", scf = generatorProps[2] and generatorProps[2].scf, prop = generatorProps[2] and generatorProps[2].prop, cpc = generatorProps[2] and generatorProps[2].cpc, isPelvis = true },

            { name = "LeftUpperArm", scf = generatorProps[3] and generatorProps[3].scf, prop = generatorProps[3] and generatorProps[3].prop, cpc = generatorProps[3] and generatorProps[3].cpc, isLeftArm = true },
            { name = "LeftLowerArm", scf = generatorProps[4] and generatorProps[4].scf, prop = generatorProps[4] and generatorProps[4].prop, cpc = generatorProps[4] and generatorProps[4].cpc, isLeftArm = true },
            { name = "LeftHand",     scf = generatorProps[5] and generatorProps[5].scf, prop = generatorProps[5] and generatorProps[5].prop, cpc = generatorProps[5] and generatorProps[5].cpc, isLeftArm = true },

            { name = "RightUpperArm", scf = generatorProps[6] and generatorProps[6].scf, prop = generatorProps[6] and generatorProps[6].prop, cpc = generatorProps[6] and generatorProps[6].cpc, isRightArm = true },
            { name = "RightLowerArm", scf = generatorProps[7] and generatorProps[7].scf, prop = generatorProps[7] and generatorProps[7].prop, cpc = generatorProps[7] and generatorProps[7].cpc, isRightArm = true },
            { name = "RightHand",     scf = generatorProps[8] and generatorProps[8].scf, prop = generatorProps[8] and generatorProps[8].prop, cpc = generatorProps[8] and generatorProps[8].cpc, isRightArm = true },

            { name = "LeftUpperLeg", scf = generatorProps[9] and generatorProps[9].scf, prop = generatorProps[9] and generatorProps[9].prop, cpc = generatorProps[9] and generatorProps[9].cpc, isLeftLeg = true },
            { name = "LeftLowerLeg", scf = generatorProps[10] and generatorProps[10].scf, prop = generatorProps[10] and generatorProps[10].prop, cpc = generatorProps[10] and generatorProps[10].cpc, isLeftLeg = true },
            { name = "LeftFoot",     scf = generatorProps[11] and generatorProps[11].scf, prop = generatorProps[11] and generatorProps[11].prop, cpc = generatorProps[11] and generatorProps[11].cpc, isLeftLeg = true },

            { name = "RightUpperLeg", scf = generatorProps[12] and generatorProps[12].scf, prop = generatorProps[12] and generatorProps[12].prop, cpc = generatorProps[12] and generatorProps[12].cpc, isRightLeg = true },
            { name = "RightLowerLeg", scf = generatorProps[13] and generatorProps[13].scf, prop = generatorProps[13] and generatorProps[13].prop, cpc = generatorProps[13] and generatorProps[13].cpc, isRightLeg = true },
            { name = "RightFoot",     scf = generatorProps[14] and generatorProps[14].scf, prop = generatorProps[14] and generatorProps[14].prop, cpc = generatorProps[14] and generatorProps[14].cpc, isRightLeg = true },
        }

        genPartsList = MECHA_PARTS

        if genColorTheme ~= "Default" then
            applyGeneratorTitanTheme(genColorTheme)
        end

        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        genLoop = rs.RenderStepped:Connect(function()
            if not genActive then return end

            local cc = lp.Character
            if not cc then return end
            local hrpNow = cc:FindFirstChild("HumanoidRootPart")
            local humNow = cc:FindFirstChildOfClass("Humanoid")
            if not hrpNow or not humNow then return end

            
            local rayFilter = {cc, workspace:FindFirstChild("WorkspaceCom")}
            for _, plr in ipairs(Players:GetPlayers()) do
                if plr.Character and plr.Character ~= cc then
                    table.insert(rayFilter, plr.Character)
                end
            end
            rayParams.FilterDescendantsInstances = rayFilter
            local ray = workspace:Raycast(hrpNow.Position + Vector3.new(0, 10, 0), Vector3.new(0, -250, 0), rayParams)
            local groundY = ray and ray.Position.Y
            local minFloorY = groundY and (groundY + currentHipHeightVal) or nil

            if minFloorY and hrpNow.Position.Y < minFloorY then
                local vel = hrpNow.AssemblyLinearVelocity
                if vel.Y < 0 then
                    hrpNow.AssemblyLinearVelocity = Vector3.new(vel.X, 0, vel.Z)
                end
                hrpNow.CFrame = CFrame.new(hrpNow.Position.X, minFloorY, hrpNow.Position.Z) * hrpNow.CFrame.Rotation
                if humNow and humNow:GetState() == Enum.HumanoidStateType.Freefall then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Landed) end)
                end
            end

            local hrpCF = hrpNow.CFrame
            local forwardFlat = (hrpCF.LookVector * Vector3.new(1, 0, 1)).Unit
            if forwardFlat.Magnitude < 0.001 then forwardFlat = hrpCF.LookVector end

            local centerCF = hrpCF * CFrame.new(mechaConfig.posX, mechaConfig.height, mechaConfig.posZ) * CFrame.Angles(0, math.rad(mechaConfig.rotY), 0)

            
            for _, p in pairs(cc:GetDescendants()) do
                if (p:IsA("BasePart") or p:IsA("Decal")) and p.Transparency < 1 then
                    if not savedTransparencies[p] then
                        savedTransparencies[p] = p.Transparency
                    end
                    p.Transparency = 1
                end
            end

            for _, entry in ipairs(MECHA_PARTS) do
                if entry.scf then
                    local part = cc:FindFirstChild(entry.name)
                    if not part and entry.name == "UpperTorso" then
                        part = cc:FindFirstChild("Torso") or hrpNow
                    end
                    if part then
                        local rel = hrpCF:ToObjectSpace(part.CFrame)

                        local xMult = 1.0
                        if entry.name:find("Arm") or entry.name:find("Hand") then
                            xMult = mechaConfig.armWidth
                        elseif entry.name:find("Leg") or entry.name:find("Foot") then
                            xMult = mechaConfig.legWidth
                        end

                        local scaledPos = Vector3.new(rel.Position.X * mechaConfig.size * xMult, rel.Position.Y * mechaConfig.size, rel.Position.Z * mechaConfig.size)

                        if entry.name:find("LowerArm") then
                            scaledPos = scaledPos + Vector3.new(0, -mechaConfig.armSpacing, 0)
                        elseif entry.name:find("Hand") then
                            scaledPos = scaledPos + Vector3.new(0, -mechaConfig.armSpacing * 2, 0)
                        end

                        if entry.name:find("Leg") or entry.name:find("Foot") then
                            scaledPos = scaledPos + Vector3.new(0, mechaConfig.legOffsetY, 0)
                            if entry.name:find("LowerLeg") then
                                scaledPos = scaledPos + Vector3.new(0, mechaConfig.legSpacing, 0)
                            elseif entry.name:find("UpperLeg") then
                                scaledPos = scaledPos + Vector3.new(0, mechaConfig.legSpacing * 2, 0)
                            end
                        end

                        local relRot = rel - rel.Position

                        local extraOffset = entry.offset or CFrame.new()
                        if entry.name == "UpperTorso" then
                            extraOffset = CFrame.new(mechaConfig.torsoOffsetX, mechaConfig.torsoOffsetY, mechaConfig.torsoOffsetZ)
                                * CFrame.Angles(math.rad(mechaConfig.torsoRotX), math.rad(mechaConfig.torsoRotY), math.rad(mechaConfig.torsoRotZ))
                        elseif entry.isHead then
                            extraOffset = CFrame.new(0, 1.5, 0) * CFrame.Angles(math.rad(limbRot.headPitch), math.rad(limbRot.headYaw), math.rad(limbRot.headRoll))
                        elseif entry.isPelvis then
                            extraOffset = (entry.offset or CFrame.new())
                                * CFrame.new(mechaConfig.pelvisOffsetX, mechaConfig.pelvisOffsetY, mechaConfig.pelvisOffsetZ)
                                * CFrame.Angles(math.rad(mechaConfig.pelvisRotX + limbRot.pelvisPitch), math.rad(mechaConfig.pelvisRotY + limbRot.pelvisYaw), math.rad(mechaConfig.pelvisRotZ + limbRot.pelvisRoll))
                        elseif entry.isLeftArm then
                            extraOffset = CFrame.Angles(math.rad(limbRot.armPitch), math.rad(limbRot.armYaw), math.rad(limbRot.armRoll))
                        elseif entry.isRightArm then
                            extraOffset = CFrame.Angles(math.rad(limbRot.armPitch), math.rad(-limbRot.armYaw), math.rad(-limbRot.armRoll))
                        elseif entry.isLeftLeg then
                            extraOffset = CFrame.Angles(math.rad(limbRot.legPitch), math.rad(limbRot.legYaw), math.rad(limbRot.legRoll))
                        elseif entry.isRightLeg then
                            extraOffset = CFrame.Angles(math.rad(limbRot.legPitch), math.rad(-limbRot.legYaw), math.rad(-limbRot.legRoll))
                        end

                        local targetCF = centerCF * CFrame.new(scaledPos) * relRot * extraOffset

                        sendCF(entry.scf, targetCF, entry.prop)
                    end
                end
            end

            
            if humNow and humNow.MoveDirection.Magnitude > 0.05 then
                if humNow:GetState() ~= Enum.HumanoidStateType.Running then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Running) end)
                end
                if humNow.WalkSpeed < mechaConfig.walkSpeed then
                    humNow.WalkSpeed = mechaConfig.walkSpeed
                end

                local hasRun = false
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("idle") or animName:find("walk") then
                        track:Stop(0.05)
                    elseif animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        hasRun = true
                        track:AdjustSpeed(mechaConfig.animSpeed)
                    end
                end

                if not hasRun then
                    local animScript = cc:FindFirstChild("Animate")
                    local runAnim = animScript and animScript:FindFirstChild("run") and animScript.run:FindFirstChildOfClass("Animation")
                    if runAnim then
                        pcall(function()
                            local runTrack = humNow:LoadAnimation(runAnim)
                            runTrack.Priority = Enum.AnimationPriority.Action4
                            runTrack:Play(0.05)
                            runTrack:AdjustSpeed(mechaConfig.animSpeed)
                        end)
                    end
                end
            else
                
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        track:Stop(0.1)
                    end
                end
            end

            
            if humNow.HipHeight ~= currentHipHeightVal and currentHipHeightVal >= 60.0 then
                humNow.HipHeight = currentHipHeightVal
            end

            
            if genColorTheme == "Rainbow" then
                local nowTick = tick()
                for i, entry in ipairs(MECHA_PARTS) do
                    local waveColor = Color3.fromHSV((nowTick * 0.4 + (i / #MECHA_PARTS)) % 1, 1, 1)

                    if entry.prop then
                        pcall(function()
                            for _, p in ipairs(entry.prop:GetDescendants()) do
                                if p:IsA("BasePart") then
                                    p.Color = waveColor
                                end
                            end
                        end)
                    end

                    if entry.cpc and (nowTick - (entry.lastColorSent or 0) >= 0.25) then
                        entry.lastColorSent = nowTick
                        task.spawn(function()
                            pcall(function() entry.cpc:InvokeServer(waveColor) end)
                        end)
                    end
                end
            end
        end)
    end

    
    
    
    BuildV2Tab:AddSection({"> Generator Titan Robot"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Generator Titan",
        Description = "Pilot a colossal 15-prop generator mecha with real-time R15 motion replication",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not genActive then return end
            if val then
                task.spawn(spawnGeneratorTitan)
            else
                stopGeneratorTitan()
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Generator Titan Color Theme",
        Description = "Select robot color or animated wave",
        Options = {
            "Default",
            "Rainbow",
            "Cyberpunk",
            "Obsidian",
            "Gold",
            "Crimson",
            "Violet",
            "Acid",
            "Ice",
            "Magma"
        },
        Default = "Default",
        Callback = function(choice)
            applyGeneratorTitanTheme(choice)
        end
    })
end

do
    local lp = game:GetService("Players").LocalPlayer
    local rs = game:GetService("RunService")
    local rep = game:GetService("ReplicatedStorage")

    local fusionActive = false
    local fusionLoop = nil
    local fusionPhysicsConn = nil
    local myPropName = "Prop" .. lp.Name

    local savedHipHeight = 2.0
    local savedWalkSpeed = 16
    local savedJumpPower = 50
    local savedUseJumpPower = false
    local currentHipHeightVal = 62.0
    local savedTransparencies = {}
    local fusionColorTheme = "Default"

    local fusionPartsList = {}
    local currentFusionOrigin = nil

    local fusionConfig = {
        size = 11.0,
        height = -7.50,
        posZ = 0.0,
        posX = 0.0,
        rotY = 0.0,
        walkSpeed = 42.0,
        animSpeed = 0.85,
        playerLift = 62.0,
        torsoRotX = 90.0,
        torsoRotY = 0.0,
        torsoRotZ = 0.0,
        torsoOffsetX = 1.0,
        torsoOffsetY = -2.0,
        torsoOffsetZ = -3.0,
        armWidth = 1.03,
        armSpacing = 5.45,
        legWidth = 0.74,
        legOffsetY = 0.0,
        legSpacing = 0.0,
        pelvisOffsetX = -2.00,
        pelvisOffsetY = -3.50,
        pelvisOffsetZ = -1.00,
        pelvisRotX = 0.00,
        pelvisRotY = 85.00,
        pelvisRotZ = 0.00,
    }

    local FUSION_THEMES = {
        ["Default"]   = Color3.fromRGB(130, 135, 145),
        ["Obsidian"]  = Color3.fromRGB(15, 15, 18),
        ["Gold"]      = Color3.fromRGB(255, 215, 0),
        ["Crimson"]   = Color3.fromRGB(220, 20, 35),
        ["Violet"]    = Color3.fromRGB(150, 40, 240),
        ["Acid"]      = Color3.fromRGB(50, 255, 60),
        ["Ice"]       = Color3.fromRGB(0, 230, 255),
        ["Magma"]     = Color3.fromRGB(255, 115, 0),
    }

    local function fNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function applyPlayerLift(newVal)
        local char = lp.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hum and hrp then
                hum.HipHeight = newVal
                if newVal > 3.0 then
                    hrp.CFrame = hrp.CFrame + Vector3.new(0, 0.5, 0)
                end
            end
        end
    end

    local function smoothClimbTo(targetVal, step)
        step = step or 3.0
        task.spawn(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local current = (hum and hum.HipHeight) or 2.0
            if current < targetVal then
                for v = current + step, targetVal, step do
                    if not fusionActive then break end
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                if fusionActive then applyPlayerLift(targetVal) end
            else
                for v = current - step, targetVal, -step do
                    if not fusionActive then break end
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                applyPlayerLift(targetVal)
            end
        end)
    end

    local function setCharacterHidden(hide)
        local char = lp.Character
        if not char then return end
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                if hide then
                    if savedTransparencies[part] == nil then
                        savedTransparencies[part] = part.Transparency
                    end
                    part.Transparency = 1
                else
                    if savedTransparencies[part] ~= nil then
                        part.Transparency = savedTransparencies[part]
                    end
                end
            elseif part:IsA("Decal") then
                if hide then
                    if savedTransparencies[part] == nil then
                        savedTransparencies[part] = part.Transparency
                    end
                    part.Transparency = 1
                else
                    if savedTransparencies[part] ~= nil then
                        part.Transparency = savedTransparencies[part]
                    end
                end
            end
        end
        if not hide then savedTransparencies = {} end
    end

    local function getOrFetchPropMaker()
        local tool = lp.Character and lp.Character:FindFirstChild("PropMaker")
        if not tool then
            tool = lp.Backpack:FindFirstChild("PropMaker")
        end
        if not tool then
            local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
            pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
            task.wait(0.3)
            tool = lp.Character and lp.Character:FindFirstChild("PropMaker")
            if not tool then tool = lp.Backpack:FindFirstChild("PropMaker") end
        end
        return tool
    end

    local function duplicateExistingProp(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
        if not dupRemote then return false end

        for i = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
            task.wait(0.05)
        end
        return true
    end

    local senders = {}
    local RATE_LIMIT = 0.03

    local function sendCF(scf, cf, prop)
        if not fusionActive or not scf then return end

        if prop then
            pcall(function()
                if prop:IsA("Model") then
                    prop:PivotTo(cf)
                elseif prop:IsA("BasePart") then
                    prop.CFrame = cf
                end
            end)
        end

        local entry = senders[scf]
        local now = tick()
        if not entry then
            entry = { lastSent = 0, nextCF = cf, sending = false }
            senders[scf] = entry
        else
            entry.nextCF = cf
        end

        if not entry.sending and (now - entry.lastSent >= RATE_LIMIT) then
            entry.sending = true
            entry.lastSent = now
            local toSend = entry.nextCF
            entry.nextCF = nil
            task.spawn(function()
                pcall(function() scf:InvokeServer(toSend) end)
                entry.sending = false
                if fusionActive and entry.nextCF and (tick() - entry.lastSent >= RATE_LIMIT) then
                    entry.sending = true
                    entry.lastSent = tick()
                    local nextSend = entry.nextCF
                    entry.nextCF = nil
                    task.spawn(function()
                        pcall(function() scf:InvokeServer(nextSend) end)
                        entry.sending = false
                    end)
                end
            end)
        end
    end

    local function applyThemeToPart(prop, cpc, color)
        if prop then
            pcall(function()
                for _, p in ipairs(prop:GetDescendants()) do
                    if p:IsA("BasePart") then
                        p.Color = color
                    end
                end
            end)
        end
        if cpc then
            task.spawn(function()
                pcall(function() cpc:InvokeServer(color) end)
            end)
        end
    end

    local function applyFusionTheme(themeName)
        fusionColorTheme = themeName
        if not fusionActive or #fusionPartsList == 0 then return end

        if themeName == "Default" then
            for _, entry in ipairs(fusionPartsList) do
                local col = (entry.name == "UpperTorso" or entry.name == "LowerTorso")
                    and Color3.fromRGB(115, 120, 130)
                    or Color3.fromRGB(145, 150, 160)
                applyThemeToPart(entry.prop, entry.cpc, col)
            end
            fNotif("Fusion Titan", "Theme: Default", 2)
            return
        elseif themeName == "Rainbow" then
            fNotif("Fusion Titan", "Theme: Rainbow", 2)
            return
        elseif themeName == "Cyberpunk" then
            for i, entry in ipairs(fusionPartsList) do
                local col = (i % 2 == 0) and Color3.fromRGB(0, 235, 255) or Color3.fromRGB(255, 30, 150)
                applyThemeToPart(entry.prop, entry.cpc, col)
            end
            fNotif("Fusion Titan", "Theme: Cyberpunk", 2)
            return
        end

        local solidCol = FUSION_THEMES[themeName] or Color3.fromRGB(145, 150, 160)
        for _, entry in ipairs(fusionPartsList) do
            applyThemeToPart(entry.prop, entry.cpc, solidCol)
        end
        fNotif("Fusion Titan", "Theme: " .. themeName, 2)
    end

    local function stopFusionTitan()
        if not fusionActive then return end
        fusionActive = false
        if fusionLoop then
            fusionLoop:Disconnect()
            fusionLoop = nil
        end
        currentFusionOrigin = nil
        senders = {}
        fusionPartsList = {}

        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.WalkSpeed = savedWalkSpeed or 16
            if savedJumpPower then hum.JumpPower = savedJumpPower end
            if savedUseJumpPower ~= nil then hum.UseJumpPower = savedUseJumpPower end
            pcall(function() hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true) end)
            for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
                if track.Priority == Enum.AnimationPriority.Action4 or track.Name:lower():find("run") then
                    pcall(function() track:Stop(0.1) end)
                end
            end
            pcall(function() hum:ChangeState(Enum.HumanoidStateType.Landed) end)
        end

        smoothClimbTo(savedHipHeight or 2.0, 4.0)
        setCharacterHidden(false)

        task.wait(0.15)
        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.3)
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        fNotif("Fusion Titan", "Mega Fusion Titan dismantled.", 2)
    end

    local function spawnFusionTitan()
        if fusionActive then stopFusionTitan() task.wait(0.5) end
        fusionActive = true
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:WaitForChild("Humanoid")

        savedHipHeight = hum.HipHeight
        savedWalkSpeed = hum.WalkSpeed
        savedJumpPower = hum.JumpPower
        savedUseJumpPower = hum.UseJumpPower
        hum.WalkSpeed = fusionConfig.walkSpeed
        hum.UseJumpPower = true
        hum.JumpPower = 110
        pcall(function() hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true) end)
        fNotif("Fusion Titan", "Forging Mega Fusion Chimera (Robot 4)...", 4)

        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then fNotif("Fusion Titan", "PropMaker missing!", 3) return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local wc = workspace:FindFirstChild("WorkspaceCom")
        if not wc then fNotif("Fusion Titan", "WorkspaceCom missing!", 3) return end

        local bunkerTarget  = hrp.Position + (hrp.CFrame.LookVector * 14)
        local toiletTarget  = hrp.Position + (hrp.CFrame.RightVector * 10)
        local genTarget     = hrp.Position - (hrp.CFrame.RightVector * 10)
        local turbineTarget = hrp.Position - (hrp.CFrame.LookVector * 12)

        local Telemetry = rep:FindFirstChild("Remotes") and rep.Remotes:FindFirstChild("TelemetryClientInteraction")
        local existingProps = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    existingProps[prop] = true
                end
            end
        end

        local function spawnSingleProp(propName, categoryName, targetPos)
            local tool = getOrFetchPropMaker()
            if not tool then return nil end

            if Telemetry then
                pcall(function() Telemetry:FireServer("filterClick", {name = categoryName, itemType = "Props"}) end)
                task.wait(0.05)
                pcall(function() Telemetry:FireServer("filterClick", {name = propName, itemType = "Props"}) end)
                task.wait(0.05)
            end

            pcall(function() clearTools:FireServer("RequestingPropName", propName, categoryName) end)
            task.wait(0.3)

            local currentHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if currentHum and tool.Parent ~= lp.Character then
                currentHum:EquipTool(tool)
                task.wait(0.1)
            end

            local tpm = tool:FindFirstChild("Tool_PropMake")
            local startW = tick()
            while not tpm and tick() - startW < 4.0 and fusionActive do
                tpm = tool:FindFirstChild("Tool_PropMake")
                if not tpm then
                    local bp = lp.Backpack:FindFirstChild("PropMaker")
                    if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
                end
                if not tpm then task.wait(0.1) end
            end

            if not tpm then return nil end

            pcall(function() tpm:FireServer(workspace.Terrain, targetPos) end)

            local spawned = nil
            local t0 = tick()
            while tick() - t0 < 4.0 and fusionActive do
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        if prop.Name == myPropName and not existingProps[prop] then
                            spawned = prop
                            existingProps[prop] = true
                            break
                        end
                    end
                    if spawned then break end
                end
                if spawned then break end
                task.wait(0.05)
            end

            return spawned
        end

        
        local chestProp = spawnSingleProp("BigGunBunker", "Big", bunkerTarget)
        if not chestProp then chestProp = spawnSingleProp("BigGunBunker", "Big", bunkerTarget) end
        if not chestProp then fNotif("Fusion Titan", "Chest prop failed!", 3) return end

        
        local firstToilet = spawnSingleProp("BigOutToilet", "Building", toiletTarget)
        if not firstToilet then firstToilet = spawnSingleProp("BigOutToilet", "Building", toiletTarget) end
        if not firstToilet then fNotif("Fusion Titan", "Toilet prop failed!", 3) return end
        duplicateExistingProp(firstToilet, 3)
        task.wait(0.2)

        local toiletProps = { firstToilet }
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and not existingProps[prop] then
                    existingProps[prop] = true
                    table.insert(toiletProps, prop)
                end
            end
        end

        
        local firstGen = spawnSingleProp("BigGenerator", "Building", genTarget)
        if not firstGen then firstGen = spawnSingleProp("BigGenerator", "Building", genTarget) end
        if not firstGen then fNotif("Fusion Titan", "Generator prop failed!", 3) return end
        duplicateExistingProp(firstGen, 3)
        task.wait(0.2)

        local genProps = { firstGen }
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and not existingProps[prop] then
                    existingProps[prop] = true
                    table.insert(genProps, prop)
                end
            end
        end

        
        local firstTurbine = spawnSingleProp("PowerTurbine", "Barrel", turbineTarget)
        if not firstTurbine then firstTurbine = spawnSingleProp("PowerTurbine", "001_TrafficCones", turbineTarget) end
        if not firstTurbine then firstTurbine = spawnSingleProp("PowerTurbine", "Barrel", turbineTarget) end
        if not firstTurbine then fNotif("Fusion Titan", "Turbine prop failed!", 3) return end
        duplicateExistingProp(firstTurbine, 5)
        task.wait(0.8)

        local turbineProps = { firstTurbine }
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and not existingProps[prop] then
                    existingProps[prop] = true
                    table.insert(turbineProps, prop)
                end
            end
        end

        -- Retrait du carton apres 1 seconde (LOCKED)
        task.wait(1.0)
        pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        local function getPropRemotes(p)
            if not p then return nil, nil end
            local scf = p:FindFirstChild("SetCurrentCFrame") or p:FindFirstChild("SetCurrentCFrame", true) or p:FindFirstChildWhichIsA("RemoteFunction", true)
            local cpc = p:FindFirstChild("ChangePropColor") or p:FindFirstChild("ChangePropColor", true)
            return scf, cpc
        end

        local cScf, cCpc = getPropRemotes(chestProp)
        local t1Scf, t1Cpc = getPropRemotes(toiletProps[1])
        local t2Scf, t2Cpc = getPropRemotes(toiletProps[2])
        local t3Scf, t3Cpc = getPropRemotes(toiletProps[3])
        local t4Scf, t4Cpc = getPropRemotes(toiletProps[4])

        local g1Scf, g1Cpc = getPropRemotes(genProps[1])
        local g2Scf, g2Cpc = getPropRemotes(genProps[2])
        local g3Scf, g3Cpc = getPropRemotes(genProps[3])
        local g4Scf, g4Cpc = getPropRemotes(genProps[4])

        local tb1Scf, tb1Cpc = getPropRemotes(turbineProps[1])
        local tb2Scf, tb2Cpc = getPropRemotes(turbineProps[2])
        local tb3Scf, tb3Cpc = getPropRemotes(turbineProps[3])
        local tb4Scf, tb4Cpc = getPropRemotes(turbineProps[4])
        local tb5Scf, tb5Cpc = getPropRemotes(turbineProps[5])
        local tb6Scf, tb6Cpc = getPropRemotes(turbineProps[6])

        if not cScf then fNotif("Fusion Titan", "Chest SCF missing!", 3) return end

        task.wait(0.3)
        smoothClimbTo(fusionConfig.playerLift, 2.0)
        setCharacterHidden(true)

        
        
        
        
        
        
        
        
        
        
        local FUSION_PARTS = {
            { name = "UpperTorso",   scf = cScf,   prop = chestProp,       cpc = cCpc,   offset = CFrame.new(1.0, -2.0, -3.0) * CFrame.Angles(math.rad(90), 0, 0) },
            { name = "LowerTorso",   scf = t1Scf,  prop = toiletProps[1],  cpc = t1Cpc,  offset = CFrame.new(-2.0, -3.5, -1.0) * CFrame.Angles(0, math.rad(85), 0) },
            { name = "Head",         scf = tb1Scf, prop = turbineProps[1], cpc = tb1Cpc, offset = CFrame.new(0, 1.8, 0) * CFrame.Angles(0, math.rad(90), math.rad(90)) },

            
            { name = "LeftUpperArm", scf = g1Scf,  prop = genProps[1],     cpc = g1Cpc,  offset = CFrame.new(0, 0.5, 0) * CFrame.Angles(0, math.rad(33), 0) },
            { name = "LeftLowerArm", scf = tb2Scf, prop = turbineProps[2], cpc = tb2Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftHand",     scf = t2Scf,  prop = toiletProps[2],  cpc = t2Cpc,  offset = CFrame.new(0, -1.0, 0) * CFrame.Angles(math.rad(180), 0, 0) },

            
            { name = "RightUpperArm",scf = g2Scf,  prop = genProps[2],     cpc = g2Cpc,  offset = CFrame.new(0, 0.5, 0) * CFrame.Angles(0, math.rad(-33), 0) },
            { name = "RightLowerArm",scf = tb3Scf, prop = turbineProps[3], cpc = tb3Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightHand",    scf = tb4Scf, prop = turbineProps[4], cpc = tb4Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },

            
            { name = "LeftUpperLeg", scf = g3Scf,  prop = genProps[3],     cpc = g3Cpc,  offset = CFrame.Angles(0, math.rad(33), 0) },
            { name = "LeftLowerLeg", scf = tb5Scf, prop = turbineProps[5], cpc = tb5Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftFoot",     scf = t3Scf,  prop = toiletProps[3],  cpc = t3Cpc,  offset = CFrame.new(0, -0.5, 0) * CFrame.Angles(0, math.rad(90), 0) },

            
            { name = "RightUpperLeg",scf = g4Scf,  prop = genProps[4],     cpc = g4Cpc,  offset = CFrame.Angles(0, math.rad(-33), 0) },
            { name = "RightLowerLeg",scf = tb6Scf, prop = turbineProps[6], cpc = tb6Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightFoot",    scf = t4Scf,  prop = toiletProps[4],  cpc = t4Cpc,  offset = CFrame.new(0, -0.5, 0) * CFrame.Angles(0, math.rad(-90), 0) },
        }
        fusionPartsList = FUSION_PARTS

        applyFusionTheme(fusionColorTheme)
        fNotif("Fusion Titan", "Omega Chimera Titan 100% Online!", 3)

        if fusionLoop then fusionLoop:Disconnect() fusionLoop = nil end
        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        fusionLoop = rs.RenderStepped:Connect(function()
            if not fusionActive then return end

            local cc = lp.Character
            if not cc then return end
            local hrpNow = cc:FindFirstChild("HumanoidRootPart")
            if not hrpNow then return end
            local humNow = cc:FindFirstChildOfClass("Humanoid")

            local rayFilter = {cc, workspace:FindFirstChild("WorkspaceCom")}
            for _, plr in ipairs(game:GetService("Players"):GetPlayers()) do
                if plr.Character and plr.Character ~= cc then
                    table.insert(rayFilter, plr.Character)
                end
            end
            rayParams.FilterDescendantsInstances = rayFilter
            local ray = workspace:Raycast(hrpNow.Position + Vector3.new(0, 10, 0), Vector3.new(0, -250, 0), rayParams)
            local groundY = ray and ray.Position.Y
            local minFloorY = groundY and (groundY + currentHipHeightVal) or nil

            if minFloorY and hrpNow.Position.Y < minFloorY then
                local vel = hrpNow.AssemblyLinearVelocity
                if vel.Y < 0 then
                    hrpNow.AssemblyLinearVelocity = Vector3.new(vel.X, 0, vel.Z)
                end
                hrpNow.CFrame = CFrame.new(hrpNow.Position.X, minFloorY, hrpNow.Position.Z) * hrpNow.CFrame.Rotation
                if humNow and humNow:GetState() == Enum.HumanoidStateType.Freefall then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Landed) end)
                end
            end

            local clampedY = hrpNow.Position.Y
            if minFloorY and clampedY < minFloorY then
                clampedY = minFloorY
            end
            local clampedHrpCF = CFrame.new(hrpNow.Position.X, clampedY, hrpNow.Position.Z) * hrpNow.CFrame.Rotation
            local targetCenterCF = clampedHrpCF * CFrame.new(fusionConfig.posX, fusionConfig.height, fusionConfig.posZ) * CFrame.Angles(0, math.rad(fusionConfig.rotY), 0)
            if not currentFusionOrigin then
                currentFusionOrigin = targetCenterCF
            else
                currentFusionOrigin = currentFusionOrigin:Lerp(targetCenterCF, 0.22)
            end
            local centerCF = currentFusionOrigin

            for _, entry in ipairs(FUSION_PARTS) do
                if entry.scf then
                    local part = cc:FindFirstChild(entry.name)
                    if not part and entry.name == "UpperTorso" then
                        part = cc:FindFirstChild("Torso") or hrpNow
                    end
                    if part then
                        local rel = clampedHrpCF:ToObjectSpace(part.CFrame)

                        local xMult = 1.0
                        if entry.name:find("Arm") or entry.name:find("Hand") then
                            xMult = fusionConfig.armWidth
                        elseif entry.name:find("Leg") or entry.name:find("Foot") then
                            xMult = fusionConfig.legWidth
                        end

                        local scaledPos = Vector3.new(rel.Position.X * fusionConfig.size * xMult, rel.Position.Y * fusionConfig.size, rel.Position.Z * fusionConfig.size)

                        if entry.name:find("LowerArm") then
                            scaledPos = scaledPos + Vector3.new(0, -fusionConfig.armSpacing, 0)
                        elseif entry.name:find("Hand") then
                            scaledPos = scaledPos + Vector3.new(0, -fusionConfig.armSpacing * 2, 0)
                        end

                        if entry.name:find("Leg") or entry.name:find("Foot") then
                            scaledPos = scaledPos + Vector3.new(0, fusionConfig.legOffsetY, 0)
                            if entry.name:find("LowerLeg") then
                                scaledPos = scaledPos + Vector3.new(0, fusionConfig.legSpacing, 0)
                            elseif entry.name:find("UpperLeg") then
                                scaledPos = scaledPos + Vector3.new(0, fusionConfig.legSpacing * 2, 0)
                            end
                        end

                        local relRot = rel - rel.Position

                        local extraOffset = entry.offset or CFrame.new()
                        if entry.name == "UpperTorso" then
                            extraOffset = CFrame.new(fusionConfig.torsoOffsetX, fusionConfig.torsoOffsetY, fusionConfig.torsoOffsetZ)
                                * CFrame.Angles(math.rad(fusionConfig.torsoRotX), math.rad(fusionConfig.torsoRotY), math.rad(fusionConfig.torsoRotZ))
                        elseif entry.name == "LowerTorso" then
                            extraOffset = (entry.offset or CFrame.new())
                                * CFrame.new(fusionConfig.pelvisOffsetX, fusionConfig.pelvisOffsetY, fusionConfig.pelvisOffsetZ)
                                * CFrame.Angles(math.rad(fusionConfig.pelvisRotX), math.rad(fusionConfig.pelvisRotY), math.rad(fusionConfig.pelvisRotZ))
                        end

                        local targetCF = centerCF * CFrame.new(scaledPos) * relRot * extraOffset

                        sendCF(entry.scf, targetCF, entry.prop)
                    end
                end
            end

            
            if humNow and humNow.MoveDirection.Magnitude > 0.05 then
                if humNow:GetState() ~= Enum.HumanoidStateType.Running then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Running) end)
                end
                if humNow.WalkSpeed < fusionConfig.walkSpeed then
                    humNow.WalkSpeed = fusionConfig.walkSpeed
                end

                local hasRun = false
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("idle") or animName:find("walk") then
                        track:Stop(0.05)
                    elseif animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        hasRun = true
                        track:AdjustSpeed(fusionConfig.animSpeed)
                    end
                end

                if not hasRun then
                    local animScript = cc:FindFirstChild("Animate")
                    local runAnim = animScript and animScript:FindFirstChild("run") and animScript.run:FindFirstChildOfClass("Animation")
                    if runAnim then
                        pcall(function()
                            local runTrack = humNow:LoadAnimation(runAnim)
                            runTrack.Priority = Enum.AnimationPriority.Action4
                            runTrack:Play(0.05)
                            runTrack:AdjustSpeed(fusionConfig.animSpeed)
                        end)
                    end
                end
            else
                
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        track:Stop(0.1)
                    end
                end
            end

            
            if humNow and humNow.HipHeight ~= fusionConfig.playerLift and currentHipHeightVal >= fusionConfig.playerLift then
                humNow.HipHeight = fusionConfig.playerLift
            end

            
            if fusionColorTheme == "Rainbow" then
                local nowTick = tick()
                for i, entry in ipairs(FUSION_PARTS) do
                    local waveColor = Color3.fromHSV((nowTick * 0.4 + (i / #FUSION_PARTS)) % 1, 1, 1)

                    if entry.prop then
                        pcall(function()
                            for _, p in ipairs(entry.prop:GetDescendants()) do
                                if p:IsA("BasePart") then
                                    p.Color = waveColor
                                end
                            end
                        end)
                    end

                    if entry.cpc and (nowTick - (entry.lastColorSent or 0) >= 0.25) then
                        entry.lastColorSent = nowTick
                        task.spawn(function()
                            pcall(function() entry.cpc:InvokeServer(waveColor) end)
                        end)
                    end
                end
            end
        end)
    end

    
    
    
    BuildV2Tab:AddSection({"> Mega Fusion Titan (Robot 4)"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Mega Fusion Titan",
        Description = "Pilote an unstoppable 15-prop colossus fusing Bunker, Toilet, Generator & Turbines",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not fusionActive then return end
            if val then
                task.spawn(spawnFusionTitan)
            else
                stopFusionTitan()
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Fusion Titan Color Theme",
        Description = "Select fusion robot color or animated wave",
        Options = {
            "Default",
            "Rainbow",
            "Cyberpunk",
            "Obsidian",
            "Gold",
            "Crimson",
            "Violet",
            "Acid",
            "Ice",
            "Magma"
        },
        Default = "Default",
        Callback = function(choice)
            applyFusionTheme(choice)
        end
    })
end

do
    local lp = game:GetService("Players").LocalPlayer
    local rs = game:GetService("RunService")
    local rep = game:GetService("ReplicatedStorage")

    local apexActive = false
    local apexLoop = nil
    local myPropName = "Prop" .. lp.Name

    local savedHipHeight = 2.0
    local savedWalkSpeed = 16
    local savedJumpPower = 50
    local savedUseJumpPower = false
    local currentHipHeightVal = 62.0
    local savedTransparencies = {}
    local apexColorTheme = "Default"

    local apexPartsList = {}
    local currentApexOrigin = nil

    local apexConfig = {
        size = 11.0,
        height = 1.0,
        playerLift = 62.0,
        posZ = 0.0,
        posX = 0.0,
        rotY = 0.0,
        walkSpeed = 44.0,
        animSpeed = 0.90,
        torsoRotX = 90.0,
        torsoRotY = 0.0,
        torsoRotZ = 0.0,
        torsoOffsetX = 1.0,
        torsoOffsetY = -2.0,
        torsoOffsetZ = -3.0,
        armWidth = 1.03,
        armSpacing = 5.45,
        legWidth = 0.74,
        legOffsetY = 0.0,
        legSpacing = 0.0,
        pelvisOffsetX = -2.00,
        pelvisOffsetY = -3.50,
        pelvisOffsetZ = -1.00,
        pelvisRotX = 0.00,
        pelvisRotY = 85.00,
        pelvisRotZ = 0.00,
    }

    local APEX_THEMES = {
        ["Default"]        = Color3.fromRGB(140, 150, 165),
        ["Cyber Neon"]     = Color3.fromRGB(0, 240, 255),
        ["Solar Flare"]    = Color3.fromRGB(255, 215, 0),
        ["Abyssal Void"]   = Color3.fromRGB(18, 18, 22),
        ["Toxic Hazard"]   = Color3.fromRGB(57, 255, 20),
        ["Crimson Fury"]   = Color3.fromRGB(220, 20, 35),
        ["Glacial Frost"]  = Color3.fromRGB(0, 225, 255),
    }

    local function aNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function applyPlayerLift(newVal)
        local char = lp.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hum and hrp then
                hum.HipHeight = newVal
                if newVal > 3.0 then
                    hrp.CFrame = hrp.CFrame + Vector3.new(0, 0.5, 0)
                end
            end
        end
    end

    local function smoothClimbTo(targetVal, step)
        step = step or 3.0
        task.spawn(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local current = (hum and hum.HipHeight) or 2.0
            if current < targetVal then
                for v = current + step, targetVal, step do
                    if not apexActive then break end
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                if apexActive then applyPlayerLift(targetVal) end
            else
                for v = current - step, targetVal, -step do
                    if not apexActive then break end
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                applyPlayerLift(targetVal)
            end
        end)
    end

    local function setCharacterHidden(hide)
        local char = lp.Character
        if not char then return end
        for _, part in ipairs(char:GetDescendants()) do
            if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                if hide then
                    if savedTransparencies[part] == nil then
                        savedTransparencies[part] = part.Transparency
                    end
                    part.Transparency = 1
                else
                    if savedTransparencies[part] ~= nil then
                        part.Transparency = savedTransparencies[part]
                    end
                end
            elseif part:IsA("Decal") then
                if hide then
                    if savedTransparencies[part] == nil then
                        savedTransparencies[part] = part.Transparency
                    end
                    part.Transparency = 1
                else
                    if savedTransparencies[part] ~= nil then
                        part.Transparency = savedTransparencies[part]
                    end
                end
            end
        end
        if not hide then savedTransparencies = {} end
    end

    local function getOrFetchPropMaker()
        local tool = lp.Character and lp.Character:FindFirstChild("PropMaker")
        if not tool then
            tool = lp.Backpack:FindFirstChild("PropMaker")
        end
        if not tool then
            local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
            pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
            task.wait(0.3)
            tool = lp.Character and lp.Character:FindFirstChild("PropMaker")
            if not tool then tool = lp.Backpack:FindFirstChild("PropMaker") end
        end
        return tool
    end

    local function duplicateExistingProp(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
        if not dupRemote then return false end

        for i = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
            task.wait(0.05)
        end
        return true
    end

    local senders = {}
    local RATE_LIMIT = 0.03

    local function sendCF(scf, cf, prop)
        if not apexActive or not scf then return end

        if prop then
            pcall(function()
                if prop:IsA("Model") then
                    prop:PivotTo(cf)
                elseif prop:IsA("BasePart") then
                    prop.CFrame = cf
                end
            end)
        end

        local entry = senders[scf]
        local now = tick()
        if not entry then
            entry = { lastSent = 0, nextCF = cf, sending = false }
            senders[scf] = entry
        else
            entry.nextCF = cf
        end

        if not entry.sending and (now - entry.lastSent >= RATE_LIMIT) then
            entry.sending = true
            entry.lastSent = now
            local toSend = entry.nextCF
            entry.nextCF = nil
            task.spawn(function()
                pcall(function() scf:InvokeServer(toSend) end)
                entry.sending = false
                if apexActive and entry.nextCF and (tick() - entry.lastSent >= RATE_LIMIT) then
                    entry.sending = true
                    entry.lastSent = tick()
                    local nextSend = entry.nextCF
                    entry.nextCF = nil
                    task.spawn(function()
                        pcall(function() scf:InvokeServer(nextSend) end)
                        entry.sending = false
                    end)
                end
            end)
        end
    end

    local function applyThemeToPart(prop, cpc, color)
        if prop then
            pcall(function()
                for _, p in ipairs(prop:GetDescendants()) do
                    if p:IsA("BasePart") then
                        p.Color = color
                    end
                end
            end)
        end
        if cpc then
            task.spawn(function()
                pcall(function() cpc:InvokeServer(color) end)
            end)
        end
    end

    local function applyApexTheme(themeName)
        apexColorTheme = themeName
        if not apexActive or #apexPartsList == 0 then return end

        if themeName == "Default" then
            for _, entry in ipairs(apexPartsList) do
                local col = (entry.name:find("Torso") or entry.name:find("Head"))
                    and Color3.fromRGB(140, 150, 165)
                    or Color3.fromRGB(180, 185, 195)
                applyThemeToPart(entry.prop, entry.cpc, col)
            end
            aNotif("Apex Titan", "Theme: Default Titanium", 2)
            return
        elseif themeName == "Rainbow" then
            aNotif("Apex Titan", "Theme: Rainbow Spectrum", 2)
            return
        elseif themeName == "Cyber Neon" then
            for i, entry in ipairs(apexPartsList) do
                local col = (i % 2 == 0) and Color3.fromRGB(0, 240, 255) or Color3.fromRGB(255, 20, 147)
                applyThemeToPart(entry.prop, entry.cpc, col)
            end
            aNotif("Apex Titan", "Theme: Cyber Neon", 2)
            return
        end

        local solidCol = APEX_THEMES[themeName] or Color3.fromRGB(150, 155, 165)
        for _, entry in ipairs(apexPartsList) do
            applyThemeToPart(entry.prop, entry.cpc, solidCol)
        end
        aNotif("Apex Titan", "Theme: " .. themeName, 2)
    end

    local function stopApexTitan()
        if not apexActive then return end
        apexActive = false
        if apexLoop then
            apexLoop:Disconnect()
            apexLoop = nil
        end
        currentApexOrigin = nil
        senders = {}
        apexPartsList = {}

        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum.WalkSpeed = savedWalkSpeed or 16
            if savedJumpPower then hum.JumpPower = savedJumpPower end
            if savedUseJumpPower ~= nil then hum.UseJumpPower = savedUseJumpPower end
            pcall(function() hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true) end)
            for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
                if track.Priority == Enum.AnimationPriority.Action4 or track.Name:lower():find("run") then
                    pcall(function() track:Stop(0.1) end)
                end
            end
            pcall(function() hum:ChangeState(Enum.HumanoidStateType.Landed) end)
        end

        smoothClimbTo(savedHipHeight or 2.0, 4.0)
        setCharacterHidden(false)

        task.wait(0.15)
        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.3)
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        aNotif("Apex Titan", "Apex Dreadnought Titan dismantled.", 2)
    end

    local function spawnApexTitan()
        if apexActive then stopApexTitan() task.wait(0.5) end
        apexActive = true
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:WaitForChild("Humanoid")

        savedHipHeight = hum.HipHeight
        savedWalkSpeed = hum.WalkSpeed
        savedJumpPower = hum.JumpPower
        savedUseJumpPower = hum.UseJumpPower
        hum.WalkSpeed = apexConfig.walkSpeed
        hum.UseJumpPower = true
        hum.JumpPower = 120
        pcall(function() hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true) end)
        aNotif("Apex Titan", "Assembling Apex Dreadnought Titan (Robot 5)...", 4)

        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then aNotif("Apex Titan", "PropMaker missing!", 3) return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local wc = workspace:FindFirstChild("WorkspaceCom")
        if not wc then aNotif("Apex Titan", "WorkspaceCom missing!", 3) return end

        local bunkerTarget  = hrp.Position + (hrp.CFrame.LookVector * 14)
        local bedTarget     = hrp.Position + (hrp.CFrame.RightVector * 11)
        local turbineTarget = hrp.Position - (hrp.CFrame.LookVector * 12)

        local Telemetry = rep:FindFirstChild("Remotes") and rep.Remotes:FindFirstChild("TelemetryClientInteraction")
        local existingProps = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    existingProps[prop] = true
                end
            end
        end

        local function spawnSingleProp(propName, categoryName, targetPos)
            local tool = getOrFetchPropMaker()
            if not tool then return nil end

            if Telemetry then
                pcall(function() Telemetry:FireServer("filterClick", {name = categoryName, itemType = "Props"}) end)
                task.wait(0.05)
                pcall(function() Telemetry:FireServer("filterClick", {name = propName, itemType = "Props"}) end)
                task.wait(0.05)
            end

            pcall(function() clearTools:FireServer("RequestingPropName", propName, categoryName) end)
            task.wait(0.3)

            local currentHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if currentHum and tool.Parent ~= lp.Character then
                currentHum:EquipTool(tool)
                task.wait(0.1)
            end

            local tpm = tool:FindFirstChild("Tool_PropMake")
            local startW = tick()
            while not tpm and tick() - startW < 4.0 and apexActive do
                tpm = tool:FindFirstChild("Tool_PropMake")
                if not tpm then
                    local bp = lp.Backpack:FindFirstChild("PropMaker")
                    if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
                end
                if not tpm then task.wait(0.1) end
            end

            if not tpm then return nil end 

            pcall(function() tpm:FireServer(workspace.Terrain, targetPos) end)

            local spawned = nil
            local t0 = tick()
            while tick() - t0 < 4.0 and apexActive do
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        if prop.Name == myPropName and not existingProps[prop] then
                            spawned = prop
                            existingProps[prop] = true
                            break
                        end
                    end
                    if spawned then break end
                end
                if spawned then break end
                task.wait(0.05)
            end

            return spawned
        end

        
        local firstBunker = spawnSingleProp("BigGunBunker", "Big", bunkerTarget)
        if not firstBunker then firstBunker = spawnSingleProp("BigGunBunker", "Big", bunkerTarget) end
        if not firstBunker then aNotif("Apex Titan", "Bunker prop failed!", 3) return end
        duplicateExistingProp(firstBunker, 3)
        task.wait(0.2)

        local bunkerProps = { firstBunker }
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and not existingProps[prop] then
                    existingProps[prop] = true
                    table.insert(bunkerProps, prop)
                end
            end
        end

        
        local firstBed = spawnSingleProp("FuturisticLargeBed", "Furniture", bedTarget)
        if not firstBed then firstBed = spawnSingleProp("FuturisticLargeBed", "Furniture", bedTarget) end
        if not firstBed then aNotif("Apex Titan", "Bed prop failed!", 3) return end
        duplicateExistingProp(firstBed, 3)
        task.wait(0.2)

        local bedProps = { firstBed }
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and not existingProps[prop] then
                    existingProps[prop] = true
                    table.insert(bedProps, prop)
                end
            end
        end

        
        local firstTurbine = spawnSingleProp("PowerTurbine", "Barrel", turbineTarget)
        if not firstTurbine then firstTurbine = spawnSingleProp("PowerTurbine", "001_TrafficCones", turbineTarget) end
        if not firstTurbine then firstTurbine = spawnSingleProp("PowerTurbine", "Barrel", turbineTarget) end
        if not firstTurbine then aNotif("Apex Titan", "Turbine prop failed!", 3) return end
        duplicateExistingProp(firstTurbine, 6)
        task.wait(0.8)

        local turbineProps = { firstTurbine }
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and not existingProps[prop] then
                    existingProps[prop] = true
                    table.insert(turbineProps, prop)
                end
            end
        end

        -- Retrait du carton apres 1 seconde (LOCKED Rule 7)
        task.wait(1.0)
        pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        local function getPropRemotes(p)
            if not p then return nil, nil end
            local scf = p:FindFirstChild("SetCurrentCFrame") or p:FindFirstChild("SetCurrentCFrame", true) or p:FindFirstChildWhichIsA("RemoteFunction", true)
            local cpc = p:FindFirstChild("ChangePropColor") or p:FindFirstChild("ChangePropColor", true)
            return scf, cpc
        end

        local bk1Scf, bk1Cpc = getPropRemotes(bunkerProps[1])
        local bk2Scf, bk2Cpc = getPropRemotes(bunkerProps[2])
        local bk3Scf, bk3Cpc = getPropRemotes(bunkerProps[3])
        local bk4Scf, bk4Cpc = getPropRemotes(bunkerProps[4])

        local bd1Scf, bd1Cpc = getPropRemotes(bedProps[1])
        local bd2Scf, bd2Cpc = getPropRemotes(bedProps[2])
        local bd3Scf, bd3Cpc = getPropRemotes(bedProps[3])
        local bd4Scf, bd4Cpc = getPropRemotes(bedProps[4])

        local tb1Scf, tb1Cpc = getPropRemotes(turbineProps[1])
        local tb2Scf, tb2Cpc = getPropRemotes(turbineProps[2])
        local tb3Scf, tb3Cpc = getPropRemotes(turbineProps[3])
        local tb4Scf, tb4Cpc = getPropRemotes(turbineProps[4])
        local tb5Scf, tb5Cpc = getPropRemotes(turbineProps[5])
        local tb6Scf, tb6Cpc = getPropRemotes(turbineProps[6])
        local tb7Scf, tb7Cpc = getPropRemotes(turbineProps[7])

        if not bd1Scf then aNotif("Apex Titan", "Chest SCF missing!", 3) return end

        task.wait(0.3)
        smoothClimbTo(apexConfig.playerLift, 2.0)
        setCharacterHidden(true)

        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        local APEX_PARTS = {
            { name = "UpperTorso",   scf = bd1Scf, prop = bedProps[1],     cpc = bd1Cpc, offset = CFrame.new(0, -2.5, 0) },
            { name = "Head",         scf = tb1Scf, prop = turbineProps[1], cpc = tb1Cpc, offset = CFrame.new(0, 1.5, 0) * CFrame.Angles(math.rad(-90), 0, 0) },
            { name = "LowerTorso",   scf = tb2Scf, prop = turbineProps[2], cpc = tb2Cpc, offset = CFrame.Angles(0, 0, math.rad(90)) },

            
            { name = "LeftUpperArm", scf = bd2Scf, prop = bedProps[2],     cpc = bd2Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftLowerArm", scf = bk1Scf, prop = bunkerProps[1],  cpc = bk1Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftHand",     scf = bd4Scf, prop = bedProps[4],     cpc = bd4Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },

            
            { name = "RightUpperArm",scf = bd3Scf, prop = bedProps[3],     cpc = bd3Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightLowerArm",scf = bk2Scf, prop = bunkerProps[2],  cpc = bk2Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightHand",    scf = tb3Scf, prop = turbineProps[3], cpc = tb3Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },

            
            { name = "LeftUpperLeg", scf = tb4Scf, prop = turbineProps[4], cpc = tb4Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftLowerLeg", scf = bk3Scf, prop = bunkerProps[3],  cpc = bk3Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftFoot",     scf = tb6Scf, prop = turbineProps[6], cpc = tb6Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },

            
            { name = "RightUpperLeg",scf = tb5Scf, prop = turbineProps[5], cpc = tb5Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightLowerLeg",scf = bk4Scf, prop = bunkerProps[4],  cpc = bk4Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightFoot",    scf = tb7Scf, prop = turbineProps[7], cpc = tb7Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
        }
        apexPartsList = APEX_PARTS

        applyApexTheme(apexColorTheme)
        aNotif("Apex Titan", "Apex Dreadnought Titan 100% Online!", 3)

        if apexLoop then apexLoop:Disconnect() apexLoop = nil end
        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        local function getLimbPart(character, limbName)
            local part = character:FindFirstChild(limbName)
            if part then return part end
            
            if limbName == "UpperTorso" or limbName == "LowerTorso" then
                return character:FindFirstChild("Torso") or character:FindFirstChild("HumanoidRootPart")
            elseif limbName == "Head" then
                return character:FindFirstChild("Head")
            elseif limbName:find("Left") and (limbName:find("Arm") or limbName:find("Hand")) then
                return character:FindFirstChild("Left Arm")
            elseif limbName:find("Right") and (limbName:find("Arm") or limbName:find("Hand")) then
                return character:FindFirstChild("Right Arm")
            elseif limbName:find("Left") and (limbName:find("Leg") or limbName:find("Foot")) then
                return character:FindFirstChild("Left Leg")
            elseif limbName:find("Right") and (limbName:find("Leg") or limbName:find("Foot")) then
                return character:FindFirstChild("Right Leg")
            end
            return nil
        end

        apexLoop = rs.RenderStepped:Connect(function()
            if not apexActive then return end

            local cc = lp.Character
            if not cc then return end
            local hrpNow = cc:FindFirstChild("HumanoidRootPart")
            if not hrpNow then return end
            local humNow = cc:FindFirstChildOfClass("Humanoid")

            local rayFilter = {cc, workspace:FindFirstChild("WorkspaceCom")}
            for _, plr in ipairs(game:GetService("Players"):GetPlayers()) do
                if plr.Character and plr.Character ~= cc then
                    table.insert(rayFilter, plr.Character)
                end
            end
            rayParams.FilterDescendantsInstances = rayFilter
            local ray = workspace:Raycast(hrpNow.Position + Vector3.new(0, 10, 0), Vector3.new(0, -250, 0), rayParams)
            local groundY = ray and ray.Position.Y
            local minFloorY = groundY and (groundY + currentHipHeightVal) or nil

            if minFloorY and hrpNow.Position.Y < minFloorY then
                local vel = hrpNow.AssemblyLinearVelocity
                if vel.Y < 0 then
                    hrpNow.AssemblyLinearVelocity = Vector3.new(vel.X, 0, vel.Z)
                end
                hrpNow.CFrame = CFrame.new(hrpNow.Position.X, minFloorY, hrpNow.Position.Z) * hrpNow.CFrame.Rotation
                if humNow and humNow:GetState() == Enum.HumanoidStateType.Freefall then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Landed) end)
                end
            end

            local clampedY = hrpNow.Position.Y
            if minFloorY and clampedY < minFloorY then
                clampedY = minFloorY
            end
            local clampedHrpCF = CFrame.new(hrpNow.Position.X, clampedY, hrpNow.Position.Z) * hrpNow.CFrame.Rotation
            local targetCenterCF = clampedHrpCF * CFrame.new(apexConfig.posX, apexConfig.height, apexConfig.posZ) * CFrame.Angles(0, math.rad(apexConfig.rotY), 0)
            if not currentApexOrigin then
                currentApexOrigin = targetCenterCF
            else
                currentApexOrigin = currentApexOrigin:Lerp(targetCenterCF, 0.22)
            end
            local centerCF = currentApexOrigin

            for _, entry in ipairs(APEX_PARTS) do
                if entry.scf then
                    local part = getLimbPart(cc, entry.name)
                    if part then
                        local rel = clampedHrpCF:ToObjectSpace(part.CFrame)

                        local xMult = 1.0
                        if entry.name:find("Arm") or entry.name:find("Hand") then
                            xMult = apexConfig.armWidth
                        elseif entry.name:find("Leg") or entry.name:find("Foot") then
                            xMult = apexConfig.legWidth
                        end

                        local scaledPos = Vector3.new(rel.Position.X * apexConfig.size * xMult, rel.Position.Y * apexConfig.size, rel.Position.Z * apexConfig.size)

                        if entry.name:find("LowerArm") then
                            scaledPos = scaledPos + Vector3.new(0, -apexConfig.armSpacing, 0)
                        elseif entry.name:find("Hand") then
                            scaledPos = scaledPos + Vector3.new(0, -apexConfig.armSpacing * 2, 0)
                        end

                        if entry.name:find("Leg") or entry.name:find("Foot") then
                            scaledPos = scaledPos + Vector3.new(0, apexConfig.legOffsetY, 0)
                            if entry.name:find("LowerLeg") then
                                scaledPos = scaledPos + Vector3.new(0, apexConfig.legSpacing, 0)
                            elseif entry.name:find("UpperLeg") then
                                scaledPos = scaledPos + Vector3.new(0, apexConfig.legSpacing * 2, 0)
                            end
                        end

                        local relRot = rel - rel.Position

                        local extraOffset = entry.offset or CFrame.new()
                        if entry.name == "UpperTorso" then
                            extraOffset = CFrame.new(apexConfig.torsoOffsetX, apexConfig.torsoOffsetY, apexConfig.torsoOffsetZ)
                                * CFrame.Angles(math.rad(apexConfig.torsoRotX), math.rad(apexConfig.torsoRotY), math.rad(apexConfig.torsoRotZ))
                        elseif entry.name == "LowerTorso" then
                            extraOffset = (entry.offset or CFrame.new())
                                * CFrame.new(apexConfig.pelvisOffsetX, apexConfig.pelvisOffsetY, apexConfig.pelvisOffsetZ)
                                * CFrame.Angles(math.rad(apexConfig.pelvisRotX), math.rad(apexConfig.pelvisRotY), math.rad(apexConfig.pelvisRotZ))
                        end

                        local targetCF = centerCF * CFrame.new(scaledPos) * relRot * extraOffset

                        sendCF(entry.scf, targetCF, entry.prop)
                    end
                end
            end

            
            if humNow and humNow.MoveDirection.Magnitude > 0.05 then
                if humNow:GetState() ~= Enum.HumanoidStateType.Running then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Running) end)
                end
                if humNow.WalkSpeed < apexConfig.walkSpeed then
                    humNow.WalkSpeed = apexConfig.walkSpeed
                end

                local hasRun = false
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("idle") or animName:find("walk") then
                        track:Stop(0.05)
                    elseif animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        hasRun = true
                        track:AdjustSpeed(apexConfig.animSpeed)
                    end
                end

                if not hasRun then
                    local animScript = cc:FindFirstChild("Animate")
                    local runAnim = animScript and animScript:FindFirstChild("run") and animScript.run:FindFirstChildOfClass("Animation")
                    if runAnim then
                        pcall(function()
                            local runTrack = humNow:LoadAnimation(runAnim)
                            runTrack.Priority = Enum.AnimationPriority.Action4
                            runTrack:Play(0.05)
                            runTrack:AdjustSpeed(apexConfig.animSpeed)
                        end)
                    end
                end
            else
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        track:Stop(0.1)
                    end
                end
            end

            
            if humNow and humNow.HipHeight ~= apexConfig.playerLift and currentHipHeightVal >= apexConfig.playerLift then
                humNow.HipHeight = apexConfig.playerLift
            end

            
            if apexColorTheme == "Rainbow" then
                local nowTick = tick()
                for i, entry in ipairs(APEX_PARTS) do
                    local waveColor = Color3.fromHSV((nowTick * 0.4 + (i / #APEX_PARTS)) % 1, 1, 1)

                    if entry.prop then
                        pcall(function()
                            for _, p in ipairs(entry.prop:GetDescendants()) do
                                if p:IsA("BasePart") then
                                    p.Color = waveColor
                                end
                            end
                        end)
                    end

                    if entry.cpc and (nowTick - (entry.lastColorSent or 0) >= 0.25) then
                        entry.lastColorSent = nowTick
                        task.spawn(function()
                            pcall(function() entry.cpc:InvokeServer(waveColor) end)
                        end)
                    end
                end
            end
        end)
    end

    _G.BTR_StopApexTitan = stopApexTitan

    
    
    
    BuildV2Tab:AddSection({"> Apex Dreadnought Titan (Robot 5 - Cyber God)"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Apex Dreadnought Titan",
        Description = "Pilote an apex 15-prop winged war colossus with 8 railgun cannons & plasma blades",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not apexActive then return end
            if val then
                task.spawn(spawnApexTitan)
            else
                stopApexTitan()
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Apex Titan Color Theme",
        Description = "Select god mech finish or animated rainbow stream",
        Options = {
            "Default",
            "Rainbow",
            "Cyber Neon",
            "Solar Flare",
            "Abyssal Void",
            "Toxic Hazard",
            "Crimson Fury",
            "Glacial Frost"
        },
        Default = "Default",
        Callback = function(choice)
            applyApexTheme(choice)
        end
    })

end

do
    local lp = game:GetService("Players").LocalPlayer
    local rs = game:GetService("RunService")
    local rep = game:GetService("ReplicatedStorage")

    local gigActive = false
    local gigLoop = nil
    local gigPhysicsLoop = nil
    local myPropName = "Prop" .. lp.Name

    local savedHipHeight = 2.0
    local savedWalkSpeed = 16
    local savedJumpPower = 50
    local savedUseJumpPower = false
    local currentHipHeightVal = 55.0
    local savedTransparencies = {}
    local gigColorTheme = "Default"

    local gigPartsList = {}
    local currentGigOrigin = nil

    local gigConfig = {
        size = 11.0,
        height = 1.0,
        playerLift = 55.0,
        posZ = 0.0,
        posX = 0.0,
        rotY = 0.0,
        walkSpeed = 44.0,
        animSpeed = 0.90,
        torsoRotX = 90.0,
        torsoRotY = 0.0,
        torsoRotZ = 0.0,
        torsoOffsetX = 1.0,
        torsoOffsetY = -2.0,
        torsoOffsetZ = -3.0,
        armWidth = 1.03,
        armSpacing = 5.45,
        legWidth = 0.74,
        legOffsetY = 0.0,
        legSpacing = 0.0,
        pelvisOffsetX = -2.00,
        pelvisOffsetY = -3.50,
        pelvisOffsetZ = -1.00,
        pelvisRotX = 0.00,
        pelvisRotY = 85.00,
        pelvisRotZ = 0.00,
    }

    local GIG_THEMES = {
        ["Default"]       = Color3.fromRGB(140, 150, 165),
        ["Cyber Neon"]    = Color3.fromRGB(0, 240, 255),
        ["Solar Flare"]   = Color3.fromRGB(255, 215, 0),
        ["Abyssal Void"]  = Color3.fromRGB(18, 18, 22),
        ["Toxic Hazard"]  = Color3.fromRGB(57, 255, 20),
        ["Crimson Fury"]  = Color3.fromRGB(220, 20, 35),
        ["Glacial Frost"] = Color3.fromRGB(0, 225, 255),
    }

    local function gNotif(title, text, dur)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = dur or 3
            })
        end)
    end

    local function applyPlayerLift(newVal)
        currentHipHeightVal = newVal
        local char = lp.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.HipHeight = newVal
            end
        end
    end

    local function smoothClimbTo(targetVal, step)
        step = step or 2.0
        task.spawn(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            local current = (hum and hum.HipHeight) or 2.0
            if current < targetVal then
                for v = current + step, targetVal, step do
                    if not gigActive then break end
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                if gigActive then applyPlayerLift(targetVal) end
            else
                for v = current - step, targetVal, -step do
                    applyPlayerLift(v)
                    task.wait(0.02)
                end
                applyPlayerLift(targetVal)
            end
        end)
    end

    local function setCharacterHidden(hide)
        local char = lp.Character
        if not char then return end
        if hide then
            savedTransparencies = {}
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") or part:IsA("Decal") then
                    savedTransparencies[part] = part.Transparency
                    part.Transparency = 1
                end
            end
        else
            for part, origVal in pairs(savedTransparencies) do
                if part and part.Parent then
                    part.Transparency = origVal
                end
            end
            savedTransparencies = {}
            for _, part in ipairs(char:GetDescendants()) do
                if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                    part.CanCollide = true
                    part.CanTouch = true
                end
            end
        end
    end

    local function getOrFetchPropMaker()
        local backpack = lp:FindFirstChild("Backpack")
        local character = lp.Character
        if not backpack or not character then return nil end
        local tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
        if not tool then
            local RE = rep:FindFirstChild("RE")
            local toolRemote = RE and RE:FindFirstChild("1Too1l")
            if toolRemote then
                pcall(function() toolRemote:InvokeServer("PickingTools", "PropMaker") end)
                local start = tick()
                while tick() - start < 2.0 do
                    tool = character:FindFirstChild("PropMaker") or backpack:FindFirstChild("PropMaker")
                    if tool then break end
                    task.wait(0.05)
                end
            end
        end
        if not tool then return nil end
        if tool.Parent ~= character then
            local hum = character:FindFirstChildOfClass("Humanoid")
            if hum then
                hum:EquipTool(tool)
                local eqStart = tick()
                while tick() - eqStart < 2.0 do
                    if tool.Parent == character then break end
                    task.wait(0.05)
                end
            end
        end
        return tool
    end

    local function duplicateExistingProp(firstProp, countNeeded)
        if not firstProp then return false end
        local dupRemote = firstProp:FindFirstChild("DuplicateProp") or firstProp:WaitForChild("DuplicateProp", 2)
        if not dupRemote then return false end
        for i = 1, countNeeded do
            pcall(function() dupRemote:InvokeServer() end)
            task.wait(0.05)
        end
        return true
    end

    local senders = {}
    local RATE_LIMIT = 0.03

    local function sendCF(scf, cf, prop)
        if not gigActive or not scf then return end

        if prop then
            pcall(function()
                if prop:IsA("Model") then
                    prop:PivotTo(cf)
                elseif prop:IsA("BasePart") then
                    prop.CFrame = cf
                end
            end)
        end

        local entry = senders[scf]
        local now = tick()
        if not entry then
            entry = { lastSent = 0, nextCF = cf, sending = false }
            senders[scf] = entry
        else
            entry.nextCF = cf
        end

        if not entry.sending and (now - entry.lastSent >= RATE_LIMIT) then
            entry.sending = true
            entry.lastSent = now
            local toSend = entry.nextCF
            entry.nextCF = nil
            task.spawn(function()
                pcall(function() scf:InvokeServer(toSend) end)
                entry.sending = false
                if gigActive and entry.nextCF and (tick() - entry.lastSent >= RATE_LIMIT) then
                    entry.sending = true
                    entry.lastSent = tick()
                    local nextSend = entry.nextCF
                    entry.nextCF = nil
                    task.spawn(function()
                        pcall(function() scf:InvokeServer(nextSend) end)
                        entry.sending = false
                    end)
                end
            end)
        end
    end

    local function applyGigTheme(themeName)
        gigColorTheme = themeName
        if not gigActive or #gigPartsList == 0 then return end

        if themeName == "Rainbow" then
            gNotif("Gigantic Titan", "Theme: Rainbow Stream", 2)
            return
        end

        local solidCol = GIG_THEMES[themeName] or Color3.fromRGB(140, 150, 165)
        for _, entry in ipairs(gigPartsList) do
            if entry.prop then
                pcall(function()
                    for _, p in ipairs(entry.prop:GetDescendants()) do
                        if p:IsA("BasePart") then
                            p.Color = solidCol
                        end
                    end
                end)
            end
            if entry.cpc then
                task.spawn(function()
                    pcall(function() entry.cpc:InvokeServer(solidCol) end)
                end)
            end
        end
        gNotif("Gigantic Titan", "Theme: " .. themeName, 2)
    end

    local function stopGiganticTitan()
        if not gigActive then return end
        gigActive = false
        if gigLoop then
            gigLoop:Disconnect()
            gigLoop = nil
        end
        if gigPhysicsLoop then
            gigPhysicsLoop:Disconnect()
            gigPhysicsLoop = nil
        end
        currentGigOrigin = nil
        senders = {}
        gigPartsList = {}

        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        local naturalHip = savedHipHeight or (hum and hum.HipHeight) or 2.35
        if hum then
            hum.WalkSpeed = savedWalkSpeed or 16
            if savedJumpPower then hum.JumpPower = savedJumpPower end
            if savedUseJumpPower ~= nil then hum.UseJumpPower = savedUseJumpPower end
            pcall(function() hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true) end)
            for _, track in ipairs(hum:GetPlayingAnimationTracks()) do
                if track.Priority == Enum.AnimationPriority.Action4 or track.Name:lower():find("run") then
                    pcall(function() track:Stop(0.1) end)
                end
            end
        end

        smoothClimbTo(naturalHip, 4.0)
        task.wait(0.35)
        setCharacterHidden(false)

        local debugMsg = "Normal"
        if hrp then
            hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            local landParams = RaycastParams.new()
            landParams.FilterDescendantsInstances = {char, workspace:FindFirstChild("WorkspaceCom")}
            landParams.FilterType = Enum.RaycastFilterType.Exclude
            local groundRay = workspace:Raycast(hrp.Position + Vector3.new(0, 5, 0), Vector3.new(0, -50, 0), landParams)
            if groundRay and groundRay.Position then
                local targetY = groundRay.Position.Y + naturalHip + 1.6
                hrp.CFrame = CFrame.new(hrp.Position.X, targetY, hrp.Position.Z) * hrp.CFrame.Rotation
                debugMsg = string.format("Y:%.1f | Hip:%.2f", targetY, naturalHip)
            end
        end
        if hum then
            hum.HipHeight = naturalHip
            pcall(function() hum:ChangeState(Enum.HumanoidStateType.Landed) end)
        end

        task.wait(0.15)
        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.3)
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        gNotif("Gigantic Titan", "Dismantled [" .. debugMsg .. "]", 3)
    end

    local function getLimbPart(character, limbName)
        local part = character:FindFirstChild(limbName)
        if part then return part end
        if limbName == "UpperTorso" or limbName == "LowerTorso" then
            return character:FindFirstChild("Torso") or character:FindFirstChild("HumanoidRootPart")
        elseif limbName == "Head" then
            return character:FindFirstChild("Head")
        elseif limbName:find("Left") and (limbName:find("Arm") or limbName:find("Hand")) then
            return character:FindFirstChild("Left Arm")
        elseif limbName:find("Right") and (limbName:find("Arm") or limbName:find("Hand")) then
            return character:FindFirstChild("Right Arm")
        elseif limbName:find("Left") and (limbName:find("Leg") or limbName:find("Foot")) then
            return character:FindFirstChild("Left Leg")
        elseif limbName:find("Right") and (limbName:find("Leg") or limbName:find("Foot")) then
            return character:FindFirstChild("Right Leg")
        end
        return nil
    end

    local function spawnGiganticTitan()
        if gigActive then stopGiganticTitan() task.wait(0.5) end
        gigActive = true
        local char = lp.Character or lp.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        local hum = char:WaitForChild("Humanoid")

        savedHipHeight = hum.HipHeight
        savedWalkSpeed = hum.WalkSpeed
        savedJumpPower = hum.JumpPower
        savedUseJumpPower = hum.UseJumpPower
        hum.WalkSpeed = gigConfig.walkSpeed
        pcall(function() hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true) end)
        gNotif("Gigantic Titan", "Assembling Titan (5 Bunkers + 10 Beds)...", 4)

        local clearTools = rep:WaitForChild("RE"):WaitForChild("1Clea1rTool1s")
        local toolRE = rep:WaitForChild("RE"):WaitForChild("1Too1l")
        pcall(function() clearTools:FireServer("ClearAllProps") end)
        task.wait(0.5)

        local propsTool = getOrFetchPropMaker()
        if not propsTool then return end

        pcall(function() clearTools:FireServer("OpenPropMenu") end)
        task.wait(0.2)

        local wc = workspace:FindFirstChild("WorkspaceCom")
        if not wc then gNotif("Gigantic Titan", "WorkspaceCom missing!", 3) return end

        local bunkerTarget = hrp.Position + (hrp.CFrame.LookVector * 14)
        local bedTarget    = hrp.Position + (hrp.CFrame.RightVector * 11)

        local Telemetry = rep:FindFirstChild("Remotes") and rep.Remotes:FindFirstChild("TelemetryClientInteraction")
        local existingProps = {}
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName then
                    existingProps[prop] = true
                end
            end
        end

        local function spawnSingleProp(propName, categoryName, targetPos)
            local tool = getOrFetchPropMaker()
            if not tool then return nil end

            if Telemetry then
                pcall(function() Telemetry:FireServer("filterClick", {name = categoryName, itemType = "Props"}) end)
                task.wait(0.05)
                pcall(function() Telemetry:FireServer("filterClick", {name = propName, itemType = "Props"}) end)
                task.wait(0.05)
            end

            pcall(function() clearTools:FireServer("RequestingPropName", propName, categoryName) end)
            task.wait(0.3)

            local currentHum = lp.Character and lp.Character:FindFirstChildOfClass("Humanoid")
            if currentHum and tool.Parent ~= lp.Character then
                currentHum:EquipTool(tool)
                task.wait(0.1)
            end

            local tpm = tool:FindFirstChild("Tool_PropMake")
            local startW = tick()
            while not tpm and tick() - startW < 4.0 and gigActive do
                tpm = tool:FindFirstChild("Tool_PropMake")
                if not tpm then
                    local bp = lp.Backpack:FindFirstChild("PropMaker")
                    if bp then tpm = bp:FindFirstChild("Tool_PropMake") end
                end
                if not tpm then task.wait(0.1) end
            end

            if not tpm then return nil end

            pcall(function() tpm:FireServer(workspace.Terrain, targetPos) end)

            local spawned = nil
            local t0 = tick()
            while tick() - t0 < 4.0 and gigActive do
                for _, folder in pairs(wc:GetChildren()) do
                    for _, prop in pairs(folder:GetChildren()) do
                        if prop.Name == myPropName and not existingProps[prop] then
                            spawned = prop
                            existingProps[prop] = true
                            break
                        end
                    end
                    if spawned then break end
                end
                if spawned then break end
                task.wait(0.05)
            end

            return spawned
        end

        
        local firstBunker = spawnSingleProp("BigGunBunker", "Big", bunkerTarget)
        if not firstBunker then firstBunker = spawnSingleProp("BigGunBunker", "Big", bunkerTarget) end
        if not firstBunker then gNotif("Gigantic Titan", "Bunker prop failed!", 3) return end
        duplicateExistingProp(firstBunker, 4)
        task.wait(0.2)

        local bunkerProps = { firstBunker }
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and not existingProps[prop] then
                    existingProps[prop] = true
                    table.insert(bunkerProps, prop)
                end
            end
        end

        
        local firstBed = spawnSingleProp("FuturisticLargeBed", "Furniture", bedTarget)
        if not firstBed then firstBed = spawnSingleProp("FuturisticLargeBed", "Furniture", bedTarget) end
        if not firstBed then gNotif("Gigantic Titan", "Bed prop failed!", 3) return end
        duplicateExistingProp(firstBed, 9)
        task.wait(0.4)

        local bedProps = { firstBed }
        for _, folder in pairs(wc:GetChildren()) do
            for _, prop in pairs(folder:GetChildren()) do
                if prop.Name == myPropName and not existingProps[prop] then
                    existingProps[prop] = true
                    table.insert(bedProps, prop)
                end
            end
        end

        -- Retrait du carton apres 1 seconde (LOCKED Rule 7)
        task.wait(1.0)
        pcall(function() toolRE:InvokeServer("PickingTools", "PropMaker") end)
        pcall(function() clearTools:FireServer("ClosePropMenu") end)

        local function getPropRemotes(p)
            if not p then return nil, nil end
            local scf = p:FindFirstChild("SetCurrentCFrame") or p:FindFirstChild("SetCurrentCFrame", true) or p:FindFirstChildWhichIsA("RemoteFunction", true)
            local cpc = p:FindFirstChild("ChangePropColor") or p:FindFirstChild("ChangePropColor", true)
            return scf, cpc
        end

        local bk1Scf, bk1Cpc = getPropRemotes(bunkerProps[1])
        local bk2Scf, bk2Cpc = getPropRemotes(bunkerProps[2])
        local bk3Scf, bk3Cpc = getPropRemotes(bunkerProps[3])
        local bk4Scf, bk4Cpc = getPropRemotes(bunkerProps[4])
        local bk5Scf, bk5Cpc = getPropRemotes(bunkerProps[5])

        local bd1Scf, bd1Cpc = getPropRemotes(bedProps[1])
        local bd2Scf, bd2Cpc = getPropRemotes(bedProps[2])
        local bd3Scf, bd3Cpc = getPropRemotes(bedProps[3])
        local bd4Scf, bd4Cpc = getPropRemotes(bedProps[4])
        local bd5Scf, bd5Cpc = getPropRemotes(bedProps[5])
        local bd6Scf, bd6Cpc = getPropRemotes(bedProps[6])
        local bd7Scf, bd7Cpc = getPropRemotes(bedProps[7])
        local bd8Scf, bd8Cpc = getPropRemotes(bedProps[8])
        local bd9Scf, bd9Cpc = getPropRemotes(bedProps[9])
        local bd10Scf, bd10Cpc = getPropRemotes(bedProps[10])

        if not bk1Scf then gNotif("Gigantic Titan", "Torso SCF missing!", 3) return end

        task.wait(0.3)
        smoothClimbTo(gigConfig.playerLift, 2.0)
        setCharacterHidden(true)

        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        
        local GIG_PARTS = {
            { name = "UpperTorso",    scf = bk1Scf, prop = bunkerProps[1], cpc = bk1Cpc, offset = CFrame.new(0, -2.5, 0) },
            { name = "Head",          scf = bd1Scf, prop = bedProps[1],    cpc = bd1Cpc, offset = CFrame.new(0, 1.5, 0) * CFrame.Angles(math.rad(-90), 0, 0) },
            { name = "LowerTorso",    scf = bd2Scf, prop = bedProps[2],    cpc = bd2Cpc, offset = CFrame.Angles(0, 0, math.rad(90)) },

            { name = "LeftUpperArm",  scf = bd3Scf, prop = bedProps[3],    cpc = bd3Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftLowerArm",  scf = bk2Scf, prop = bunkerProps[2], cpc = bk2Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftHand",      scf = bd4Scf, prop = bedProps[4],    cpc = bd4Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },

            { name = "RightUpperArm", scf = bd5Scf, prop = bedProps[5],    cpc = bd5Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightLowerArm", scf = bk3Scf, prop = bunkerProps[3], cpc = bk3Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightHand",     scf = bd6Scf, prop = bedProps[6],    cpc = bd6Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },

            { name = "LeftUpperLeg",  scf = bd7Scf, prop = bedProps[7],    cpc = bd7Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftLowerLeg",  scf = bk4Scf, prop = bunkerProps[4], cpc = bk4Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },
            { name = "LeftFoot",      scf = bd8Scf, prop = bedProps[8],    cpc = bd8Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(90)) },

            { name = "RightUpperLeg", scf = bd9Scf, prop = bedProps[9],    cpc = bd9Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightLowerLeg", scf = bk5Scf, prop = bunkerProps[5], cpc = bk5Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
            { name = "RightFoot",     scf = bd10Scf, prop = bedProps[10],  cpc = bd10Cpc, offset = CFrame.Angles(math.rad(90), 0, math.rad(-90)) },
        }
        gigPartsList = GIG_PARTS

        
        gigPhysicsLoop = rs.Stepped:Connect(function()
            if not gigActive then return end
            local cc = lp.Character
            if cc then
                for _, part in ipairs(cc:GetDescendants()) do
                    if part:IsA("BasePart") and part.Name ~= "HumanoidRootPart" then
                        part.CanCollide = false
                        part.CanTouch = false
                    end
                end
            end
            for _, entry in ipairs(gigPartsList) do
                if entry.prop then
                    pcall(function()
                        for _, p in ipairs(entry.prop:GetDescendants()) do
                            if p:IsA("BasePart") then
                                p.CanCollide = false
                                p.CanTouch = false
                                p.CanQuery = false
                                p.Massless = true
                            end
                        end
                    end)
                end
            end
        end)

        applyGigTheme(gigColorTheme)
        gNotif("Gigantic Titan", "Titan Online! 5 Bunkers + 10 Beds = 15/15", 3)

        if gigLoop then gigLoop:Disconnect() gigLoop = nil end
        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude

        gigLoop = rs.RenderStepped:Connect(function()
            if not gigActive then return end

            local cc = lp.Character
            if not cc then return end
            local hrpNow = cc:FindFirstChild("HumanoidRootPart")
            if not hrpNow then return end
            local humNow = cc:FindFirstChildOfClass("Humanoid")

            local rayFilter = {cc, workspace:FindFirstChild("WorkspaceCom")}
            for _, plr in ipairs(game:GetService("Players"):GetPlayers()) do
                if plr.Character and plr.Character ~= cc then
                    table.insert(rayFilter, plr.Character)
                end
            end
            rayParams.FilterDescendantsInstances = rayFilter
            local ray = workspace:Raycast(hrpNow.Position + Vector3.new(0, 10, 0), Vector3.new(0, -250, 0), rayParams)
            local groundY = ray and ray.Position.Y
            local minFloorY = groundY and (groundY + currentHipHeightVal) or nil

            if minFloorY and hrpNow.Position.Y < minFloorY then
                local vel = hrpNow.AssemblyLinearVelocity
                if vel.Y < 0 then
                    hrpNow.AssemblyLinearVelocity = Vector3.new(vel.X, 0, vel.Z)
                end
                hrpNow.CFrame = CFrame.new(hrpNow.Position.X, minFloorY, hrpNow.Position.Z) * hrpNow.CFrame.Rotation
                if humNow and humNow:GetState() == Enum.HumanoidStateType.Freefall then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Landed) end)
                end
            end

            local clampedY = hrpNow.Position.Y
            if minFloorY and clampedY < minFloorY then
                clampedY = minFloorY
            end
            local clampedHrpCF = CFrame.new(hrpNow.Position.X, clampedY, hrpNow.Position.Z) * hrpNow.CFrame.Rotation
            local targetCenterCF = clampedHrpCF * CFrame.new(gigConfig.posX, gigConfig.height, gigConfig.posZ) * CFrame.Angles(0, math.rad(gigConfig.rotY), 0)
            if not currentGigOrigin then
                currentGigOrigin = targetCenterCF
            else
                currentGigOrigin = currentGigOrigin:Lerp(targetCenterCF, 0.22)
            end
            local centerCF = currentGigOrigin

            for _, entry in ipairs(GIG_PARTS) do
                if entry.scf then
                    local part = getLimbPart(cc, entry.name)
                    if part then
                        local rel = clampedHrpCF:ToObjectSpace(part.CFrame)

                        local xMult = 1.0
                        if entry.name:find("Arm") or entry.name:find("Hand") then
                            xMult = gigConfig.armWidth
                        elseif entry.name:find("Leg") or entry.name:find("Foot") then
                            xMult = gigConfig.legWidth
                        end

                        local scaledPos = Vector3.new(rel.Position.X * gigConfig.size * xMult, rel.Position.Y * gigConfig.size, rel.Position.Z * gigConfig.size)

                        if entry.name:find("LowerArm") then
                            scaledPos = scaledPos + Vector3.new(0, -gigConfig.armSpacing, 0)
                        elseif entry.name:find("Hand") then
                            scaledPos = scaledPos + Vector3.new(0, -gigConfig.armSpacing * 2, 0)
                        end

                        if entry.name:find("Leg") or entry.name:find("Foot") then
                            scaledPos = scaledPos + Vector3.new(0, gigConfig.legOffsetY, 0)
                            if entry.name:find("LowerLeg") then
                                scaledPos = scaledPos + Vector3.new(0, gigConfig.legSpacing, 0)
                            elseif entry.name:find("UpperLeg") then
                                scaledPos = scaledPos + Vector3.new(0, gigConfig.legSpacing * 2, 0)
                            end
                        end

                        local relRot = rel - rel.Position

                        local extraOffset = entry.offset or CFrame.new()
                        if entry.name == "UpperTorso" then
                            extraOffset = CFrame.new(gigConfig.torsoOffsetX, gigConfig.torsoOffsetY, gigConfig.torsoOffsetZ)
                                * CFrame.Angles(math.rad(gigConfig.torsoRotX), math.rad(gigConfig.torsoRotY), math.rad(gigConfig.torsoRotZ))
                        elseif entry.name == "LowerTorso" then
                            extraOffset = (entry.offset or CFrame.new())
                                * CFrame.new(gigConfig.pelvisOffsetX, gigConfig.pelvisOffsetY, gigConfig.pelvisOffsetZ)
                                * CFrame.Angles(math.rad(gigConfig.pelvisRotX), math.rad(gigConfig.pelvisRotY), math.rad(gigConfig.pelvisRotZ))
                        end

                        local targetCF = centerCF * CFrame.new(scaledPos) * relRot * extraOffset

                        sendCF(entry.scf, targetCF, entry.prop)
                    end
                end
            end

            
            if humNow and humNow.MoveDirection.Magnitude > 0.05 then
                if humNow:GetState() ~= Enum.HumanoidStateType.Running then
                    pcall(function() humNow:ChangeState(Enum.HumanoidStateType.Running) end)
                end
                if humNow.WalkSpeed < gigConfig.walkSpeed then
                    humNow.WalkSpeed = gigConfig.walkSpeed
                end

                local hasRun = false
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("idle") or animName:find("walk") then
                        track:Stop(0.05)
                    elseif animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        hasRun = true
                        track:AdjustSpeed(gigConfig.animSpeed)
                    end
                end

                if not hasRun then
                    local animScript = cc:FindFirstChild("Animate")
                    local runAnim = animScript and animScript:FindFirstChild("run") and animScript.run:FindFirstChildOfClass("Animation")
                    if runAnim then
                        pcall(function()
                            local runTrack = humNow:LoadAnimation(runAnim)
                            runTrack.Priority = Enum.AnimationPriority.Action4
                            runTrack:Play(0.05)
                            runTrack:AdjustSpeed(gigConfig.animSpeed)
                        end)
                    end
                end
            else
                for _, track in ipairs(humNow:GetPlayingAnimationTracks()) do
                    local animName = track.Name:lower()
                    if animName:find("run") and track.Priority == Enum.AnimationPriority.Action4 then
                        track:Stop(0.1)
                    end
                end
            end

            
            if humNow and humNow.HipHeight ~= gigConfig.playerLift and currentHipHeightVal >= gigConfig.playerLift then
                humNow.HipHeight = gigConfig.playerLift
            end

            
            if gigColorTheme == "Rainbow" then
                local nowTick = tick()
                for i, entry in ipairs(GIG_PARTS) do
                    local waveColor = Color3.fromHSV((nowTick * 0.4 + (i / #GIG_PARTS)) % 1, 1, 1)

                    if entry.prop then
                        pcall(function()
                            for _, p in ipairs(entry.prop:GetDescendants()) do
                                if p:IsA("BasePart") then
                                    p.Color = waveColor
                                end
                            end
                        end)
                    end

                    if entry.cpc and (nowTick - (entry.lastColorSent or 0) >= 0.25) then
                        entry.lastColorSent = nowTick
                        task.spawn(function()
                            pcall(function() entry.cpc:InvokeServer(waveColor) end)
                        end)
                    end
                end
            end
        end)
    end

    _G.BTR_StopGiganticTitan = stopGiganticTitan

    
    
    
    BuildV2Tab:AddSection({"> Gigantic Titan Robot (Robot 6 - Bunker + Beds)"})

    BuildV2Tab:AddToggle({
        Name = "Spawn Gigantic Titan",
        Description = "Pilote a 15-prop war titan made of 5 Bunkers (joints) + 10 Beds (limbs)",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if not val and not gigActive then return end
            if val then
                task.spawn(spawnGiganticTitan)
            else
                stopGiganticTitan()
            end
        end
    })

    BuildV2Tab:AddDropdown({
        Name = "Gigantic Titan Color Theme",
        Description = "Select titan armor finish or animated rainbow stream",
        Options = {
            "Default",
            "Rainbow",
            "Cyber Neon",
            "Solar Flare",
            "Abyssal Void",
            "Toxic Hazard",
            "Crimson Fury",
            "Glacial Frost"
        },
        Default = "Default",
        Callback = function(choice)
            applyGigTheme(choice)
        end
    })

end

PremiumTab:AddSection({
    "> Prop Unlocker"
})

local VIP_PROPS_DATA = {
    { Name = "Golden Toilet", Id = "GoldenToilet" },
    { Name = "Military Heavy Minigun", Id = "MilitaryMiniGunVIP" },
    { Name = "Military Car Minigun", Id = "MilitaryCarMiniGunVIP" },
    { Name = "VIP Gun Rack", Id = "VIPGunRack" },
    { Name = "Horse - Akhal-Teke", Id = "PropAkhal-Teke" },
    { Name = "Horse - Friesian", Id = "PropFriesian" },
    { Name = "Horse - Shire Giant", Id = "PropShire" },
    { Name = "Horse - Clydesdale", Id = "PropClydesdale" },
    { Name = "Horse - Mini Pony", Id = "PropMiniHorse" },
    { Name = "Donkey", Id = "PropDonkey" },
    { Name = "Big Trophy Fish", Id = "AnimalFishBigVIP" },
    { Name = "Carnival Dunk Tank", Id = "DunkTank" },
    { Name = "Popcorn Machine", Id = "PopcornMachine" },
    { Name = "Carnival Booth", Id = "CarnivalBooth" },
    { Name = "Carnival Tent", Id = "CarnivalTent" },
    { Name = "Carnival Neon Sign 1", Id = "CarnivalSign1" },
    { Name = "Carnival Neon Sign 2", Id = "CarnivalSign2" },
    { Name = "Carnival Neon Sign 5", Id = "CarnivalSign5" },
    { Name = "Flashing Light Arch", Id = "FlashingLightArch" },
    { Name = "Bench Swing", Id = "BenchSwing" },
    { Name = "Boy Play Table", Id = "BoyPlayTable" },
    { Name = "Girl Play Table", Id = "GirlPlayTable" },
    { Name = "Summer Water Slip & Slide", Id = "Summer2026SlipAndSlide" },
    { Name = "Summer Trampoline", Id = "Summer2026Trampoline" },
    { Name = "Summer Photo Booth", Id = "Summer2026PhotoBooth" },
    { Name = "Summer Bobcat Digger", Id = "Summer2026BobcatProp" },
    { Name = "Unicorn Plush", Id = "UnicornPlush" },
    { Name = "Giant Bear Plush", Id = "BearPlush" },
    { Name = "Car Engine Blower", Id = "CarEngineBlower" },
    { Name = "Car Full Color VIP", Id = "CarFullColorVIP" },
    { Name = "Car Race Scoop", Id = "CarRaceScoopeVIP" },
    { Name = "Car Truck Orange Lights", Id = "CarTruckLightsOrange" },
    { Name = "Car Truck Turbo 3", Id = "CarTruckTurbo3" },
    { Name = "Car Half Color Wing", Id = "CarWingHalfColorVIP" },
    { Name = "Fart Machine", Id = "ToolsFart" },
    { Name = "Agency Graffiti", Id = "GraffitiAgency" }
}

local vipNamesList = {}
local vipIdMap = {}
for _, prop in ipairs(VIP_PROPS_DATA) do
    table.insert(vipNamesList, prop.Name)
    vipIdMap[prop.Name] = prop.Id
end

local selectedVIPPropName = vipNamesList[1]
local selectedVIPPropId = vipIdMap[selectedVIPPropName]

local function getOrEquipVIPPropMaker()
    local lp = game:GetService("Players").LocalPlayer
    local char = lp.Character or lp.CharacterAdded:Wait()
    local pm = char:FindFirstChild("PropMaker")
    if pm then return pm end
    pm = lp.Backpack:FindFirstChild("PropMaker")
    if not pm then
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        local toolRemote = RE and RE:FindFirstChild("1Too1l")
        if toolRemote then
            pcall(function() toolRemote:InvokeServer("PickingTools", "PropMaker") end)
            task.wait(0.2)
        end
        pm = lp.Backpack:FindFirstChild("PropMaker") or char:FindFirstChild("PropMaker")
    end
    if pm and pm.Parent ~= char then
        local hum = char:FindFirstChildOfClass("Humanoid")
        if hum then hum:EquipTool(pm) end
        task.wait(0.15)
    end
    return char:FindFirstChild("PropMaker") or pm
end

local function vipNotify(title, text)
    pcall(function()
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = title,
            Text = text,
            Duration = 3
        })
    end)
end

PremiumTab:AddDropdown({
    Name = "Select VIP Prop",
    Description = "Choose a VIP prop to unlock",
    Options = vipNamesList,
    Default = vipNamesList[1],
    Callback = function(val)
        selectedVIPPropName = val
        selectedVIPPropId = vipIdMap[val] or "GoldenToilet"
    end
})

PremiumTab:AddButton({
    Name = "Equip to PropMaker",
    Description = "Unlocks the prop into your hand to place freely",
    Callback = function()
        task.spawn(function()
            local pm = getOrEquipVIPPropMaker()
            if not pm then
                vipNotify("Prop Unlocker", "Failed to get PropMaker!")
                return
            end
            local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local clearRemote = RE and RE:FindFirstChild("1Clea1rTool1s")
            if clearRemote then
                pcall(function() clearRemote:FireServer("OpenPropMenu") end)
                pcall(function() clearRemote:FireServer("RequestingPropName", selectedVIPPropId, "Car", "Gamepass") end)
                vipNotify("Prop Unlocker", "Loaded: " .. selectedVIPPropName .. "! Click to place.")
            end
        end)
    end
})

PremiumTab:AddButton({
    Name = "Place In Front of Me",
    Description = "Spawns the VIP prop directly where you are standing",
    Callback = function()
        task.spawn(function()
            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            if not hrp then
                vipNotify("Prop Unlocker", "Character not found!")
                return
            end

            local pm = getOrEquipVIPPropMaker()
            if not pm then
                vipNotify("Prop Unlocker", "Failed to get PropMaker!")
                return
            end

            local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local clearRemote = RE and RE:FindFirstChild("1Clea1rTool1s")
            local toolRemote = RE and RE:FindFirstChild("1Too1l")
            if not clearRemote then return end

            pcall(function() clearRemote:FireServer("OpenPropMenu") end)
            pcall(function() clearRemote:FireServer("RequestingPropName", selectedVIPPropId, "Car", "Gamepass") end)

            local toolPropMake = pm:FindFirstChild("Tool_PropMake") or (lp.Backpack:FindFirstChild("PropMaker") and lp.Backpack.PropMaker:FindFirstChild("Tool_PropMake"))
            if not toolPropMake then
                local ws = tick()
                while not toolPropMake and tick() - ws < 1.5 do
                    task.wait(0.05)
                    toolPropMake = pm:FindFirstChild("Tool_PropMake") or (lp.Backpack:FindFirstChild("PropMaker") and lp.Backpack.PropMaker:FindFirstChild("Tool_PropMake"))
                end
            end

            if not toolPropMake then
                vipNotify("Prop Unlocker", "Tool_PropMake not ready!")
                return
            end

            local forwardPos = hrp.Position + (hrp.CFrame.LookVector * 6)
            local rayParams = RaycastParams.new()
            rayParams.FilterDescendantsInstances = {char}
            rayParams.FilterType = Enum.RaycastFilterType.Exclude

            local gRay = workspace:Raycast(forwardPos + Vector3.new(0, 5, 0), Vector3.new(0, -20, 0), rayParams)
            local hitInst = gRay and gRay.Instance or workspace.Terrain
            local groundPos = gRay and gRay.Position or Vector3.new(forwardPos.X, hrp.Position.Y - 2.5, forwardPos.Z)

            pcall(function() toolPropMake:FireServer(hitInst, groundPos) end)
            vipNotify("Prop Unlocker", "Spawned: " .. selectedVIPPropName .. "!")

            task.spawn(function()
                task.wait(1)
                if toolRemote then pcall(function() toolRemote:InvokeServer("PickingTools", "PropMaker") end) end
                pcall(function() clearRemote:FireServer("ClosePropMenu") end)
            end)
        end)
    end
})

PremiumTab:AddButton({
    Name = "Clear All Props",
    Description = "Removes all placed props from the map",
    Callback = function()
        pcall(function()
            local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local clearRemote = RE and RE:FindFirstChild("1Clea1rTool1s")
            if clearRemote then clearRemote:FireServer("ClearAllProps") end
        end)
        vipNotify("Prop Unlocker", "All props cleared!")
    end
})

PremiumTab:AddSection({
    "> " .. Translations["Vehicle Speed Unlocker"]
})

local premiumSpeed = 45
local speedSliderRef = nil
speedSliderRef = PremiumTab:AddSlider({
    Name = "Max Speed (25 - 500)",
    Description = "Sets the speed of ALL vehicles (MaxSpeed + TopSpeed + Turbo)",
    Min = 25,
    Max = 500,
    Increase = 5,
    Default = 45,
    Callback = function(v)
        premiumSpeed = v
    end
})

PremiumTab:AddButton({
    Name = "Apply Speed to My Car",
    Description = "Applies the speed to your current vehicle",
    Callback = function()
        local lp = game:GetService("Players").LocalPlayer
        local vehicles = workspace:FindFirstChild("Vehicles")
        local car = vehicles and vehicles:FindFirstChild(lp.Name .. "Car")
        local seat = car and (car:FindFirstChild("Seats") and car.Seats:FindFirstChild("VehicleSeat") or (car:FindFirstChild("Body") and car.Body:FindFirstChild("VehicleSeat")))
        if not seat then
            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Speed Unlocker", Text="Spawn a car first!", Duration=3})
            return
        end
        local applied = 0
        pcall(function()
            local ms = seat:FindFirstChild("MaxSpeed")
            if ms and ms:IsA("NumberValue") then ms.Value = premiumSpeed applied = applied + 1 end
            local ts = seat:FindFirstChild("TopSpeed")
            if ts and ts:IsA("NumberValue") then ts.Value = premiumSpeed applied = applied + 1 end
            local tb = seat:FindFirstChild("Turbo")
            if tb then tb.Value = tostring(premiumSpeed) end
        end)
        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Speed Unlocker", Text="Speed applied: " .. premiumSpeed, Duration=3})
    end
})

PremiumTab:AddButton({
    Name = "Reset Speed to Default",
    Description = "Resets speed back to default (45) for all vehicles",
    Callback = function()
        premiumSpeed = 45
        if speedSliderRef then
            pcall(function() speedSliderRef:Set(45) end)
        end
        local vehicles = workspace:FindFirstChild("Vehicles")
        local count = 0
        if vehicles then
            for _, car in ipairs(vehicles:GetChildren()) do
                pcall(function()
                    local seat = car:FindFirstChild("Seats") and car.Seats:FindFirstChild("VehicleSeat") or (car:FindFirstChild("Body") and car.Body:FindFirstChild("VehicleSeat"))
                    if seat then
                        local ms = seat:FindFirstChild("MaxSpeed")
                        if ms and ms:IsA("NumberValue") then ms.Value = 45 count = count + 1 end
                        local ts = seat:FindFirstChild("TopSpeed")
                        if ts and ts:IsA("NumberValue") then ts.Value = 45 end
                        local tb = seat:FindFirstChild("Turbo")
                        if tb then tb.Value = "45" end
                    end
                end)
            end
        end
        game:GetService("StarterGui"):SetCore("SendNotification", {Title="Speed Unlocker", Text="Reset speed to default (45) on " .. count .. " vehicles", Duration=3})
    end
})

PremiumTab:AddSection({
    "> VIP Items Giver"
})

local VIP_ITEMS_DATA = {
    { Name = "Tear Gas Launcher (VIP)", Id = "TearGasLauncher" },
    { Name = "Warden Pistol (VIP)", Id = "WardenPistol" },
    { Name = "Snowboard (VIP)", Id = "Snowboard" },
    { Name = "Skis (VIP)", Id = "Skis" },
    { Name = "Money Gun (VIP)", Id = "MoneyGun" }
}

local vipItemNames = {}
local vipItemMap = {}
for _, it in ipairs(VIP_ITEMS_DATA) do
    table.insert(vipItemNames, it.Name)
    vipItemMap[it.Name] = it.Id
end

local selectedVIPItemName = vipItemNames[1]
local selectedVIPItemId = vipItemMap[selectedVIPItemName]
local selectedVIPTargetName = "Myself"

local function getVIPTargetPlayers()
    local list = { "Myself" }
    for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
        if p ~= game:GetService("Players").LocalPlayer then
            table.insert(list, p.Name)
        end
    end
    return list
end

local function fireGiveVIPTool(targetPlayer, toolId)
    local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
    local triggerRemote = RE and RE:FindFirstChild("1Playe1rTrigge1rEven1t")
    local toolRemote = RE and RE:FindFirstChild("1Too1l")
    local lp = game:GetService("Players").LocalPlayer

    
    if targetPlayer == lp and toolRemote then
        pcall(function()
            toolRemote:InvokeServer("PickingTools", toolId)
        end)
    end

    if not triggerRemote or not targetPlayer or not toolId then return end

    local targetName = typeof(targetPlayer) == "Instance" and targetPlayer.Name or tostring(targetPlayer)
    local targetObj = typeof(targetPlayer) == "Instance" and targetPlayer or game:GetService("Players"):FindFirstChild(tostring(targetPlayer))

    pcall(function()
        triggerRemote:FireServer("ToolGiveToServer", targetName, toolId)
    end)
    if targetObj then
        pcall(function()
            triggerRemote:FireServer("ToolGiveToServer", targetObj, toolId)
        end)
        pcall(function()
            triggerRemote:FireServer("AcceptedToolToServer", toolId, targetObj)
        end)
    end
end

local function executeGiveItemSingle(targetPlayer, toolId, toolDisplayName)
    local lp = game:GetService("Players").LocalPlayer
    local myChar = lp.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    local origCF = myHRP and myHRP.CFrame

    local tChar = targetPlayer.Character
    local tHRP = tChar and tChar:FindFirstChild("HumanoidRootPart")

    if targetPlayer ~= lp and myHRP and tHRP then
        myHRP.CFrame = tHRP.CFrame * CFrame.new(0, 0, 2)
        task.wait(0.18)
    end

    fireGiveVIPTool(targetPlayer, toolId)
    vipNotify("VIP Items", "Gave " .. (toolDisplayName or toolId) .. " to " .. targetPlayer.DisplayName .. "!")

    if targetPlayer ~= lp and myHRP and origCF then
        task.wait(0.15)
        myHRP.CFrame = origCF
    end
end

local function executeGiveItemAll(toolId, toolDisplayName)
    local lp = game:GetService("Players").LocalPlayer
    local myChar = lp.Character
    local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
    local origCF = myHRP and myHRP.CFrame

    if not myHRP then
        vipNotify("VIP Items", "Character not found!")
        return
    end

    local players = game:GetService("Players"):GetPlayers()
    vipNotify("VIP Items", "Delivering " .. (toolDisplayName or toolId) .. " to " .. #players .. " players...")

    for _, p in ipairs(players) do
        if p ~= lp and p.Character then
            local targetHRP = p.Character:FindFirstChild("HumanoidRootPart")
            if targetHRP and myHRP and myHRP.Parent then
                myHRP.CFrame = targetHRP.CFrame * CFrame.new(0, 0, 2)
                task.wait(0.18)
                fireGiveVIPTool(p, toolId)
                task.wait(0.12)
            end
        elseif p == lp then
            fireGiveVIPTool(lp, toolId)
            task.wait(0.05)
        end
    end

    if myHRP and myHRP.Parent and origCF then
        myHRP.CFrame = origCF
    end

    vipNotify("VIP Items", "Delivered " .. (toolDisplayName or toolId) .. " to everyone & returned back!")
end

_G.BTR_ExecuteGiveItemSingle = executeGiveItemSingle
_G.BTR_ExecuteGiveItemAll = executeGiveItemAll

local function findMatchingVIPTool(itemInput)
    if not itemInput or itemInput == "" then
        return selectedVIPItemId, selectedVIPItemName
    end
    local clean = itemInput:lower():gsub("%s+", "")
    for _, it in ipairs(VIP_ITEMS_DATA) do
        local idClean = it.Id:lower():gsub("%s+", "")
        local nameClean = it.Name:lower():gsub("%s+", "")
        if idClean:find(clean, 1, true) or nameClean:find(clean, 1, true) then
            return it.Id, it.Name
        end
    end
    return itemInput, itemInput
end

_G.BTR_FindMatchingVIPTool = findMatchingVIPTool

local function runVIPAdminCommand(cmdText)
    if not cmdText or cmdText == "" then return end
    cmdText = cmdText:match("^%s*(.-)%s*$")
    if cmdText:sub(1, 1) == ":" or cmdText:sub(1, 1) == "/" or cmdText:sub(1, 1) == ";" or cmdText:sub(1, 1) == "!" then
        cmdText = cmdText:sub(2)
    end
    local verb, rest = cmdText:match("^([%w_]+)%s*(.*)$")
    if not verb then return end
    verb = verb:lower()
    rest = rest and rest:match("^%s*(.-)%s*$") or ""

    if verb == "giveall" then
        local toolId, toolName = findMatchingVIPTool(rest)
        task.spawn(function()
            executeGiveItemAll(toolId, toolName)
        end)
    elseif verb == "give" then
        local targetRaw, itemPart = rest:match("^([^%s]+)%s*(.*)$")
        if not targetRaw then
            vipNotify("VIP Cmds", "Usage: :give [player/all] [item]")
            return
        end
        if targetRaw:lower() == "all" or targetRaw:lower() == "everyone" then
            local toolId, toolName = findMatchingVIPTool(itemPart)
            task.spawn(function()
                executeGiveItemAll(toolId, toolName)
            end)
        else
            local targetPlayer = nil
            local Players = game:GetService("Players")
            local cleanT = targetRaw:lower()

            if cleanT == "me" or cleanT == "myself" then
                targetPlayer = Players.LocalPlayer
            else
                for _, p in ipairs(Players:GetPlayers()) do
                    if p.Name:lower() == cleanT or p.DisplayName:lower() == cleanT then
                        targetPlayer = p
                        break
                    end
                end
                if not targetPlayer then
                    for _, p in ipairs(Players:GetPlayers()) do
                        if p.Name:lower():sub(1, #cleanT) == cleanT or p.DisplayName:lower():sub(1, #cleanT) == cleanT then
                            targetPlayer = p
                            break
                        end
                    end
                end
                if not targetPlayer then
                    for _, p in ipairs(Players:GetPlayers()) do
                        if p.Name:lower():find(cleanT, 1, true) or p.DisplayName:lower():find(cleanT, 1, true) then
                            targetPlayer = p
                            break
                        end
                    end
                end
            end

            if not targetPlayer then
                vipNotify("VIP Cmds", "Player '" .. targetRaw .. "' not found!")
                return
            end

            local toolId, toolName = findMatchingVIPTool(itemPart)
            task.spawn(function()
                executeGiveItemSingle(targetPlayer, toolId, toolName)
            end)
        end
    else
        vipNotify("VIP Cmds", "Unknown command: " .. verb .. " (Use :give [player] [item] or :giveall [item])")
    end
end

_G.BTR_RunVIPAdminCommand = runVIPAdminCommand

PremiumTab:AddDropdown({
    Name = "Select VIP Item",
    Description = "Choose a VIP item to give",
    Options = vipItemNames,
    Default = vipItemNames[1],
    Callback = function(val)
        selectedVIPItemName = val
        selectedVIPItemId = vipItemMap[val] or "Snowboard"
    end
})

local vipTargetDropdown
vipTargetDropdown = PremiumTab:AddDropdown({
    Name = "Select Target Player",
    Description = "Choose who receives the item",
    Options = getVIPTargetPlayers(),
    Default = "Myself",
    Callback = function(val)
        selectedVIPTargetName = val
    end
})

PremiumTab:AddButton({
    Name = "Refresh Player List",
    Description = "Updates the target player dropdown with connected players",
    Callback = function()
        if vipTargetDropdown then
            pcall(function() vipTargetDropdown:Set(getVIPTargetPlayers()) end)
        end
    end
})

PremiumTab:AddButton({
    Name = "Give Item to Target",
    Description = "Gives the selected item to the chosen target player",
    Callback = function()
        local lp = game:GetService("Players").LocalPlayer
        local target = (selectedVIPTargetName == "Myself") and lp or game:GetService("Players"):FindFirstChild(selectedVIPTargetName)
        if not target then
            vipNotify("VIP Items", "Target player not found!")
            return
        end
        task.spawn(function()
            executeGiveItemSingle(target, selectedVIPItemId, selectedVIPItemName)
        end)
    end
})

PremiumTab:AddButton({
    Name = "Give Item to ALL Players (Server)",
    Description = "Flash teleports behind every player to deliver the item and returns you back",
    Callback = function()
        task.spawn(function()
            executeGiveItemAll(selectedVIPItemId, selectedVIPItemName)
        end)
    end
})

PremiumTab:AddParagraph({
    "VIP Commands & Items Guide",
    "• Chat & CmdBar Commands:\n  :give me [item]  -> Give VIP item to yourself\n  :give [player] [item]  -> Give VIP item to target\n  :give all [item]  -> Give VIP item to all players\n  :giveall [item]  -> Give VIP item to all players\n\n• Official VIP Tools:\n  TearGasLauncher, WardenPistol, Snowboard, Skis, MoneyGun"
})

PremiumTab:AddButton({
    Name = "Cmds",
    Description = "Opens interactive VIP admin command bar & full item reference",
    Callback = function()
        local lp = game:GetService("Players").LocalPlayer
        local pg = lp and lp:FindFirstChild("PlayerGui")
        if not pg then return end

        local old = pg:FindFirstChild("BTR_VIPCmdsGui")
        if old then pcall(function() old:Destroy() end) end

        local sg = Instance.new("ScreenGui")
        sg.Name = "BTR_VIPCmdsGui"
        sg.ResetOnSpawn = false
        sg.DisplayOrder = 10000
        sg.Parent = pg

        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(0, 440, 0, 230)
        frame.Position = UDim2.new(0.5, -220, 0.4, -115)
        frame.BackgroundColor3 = Color3.fromRGB(20, 20, 28)
        frame.BorderSizePixel = 0
        frame.Parent = sg

        local corner = Instance.new("UICorner")
        corner.CornerRadius = UDim.new(0, 10)
        corner.Parent = frame

        local stroke = Instance.new("UIStroke")
        stroke.Color = Color3.fromRGB(80, 120, 255)
        stroke.Thickness = 1.5
        stroke.Parent = frame

        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, -40, 0, 28)
        title.Position = UDim2.new(0, 15, 0, 8)
        title.BackgroundTransparency = 1
        title.Text = "VIP Command Bar & Items"
        title.TextColor3 = Color3.fromRGB(255, 255, 255)
        title.TextSize = 14
        title.Font = Enum.Font.GothamBold
        title.TextXAlignment = Enum.TextXAlignment.Left
        title.Parent = frame

        local closeBtn = Instance.new("TextButton")
        closeBtn.Size = UDim2.new(0, 24, 0, 24)
        closeBtn.Position = UDim2.new(1, -32, 0, 8)
        closeBtn.BackgroundColor3 = Color3.fromRGB(40, 40, 52)
        closeBtn.Text = "X"
        closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        closeBtn.TextSize = 12
        closeBtn.Font = Enum.Font.GothamBold
        closeBtn.Parent = frame

        local closeCorner = Instance.new("UICorner")
        closeCorner.CornerRadius = UDim.new(0, 6)
        closeCorner.Parent = closeBtn

        closeBtn.MouseButton1Click:Connect(function()
            pcall(function() sg:Destroy() end)
        end)

        local helpLbl = Instance.new("TextLabel")
        helpLbl.Size = UDim2.new(1, -30, 0, 18)
        helpLbl.Position = UDim2.new(0, 15, 0, 36)
        helpLbl.BackgroundTransparency = 1
        helpLbl.Text = "Commands: :give me [item] | :give [player] [item] | :giveall [item]"
        helpLbl.TextColor3 = Color3.fromRGB(150, 180, 255)
        helpLbl.TextSize = 11
        helpLbl.Font = Enum.Font.GothamBold
        helpLbl.TextXAlignment = Enum.TextXAlignment.Left
        helpLbl.Parent = frame

        local itemsBox = Instance.new("TextLabel")
        itemsBox.Size = UDim2.new(1, -30, 0, 44)
        itemsBox.Position = UDim2.new(0, 15, 0, 56)
        itemsBox.BackgroundColor3 = Color3.fromRGB(14, 14, 20)
        itemsBox.TextColor3 = Color3.fromRGB(200, 210, 230)
        itemsBox.TextSize = 11
        itemsBox.Font = Enum.Font.Gotham
        itemsBox.TextWrapped = true
        itemsBox.TextXAlignment = Enum.TextXAlignment.Left
        itemsBox.TextYAlignment = Enum.TextYAlignment.Top
        itemsBox.Text = "VIP Items: TearGasLauncher, WardenPistol, Snowboard, Skis, MoneyGun"
        itemsBox.Parent = frame

        local itemsCorner = Instance.new("UICorner")
        itemsCorner.CornerRadius = UDim.new(0, 6)
        itemsCorner.Parent = itemsBox

        local itemsPadding = Instance.new("UIPadding")
        itemsPadding.PaddingTop = UDim.new(0, 8)
        itemsPadding.PaddingLeft = UDim.new(0, 8)
        itemsPadding.PaddingRight = UDim.new(0, 8)
        itemsPadding.PaddingBottom = UDim.new(0, 8)
        itemsPadding.Parent = itemsBox

        local tb = Instance.new("TextBox")
        tb.Size = UDim2.new(1, -30, 0, 34)
        tb.Position = UDim2.new(0, 15, 0, 108)
        tb.BackgroundColor3 = Color3.fromRGB(30, 30, 42)
        tb.PlaceholderText = "Type command here (ex: :give me TearGasLauncher)..."
        tb.PlaceholderColor3 = Color3.fromRGB(120, 120, 140)
        tb.Text = ""
        tb.TextColor3 = Color3.fromRGB(255, 255, 255)
        tb.TextSize = 12
        tb.Font = Enum.Font.Code
        tb.ClearTextOnFocus = false
        tb.Parent = frame

        local tbCorner = Instance.new("UICorner")
        tbCorner.CornerRadius = UDim.new(0, 6)
        tbCorner.Parent = tb

        local execBtn = Instance.new("TextButton")
        execBtn.Size = UDim2.new(1, -30, 0, 34)
        execBtn.Position = UDim2.new(0, 15, 0, 150)
        execBtn.BackgroundColor3 = Color3.fromRGB(50, 100, 220)
        execBtn.Text = "Execute Command"
        execBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        execBtn.TextSize = 13
        execBtn.Font = Enum.Font.GothamBold
        execBtn.Parent = frame

        local execCorner = Instance.new("UICorner")
        execCorner.CornerRadius = UDim.new(0, 6)
        execCorner.Parent = execBtn

        local function doExec()
            local input = tb.Text
            if input and input ~= "" then
                runVIPAdminCommand(input)
                pcall(function() sg:Destroy() end)
            end
        end

        execBtn.MouseButton1Click:Connect(doExec)
        tb.FocusLost:Connect(function(enterPressed)
            if enterPressed then
                doExec()
            end
        end)
    end
})

PremiumTab:AddButton({
    Name = "Clear Backpack Tools",
    Description = "Removes all tools from your inventory",
    Callback = function()
        pcall(function()
            local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
            local clearRemote = RE and RE:FindFirstChild("1Clea1rTool1s")
            if clearRemote then clearRemote:FireServer("ClearAllTools") end
        end)
        vipNotify("VIP Items", "Inventory tools cleared!")
    end
})

-- [LOCKED PATTERN: skate + PickingScooterMusicText + Delete NoMotorVehicle]
local RE_music = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
local musicVehicleRemote = RE_music and RE_music:FindFirstChild("1NoMoto1rVehicle1s")
local musicClearRemote = RE_music and RE_music:FindFirstChild("1Clea1rTool1s")

local MUSIC_PRESET_LIST = {
    { Name = "Soft Skies & You", Id = 140736931153864 },
    { Name = "Blue Young Kai", Id = 84729628311938 },
    { Name = "Summertime Sadness", Id = 107887768910692 },
    { Name = "Love Story", Id = 135528953872601 },
    { Name = "Mommyy", Id = 112429422439068 },
    { Name = "I Got You Baby", Id = 115492336583856 },
    { Name = "Alone", Id = 105512964264815 },
    { Name = "Another World", Id = 111351357978027 },
    { Name = "Isekai", Id = 131874270028720 },
    { Name = "StarsMoon", Id = 112355709978731 },
    { Name = "Meow Meow", Id = 99034537604129 },
    { Name = "Under the influence", Id = 103222441664136 },
    { Name = "Sadness", Id = 128159342762162 },
    { Name = "Sweater Weather", Id = 75254239320157 },
    { Name = "Doraemon", Id = 121640028060880 },
    { Name = "Baby Shark", Id = 126462032038855 },
    { Name = "So Clean", Id = 93958751571254 },
    { Name = "NoCopyrightSound", Id = 72031945149756 },
    { Name = "LegendNeverDies", Id = 112738519477450 },
    { Name = "Thought", Id = 87373439541502 },
    { Name = "Confess You Love", Id = 87559737835956 },
    { Name = "SetMcMn", Id = 78631447496051 },
    { Name = "The Weeknd - Blinding Lights", Id = 1842805773 },
    { Name = "Dua Lipa - Levitating", Id = 6606223788 },
    { Name = "Harry Styles - As It Was", Id = 8915577374 },
    { Name = "Taylor Swift - Anti-Hero", Id = 11098454532 },
    { Name = "Miley Cyrus - Flowers", Id = 12211382345 },
    { Name = "Doja Cat - Paint The Town Red", Id = 14372520607 },
    { Name = "Olivia Rodrigo - Vampire", Id = 14019995782 },
    { Name = "SZA - Kill Bill", Id = 12046416989 },
    { Name = "Beyonc - CUFF IT", Id = 10131382078 },
    { Name = "Sabrina Carpenter - Espresso", Id = 16789123456 },
    { Name = "HE RANGLO", Id = 128232876294868 },
    { Name = "Akhiyan", Id = 104380086210152 },
    { Name = "Aalu Kachlu Beta", Id = 138396893861830 },
    { Name = "Bada Pachtaoge", Id = 79561785821029 },
    { Name = "Saiyara", Id = 110710316953203 },
    { Name = "Unke Andaz Karam", Id = 95589943778476 },
    { Name = "Hai Dill Yein Mera", Id = 113792808283128 },
    { Name = "Dhun Female", Id = 129981117529204 },
    { Name = "Manja", Id = 83325283987770 },
    { Name = "Aaj Mangalwar Hai", Id = 98606901625415 },
    { Name = "Majisa", Id = 91368973732735 },
    { Name = "Pal Pal", Id = 77002121761232 },
    { Name = "Nacho Saare", Id = 108968616078928 },
    { Name = "Tumse Mil Kar", Id = 133053501453467 },
    { Name = "Pyar Ka Ehsas", Id = 105568083374120 },
    { Name = "Ladki Badi Anjani Hai", Id = 128892246666972 },
    { Name = "Dil Ne Ye Kaha", Id = 113554906905279 },
    { Name = "Gulabi Gulabi", Id = 102924925389960 },
    { Name = "Addat", Id = 94103203628909 },
    { Name = "Naddiyaaa", Id = 128688756073670 },
    { Name = "Kabhi Kabhi", Id = 101068303675725 },
    { Name = "Jhol", Id = 121180010622504 },
    { Name = "Maula Mere", Id = 105017950345985 },
    { Name = "Mann Mera", Id = 140298499491642 },
    { Name = "Deewana", Id = 88345320570909 },
    { Name = "Naini Se Baan", Id = 108734798852042 },
    { Name = "Saawre", Id = 111219978139804 },
    { Name = "Kitni Hasrat", Id = 93457193569619 },
    { Name = "Grish Ka Ganna", Id = 104916889617105 },
    { Name = "Sason Ki Mala", Id = 120349214701956 },
    { Name = "Holi Ke din", Id = 115634143210295 },
    { Name = "Aaj Ki Raat", Id = 73958484988503 },
    { Name = "Hathi Raja", Id = 106266156568507 },
    { Name = "New Krish Ka Gana", Id = 112509603877994 },
    { Name = "Ranjheya Ve", Id = 119786491353134 },
    { Name = "Mujhe Nafrat Hain", Id = 88750836439164 },
    { Name = "Pardeshi", Id = 90560862399777 },
    { Name = "Kabhi Kab Rap", Id = 137355570793959 },
    { Name = "O Yaara", Id = 117377307469141 },
    { Name = "Sitam", Id = 81582927737472 },
    { Name = "Muskaan", Id = 74180449839527 },
    { Name = "Ishq Aashiqana", Id = 84689783601883 },
    { Name = "NaNa Karte Karte", Id = 87629461934311 },
    { Name = "Parde Mein", Id = 81161805007229 },
    { Name = "Dil Hain Tera", Id = 126020491218161 },
    { Name = "Zaroori Tha", Id = 90050076108794 },
    { Name = "Mera Pyaar", Id = 86442760739586 },
    { Name = "Diwaanaa", Id = 74803752690893 },
    { Name = "Tum Jo Aayein", Id = 123883140253393 },
    { Name = "Tera Mera Rista", Id = 133309449131920 },
    { Name = "Tumhee Aana", Id = 121581884194482 },
    { Name = "Rab Kare Tujhko", Id = 85476270857623 },
    { Name = "Tujh Se Naraj", Id = 132205900730979 },
    { Name = "Bada Achha Lagta", Id = 109840879921151 },
    { Name = "Kesariya", Id = 10059668834 },
    { Name = "Apna Bana Le", Id = 11005590115 },
    { Name = "Raataan Lambiyan", Id = 7384523412 },
    { Name = "Ranjha", Id = 9102384756 },
    { Name = "Shayad", Id = 6521458932 },
    { Name = "Tujhe Kitna Chahne Lage", Id = 5432678901 },
    { Name = "Drake - God's Plan", Id = 1665926924 },
    { Name = "Kanye West - Stronger", Id = 136209425 },
    { Name = "Coolio - Gangsta's Paradise", Id = 6070263388 },
    { Name = "Lil Nas X - Industry Baby", Id = 7253841629 },
    { Name = "Wiz Khalifa - Black and Yellow", Id = 139235100 },
    { Name = "A Roblox Rap", Id = 1259050178 },
    { Name = "Polo G - RAPSTAR", Id = 6678031214 },
    { Name = "Childish Gambino - This Is America", Id = 2062482384 },
    { Name = "Snoop Dogg - Drop It Like It's Hot", Id = 292861322 },
    { Name = "Eminem - Without Me", Id = 6689996382 },
    { Name = "Eminem - Lose Yourself", Id = 142297926 },
    { Name = "Eminem - Rap God", Id = 163169183 },
    { Name = "Eminem - Not Afraid", Id = 289230995 },
    { Name = "Eminem - The Real Slim Shady", Id = 142376088 },
    { Name = "Eminem - Love The Way You Lie", Id = 152829789 },
    { Name = "Drake - In My Feelings", Id = 2153662828 },
    { Name = "Drake - Hotline Bling", Id = 289230995 },
    { Name = "Drake - One Dance", Id = 1102847415 },
    { Name = "Drake - Nonstop", Id = 6568276171 },
    { Name = "Drake - Toosie Slide", Id = 7178201139 },
    { Name = "Kendrick Lamar - HUMBLE.", Id = 6568276171 },
    { Name = "Kendrick Lamar - DNA.", Id = 7673592454 },
    { Name = "Kendrick Lamar - Alright", Id = 289230123 },
    { Name = "Kendrick Lamar - Swimming Pools", Id = 142376456 },
    { Name = "Kendrick Lamar - King Kunta", Id = 289231234 },
    { Name = "Travis Scott - SICKO MODE", Id = 2153662828 },
    { Name = "Travis Scott - Goosebumps", Id = 1102847415 },
    { Name = "Travis Scott - HIGHEST IN THE ROOM", Id = 4053876340 },
    { Name = "Travis Scott - Antidote", Id = 289231567 },
    { Name = "Travis Scott - STARGAZING", Id = 6568277890 },
    { Name = "Post Malone - Circles", Id = 3951656337 },
    { Name = "Post Malone - Rockstar", Id = 1102847415 },
    { Name = "Post Malone - Congratulations", Id = 2153662828 },
    { Name = "Post Malone - Sunflower", Id = 289231890 },
    { Name = "Post Malone - Better Now", Id = 6568278901 },
    { Name = "Kanye West - Heartless", Id = 152829345 },
    { Name = "Kanye West - Gold Digger", Id = 142376088 },
    { Name = "Kanye West - Power", Id = 289232123 },
    { Name = "Kanye West - All of the Lights", Id = 6568279012 },
    { Name = "Kanye West - Black Skinhead", Id = 7178204567 },
    { Name = "Lil Nas X - Old Town Road", Id = 2892309951 },
    { Name = "Lil Nas X - Montero", Id = 6568276171 },
    { Name = "Lil Nas X - Panini", Id = 289232456 },
    { Name = "Nicki Minaj - Super Bass", Id = 473218026 },
    { Name = "Nicki Minaj - Starships", Id = 289230456 },
    { Name = "Nicki Minaj - Anaconda", Id = 152829789 },
    { Name = "Nicki Minaj - Bang Bang", Id = 289232789 },
    { Name = "50 Cent - In Da Club", Id = 142305215 },
    { Name = "50 Cent - Candy Shop", Id = 152830123 },
    { Name = "50 Cent - P.I.M.P.", Id = 142376456 },
    { Name = "50 Cent - Many Men", Id = 289233012 },
    { Name = "Jack Harlow - First Class", Id = 9043629582 },
    { Name = "Jack Harlow - WHATS POPPIN", Id = 6568276234 },
    { Name = "Cardi B - WAP", Id = 6568276789 },
    { Name = "Cardi B - Bodak Yellow", Id = 289231234 },
    { Name = "Cardi B - I Like It", Id = 152830567 },
    { Name = "Megan Thee Stallion - Savage", Id = 6568277890 },
    { Name = "Megan Thee Stallion - Body", Id = 7178202345 },
    { Name = "DaBaby - ROCKSTAR", Id = 6568278901 },
    { Name = "DaBaby - BOP", Id = 289231890 },
    { Name = "Pop Smoke - Dior", Id = 6568279012 },
    { Name = "Pop Smoke - For The Night", Id = 289232123 },
    { Name = "Juice WRLD - Lucid Dreams", Id = 289232456 },
    { Name = "Juice WRLD - Robbery", Id = 7178205678 },
    { Name = "XXXTentacion - SAD!", Id = 289232789 },
    { Name = "XXXTentacion - Moonlight", Id = 6568281234 },
    { Name = "Lil Baby - Drip Too Hard", Id = 289233012 },
    { Name = "Lil Baby - Woah", Id = 6568282345 },
    { Name = "Roddy Ricch - The Box", Id = 289233345 },
    { Name = "Roddy Ricch - High Fashion", Id = 6568283456 },
    { Name = "NF - The Search", Id = 289234567 },
    { Name = "NF - Let You Down", Id = 6568291234 },
    { Name = "Logic - 1-800-273-8255", Id = 7178215678 },
    { Name = "J. Cole - MIDDLE CHILD", Id = 289234890 },
    { Name = "21 Savage - a lot", Id = 7178216789 },
    { Name = "Future - Mask Off", Id = 289235012 },
    { Name = "Migos - Bad and Boujee", Id = 6568293456 },
    { Name = "Lil Uzi Vert - XO Tour Llif3", Id = 7178217890 },
    { Name = "Lamba Lamba Ghughat", Id = 78431826650714 },
    { Name = "Balam Ke Pichkari", Id = 89700384406008 },
    { Name = "Moh Lelo", Id = 95909411418420 },
    { Name = "Sunny Dancer", Id = 133421259018974 },
    { Name = "TakTaki Bhojpuri", Id = 78735782383680 },
    { Name = "Ladki Deewani", Id = 102511873453786 },
    { Name = "Bansuri", Id = 79568658897083 },
    { Name = "Sejiya Pe Piya", Id = 87577798625777 },
    { Name = "Nimbu Kharbuja", Id = 87577798625777 },
    { Name = "Sadi Ladki", Id = 104574653065736 },
    { Name = "Dar Lage", Id = 128470232274219 },
    { Name = "Hamra Mard", Id = 131679833636653 },
    { Name = "Video Calling", Id = 126309103230345 },
    { Name = "Sadiya Karoya", Id = 140415804746906 },
    { Name = "Ghaghri", Id = 124233673176294 },
    { Name = "TakaTaki", Id = 117615820552185 },
    { Name = "Kunj Bihari", Id = 108588298945210 },
    { Name = "Lollypop Lagelu", Id = 1834567890 },
    { Name = "Pawan Singh - Chhalakata", Id = 2945678901 },
    { Name = "Khesari Lal - Dulha", Id = 3056789012 },
    { Name = "Ritesh Pandey - Hello Koun", Id = 4167890123 },
    { Name = "Shilpi Raj - Kamar Dab", Id = 5278901234 },
    { Name = "Aamrapali Dubey - Patna Se", Id = 6389012345 },
    { Name = "Antra Singh - Sarkar", Id = 7490123456 },
    { Name = "Neelkamal - Jila Top", Id = 8501234567 },
    { Name = "Chinta Devi - Saiyan", Id = 9612345678 },
    { Name = "Gunjan Singh - Dewar", Id = 10723456789 },
    { Name = "UwU", Id = 105577043687038 },
    { Name = "Senpai", Id = 115498703521334 },
    { Name = "Kiss", Id = 118235447189969 },
    { Name = "Anime Ahh", Id = 119651952208423 },
    { Name = "Ah", Id = 121259252258118 },
    { Name = "18+ Girl", Id = 131014261385625 },
    { Name = "Girl Evil Laugh", Id = 134548905274433 },
    { Name = "Tom", Id = 139694892021582 },
    { Name = "Slayy", Id = 139740597178232 },
    { Name = "Scary", Id = 140028279221307 },
    { Name = "Kitisan", Id = 122462786517802 },
    { Name = "Flash", Id = 123459967526974 },
    { Name = "The Hand erase", Id = 132573001115155 },
    { Name = "Aishiteru", Id = 132952117418468 },
    { Name = "Msg Y'all", Id = 134489447055645 },
    { Name = "Goofy Horn", Id = 136188985418657 },
    { Name = "Faaahhhhh", Id = 139999225792687 },
    { Name = "Shidouuu", Id = 89016013924693 },
    { Name = "Jumpscare", Id = 6201427049 },
    { Name = "Desi Gun", Id = 3177712713 },
    { Name = "Laughing", Id = 4767799547 },
    { Name = "CID", Id = 105096889615032 },
    { Name = "Muthhal Baj", Id = 87111140463459 },
    { Name = "Nya Zo", Id = 8842446965 },
    { Name = "Chicken Sound", Id = 117909139728666 },
    { Name = "Ladle Ghop", Id = 88622194255425 },
    { Name = "MadarChod", Id = 126782260874451 },
    { Name = "Yamate Kudasajj", Id = 18990591883 },
    { Name = "Monkey", Id = 7196237097 },
    { Name = "Ahhhhh", Id = 169664410 },
    { Name = "PunjabiMeme", Id = 123161971384069 },
    { Name = "Sound 1", Id = 77446238713316 },
    { Name = "Sound 2", Id = 4575953237 },
    { Name = "Popopo", Id = 108896377576102 },
    { Name = "SariRoti", Id = 9075261161 },
    { Name = "Vine Boom", Id = 2614753599 },
    { Name = "Bruh Sound Effect", Id = 4567890123 },
    { Name = "Fart Sound", Id = 5678901234 },
    { Name = "Screaming", Id = 6789012345 },
    { Name = "Anime Wow", Id = 7890123456 },
    { Name = "Sad Violin", Id = 8901234567 },
    { Name = "Taco Bell", Id = 9012345678 },
    { Name = "Roblox Death Sound", Id = 1223456789 },
    { Name = "Mario Jump", Id = 2334567890 },
    { Name = "Among Us Emergency", Id = 3445678901 },
}

local MUSIC_SELECTED = ""
local MUSIC_CUSTOM_ID = ""
local MUSIC_NAMES = {}
local MUSIC_ID_MAP = {}
for _, mus in ipairs(MUSIC_PRESET_LIST) do
    table.insert(MUSIC_NAMES, mus.Name)
    MUSIC_ID_MAP[mus.Name] = tostring(mus.Id)
end
if MUSIC_NAMES[1] then MUSIC_SELECTED = MUSIC_ID_MAP[MUSIC_NAMES[1]] or "" end

local function musicForcePlay(id)
    if not musicVehicleRemote or id == "" then return end
    pcall(function() musicVehicleRemote:FireServer("PickingScooterMusicText", id, nil, true) end)
    pcall(function() musicVehicleRemote:FireServer("PickingScooterMusicText", id) end)
    pcall(function() musicVehicleRemote:FireServer("PickingScooterMusicText", tonumber(id), nil, true) end)
end

local function musicStop()
    if musicVehicleRemote then pcall(function() musicVehicleRemote:FireServer("Delete NoMotorVehicle") end) end
    task.spawn(function()
        task.wait(0.1)
        if musicClearRemote then pcall(function() musicClearRemote:FireServer("ClearAllTools") end) end
    end)
end

local function musicPlay(id)
    if not musicVehicleRemote or id == "" then return end
    task.spawn(function()
        pcall(function() musicVehicleRemote:FireServer("Delete NoMotorVehicle") end)
        task.wait(0.1)
        if musicClearRemote then pcall(function() musicClearRemote:FireServer("ClearAllTools") end) end
        task.wait(0.15)
        pcall(function() musicVehicleRemote:FireServer("SkateBoard") end)
        task.wait(1.0)
        musicForcePlay(id)
    end)
end

AudioTab:AddSection({
    "> Music (FE)"
})

AudioTab:AddDropdown({
    Name = "Song List (242 Audios)",
    Description = "Choose a song from the list",
    Options = MUSIC_NAMES,
    Default = MUSIC_NAMES[1],
    Callback = function(val)
        if MUSIC_ID_MAP[val] then MUSIC_SELECTED = MUSIC_ID_MAP[val] end
    end
})

AudioTab:AddButton({
    Name = "> Play Song",
    Description = "Spawns the skate and plays the selected song",
    Callback = function()
        if MUSIC_SELECTED ~= "" then
            musicPlay(MUSIC_SELECTED)
        else
            game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Music FE", Text = "Select a song first!", Duration = 2 })
        end
    end
})

AudioTab:AddTextBox({
    Name = "Custom Music ID",
    Description = "Paste any audio ID",
    PlaceholderText = "Custom ID...",
    Callback = function(val)
        if val ~= "" then MUSIC_CUSTOM_ID = tostring(val) end
    end
})

AudioTab:AddButton({
    Name = "> Play Custom Music",
    Description = "Spawns the skate and plays your custom ID",
    Callback = function()
        if MUSIC_CUSTOM_ID ~= "" then
            musicPlay(MUSIC_CUSTOM_ID)
        else
            game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Music FE", Text = "Enter an ID first!", Duration = 2 })
        end
    end
})

AudioTab:AddButton({
    Name = "X Stop Music + Remove Skate",
    Description = "Stops the music and removes the skate",
    Callback = function()
        musicStop()
        game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Music FE", Text = "Music stopped, skate removed", Duration = 2 })
    end
})

PremiumTab:AddSection({
    "> Skate (FE)"
})

local skateRGBOn = false
local function stopSkateRGB() skateRGBOn = false end
local function startSkateRGB()
    if skateRGBOn then return end
    skateRGBOn = true
    task.spawn(function()
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        local CarRemote = RE and RE:FindFirstChild("1Player1sCa1r")
        while skateRGBOn do
            local c = Color3.fromHSV(tick() % 5 / 5, 1, 1)
            if CarRemote then
                pcall(function() CarRemote:FireServer("NoMotorColor", c) end)
            end
            pcall(function()
                game.Players.LocalPlayer.PlayerGui.MainGUIHandler
                    .NoMotorVehicleControl.NoMotorColorPicks.SetColor:FireServer(c)
            end)
            task.wait(0.1)
        end
    end)
end

PremiumTab:AddToggle({
    Name = "RGB Skate",
    Description = "Rainbow colors on the skateboard",
    Default = false,
    Callback = function(state)
        if state then startSkateRGB() else stopSkateRGB() end
    end
})

PremiumTab:AddButton({
    Name = "Remove Skate",
    Description = "Removes the vehicle and stops the RGB",
    Callback = function()
        task.spawn(function()
            stopSkateRGB()
            pcall(function()
                local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                local VR = RE and RE:FindFirstChild("1NoMoto1rVehicle1s")
                local CR = RE and RE:FindFirstChild("1Clea1rTool1s")
                if VR then VR:FireServer("Delete NoMotorVehicle") end
                task.wait(0.1)
                if CR then CR:FireServer("ClearAllTools") end
            end)
        end)
    end
})

AudioTab:AddSection({
    "> Piano FE (Gun Sound)"
})

local PianoNotes = {
    ["C2"]="111842008687926",["C#2"]="70981692272966",["D2"]="139380394259973",
    ["D#2"]="93444915027584",["E2"]="87629114895960",["F2"]="73972689087289",
    ["F#2"]="70376334727828",["G2"]="100837629997022",["G#2"]="136188639889438",
    ["A2"]="71752018166979",["A#2"]="120262398697016",["B2"]="110455028643359",
    ["C3"]="123112876875374",["C#3"]="120923365636009",["D3"]="106306658545729",
    ["D#3"]="124841365961522",["E3"]="82335077730546",["F3"]="139732365240441",
    ["F#3"]="83552558259422",["G3"]="137224813797993",["G#3"]="131726792016780",
    ["A3"]="95091023271668",["A#3"]="112380265050214",["B3"]="105256060658131",
    ["C4"]="78413131279275",["C#4"]="72998150378307",["D4"]="105581334194673",
    ["D#4"]="86516725681894",["E4"]="132881598286319",["F4"]="133575380950758",
    ["F#4"]="126196495975202",["G4"]="114434657963330",["G#4"]="133080223235416",
    ["A4"]="136348068040461",["A#4"]="128661535499577",["B4"]="128462707908914",
    ["C5"]="121997668450827",["C#5"]="121413839180669",["D5"]="90740749235134",
    ["D#5"]="113030261877109",["E5"]="140406182404884",["F5"]="119631153742000",
    ["F#5"]="70828942309686",["G5"]="131269431485825",["G#5"]="118262323318899",
    ["A5"]="125853393278154",["A#5"]="99745152176175",["B5"]="138660648674060",
    ["C6"]="110812706317973",["C#6"]="104201273760781",["D6"]="129569432159919",
    ["D#6"]="77417182913309",["E6"]="91804565247146",["F6"]="74247761440555",
    ["F#6"]="106884926622420",["G6"]="107760096922427",["G#6"]="90865662835904",
    ["A6"]="134133616625207",["A#6"]="79684302895378",["B6"]="93034533215278",
    ["C7"]="125973710014979",
}
local NoteMapping = {
    "1","!","2","@","3","4","$","5","%","6","^","7","8","*","9","(","0",
    "q","Q","w","W","e","E","r","t","T","y","Y","u","i","I","o","O","p",
    "P","a","s","S","d","D","f","g","G","h","H","j","J","k","l","L","z",
    "Z","x","c","C","v","V","b","B","n","m"
}
local KeysToNotes = {}
do
    local notes = {}
    for note in pairs(PianoNotes) do table.insert(notes, note) end
    table.sort(notes, function(a, b)
        local pitch = {["C"]=0,["C#"]=1,["D"]=2,["D#"]=3,["E"]=4,["F"]=5,["F#"]=6,["G"]=7,["G#"]=8,["A"]=9,["A#"]=10,["B"]=11}
        local function getVal(n)
            local n2, oct = n:match("([A-G]#?)(%d+)")
            return tonumber(oct)*12 + pitch[n2]
        end
        return getVal(a) < getVal(b)
    end)
    for i, n in ipairs(NoteMapping) do KeysToNotes[n] = notes[i] end
end

local PianoSongs = {
    {
        name = "Demo Original (Anime Piano)",
        tempo = 240,
        sheet = "[id] [pf] [sh] [pj] [sh] [pf] [id] [pf] [sh] [pj] [sh] [pf] [id] [pf] [sh] [pj] [sh] [pf] [us] [of] [ah] [oj] [ah] [of] [us] [of] [ah] [oj] [ah] [of] [Ys] [of] [ah] [oj] [ah] [of] [ys] [id] [pg] [ih] [pg] [id] [ys] [id] [pg] [ih] [pg] [id] [ys] [id] [pg] [ih] [pg] [id] [ys] [id] [pg] [ih] [pg] [id] [ta] [us] [od] [uf] [od] [us] [ta] [us] [od] [uf] [od] [us] [ta] [us] [od] [uf] [od] [us] [ta] [us] [od] [uf] [od] [us] [if] [pf] [sf] [pf] [sf] [pd] [ix] p s p s p [if] [pf] [sf] [pf] [sf] p [if] [pf] [sf] [pf] [sf] p [uf] [of] [af] [of] [af] [od] [ux] o a o a o [uf] [of] [af] [of] [af] o [Yf] [of] [af] [of] [af] o [yd] [id] [pf] [id] [pf] [id] [yx] i p i p i [yd] [id] [pf] [id] [pf] [id] [yx] i p i p i [tf] [ud] [of] [ud] [of] u [tf] [ud] [of] [ud] [of] u [tf] [ud] [of] u [od] [us] t [uf] [oh] [4psfj] [qet] [pj] [ak] [sl] 4 [qet] [pj] [sl] [3ak] [pj] [oh] [0wroh] [uf] [3ak] [pj] [oh] [0wrak] [pj] [oh] [2oh] [ipsg] [9qe] 2 [ts] [9qeoh] [ig] [uf] [3uf] 3 3 [3ep] 3 3 [3ts] 3 3 [yd]"
    },
    {
        name = "Beauty and a Beat - Justin Bieber",
        tempo = 190,
        sheet = "4e 8 4e 8 p P [0o] p [eti] 8t 4e 8 4e 8 p P [0o] p [eti] [es] p P [8o] p [8i] [4t] p P [8o] p [8i] [es] 4 8 w e t u i E p P o [8i] [4t] e t u i [8o] u i [4e] [9i] o p P [6s] [es] P p o [8i] P p o i [8t] p P o p [eti] e t u i E tt u i o P p i t e t u i o p o p s i i u i p t p P o p [eti] p P [8o] p [8i] [4t] [9i] o p P [6s] [9i] o p d s [9i] o p P [6s] [es] P p o [8i] i o p i o i o e t u i E e t u i o [9i] o p P [6s] [es] P p o [8i] P p o i [8t] e t u i E e t u i o p o p [es] [9i] i u i p [8t] p P o p [eti] p o p [es] [9i] i u i p [8t] p P o p [4eti]"
    },
    {
        name = "Believer - Imagine Dragons",
        tempo = 220,
        sheet = "[ep] t [ef] t [ed] t [ed] t [ed] [ts] [ed] t [ed] [tf] [ed] [ts] [ep] [to] [qp] t [qf] t [qd] t [qd] [ts] [qd] t [qd] [tf] [qd] [ts] [qp] [qo] [ep] t [ef] t [ed] t [ed] [ts] [ed] t [ed] [tf] [ed] [ts] [ep] [to] [qp] t [qf] t [qd] t [qd] [ts] [qd] t [qd] [tf] [qd] [ts] [qp] [0wr] [ep] t [ef] t [ed] t [ed] [ts] [ed] t [ef] t [ed] t [ef] [ts] [ed] [ts] [ep] t [ef] t [ed] t [ed] [ts] [ed] [ts] [ep] [to] [ep] t [ef] t [ed] t [ed] [ts] [ed] [ts] [ep] [to]"
    },
    {
        name = "Coffin Dance (Astronomia)",
        tempo = 200,
        sheet = "[to] [yo] [to] [yo] [to] [yo] [to] [eyo] [to] [yo] [to] [yo] [uo] [yo] [uo] [eyo] [to] [yo] [to] [yo] [to] [yo] [to] [eyo] [to] [yo] [to] [yo] [ro] [yo] [ro] [eyo] t y t y [et] y [et] [ey] t y t y u y u [ey] [to] [yo] [to] [yo] [to] [yo] [to] [eyo] [to] [yo] [to] [yo] [uo] [yo] [uo] [eyo]"
    },
    {
        name = "Bad Guy - Billie Eilish",
        tempo = 230,
        sheet = "o o o o o o o u u u u u u u t t t t t t t u u u u u u o o o o o o o u u u u u u u t t t t t t t [yuo] [yuo] [yuo] [yuo] [yuo] [yuo] [yuo] [yuo] [tuo] [tuo] [tuo] [tuo] [tuo] [tuo] [tuo] [tuo]"
    }
}
local songNames = {}
local songByName = {}
for _, s in ipairs(PianoSongs) do
    table.insert(songNames, s.name)
    songByName[s.name] = s
end

local pianoPlaying = false
local pianoPlayID = 0
local selectedSong = PianoSongs[1]
local pianoTempo = PianoSongs[1].tempo

local toggleshots = false
local function playFENote(noteId)
    pcall(function()
        local player = game:GetService("Players").LocalPlayer
        local char = player.Character
        if not char then return end
        local tool = char:FindFirstChild("Assault")
        if not tool then return end
        local handle = tool:FindFirstChild("Handle")
        if not handle then return end
        local rs = game:GetService("ReplicatedStorage")
        local ws = workspace
        toggleshots = not toggleshots
        if toggleshots then
            local a = {
                ws:WaitForChild("Model", 9e9):GetChildren()[154]:GetChildren()[45],
                handle,
                Vector3.new(0.2, 0.3, -2.5),
                Vector3.new(16.8228, 0.025, -43.4795),
                tool:WaitForChild("GunScript_Local", 9e9):WaitForChild("MuzzleEffect", 9e9),
                tool:WaitForChild("GunScript_Local", 9e9):WaitForChild("HitEffect", 9e9),
                noteId, noteId,
                { false },
                { 25, Vector3.new(0.25, 0.25, 100), BrickColor.new("Bright yellow"), 0.25, Enum.Material.SmoothPlastic, 0.25 },
                true, false
            }
            rs:WaitForChild("RE", 9e9):WaitForChild("1Gu1n", 9e9):FireServer(unpack(a))
        else
            local args = {
                handle,
                noteId,
                1
            }
            rs:WaitForChild("RE", 9e9):WaitForChild("1Gu1nSound1s", 9e9):FireServer(unpack(args))
        end
    end)
end

local function playNote(note)
    local id = PianoNotes[note]
    if not id then return end
    local snd = Instance.new("Sound", workspace)
    snd.SoundId = "rbxassetid://" .. id
    snd.Volume = 1
    snd:Play()
    game:GetService("Debris"):AddItem(snd, 3)
    playFENote(id)
end

local function playSheet(sheet, tempo)
    local beat = 60 / ((tempo or 200) * 4)
    local i = 1
    local myID = pianoPlayID
    while i <= #sheet and pianoPlaying and pianoPlayID == myID do
        local c = sheet:sub(i, i)
        if c == "[" then
            local close = sheet:find("]", i, true)
            if close then
                local group = sheet:sub(i+1, close-1)
                for ch in group:gmatch(".") do
                    if pianoPlayID ~= myID then break end
                    local note = KeysToNotes[ch]
                    if note then playNote(note) end
                    task.wait(0.05)
                end
                task.wait(beat)
                i = close
            end
        elseif c == " " or c == "\n" or c == "\r" then
            task.wait(beat)
        elseif c == "-" then
            task.wait(beat * 0.5)
        else
            local note = KeysToNotes[c]
            if note then playNote(note) end
            task.wait(beat)
        end
        i = i + 1
    end
    pianoPlaying = false
end

local function equipAssault()
    pcall(function()
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        local toolRemote = RE and RE:FindFirstChild("1Too1l")
        if not toolRemote then return end
        toolRemote:InvokeServer("PickingTools", "Assault")
        local player = game:GetService("Players").LocalPlayer
        local backpack = player:WaitForChild("Backpack", 5)
        local assault
        for _ = 1, 20 do
            assault = backpack:FindFirstChild("Assault")
            if assault then break end
            task.wait(0.15)
        end
        if assault and player.Character then
            assault.Parent = player.Character
            task.wait(0.3)
        end
    end)
end

AudioTab:AddDropdown({
    Name = "Piano - Choose a song",
    Description = "Tempo updates automatically",
    Options = songNames,
    Default = songNames[1],
    Callback = function(val)
        local s = songByName[val]
        if s then
            selectedSong = s
            pianoTempo = s.tempo
        end
    end
})

AudioTab:AddButton({
    Name = "Play Piano (FE)",
    Description = "Auto-equips the Assault gun and plays the song",
    Callback = function()
        task.spawn(function()
            if pianoPlaying then return end
            if not selectedSong then return end
            pianoTempo = selectedSong.tempo
            equipAssault()
            pianoPlaying = true
            pianoPlayID = tick()
            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Piano FE", Text=selectedSong.name .. " (tempo " .. pianoTempo .. ")", Duration=4})
            playSheet(selectedSong.sheet, pianoTempo)
        end)
    end
})

AudioTab:AddButton({
    Name = "Stop Piano + Remove Gun",
    Description = "Stops the playback and removes the Assault gun",
    Callback = function()
        task.spawn(function()
            pianoPlaying = false
            pianoPlayID = tick()
            pcall(function()
                local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                local CR = RE and RE:FindFirstChild("1Clea1rTool1s")
                if CR then CR:FireServer("ClearAllTools") end
            end)
            game:GetService("StarterGui"):SetCore("SendNotification", {Title="Piano FE", Text="Stopped + gun removed!", Duration=3})
        end)
    end
})

AudioTab:AddSection({
    "> Global Meme Sounds (FE)"
})

local globalMemeSounds = {
    { name = "Trollface laugh", id = "73753120048787" },
    { name = "Troll face sussy", id = "9098738774" },
    { name = "Troll cut", id = "8389041427" },
    { name = "Troll transition", id = "7705506391" },
    { name = "Troll laugh", id = "7816195044" },
    { name = "Magic2", id = "9066733515" },
    { name = "Homero brasileiro", id = "115224076671067" },
    { name = "LOUD Youve been trolled", id = "6787686247" },
    { name = "Fart Meme Sound", id = "6454805792" },
    { name = "Metal Rattle 2 SFX", id = "9116788555" },
    { name = "Hentai wiaaaaan", id = "88332347208779" },
    { name = "Yamete kudasai", id = "108494476595033" },
    { name = "Onii-chan onnn...", id = "134640594695384" },
    { name = "Toma jack", id = "132603645477541" },
    { name = "Toma jack V2", id = "100446887985203" },
    { name = "Toma jack no sol quente", id = "97476487963273" },
    { name = "iFood notification", id = "133843750864059" },
    { name = "Ela quer ram", id = "94395705857835" },
    { name = "Lula vai todo mundo", id = "136804576009416" },
    { name = "Coringa laugh", id = "84663543883498" },
    { name = "Shopee song", id = "8747441609" },
    { name = "Que nojo", id = "103440368630269" },
    { name = "Sai dai lava prato", id = "101232400175829" },
    { name = "Se e loko", id = "78442476709262" },
    { name = "Mita sequer", id = "94889439372168" },
    { name = "Hoje vou ser tua mulher", id = "90844637105538" },
    { name = "Deita aqui sirens", id = "100291188941582" },
    { name = "Miau cat", id = "131804436682424" },
    { name = "Skibidi toilet", id = "128771670035179" },
    { name = "Biruleibi", id = "121569761604968" },
    { name = "Sai daqui", id = "121169949217007" },
    { name = "Risada boa demais", id = "127589011971759" },
    { name = "Vacilo perna de pau", id = "106809680656199" },
    { name = "Gomu Gomu No!", id = "137067472449625" },
    { name = "Arroto burp", id = "140203378050178" },
    { name = "Iraaaa scream", id = "136752451575091" },
    { name = "Nao fica se achando", id = "101588606280167" },
    { name = "WhatsApp notification", id = "107004225739474" },
    { name = "Samsung notification", id = "123767635061073" },
    { name = "Shiiii quiet", id = "120566727202986" },
    { name = "Ai toma Hatsune Miku", id = "139770074770361" },
    { name = "Kuru Kuru Herta", id = "122465710753374" },
    { name = "PM ROCAM siren", id = "96161547081609" },
    { name = "Cavalo sound effect", id = "78871573440184" },
    { name = "Deixa os garoto brinca", id = "80291355054807" },
    { name = "Flamengo anthem", id = "137774355552052" },
    { name = "Sai do meio", id = "127944706557246" },
    { name = "Namoral agora e a hora", id = "120677947987369" },
    { name = "Cerebro burro", id = "82284055473737" },
    { name = "Vou te pegar", id = "120214772725166" },
    { name = "Delay bass", id = "102906880476838" },
    { name = "Tu e um beta", id = "130233956349541" },
    { name = "Por favor nao me tira", id = "85321374020324" },
    { name = "Ola beleza", id = "74235334504693" },
    { name = "Discord sus call", id = "122662798976905" },
    { name = "Rojao apito firework", id = "6549021381" },
    { name = "Off shut down", id = "1778829098" },
    { name = "Kazuma Kazuma", id = "127954653962405" },
    { name = "Som estourado bass boost", id = "123592956882621" },
    { name = "Estourado espad", id = "136179020015211" },
    { name = "Alaku boom", id = "110796593805268" },
    { name = "Bus horn sound", id = "139841197791567" },
    { name = "Estourado wItb earrape", id = "137478052262430" },
    { name = "Sla meme", id = "116672405522828" },
    { name = "HA HA HA phonk/laugh", id = "138236682866721" }
}

local globalMemeNames = {}
local globalMemeMap = {}
for _, v in ipairs(globalMemeSounds) do
    table.insert(globalMemeNames, v.name)
    globalMemeMap[v.name] = v.id
end

local selectedGlobalSound = globalMemeSounds[1].id
local globalLoopRunning = false

local function playSoundGlobally(soundId)
    if not soundId or soundId == "" then return end
    local Players = game:GetService("Players")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local remote = ReplicatedStorage:FindFirstChild("RE")
    local gunRemote = remote and remote:FindFirstChild("1Gu1n")
    if not gunRemote then return end

    for _, player in ipairs(Players:GetPlayers()) do
        if player.Character and player.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = player.Character.HumanoidRootPart
            local args = {
                [1] = hrp,
                [2] = hrp,
                [3] = Vector3.new(0, -1, 0),
                [4] = Vector3.new(0, 0, 0),
                [5] = hrp,
                [6] = hrp,
                [7] = tostring(soundId),
                [8] = tostring(soundId),
                [9] = { [1] = true },
                [10] = {
                    [1] = 25,
                    [2] = Vector3.new(0.25, 0.25, 100),
                    [3] = BrickColor.new(24),
                    [4] = 0.25,
                    [5] = Enum.Material.SmoothPlastic,
                    [6] = 0.25
                },
                [11] = true,
                [12] = false
            }
            pcall(function()
                gunRemote:FireServer(unpack(args))
            end)
        end
    end
end

local function playLocalSoundPreview(soundId)
    pcall(function()
        local s = Instance.new("Sound")
        s.SoundId = "rbxassetid://" .. tostring(soundId)
        s.Volume = 3
        s.Looped = false
        s.Parent = workspace
        s:Play()
        task.delay(5, function()
            pcall(function() s:Destroy() end)
        end)
    end)
end

AudioTab:AddDropdown({
    Name = "Global Meme Sounds",
    Description = "Choose a sound to play globally",
    Default = globalMemeNames[1],
    Options = globalMemeNames,
    Callback = function(val)
        selectedGlobalSound = globalMemeMap[val] or globalMemeSounds[1].id
    end
})

AudioTab:AddButton({
    Name = "Play Selected Sound",
    Description = "Plays the sound globally for everyone",
    Callback = function()
        if selectedGlobalSound then
            playSoundGlobally(selectedGlobalSound)
            playLocalSoundPreview(selectedGlobalSound)
        end
    end
})

AudioTab:AddToggle({
    Name = "Loop Global Sound",
    Description = "Repeats the selected sound globally",
    Default = false,
    Callback = function(state)
        globalLoopRunning = state
        if globalLoopRunning then
            task.spawn(function()
                while globalLoopRunning do
                    if selectedGlobalSound then
                        playSoundGlobally(selectedGlobalSound)
                        playLocalSoundPreview(selectedGlobalSound)
                    end
                    task.wait(1)
                end
            end)
        end
    end
})

-- [LOCKED: WORK PERFECTLY] ONGLET PREMIUM COMPLET

HouseTab:AddSection({
    "> Advanced House Functions"
})

HouseTab:AddButton({
    Name = "Unban All Houses",
    Description = "Destroys ban barriers in all houses",
    Callback = function()
        local lotsFolder = workspace:FindFirstChild("001_Lots")
        if not lotsFolder then return end
        for _, lot in pairs(lotsFolder:GetChildren()) do
            pcall(function()
                local housePicked = lot:FindFirstChild("HousePickedByPlayer")
                if housePicked then
                    local houseModel = housePicked:FindFirstChild("HouseModel")
                    if houseModel then
                        local bannedBlock = houseModel:FindFirstChild("BannedBlock")
                        if bannedBlock then bannedBlock:Destroy() end
                    end
                end
            end)
        end
    end
})

_G.LoopUnbanHouses = false
HouseTab:AddToggle({
    Name = "Auto Unban (Loop)",
    Description = "Automatically destroys ban barriers (every 0.5s)",
    Default = false,
    Callback = function(state)
        _G.LoopUnbanHouses = state
        if state then
            task.spawn(function()
                while _G.LoopUnbanHouses do
                    local lotsFolder = workspace:FindFirstChild("001_Lots")
                    if lotsFolder then
                        for _, lot in pairs(lotsFolder:GetChildren()) do
                            pcall(function()
                                local housePicked = lot:FindFirstChild("HousePickedByPlayer")
                                if housePicked then
                                    local houseModel = housePicked:FindFirstChild("HouseModel")
                                    if houseModel then
                                        local bannedBlock = houseModel:FindFirstChild("BannedBlock")
                                        if bannedBlock then bannedBlock:Destroy() end
                                    end
                                end
                            end)
                        end
                    end
                    task.wait(0.5)
                end
            end)
        end
    end
})

_G.RainbowHouseActive = false
HouseTab:AddToggle({
    Name = "Rainbow House (RGB)",
    Description = "Cycles your house color through RGB",
    Default = false,
    Callback = function(state)
        _G.RainbowHouseActive = state
        if state then
            task.spawn(function()
                while _G.RainbowHouseActive do
                    local t = tick()
                    local hue = (t % 10) / 10
                    local color = Color3.fromHSV(hue, 1, 1)
                    local re = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                    if re then
                        local remote = re:FindFirstChild("1Player1sHous1e")
                        if remote then
                            pcall(function() remote:FireServer("ColorPickHouse", color) end)
                        end
                    end
                    task.wait(0.05)
                end
            end)
        end
    end
})

_G.RainbowTextActive = false
HouseTab:AddToggle({
    Name = "Rainbow Sign Text (RGB)",
    Description = "Cycles your house sign text color through RGB",
    Default = false,
    Callback = function(state)
        _G.RainbowTextActive = state
        if state then
            task.spawn(function()
                while _G.RainbowTextActive do
                    local t = tick()
                    local hue = (t % 10) / 10
                    local color = Color3.fromHSV(hue, 1, 1)
                    local re = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
                    if re then
                        local remote = re:FindFirstChild("1RPHous1eEven1tColo1r")
                        if remote then
                            pcall(function() remote:FireServer("PickingBusinessNameColor", color) end)
                        end
                    end
                    task.wait(0.05)
                end
            end)
        end
    end
})

HouseTab:AddSection({
    "Music Player"
})

HouseTab:AddButton({
    Name = "Play: It's Raining Tacos",
    Description = "Plays It's Raining Tacos in your house",
    Callback = function()
        local re = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        local remote = re and re:FindFirstChild("1Player1sHous1e")
        if remote then
            pcall(function() remote:FireServer("PickHouseMusicText", "142376088", nil, true) end)
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "House Music",
                Text = "Now playing: It's Raining Tacos",
                Duration = 4
            })
        end
    end
})

HouseTab:AddButton({
    Name = "Stop Music",
    Description = "Stops the current music in your house",
    Callback = function()
        local re = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        local remote = re and re:FindFirstChild("1Player1sHous1e")
        if remote then
            pcall(function() remote:FireServer("PickingHouseMusicStop", "") end)
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "House Music",
                Text = "Music stopped.",
                Duration = 3
            })
        end
    end
})

local _customHouseMusicId = ""
HouseTab:AddTextBox({
    Title = "Custom Music ID",
    Description = "Enter a Roblox Sound ID to play in your house",
    Default = "",
    PlaceholderText = "e.g. 142376088",
    ClearText = false,
    Callback = function(value)
        _customHouseMusicId = tostring(value)
    end
})

HouseTab:AddButton({
    Name = "Play Custom ID",
    Description = "Plays the custom sound ID entered above",
    Callback = function()
        if _customHouseMusicId == "" then
            game:GetService("StarterGui"):SetCore("SendNotification", {Title="House Music", Text="Enter an ID first!", Duration=3})
            return
        end
        local re = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        local remote = re and re:FindFirstChild("1Player1sHous1e")
        if remote then
            pcall(function() remote:FireServer("PickHouseMusicText", _customHouseMusicId, nil, true) end)
            game:GetService("StarterGui"):SetCore("SendNotification", {Title="House Music", Text="Playing ID: " .. _customHouseMusicId, Duration=4})
        end
    end
})

-- [LOCKED: WORK PERFECTLY] Scan des vehicules joueurs (Owner/Chassis/GUID)

local function scanVehicles()
    _G.FunVehicleData = {}
    _G.FunVehicleLabels = { "---" }
    _G.FunLabelToKey = {}
    local vehiclesFolder = workspace:FindFirstChild("Vehicles")
    if not vehiclesFolder then return end
    
    local playerNames = {}
    for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
        playerNames[string.lower(p.Name)] = true
        if p.DisplayName and p.DisplayName ~= "" then
            playerNames[string.lower(p.DisplayName)] = true
        end
    end
    for _, v in ipairs(vehiclesFolder:GetChildren()) do
        pcall(function()
            if not v:IsA("Model") then return end
            
            local seat = nil
            for _, d in ipairs(v:GetDescendants()) do
                if d:IsA("VehicleSeat") or d:IsA("Seat") then
                    seat = d
                    break
                end
            end
            if not seat then return end
            
            if string.find(string.lower(v.Name), "brookhaven", 1, true) then return end

            
            local ownerVal = v:FindFirstChild("Owner")
            local ownerName = nil
            if ownerVal then
                pcall(function()
                    if ownerVal:IsA("ObjectValue") and ownerVal.Value then
                        ownerName = ownerVal.Value.Name
                    elseif ownerVal.Value then
                        ownerName = tostring(ownerVal.Value)
                    end
                end)
            end
            
            local lowerName = string.lower(v.Name)
            if not ownerName or ownerName == "" or ownerName == "nil" then
                for pname, _ in pairs(playerNames) do
                    if string.find(lowerName, pname, 1, true) then
                        ownerName = pname
                        break
                    end
                end
            end

            
            local hasGuid = string.find(lowerName, "-%x+") ~= nil
            local hasChassis = v:FindFirstChild("Chassis") ~= nil
            local isPlayerNamed = false
            local ownerNameLower = ownerName and string.lower(ownerName) or ""
            if ownerNameLower ~= "" then
                if string.find(lowerName, ownerNameLower .. "car", 1, true) then
                    isPlayerNamed = true
                end
            end

            local ownerIsOnline = false
            if ownerName then
                ownerIsOnline = playerNames[string.lower(ownerName)] == true
            end
            local nameHasOnline = false
            for pname, _ in pairs(playerNames) do
                if string.find(lowerName, pname, 1, true) then
                    nameHasOnline = true
                    break
                end
            end

            local isPlayerVehicle = (hasGuid or isPlayerNamed or hasChassis) and (ownerIsOnline or nameHasOnline)
            if not isPlayerVehicle then return end

            
            local occupied = (seat.Occupant ~= nil)
            if occupied then return end

            
            local groundCheck = v.PrimaryPart or seat or v:FindFirstChildOfClass("BasePart")
            if groundCheck then
                if groundCheck.Position.Y < -50 then return end
                local rayParams = RaycastParams.new()
                rayParams.FilterType = Enum.RaycastFilterType.Exclude
                rayParams.FilterDescendantsInstances = { v }
                local rayOrigin = groundCheck.Position + Vector3.new(0, 1, 0)
                local rayDir = Vector3.new(0, -50, 0)
                local rayHit = workspace:Raycast(rayOrigin, rayDir, rayParams)
                if rayHit then
                    local distToGround = (groundCheck.Position.Y + 1 - rayHit.Position.Y)
                    if distToGround > 8 then return end
                end
            end

            local key = v.Name
            local cleanName = key:gsub("%-%-?%x+$", "")
            if cleanName == "" then cleanName = key end

            _G.FunVehicleData[key] = {
                model = v,
                seat = seat,
                occupied = false,
                ownerName = ownerName,
                cleanName = cleanName
            }

            local display = (ownerName and ownerName ~= "" and ownerName ~= "nil") and ownerName or cleanName
            local uniqueDisplay = display
            local counter = 2
            while _G.FunLabelToKey[uniqueDisplay] do
                uniqueDisplay = display .. " #" .. counter
                counter = counter + 1
            end

            _G.FunLabelToKey[uniqueDisplay] = key
            table.insert(_G.FunVehicleLabels, uniqueDisplay)
        end)
    end
end

local funTargetDD = nil
local lastVehicleSig = ""

local function softRefreshFunTargets()
    local prevKey = _G.FunTarget
    scanVehicles()
    if funTargetDD then
        local currentSig = table.concat(_G.FunVehicleLabels or {}, "|")
        if currentSig ~= lastVehicleSig then
            lastVehicleSig = currentSig
            pcall(function() funTargetDD:Set(_G.FunVehicleLabels) end)
        end
        
        if prevKey and prevKey ~= "---" and _G.FunVehicleData[prevKey] then
            local newLabel = nil
            for lbl, key in pairs(_G.FunLabelToKey or {}) do
                if key == prevKey then
                    newLabel = lbl
                    break
                end
            end
            if newLabel then
                pcall(function() funTargetDD:SetValue(newLabel) end)
                _G.FunTarget = prevKey
            end
        else
            pcall(function() funTargetDD:SetValue("---") end)
            _G.FunTarget = "---"
        end
    end
end

local function refreshFunTargets()
    scanVehicles()
    if funTargetDD then
        lastVehicleSig = table.concat(_G.FunVehicleLabels or {}, "|")
        pcall(function() funTargetDD:Set(_G.FunVehicleLabels) end)
        pcall(function() funTargetDD:SetValue("---") end)
    end
    _G.FunTarget = "---"
end

task.spawn(function()
    local vehiclesFolder = workspace:WaitForChild("Vehicles", 10)
    if vehiclesFolder then
        vehiclesFolder.ChildAdded:Connect(function()
            task.wait(0.5) 
            softRefreshFunTargets()
        end)
        vehiclesFolder.ChildRemoved:Connect(function()
            softRefreshFunTargets()
        end)
    end
end)

task.spawn(function()
    while true do
        task.wait(8)
        softRefreshFunTargets()
    end
end)

funTargetDD = VehicleTab:AddDropdown({
    Name = "Target Vehicle",
    Description = "Select the vehicle to use",
    Options = { "---" },
    Default = "---",
    Callback = function(v)
        _G.FunTarget = _G.FunLabelToKey and _G.FunLabelToKey[v] or "---"
        if not _G.FunTarget then _G.FunTarget = "---" end
    end
})

VehicleTab:AddSection({
    "> Vehicle"
})

-- [LOCKED: WORK PERFECTLY] Destroy (scan map)
VehicleTab:AddButton({
    Name = "Destroy",
    Description = "Destroys the selected vehicle",
    Callback = function()
        task.spawn(function()
            _G.withAntiSitDisabled(function()
                local Players = game:GetService("Players")
                local lp = Players.LocalPlayer
                local char = lp.Character or lp.CharacterAdded:Wait()
                local hrp = char:WaitForChild("HumanoidRootPart")
                local hum = char:WaitForChild("Humanoid")
                local startCF = hrp.CFrame

                if not _G.FunTarget or _G.FunTarget == "---" then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Carjack", Text="Scan + select a vehicle first!", Duration=3})
                    return
                end

                local data = _G.FunVehicleData[_G.FunTarget]
                if not data then
                    
                    scanVehicles()
                    data = _G.FunVehicleData[_G.FunTarget]
                end
                if not data then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Carjack", Text="Vehicle not found! Re-scan.", Duration=3})
                    return
                end

                local car, seat = data.model, data.seat
                if not car or not seat then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Carjack", Text="No seat found on this vehicle!", Duration=3})
                    return
                end
                if data.occupied then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Carjack", Text="Seat occupied! (FREE vehicles only)", Duration=3})
                    return
                end

                
                pcall(function() hrp.CFrame = seat.CFrame * CFrame.new(0, 1.5, 0) end)
                task.wait(0.2)
                
                hum.Sit = true
                local sitTries = 0
                local sat = false
                while sitTries < 15 do
                    if hum.SeatPart == seat then sat = true break end
                    pcall(function() seat:Sit(hum) end)
                    hum.Sit = true
                    task.wait(0.1)
                    sitTries = sitTries + 1
                end
                if not sat then
                    pcall(function()
                        local myChar = lp.Character
                        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                        if myHrp then myHrp.CFrame = startCF end
                    end)
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Carjack", Text="Carjack failed! (can't sit in this vehicle)", Duration=3})
                    return
                end

                
                task.wait(0.5)
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Carjack", Text="Carjacked! Flinging " .. car.Name .. " to the void...", Duration=3})

                
                
                
                local pp = car.PrimaryPart or seat
                local bv = nil
                pcall(function()
                    bv = Instance.new("BodyVelocity")
                    bv.Name = "CarjackPower"
                    bv.Velocity = Vector3.new(0, 5000, 0) 
                    bv.MaxForce = Vector3.new(9e8, 9e8, 9e8)
                    bv.P = 125000
                    bv.Parent = pp
                end)
                pcall(function()
                    pp.AssemblyLinearVelocity = Vector3.new(0, 5000, 0)
                end)
                
                task.wait(0.15)

                
                pcall(function()
                    pp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                    pp.RotVelocity = Vector3.new(0, 0, 0)
                end)
                task.wait()
                pcall(function() hum.Sit = false end)
                pcall(function() hum:ChangeState(Enum.HumanoidStateType.GettingUp) end)
                pcall(function() hum.Jump = true end)
                pcall(function() hum:UnequipTools() end)
                local myHrpNow = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
                if myHrpNow then
                    pcall(function()
                        myHrpNow.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                        myHrpNow.RotVelocity = Vector3.new(0, 0, 0)
                    end)
                end

                
                pcall(function()
                    local myChar = lp.Character
                    local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                    if myHrp then myHrp.CFrame = startCF end
                end)

                
                task.wait(0.2)
                if bv then
                    
                    pcall(function() bv.Velocity = Vector3.new(1500, -5000, 1500) end)
                end
                pcall(function()
                    pp.AssemblyLinearVelocity = Vector3.new(1500, -5000, 1500)
                end)
                task.wait(0.2)
                pcall(function()
                    if car.PrimaryPart then
                        car:PivotTo(CFrame.new(car.PrimaryPart.Position.X, -90000, car.PrimaryPart.Position.Z))
                    else
                        seat.CFrame = CFrame.new(seat.Position.X, -90000, seat.Position.Z)
                    end
                end)
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Carjack", Text="Vehicle sent to the void! TP back done.", Duration=4})
            end)
        end)
    end
})

-- [LOCKED: WORK PERFECTLY] Glitch Vehicle

VehicleTab:AddButton({
    Name = "Glitch Vehicle",
    Description = "The selected vehicle can no longer be spawned by its owner",
    Callback = function()
        task.spawn(function()
            _G.withAntiSitDisabled(function()
                local Players = game:GetService("Players")
                local lp = Players.LocalPlayer
                local char = lp.Character or lp.CharacterAdded:Wait()
                local hrp = char:WaitForChild("HumanoidRootPart")
                local hum = char:WaitForChild("Humanoid")
                local startCF = hrp.CFrame

                if not _G.FunTarget or _G.FunTarget == "---" then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Glitch Vehicle", Text="Scan + select a vehicle first!", Duration=3})
                    return
                end
                local data = _G.FunVehicleData[_G.FunTarget]
                if not data then
                    scanVehicles()
                    data = _G.FunVehicleData[_G.FunTarget]
                end
                if not data then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Glitch Vehicle", Text="Vehicle not found! Re-scan.", Duration=3})
                    return
                end
                local car, seat = data.model, data.seat
                if not car or not seat then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Glitch Vehicle", Text="No seat found on this vehicle!", Duration=3})
                    return
                end
                if data.occupied then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Glitch Vehicle", Text="Seat occupied! (FREE vehicles only)", Duration=3})
                    return
                end

                
                pcall(function() hrp.CFrame = seat.CFrame * CFrame.new(0, 1.5, 0) end)
                task.wait(0.2)
                
                hum.Sit = true
                local sitTries = 0
                local sat = false
                while sitTries < 15 do
                    if hum.SeatPart == seat then sat = true break end
                    pcall(function() seat:Sit(hum) end)
                    hum.Sit = true
                    task.wait(0.1)
                    sitTries = sitTries + 1
                end
                if not sat then
                    pcall(function()
                        local myChar = lp.Character
                        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                        if myHrp then myHrp.CFrame = startCF end
                    end)
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Glitch Vehicle", Text="Glitch failed! (can't sit in this vehicle)", Duration=3})
                    return
                end

                
                task.wait(0.5)
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Glitch Vehicle", Text="Glitching! Sending " .. car.Name .. " to the kill zone...", Duration=3})

                
                
                
                
                
                
                local pp = car.PrimaryPart or seat
                local bv = nil
                pcall(function()
                    bv = Instance.new("BodyVelocity")
                    bv.Name = "GlitchPower"
                    bv.Velocity = Vector3.new(0, -5000, 0) 
                    bv.MaxForce = Vector3.new(9e8, 9e8, 9e8) 
                    bv.P = 125000 
                    bv.Parent = pp
                end)
                pcall(function()
                    pp.AssemblyLinearVelocity = Vector3.new(0, -5000, 0)
                end)

                
                
                local died = false
                for _ = 1, 20 do
                    task.wait(0.1)
                    local ch = lp.Character
                    local h = ch and ch:FindFirstChildOfClass("Humanoid")
                    if not ch or not h or h.Health <= 0 then died = true break end
                end

                
                
                if bv then
                    pcall(function() bv.Velocity = Vector3.new(0, -20000, 0) end)
                end
                task.wait(0.2)
                pcall(function()
                    if car.PrimaryPart then
                        car:PivotTo(CFrame.new(car.PrimaryPart.Position.X, -90000, car.PrimaryPart.Position.Z))
                    else
                        seat.CFrame = CFrame.new(seat.Position.X, -90000, seat.Position.Z)
                    end
                end)
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Glitch Vehicle", Text="Vehicle glitched! The owner can't respawn it anymore.", Duration=4})
            end)
        end)
    end
})

-- [LOCKED: WORK PERFECTLY] Glitch All Free Vehicles

VehicleTab:AddButton({
    Name = "Glitch All Free Vehicles",
    Description = "Glitches every free vehicle to the void (same as Glitch Vehicle)",
    Callback = function()
        task.spawn(function()
            _G.withAntiSitDisabled(function()
                local Players = game:GetService("Players")
                local lp = Players.LocalPlayer
                local char = lp.Character or lp.CharacterAdded:Wait()
                local hrp = char:WaitForChild("HumanoidRootPart")
                local hum = char:WaitForChild("Humanoid")
                local startCF = hrp.CFrame
                
                scanVehicles()
                local freeList = {}
                for _, d in pairs(_G.FunVehicleData) do
                    if d and not d.occupied then
                        table.insert(freeList, d)
                    end
                end
                if #freeList == 0 then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Glitch All Free Vehicles", Text="No free PLAYER vehicles found!", Duration=3})
                    return
                end
                
                -- EXACT que Glitch Vehicle (LOCKED) pour chaque vehicule libre.
                
                
                
                
                local removed = 0
                for _, d in ipairs(freeList) do
                    pcall(function()
                        local car = d.model
                        local seat = d.seat
                        if not car or not seat then return end

                        
                        if hrp then
                            pcall(function() hrp.CFrame = seat.CFrame * CFrame.new(0, 1.5, 0) end)
                        end
                        task.wait(0.2)
                        
                        if hum then hum.Sit = true end
                        local sitTries = 0
                        local sat = false
                        while sitTries < 15 do
                            if hum and hum.SeatPart == seat then sat = true break end
                            pcall(function() seat:Sit(hum) end)
                            if hum then hum.Sit = true end
                            task.wait(0.1)
                            sitTries = sitTries + 1
                        end
                        if not sat then return end

                        
                        task.wait(0.5)

                        
                        local pp = car.PrimaryPart or seat
                        local bv = nil
                        pcall(function()
                            bv = Instance.new("BodyVelocity")
                            bv.Name = "GlitchAllPower"
                            bv.Velocity = Vector3.new(0, -5000, 0)
                            bv.MaxForce = Vector3.new(9e8, 9e8, 9e8)
                            bv.P = 125000
                            bv.Parent = pp
                        end)
                        pcall(function()
                            pp.AssemblyLinearVelocity = Vector3.new(0, -5000, 0)
                        end)

                        
                        local died = false
                        for _ = 1, 20 do
                            task.wait(0.1)
                            local ch = lp.Character
                            local h = ch and ch:FindFirstChildOfClass("Humanoid")
                            if not ch or not h or h.Health <= 0 then died = true break end
                        end

                        
                        if bv then
                            pcall(function() bv.Velocity = Vector3.new(0, -20000, 0) end)
                        end
                        task.wait(0.2)
                        pcall(function()
                            if car.PrimaryPart then
                                car:PivotTo(CFrame.new(car.PrimaryPart.Position.X, -90000, car.PrimaryPart.Position.Z))
                            else
                                seat.CFrame = CFrame.new(seat.Position.X, -90000, seat.Position.Z)
                            end
                        end)
                        removed = removed + 1
                    end)

                    
                    task.wait(1)
                    local curChar = lp.Character
                    local curHum = curChar and curChar:FindFirstChildOfClass("Humanoid")
                    if not curChar or not curHum or curHum.Health <= 0 then
                        local newChar = lp.Character or lp.CharacterAdded:Wait()
                        local newHrp = newChar:WaitForChild("HumanoidRootPart", 5)
                        local newHum = newChar:FindFirstChildOfClass("Humanoid")
                        if newHrp then
                            char = newChar
                            hrp = newHrp
                            hum = newHum
                        end
                        task.wait(0.5)
                    end
                end
                
                pcall(function() hrp.CFrame = startCF end)
                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Glitch All Free Vehicles", Text="Glitched " .. removed .. " free vehicles to the void!", Duration=4})

            end)
        end)
    end
})

-- [LOCKED: WORK PERFECTLY] REMOVE ALL VEHICLES (vrai wipe serveur)

VehicleTab:AddButton({
    Name = "Remove All Vehicles",
    Description = "Carjacks ALL free vehicles to the void (you survive + TP back)",
    Callback = function()
        task.spawn(function()
            _G.withAntiSitDisabled(function()
                local Players = game:GetService("Players")
                local lp = Players.LocalPlayer
                local char = lp.Character or lp.CharacterAdded:Wait()
                local hrp = char:WaitForChild("HumanoidRootPart")
                local hum = char:WaitForChild("Humanoid")
                local startCF = hrp.CFrame

                
                scanVehicles()
                local allList = {}
                for _, d in pairs(_G.FunVehicleData) do
                    if d and not d.occupied then
                        table.insert(allList, d)
                    end
                end
                if #allList == 0 then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Remove All Vehicles", Text="No free player vehicles found!", Duration=3})
                    return
                end

                local removed = 0

                
                for _, d in ipairs(allList) do
                    pcall(function()
                        local car = d.model
                        local seat = d.seat
                        if not car or not seat then return end

                        
                        pcall(function() hrp.CFrame = seat.CFrame * CFrame.new(0, 1.5, 0) end)
                        task.wait(0.2)

                        
                        hum.Sit = true
                        local sitTries = 0
                        local sat = false
                        while sitTries < 15 do
                            if hum.SeatPart == seat then sat = true break end
                            pcall(function() seat:Sit(hum) end)
                            hum.Sit = true
                            task.wait(0.1)
                            sitTries = sitTries + 1
                        end
                        if not sat then return end

                        
                        task.wait(0.3)

                        
                        local pp = car.PrimaryPart or seat
                        local bv = nil
                        pcall(function()
                            bv = Instance.new("BodyVelocity")
                            bv.Name = "RemoveAllPower"
                            bv.Velocity = Vector3.new(0, 5000, 0) 
                            bv.MaxForce = Vector3.new(9e8, 9e8, 9e8)
                            bv.P = 125000
                            bv.Parent = pp
                        end)
                        pcall(function() pp.AssemblyLinearVelocity = Vector3.new(0, 5000, 0) end)
                        task.wait(0.15)

                        
                        pcall(function()
                            pp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                            pp.RotVelocity = Vector3.new(0, 0, 0)
                        end)
                        task.wait()
                        pcall(function() hum.Sit = false end)
                        pcall(function() hum:ChangeState(Enum.HumanoidStateType.GettingUp) end)
                        pcall(function() hum.Jump = true end)
                        pcall(function() hum:UnequipTools() end)
                        local myHrpNow = lp.Character and lp.Character:FindFirstChild("HumanoidRootPart")
                        if myHrpNow then
                            pcall(function()
                                myHrpNow.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                                myHrpNow.RotVelocity = Vector3.new(0, 0, 0)
                            end)
                        end

                        
                        pcall(function()
                            local myChar = lp.Character
                            local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                            if myHrp then myHrp.CFrame = startCF end
                        end)

                        
                        task.wait(0.2)
                        if bv then
                            pcall(function() bv.Velocity = Vector3.new(1500, -5000, 1500) end)
                        end
                        pcall(function() pp.AssemblyLinearVelocity = Vector3.new(1500, -5000, 1500) end)
                        task.wait(0.2)
                        pcall(function()
                            if car.PrimaryPart then
                                car:PivotTo(CFrame.new(car.PrimaryPart.Position.X, -90000, car.PrimaryPart.Position.Z))
                            else
                                seat.CFrame = CFrame.new(seat.Position.X, -90000, seat.Position.Z)
                            end
                        end)
                        removed = removed + 1
                    end)
                end

                game:GetService("StarterGui"):SetCore("SendNotification", {Title="Remove All Vehicles", Text="Sent " .. removed .. " vehicles to the void! TP back done.", Duration=4})
            end)
        end)
    end
})

-- [LOCKED: WORK PERFECTLY] Car Dislocator System (FE Wheel Tear)
_G.BTR_CarDislocator = {}
do
    local function enableSimulationRadius()
        pcall(function()
            local lp = game:GetService("Players").LocalPlayer
            lp.ReplicationFocus = workspace
            if setscriptable then
                setscriptable(lp, "SimulationRadius", true)
                setscriptable(lp, "MaximumSimulationRadius", true)
            end
            if sethiddenproperty then
                sethiddenproperty(lp, "MaximumSimulationRadius", 1e9)
                sethiddenproperty(lp, "SimulationRadius", 1e9)
            end
        end)
    end

    function _G.BTR_CarDislocator.getNearbyVehicles(radius)
        local lp = game:GetService("Players").LocalPlayer
        local char = lp and lp.Character
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if not root then return {} end

        local myPos = root.Position
        local found = {}
        local seenModels = {}

        local function checkModel(model)
            if not model or not model:IsA("Model") or seenModels[model] then return end
            if char and model:IsDescendantOf(char) then return end

            local seat = model:FindFirstChildWhichIsA("VehicleSeat", true)
            if not seat then return end

            local primary = model.PrimaryPart or (seat and seat.Parent and seat.Parent:FindFirstChildWhichIsA("BasePart")) or seat
            if not primary then return end

            local dist = (primary.Position - myPos).Magnitude
            if not radius or dist <= radius then
                seenModels[model] = true
                table.insert(found, {
                    Model = model,
                    Seat = seat,
                    Primary = primary,
                    Distance = dist
                })
            end
        end

        local vehiclesFolder = workspace:FindFirstChild("Vehicles")
        if vehiclesFolder then
            for _, veh in ipairs(vehiclesFolder:GetChildren()) do
                checkModel(veh)
            end
        end

        for _, obj in ipairs(workspace:GetChildren()) do
            if obj:IsA("Model") and obj ~= char then
                checkModel(obj)
            end
        end

        local wc = workspace:FindFirstChild("WorkspaceCom")
        local helis = wc and wc:FindFirstChild("001_HeliStorage")
        if helis then
            for _, h in ipairs(helis:GetChildren()) do
                checkModel(h)
            end
        end

        table.sort(found, function(a, b) return a.Distance < b.Distance end)
        return found
    end

    function _G.BTR_CarDislocator.dislocate(target, returnCF)
        local lp = game:GetService("Players").LocalPlayer
        local char = lp and lp.Character or lp.CharacterAdded:Wait()
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        local root = char and char:FindFirstChild("HumanoidRootPart")
        if not char or not hum or not root then return false end

        local model = target.Model or target.model or target
        local seat = target.Seat or target.seat or (model and model:FindFirstChildWhichIsA("VehicleSeat", true))
        local primary = target.Primary or target.primary or (model and model.PrimaryPart) or seat
        if not seat or not seat.Parent or not model or not model.Parent or not primary then return false end
        if seat.Occupant and seat.Occupant ~= hum then return false end

        enableSimulationRadius()

        local origCF = returnCF or root.CFrame

        pcall(function()
            seat.Disabled = false
            seat.HandBrake = false
        end)

        if firetouchinterest then
            pcall(function() firetouchinterest(root, seat, 0) end)
        end
        pcall(function() seat:Sit(hum) end)
        pcall(function() hum:ChangeState(Enum.HumanoidStateType.Seated) end)

        local sitStart = tick()
        while not hum.Sit and (tick() - sitStart < 0.7) do
            root.CFrame = seat.CFrame * CFrame.new(0, 0.9, 0)
            root.AssemblyLinearVelocity = Vector3.zero
            pcall(function()
                hum:ChangeState(Enum.HumanoidStateType.Seated)
                seat:Sit(hum)
            end)
            task.wait(0.03)
        end

        if firetouchinterest then
            pcall(function() firetouchinterest(root, seat, 1) end)
        end

        task.wait(0.08)

        pcall(function()
            seat.HandBrake = false
        end)

        pcall(function()
            local wheelParts = {}
            for _, constraint in ipairs(model:GetDescendants()) do
                if constraint:IsA("Constraint") then
                    if constraint.Attachment0 and constraint.Attachment0.Parent and constraint.Attachment0.Parent:IsA("BasePart") then
                        local p = constraint.Attachment0.Parent
                        if p ~= primary and p ~= seat and not p:IsDescendantOf(char) then
                            wheelParts[p] = true
                        end
                    end
                    if constraint.Attachment1 and constraint.Attachment1.Parent and constraint.Attachment1.Parent:IsA("BasePart") then
                        local p = constraint.Attachment1.Parent
                        if p ~= primary and p ~= seat and not p:IsDescendantOf(char) then
                            wheelParts[p] = true
                        end
                    end
                end
            end

            for _, p in ipairs(model:GetDescendants()) do
                if p:IsA("BasePart") and p ~= primary and p ~= seat and not p:IsDescendantOf(char) then
                    local n = p.Name:lower()
                    if n:find("wheel") or n:find("tire") or n:find("rim") or n:find("roue") or n:find("fl") or n:find("fr") or n:find("rl") or n:find("rr") then
                        wheelParts[p] = true
                    end
                end
            end

            primary.Anchored = true
            seat.Anchored = true
            primary.AssemblyLinearVelocity = Vector3.zero
            primary.AssemblyAngularVelocity = Vector3.zero

            for _, constraint in ipairs(model:GetDescendants()) do
                if constraint:IsA("SpringConstraint") then
                    pcall(function()
                        constraint.Stiffness = 0
                        constraint.Damping = 0
                        constraint.FreeLength = 12
                        constraint.MaxLength = 50
                    end)
                elseif constraint:IsA("CylindricalConstraint") or constraint:IsA("PrismaticConstraint") or constraint:IsA("HingeConstraint") then
                    pcall(function()
                        if constraint.LimitsEnabled ~= nil then constraint.LimitsEnabled = false end
                        if constraint.UpperLimit ~= nil then constraint.UpperLimit = 50 end
                        if constraint.LowerLimit ~= nil then constraint.LowerLimit = -50 end
                    end)
                end
            end

            local chassisCF = primary.CFrame
            for step = 1, 3 do
                for wheel, _ in pairs(wheelParts) do
                    if wheel and wheel.Parent then
                        local relPos = chassisCF:PointToObjectSpace(wheel.Position)
                        local isRight = (relPos.X >= 0)
                        local sideSign = isRight and 1 or -1
                        local sideDir = chassisCF.RightVector * sideSign

                        local targetPos = wheel.Position + (sideDir * 4.5) - Vector3.new(0, 1.2, 0)
                        wheel.CFrame = CFrame.new(targetPos, targetPos + chassisCF.LookVector) * CFrame.Angles(math.rad(90), 0, 0)

                        wheel.AssemblyLinearVelocity = (sideDir * 6) + Vector3.new(0, -8, 0)
                        wheel.AssemblyAngularVelocity = Vector3.new(0, 2, 0)
                    end
                end
                task.wait(0.04)
            end

            task.wait(0.1)

            primary.Anchored = false
            seat.Anchored = false
            primary.AssemblyLinearVelocity = Vector3.new(0, -10, 0)
            primary.AssemblyAngularVelocity = Vector3.zero
            task.wait(0.08)
        end)

        hum.Sit = false
        task.wait(0.04)

        if root and root.Parent and origCF then
            root.CFrame = origCF
            root.AssemblyLinearVelocity = Vector3.zero
            root.AssemblyAngularVelocity = Vector3.zero
        end

        return true
    end
end

-- [LOCKED: WORK PERFECTLY] Dislocate Closest Car
VehicleTab:AddButton({
    Name = "Dislocate Closest Car",
    Description = "Dislocates and tears off the wheels of the nearest car (FE)",
    Callback = function()
        task.spawn(function()
            local wasAntiSit = (_G.AntiSitValue == true)
            if wasAntiSit then
                if _G.AntiSitForceOff then pcall(_G.AntiSitForceOff) end
                _G.VehicleFlingActive = true
                task.wait(0.05)
            end

            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character or lp.CharacterAdded:Wait()
            local hrp = char:WaitForChild("HumanoidRootPart")
            local startCF = hrp.CFrame

            local list = _G.BTR_CarDislocator.getNearbyVehicles(150)
            local target = list and list[1]
            if not target or not target.Model or not target.Seat then
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Dislocate Car",
                    Text = "No vehicle found within 150 studs!",
                    Duration = 3
                })
                if wasAntiSit then
                    _G.VehicleFlingActive = false
                    if _G.AntiSitForceOn then pcall(_G.AntiSitForceOn) end
                end
                return
            end

            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Dislocate Car",
                Text = "Dislocating " .. target.Model.Name .. " (" .. math.floor(target.Distance or 0) .. " studs)...",
                Duration = 2
            })

            local ok = _G.BTR_CarDislocator.dislocate(target, startCF)

            if wasAntiSit then
                _G.VehicleFlingActive = false
                if _G.AntiSitForceOn then pcall(_G.AntiSitForceOn) end
            end

            if ok then
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Dislocate Car",
                    Text = target.Model.Name .. " wheels dislocated in FE!",
                    Duration = 3
                })
            else
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Dislocate Car",
                    Text = "Failed to dislocate vehicle.",
                    Duration = 3
                })
            end
        end)
    end
})

-- [LOCKED: WORK PERFECTLY] Dislocate Vehicle (Target)
VehicleTab:AddButton({
    Name = "Dislocate Vehicle",
    Description = "Dislocates and tears off the wheels of the selected vehicle (FE)",
    Callback = function()
        task.spawn(function()
            local wasAntiSit = (_G.AntiSitValue == true)
            if wasAntiSit then
                if _G.AntiSitForceOff then pcall(_G.AntiSitForceOff) end
                _G.VehicleFlingActive = true
                task.wait(0.05)
            end

            local lp = game:GetService("Players").LocalPlayer
            local char = lp.Character or lp.CharacterAdded:Wait()
            local hrp = char:WaitForChild("HumanoidRootPart")
            local startCF = hrp.CFrame

            if not _G.FunTarget or _G.FunTarget == "---" then
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Dislocate Vehicle",
                    Text = "Select a vehicle first!",
                    Duration = 3
                })
                if wasAntiSit then
                    _G.VehicleFlingActive = false
                    if _G.AntiSitForceOn then pcall(_G.AntiSitForceOn) end
                end
                return
            end

            local data = _G.FunVehicleData and _G.FunVehicleData[_G.FunTarget]
            if not data then
                scanVehicles()
                data = _G.FunVehicleData and _G.FunVehicleData[_G.FunTarget]
            end

            if not data or not data.model or not data.seat then
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Dislocate Vehicle",
                    Text = "Vehicle not found! Re-scan.",
                    Duration = 3
                })
                if wasAntiSit then
                    _G.VehicleFlingActive = false
                    if _G.AntiSitForceOn then pcall(_G.AntiSitForceOn) end
                end
                return
            end

            if data.occupied then
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Dislocate Vehicle",
                    Text = "Vehicle is occupied!",
                    Duration = 3
                })
                if wasAntiSit then
                    _G.VehicleFlingActive = false
                    if _G.AntiSitForceOn then pcall(_G.AntiSitForceOn) end
                end
                return
            end

            local targetObj = {
                Model = data.model,
                Seat = data.seat,
                Primary = data.model.PrimaryPart or data.seat
            }

            local ok = _G.BTR_CarDislocator.dislocate(targetObj, startCF)

            if wasAntiSit then
                _G.VehicleFlingActive = false
                if _G.AntiSitForceOn then pcall(_G.AntiSitForceOn) end
            end

            if ok then
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Dislocate Vehicle",
                    Text = data.model.Name .. " wheels dislocated in FE!",
                    Duration = 3
                })
            else
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Dislocate Vehicle",
                    Text = "Failed to dislocate vehicle.",
                    Duration = 3
                })
            end
        end)
    end
})

-- [LOCKED: WORK PERFECTLY] BTR BRING CAR v1  Bring Car / Bring Car All

VehicleTab:AddSection({
    "> Bring Car"
})

local function isVehicleOccupied(car)
    if not car or not car:IsA("Model") then return false end
    local lp = game:GetService("Players").LocalPlayer
    local lpChar = lp and lp.Character
    for _, obj in ipairs(car:GetDescendants()) do
        if obj:IsA("Seat") or obj:IsA("VehicleSeat") then
            if obj.Occupant and obj.Occupant.Parent and obj.Occupant.Parent ~= lpChar then
                return true
            end
        end
    end
    return false
end

local function _bringVehicleToPlayer(car, seat, targetCF, returnCF)
    local lp   = game:GetService("Players").LocalPlayer
    local char = lp.Character or lp.CharacterAdded:Wait()
    local hrp  = char:WaitForChild("HumanoidRootPart")
    local hum  = char:WaitForChild("Humanoid")

    if isVehicleOccupied(car) then return false end

    pcall(function() hum.Sit = false end)
    task.wait(0.05)
    for _ = 1, 15 do
        if isVehicleOccupied(car) then return false end
        pcall(function() hrp.CFrame = seat.CFrame * CFrame.new(0, 1.5, 0) end)
        task.wait(0.06)
        if seat:IsA("Seat") or seat:IsA("VehicleSeat") then
            pcall(function() seat:Sit(hum) end)
        end
        if hum.Sit then break end
    end
    task.wait(0.2)
    if not hum.Sit then return false end

    if not car.PrimaryPart then
        pcall(function() car.PrimaryPart = seat end)
    end
    pcall(function()
        if car.PrimaryPart then
            car:SetPrimaryPartCFrame(targetCF)
        else
            car:PivotTo(targetCF)
        end
    end)
    task.wait(0.15)

    pcall(function()
        if seat then
            for _, w in ipairs(seat:GetChildren()) do
                if w:IsA("Weld") or w.Name == "SeatWeld" or w.Name:find("Seat") then
                    w:Destroy()
                end
            end
        end
        for _, p in ipairs(char:GetDescendants()) do
            if p:IsA("Weld") and (p.Name == "SeatWeld" or p.Name:find("Seat")) then
                p:Destroy()
            end
        end
        hum.Sit = false
        hum.PlatformStand = false
        hum:ChangeState(Enum.HumanoidStateType.Running)
        if returnCF then
            hrp.CFrame = returnCF
        end
        hrp.AssemblyLinearVelocity = Vector3.zero
        hrp.AssemblyAngularVelocity = Vector3.zero
        hrp.Velocity = Vector3.zero
    end)
    task.wait(0.05)
    return true
end

VehicleTab:AddButton({
    Name = "Bring Car",
    Description = "Brings the selected vehicle to you instantly without jumping",
    Callback = function()
        task.spawn(function()
            _G.withAntiSitDisabled(function()
                local lp = game:GetService("Players").LocalPlayer
                local char = lp.Character or lp.CharacterAdded:Wait()
                local hrp = char:WaitForChild("HumanoidRootPart")
                local startCF = hrp.CFrame

                if not _G.FunTarget or _G.FunTarget == "---" then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Bring Car", Text="Select a vehicle first!", Duration=3})
                    return
                end
                local data = _G.FunVehicleData[_G.FunTarget]
                if not data then
                    scanVehicles()
                    data = _G.FunVehicleData[_G.FunTarget]
                end
                if not data then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Bring Car", Text="Vehicle not found! Re-scan.", Duration=3})
                    return
                end
                if not data.model or not data.seat then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Bring Car", Text="No seat found!", Duration=3})
                    return
                end
                if isVehicleOccupied(data.model) then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Bring Car", Text="Seat occupied! Vehicle must be free to bring.", Duration=3})
                    return
                end

                local targetCF = startCF * CFrame.new(0, 0, -7)
                local ok = _bringVehicleToPlayer(data.model, data.seat, targetCF, startCF)
                pcall(function() hrp.CFrame = startCF end)
                if ok then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Bring Car", Text="Car brought to your position!", Duration=4})
                else
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Bring Car", Text="Failed to sit in this vehicle!", Duration=3})
                end
            end)
        end)
    end
})

VehicleTab:AddButton({
    Name = "Bring All Cars",
    Description = "Brings every free player vehicle to you instantly (circle formation)",
    Callback = function()
        task.spawn(function()
            _G.withAntiSitDisabled(function()
                local lp = game:GetService("Players").LocalPlayer
                local char = lp.Character or lp.CharacterAdded:Wait()
                local hrp = char:WaitForChild("HumanoidRootPart")
                local startCF = hrp.CFrame

                scanVehicles()
                local allList = {}
                for _, d in pairs(_G.FunVehicleData) do
                    if d and not isVehicleOccupied(d.model) then
                        table.insert(allList, d)
                    end
                end

                if #allList == 0 then
                    game:GetService("StarterGui"):SetCore("SendNotification", {Title="Bring All Cars", Text="No free player vehicles found!", Duration=3})
                    return
                end

                local brought = 0
                for i, d in ipairs(allList) do
                    if not isVehicleOccupied(d.model) then
                        pcall(function()
                            local angle  = (i - 1) * (math.pi * 2 / #allList)
                            local target = startCF * CFrame.new(math.sin(angle) * 10, 0, math.cos(angle) * 10)
                            local ok = _bringVehicleToPlayer(d.model, d.seat, target, startCF)
                            if ok then brought = brought + 1 end
                        end)
                    end
                end

                pcall(function() hrp.CFrame = startCF end)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Bring All Cars",
                    Text  = "All vehicles brought to your position!",
                    Duration = 4
                })
            end)
        end)
    end
})

VehicleTab:AddSection({
    "> Pirate Ship Tornado"
})

-- [LOCKED: WORK PERFECTLY] PIRATE SHIP TORNADO - VRAI CODE GOAT HUB

do
    local Players = game:GetService("Players")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local RunService = game:GetService("RunService")
    local LocalPlayer = Players.LocalPlayer

    local ShipSystem = {
        tornadoActive = false,
        currentShip = nil,
        antiSitWasActive = false,
        prevFling = false,
        tornadoSession = 0
    }

    local function spawnPirateShip()
        local character = LocalPlayer.Character
        if not character then return nil end

        local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
        if not humanoidRootPart then return nil end

        humanoidRootPart.CFrame = CFrame.new(1754, -2, 58)
        task.wait(0.5)

        local re = ReplicatedStorage:WaitForChild("RE")
        local carRemote = re:FindFirstChild("1Ca1r")
        if carRemote then
            carRemote:FireServer("PickingBoat", "PirateFree")
        end
        task.wait(1)

        local vehicles = workspace:WaitForChild("Vehicles", 5)
        if vehicles then
            for _ = 1, 15 do
                local ship = vehicles:FindFirstChild(LocalPlayer.Name .. "Car")
                if ship then return ship end
                task.wait(0.2)
            end
        end
        return nil
    end

    local function playShipAudio()
        local audioID = 9068077052
        local args = {workspace, audioID, 1}

        for i = 1, 5 do
            local re = ReplicatedStorage.RE:FindFirstChild("1Gu1nSound1s")
            if re then
                re:FireServer(unpack(args))
            end

            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                local sound = Instance.new("Sound")
                sound.SoundId = "rbxassetid://" .. audioID
                sound.Parent = LocalPlayer.Character.HumanoidRootPart
                sound:Play()

                task.wait(1.5)
                sound:Destroy()
            end
        end
    end

    local function startTornado()
        local character = LocalPlayer.Character
        local humanoid = character and character:FindFirstChildOfClass("Humanoid")
        local humanoidRootPart = character and character:FindFirstChild("HumanoidRootPart")
        if not character or not humanoid or not humanoidRootPart then return end

        _G.VehicleFlingActive = true
        pcall(function()
            humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
        end)

        ShipSystem.tornadoActive = true
        ShipSystem.currentShip = spawnPirateShip()
        if not ShipSystem.currentShip then
            _G.VehicleFlingActive = false
            ShipSystem.tornadoActive = false
            return
        end

        local seat = ShipSystem.currentShip:FindFirstChild("Body")
        seat = seat and seat:FindFirstChild("VehicleSeat")
        if not seat then
            seat = ShipSystem.currentShip:FindFirstChildWhichIsA("VehicleSeat", true) or ShipSystem.currentShip:FindFirstChildWhichIsA("Seat", true)
        end
        if not seat then
            _G.VehicleFlingActive = false
            ShipSystem.tornadoActive = false
            return
        end

        if not ShipSystem.currentShip.PrimaryPart then
            ShipSystem.currentShip.PrimaryPart = seat
        end

        local sitTimeout = tick() + 6
        repeat
            task.wait(0.1)
            if not ShipSystem.tornadoActive then return end
            humanoidRootPart.CFrame = seat.CFrame * CFrame.new(0, 1, 0)
        until humanoid.SeatPart == seat or tick() > sitTimeout

        if humanoid.SeatPart ~= seat then
            _G.VehicleFlingActive = false
            ShipSystem.tornadoActive = false
            return
        end

        task.spawn(playShipAudio)

        task.delay(4, function()
            if not ShipSystem.tornadoActive then return end
            if humanoid.SeatPart then
                humanoid.Sit = false
            end
            pcall(function() humanoid:ChangeState(Enum.HumanoidStateType.GettingUp) end)
            pcall(function() humanoid.Jump = true end)
            task.wait(0.05)
            pcall(function() humanoid.Jump = false end)
            if humanoidRootPart then
                humanoidRootPart.CFrame = CFrame.new(0, 5, 0)
            end
            _G.VehicleFlingActive = false
        end)

        task.spawn(function()
            local re = ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Player1sCa1r")
            while ShipSystem.tornadoActive and ShipSystem.currentShip and ShipSystem.currentShip.Parent do
                re:FireServer("Flip")
                task.wait(0.5)
            end
        end)

        local waypoints = {
            Vector3.new(-16, 0, -47),
            Vector3.new(-110, 0, -45),
            Vector3.new(16, 0, -55)
        }

        ShipSystem.currentShip:SetPrimaryPartCFrame(CFrame.new(waypoints[1]))

        local currentIndex = 1
        local nextIndex = 2
        local moveSpeed = 30
        local rotationSpeed = math.rad(720)
        local progress = 0
        local currentRotation = 0

        local connection
        connection = RunService.Heartbeat:Connect(function(dt)
            if not ShipSystem.tornadoActive or not ShipSystem.currentShip or not ShipSystem.currentShip.PrimaryPart or not ShipSystem.currentShip.Parent then
                connection:Disconnect()
                return
            end

            local startPos = waypoints[currentIndex]
            local endPos = waypoints[nextIndex]

            progress = progress + (moveSpeed * dt) / (startPos - endPos).Magnitude
            if progress >= 1 then
                progress = 0
                currentIndex = nextIndex
                nextIndex = (nextIndex % #waypoints) + 1
            end

            local newPos = startPos:lerp(endPos, progress)
            currentRotation = currentRotation + rotationSpeed * dt

            local cframe = CFrame.new(newPos) * CFrame.Angles(0, currentRotation, 0)
            ShipSystem.currentShip:SetPrimaryPartCFrame(cframe)
        end)
    end

    local function stopTornado()
        ShipSystem.tornadoActive = false
        _G.VehicleFlingActive = false

        task.spawn(function()
            local args = {"DeleteAllVehicles"}
            local re = ReplicatedStorage:WaitForChild("RE"):WaitForChild("1Ca1r")
            if re then
                re:FireServer(unpack(args))
            end
        end)
    end

    local tornadoLockOwner = false 
    local TornadoToggle = VehicleTab:AddToggle({
        Name = "Pirate Ship Tornado",
        Description = "Pulls Pirate Ship to the Square",
        Default = false
    })

    TornadoToggle:Callback(function(Value)
        if Value then
            if _G.TornadoLock then
                pcall(function() game:GetService("StarterGui"):SetCore("SendNotification", {Title="Pirate Ship Tornado", Text="Tornado already active! Turn off the other one first.", Duration=3}) end)
                TornadoToggle:SetValue(false)
                return
            end
            tornadoLockOwner = true
            _G.TornadoLock = true
            task.spawn(function()
                startTornado()
                task.wait(5)
                TornadoToggle:SetValue(false)
            end)
        else
            stopTornado()
            if tornadoLockOwner then
                tornadoLockOwner = false
                _G.TornadoLock = false
            end
        end
    end)
end

-- CANOE SPAM (SUPER SKYBOX EXACT PATTERN) [LOCKED: WORK PERFECTLY]

_G.CanoeSpamActive = false
local canoeOriginalPos = nil

VehicleTab:AddToggle({
    Name = "Canoe Spam",
    Description = "Spawns and scatters canoes at spawn area",
    Default = false,
    Callback = function(p163)
        getgenv().Toggle = p163
        _G.CanoeSpamActive = p163

        if p163 then
            local LocalPlayer4 = game:GetService("Players").LocalPlayer
            local v836 = LocalPlayer4.Character or LocalPlayer4.CharacterAdded:Wait()
            local HumanoidRootPart = v836:WaitForChild("HumanoidRootPart")
            local Humanoid = v836:FindFirstChildOfClass("Humanoid")
            local wc = workspace:WaitForChild("WorkspaceCom", 5)
            local buttonModel = wc and wc:WaitForChild("001_CanoeCloneButton", 5)
            local Button = buttonModel and buttonModel:WaitForChild("Button", 5)
            local ClickDetector = Button and Button:FindFirstChild("ClickDetector")
            local storage = wc and wc:WaitForChild("001_CanoeStorage", 5)

            if not Button or not ClickDetector or not storage then
                _G.CanoeSpamActive = false
                getgenv().Toggle = false
                pcall(function()
                    game:GetService("StarterGui"):SetCore("SendNotification", {
                        Title = "Canoe Spam",
                        Text = "Canoe button or storage not found!",
                        Duration = 3
                    })
                end)
                return
            end

            canoeOriginalPos = HumanoidRootPart.CFrame
            local sessionToken = tick()
            _G.BTR_CANOE_SESSION = sessionToken

            
            task.spawn(function()
                while getgenv().Toggle and _G.CanoeSpamActive and _G.BTR_CANOE_SESSION == sessionToken do
                    local char = LocalPlayer4.Character
                    local hrp = char and char:FindFirstChild("HumanoidRootPart")
                    local hum = char and char:FindFirstChildOfClass("Humanoid")
                    if not hrp or not hum then
                        task.wait(0.3)
                        continue
                    end

                    
                    hum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)

                    
                    hrp.CFrame = Button.CFrame
                    task.wait(0.08)

                    
                    pcall(function()
                        fireclickdetector(ClickDetector, 0)
                        fireclickdetector(ClickDetector)
                    end)

                    
                    pcall(function()
                        local pGui = LocalPlayer4:FindFirstChild("PlayerGui")
                        if pGui then
                            for _, g in ipairs(pGui:GetDescendants()) do
                                if (g:IsA("TextLabel") or g:IsA("TextButton")) and g.Visible then
                                    local t = g.Text:lower()
                                    if t:find("patienter") or t:find("please wait") or t:find("wait a moment") then
                                        local container = g:FindFirstAncestorOfClass("Frame") or g:FindFirstAncestorOfClass("ScreenGui") or g
                                        if container then container.Visible = false end
                                    end
                                end
                            end
                        end
                    end)

                    
                    local targetCanoe = nil
                    local t0 = tick()
                    while tick() - t0 < 1.2 and getgenv().Toggle and _G.CanoeSpamActive do
                        if storage then
                            local c = storage:FindFirstChild("Canoe")
                            if c and c:FindFirstChild("VehicleSeat") then
                                targetCanoe = c
                                break
                            end
                        end
                        task.wait(0.04)
                    end

                    
                    if targetCanoe and getgenv().Toggle and _G.CanoeSpamActive then
                        local seat = targetCanoe:FindFirstChild("VehicleSeat")
                        if seat then
                            local sitAttempts = 0
                            local maxSitAttempts = 35
                            repeat
                                sitAttempts = sitAttempts + 1
                                if char and char:FindFirstChild("HumanoidRootPart") then
                                    char.HumanoidRootPart.CFrame = seat.CFrame + Vector3.new(0, math.random(-0.5, 0.5), 0)
                                end
                                pcall(function() seat:Sit(hum) end)
                                task.wait(0.03)
                            until hum.Sit or not getgenv().Toggle or not _G.CanoeSpamActive or sitAttempts >= maxSitAttempts

                            
                            if hum.Sit and getgenv().Toggle and _G.CanoeSpamActive then
                                
                                if char and char:FindFirstChild("HumanoidRootPart") then
                                    char.HumanoidRootPart.CFrame = char.HumanoidRootPart.CFrame * CFrame.new(0, 0, -25)
                                    if seat and seat:IsA("BasePart") then
                                        seat.AssemblyLinearVelocity = char.HumanoidRootPart.CFrame.LookVector * 50
                                    end
                                end
                                task.wait(0.2)

                                
                                local dest = CFrame.new(math.random(-45, 45), 4.55, math.random(-45, 45))
                                if char and char:FindFirstChild("HumanoidRootPart") then
                                    char.HumanoidRootPart.CFrame = dest
                                end
                                task.wait(0.2)

                                
                                hum.Sit = false
                                pcall(function() targetCanoe.Name = "CanoeMolestaHavens" end)
                                task.wait(0.1)
                            end
                        end
                    end

                    task.wait(0.6)
                end
            end)
        else
            getgenv().Toggle = false
            _G.CanoeSpamActive = false
            _G.BTR_CANOE_SESSION = nil
            local lp = game:GetService("Players").LocalPlayer
            local char = lp and lp.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if hum then
                hum.Sit = false
                hum:ChangeState(Enum.HumanoidStateType.GettingUp)
                if _G.AntiSitValue then
                    hum:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
                end
            end
            if canoeOriginalPos and hrp then
                task.wait(0.1)
                pcall(function()
                    hrp.CFrame = canoeOriginalPos
                end)
                canoeOriginalPos = nil
            end
        end
    end
})

do
    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local LocalPlayer = Players.LocalPlayer
    local Workspace = game:GetService("Workspace")
    local StarterGui = game:GetService("StarterGui")

    
    _G.VehicleFlingActive = false
    _G.VehicleFlingCar = nil

    local function isOurFlingVehicle(item)
        
        if (_G.VehicleFlingActive or _G.TornadoLock) and item.Name == LocalPlayer.Name .. "Car" then
            return true
        end
        
        if LocalPlayer.Character then
            for _, seat in ipairs(item:GetDescendants()) do
                if (seat:IsA("VehicleSeat") or seat:IsA("Seat")) and seat.Occupant and seat.Occupant.Parent == LocalPlayer.Character then
                    return true
                end
            end
        end
        return false
    end

    
    do
        local antiSitConnections = {}
        local disabledSeats = {}
        local isAntiSitActive = false

        local function disableSingleSeat(seat)
            if seat and (seat:IsA("Seat") or seat:IsA("VehicleSeat")) then
                disabledSeats[seat] = true
                pcall(function()
                    seat.Disabled = true
                    seat.CanTouch = false
                end)
            end
        end

        local function restoreAllSeats()
            for seat in pairs(disabledSeats) do
                if seat and seat.Parent then
                    pcall(function()
                        seat.Disabled = false
                        seat.CanTouch = true
                    end)
                end
            end
            disabledSeats = {}
        end

        local function resetSitting()
            local player = game.Players.LocalPlayer
            for _, connection in ipairs(antiSitConnections) do
                if connection and connection.Connected then
                    connection:Disconnect()
                end
            end
            antiSitConnections = {}
            restoreAllSeats()
            local humanoid = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
            if humanoid then
                humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
            end
            isAntiSitActive = false
        end

        local function activateAntiSit()
            local player = game.Players.LocalPlayer
            resetSitting()
            isAntiSitActive = true

            
            local vFolder = workspace:FindFirstChild("Vehicles")
            if vFolder then
                for _, car in ipairs(vFolder:GetChildren()) do
                    for _, s in ipairs(car:GetDescendants()) do
                        disableSingleSeat(s)
                    end
                end
                local vAddedConn = vFolder.DescendantAdded:Connect(function(inst)
                    if _G.AntiSitValue and not _G.VehicleFlingActive and not _G.CanoeSpamActive then
                        disableSingleSeat(inst)
                    end
                end)
                table.insert(antiSitConnections, vAddedConn)
            end

            local function preventSitting(humanoid, character)
                if humanoid then
                    humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)

                    local sitConnection = humanoid.StateChanged:Connect(function(_, newState)
                        if newState == Enum.HumanoidStateType.Seated and _G.AntiSitValue and not _G.VehicleFlingActive and not _G.CanoeSpamActive then
                            humanoid.Sit = false
                            humanoid.PlatformStand = false
                            humanoid:ChangeState(Enum.HumanoidStateType.Running)
                        end
                    end)
                    table.insert(antiSitConnections, sitConnection)
                end
            end

            local function onCharacterAdded(character)
                local humanoid = character:WaitForChild("Humanoid")
                preventSitting(humanoid, character)
            end

            local characterAddedConnection = player.CharacterAdded:Connect(onCharacterAdded)
            table.insert(antiSitConnections, characterAddedConnection)
            if player.Character then
                onCharacterAdded(player.Character)
            end

            local heartbeatConnection = RunService.Heartbeat:Connect(function()
                local charObj = player.Character
                local humanoid = charObj and charObj:FindFirstChildOfClass("Humanoid")
                if humanoid then
                    if _G.AntiSitValue and not _G.VehicleFlingActive and not _G.CanoeSpamActive then
                        humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
                        if humanoid.Sit or humanoid.SeatPart then
                            local sp = humanoid.SeatPart
                            if sp then
                                disableSingleSeat(sp)
                            end
                            humanoid.Sit = false
                            humanoid.PlatformStand = false
                            humanoid:ChangeState(Enum.HumanoidStateType.Running)
                        end
                    elseif _G.VehicleFlingActive or _G.CanoeSpamActive then
                        humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
                    end
                end
            end)
            table.insert(antiSitConnections, heartbeatConnection)
        end

        local function setAntiSitCallback(Value)
            _G.AntiSitValue = Value
            if Value then
                activateAntiSit()
            else
                resetSitting()
            end
        end
        _G.BTR_PROT_CALLBACKS.AntiSit = setAntiSitCallback

        _G.AntiSitToggle = ProtectionTab:AddToggle({
            Name = "Anti-Sit",
            Description = "No sitting (auto-paused during your own Vehicle Fling)",
            Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.AntiSit ~= false),
            Callback = function(Value)
                _G.setBTRProtection("AntiSit", Value, "ProtectionTab")
            end
        })
        _G.BTR_PROT_TOGGLES.AntiSit = _G.AntiSitToggle

        _G.AntiSitValue = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.AntiSit ~= false)

        -- [LOCKED Keito] Force OFF/ON de l'anti-sit SANS passer par Toggle:Set()
        
        
        _G.AntiSitForceOff = function()
            _G.AntiSitValue = false
            resetSitting()  
            
            if _G.AntiSitToggle then
                pcall(function() _G.AntiSitToggle:Set(false) end)
            end
        end
        _G.AntiSitForceOn = function()
            _G.AntiSitValue = true
            activateAntiSit()
            
            if _G.AntiSitToggle then
                pcall(function() _G.AntiSitToggle:Set(true) end)
            end
        end
    end

    
    
    local antiFlingStates = {
        Vehicles = false,
        Canoes = false,
        Jets = false,
        Helis = false,
        Balls = false,
        Doors = false
    }

    local function destroyForeignIn(folder)
        if not folder then return end
        local lpName = LocalPlayer and LocalPlayer.Name or ""
        for _, item in ipairs(folder:GetChildren()) do
            
            if isOurFlingVehicle(item) then continue end
            if item.Name == ("Soccer" .. lpName) then continue end
            if item.Name:find(lpName) then continue end
            if _G.CanoeSpamActive and (item.Name == "Canoe" or item.Name:find("Canoe")) then continue end

            local occupiedByUs = false
            if LocalPlayer.Character then
                for _, seat in ipairs(item:GetDescendants()) do
                    if (seat:IsA("VehicleSeat") or seat:IsA("Seat")) and seat.Occupant and seat.Occupant.Parent == LocalPlayer.Character then
                        occupiedByUs = true
                        break
                    end
                end
            end
            if not occupiedByUs then
                pcall(function()
                    if item:IsA("BasePart") then
                        item.CanCollide = false
                        item.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                        item.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                    end
                    for _, p in ipairs(item:GetDescendants()) do
                        if p:IsA("BasePart") then
                            p.CanCollide = false
                            p.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                        end
                    end
                    item:Destroy()
                end)
            end
        end
    end

    
    local function hideDoors()
        for _, v in ipairs(Workspace:GetDescendants()) do
            if v:IsA("BasePart") and v.Name:lower():find("door") then
                v.Transparency = 1
                v.CanCollide = false
            end
        end
    end

    
    local lastVehFlingCheck = 0
    local vehSteppedConn = RunService.Stepped:Connect(function()
        if not antiFlingStates.Vehicles then return end
        local now = tick()
        if now - lastVehFlingCheck < 0.1 then return end
        lastVehFlingCheck = now

        pcall(function()
            local vehicles = Workspace:FindFirstChild("Vehicles")
            if not vehicles then return end
            for _, item in ipairs(vehicles:GetChildren()) do
                if not isOurFlingVehicle(item) then
                    local root = item.PrimaryPart or item:FindFirstChildWhichIsA("BasePart")
                    if root and (root.AssemblyLinearVelocity.Magnitude > 80 or root.Velocity.Magnitude > 80) then
                        for _, veh in ipairs(item:GetDescendants()) do
                            if veh:IsA("BasePart") then
                                veh.CanCollide = false
                                veh.AssemblyLinearVelocity = Vector3.zero
                                veh.AssemblyAngularVelocity = Vector3.zero
                                veh.Velocity = Vector3.zero
                                veh.RotVelocity = Vector3.zero
                            end
                        end
                    end
                end
            end
        end)
    end)
    table.insert(_G.BTR_CONNECTIONS, vehSteppedConn)

    local function antiFlingTick()
        local anyActive = antiFlingStates.Canoes or antiFlingStates.Jets
            or antiFlingStates.Helis or antiFlingStates.Balls
        if not anyActive then return end

        if antiFlingStates.Canoes and not _G.CanoeSpamActive then
            local wc = Workspace:FindFirstChild("WorkspaceCom")
            local canoes = wc and wc:FindFirstChild("001_CanoeStorage")
            if canoes then destroyForeignIn(canoes) end
        end
        if antiFlingStates.Jets then
            local wc = Workspace:FindFirstChild("WorkspaceCom")
            local airport = wc and wc:FindFirstChild("001_Airport")
            local hangar = airport and airport:FindFirstChild("AirportHanger")
            local jetStorage = hangar and hangar:FindFirstChild("001_JetStorage")
            local jets = jetStorage and jetStorage:FindFirstChild("JetAirport")
            if jets then destroyForeignIn(jets) end
        end
        if antiFlingStates.Helis then
            local wc = Workspace:FindFirstChild("WorkspaceCom")
            local helis = wc and wc:FindFirstChild("001_HeliStorage")
            local heli = helis and helis:FindFirstChild("PoliceStationHeli")
            if heli then destroyForeignIn(heli) end
        end
        if antiFlingStates.Balls then
            local wc = Workspace:FindFirstChild("WorkspaceCom")
            local balls = wc and wc:FindFirstChild("001_SoccerBalls")
            if balls then destroyForeignIn(balls) end
        end
    end

    task.spawn(function()
        local currentSession = _G.BTR_SESSION_ID
        local wc = Workspace:WaitForChild("WorkspaceCom", 10)
        local ballsFolder = wc and (wc:FindFirstChild("001_SoccerBalls") or wc:WaitForChild("001_SoccerBalls", 5))
        if ballsFolder then
            local ballConn = ballsFolder.ChildAdded:Connect(function(child)
                if antiFlingStates.Balls then
                    local lpName = LocalPlayer and LocalPlayer.Name or ""
                    if child.Name ~= ("Soccer" .. lpName) and not child.Name:find(lpName) then
                        pcall(function()
                            if child:IsA("BasePart") then
                                child.CanCollide = false
                                child.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                            end
                            for _, p in ipairs(child:GetDescendants()) do
                                if p:IsA("BasePart") then p.CanCollide = false end
                            end
                            child:Destroy()
                        end)
                    end
                end
            end)
            table.insert(_G.BTR_CONNECTIONS, ballConn)
        end

        while currentSession == _G.BTR_SESSION_ID do
            antiFlingTick()
            task.wait(0.25)
        end
    end)

    
    _G.BTR_PROT_TOGGLES = _G.BTR_PROT_TOGGLES or {}
    _G.BTR_PROT_CALLBACKS = _G.BTR_PROT_CALLBACKS or {}

    local function setVehicles(state)
        antiFlingStates.Vehicles = state
    end
    local function setCanoes(state) antiFlingStates.Canoes = state end
    local function setJets(state) antiFlingStates.Jets = state end
    local function setHelis(state) antiFlingStates.Helis = state end
    local function setBalls(state) antiFlingStates.Balls = state end
    local function setDoors(state)
        antiFlingStates.Doors = state
        if state then hideDoors() end
    end

    _G.BTR_PROT_CALLBACKS.Vehicles = setVehicles
    _G.BTR_PROT_CALLBACKS.Canoes = setCanoes
    _G.BTR_PROT_CALLBACKS.Jets = setJets
    _G.BTR_PROT_CALLBACKS.Helis = setHelis
    _G.BTR_PROT_CALLBACKS.Balls = setBalls
    _G.BTR_PROT_CALLBACKS.Doors = setDoors

    _G.BTR_PROT_TOGGLES.Vehicles = ProtectionTab:AddToggle({
        Name = "Anti Fling Vehicles",
        Description = "Neutralizes fast moving vehicles to prevent flings",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.Vehicles == true) or false,
        Callback = function(state) _G.setBTRProtection("Vehicles", state, "ProtectionTab") end
    })
    _G.BTR_PROT_TOGGLES.Canoes = ProtectionTab:AddToggle({
        Name = "Anti Canoe Fling",
        Description = "Destroys foreign canoes",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.Canoes == true) or false,
        Callback = function(state) _G.setBTRProtection("Canoes", state, "ProtectionTab") end
    })
    _G.BTR_PROT_TOGGLES.Jets = ProtectionTab:AddToggle({
        Name = "Anti Fling Jets",
        Description = "Destroys foreign jets",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.Jets == true) or false,
        Callback = function(state) _G.setBTRProtection("Jets", state, "ProtectionTab") end
    })
    _G.BTR_PROT_TOGGLES.Helis = ProtectionTab:AddToggle({
        Name = "Anti Fling Helicopters",
        Description = "Destroys foreign helicopters",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.Helis == true) or false,
        Callback = function(state) _G.setBTRProtection("Helis", state, "ProtectionTab") end
    })
    _G.BTR_PROT_TOGGLES.Balls = ProtectionTab:AddToggle({
        Name = "Anti Fling Ball",
        Description = "Destroys soccer balls",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.Balls == true) or false,
        Callback = function(state) _G.setBTRProtection("Balls", state, "ProtectionTab") end
    })
    _G.BTR_PROT_TOGGLES.Doors = ProtectionTab:AddToggle({
        Name = "Anti Fling Doors",
        Description = "Hides all doors (one-time, no lag)",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.Doors == true) or false,
        Callback = function(state) _G.setBTRProtection("Doors", state, "ProtectionTab") end
    })

    
    local _antiDumpRunning = false
    local function setAntiDump(state)
        _antiDumpRunning = state
        if state then
            task.spawn(function()
                while _antiDumpRunning do
                    for _, plr in ipairs(Players:GetPlayers()) do
                        if plr ~= LocalPlayer then
                            local char = plr.Character
                            local backpack = plr:FindFirstChildOfClass("Backpack")
                            if char then
                                for _, tool in ipairs(char:GetChildren()) do
                                    if tool:IsA("Tool") and tool.Name:lower():find("dump") then
                                        pcall(function() tool:Destroy() end)
                                    end
                                end
                            end
                            if backpack then
                                for _, tool in ipairs(backpack:GetChildren()) do
                                    if tool:IsA("Tool") and tool.Name:lower():find("dump") then
                                        pcall(function() tool:Destroy() end)
                                    end
                                end
                            end
                        end
                    end
                    task.wait(0.1)
                end
            end)
        end
    end
    _G.BTR_PROT_CALLBACKS.Dump = setAntiDump
    _G.BTR_PROT_TOGGLES.Dump = ProtectionTab:AddToggle({
        Name = "Anti Dump Tool",
        Description = "Destroys spawned dump tools from other players",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.Dump == true) or false,
        Callback = function(state) _G.setBTRProtection("Dump", state, "ProtectionTab") end
    })

    
    local _antiBangActive = false

    ProtectionTab:AddToggle({
        Name = "Anti-Bang (Total)",
        Description = "Detects any bang (CFrame loop, TP, or animation) and stops the attacker immediately",
        Default = false,
        Callback = function(state)
            _antiBangActive = state
            if state then
                task.spawn(function()
                    local lastPositions = {}
                    local suspiciousTicks = {}

                    local function counterAttack(attacker)
                        
                        local aChar = attacker and attacker.Character
                        local aHrp = aChar and aChar:FindFirstChild("HumanoidRootPart")
                        if aHrp then
                            pcall(function() aHrp.CFrame = CFrame.new(0, -90000, 0) end)
                            pcall(function() aHrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0) end)
                        end
                        
                        local myChar = LocalPlayer.Character
                        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                        if myHrp then
                            pcall(function() myHrp.CFrame = myHrp.CFrame * CFrame.new(0, 8, -5) end)
                            task.wait(0.05)
                            pcall(function() myHrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0) end)
                        end
                        pcall(function()
                            local myHum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChildOfClass("Humanoid")
                            if myHum then myHum.Sit = false end
                        end)
                        StarterGui:SetCore("SendNotification", {
                            Title = "Anti-Bang",
                            Text = "Bang detected from " .. (attacker and attacker.Name or "Player") .. "! Attack neutralized.",
                            Duration = 3
                        })
                    end

                    while _antiBangActive do
                        local myChar = LocalPlayer.Character
                        local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                        if myHrp then
                            for _, plr in ipairs(Players:GetPlayers()) do
                                if plr ~= LocalPlayer and plr.Character then
                                    local aChar = plr.Character
                                    local aHum = aChar:FindFirstChildOfClass("Humanoid")
                                    local aHrp = aChar:FindFirstChild("HumanoidRootPart")
                                    if aHum and aHrp then
                                        local dist = (aHrp.Position - myHrp.Position).Magnitude
                                        local last = lastPositions[plr]
                                        local tpJump = false
                                        if last then
                                            tpJump = (aHrp.Position - last).Magnitude > 25
                                        end
                                        lastPositions[plr] = aHrp.Position

                                        
                                        local animPlaying = #aHum:GetPlayingAnimationTracks() > 0

                                        
                                        local toAttacker = (aHrp.Position - myHrp.Position).Unit
                                        local myLook = myHrp.CFrame.LookVector
                                        local isBehind = myLook:Dot(toAttacker) < -0.4

                                        
                                        if dist < 3.5 and (isBehind or animPlaying) then
                                            suspiciousTicks[plr] = (suspiciousTicks[plr] or 0) + 1
                                            if suspiciousTicks[plr] >= 2 then
                                                counterAttack(plr)
                                                suspiciousTicks[plr] = 0
                                                break
                                            end
                                        
                                        elseif dist < 8 and animPlaying and tpJump then
                                            counterAttack(plr)
                                            suspiciousTicks[plr] = 0
                                            break
                                        else
                                            suspiciousTicks[plr] = 0
                                        end
                                    end
                                end
                            end

                            
                            local vel = myHrp.AssemblyLinearVelocity
                            if vel.Magnitude > 4000 then
                                pcall(function() myHrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0) end)
                            end
                            if myHrp.Position.Y < -100 then
                                pcall(function() myHrp.CFrame = CFrame.new(myHrp.Position.X, 200, myHrp.Position.Z) end)
                            end
                        end
                        task.wait(0.1)
                    end
                    lastPositions = {}
                    suspiciousTicks = {}
                end)
            end
        end
    })

    
    local function setAntiAfk(state)
        getgenv().AntiAfk = state
        if state then
            local VirtualUser = game:GetService("VirtualUser")
            game:GetService("Players").LocalPlayer.Idled:Connect(function()
                if getgenv().AntiAfk then
                    VirtualUser:CaptureController()
                    VirtualUser:ClickButton2(Vector2.new())
                end
            end)
        end
    end
    _G.BTR_PROT_CALLBACKS.AntiAfk = setAntiAfk
    _G.BTR_PROT_TOGGLES.AntiAfk = ProtectionTab:AddToggle({
        Name = "Anti-AFK",
        Description = "Prevents Roblox from kicking you for inactivity (20m)",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.AntiAfk == true) or false,
        Callback = function(state) _G.setBTRProtection("AntiAfk", state, "ProtectionTab") end
    })

    
    ProtectionTab:AddSection({"> Anti-Copy Avatar"})

    local _antiCopyActive = false
    local _notifiedCopyPlayers = {}
    local _baselinePlayerAssets = {}
    local _isRejoiningServer = false
    local _lastMyIds = nil
    local _lastMyCount = 0

    local function loadSlotOutfit(slotNum)
        pcall(function()
            local RS = game:GetService("ReplicatedStorage")
            local remotes = RS:FindFirstChild("Remotes")
            local loadOutfit = remotes and remotes:FindFirstChild("LoadOutfit")
            if loadOutfit then
                loadOutfit:InvokeServer(tonumber(slotNum) or 1)
            end
        end)
    end

    local function getPlayerAssetIds(char)
        local ids = {}
        if not char then return ids end
        for _, c in ipairs(char:GetChildren()) do
            if c:IsA("Accessory") then
                local handle = c:FindFirstChild("Handle")
                if handle then
                    local sm = handle:FindFirstChildOfClass("SpecialMesh") or handle:FindFirstChildOfClass("FileMesh")
                    local meshId = sm and sm.MeshId or (handle:IsA("MeshPart") and handle.MeshId or "")
                    local num = meshId:match("%d+")
                    if num and #num > 4 then
                        ids["acc_" .. num] = true
                    elseif c.Name ~= "Accessory" and #c.Name > 3 then
                        ids["acc_name_" .. c.Name:lower()] = true
                    end
                end
            elseif c:IsA("Shirt") and c.ShirtTemplate ~= "" then
                local num = c.ShirtTemplate:match("%d+")
                if num and #num > 4 then ids["shirt_" .. num] = true end
            elseif c:IsA("Pants") and c.PantsTemplate ~= "" then
                local num = c.PantsTemplate:match("%d+")
                if num and #num > 4 then ids["pants_" .. num] = true end
            elseif c:IsA("ShirtGraphic") and c.Graphic ~= "" then
                local num = c.Graphic:match("%d+")
                if num and #num > 4 then ids["tshirt_" .. num] = true end
            end
        end
        return ids
    end

    local function syncBaseline()
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr ~= LocalPlayer and plr.Character then
                _baselinePlayerAssets[plr.UserId] = getPlayerAssetIds(plr.Character)
            end
        end
    end
    _G.BTR_SYNC_ANTI_COPY_BASELINE = syncBaseline

    local function setAntiCopy(state)
        _antiCopyActive = state
        _notifiedCopyPlayers = {}
        _baselinePlayerAssets = {}
        _lastMyIds = nil
        _lastMyCount = 0
        if state then
            task.spawn(function()
                task.wait(4)
                syncBaseline()

                while _antiCopyActive and not _isRejoiningServer do
                    if _G.BTR_IS_COPYING_AVATAR or (os.clock() - (_G.BTR_LAST_COPY_TIME or 0) < 3) then
                        task.wait(1)
                        continue
                    end

                    local lpChar = LocalPlayer.Character
                    if lpChar then
                        local myIds = getPlayerAssetIds(lpChar)
                        local myCount = 0
                        for _ in pairs(myIds) do myCount = myCount + 1 end

                        if myCount >= 3 then
                            local myOutfitChanged = false
                            if _lastMyIds then
                                local matchCount = 0
                                for id in pairs(myIds) do
                                    if _lastMyIds[id] then matchCount = matchCount + 1 end
                                end
                                if myCount ~= _lastMyCount or matchCount < math.min(myCount, 3) then
                                    myOutfitChanged = true
                                end
                            end
                            _lastMyIds = myIds
                            _lastMyCount = myCount

                            if myOutfitChanged then
                                syncBaseline()
                                task.wait(1)
                                continue
                            end

                            for _, plr in ipairs(Players:GetPlayers()) do
                                if plr ~= LocalPlayer and plr.Character and not _notifiedCopyPlayers[plr.UserId] then
                                    if _G.BTR_COPIED_PLAYERS and _G.BTR_COPIED_PLAYERS[plr.UserId] then
                                        continue
                                    end

                                    local currentIds = getPlayerAssetIds(plr.Character)
                                    local currentCount = 0
                                    for _ in pairs(currentIds) do currentCount = currentCount + 1 end

                                    local baseline = _baselinePlayerAssets[plr.UserId]
                                    local baselineCount = 0
                                    if baseline then
                                        for _ in pairs(baseline) do baselineCount = baselineCount + 1 end
                                    end

                                    if not baseline or baselineCount == 0 then
                                        _baselinePlayerAssets[plr.UserId] = currentIds
                                    else
                                        local newlyEquippedMatches = 0
                                        local totalMatches = 0
                                        for id in pairs(currentIds) do
                                            if myIds[id] then
                                                totalMatches = totalMatches + 1
                                                if not baseline[id] then
                                                    newlyEquippedMatches = newlyEquippedMatches + 1
                                                end
                                            end
                                        end

                                        if newlyEquippedMatches >= 3 then
                                            _notifiedCopyPlayers[plr.UserId] = true
                                            _baselinePlayerAssets[plr.UserId] = currentIds

                                            StarterGui:SetCore("SendNotification", {
                                                Title = "Anti-Copy Alert!",
                                                Text = plr.Name .. " is copying your avatar!",
                                                Duration = 4
                                            })

                                            local reaction = _G.BTR_ANTI_COPY_REACTION or "Load Saved Slot Outfit"
                                            local slot = _G.BTR_OUTFIT_SLOT or 1

                                            if reaction == "Load Saved Slot Outfit" then
                                                loadSlotOutfit(slot)
                                                StarterGui:SetCore("SendNotification", {
                                                    Title = "Anti-Copy",
                                                    Text = "Loaded Slot " .. tostring(slot) .. " Outfit instantly!",
                                                    Duration = 3
                                                })
                                            elseif reaction == "Auto Rejoin" then
                                                _isRejoiningServer = true
                                                StarterGui:SetCore("SendNotification", {
                                                    Title = "Anti-Copy",
                                                    Text = "Copycat detected! Rejoining server...",
                                                    Duration = 3
                                                })
                                                task.wait(0.3)
                                                pcall(function()
                                                    local TS = game:GetService("TeleportService")
                                                    local Players = game:GetService("Players")
                                                    if #Players:GetPlayers() <= 1 then
                                                        Players.LocalPlayer:Kick("\nRejoining...")
                                                        task.wait()
                                                        TS:Teleport(game.PlaceId, Players.LocalPlayer)
                                                    else
                                                        TS:TeleportToPlaceInstance(game.PlaceId, game.JobId, Players.LocalPlayer)
                                                    end
                                                end)
                                                break
                                            end
                                        end
                                    end
                                end
                            end
                        end
                    end
                    task.wait(1)
                end
            end)
        end
    end

    _G.BTR_PROT_CALLBACKS.AntiCopy = setAntiCopy
    _G.BTR_PROT_TOGGLES.AntiCopy = ProtectionTab:AddToggle({
        Name = "Anti-Copy Avatar",
        Description = "Detects players copying 3+ of your items and reacts immediately",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.AntiCopy == true) or false,
        Callback = function(state) _G.setBTRProtection("AntiCopy", state, "ProtectionTab") end
    })

    local slotStrDefault = "Slot " .. tostring(_G.BTR_OUTFIT_SLOT or 1)

    ProtectionTab:AddDropdown({
        Name = "Copy Reaction Mode",
        Description = "Action to take when a copycat is detected",
        Options = {"Load Saved Slot Outfit", "Auto Rejoin", "Notification Only"},
        Default = _G.BTR_ANTI_COPY_REACTION or "Load Saved Slot Outfit",
        Callback = function(mode)
            _G.BTR_ANTI_COPY_REACTION = mode
            _G.saveBTRConfig()
        end
    })

    ProtectionTab:AddDropdown({
        Name = "Bait Outfit Slot",
        Description = "Brookhaven wardrobe slot to load if copied (1, 2 or 3)",
        Options = {"Slot 1", "Slot 2", "Slot 3"},
        Default = slotStrDefault,
        Callback = function(slot)
            if slot == "Slot 1" then _G.BTR_OUTFIT_SLOT = 1
            elseif slot == "Slot 2" then _G.BTR_OUTFIT_SLOT = 2
            elseif slot == "Slot 3" then _G.BTR_OUTFIT_SLOT = 3
            end
            _G.saveBTRConfig()
        end
    })

    ProtectionTab:AddButton({
        Name = "Load Selected Slot Now",
        Description = "Manually loads your chosen Brookhaven outfit slot",
        Callback = function()
            local slot = _G.BTR_OUTFIT_SLOT or 1
            loadSlotOutfit(slot)
            StarterGui:SetCore("SendNotification", {Title="Outfit Loaded", Text="Slot " .. tostring(slot) .. " loaded!", Duration=2})
        end
    })
end

--  [PROTECTION PROPS v3] Unlock House Props - REMOTE ENGINE [LOCKED: WORK PERFECTLY]

do
    local PropSystem = {
        active = false,
        running = false,
    }

    local lotAddedConn = nil
    local lotRemotes = {}

    
    local function getLotRemotes(lot)
        if not lot or not lot:IsA("Instance") then return nil, nil end
        local cached = lotRemotes[lot]
        if cached and cached.set and cached.set.Parent then
            return cached.set, cached.is
        end
        local setBlocked = lot:FindFirstChild("Permissions:SetPropPlacementBlockedForOthers", true)
        local isBlocked = lot:FindFirstChild("Permissions:IsPropPlacementBlockedForOthers", true)
        if setBlocked and setBlocked:IsA("RemoteFunction") then
            lotRemotes[lot] = { set = setBlocked, is = isBlocked }
            return setBlocked, isBlocked
        end
        return nil, nil
    end

    local function unlockLotRemote(lot)
        local setBlocked, isBlocked = getLotRemotes(lot)
        if setBlocked then
            task.spawn(function()
                local etat = nil
                if isBlocked and isBlocked:IsA("RemoteFunction") then
                    pcall(function() etat = isBlocked:InvokeServer() end)
                end
                if etat == true or etat == nil then
                    pcall(function() setBlocked:InvokeServer(false) end)
                end
            end)
        end
    end

    local function sweepRemoteLots()
        local lots = workspace:FindFirstChild("001_Lots")
        if lots then
            for _, lot in pairs(lots:GetChildren()) do
                if not PropSystem.active then break end
                unlockLotRemote(lot)
                task.wait(0.05)
            end
        end
    end

    
    local function propProtectLoop()
        PropSystem.running = true
        while PropSystem.active do
            sweepRemoteLots()
            task.wait(12)
        end
        PropSystem.running = false
    end

    local function ensurePropLoop()
        if PropSystem.active and not PropSystem.running then
            task.spawn(function()
                pcall(function() propProtectLoop() end)
            end)
        end
    end

    local function setupListener()
        if lotAddedConn then
            pcall(function() lotAddedConn:Disconnect() end)
            lotAddedConn = nil
        end

        local lots = workspace:FindFirstChild("001_Lots")
        if not lots then return end

        lotAddedConn = lots.DescendantAdded:Connect(function(descendant)
            if not PropSystem.active then return end
            if descendant.Name == "Permissions:SetPropPlacementBlockedForOthers" and descendant:IsA("RemoteFunction") then
                local lot = descendant:FindFirstAncestorOfClass("Model") or descendant.Parent
                if lot then
                    task.defer(function()
                        unlockLotRemote(lot)
                    end)
                end
            end
        end)
    end

    local function setPropPlacement(state)
        if state then
            if PropSystem.active then return end
            PropSystem.active = true

            setupListener()
            ensurePropLoop()
            task.spawn(function()
                pcall(function() sweepRemoteLots() end)
            end)
        else
            PropSystem.active = false
            if lotAddedConn then
                pcall(function() lotAddedConn:Disconnect() end)
                lotAddedConn = nil
            end
        end
    end
    _G.BTR_PROT_CALLBACKS.PropPlacement = setPropPlacement

    _G.setBTRPropBypassMode = function(newMode)
        _G.BTR_PROP_BYPASS_MODE = "Remote"
        if _G.saveBTRConfig then _G.saveBTRConfig() end
    end

    
    _G.BTR_PROT_TOGGLES.PropPlacement = ProtectionTab:AddToggle({
        Name = "Unlock House Props (All)",
        Description = "Global protection: automatically unblocks prop placement on EVERY house via server Remote. ON by default.",
        Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.PropPlacement == true) or false,
        Callback = function(state) _G.setBTRProtection("PropPlacement", state, "ProtectionTab") end
    })
end

--  FIN PROTECTION PROPS v3 [LOCKED: WORK PERFECTLY]

local invincibleShieldActive = false
local invincibleCharConn = nil
local invincibleHeartbeat = nil

local function applyUndeadShield(char)
    if not char then return end
    local hum = char:WaitForChild("Humanoid", 4)
    local hrp = char:WaitForChild("HumanoidRootPart", 4)
    if not hum or not hrp then return end

    hum:SetStateEnabled(Enum.HumanoidStateType.Dead, false)
    pcall(function() char.BreakJointsOnDeath = false end)
    pcall(function() hum.Health = 0 end)

    pcall(function()
        hrp.CustomPhysicalProperties = PhysicalProperties.new(100, 0, 0, 0, 0)
    end)
end

local function restoreNormalState()
    local char = game:GetService("Players").LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if hum then
        hum:SetStateEnabled(Enum.HumanoidStateType.Dead, true)
        pcall(function() hum.Health = 100 end)
    end
    if hrp then
        pcall(function()
            hrp.CustomPhysicalProperties = nil
        end)
    end
end

local function setInvincibleShieldCallback(state)
    invincibleShieldActive = (state == true)
    _G.InvincibleShieldActive = invincibleShieldActive

    if invincibleCharConn then
        invincibleCharConn:Disconnect()
        invincibleCharConn = nil
    end
    if invincibleHeartbeat then
        invincibleHeartbeat:Disconnect()
        invincibleHeartbeat = nil
    end

    local lp = game:GetService("Players").LocalPlayer

    if invincibleShieldActive then
        applyUndeadShield(lp.Character)

        invincibleCharConn = lp.CharacterAdded:Connect(function(newChar)
            task.wait(0.3)
            if invincibleShieldActive then
                applyUndeadShield(newChar)
            end
        end)

        invincibleHeartbeat = game:GetService("RunService").Heartbeat:Connect(function()
            local char = lp.Character
            local hum = char and char:FindFirstChildOfClass("Humanoid")
            if not hum then return end

            
            if hum.Health > 0 then
                hum:SetStateEnabled(Enum.HumanoidStateType.Dead, false)
                pcall(function() char.BreakJointsOnDeath = false end)
                pcall(function() hum.Health = 0 end)
            end
        end)

        if _G.BTR_ALL_LOADED then
            pcall(function()
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Invincible Shield",
                    Text = "Exploit Immunity ACTIVE! Health = 0 (Undead). Hubs will refuse to attack you.",
                    Duration = 4
                })
            end)
        end
    else
        restoreNormalState()
        if _G.BTR_ALL_LOADED then
            pcall(function()
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Invincible Shield",
                    Text = "Exploit Immunity disabled. Health restored to normal.",
                    Duration = 3
                })
            end)
        end
    end
end

_G.BTR_PROT_CALLBACKS.InvincibleShield = setInvincibleShieldCallback

ProtectionTab:AddSection({
    "> Invincible Exploit Protection"
})

_G.BTR_PROT_TOGGLES.InvincibleShield = ProtectionTab:AddToggle({
    Name = "Invincible Hacker Shield",
    Description = "Permanently neutralizes Dark Hub, Bus Fling, Boat Fling, & SkidFling using Undead Godmode (Health = 0). Auto-reactivates on respawn.",
    Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.InvincibleShield == true) or false,
    Callback = function(state)
        _G.setBTRProtection("InvincibleShield", state, "ProtectionTab")
    end
})

SettingsTab:AddSection({
    "> Hub Themes & Wallpapers"
})

local wallpaperDropdown
wallpaperDropdown = SettingsTab:AddDropdown({
    Name = "Hub Wallpaper",
    Description = "Choose the background wallpaper texture",
    Options = {"Default", "Wallpaper 1", "Wallpaper 2", "Wallpaper 3", "Wallpaper 4", "Custom ID", "None"},
    Default = _G.BTR_SELECTED_WALLPAPER or "Wallpaper 1",
    Callback = function(choice)
        _G.BTR_SELECTED_WALLPAPER = choice
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_SetWallpaper then
            _G.BTR_SetWallpaper(choice)
        end
        if _G.BTR_ALL_LOADED then
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Wallpaper",
                Text = "Applied: " .. tostring(choice),
                Duration = 2
            })
        end
    end
})

SettingsTab:AddTextBox({
    Name = "Custom Wallpaper Asset ID",
    Description = "Enter any custom Roblox Image ID",
    Default = _G.BTR_CUSTOM_WALLPAPER_ID or "",
    PlaceholderText = "Paste Image ID (e.g. 8942361465)...",
    ClearText = false,
    Callback = function(val)
        local cleanId = tostring(val or ""):gsub("%D", "")
        if cleanId:len() > 3 then
            _G.BTR_CUSTOM_WALLPAPER_ID = cleanId
            _G.BTR_SELECTED_WALLPAPER = "Custom ID"
            _G.BTR_CUSTOM_WALLPAPERS_HISTORY = _G.BTR_CUSTOM_WALLPAPERS_HISTORY or {}
            local exists = false
            for _, id in ipairs(_G.BTR_CUSTOM_WALLPAPERS_HISTORY) do
                if id == cleanId then exists = true break end
            end
            if not exists then
                table.insert(_G.BTR_CUSTOM_WALLPAPERS_HISTORY, 1, cleanId)
                if #_G.BTR_CUSTOM_WALLPAPERS_HISTORY > 10 then
                    table.remove(_G.BTR_CUSTOM_WALLPAPERS_HISTORY)
                end
            end
            if _G.saveBTRConfig then
                _G.saveBTRConfig()
            end
            if _G.BTR_SetWallpaper then
                _G.BTR_SetWallpaper("Custom ID")
            end
            if wallpaperDropdown and wallpaperDropdown.Set then
                pcall(function() wallpaperDropdown:Set("Custom ID") end)
            end
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Custom Wallpaper",
                Text = "Saved & Applied Asset ID: " .. cleanId,
                Duration = 2
            })
        end
    end
})

SettingsTab:AddButton({
    Name = "Apply & Save Custom Wallpaper",
    Description = "Saves and renders the custom asset ID permanently",
    Callback = function()
        local cleanId = tostring(_G.BTR_CUSTOM_WALLPAPER_ID or ""):gsub("%D", "")
        if cleanId:len() > 3 then
            _G.BTR_SELECTED_WALLPAPER = "Custom ID"
            if _G.saveBTRConfig then
                _G.saveBTRConfig()
            end
            if _G.BTR_SetWallpaper then
                _G.BTR_SetWallpaper("Custom ID")
            end
            if wallpaperDropdown and wallpaperDropdown.Set then
                pcall(function() wallpaperDropdown:Set("Custom ID") end)
            end
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Wallpaper Saved",
                Text = "Permanent Asset ID: " .. cleanId,
                Duration = 2
            })
        else
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Custom Wallpaper",
                Text = "Please enter an Image ID first!",
                Duration = 2
            })
        end
    end
})

local iconDropdown
iconDropdown = SettingsTab:AddDropdown({
    Name = "Hub Icon",
    Description = "Choose the hub button and header icon",
    Options = {"Default", "Option 1", "Custom ID"},
    Default = _G.BTR_SELECTED_ICON or "Default",
    Callback = function(choice)
        _G.BTR_SELECTED_ICON = choice
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_SetHubIcon then
            _G.BTR_SetHubIcon(choice)
        end
        if _G.BTR_ALL_LOADED then
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Hub Icon",
                Text = "Applied: " .. tostring(choice),
                Duration = 2
            })
        end
    end
})

SettingsTab:AddTextBox({
    Name = "Custom Icon Asset ID",
    Description = "Enter any custom Roblox Image/Texture ID",
    Default = _G.BTR_CUSTOM_ICON_ID or "",
    PlaceholderText = "Paste Icon ID (e.g. 125815391535552)...",
    ClearText = false,
    Callback = function(val)
        local cleanId = tostring(val or ""):gsub("%D", "")
        if cleanId:len() > 3 then
            _G.BTR_CUSTOM_ICON_ID = cleanId
            _G.BTR_SELECTED_ICON = "Custom ID"
            if _G.saveBTRConfig then
                _G.saveBTRConfig()
            end
            if _G.BTR_SetHubIcon then
                _G.BTR_SetHubIcon("Custom ID")
            end
            if iconDropdown and iconDropdown.Set then
                pcall(function() iconDropdown:Set("Custom ID") end)
            end
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Custom Icon",
                Text = "Saved & Applied Asset ID: " .. cleanId,
                Duration = 2
            })
        end
    end
})

SettingsTab:AddButton({
    Name = "Apply & Save Custom Icon",
    Description = "Saves and renders the custom icon ID in real-time",
    Callback = function()
        local cleanId = tostring(_G.BTR_CUSTOM_ICON_ID or ""):gsub("%D", "")
        if cleanId:len() > 3 then
            _G.BTR_SELECTED_ICON = "Custom ID"
            if _G.saveBTRConfig then
                _G.saveBTRConfig()
            end
            if _G.BTR_SetHubIcon then
                _G.BTR_SetHubIcon("Custom ID")
            end
            if iconDropdown and iconDropdown.Set then
                pcall(function() iconDropdown:Set("Custom ID") end)
            end
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Icon Saved",
                Text = "Permanent Asset ID: " .. cleanId,
                Duration = 2
            })
        else
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Custom Icon",
                Text = "Please enter an Image ID first!",
                Duration = 2
            })
        end
    end
})

SettingsTab:AddToggle({
    Name = "Floating BTR Button",
    Description = "Show or hide the on-screen BTR toggle button (Hotkey: RightControl)",
    Default = _G.BTR_SHOW_FLOATING_BUTTON ~= false,
    Callback = function(val)
        _G.BTR_SHOW_FLOATING_BUTTON = val
        if _G.saveBTRConfig then _G.saveBTRConfig() end
        if _G.BTR_FloatingButton then
            _G.BTR_FloatingButton.Visible = val
        end
    end
})

_G.BTR_NO_ANIMATION_TOGGLE = SettingsTab:AddToggle({
    Name = "No Category Animation",
    Description = "Instantly display tab contents without slide animation",
    Default = (_G.BTR_NO_ANIMATION == true),
    Callback = function(val)
        _G.BTR_NO_ANIMATION = val
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_ApplyNoAnimation then
            _G.BTR_ApplyNoAnimation()
        end
    end
})

SettingsTab:AddSection({
    "> Color Customization (Dark, Stealth & Vibrant)"
})

SettingsTab:AddDropdown({
    Name = "Category Color",
    Description = "Choose category tab background color (Sleek dark & bright neon options)",
    Options = {
        "AMOLED Black (Pure)",
        "Midnight Obsidian",
        "Deep Charcoal",
        "Stealth Slate",
        "Dark Classic",
        "Blood Dark Crimson",
        "Abyss Dark Navy",
        "Royal Dark Violet",
        "Emerald Dark Forest",
        "Cyber Dark Smoke",
        "Matrix Dark Olive",
        "Dark Copper Brown",
        "White",
        "Neon Cyan",
        "Electric Purple",
        "Hot Pink",
        "Lime Green",
        "Sunset Orange",
        "Golden Yellow",
        "Crimson Red",
        "Rainbow"
    },
    Default = _G.BTR_CATEGORY_COLOR or "White",
    Callback = function(val)
        _G.BTR_CATEGORY_COLOR = val
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_ApplyMenuCustoms then
            _G.BTR_ApplyMenuCustoms()
        end
    end
})

SettingsTab:AddDropdown({
    Name = "Menu Text Color",
    Description = "Choose options text color with high contrast palettes",
    Options = {
        "Default",
        "Pure White",
        "Silver Slate",
        "Muted Smoke",
        "Stealth Charcoal",
        "Ice Mint",
        "Cyber Cyan",
        "Electric Purple",
        "Hot Pink",
        "Lime Green",
        "Toxic Lime",
        "Sunset Orange",
        "Golden Amber",
        "Crimson Blood",
        "Rainbow"
    },
    Default = _G.BTR_TEXT_COLOR or "Default",
    Callback = function(val)
        _G.BTR_TEXT_COLOR = val
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_ApplyTextColors then
            _G.BTR_ApplyTextColors()
        end
    end
})

SettingsTab:AddDropdown({
    Name = "Title Font Style",
    Description = "Choose font family for the BTR CLIENT header",
    Options = {"GothamBlack", "FredokaOne", "Arcade", "Creepster", "PermanentMarker", "BuilderSansBold", "SpecialElite", "SciFi"},
    Default = _G.BTR_TITLE_FONT or "GothamBlack",
    Callback = function(val)
        _G.BTR_TITLE_FONT = val
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_ApplyHeaderStyle then
            _G.BTR_ApplyHeaderStyle()
        end
    end
})

SettingsTab:AddDropdown({
    Name = "Title Color Theme",
    Description = "Choose color theme for the BTR CLIENT header",
    Options = {
        "Cyberpunk (Pink & Cyan)",
        "Stealth Obsidian (Dark Gray & Neon Cyan)",
        "Midnight Blood (Dark Charcoal & Crimson)",
        "Abyss Purple (Dark Smoke & Violet)",
        "Phantom Emerald (Dark Carbon & Toxic Green)",
        "Pure Stealth (Charcoal & Ghost White)",
        "Dark Gold Luxury (Dark Bronze & Bright Gold)",
        "Blood Samurai (Crimson & White)",
        "Golden Dragon (Gold & Amber)",
        "Venom Toxic (Neon Lime & Emerald)",
        "Shadow Void (Purple & Magenta)",
        "Glacier Ice (Diamond & Pure Cyan)",
        "Monochrome (Pure White & Silver)",
        "Rainbow Chroma (Animated Wave)"
    },
    Default = _G.BTR_TITLE_STYLE or "Cyberpunk (Pink & Cyan)",
    Callback = function(val)
        _G.BTR_TITLE_STYLE = val
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_ApplyHeaderStyle then
            _G.BTR_ApplyHeaderStyle()
        end
    end
})

SettingsTab:AddSection({
    "> Interface Scale & Transparency"
})

SettingsTab:AddSlider({
    Name = "Menu Opacity",
    Description = "Adjust overall Hub transparency in real-time",
    Min = 10,
    Max = 100,
    Default = _G.BTR_MENU_OPACITY or 100,
    Suffix = "%",
    Callback = function(val)
        _G.BTR_MENU_OPACITY = val
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_ApplyMenuCustoms then
            _G.BTR_ApplyMenuCustoms()
        end
    end
})

SettingsTab:AddSlider({
    Name = "Category Opacity",
    Description = "Adjust category background & text transparency in real-time",
    Min = 10,
    Max = 100,
    Default = _G.BTR_CATEGORY_OPACITY or 100,
    Suffix = "%",
    Callback = function(val)
        _G.BTR_CATEGORY_OPACITY = val
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_ApplyMenuCustoms then
            _G.BTR_ApplyMenuCustoms()
        end
    end
})

SettingsTab:AddDropdown({
    Name = "Hub Scale Preset",
    Description = "Choose a preset size instantly without dragging",
    Options = {"80%", "90%", "100% (Default)", "110%", "120%", "130%", "140%"},
    Default = (_G.BTR_HUB_SCALE == 100 and "100% (Default)") or (tostring(_G.BTR_HUB_SCALE or 100) .. "%"),
    Callback = function(choice)
        local num = tonumber(choice:match("%d+")) or 100
        _G.BTR_HUB_SCALE = num
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_SetHubScale then
            _G.BTR_SetHubScale(num)
        end
    end
})

SettingsTab:AddSlider({
    Name = "Custom Scale Value",
    Description = "Select fine-tuned scale percentage (70% - 150%)",
    Min = 70,
    Max = 150,
    Default = _G.BTR_HUB_SCALE or 100,
    Suffix = "%",
    Callback = function(val)
        _G.BTR_HUB_SCALE = val
    end
})

SettingsTab:AddButton({
    Name = "Apply Scale",
    Description = "Click to apply chosen scale percentage smoothly",
    Callback = function()
        if _G.saveBTRConfig then
            _G.saveBTRConfig()
        end
        if _G.BTR_SetHubScale then
            _G.BTR_SetHubScale(_G.BTR_HUB_SCALE or 100)
        end
        if _G.BTR_ALL_LOADED then
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Hub Scale",
                Text = "Applied Scale: " .. tostring(_G.BTR_HUB_SCALE or 100) .. "%",
                Duration = 2
            })
        end
    end
})

SettingsTab:AddSection({
    "> Auto-Launch Protections (Startup Defaults)"
})

_G.BTR_SETTINGS_TOGGLES.AntiSit = SettingsTab:AddToggle({
    Name = "Auto-Start: Anti-Sit",
    Default = (_G.BTR_AUTORUN and _G.BTR_AUTORUN.AntiSit ~= false),
    Callback = function(val)
        _G.setBTRStartupPref("AntiSit", val)
    end
})

_G.BTR_SETTINGS_TOGGLES.AntiCopy = SettingsTab:AddToggle({
    Name = "Auto-Start: Anti-Copy Avatar",
    Default = _G.BTR_AUTORUN and (_G.BTR_AUTORUN.AntiCopy == true) or false,
    Callback = function(val)
        _G.setBTRStartupPref("AntiCopy", val)
    end
})

_G.BTR_SETTINGS_TOGGLES.AntiAfk = SettingsTab:AddToggle({
    Name = "Auto-Start: Anti-AFK",
    Default = _G.BTR_AUTORUN and (_G.BTR_AUTORUN.AntiAfk == true) or false,
    Callback = function(val)
        _G.setBTRStartupPref("AntiAfk", val)
    end
})

_G.BTR_SETTINGS_TOGGLES.Vehicles = SettingsTab:AddToggle({
    Name = "Auto-Start: Anti-Fling Vehicles",
    Default = _G.BTR_AUTORUN and (_G.BTR_AUTORUN.Vehicles == true) or false,
    Callback = function(val)
        _G.setBTRStartupPref("Vehicles", val)
    end
})

_G.BTR_SETTINGS_TOGGLES.Dump = SettingsTab:AddToggle({
    Name = "Auto-Start: Anti-Dump Tool",
    Default = _G.BTR_AUTORUN and (_G.BTR_AUTORUN.Dump == true) or false,
    Callback = function(val)
        _G.setBTRStartupPref("Dump", val)
    end
})

_G.BTR_SETTINGS_TOGGLES.Canoes = SettingsTab:AddToggle({
    Name = "Auto-Start: Anti-Canoe / Jets / Helis Fling",
    Default = _G.BTR_AUTORUN and (_G.BTR_AUTORUN.Canoes == true or _G.BTR_AUTORUN.Jets == true or _G.BTR_AUTORUN.Helis == true) or false,
    Callback = function(val)
        _G.setBTRStartupPref("Canoes", val)
        _G.setBTRStartupPref("Jets", val)
        _G.setBTRStartupPref("Helis", val)
    end
})

_G.BTR_SETTINGS_TOGGLES.PropPlacement = SettingsTab:AddToggle({
    Name = "Auto-Start: Unlock House Props",
    Default = _G.BTR_AUTORUN and (_G.BTR_AUTORUN.PropPlacement == true) or false,
    Callback = function(val)
        _G.setBTRStartupPref("PropPlacement", val)
    end
})

_G.BTR_SETTINGS_TOGGLES.Balls = SettingsTab:AddToggle({
    Name = "Auto-Start: Anti-Fling Ball",
    Default = _G.BTR_AUTORUN and (_G.BTR_AUTORUN.Balls == true) or false,
    Callback = function(val)
        _G.setBTRStartupPref("Balls", val)
    end
})

_G.BTR_SETTINGS_TOGGLES.Doors = SettingsTab:AddToggle({
    Name = "Auto-Start: Anti-Fling Doors",
    Default = _G.BTR_AUTORUN and (_G.BTR_AUTORUN.Doors == true) or false,
    Callback = function(val)
        _G.setBTRStartupPref("Doors", val)
    end
})

_G.BTR_SETTINGS_TOGGLES.InvincibleShield = SettingsTab:AddToggle({
    Name = "Auto-Start: Invincible Hacker Shield",
    Default = _G.BTR_AUTORUN and (_G.BTR_AUTORUN.InvincibleShield == true) or false,
    Callback = function(val)
        _G.setBTRStartupPref("InvincibleShield", val)
    end
})

SettingsTab:AddSection({
    "> Performance & Graphics"
})

local _savedGraphics = {}
local _antiLagActive = false
SettingsTab:AddToggle({
    Name = "Anti-Lag (FPS Boost)",
    Description = "Boosts FPS by simplifying textures (restores on toggle OFF)",
    Default = false,
    Callback = function(state)
        _antiLagActive = state
        local Lighting = game:GetService("Lighting")
        local Terrain = workspace:FindFirstChildOfClass("Terrain")

        if state then
            
            _savedGraphics = {
                GlobalShadows = Lighting.GlobalShadows,
                FogEnd = Lighting.FogEnd,
                Brightness = Lighting.Brightness,
                TerrainWaterWaveSize = Terrain and Terrain.WaterWaveSize or 0,
                Parts = {}
            }

            pcall(function()
                Lighting.GlobalShadows = false
                Lighting.FogEnd = 9e9
                if Terrain then Terrain.WaterWaveSize = 0 end
            end)

            for _, obj in ipairs(workspace:GetDescendants()) do
                pcall(function()
                    if obj:IsA("BasePart") and not obj.Parent:FindFirstChildOfClass("Humanoid") then
                        _savedGraphics.Parts[obj] = {
                            Material = obj.Material,
                            CastShadow = obj.CastShadow
                        }
                        obj.Material = Enum.Material.SmoothPlastic
                        obj.CastShadow = false
                    elseif obj:IsA("Decal") or obj:IsA("Texture") then
                        _savedGraphics.Parts[obj] = { Transparency = obj.Transparency }
                        obj.Transparency = 0.8
                    elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire") then
                        _savedGraphics.Parts[obj] = { Enabled = obj.Enabled }
                        obj.Enabled = false
                    end
                end)
            end
        else
            
            pcall(function()
                if _savedGraphics.GlobalShadows ~= nil then Lighting.GlobalShadows = _savedGraphics.GlobalShadows end
                if _savedGraphics.FogEnd ~= nil then Lighting.FogEnd = _savedGraphics.FogEnd end
                if _savedGraphics.Brightness ~= nil then Lighting.Brightness = _savedGraphics.Brightness end
                if Terrain and _savedGraphics.TerrainWaterWaveSize ~= nil then Terrain.WaterWaveSize = _savedGraphics.TerrainWaterWaveSize end
            end)

            for obj, props in pairs(_savedGraphics.Parts or {}) do
                pcall(function()
                    if obj and obj.Parent then
                        if props.Material ~= nil then obj.Material = props.Material end
                        if props.CastShadow ~= nil then obj.CastShadow = props.CastShadow end
                        if props.Transparency ~= nil then obj.Transparency = props.Transparency end
                        if props.Enabled ~= nil then obj.Enabled = props.Enabled end
                    end
                end)
            end
            _savedGraphics = {}
        end
    end
})

local _shadersActive = false
local _shaderSavedLighting = {}
local _shaderSavedMaterials = {}
local _shaderOrigSky = nil
local _shaderInstances = {}
local _shaderConnections = {}

local function revertDarkBrookShaders()
    _shadersActive = false

    for _, conn in ipairs(_shaderConnections) do
        pcall(function() conn:Disconnect() end)
    end
    _shaderConnections = {}

    for _, inst in ipairs(_shaderInstances) do
        pcall(function()
            if inst and inst.Parent then
                inst:Destroy()
            end
        end)
    end
    _shaderInstances = {}

    for part, origMat in pairs(_shaderSavedMaterials) do
        pcall(function()
            if part and part.Parent then
                part.Material = origMat
            end
        end)
    end
    _shaderSavedMaterials = {}

    local Lighting = game:GetService("Lighting")
    pcall(function()
        if _shaderSavedLighting.Brightness ~= nil then Lighting.Brightness = _shaderSavedLighting.Brightness end
        if _shaderSavedLighting.GlobalShadows ~= nil then Lighting.GlobalShadows = _shaderSavedLighting.GlobalShadows end
        if _shaderSavedLighting.OutdoorAmbient ~= nil then Lighting.OutdoorAmbient = _shaderSavedLighting.OutdoorAmbient end
        if _shaderSavedLighting.FogColor ~= nil then Lighting.FogColor = _shaderSavedLighting.FogColor end
        if _shaderSavedLighting.FogStart ~= nil then Lighting.FogStart = _shaderSavedLighting.FogStart end
        if _shaderSavedLighting.FogEnd ~= nil then Lighting.FogEnd = _shaderSavedLighting.FogEnd end
        if _shaderSavedLighting.EnvironmentSpecularScale ~= nil then Lighting.EnvironmentSpecularScale = _shaderSavedLighting.EnvironmentSpecularScale end
        if _shaderSavedLighting.EnvironmentDiffuseScale ~= nil then Lighting.EnvironmentDiffuseScale = _shaderSavedLighting.EnvironmentDiffuseScale end
    end)
    _shaderSavedLighting = {}

    if _shaderOrigSky and _shaderOrigSky.Parent == nil then
        pcall(function() _shaderOrigSky.Parent = Lighting end)
    end
    _shaderOrigSky = nil
end

local function applyDarkBrookShaders()
    _shadersActive = true
    local Lighting = game:GetService("Lighting")
    local TweenService = game:GetService("TweenService")
    local RunService = game:GetService("RunService")
    local Debris = game:GetService("Debris")
    local Players = game:GetService("Players")
    local player = Players.LocalPlayer

    _shaderSavedLighting = {
        Brightness = Lighting.Brightness,
        GlobalShadows = Lighting.GlobalShadows,
        OutdoorAmbient = Lighting.OutdoorAmbient,
        FogColor = Lighting.FogColor,
        FogStart = Lighting.FogStart,
        FogEnd = Lighting.FogEnd,
        EnvironmentSpecularScale = Lighting.EnvironmentSpecularScale,
        EnvironmentDiffuseScale = Lighting.EnvironmentDiffuseScale
    }

    local existingSky = Lighting:FindFirstChildOfClass("Sky")
    if existingSky then
        _shaderOrigSky = existingSky
        existingSky.Parent = nil
    end

    local model = workspace:FindFirstChild("Model")
    if model then
        local function setMat(obj)
            for _, c in pairs(obj:GetChildren()) do
                if c:IsA("BasePart") then
                    if not _shaderSavedMaterials[c] then _shaderSavedMaterials[c] = c.Material end
                    c.Material = Enum.Material.Basalt
                elseif c:IsA("Model") or c:IsA("Folder") then
                    setMat(c)
                end
            end
        end

        if model:FindFirstChild("001_SnowStreet") then setMat(model["001_SnowStreet"]) end
        if model:FindFirstChild("Street") then
            for _, o in pairs(model.Street:GetDescendants()) do
                if o:IsA("BasePart") then
                    if not _shaderSavedMaterials[o] then _shaderSavedMaterials[o] = o.Material end
                    o.Material = Enum.Material.Basalt
                end
            end
        end

        for _, o in pairs(model:GetChildren()) do
            if o:IsA("BasePart") and (o.Name == "Sidewalk" or o.Name == "Wedge") and o.Material == Enum.Material.SmoothPlastic then
                if not _shaderSavedMaterials[o] then _shaderSavedMaterials[o] = o.Material end
                o.Material = Enum.Material.Cobblestone
            end
        end

        local conn = model.ChildAdded:Connect(function(obj)
            if not _shadersActive then return end
            if obj:IsA("BasePart") and (obj.Name == "Sidewalk" or obj.Name == "Wedge") and obj.Material == Enum.Material.SmoothPlastic then
                if not _shaderSavedMaterials[obj] then _shaderSavedMaterials[obj] = obj.Material end
                obj.Material = Enum.Material.Cobblestone
            end
        end)
        table.insert(_shaderConnections, conn)
    end

    local soundPart = Instance.new("Part")
    soundPart.Name = "BTR_ShaderSoundPart"
    soundPart.Size = Vector3.new(1, 1, 1)
    soundPart.Transparency = 1
    soundPart.Anchored = true
    soundPart.CanCollide = false
    soundPart.Parent = workspace
    table.insert(_shaderInstances, soundPart)

    local birdSound = Instance.new("Sound")
    birdSound.Name = "BirdsSound"
    birdSound.SoundId = "rbxassetid://1237969272"
    birdSound.Looped = true
    birdSound.Volume = 0.5
    birdSound.Parent = soundPart

    local wolfSound = Instance.new("Sound")
    wolfSound.Name = "WolfSound"
    wolfSound.SoundId = "rbxassetid://9069609200"
    wolfSound.Looped = false
    wolfSound.Volume = 0.7
    wolfSound.Parent = soundPart

    local function isNight()
        local t = Lighting.ClockTime
        return t >= 18 or t <= 6
    end

    task.spawn(function()
        while _shadersActive do
            if isNight() then
                if birdSound.IsPlaying then birdSound:Stop() end
                if not wolfSound.IsPlaying then wolfSound:Play() end
            else
                if wolfSound.IsPlaying then wolfSound:Stop() end
                if not birdSound.IsPlaying then birdSound:Play() end
            end
            task.wait(20)
        end
    end)

    local shootingStarsFolder = Instance.new("Folder")
    shootingStarsFolder.Name = "BTR_ShootingStars"
    shootingStarsFolder.Parent = workspace
    table.insert(_shaderInstances, shootingStarsFolder)

    local spaceSound = Instance.new("Sound")
    spaceSound.Name = "SpaceSound"
    spaceSound.SoundId = "rbxassetid://9069609200"
    spaceSound.Looped = true
    spaceSound.Volume = 0.4
    spaceSound.Parent = soundPart

    local function createShootingStar()
        if not _shadersActive or not shootingStarsFolder.Parent then return end
        local char = player.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        local star = Instance.new("Part")
        star.Size = Vector3.new(0.3, 0.3, 0.3)
        star.Material = Enum.Material.Neon
        star.Color = Color3.fromRGB(255, 255, 255)
        star.Anchored = true
        star.CanCollide = false
        star.Parent = shootingStarsFolder

        local angle = math.rad(math.random(0, 360))
        local dist = math.random(100, 300)
        local startPos = hrp.Position + Vector3.new(math.cos(angle) * dist, math.random(150, 300), math.sin(angle) * dist)
        star.Position = startPos

        local dir = Vector3.new(math.random(-100, 100) / 100, -0.7, math.random(-100, 100) / 100).Unit
        local speed = math.random(250, 450)
        local lifetime = math.random(1, 1.5)
        local timePassed = 0

        local moveConn
        moveConn = RunService.Heartbeat:Connect(function(dt)
            if not _shadersActive or not star.Parent then
                if moveConn then moveConn:Disconnect() end
                if star.Parent then star:Destroy() end
                return
            end
            timePassed = timePassed + dt
            if timePassed >= lifetime then
                if moveConn then moveConn:Disconnect() end
                if star.Parent then star:Destroy() end
                return
            end
            star.Position = star.Position + dir * speed * dt
        end)
    end

    task.spawn(function()
        while _shadersActive do
            if isNight() then
                createShootingStar()
                task.wait(math.random(2, 5))
            else
                task.wait(3)
            end
        end
    end)

    local rainFolder = Instance.new("Folder")
    rainFolder.Name = "BTR_FakeRain"
    rainFolder.Parent = workspace
    table.insert(_shaderInstances, rainFolder)

    local function spawnRain()
        if not _shadersActive or not rainFolder.Parent then return end
        local char = player.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        for i = 1, 10 do
            local drop = Instance.new("Part")
            drop.Size = Vector3.new(0.05, 0.7, 0.05)
            drop.Color = Color3.fromRGB(180, 200, 255)
            drop.Material = Enum.Material.Glass
            drop.Transparency = 0.5
            drop.Anchored = true
            drop.CanCollide = false
            drop.Position = hrp.Position + Vector3.new(math.random(-40, 40), 30, math.random(-40, 40))
            drop.Parent = rainFolder

            local tween = TweenService:Create(drop, TweenInfo.new(0.6), { Position = drop.Position - Vector3.new(0, 45, 0) })
            tween:Play()
            Debris:AddItem(drop, 0.65)
        end
    end

    task.spawn(function()
        while _shadersActive do
            spawnRain()
            task.wait(0.2)
        end
    end)

    Lighting.Brightness = 2
    Lighting.GlobalShadows = true
    Lighting.OutdoorAmbient = Color3.fromRGB(70, 70, 70)
    Lighting.FogColor = Color3.fromRGB(120, 130, 140)
    Lighting.FogStart = 80
    Lighting.FogEnd = 600
    Lighting.EnvironmentSpecularScale = 1
    Lighting.EnvironmentDiffuseScale = 0.5

    local sky = Instance.new("Sky")
    sky.Name = "BTR_DarkBrookSky"
    sky.SkyboxBk = "rbxassetid://159454299"
    sky.SkyboxDn = "rbxassetid://159454296"
    sky.SkyboxFt = "rbxassetid://159454293"
    sky.SkyboxLf = "rbxassetid://159454286"
    sky.SkyboxRt = "rbxassetid://159454300"
    sky.SkyboxUp = "rbxassetid://159454304"
    sky.Parent = Lighting
    table.insert(_shaderInstances, sky)

    local color = Instance.new("ColorCorrectionEffect")
    color.Name = "BTR_ColorCorrection"
    color.Brightness = 0.03
    color.Contrast = 0.15
    color.Saturation = 0.05
    color.TintColor = Color3.fromRGB(255, 240, 220)
    color.Parent = Lighting
    table.insert(_shaderInstances, color)

    local bloom = Instance.new("BloomEffect")
    bloom.Name = "BTR_Bloom"
    bloom.Intensity = 0.8
    bloom.Size = 56
    bloom.Threshold = 0.9
    bloom.Parent = Lighting
    table.insert(_shaderInstances, bloom)

    local sunRays = Instance.new("SunRaysEffect")
    sunRays.Name = "BTR_SunRays"
    sunRays.Intensity = 0.05
    sunRays.Spread = 0.8
    sunRays.Parent = Lighting
    table.insert(_shaderInstances, sunRays)

    local blur = Instance.new("BlurEffect")
    blur.Name = "BTR_Blur"
    blur.Size = 0
    blur.Parent = Lighting
    table.insert(_shaderInstances, blur)
end

SettingsTab:AddToggle({
    Name = "DarkBrook Shaders (Reversible)",
    Description = "Realistic sky, lighting, ambient audio & materials (fully reversible on OFF)",
    Default = false,
    Callback = function(state)
        if state then
            applyDarkBrookShaders()
            game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Shaders", Text = "DarkBrook Shaders enabled!", Duration = 3 })
        else
            revertDarkBrookShaders()
            game:GetService("StarterGui"):SetCore("SendNotification", { Title = "Shaders", Text = "Default graphics restored!", Duration = 3 })
        end
    end
})

SettingsTab:AddSection({
    "> Server Hopping & Utilities"
})

SettingsTab:AddButton({
    Name = "Rejoin Server",
    Description = "Rejoins the current Brookhaven server",
    Callback = function()
        pcall(function()
            local TS = game:GetService("TeleportService")
            local Players = game:GetService("Players")
            if #Players:GetPlayers() <= 1 then
                Players.LocalPlayer:Kick("\nRejoining...")
                task.wait()
                TS:Teleport(game.PlaceId, Players.LocalPlayer)
            else
                TS:TeleportToPlaceInstance(game.PlaceId, game.JobId, Players.LocalPlayer)
            end
        end)
    end
})

local function performServerHop(mode)
    task.spawn(function()
        local TS = game:GetService("TeleportService")
        local HttpService = game:GetService("HttpService")
        local Players = game:GetService("Players")
        local StarterGui = game:GetService("StarterGui")
        local placeId = game.PlaceId
        local currentJob = game.JobId

        StarterGui:SetCore("SendNotification", {Title="Server Hop", Text="Searching for server (" .. mode .. ")...", Duration=3})

        local url = "https://games.roblox.com/v1/games/" .. placeId .. "/servers/Public?sortOrder=" .. (mode == "Small" and "Asc" or "Desc") .. "&limit=100"
        local success, result = pcall(function()
            return game:HttpGet(url)
        end)

        if not success or not result then
            StarterGui:SetCore("SendNotification", {Title="Server Hop", Text="Failed to fetch servers list.", Duration=3})
            return
        end

        local data = nil
        pcall(function() data = HttpService:JSONDecode(result) end)

        if not data or not data.data or #data.data == 0 then
            StarterGui:SetCore("SendNotification", {Title="Server Hop", Text="No servers found!", Duration=3})
            return
        end

        local eligibleServers = {}
        for _, s in ipairs(data.data) do
            if type(s) == "table" and s.id and s.id ~= currentJob and s.playing and s.maxPlayers and s.playing < s.maxPlayers then
                if mode == "Small" and s.playing <= 8 then
                    table.insert(eligibleServers, s.id)
                elseif mode == "Large" and s.playing >= (s.maxPlayers - 4) then
                    table.insert(eligibleServers, s.id)
                elseif mode == "Random" then
                    table.insert(eligibleServers, s.id)
                end
            end
        end

        
        if #eligibleServers == 0 then
            for _, s in ipairs(data.data) do
                if type(s) == "table" and s.id and s.id ~= currentJob and s.playing and s.maxPlayers and s.playing < s.maxPlayers then
                    table.insert(eligibleServers, s.id)
                end
            end
        end

        if #eligibleServers > 0 then
            local targetServer = eligibleServers[math.random(1, #eligibleServers)]
            StarterGui:SetCore("SendNotification", {Title="Server Hop", Text="Teleporting to new server...", Duration=3})
            pcall(function()
                TS:TeleportToPlaceInstance(placeId, targetServer, Players.LocalPlayer)
            end)
        else
            StarterGui:SetCore("SendNotification", {Title="Server Hop", Text="No eligible server available.", Duration=3})
        end
    end)
end

SettingsTab:AddButton({
    Name = "Server Hop (Random)",
    Description = "Teleports you to a random public Brookhaven server",
    Callback = function()
        performServerHop("Random")
    end
})

SettingsTab:AddButton({
    Name = "Server Hop (Small Server)",
    Description = "Teleports you to a low-player Brookhaven server",
    Callback = function()
        performServerHop("Small")
    end
})

SettingsTab:AddButton({
    Name = "Server Hop (Large Server)",
    Description = "Teleports you to a crowded Brookhaven server",
    Callback = function()
        performServerHop("Large")
    end
})

SettingsTab:AddSection({
    "> Script Management & Unload"
})

SettingsTab:AddButton({
    Name = "Unload Script",
    Description = "Completely clears BTR Client, disconnects all loops and removes GUI",
    Callback = function()
        task.spawn(function()
            local StarterGui = game:GetService("StarterGui")
            StarterGui:SetCore("SendNotification", {Title="BTR Client", Text="Unloading script completely...", Duration=2})

            
            if _G.BTR_MASTER_CLEANUP then
                pcall(_G.BTR_MASTER_CLEANUP)
            end

            
            if _shadersActive then
                pcall(revertDarkBrookShaders)
            end

            
            if _antiLagActive and _savedGraphics then
                pcall(function()
                    for obj, props in pairs(_savedGraphics.Parts or {}) do
                        if obj and obj.Parent then
                            if props.Material ~= nil then obj.Material = props.Material end
                            if props.CastShadow ~= nil then obj.CastShadow = props.CastShadow end
                            if props.Transparency ~= nil then obj.Transparency = props.Transparency end
                            if props.Enabled ~= nil then obj.Enabled = props.Enabled end
                        end
                    end
                end)
            end

            
            pcall(function() workspace.CurrentCamera.FieldOfView = 70 end)

            
            task.wait(0.2)
            pcall(function()
                local CoreGui = game:GetService("CoreGui")
                for _, g in ipairs(CoreGui:GetChildren()) do
                    if g.Name:find("Redz") or g.Name:find("BTR") or g.Name:find("Drip") then
                        g:Destroy()
                    end
                end
            end)
            pcall(function()
                local lp = game:GetService("Players").LocalPlayer
                for _, g in ipairs(lp.PlayerGui:GetChildren()) do
                    if g.Name:find("Redz") or g.Name:find("BTR") or g.Name:find("Drip") then
                        g:Destroy()
                    end
                end
            end)

            _G.BTR_UI_READY = false
            StarterGui:SetCore("SendNotification", {Title="BTR Client", Text="BTR Client has been fully unloaded!", Duration=3})
        end)
    end
})

local function _btrInitStalkerMimic()
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    local RunService = game:GetService("RunService")
    local TextChatService = game:GetService("TextChatService")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local StarterGui = game:GetService("StarterGui")

    local stalkSelectedTarget = nil
    local copyChatEnabled = false
    local copyMoveEnabled = false
    local stalkerEnabled = false

    local copyMoveMode = "Mirror"
    local copyMoveDistance = 6
    local stalkPositionMode = "Dynamic"
    local stalkDistance = 30
    local stalkFleeOnSight = true

    local function resolveTarget()
        if stalkSelectedTarget and stalkSelectedTarget ~= "---" then
            local p = Players:FindFirstChild(stalkSelectedTarget)
            if p then return p end
        end
        if _G.TrollTarget and _G.TrollTarget ~= "---" then
            local p = Players:FindFirstChild(_G.TrollTarget)
            if p then return p end
        end
        return nil
    end

    local function notify(title, text)
        pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = 3
            })
        end)
    end

    
    local lastParrotMsg = ""
    local lastParrotTime = 0

    local function sendChatMsg(msg)
        if not msg or msg == "" then return end
        local sent = false
        pcall(function()
            if TextChatService and TextChatService.ChatVersion == Enum.ChatVersion.TextChatService then
                local channels = TextChatService:FindFirstChild("TextChannels")
                local rbxGeneral = channels and channels:FindFirstChild("RBXGeneral")
                if rbxGeneral then
                    rbxGeneral:SendAsync(msg)
                    sent = true
                end
            end
        end)
        if sent then return end
        pcall(function()
            local defaultChat = ReplicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
            local sayMsg = defaultChat and defaultChat:FindFirstChild("SayMessageRequest")
            if sayMsg then
                sayMsg:FireServer(msg, "All")
            end
        end)
    end

    local function onChatReceived(sender, msg)
        if not copyChatEnabled then return end
        local target = resolveTarget()
        if not target or sender ~= target then return end
        if sender == LocalPlayer then return end

        local now = tick()
        if msg == lastParrotMsg and (now - lastParrotTime) < 0.5 then return end
        lastParrotMsg = msg
        lastParrotTime = now

        task.spawn(function()
            task.wait(0.12)
            sendChatMsg(msg)
        end)
    end

    pcall(function()
        if TextChatService then
            TextChatService.MessageReceived:Connect(function(textChatMessage)
                if textChatMessage.TextSource then
                    local sender = Players:GetPlayerByUserId(textChatMessage.TextSource.UserId)
                    if sender then
                        onChatReceived(sender, textChatMessage.Text)
                    end
                end
            end)
        end
    end)

    local function hookPlayerLegacyChat(p)
        p.Chatted:Connect(function(msg)
            onChatReceived(p, msg)
        end)
    end
    for _, p in ipairs(Players:GetPlayers()) do
        hookPlayerLegacyChat(p)
    end
    Players.PlayerAdded:Connect(hookPlayerLegacyChat)

    
    local copyMoveConn = nil
    local noClipMoveConn = nil

    local function stopCopyMovement()
        if copyMoveConn then copyMoveConn:Disconnect(); copyMoveConn = nil end
        if noClipMoveConn then noClipMoveConn:Disconnect(); noClipMoveConn = nil end
        local char = LocalPlayer.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then
            hum:Move(Vector3.zero, false)
            hum.WalkSpeed = 16
        end
    end

    local function startCopyMovement()
        stopCopyMovement()
        local target = resolveTarget()
        if not target then
            notify("Copy Movement", "Select a target first")
            return false
        end

        noClipMoveConn = RunService.Stepped:Connect(function()
            local char = LocalPlayer.Character
            if char then
                for _, part in ipairs(char:GetDescendants()) do
                    if part:IsA("BasePart") and part.CanCollide and part.Name ~= "HumanoidRootPart" then
                        part.CanCollide = false
                    end
                end
            end
        end)

        copyMoveConn = RunService.RenderStepped:Connect(function(dt)
            local currentTarget = resolveTarget()
            if not currentTarget or not currentTarget.Character then return end
            local tChar = currentTarget.Character
            local tHrp = tChar:FindFirstChild("HumanoidRootPart")
            local tHum = tChar:FindFirstChildOfClass("Humanoid")
            if not tHrp or not tHum then return end

            local myChar = LocalPlayer.Character
            if not myChar then return end
            local myHrp = myChar:FindFirstChild("HumanoidRootPart")
            local myHum = myChar:FindFirstChildOfClass("Humanoid")
            if not myHrp or not myHum then return end

            if myHum.WalkSpeed ~= tHum.WalkSpeed then
                myHum.WalkSpeed = tHum.WalkSpeed
            end

            if tHum.Jump or tHum:GetState() == Enum.HumanoidStateType.Jumping then
                myHum.Jump = true
            end

            if tHum.Sit and not myHum.Sit then
                myHum.Sit = true
            elseif not tHum.Sit and myHum.Sit then
                myHum.Sit = false
                pcall(function() myHum:ChangeState(Enum.HumanoidStateType.GettingUp) end)
            end

            local idealPos = myHrp.Position
            local lookAtPos = tHrp.Position

            if copyMoveMode == "Mirror" then
                idealPos = tHrp.Position + (tHrp.CFrame.LookVector * copyMoveDistance)
                lookAtPos = tHrp.Position
            elseif copyMoveMode == "Follow" then
                idealPos = tHrp.Position - (tHrp.CFrame.LookVector * copyMoveDistance)
                lookAtPos = idealPos + tHrp.CFrame.LookVector
            elseif copyMoveMode == "Exact" then
                idealPos = tHrp.Position + (tHrp.CFrame.RightVector * copyMoveDistance)
                lookAtPos = idealPos + tHrp.CFrame.LookVector
            end

            local distError = (idealPos - myHrp.Position).Magnitude
            if distError > 12 then
                myHrp.CFrame = CFrame.lookAt(idealPos, Vector3.new(lookAtPos.X, idealPos.Y, lookAtPos.Z))
            else
                local offset = (idealPos - myHrp.Position)
                local horizontalOffset = Vector3.new(offset.X, 0, offset.Z)
                if horizontalOffset.Magnitude > 0.25 then
                    myHum:Move(horizontalOffset.Unit, false)
                else
                    myHum:Move(Vector3.zero, false)
                end
                local targetRot = CFrame.lookAt(myHrp.Position, Vector3.new(lookAtPos.X, myHrp.Position.Y, lookAtPos.Z))
                myHrp.CFrame = myHrp.CFrame:Lerp(targetRot, math.clamp(dt * 15, 0, 1))
            end
        end)
        return true
    end

    
    local stalkerRunning = false
    local stalkerThread = nil
    local lastRelocateTime = 0
    local isRelocating = false

    local function findGroundLevel(pos, ignoreList)
        local rayParams = RaycastParams.new()
        rayParams.FilterType = Enum.RaycastFilterType.Exclude
        rayParams.FilterDescendantsInstances = ignoreList or {}
        local res = workspace:Raycast(pos + Vector3.new(0, 10, 0), Vector3.new(0, -60, 0), rayParams)
        if res then
            return res.Position.Y
        end
        return pos.Y
    end

    local function calculateStalkPosition(tHrp, myChar)
        local tPos = tHrp.Position
        local tCF = tHrp.CFrame
        local chosenMode = stalkPositionMode

        if chosenMode == "Dynamic" then
            local modes = { "Behind", "Rooftops", "Corners" }
            chosenMode = modes[math.random(1, #modes)]
        end

        local candidate = tPos
        local ignore = { myChar, tHrp.Parent }

        if chosenMode == "Behind" then
            local angle = math.rad(math.random(-50, 50))
            local dist = stalkDistance + math.random(-4, 6)
            local offset = CFrame.Angles(0, angle, 0) * Vector3.new(0, 0, dist)
            candidate = tCF:PointToWorldSpace(offset)
            local groundY = findGroundLevel(candidate, ignore)
            candidate = Vector3.new(candidate.X, groundY + 3, candidate.Z)

        elseif chosenMode == "Rooftops" then
            local foundElevated = false
            for attempt = 1, 8 do
                local ang = math.rad(math.random(0, 360))
                local dist = stalkDistance + math.random(-5, 10)
                local sample = tPos + Vector3.new(math.sin(ang) * dist, 0, math.cos(ang) * dist)
                local highY = findGroundLevel(sample + Vector3.new(0, 25, 0), ignore)
                if (highY - tPos.Y) >= 10 then
                    candidate = Vector3.new(sample.X, highY + 3, sample.Z)
                    foundElevated = true
                    break
                end
            end
            if not foundElevated then
                local offset = Vector3.new(math.random(-15, 15), 0, stalkDistance)
                candidate = tCF:PointToWorldSpace(offset)
                local groundY = findGroundLevel(candidate, ignore)
                candidate = Vector3.new(candidate.X, groundY + 3, candidate.Z)
            end

        elseif chosenMode == "Corners" then
            local side = (math.random(1, 2) == 1) and 1 or -1
            local offset = Vector3.new(side * (stalkDistance * 0.7), 0, (stalkDistance * 0.8))
            candidate = tCF:PointToWorldSpace(offset)
            local groundY = findGroundLevel(candidate, ignore)
            candidate = Vector3.new(candidate.X, groundY + 3, candidate.Z)
        end

        return candidate
    end

    local function isTargetLookingAtUs(tChar, myChar)
        local tHead = tChar:FindFirstChild("Head") or tChar:FindFirstChild("HumanoidRootPart")
        local myHrp = myChar:FindFirstChild("HumanoidRootPart")
        if not tHead or not myHrp then return false end

        local toUs = (myHrp.Position - tHead.Position)
        local dist = toUs.Magnitude
        if dist < 0.1 then return false end
        local dir = toUs.Unit

        local lookDot = tHead.CFrame.LookVector:Dot(dir)
        if lookDot > 0.45 then
            local rayParams = RaycastParams.new()
            rayParams.FilterType = Enum.RaycastFilterType.Exclude
            rayParams.FilterDescendantsInstances = { tChar, myChar }
            local res = workspace:Raycast(tHead.Position, toUs, rayParams)
            if not res or (res.Instance and res.Instance:IsDescendantOf(myChar)) then
                return true
            end
        end
        return false
    end

    local function relocateStalker(tHrp, myChar)
        if isRelocating then return end
        isRelocating = true
        local myHrp = myChar:FindFirstChild("HumanoidRootPart")
        if not myHrp then isRelocating = false; return end

        local newPos = calculateStalkPosition(tHrp, myChar)
        myHrp.CFrame = CFrame.lookAt(newPos, Vector3.new(tHrp.Position.X, newPos.Y, tHrp.Position.Z))
        myHrp.AssemblyLinearVelocity = Vector3.zero
        lastRelocateTime = tick()
        task.wait(0.2)
        isRelocating = false
    end

    local function startStalker()
        stalkerRunning = true
        lastRelocateTime = 0
        isRelocating = false

        stalkerThread = task.spawn(function()
            while stalkerRunning do
                local target = resolveTarget()
                local myChar = LocalPlayer.Character
                local myHrp = myChar and myChar:FindFirstChild("HumanoidRootPart")
                local myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")

                if target and target.Character and myHrp and myHum then
                    local tChar = target.Character
                    local tHrp = tChar:FindFirstChild("HumanoidRootPart")
                    if tHrp then
                        local dist = (myHrp.Position - tHrp.Position).Magnitude

                        if dist > 110 or dist < 8 or lastRelocateTime == 0 then
                            relocateStalker(tHrp, myChar)
                        else
                            myHrp.CFrame = CFrame.lookAt(myHrp.Position, Vector3.new(tHrp.Position.X, myHrp.Position.Y, tHrp.Position.Z))
                            myHum:Move(Vector3.zero, false)

                            if stalkFleeOnSight and not isRelocating then
                                if isTargetLookingAtUs(tChar, myChar) then
                                    task.wait(0.15)
                                    if stalkerRunning and target.Character then
                                        relocateStalker(tHrp, myChar)
                                    end
                                end
                            end

                            if tick() - lastRelocateTime > 16 and not isRelocating then
                                relocateStalker(tHrp, myChar)
                            end
                        end
                    end
                end
                task.wait(0.08)
            end
        end)
    end

    local function stopStalker()
        stalkerRunning = false
        if stalkerThread then
            pcall(function() task.cancel(stalkerThread) end)
            stalkerThread = nil
        end
        isRelocating = false
    end

    
    TrollV2Tab:AddSection({ "> Stalker & Mimic" })

    local function getPlayerList()
        local list = {}
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                table.insert(list, p.Name)
            end
        end
        return list
    end

    local stalkTargetDropdown = TrollV2Tab:AddDropdown({
        Name = "Stalk Target",
        Description = "Target player for Mimic and Stalker features",
        Options = getPlayerList(),
        Default = _G.TrollTarget or "---",
        Callback = function(val)
            stalkSelectedTarget = val
            _G.StalkTarget = val
        end
    })

    TrollV2Tab:AddButton({
        Name = "Refresh Player List",
        Description = "Update the player selection dropdown",
        Callback = function()
            local opts = getPlayerList()
            pcall(function() stalkTargetDropdown:Set(opts) end)
        end
    })

    _G.BTR_CopyChatToggle = TrollV2Tab:AddToggle({
        Name = "Copy Chat",
        Description = "Automatically repeats anything the target says in chat",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            copyChatEnabled = val
            if val then
                local target = resolveTarget()
                if not target then
                    notify("Copy Chat", "Select a target first")
                    pcall(function() _G.BTR_CopyChatToggle:Set(false) end)
                    copyChatEnabled = false
                    return
                end

                
                if _G.BTR_AUTOLOAD_OUTFIT then
                    pcall(function()
                        local slotToLoad = tonumber(_G.BTR_AUTOLOAD_SLOT) or 3
                        local RS = game:GetService("ReplicatedStorage")
                        local remotes = RS:FindFirstChild("Remotes")
                        local loadRemote = remotes and remotes:FindFirstChild("LoadOutfit")
                        if loadRemote then
                            loadRemote:InvokeServer(slotToLoad)
                        end
                    end)
                end

                notify("Copy Chat", "Enabled for " .. target.Name)
            else
                notify("Copy Chat", "Disabled")
            end
        end
    })

    _G.BTR_CopyMoveToggle = TrollV2Tab:AddToggle({
        Name = "Copy Movement",
        Description = "Mirrors or follows target movement in real-time",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            copyMoveEnabled = val
            if val then
                local ok = startCopyMovement()
                if not ok then
                    pcall(function() _G.BTR_CopyMoveToggle:Set(false) end)
                    copyMoveEnabled = false
                else
                    local target = resolveTarget()
                    notify("Copy Movement", "Active on " .. (target and target.Name or "target"))
                end
            else
                stopCopyMovement()
                notify("Copy Movement", "Disabled")
            end
        end
    })

    TrollV2Tab:AddDropdown({
        Name = "Movement Mode",
        Description = "Mirror (facing reflection), Follow (bodyguard), Exact (synchronous)",
        Options = { "Mirror", "Follow", "Exact" },
        Default = "Mirror",
        Callback = function(val)
            copyMoveMode = val
        end
    })

    TrollV2Tab:AddSlider({
        Name = "Movement Distance",
        Description = "Distance kept from the target",
        Min = 3,
        Max = 25,
        Default = 6,
        Suffix = " studs",
        Callback = function(val)
            copyMoveDistance = val
        end
    })

    _G.BTR_StalkerToggle = TrollV2Tab:AddToggle({
        Name = "Stalker Mode",
        Description = "Haunts the target from creepy vantage points and flees when seen",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            stalkerEnabled = val
            if val then
                local target = resolveTarget()
                if not target then
                    notify("Stalker Mode", "Select a target first")
                    pcall(function() _G.BTR_StalkerToggle:Set(false) end)
                    stalkerEnabled = false
                    return
                end
                startStalker()
                notify("Stalker Mode", "Stalking " .. target.Name)
            else
                stopStalker()
                notify("Stalker Mode", "Disabled")
            end
        end
    })

    TrollV2Tab:AddDropdown({
        Name = "Stalk Position",
        Description = "Where the stalker lurks relative to the target",
        Options = { "Dynamic", "Behind", "Rooftops", "Corners" },
        Default = "Dynamic",
        Callback = function(val)
            stalkPositionMode = val
        end
    })

    TrollV2Tab:AddSlider({
        Name = "Stalk Distance",
        Description = "Distance from target while lurking",
        Min = 15,
        Max = 70,
        Default = 30,
        Suffix = " studs",
        Callback = function(val)
            stalkDistance = val
        end
    })

    TrollV2Tab:AddToggle({
        Name = "Flee on Sight",
        Description = "Instantly vanish to a new spot when the target looks at you",
        Default = true,
        Callback = function(val)
            stalkFleeOnSight = val
        end
    })
end
_btrInitStalkerMimic()

local function _btrInitDoorOrbit()
    TrollV2Tab:AddSection({ "> Door Fling / Orbit" })

    local Players = game:GetService("Players")
    local RunService = game:GetService("RunService")
    local Workspace = game:GetService("Workspace")
    local StarterGui = game:GetService("StarterGui")
    local LocalPlayer = Players.LocalPlayer

    local isRunning = false
    local controlledDoors = {}
    local connections = {}
    local blackHolePart = nil
    local baseAttachment = nil

    local function notify(title, text)
        pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = title,
                Text = text,
                Duration = 3
            })
        end)
    end

    local function cleanDoor(part)
        pcall(function()
            if not part or not part.Parent then return end
            part.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            part.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
            part.CanCollide = false
            for _, child in ipairs(part:GetChildren()) do
                if child:IsA("AlignPosition") or child:IsA("Torque") or child:IsA("Attachment") or child.Name == "Luscaa_Attached" then
                    child:Destroy()
                end
            end
            task.delay(0.8, function()
                pcall(function()
                    if part and part.Parent and not isRunning then
                        part.CanCollide = true
                    end
                end)
            end)
        end)
    end

    local function setupDoor(part)
        if not isRunning or not baseAttachment or not baseAttachment.Parent then return end
        if not part:IsA("BasePart") or not string.find(part.Name:lower(), "door") then return end
        if part:FindFirstChild("Luscaa_Attached") then return end

        pcall(function()
            part.CanCollide = false
            part.Anchored = false
            part:BreakJoints()
        end)

        for _, obj in ipairs(part:GetChildren()) do
            if obj:IsA("AlignPosition") or obj:IsA("Torque") or obj:IsA("Attachment") then
                obj:Destroy()
            end
        end

        local marker = Instance.new("BoolValue")
        marker.Name = "Luscaa_Attached"
        marker.Parent = part

        local doorAttachment = Instance.new("Attachment")
        doorAttachment.Name = "DoorAttachment"
        doorAttachment.Parent = part

        local align = Instance.new("AlignPosition")
        align.Attachment0 = doorAttachment
        align.Attachment1 = baseAttachment
        align.MaxForce = 1e20
        align.MaxVelocity = math.huge
        align.Responsiveness = 99999
        align.Parent = part

        local torque = Instance.new("Torque")
        torque.Attachment0 = doorAttachment
        torque.RelativeTo = Enum.ActuatorRelativeTo.World
        torque.Torque = Vector3.new(
            math.random(-10e5, 10e5) * 10000,
            math.random(-10e5, 10e5) * 10000,
            math.random(-10e5, 10e5) * 10000
        )
        torque.Parent = part

        table.insert(controlledDoors, { Part = part, Align = align })
    end

    local function stopDoorOrbit()
        isRunning = false
        for _, conn in ipairs(connections) do
            if conn and conn.Connected then
                conn:Disconnect()
            end
        end
        connections = {}

        local char = LocalPlayer.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if hrp then
            hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            hrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
        end

        for _, data in ipairs(controlledDoors) do
            cleanDoor(data.Part)
        end
        controlledDoors = {}

        if blackHolePart and blackHolePart.Parent then
            blackHolePart:Destroy()
            blackHolePart = nil
        end
        baseAttachment = nil

        task.spawn(function()
            local start = tick()
            while tick() - start < 0.5 do
                if hrp and hrp.Parent then
                    hrp.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                    hrp.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                end
                RunService.Heartbeat:Wait()
            end
        end)

        notify("Door Orbit", "Disabled. Doors safely released")
    end

    local function startDoorOrbit()
        local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        local hrp = char:WaitForChild("HumanoidRootPart")
        isRunning = true

        pcall(function()
            sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
            sethiddenproperty(LocalPlayer, "MaximumSimulationRadius", math.huge)
        end)

        blackHolePart = Instance.new("Part")
        blackHolePart.Size = Vector3.new(100000, 100000, 100000)
        blackHolePart.Transparency = 1
        blackHolePart.Anchored = true
        blackHolePart.CanCollide = false
        blackHolePart.Name = "BlackHoleTarget"
        blackHolePart.CFrame = hrp.CFrame
        blackHolePart.Parent = Workspace

        baseAttachment = Instance.new("Attachment")
        baseAttachment.Name = "Luscaa_BlackHoleAttachment"
        baseAttachment.Parent = blackHolePart

        local hbConn = RunService.Heartbeat:Connect(function()
            if not isRunning or not blackHolePart or not blackHolePart.Parent then return end
            pcall(function()
                sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge)
                sethiddenproperty(LocalPlayer, "MaximumSimulationRadius", math.huge)
            end)
            local currentChar = LocalPlayer.Character
            local currentHRP = currentChar and currentChar:FindFirstChild("HumanoidRootPart")
            if currentHRP then
                blackHolePart.CFrame = currentHRP.CFrame
            end
        end)
        table.insert(connections, hbConn)

        for _, obj in ipairs(Workspace:GetDescendants()) do
            if obj:IsA("BasePart") and string.find(obj.Name:lower(), "door") then
                setupDoor(obj)
            end
        end

        local descConn = Workspace.DescendantAdded:Connect(function(obj)
            if isRunning and obj:IsA("BasePart") and string.find(obj.Name:lower(), "door") then
                task.wait(0.1)
                setupDoor(obj)
            end
        end)
        table.insert(connections, descConn)

        notify("Door Orbit", "Enabled! Doors are orbiting around you")
    end

    _G.BTR_StopDoorOrbit = stopDoorOrbit

    LocalPlayer.CharacterAdded:Connect(function()
        if isRunning then
            stopDoorOrbit()
            if _G.BTR_DoorOrbitToggle then
                pcall(function() _G.BTR_DoorOrbitToggle:Set(false) end)
            end
        end
    end)

    _G.BTR_DoorOrbitToggle = TrollV2Tab:AddToggle({
        Name = "Door Orbit Fling",
        Description = "Controls map doors to orbit around you and fling nearby players",
        Default = false,
        Callback = function(val)
            if not _G.BTR_UI_READY then return end
            if val then
                startDoorOrbit()
            else
                stopDoorOrbit()
            end
        end
    })
end
_btrInitDoorOrbit()

local function _btrInitOutdoorBillboard()
    TrollV2Tab:AddSection({ "> Outdoor Billboard" })

    local Players = game:GetService("Players")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Workspace = game:GetService("Workspace")
    local StarterGui = game:GetService("StarterGui")
    local LocalPlayer = Players.LocalPlayer
    local Remotes = ReplicatedStorage:FindFirstChild("RE") or ReplicatedStorage

    
    local BILLBOARD_1 = CFrame.new(-242.68, 89.69, -549.65)
    local BILLBOARD_2 = CFrame.new(-630.48, 26.59, 365.14)

    local isUpdating = false
    local customMessage = "BTR CLIENT // TOTAL CONTROL"

    local function notify(title, text)
        pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = title or "Billboard",
                Text = text or "",
                Duration = 3
            })
        end)
    end

    local function getRoot()
        local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        return char and char:FindFirstChild("HumanoidRootPart")
    end

    local function fireBillboardRemotes(text)
        if not Remotes then return end
        local remCemetery = Remotes:FindFirstChild("1Cemeter1y")
        if remCemetery then
            for spot = 1, 3 do
                pcall(function()
                    remCemetery:FireServer("ReturningCommercialWords", spot, nil, text)
                end)
                pcall(function()
                    remCemetery:FireServer("CommercialWordColor", spot, Color3.fromRGB(255, 255, 255))
                end)
                pcall(function()
                    remCemetery:FireServer("CommercialBackGround", spot, Color3.fromRGB(0, 0, 0))
                end)
            end
        end
        local remCommercial = Remotes:FindFirstChild("1Commercial1s") or Remotes:FindFirstChild("1Commercial")
        if remCommercial then
            for spot = 1, 3 do
                pcall(function() remCommercial:FireServer("ReturningCommercialWords", spot, nil, text) end)
                pcall(function() remCommercial:FireServer("BillboardText", spot, text) end)
            end
        end
        local remSign = Remotes:FindFirstChild("1Sign")
        if remSign then
            pcall(function() remSign:FireServer("BillboardText", text) end)
        end
    end

    local function runBillboardSequence(message)
        if isUpdating then
            notify("Billboard", "Sequence already in progress, please wait...")
            return
        end

        local root = getRoot()
        if not root then
            notify("Billboard", "Character or HumanoidRootPart not found!")
            return
        end

        isUpdating = true
        task.spawn(function()
            local origCF = root.CFrame
            notify("Billboard", "Starting cycle: Teleporting to Billboard 1...")

            
            pcall(function()
                root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                root.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                root.CFrame = BILLBOARD_1 + Vector3.new(0, 3, 0)
            end)
            task.wait(0.5)
            fireBillboardRemotes(message)
            task.wait(0.5)

            
            notify("Billboard", "Teleporting to Billboard 2...")
            pcall(function()
                root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                root.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                root.CFrame = BILLBOARD_2 + Vector3.new(0, 3, 0)
            end)
            task.wait(0.5)
            fireBillboardRemotes(message)
            task.wait(0.5)

            
            pcall(function()
                root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
                root.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
                root.CFrame = origCF
            end)
            notify("Billboard", "Billboards updated on server & returned to initial position!")
            isUpdating = false
        end)
    end

    _G.BTR_StopBillboardSequence = function()
        isUpdating = false
    end

    
    local PRESET_1 = "BTR CLIENT // TOTAL CONTROL"
    local PRESET_2 = "THE MATRIX HAS YOU // BTR"
    local PRESET_3 = "ERROR 404: REALITY NOT FOUND"

    TrollV2Tab:AddButton({
        Name = "Preset: TOTAL CONTROL",
        Description = "Applies '" .. PRESET_1 .. "' on Highway & Lake, then returns",
        Callback = function()
            runBillboardSequence(PRESET_1)
        end
    })

    TrollV2Tab:AddButton({
        Name = "Preset: THE MATRIX",
        Description = "Applies '" .. PRESET_2 .. "' on Highway & Lake, then returns",
        Callback = function()
            runBillboardSequence(PRESET_2)
        end
    })

    TrollV2Tab:AddButton({
        Name = "Preset: ERROR 404",
        Description = "Applies '" .. PRESET_3 .. "' on Highway & Lake, then returns",
        Callback = function()
            runBillboardSequence(PRESET_3)
        end
    })

    TrollV2Tab:AddTextBox({
        Name = "Custom Billboard Message",
        PlaceholderText = "Enter custom message...",
        Default = "",
        Callback = function(text)
            if text and text ~= "" then
                customMessage = text
            end
        end
    })

    TrollV2Tab:AddButton({
        Name = "Apply Custom Billboard",
        Description = "Applies custom message on Highway & Lake, then returns",
        Callback = function()
            if customMessage and customMessage ~= "" then
                runBillboardSequence(customMessage)
            else
                notify("Billboard", "Please enter a custom message first!")
            end
        end
    })
end
_btrInitOutdoorBillboard()

local function _btrInitStealItems()
    TrollV2Tab:AddSection({ "> Steal Items" })

    local Players = game:GetService("Players")
    local Workspace = game:GetService("Workspace")
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local StarterGui = game:GetService("StarterGui")
    local LocalPlayer = Players.LocalPlayer

    local Remotes = ReplicatedStorage:FindFirstChild("RE") or ReplicatedStorage
    local RemoteTool = Remotes and Remotes:FindFirstChild("1Too1l")

    local function notify(title, text)
        pcall(function()
            StarterGui:SetCore("SendNotification", {
                Title = title or "Steal Items",
                Text = text or "",
                Duration = 3
            })
        end)
    end

    local function getRoot()
        local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
        return char and char:FindFirstChild("HumanoidRootPart")
    end

    local selectedStealTarget = "---"

    local function getPlayerNames()
        local list = {"---"}
        for _, p in ipairs(Players:GetPlayers()) do
            if p ~= LocalPlayer then
                table.insert(list, p.Name)
            end
        end
        return list
    end

    local function getTargetPlayer()
        if selectedStealTarget and selectedStealTarget ~= "---" then
            local p = Players:FindFirstChild(selectedStealTarget)
            if p then return p end
        end
        if _G.TrollTarget and _G.TrollTarget ~= "---" then
            local p = Players:FindFirstChild(_G.TrollTarget)
            if p then return p end
        end
        return nil
    end

    local stealDropdown = TrollV2Tab:AddDropdown({
        Name = "Target Player",
        Description = "Choose the player to steal items from",
        Options = getPlayerNames(),
        Default = "---",
        Callback = function(val)
            selectedStealTarget = val
        end
    })

    Players.PlayerAdded:Connect(function()
        pcall(function()
            if stealDropdown and stealDropdown.SetOptions then
                stealDropdown:SetOptions(getPlayerNames())
            end
        end)
    end)
    Players.PlayerRemoving:Connect(function()
        pcall(function()
            if stealDropdown and stealDropdown.SetOptions then
                stealDropdown:SetOptions(getPlayerNames())
            end
        end)
    end)

    
    TrollV2Tab:AddButton({
        Name = "Steal Items (Target Player)",
        Description = "Clones and steals all tools from selected target player",
        Callback = function()
            local target = getTargetPlayer()
            if not target or not target.Character then
                notify("Steal Items", "Please select a target player in the dropdown above!")
                return
            end
            local count = 0
            if RemoteTool then
                for _, t in ipairs(target.Character:GetChildren()) do
                    if t:IsA("Tool") then
                        pcall(function() RemoteTool:InvokeServer("PickingTools", t.Name) end)
                        count = count + 1
                    end
                end
                if target:FindFirstChild("Backpack") then
                    for _, t in ipairs(target.Backpack:GetChildren()) do
                        if t:IsA("Tool") then
                            pcall(function() RemoteTool:InvokeServer("PickingTools", t.Name) end)
                            count = count + 1
                        end
                    end
                end
            end
            notify("Steal Items", "Stole " .. count .. " items from " .. target.Name)
        end
    })

    
    TrollV2Tab:AddButton({
        Name = "Steal Items (All Players & Map)",
        Description = "Vacuums dropped map tools and clones all player inventory tools",
        Callback = function()
            local count = 0
            local root = getRoot()
            for _, obj in ipairs(Workspace:GetChildren()) do
                if obj:IsA("Tool") and obj:FindFirstChild("Handle") and root then
                    pcall(function()
                        if firetouchinterest then
                            firetouchinterest(root, obj.Handle, 0)
                            firetouchinterest(root, obj.Handle, 1)
                        else
                            obj.Handle.CFrame = root.CFrame
                        end
                        count = count + 1
                    end)
                end
            end
            if RemoteTool then
                for _, p in ipairs(Players:GetPlayers()) do
                    if p ~= LocalPlayer and p.Character then
                        for _, t in ipairs(p.Character:GetChildren()) do
                            if t:IsA("Tool") then
                                pcall(function() RemoteTool:InvokeServer("PickingTools", t.Name) end)
                                count = count + 1
                            end
                        end
                        if p:FindFirstChild("Backpack") then
                            for _, t in ipairs(p.Backpack:GetChildren()) do
                                if t:IsA("Tool") then
                                    pcall(function() RemoteTool:InvokeServer("PickingTools", t.Name) end)
                                    count = count + 1
                                end
                            end
                        end
                    end
                end
            end
            notify("Steal Items", "Stole total " .. count .. " items across all players & map!")
        end
    })
end
_btrInitStealItems()

_G.BTR_UI_READY = true

task.spawn(function()
    task.wait(0.6)
    if _G.syncAutoProtections then
        pcall(_G.syncAutoProtections)
    end
end)

task.spawn(function()
    task.wait(1.2)
    pcall(function()
        local RE = game:GetService("ReplicatedStorage"):FindFirstChild("RE")
        local nameRemote = RE and RE:FindFirstChild("1RPNam1eTex1t")
        if nameRemote then
            nameRemote:FireServer("RolePlayName", "『 ѢҬԄ X ҪԒЇԐԊҬ 』")
        end
    end)
end)

local function initVisualSuite()
    local playersService = game:GetService("Players")
    local textChatService = game:GetService("TextChatService")
    local localPlr = playersService.LocalPlayer or playersService:FindFirstChildOfClass("Player")
    local visualConnections = {}
    local blacklistedKickedPlayers = {}
    local espVaultObjects = {}
    local espPlayerObjects = {}
    local espVaultsActive = false
    local espPlayersActive = false
    local verifiedBadgeActive = false
    local playerEspThread = nil

    local verifiedTag = nil
    local verifiedCharConn = nil
    local verifiedNameConn = nil
    local originalDisplayDistance = nil

    local announcementTitle = "SYSTEM ANNOUNCEMENT"
    local announcementBody = "Server update scheduled in 5 minutes."
    local selectedKickTarget = ""

    local function safeVisualNotify(title, text, duration)
        pcall(function()
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = title or "Visual Suite",
                Text = text or "",
                Duration = duration or 3
            })
        end)
    end

    
    
    
    local currentAnnouncementGui = nil
    local function showFakeAnnouncement(titleText, bodyText)
        local playersService = game:GetService("Players")
        local localPlr = playersService.LocalPlayer
        local coreGuiService = game:GetService("CoreGui")
        local tweenService = game:GetService("TweenService")
        local soundService = game:GetService("SoundService")

        if currentAnnouncementGui and currentAnnouncementGui.Parent then
            pcall(function() currentAnnouncementGui:Destroy() end)
            currentAnnouncementGui = nil
        end

        local sg = Instance.new("ScreenGui")
        sg.Name = "BTR_FakeAnnouncement"
        sg.ResetOnSpawn = false
        pcall(function() sg.Parent = coreGuiService end)
        if not sg.Parent and localPlr then
            sg.Parent = localPlr:FindFirstChild("PlayerGui")
        end
        currentAnnouncementGui = sg

        local banner = Instance.new("Frame")
        banner.Size = UDim2.new(0.5, 0, 0, 78)
        banner.Position = UDim2.new(0.25, 0, -0.2, 0)
        banner.BackgroundColor3 = Color3.fromRGB(15, 17, 24)
        banner.BackgroundTransparency = 0.12
        banner.BorderSizePixel = 0
        banner.Parent = sg

        local corner = Instance.new("UICorner", banner)
        corner.CornerRadius = UDim.new(0, 10)

        local stroke = Instance.new("UIStroke", banner)
        stroke.Color = Color3.fromRGB(255, 190, 40)
        stroke.Thickness = 1.8

        local icon = Instance.new("ImageLabel", banner)
        icon.Size = UDim2.new(0, 34, 0, 34)
        icon.Position = UDim2.new(0, 14, 0.5, -17)
        icon.BackgroundTransparency = 1
        icon.Image = "rbxassetid://10709791437"
        icon.ImageColor3 = Color3.fromRGB(255, 190, 40)

        local tLabel = Instance.new("TextLabel", banner)
        tLabel.Size = UDim2.new(1, -62, 0, 24)
        tLabel.Position = UDim2.new(0, 56, 0, 10)
        tLabel.BackgroundTransparency = 1
        tLabel.Text = string.upper(titleText or "SYSTEM ANNOUNCEMENT")
        tLabel.TextColor3 = Color3.fromRGB(255, 200, 50)
        tLabel.Font = Enum.Font.GothamBold
        tLabel.TextSize = 14
        tLabel.TextXAlignment = Enum.TextXAlignment.Left

        local bLabel = Instance.new("TextLabel", banner)
        bLabel.Size = UDim2.new(1, -62, 0, 34)
        bLabel.Position = UDim2.new(0, 56, 0, 34)
        bLabel.BackgroundTransparency = 1
        bLabel.Text = bodyText or "Server update scheduled in 5 minutes."
        bLabel.TextColor3 = Color3.fromRGB(240, 240, 245)
        bLabel.Font = Enum.Font.GothamMedium
        bLabel.TextSize = 12
        bLabel.TextWrapped = true
        bLabel.TextXAlignment = Enum.TextXAlignment.Left

        local s = Instance.new("Sound")
        s.SoundId = "rbxassetid://9069609200"
        s.Volume = 0.85
        s.Parent = soundService
        s:Play()

        local tweenIn = tweenService:Create(banner, TweenInfo.new(0.5, Enum.EasingStyle.Quart, Enum.EasingDirection.Out), {
            Position = UDim2.new(0.25, 0, 0.05, 0)
        })
        tweenIn:Play()

        task.delay(5.0, function()
            if banner and banner.Parent then
                local tweenOut = tweenService:Create(banner, TweenInfo.new(0.45, Enum.EasingStyle.Quart, Enum.EasingDirection.In), {
                    Position = UDim2.new(0.25, 0, -0.2, 0)
                })
                tweenOut:Play()
                task.wait(0.5)
                pcall(function() sg:Destroy() end)
                pcall(function() s:Destroy() end)
                if currentAnnouncementGui == sg then currentAnnouncementGui = nil end
            end
        end)
    end

    
    
    
    local function fakeKickPlayer(targetName)
        local playersService = game:GetService("Players")
        local textChatService = game:GetService("TextChatService")
        local coreGui = game:GetService("CoreGui")
        local ws = game:GetService("Workspace")

        local target = nil
        if type(targetName) == "string" and targetName ~= "" then
            local lowerQuery = string.lower(targetName)
            for _, plr in ipairs(playersService:GetPlayers()) do
                if string.lower(plr.Name) == lowerQuery or string.lower(plr.DisplayName) == lowerQuery then
                    target = plr
                    break
                end
            end
            if not target then
                for _, plr in ipairs(playersService:GetPlayers()) do
                    if string.find(string.lower(plr.Name), lowerQuery) or string.find(string.lower(plr.DisplayName), lowerQuery) then
                        target = plr
                        break
                    end
                end
            end
        end

        if not target then
            safeVisualNotify("Fake Kick", "Target player not found!", 2)
            return
        end

        local tName = target.Name
        local tDisplayName = target.DisplayName
        blacklistedKickedPlayers[tName] = true

        
        if target.Character then
            pcall(function() target.Character:Destroy() end)
        end

        local charConn = target.CharacterAdded:Connect(function(newChar)
            if blacklistedKickedPlayers[tName] then
                task.wait()
                pcall(function() newChar:Destroy() end)
            end
        end)
        table.insert(visualConnections, charConn)

        local wsConn = ws.ChildAdded:Connect(function(child)
            if blacklistedKickedPlayers[tName] and child.Name == tName then
                task.wait()
                pcall(function() child:Destroy() end)
            end
        end)
        table.insert(visualConnections, wsConn)

        
        local function purgeFromPlayerList()
            pcall(function()
                local playerList = coreGui:FindFirstChild("PlayerList")
                if playerList then
                    for _, item in ipairs(playerList:GetDescendants()) do
                        if item:IsA("TextLabel") and (item.Text == tName or item.Text == tDisplayName) then
                            local row = item:FindFirstAncestorWhichIsA("Frame") or item.Parent
                            if row then
                                row.Visible = false
                                pcall(function() row:Destroy() end)
                            end
                        end
                    end
                end
            end)
        end
        purgeFromPlayerList()
        task.delay(0.5, purgeFromPlayerList)
        task.delay(1.5, purgeFromPlayerList)

        
        
        showFakeAnnouncement("MODERATION LOG", tDisplayName .. " (@" .. tName .. ") was kicked from the server.")
        safeVisualNotify("Player Purged", tDisplayName .. " (@" .. tName .. ") is completely erased from your game (3D & TAB)!", 4)
    end

    
    
    
    local VERIFIED_EMOJI = utf8.char(0xE000)

    local function formatVerifiedChatPrefix(originalPrefix, defaultName)
        local prefix = (originalPrefix and originalPrefix ~= "") and originalPrefix or (defaultName .. ":")
        prefix = prefix:gsub(VERIFIED_EMOJI, "")
        local beforeColon, afterColon = prefix:match("^(.-)(%s*:%s*)$")
        if beforeColon and afterColon then
            return beforeColon .. " " .. VERIFIED_EMOJI .. afterColon
        else
            return prefix .. " " .. VERIFIED_EMOJI .. ":"
        end
    end

    local verifiedChatConnections = {}
    local isPatchingText = false

    local function patchChatTextLabel(lbl)
        if not verifiedBadgeActive or isPatchingText then return end
        if not lbl or (not lbl:IsA("TextLabel") and not lbl:IsA("TextButton")) then return end

        local txt = lbl.Text
        if not txt or txt == "" then return end
        if txt:find(VERIFIED_EMOJI, 1, true) then return end

        local myName = localPlr and localPlr.Name
        local myDisp = localPlr and localPlr.DisplayName

        local isMine = false
        if myName and myName ~= "" and (txt:find(myName, 1, true) or lbl.Name:find(myName, 1, true)) then
            isMine = true
        elseif myDisp and myDisp ~= "" and (txt:find(myDisp, 1, true) or lbl.Name:find(myDisp, 1, true)) then
            isMine = true
        end

        if isMine then
            isPatchingText = true
            lbl.RichText = true
            if txt:find(":") then
                lbl.Text = txt:gsub("(:)", " " .. VERIFIED_EMOJI .. "%1", 1)
            else
                lbl.Text = txt .. " " .. VERIFIED_EMOJI
            end
            isPatchingText = false
        end
    end

    local function startSolaraChatScanner()
        stopSolaraChatScanner()

        local containers = {}
        pcall(function()
            local pg = localPlr and (localPlr:FindFirstChild("PlayerGui") or localPlr:WaitForChild("PlayerGui", 3))
            if pg then table.insert(containers, pg) end
        end)
        pcall(function()
            if gethui then table.insert(containers, gethui()) end
        end)
        pcall(function()
            table.insert(containers, game:GetService("CoreGui"))
        end)

        for _, c in ipairs(containers) do
            pcall(function()
                for _, desc in ipairs(c:GetDescendants()) do
                    if desc:IsA("TextLabel") or desc:IsA("TextButton") then
                        patchChatTextLabel(desc)
                    end
                end

                local conn = c.DescendantAdded:Connect(function(desc)
                    if not verifiedBadgeActive then return end
                    if desc:IsA("TextLabel") or desc:IsA("TextButton") then
                        task.defer(function()
                            patchChatTextLabel(desc)
                        end)
                        local textConn = desc:GetPropertyChangedSignal("Text"):Connect(function()
                            if verifiedBadgeActive and not isPatchingText then
                                patchChatTextLabel(desc)
                            end
                        end)
                        table.insert(verifiedChatConnections, textConn)
                    end
                end)
                table.insert(verifiedChatConnections, conn)
            end)
        end
    end

    local function stopSolaraChatScanner()
        for _, c in ipairs(verifiedChatConnections) do
            pcall(function() c:Disconnect() end)
        end
        verifiedChatConnections = {}
        isPatchingText = false
    end

    
    pcall(function()
        if textChatService and textChatService.ChatVersion == Enum.ChatVersion.TextChatService then
            textChatService.OnIncomingMessage = function(msg)
                if msg.TextSource and blacklistedKickedPlayers[msg.TextSource.Name] then
                    local prop = Instance.new("TextChatMessageProperties")
                    prop.Text = ""
                    return prop
                end

                if verifiedBadgeActive and msg.TextSource and localPlr and (msg.TextSource.UserId == localPlr.UserId or msg.TextSource.Name == localPlr.Name) then
                    local prop = Instance.new("TextChatMessageProperties")
                    prop.PrefixText = formatVerifiedChatPrefix(msg.PrefixText, localPlr.DisplayName or localPlr.Name)
                    return prop
                end
            end
        end
    end)

    local function getCleanPlayerDisplayName()
        local localPlr = game:GetService("Players").LocalPlayer
        if not localPlr then return "Player" end
        local name = ""
        local bag = localPlr:FindFirstChild("PlayersBag")
        if bag then
            local rpName = bag:FindFirstChild("RPName")
            if rpName and rpName.Value and tostring(rpName.Value) ~= "" and tostring(rpName.Value) ~= " " then
                name = tostring(rpName.Value)
            end
        end
        if name == "" then
            name = localPlr.DisplayName or localPlr.Name or "Player"
        end
        name = name:gsub(VERIFIED_EMOJI, ""):gsub("%s+$", "")
        return name
    end

    local function attachVerifiedBadgeToNametag(char)
        if not char then return end
        local head = char:WaitForChild("Head", 5)
        if not head then return end
        local localPlr = game:GetService("Players").LocalPlayer

        if verifiedTag then
            pcall(function() verifiedTag:Destroy() end)
            verifiedTag = nil
        end

        local baseName = getCleanPlayerDisplayName()

        
        local tag = Instance.new("BillboardGui")
        tag.Name = "BTR_VerifiedBadgeTag"
        tag.Adornee = head
        tag.Size = UDim2.new(0, 220, 0, 32)
        tag.StudsOffset = Vector3.new(0, 2.5, 0)
        tag.AlwaysOnTop = true
        tag.ResetOnSpawn = false
        tag.MaxDistance = 600
        tag.Parent = head

        local container = Instance.new("Frame", tag)
        container.Name = "BadgeContainer"
        container.Size = UDim2.new(1, 0, 1, 0)
        container.BackgroundTransparency = 1

        local nameLbl = Instance.new("TextLabel", container)
        nameLbl.Name = "NameLabel"
        nameLbl.Size = UDim2.new(1, 0, 1, 0)
        nameLbl.BackgroundTransparency = 1
        nameLbl.Text = baseName .. " " .. VERIFIED_EMOJI
        nameLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
        nameLbl.Font = Enum.Font.GothamBold
        nameLbl.TextSize = 14
        nameLbl.TextStrokeTransparency = 0.2
        nameLbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
        nameLbl.TextXAlignment = Enum.TextXAlignment.Center

        local icon = Instance.new("ImageLabel", container)
        icon.Name = "VerifiedCheckmark"
        icon.Size = UDim2.new(0, 16, 0, 16)
        icon.BackgroundTransparency = 1
        icon.Image = "rbxasset://textures/ui/PlayerList/VerifiedBadgeIcon.png"
        icon.Visible = false

        local function updateBadgeLayout()
            if not nameLbl or not nameLbl.Parent then return end
            local currentText = getCleanPlayerDisplayName()
            nameLbl.Text = currentText .. " " .. VERIFIED_EMOJI
            local halfTextWidth = math.min(nameLbl.TextBounds.X / 2, 95)
            icon.Position = UDim2.new(0.5, halfTextWidth + 4, 0.5, -8)
            icon.Visible = true
        end

        task.spawn(function()
            task.wait(0.1)
            updateBadgeLayout()
        end)

        verifiedTag = tag

        
        local bag = localPlr and localPlr:FindFirstChild("PlayersBag")
        local rpNameVal = bag and bag:FindFirstChild("RPName")
        if rpNameVal and not verifiedNameConn then
            verifiedNameConn = rpNameVal.Changed:Connect(function()
                if verifiedBadgeActive then
                    updateBadgeLayout()
                end
            end)
        end
    end

    local function toggleVerifiedBadge(state)
        verifiedBadgeActive = state
        local localPlr = game:GetService("Players").LocalPlayer

        if verifiedCharConn then
            pcall(function() verifiedCharConn:Disconnect() end)
            verifiedCharConn = nil
        end
        if verifiedNameConn then
            pcall(function() verifiedNameConn:Disconnect() end)
            verifiedNameConn = nil
        end
        if verifiedTag then
            pcall(function() verifiedTag:Destroy() end)
            verifiedTag = nil
        end

        if not state then
            stopSolaraChatScanner()
            safeVisualNotify("Verified Badge", "Verified badge removed.", 2)
            return
        end

        
        startSolaraChatScanner()

        if localPlr then
            if localPlr.Character then
                attachVerifiedBadgeToNametag(localPlr.Character)
            end
            verifiedCharConn = localPlr.CharacterAdded:Connect(function(newChar)
                if verifiedBadgeActive then
                    task.wait(0.5)
                    attachVerifiedBadgeToNametag(newChar)
                end
            end)
            table.insert(visualConnections, verifiedCharConn)
        end

        safeVisualNotify("Verified Badge", "Official verified badge active on your chat messages & overhead!", 3)
    end

    
    
    
    local function clearVaultEsp()
        for _, item in ipairs(espVaultObjects) do
            pcall(function() item:Destroy() end)
        end
        espVaultObjects = {}
    end

    local function createVaultHighlight(targetObj, labelName)
        if not targetObj then return end
        local coreGuiService = game:GetService("CoreGui")
        local localPlr = game:GetService("Players").LocalPlayer

        local hl = Instance.new("Highlight")
        hl.Name = "BTR_VaultHL"
        hl.Adornee = targetObj
        hl.FillColor = Color3.fromRGB(255, 205, 0)
        hl.FillTransparency = 0.35
        hl.OutlineColor = Color3.fromRGB(255, 255, 255)
        hl.OutlineTransparency = 0
        hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
        local ok = pcall(function() hl.Parent = coreGuiService end)
        if not ok or not hl.Parent then
            pcall(function()
                hl.Parent = (localPlr and localPlr:FindFirstChild("PlayerGui")) or targetObj
            end)
        end
        table.insert(espVaultObjects, hl)

        local part = targetObj:IsA("BasePart") and targetObj or (targetObj.PrimaryPart or targetObj:FindFirstChildWhichIsA("BasePart"))
        if part then
            local tag = Instance.new("BillboardGui")
            tag.Name = "BTR_VaultTag"
            tag.Adornee = part
            tag.Size = UDim2.new(0, 130, 0, 32)
            tag.StudsOffset = Vector3.new(0, 2.5, 0)
            tag.AlwaysOnTop = true
            tag.MaxDistance = 2500
            local tagOk = pcall(function() tag.Parent = coreGuiService end)
            if not tagOk or not tag.Parent then
                pcall(function()
                    tag.Parent = (localPlr and localPlr:FindFirstChild("PlayerGui")) or part
                end)
            end

            local frame = Instance.new("Frame", tag)
            frame.Size = UDim2.new(1, 0, 1, 0)
            frame.BackgroundColor3 = Color3.fromRGB(20, 18, 12)
            frame.BackgroundTransparency = 0.2
            Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 6)
            local stroke = Instance.new("UIStroke", frame)
            stroke.Color = Color3.fromRGB(255, 205, 0)
            stroke.Thickness = 1.2

            local lbl = Instance.new("TextLabel", frame)
            lbl.Size = UDim2.new(1, 0, 1, 0)
            lbl.BackgroundTransparency = 1
            lbl.Font = Enum.Font.GothamBold
            lbl.Text = "[ " .. tostring(labelName) .. " ]"
            lbl.TextColor3 = Color3.fromRGB(255, 220, 50)
            lbl.TextSize = 11

            table.insert(espVaultObjects, tag)
        end
    end

    local function scanAndHighlightVaults()
        clearVaultEsp()
        local ws = game:GetService("Workspace")

        local seen = {}
        for _, obj in ipairs(ws:GetDescendants()) do
            local name = obj.Name
            local lowerName = string.lower(name)
            if not seen[obj] then
                if name == "001_Safe" or name == "Safe" or name == "SafeDoor" then
                    seen[obj] = true
                    createVaultHighlight(obj, "HOUSE SAFE")
                elseif string.find(lowerName, "vault") or string.find(lowerName, "bankvault") then
                    if obj:IsA("BasePart") or obj:IsA("Model") then
                        seen[obj] = true
                        createVaultHighlight(obj, "BANK VAULT")
                    end
                end
            end
        end
    end

    local function toggleVaultEsp(state)
        espVaultsActive = state
        clearVaultEsp()
        if not state then
            safeVisualNotify("Vault ESP", "Vault ESP disabled.", 2)
            return
        end

        scanAndHighlightVaults()

        local descConn = game:GetService("Workspace").DescendantAdded:Connect(function(newObj)
            if espVaultsActive then
                local name = newObj.Name
                local lowerName = string.lower(name)
                if name == "001_Safe" or name == "Safe" or name == "SafeDoor" then
                    task.wait(0.2)
                    createVaultHighlight(newObj, "HOUSE SAFE")
                elseif string.find(lowerName, "vault") and (newObj:IsA("BasePart") or newObj:IsA("Model")) then
                    task.wait(0.2)
                    createVaultHighlight(newObj, "BANK VAULT")
                end
            end
        end)
        table.insert(visualConnections, descConn)

        safeVisualNotify("Vault ESP", "Bank Vault & House Safes highlighted across map!", 3)
    end

    
    
    
    local function clearPlayerEsp()
        for _, item in ipairs(espPlayerObjects) do
            pcall(function() item:Destroy() end)
        end
        espPlayerObjects = {}
    end

    local function applySinglePlayerEsp(plr)
        if not espPlayersActive or not plr or not plr.Character then return end
        if blacklistedKickedPlayers[plr.Name] then return end
        local coreGuiService = game:GetService("CoreGui")
        local localPlr = game:GetService("Players").LocalPlayer
        if plr == localPlr then return end

        local char = plr.Character
        local existing = char:FindFirstChild("BTR_PlayerHighlight")
        if existing then return end

        local highlight = Instance.new("Highlight")
        highlight.Name = "BTR_PlayerHighlight"
        highlight.Adornee = char
        highlight.FillColor = Color3.fromRGB(0, 255, 180)
        highlight.OutlineColor = Color3.fromRGB(0, 100, 255)
        highlight.FillTransparency = 0.5
        highlight.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop

        local ok = pcall(function() highlight.Parent = coreGuiService end)
        if not ok or not highlight.Parent then
            pcall(function()
                highlight.Parent = (localPlr and localPlr:FindFirstChild("PlayerGui")) or char
            end)
        end
        table.insert(espPlayerObjects, highlight)
    end

    local function refreshPlayerEsp()
        clearPlayerEsp()
        if not espPlayersActive then return end
        local playersService = game:GetService("Players")
        for _, plr in ipairs(playersService:GetPlayers()) do
            applySinglePlayerEsp(plr)
        end
    end

    local function togglePlayerEsp(state)
        espPlayersActive = state
        clearPlayerEsp()
        if playerEspThread then
            pcall(function() task.cancel(playerEspThread) end)
            playerEspThread = nil
        end

        if not state then
            safeVisualNotify("Player ESP", "Player ESP disabled.", 2)
            return
        end

        refreshPlayerEsp()

        playerEspThread = task.spawn(function()
            while espPlayersActive do
                task.wait(1.5)
                local playersService = game:GetService("Players")
                local localPlr = playersService.LocalPlayer
                for _, plr in ipairs(playersService:GetPlayers()) do
                    if plr ~= localPlr and plr.Character and not blacklistedKickedPlayers[plr.Name] then
                        applySinglePlayerEsp(plr)
                    end
                end
            end
        end)

        safeVisualNotify("Player ESP", "All players highlighted through walls!", 3)
    end

    
    
    
    local function fakeUnlockGamepasses()
        local localPlr = game:GetService("Players").LocalPlayer
        local pg = localPlr and localPlr:FindFirstChild("PlayerGui")
        local count = 0

        
        if hookmetamethod and getnamecallmethod then
            pcall(function()
                local oldHook; oldHook = hookmetamethod(game, "__namecall", function(self, ...)
                    local m = getnamecallmethod()
                    if m == "UserOwnsGamePassAsync" or m == "PlayerOwnsAsset" then
                        return true
                    end
                    if m == "PromptGamePassPurchase" or m == "PromptPurchase" then
                        return nil
                    end
                    return oldHook(self, ...)
                end)
            end)
        end

        
        local function processStoreItem(desc)
            if not desc or not desc.Parent then return end
            pcall(function()
                local lname = string.lower(desc.Name)

                
                if desc:IsA("TextLabel") or desc:IsA("TextButton") then
                    local rawText = desc.Text
                    local cleanDigits = rawText:gsub("%D", "")
                    local priceNum = tonumber(cleanDigits)
                    local isPrice = false

                    if priceNum and priceNum >= 15 and priceNum <= 50000 and #rawText <= 30 then
                        isPrice = true
                    end

                    local t = string.lower(rawText)
                    if string.find(t, "buy") or string.find(t, "robux") or string.find(t, "r%$") or string.find(t, "pass") or string.find(t, "acheter") then
                        isPrice = true
                    end

                    if isPrice then
                        desc.Text = "OWNED"
                        desc.TextColor3 = Color3.fromRGB(0, 255, 120)
                        count = count + 1

                        
                        local p = desc.Parent
                        if p then
                            for _, child in ipairs(p:GetChildren()) do
                                if child ~= desc and (child:IsA("ImageLabel") or child:IsA("ImageButton") or child.Name:lower():find("robux") or child.Name:lower():find("icon")) then
                                    child.Visible = false
                                end
                            end
                            if p:IsA("GuiObject") then
                                pcall(function() p.BackgroundColor3 = Color3.fromRGB(20, 160, 60) end)
                            end
                        end
                        for _, child in ipairs(desc:GetChildren()) do
                            if child:IsA("ImageLabel") or child:IsA("ImageButton") then
                                child.Visible = false
                            end
                        end
                    end

                    
                    if string.find(t, "cadeau") or string.find(t, "gift") then
                        desc.Text = "UNLOCKED"
                        desc.TextColor3 = Color3.fromRGB(180, 240, 200)
                    end
                end

                
                if desc:IsA("ImageButton") or desc:IsA("TextButton") then
                    if string.find(lname, "buy") or string.find(lname, "price") or string.find(lname, "purchase") or string.find(lname, "robux") then
                        local lbl = desc:FindFirstChildWhichIsA("TextLabel", true)
                        if lbl then
                            lbl.Text = "OWNED"
                            lbl.TextColor3 = Color3.fromRGB(0, 255, 120)
                        end
                        for _, img in ipairs(desc:GetDescendants()) do
                            if img:IsA("ImageLabel") then
                                img.Visible = false
                            end
                        end
                        pcall(function() desc.BackgroundColor3 = Color3.fromRGB(20, 160, 60) end)
                    end
                end

                
                if string.find(lname, "speed") then
                    if desc:IsA("NumberValue") or desc:IsA("IntValue") then
                        desc.Value = 120
                    end
                end

                
                if string.find(lname, "lock") or string.find(lname, "buypass") then
                    if desc:IsA("GuiObject") then
                        desc.Visible = false
                        count = count + 1
                    end
                end

                
                if desc:IsA("TextButton") or desc:IsA("ImageButton") then
                    desc.Active = true
                    desc.Selectable = true
                end
            end)
        end

        
        if pg then
            for _, desc in ipairs(pg:GetDescendants()) do
                processStoreItem(desc)
            end

            
            if not _G.BTR_StoreDescendantHook then
                _G.BTR_StoreDescendantHook = pg.DescendantAdded:Connect(function(desc)
                    task.defer(function()
                        processStoreItem(desc)
                    end)
                end)
                table.insert(visualConnections, _G.BTR_StoreDescendantHook)
            end
        end

        
        pcall(function()
            local bag = localPlr:FindFirstChild("PlayersBag")
            if bag then
                for _, v in ipairs(bag:GetChildren()) do
                    if v:IsA("BoolValue") then
                        v.Value = true
                    end
                end
            end
        end)

        safeVisualNotify("Pass Unlocker", "Gamepasses & Premium menus UNLOCKED client-side! (" .. count .. " items modified)", 4)
    end

    local function getOnlinePlayerNames()
        local playersService = game:GetService("Players")
        local localPlr = playersService.LocalPlayer
        local list = {}
        for _, plr in ipairs(playersService:GetPlayers()) do
            if plr ~= localPlr and not blacklistedKickedPlayers[plr.Name] then
                table.insert(list, plr.Name)
            end
        end
        if #list == 0 then table.insert(list, "None") end
        return list
    end

    
    local stopHackerDetector = nil

    _G.BTR_StopVisualSuite = function()
        clearVaultEsp()
        clearPlayerEsp()
        if stopHackerDetector then
            pcall(stopHackerDetector)
        end
        if playerEspThread then
            pcall(function() task.cancel(playerEspThread) end)
            playerEspThread = nil
        end
        if verifiedCharConn then
            pcall(function() verifiedCharConn:Disconnect() end)
            verifiedCharConn = nil
        end
        if verifiedNameConn then
            pcall(function() verifiedNameConn:Disconnect() end)
            verifiedNameConn = nil
        end
        if verifiedTag then
            pcall(function() verifiedTag:Destroy() end)
            verifiedTag = nil
        end
        local localPlr = game:GetService("Players").LocalPlayer
        if localPlr and localPlr.Character then
            local hum = localPlr.Character:FindFirstChildOfClass("Humanoid")
            if hum and originalDisplayDistance then
                hum.DisplayDistanceType = originalDisplayDistance
            end
        end
        for _, conn in ipairs(visualConnections) do
            pcall(function() conn:Disconnect() end)
        end
        visualConnections = {}
        espVaultsActive = false
        espPlayersActive = false
        verifiedBadgeActive = false
    end

VisualTab:AddSection({"> Fake Server Announcement"})

VisualTab:AddTextBox({
    Name = "Announcement Title",
    Description = "Header title for the server banner",
    PlaceholderText = "SYSTEM ANNOUNCEMENT",
    Callback = function(val)
        if val and val ~= "" then
            announcementTitle = tostring(val)
        end
    end
})

VisualTab:AddTextBox({
    Name = "Announcement Message",
    Description = "Content of the announcement",
    PlaceholderText = "Server update scheduled in 5 minutes.",
    Callback = function(val)
        if val and val ~= "" then
            announcementBody = tostring(val)
        end
    end
})

VisualTab:AddButton({
    Name = "Send Fake Server Announcement",
    Description = "Display animated server-wide banner and play chime",
    Callback = function()
        showFakeAnnouncement(announcementTitle, announcementBody)
    end
})

VisualTab:AddSection({"> Fake Moderation (Client Kick)"})

local kickTargetDropdownRef = nil
kickTargetDropdownRef = VisualTab:AddDropdown({
    Name = "Select Target",
    Description = "Choose an online player to purge",
    Options = getOnlinePlayerNames(),
    Default = "None",
    Callback = function(val)
        if val and val ~= "None" then
            selectedKickTarget = val
        end
    end
})

VisualTab:AddButton({
    Name = "Refresh Player List",
    Description = "Update online players in selection",
    Callback = function()
        if kickTargetDropdownRef and kickTargetDropdownRef.SetOptions then
            kickTargetDropdownRef:SetOptions(getOnlinePlayerNames())
            safeVisualNotify("Players", "Player list refreshed.", 2)
        end
    end
})

VisualTab:AddTextBox({
    Name = "Manual Target Name",
    Description = "Type username or display name directly",
    PlaceholderText = "Username or DisplayName...",
    Callback = function(val)
        if val and val ~= "" then
            selectedKickTarget = tostring(val)
        end
    end
})

VisualTab:AddButton({
    Name = "Execute Fake Kick",
    Description = "Purges target character, mutes in chat, and broadcasts ban",
    Callback = function()
        if selectedKickTarget and selectedKickTarget ~= "" and selectedKickTarget ~= "None" then
            fakeKickPlayer(selectedKickTarget)
        else
            safeVisualNotify("Fake Kick", "Please choose or type a target first!", 2)
        end
    end
})

VisualTab:AddSection({"> Verified Badge"})

VisualTab:AddToggle({
    Name = "Verified Roblox Badge",
    Description = "Displays the official Roblox blue checkmark over your head",
    Default = false,
    Callback = function(val)
        toggleVerifiedBadge(val)
    end
})

VisualTab:AddSection({"> ESP Suite"})

VisualTab:AddToggle({
    Name = "ESP Vaults & Safes",
    Description = "Highlights Bank Vault and house safes in gold through walls",
    Default = false,
    Callback = function(val)
        toggleVaultEsp(val)
    end
})

VisualTab:AddToggle({
    Name = "ESP Players",
    Description = "Highlights all other players in cyan through walls",
    Default = false,
    Callback = function(val)
        togglePlayerEsp(val)
    end
})

VisualTab:AddSection({"> Brookhaven Pass Unlocker"})

VisualTab:AddButton({
    Name = "Unlock Gamepasses (Client-Side)",
    Description = "Marks Brookhaven store buttons as OWNED",
    Callback = function()
        fakeUnlockGamepasses()
    end
})

    
    
    
    
    local plrsService = game:GetService("Players")
    local lpLocal = plrsService.LocalPlayer
    local runService = game:GetService("RunService")

    local hackerEspActive = false
    local detectedHackers = {} 
    local notifiedHackers = {} 
    local hackerHighlights = {}
    local hackerTags = {}
    local detectorThread = nil
    local distanceRenderConn = nil
    local detectorConnections = {}

    local floatCounters = {}
    local speedCounters = {}
    local spinCounters = {}
    local spawnTimes = {}

    local function getBestAdorneePart(char)
        if not char then return nil end
        return char:FindFirstChild("Head")
            or char:FindFirstChild("UpperTorso")
            or char:FindFirstChild("Torso")
            or char:FindFirstChild("HumanoidRootPart")
            or char.PrimaryPart
            or char:FindFirstChildWhichIsA("BasePart")
    end

    local function removeHackerVisual(uid)
        if hackerHighlights[uid] then
            pcall(function() hackerHighlights[uid]:Destroy() end)
            hackerHighlights[uid] = nil
        end
        if hackerTags[uid] then
            pcall(function() hackerTags[uid]:Destroy() end)
            hackerTags[uid] = nil
        end
    end

    local function applyHackerVisual(plr, hubName, cheatType)
        if not hackerEspActive or not plr or not plr.Character then return end
        local char = plr.Character
        local uid = plr.UserId
        local adornee = getBestAdorneePart(char)
        if not adornee then return end

        
        if not hackerHighlights[uid] or hackerHighlights[uid].Parent ~= char then
            pcall(function() if hackerHighlights[uid] then hackerHighlights[uid]:Destroy() end end)
            local hl = Instance.new("Highlight")
            hl.Name = "BTR_HackerHL"
            hl.Adornee = char
            hl.FillColor = Color3.fromRGB(255, 30, 30)
            hl.FillTransparency = 0.35
            hl.OutlineColor = Color3.fromRGB(255, 255, 255)
            hl.OutlineTransparency = 0
            hl.DepthMode = Enum.HighlightDepthMode.AlwaysOnTop
            hl.Parent = char
            hackerHighlights[uid] = hl
        end

        
        if not hackerTags[uid] or hackerTags[uid].Parent ~= adornee then
            pcall(function() if hackerTags[uid] then hackerTags[uid]:Destroy() end end)

            local tag = Instance.new("BillboardGui")
            tag.Name = "BTR_HackerTag"
            tag.Adornee = adornee
            tag.Size = UDim2.new(0, 230, 0, 75)
            tag.StudsOffset = Vector3.new(0, 3.2, 0)
            tag.AlwaysOnTop = true
            tag.MaxDistance = 1500
            tag.Parent = adornee

            local frame = Instance.new("Frame", tag)
            frame.Name = "TagFrame"
            frame.Size = UDim2.new(1, 0, 1, 0)
            frame.BackgroundColor3 = Color3.fromRGB(16, 18, 24)
            frame.BackgroundTransparency = 0.15
            frame.BorderSizePixel = 0
            Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 8)

            local stroke = Instance.new("UIStroke", frame)
            stroke.Color = Color3.fromRGB(255, 45, 45)
            stroke.Thickness = 1.5

            local lblHeader = Instance.new("TextLabel", frame)
            lblHeader.Size = UDim2.new(1, -10, 0, 16)
            lblHeader.Position = UDim2.new(0, 5, 0, 3)
            lblHeader.BackgroundTransparency = 1
            lblHeader.Font = Enum.Font.GothamBold
            lblHeader.Text = "[!] EXPLOIT DETECTED"
            lblHeader.TextColor3 = Color3.fromRGB(255, 65, 65)
            lblHeader.TextSize = 11

            local lblPlayer = Instance.new("TextLabel", frame)
            lblPlayer.Name = "PlayerLabel"
            lblPlayer.Size = UDim2.new(1, -10, 0, 17)
            lblPlayer.Position = UDim2.new(0, 5, 0, 19)
            lblPlayer.BackgroundTransparency = 1
            lblPlayer.Font = Enum.Font.GothamBold
            lblPlayer.Text = plr.DisplayName .. " (@" .. plr.Name .. ")"
            lblPlayer.TextColor3 = Color3.fromRGB(255, 255, 255)
            lblPlayer.TextSize = 11
            lblPlayer.TextTruncate = Enum.TextTruncate.AtEnd

            local lblInfo = Instance.new("TextLabel", frame)
            lblInfo.Name = "InfoLabel"
            lblInfo.Size = UDim2.new(1, -10, 0, 16)
            lblInfo.Position = UDim2.new(0, 5, 0, 37)
            lblInfo.BackgroundTransparency = 1
            lblInfo.Font = Enum.Font.GothamMedium
            lblInfo.Text = "Hub: " .. tostring(hubName or "None") .. " | ... studs"
            lblInfo.TextColor3 = Color3.fromRGB(255, 210, 70)
            lblInfo.TextSize = 10

            local lblCheat = Instance.new("TextLabel", frame)
            lblCheat.Name = "CheatLabel"
            lblCheat.Size = UDim2.new(1, -10, 0, 16)
            lblCheat.Position = UDim2.new(0, 5, 0, 54)
            lblCheat.BackgroundTransparency = 1
            lblCheat.Font = Enum.Font.GothamMedium
            lblCheat.Text = "Cheat: " .. tostring(cheatType or "Unknown")
            lblCheat.TextColor3 = Color3.fromRGB(255, 120, 90)
            lblCheat.TextSize = 10
            lblCheat.TextTruncate = Enum.TextTruncate.AtEnd

            hackerTags[uid] = tag
        else
            
            local frame = hackerTags[uid]:FindFirstChild("TagFrame")
            if frame then
                local lblPlayer = frame:FindFirstChild("PlayerLabel")
                if lblPlayer then lblPlayer.Text = plr.DisplayName .. " (@" .. plr.Name .. ")" end
                local lblCheat = frame:FindFirstChild("CheatLabel")
                if lblCheat then lblCheat.Text = "Cheat: " .. tostring(cheatType or "Unknown") end
            end
        end
    end

    local function detectHubSignature(plr)
        if not plr then return "None" end
        local bag = plr:FindFirstChild("PlayersBag")
        if bag then
            local rpName = bag:FindFirstChild("RPName") and tostring(bag.RPName.Value) or ""
            local rpBio = bag:FindFirstChild("RPBio") and tostring(bag.RPBio.Value) or ""
            local bioCol = bag:FindFirstChild("RPBioColor") and bag.RPBioColor.Value
            local nameCol = bag:FindFirstChild("RPNameColor") and bag.RPNameColor.Value

            if rpName == "Ez by ash" or rpBio == "Ez by ash" or string.find(rpName, "Dark Hub") or string.find(rpName, "DarkBrook") or string.find(rpBio, "Dark Hub") then
                return "Dark Hub"
            end
            if string.find(rpName, "KitK4t") or string.find(rpBio, "KitK4t") or string.find(rpName, "Slayer") then
                return "KitK4t / Slayer Hub"
            end
            if string.find(rpName, "Hexagon") or string.find(rpBio, "Hexagon") then
                return "Hexagon Hub"
            end
            if bioCol and nameCol and typeof(bioCol) == "Color3" and typeof(nameCol) == "Color3" then
                if bioCol.R > 0.9 and bioCol.G < 0.1 and bioCol.B < 0.1 and nameCol.R < 0.05 and nameCol.G < 0.05 and nameCol.B < 0.05 then
                    return "Dark Hub"
                end
            end
        end

        local char = plr.Character
        local backpack = plr:FindFirstChild("Backpack")
        if char then
            if char:FindFirstChild("Chaos.Couch") or (backpack and backpack:FindFirstChild("Chaos.Couch")) then
                return "Dark Hub"
            end
            local couch = char:FindFirstChild("Couch") or (backpack and backpack:FindFirstChild("Couch"))
            if couch and couch:FindFirstChild("Handle ") then
                return "Dark Hub"
            end
            local hrp = char:FindFirstChild("HumanoidRootPart")
            if hrp and hrp:FindFirstChild("FlingForce") then
                return "Dark Hub"
            end
        end

        return "None"
    end

    local function evaluatePlayerCheat(plr)
        if not plr or plr == lpLocal then return nil, "None" end
        local char = plr.Character
        if not char then return nil, "None" end
        local hum = char:FindFirstChildOfClass("Humanoid")
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hum or not hrp or hum.Health <= 0 then return nil, "None" end

        local hub = detectHubSignature(plr)
        local uid = plr.UserId

        
        local spawnTick = spawnTimes[uid] or 0
        if tick() - spawnTick < 3.5 then
            if hub ~= "None" then
                return "Hub User (" .. hub .. ")", hub
            end
            return nil, hub
        end

        
        local head = char:FindFirstChild("Head")
        local torso = char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso")
        if head and torso and head.Transparency >= 0.85 and torso.Transparency >= 0.85 then
            local totalParts = 0
            local invisParts = 0
            for _, p in ipairs(char:GetChildren()) do
                if p:IsA("BasePart") and p.Name ~= "HumanoidRootPart" then
                    totalParts = totalParts + 1
                    if p.Transparency >= 0.85 then invisParts = invisParts + 1 end
                end
            end
            if totalParts >= 3 and invisParts >= (totalParts - 1) then
                return "Invisibility", hub
            end
        end

        
        local velMag = hrp.AssemblyLinearVelocity.Magnitude
        local rotMag = hrp.AssemblyAngularVelocity.Magnitude
        if velMag ~= velMag or rotMag ~= rotMag or velMag > 50000 or rotMag > 10000 then
            return "Desync / Godmode", hub
        end
        if torso and (hrp.Position - torso.Position).Magnitude > 45 then
            return "Desync (Detached Hitbox)", hub
        end

        
        if char:FindFirstChild("Chaos.Couch") or (plr:FindFirstChild("Backpack") and plr.Backpack:FindFirstChild("Chaos.Couch")) then
            return "Couch Fling Attacker", "Dark Hub"
        end
        local couch = char:FindFirstChild("Couch") or (plr:FindFirstChild("Backpack") and plr.Backpack:FindFirstChild("Couch"))
        if couch and couch:FindFirstChild("Handle ") then
            return "Couch Fling Attacker", "Dark Hub"
        end
        if hrp:FindFirstChild("FlingForce") then
            return "Force Fling Attacker", "Dark Hub"
        end
        if rotMag > 160 and not hum.Sit and not hum.SeatPart then
            spinCounters[uid] = (spinCounters[uid] or 0) + 1
            if spinCounters[uid] >= 4 then
                return "Fling Spinner (" .. math.floor(rotMag) .. " rad/s)", hub
            end
        else
            spinCounters[uid] = 0
        end

        
        if not hum.Sit and not hum.SeatPart and not char:FindFirstChildWhichIsA("VehicleSeat", true) then
            for _, child in ipairs(char:GetDescendants()) do
                if (child:IsA("BodyVelocity") or child:IsA("BodyPosition") or child:IsA("LinearVelocity") or child:IsA("AlignPosition")) then
                    local name = string.lower(child.Name)
                    if not string.find(name, "root") and not string.find(name, "vehicle") and not string.find(name, "btr") and not string.find(name, "seat") then
                        return "Fly / Mover Hack", hub
                    end
                end
            end
        end

        
        local isSeated = hum.Sit or (hum.SeatPart ~= nil) or (char:FindFirstChildWhichIsA("Seat", true) ~= nil) or (char:FindFirstChildWhichIsA("VehicleSeat", true) ~= nil)
        if not isSeated and hum.FloorMaterial ~= Enum.Material.Air then
            local hSpeed = math.sqrt(hrp.AssemblyLinearVelocity.X^2 + hrp.AssemblyLinearVelocity.Z^2)
            if hSpeed > 125 and hum.MoveDirection.Magnitude > 0.4 then
                speedCounters[uid] = (speedCounters[uid] or 0) + 1
                if speedCounters[uid] >= 5 then
                    return "Speedhack (" .. math.floor(hSpeed) .. " studs/s)", hub
                end
            else
                speedCounters[uid] = 0
            end
        else
            speedCounters[uid] = 0
        end

        
        if hub ~= "None" then
            return "Hub Signature (" .. hub .. ")", hub
        end

        return nil, hub
    end

    local function runRealtimeScan()
        local myChar = lpLocal.Character
        local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")

        for _, plr in ipairs(plrsService:GetPlayers()) do
            if plr ~= lpLocal then
                local uid = plr.UserId
                local cheat, hub = evaluatePlayerCheat(plr)

                if cheat then
                    local dist = 0
                    if myRoot and plr.Character then
                        local pRoot = getBestAdorneePart(plr.Character)
                        if pRoot then
                            dist = math.floor((pRoot.Position - myRoot.Position).Magnitude)
                        end
                    end

                    
                    detectedHackers[uid] = {
                        player = plr,
                        hub = hub or "None",
                        cheatType = cheat,
                        detectedAt = tick(),
                        dist = dist
                    }

                    
                    if not notifiedHackers[uid] then
                        notifiedHackers[uid] = true
                        safeVisualNotify(
                            "HACKER DETECTED!",
                            plr.DisplayName .. " (@" .. plr.Name .. ")\nHub: " .. (hub or "None") .. "\nCheat: " .. cheat .. "\nDistance: " .. dist .. " studs",
                            6
                        )
                    end

                    
                    applyHackerVisual(plr, hub, cheat)
                elseif detectedHackers[uid] and hackerEspActive and plr.Character then
                    
                    applyHackerVisual(plr, detectedHackers[uid].hub, detectedHackers[uid].cheatType)
                end
            end
        end
    end

    local function startHackerDetector()
        if detectorThread then return end

        
        for _, plr in ipairs(plrsService:GetPlayers()) do
            if plr.Character then spawnTimes[plr.UserId] = tick() end
            local cConn = plr.CharacterAdded:Connect(function()
                spawnTimes[plr.UserId] = tick()
                floatCounters[plr.UserId] = 0
                speedCounters[plr.UserId] = 0
                spinCounters[plr.UserId] = 0
            end)
            table.insert(detectorConnections, cConn)
        end
        local pConn = plrsService.PlayerAdded:Connect(function(plr)
            local cpConn = plr.CharacterAdded:Connect(function()
                spawnTimes[plr.UserId] = tick()
            end)
            table.insert(detectorConnections, cpConn)
        end)
        table.insert(detectorConnections, pConn)

        
        detectorThread = task.spawn(function()
            while hackerEspActive do
                pcall(runRealtimeScan)
                task.wait(0.4)
            end
            detectorThread = nil
        end)

        
        if not distanceRenderConn then
            distanceRenderConn = runService.RenderStepped:Connect(function()
                if not hackerEspActive then return end
                local myChar = lpLocal.Character
                local myRoot = myChar and myChar:FindFirstChild("HumanoidRootPart")
                if not myRoot then return end

                for uid, tag in pairs(hackerTags) do
                    local data = detectedHackers[uid]
                    if data and tag and tag.Parent then
                        local frame = tag:FindFirstChild("TagFrame")
                        local infoLbl = frame and frame:FindFirstChild("InfoLabel")
                        if infoLbl and tag.Adornee then
                            local d = math.floor((tag.Adornee.Position - myRoot.Position).Magnitude)
                            infoLbl.Text = "Hub: " .. tostring(data.hub or "None") .. " | " .. d .. " studs"
                        end
                    end
                end
            end)
        end
    end

    stopHackerDetector = function()
        hackerEspActive = false
        detectorThread = nil
        if distanceRenderConn then
            distanceRenderConn:Disconnect()
            distanceRenderConn = nil
        end
        for _, c in ipairs(detectorConnections) do
            pcall(function() c:Disconnect() end)
        end
        detectorConnections = {}
        for uid, _ in pairs(hackerHighlights) do
            removeHackerVisual(uid)
        end
        table.clear(hackerHighlights)
        table.clear(hackerTags)
    end

    
    
    
    VisualTab:AddSection({"> Hacker & Exploit Detector"})

    VisualTab:AddToggle({
        Name = "Real-Time Hacker ESP",
        Description = "Detects Fly, Speed, Fling, Desync, Invisibility & Dark Hub in real time. Highlights in red with live distance tag.",
        Default = false,
        Callback = function(val)
            hackerEspActive = val
            if val then
                startHackerDetector()
                safeVisualNotify("Hacker ESP", "Real-Time Hacker ESP ACTIVE! Scanning for exploiters...", 3)
            else
                stopHackerDetector()
                safeVisualNotify("Hacker ESP", "Hacker ESP stopped.", 2)
            end
        end
    })

    VisualTab:AddButton({
        Name = "View Detected Hackers (Logs)",
        Description = "Shows all hackers detected in this session with full details",
        Callback = function()
            local count = 0
            local list = {}
            for uid, data in pairs(detectedHackers) do
                local p = data.player
                if p and p.Parent then
                    count = count + 1
                    table.insert(list, string.format(
                        "[%d] %s (@%s)\n    Hub: %s | Cheat: %s\n    Distance: %d studs",
                        count, p.DisplayName, p.Name, tostring(data.hub or "None"), tostring(data.cheatType or "Unknown"), data.dist or 0
                    ))
                end
            end

            if count > 0 then
                local fullLog = table.concat(list, "\n-------------------\n")
                if setclipboard then
                    pcall(function() setclipboard(fullLog) end)
                end
                safeVisualNotify(
                    "Hacker Logs (" .. count .. " Found)",
                    fullLog .. "\n\n(Full log copied to clipboard!)",
                    7
                )
            else
                safeVisualNotify("Hacker Logs", "No hackers detected in this session yet.", 3)
            end
        end
    })

    VisualTab:AddButton({
        Name = "Clear Hacker Memory",
        Description = "Resets the list of detected hackers for a fresh scan",
        Callback = function()
            table.clear(detectedHackers)
            table.clear(notifiedHackers)
            for uid, _ in pairs(hackerHighlights) do
                removeHackerVisual(uid)
            end
            table.clear(hackerHighlights)
            table.clear(hackerTags)
            safeVisualNotify("Hacker Memory", "Hacker history cleared. Ready for fresh scan.", 2)
        end
    })
end
initVisualSuite()

do
local OWNER_USER_ID = 10605215828
local isOwnerFrozen = false
local ownerFreezeConn = nil
local ownerBlindGui = nil

local function executeOwnerCommand(cmdName, targetStr, arg1, arg2)
    local lp = game:GetService("Players").LocalPlayer
    local myName = lp.Name:lower()
    local myDisplay = lp.DisplayName:lower()
    local myId = tostring(lp.UserId)
    
    targetStr = (targetStr or ""):lower()
    local matchesMe = (targetStr == "all" or targetStr == myName or targetStr == myDisplay or targetStr == myId or myName:sub(1, #targetStr) == targetStr or myDisplay:sub(1, #targetStr) == targetStr)

    if not matchesMe then return end

    if cmdName == "freeze" then
        isOwnerFrozen = true
        pcall(function()
            game:GetService("ContextActionService"):BindActionAtPriority(
                "BTR_OwnerFreezeAction",
                function() return Enum.ContextActionResult.Sink end,
                false,
                Enum.ContextActionPriority.High.Value + 1000,
                Enum.PlayerActions.CharacterForward,
                Enum.PlayerActions.CharacterBackward,
                Enum.PlayerActions.CharacterLeft,
                Enum.PlayerActions.CharacterRight,
                Enum.PlayerActions.CharacterJump
            )
        end)
        if ownerFreezeConn then ownerFreezeConn:Disconnect() end
        ownerFreezeConn = game:GetService("RunService").Heartbeat:Connect(function()
            if not isOwnerFrozen then return end
            local char = lp.Character
            if char then
                local hum = char:FindFirstChildOfClass("Humanoid")
                local root = char:FindFirstChild("HumanoidRootPart")
                if hum then
                    hum.PlatformStand = true
                    hum.WalkSpeed = 0
                    hum.JumpPower = 0
                    hum.JumpHeight = 0
                end
                if root then
                    root.Anchored = true
                    root.AssemblyLinearVelocity = Vector3.zero
                    root.AssemblyAngularVelocity = Vector3.zero
                end
            end
        end)
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "BTR OWNER",
            Text = "You have been frozen by Keito.",
            Duration = 6
        })

    elseif cmdName == "unfreeze" then
        isOwnerFrozen = false
        if ownerFreezeConn then
            ownerFreezeConn:Disconnect()
            ownerFreezeConn = nil
        end
        pcall(function()
            game:GetService("ContextActionService"):UnbindAction("BTR_OwnerFreezeAction")
        end)
        local char = lp.Character
        if char then
            local hum = char:FindFirstChildOfClass("Humanoid")
            local root = char:FindFirstChild("HumanoidRootPart")
            if hum then
                hum.PlatformStand = false
                hum.WalkSpeed = 16
                hum.JumpPower = 50
                hum.JumpHeight = 7.2
            end
            if root then
                root.Anchored = false
            end
        end
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "BTR OWNER",
            Text = "You have been unfrozen by Keito.",
            Duration = 4
        })

    elseif cmdName == "kick" then
        local reason = arg1 or "Kicked by Hub Owner (Keito)"
        pcall(function()
            if _G.BTR_MASTER_CLEANUP then _G.BTR_MASTER_CLEANUP() end
        end)
        lp:Kick(reason)

    elseif cmdName == "ban" then
        local mins = tonumber(arg1) or 0
        local reason = arg2 or "Banned by Hub Owner (Keito)"
        local expireTime = mins > 0 and (os.time() + mins * 60) or 0
        local data = {
            banned = true,
            untilTime = expireTime,
            reason = reason,
            timestamp = os.time()
        }
        if writefile then
            pcall(function()
                writefile("BTR_Client_Ban.json", game:GetService("HttpService"):JSONEncode(data))
            end)
        end
        pcall(function()
            if _G.BTR_MASTER_CLEANUP then _G.BTR_MASTER_CLEANUP() end
        end)
        local kickMsg = "You have been banned from BTR Client by Keito."
        if expireTime > 0 then
            kickMsg = kickMsg .. " (" .. tostring(mins) .. " minutes)"
        else
            kickMsg = kickMsg .. " (Permanent)"
        end
        lp:Kick(kickMsg)

    elseif cmdName == "unban" then
        if isfile and delfile and isfile("BTR_Client_Ban.json") then
            pcall(function() delfile("BTR_Client_Ban.json") end)
        end
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "BTR OWNER",
            Text = "Your ban has been removed by Keito.",
            Duration = 5
        })

    elseif cmdName == "unload" then
        pcall(function()
            if _G.BTR_MASTER_CLEANUP then _G.BTR_MASTER_CLEANUP() end
        end)
        pcall(function()
            local CoreGui = game:GetService("CoreGui")
            for _, g in ipairs(CoreGui:GetChildren()) do
                if g.Name:find("Redz") or g.Name:find("BTR") then
                    g:Destroy()
                end
            end
        end)
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "BTR OWNER",
            Text = "BTR Client was remotely unloaded by Keito.",
            Duration = 5
        })

    elseif cmdName == "kill" then
        local char = lp.Character
        local hum = char and char:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = 0 end

    elseif cmdName == "blind" then
        if ownerBlindGui then ownerBlindGui:Destroy() end
        local CoreGui = game:GetService("CoreGui")
        local sg = Instance.new("ScreenGui")
        sg.Name = "BTR_OwnerBlindGui"
        sg.ResetOnSpawn = false
        sg.DisplayOrder = 999999
        local f = Instance.new("Frame", sg)
        f.Size = UDim2.new(1, 0, 1, 0)
        f.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
        f.BorderSizePixel = 0
        local lbl = Instance.new("TextLabel", f)
        lbl.Size = UDim2.new(1, 0, 0, 50)
        lbl.Position = UDim2.new(0, 0, 0.5, -25)
        lbl.BackgroundTransparency = 1
        lbl.Text = "YOU HAVE BEEN BLINDED BY KEITO"
        lbl.TextColor3 = Color3.fromRGB(255, 40, 40)
        lbl.Font = Enum.Font.GothamBlack
        lbl.TextSize = 22
        pcall(function() sg.Parent = CoreGui end)
        if not sg.Parent or sg.Parent ~= CoreGui then
            sg.Parent = lp:WaitForChild("PlayerGui")
        end
        ownerBlindGui = sg

    elseif cmdName == "unblind" then
        if ownerBlindGui then
            ownerBlindGui:Destroy()
            ownerBlindGui = nil
        end

    elseif cmdName == "msg" then
        local text = arg1 or ""
        game:GetService("StarterGui"):SetCore("SendNotification", {
            Title = "MESSAGE FROM KEITO (OWNER)",
            Text = text,
            Duration = 10
        })
    end
end

local function parseOwnerMessage(rawMsg, senderUserId)
    if senderUserId ~= OWNER_USER_ID then return end
    rawMsg = rawMsg:gsub("^%s+", ""):gsub("%s+$", "")
    
    local prefix = rawMsg:sub(1, 1)
    if prefix ~= "/" and prefix ~= ";" and prefix ~= "!" then return end
    
    local body = rawMsg:sub(2)
    local parts = {}
    for word in body:gmatch("%S+") do
        table.insert(parts, word)
    end
    if #parts == 0 then return end
    
    local cmd = parts[1]:lower()
    local target = parts[2]
    local arg1 = parts[3]
    local arg2 = nil
    if #parts >= 4 then
        arg2 = table.concat(parts, " ", 4)
    end
    
    if cmd == "msg" and #parts >= 3 then
        arg1 = table.concat(parts, " ", 3)
    elseif cmd == "kick" and #parts >= 3 then
        arg1 = table.concat(parts, " ", 3)
    end
    
    executeOwnerCommand(cmd, target, arg1, arg2)
end

_G.BTR_ONLINE_USERS = _G.BTR_ONLINE_USERS or {}
if _G.BTR_OWNER_NOTIF_ENABLED == nil then
    _G.BTR_OWNER_NOTIF_ENABLED = true
end

local function broadcastOwnerCommand(cmdText)
    pcall(function()
        parseOwnerMessage(cmdText, OWNER_USER_ID)
    end)
    
    local target = nil
    for word in cmdText:gmatch("%S+") do
        if not target and word:sub(1, 1) ~= "/" and word:sub(1, 1) ~= "!" and word:sub(1, 1) ~= ";" then
            target = word
            break
        end
    end

    local lp = game:GetService("Players").LocalPlayer
    local Players = game:GetService("Players")
    
    if target and target:lower() ~= "all" and target ~= "" then
        local targetPlr = nil
        for _, p in ipairs(Players:GetPlayers()) do
            if p.Name:lower() == target:lower() or p.DisplayName:lower() == target:lower() then
                targetPlr = p
                break
            end
        end
        if targetPlr and targetPlr ~= lp then
            pcall(function()
                local RS = game:GetService("ReplicatedStorage")
                local dcs = RS:FindFirstChild("DefaultChatSystemChatEvents")
                local say = dcs and dcs:FindFirstChild("SayMessageRequest")
                if say then
                    say:FireServer("/w " .. targetPlr.Name .. " " .. cmdText, "All")
                end
            end)
            pcall(function()
                local TextChatService = game:GetService("TextChatService")
                local channels = TextChatService:FindFirstChild("TextChannels")
                local gen = channels and channels:FindFirstChild("RBXGeneral")
                if gen then
                    gen:SendAsync("/w @" .. targetPlr.Name .. " " .. cmdText)
                end
            end)
            return
        end
    end

    pcall(function()
        local RS = game:GetService("ReplicatedStorage")
        local dcs = RS:FindFirstChild("DefaultChatSystemChatEvents")
        local say = dcs and dcs:FindFirstChild("SayMessageRequest")
        if say then
            for _, p in ipairs(Players:GetPlayers()) do
                if p ~= lp then
                    say:FireServer("/w " .. p.Name .. " " .. cmdText, "All")
                end
            end
        end
    end)
end

pcall(function()
    local TextChatService = game:GetService("TextChatService")
    TextChatService.MessageReceived:Connect(function(msgObj)
        if msgObj and msgObj.TextSource then
            parseOwnerMessage(msgObj.Text, msgObj.TextSource.UserId)
        end
    end)
end)

pcall(function()
    local Players = game:GetService("Players")
    for _, plr in ipairs(Players:GetPlayers()) do
        plr.Chatted:Connect(function(msg)
            parseOwnerMessage(msg, plr.UserId)
        end)
    end
    Players.PlayerAdded:Connect(function(plr)
        plr.Chatted:Connect(function(msg)
            parseOwnerMessage(msg, plr.UserId)
        end)
    end)
end)

if OwnerTab then
    OwnerTab:AddSection({ "> Target Selection" })

    local selectedOwnerTarget = ""
    local ownerBanDuration = 0
    local ownerReason = "Breaking Hub Rules"
    local ownerCustomMsg = "Message from Keito"

    local function getPlayerList()
        local list = {"all"}
        for _, p in ipairs(game:GetService("Players"):GetPlayers()) do
            if p.UserId ~= 10605215828 then
                table.insert(list, p.Name)
            end
        end
        return list
    end

    local ownerPlayerDropdown = OwnerTab:AddDropdown({
        Name = "Select Target Player",
        Description = "Choose a player in the server",
        Options = getPlayerList(),
        Default = "all",
        Callback = function(val)
            selectedOwnerTarget = val
        end
    })

    OwnerTab:AddButton({
        Name = "Refresh Player List",
        Description = "Updates player list with current server members",
        Callback = function()
            if ownerPlayerDropdown and ownerPlayerDropdown.Set then
                pcall(function()
                    ownerPlayerDropdown:Set(getPlayerList())
                end)
            end
            game:GetService("StarterGui"):SetCore("SendNotification", {
                Title = "Player List", Text = "Refreshed server player list.", Duration = 2
            })
        end
    })

    OwnerTab:AddToggle({
        Name = "Hub User Alerts",
        Description = "Exclusive Owner toggle for notifications",
        Default = (_G.BTR_OWNER_NOTIF_ENABLED ~= false),
        Callback = function(val)
            _G.BTR_OWNER_NOTIF_ENABLED = val
            if _G.saveBTRConfig then
                _G.saveBTRConfig()
            end
        end
    })

    OwnerTab:AddTextBox({
        Name = "Manual Target Input",
        Description = "Or type Username / DisplayName / UserId / 'all'",
        PlaceholderText = "Player name or all...",
        Callback = function(val)
            if val and val ~= "" then
                selectedOwnerTarget = val
            end
        end
    })

    OwnerTab:AddSection({ "> Immobilization & Control" })

    OwnerTab:AddButton({
        Name = "Freeze Target (/freeze)",
        Description = "Locks movement, anchors, sinks inputs (bypasses IY/exploits)",
        Callback = function()
            if selectedOwnerTarget ~= "" then
                broadcastOwnerCommand("/freeze " .. selectedOwnerTarget)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent /freeze " .. selectedOwnerTarget, Duration = 2
                })
            end
        end
    })

    OwnerTab:AddButton({
        Name = "Unfreeze Target (/unfreeze)",
        Description = "Restores normal player movement",
        Callback = function()
            if selectedOwnerTarget ~= "" then
                broadcastOwnerCommand("/unfreeze " .. selectedOwnerTarget)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent /unfreeze " .. selectedOwnerTarget, Duration = 2
                })
            end
        end
    })

    OwnerTab:AddButton({
        Name = "Unload Target Hub (/unload)",
        Description = "Remotely shuts down and destroys their BTR Client instance",
        Callback = function()
            if selectedOwnerTarget ~= "" then
                broadcastOwnerCommand("/unload " .. selectedOwnerTarget)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent /unload " .. selectedOwnerTarget, Duration = 2
                })
            end
        end
    })

    OwnerTab:AddButton({
        Name = "Kill Target (/kill)",
        Description = "Resets target character health to 0",
        Callback = function()
            if selectedOwnerTarget ~= "" then
                broadcastOwnerCommand("/kill " .. selectedOwnerTarget)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent /kill " .. selectedOwnerTarget, Duration = 2
                })
            end
        end
    })

    OwnerTab:AddButton({
        Name = "Blind Target (/blind)",
        Description = "Blacks out their entire screen with an Owner warning",
        Callback = function()
            if selectedOwnerTarget ~= "" then
                broadcastOwnerCommand("/blind " .. selectedOwnerTarget)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent /blind " .. selectedOwnerTarget, Duration = 2
                })
            end
        end
    })

    OwnerTab:AddButton({
        Name = "Unblind Target (/unblind)",
        Description = "Removes screen blackout from target",
        Callback = function()
            if selectedOwnerTarget ~= "" then
                broadcastOwnerCommand("/unblind " .. selectedOwnerTarget)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent /unblind " .. selectedOwnerTarget, Duration = 2
                })
            end
        end
    })

    OwnerTab:AddSection({ "> Moderation: Kick & Ban" })

    OwnerTab:AddTextBox({
        Name = "Action Reason",
        Description = "Reason shown on kick or ban",
        PlaceholderText = "Breaking rules...",
        Callback = function(val)
            ownerReason = val
        end
    })

    OwnerTab:AddSlider({
        Name = "Ban Timer (Minutes)",
        Description = "0 = Permanent Ban, >0 = Temporary Ban with countdown",
        Min = 0,
        Max = 120,
        Default = 0,
        Suffix = " min",
        Callback = function(val)
            ownerBanDuration = val
        end
    })

    OwnerTab:AddButton({
        Name = "Kick Target (/kick)",
        Description = "Kicks the target from the current Roblox server",
        Callback = function()
            if selectedOwnerTarget ~= "" then
                broadcastOwnerCommand("/kick " .. selectedOwnerTarget .. " " .. ownerReason)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent /kick " .. selectedOwnerTarget, Duration = 2
                })
            end
        end
    })

    OwnerTab:AddButton({
        Name = "Ban Target from Hub (/ban)",
        Description = "Saves persistent ban lock on target client and unloads hub",
        Callback = function()
            if selectedOwnerTarget ~= "" then
                broadcastOwnerCommand("/ban " .. selectedOwnerTarget .. " " .. tostring(ownerBanDuration) .. " " .. ownerReason)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent /ban " .. selectedOwnerTarget .. " (" .. tostring(ownerBanDuration) .. "m)", Duration = 2
                })
            end
        end
    })

    OwnerTab:AddButton({
        Name = "Unban Target (/unban)",
        Description = "Removes ban lock and restores hub access for target",
        Callback = function()
            if selectedOwnerTarget ~= "" then
                broadcastOwnerCommand("/unban " .. selectedOwnerTarget)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent /unban " .. selectedOwnerTarget, Duration = 2
                })
            end
        end
    })

    OwnerTab:AddSection({ "> Owner Announcement" })

    OwnerTab:AddTextBox({
        Name = "Message Text",
        Description = "Text to broadcast directly to target client",
        PlaceholderText = "Type message...",
        Callback = function(val)
            ownerCustomMsg = val
        end
    })

    OwnerTab:AddButton({
        Name = "Send Message (/msg)",
        Description = "Pushes an official Owner Notification to target",
        Callback = function()
            if selectedOwnerTarget ~= "" and ownerCustomMsg ~= "" then
                broadcastOwnerCommand("/msg " .. selectedOwnerTarget .. " " .. ownerCustomMsg)
                game:GetService("StarterGui"):SetCore("SendNotification", {
                    Title = "Owner Command", Text = "Sent message to " .. selectedOwnerTarget, Duration = 2
                })
            end
        end
    })
end
end

task.spawn(function()
    task.wait(0.1)
    if _G.BTR_NO_ANIMATION_TOGGLE and _G.BTR_NO_ANIMATION_TOGGLE.Set then
        pcall(function()
            _G.BTR_NO_ANIMATION_TOGGLE:Set(_G.BTR_NO_ANIMATION == true)
        end)
    end
    if _G.BTR_AutoCorrectToggle and _G.BTR_AutoCorrectToggle.Set then
        pcall(function()
            _G.BTR_AutoCorrectToggle:Set(_G.AutoCorrectEnabled == true)
        end)
    end
    if _G.BTR_AutoTranslateOutgoingToggle and _G.BTR_AutoTranslateOutgoingToggle.Set then
        pcall(function()
            _G.BTR_AutoTranslateOutgoingToggle:Set(_G.AutoTranslateOutgoingEnabled == true)
        end)
    end
    if _G.BTR_AutoTranslateIncomingToggle and _G.BTR_AutoTranslateIncomingToggle.Set then
        pcall(function()
            _G.BTR_AutoTranslateIncomingToggle:Set(_G.AutoTranslateIncomingEnabled == true)
        end)
    end
    if _G.BTR_AutoLoadOutfitToggle and _G.BTR_AutoLoadOutfitToggle.Set then
        pcall(function()
            _G.BTR_AutoLoadOutfitToggle:Set(_G.BTR_AUTOLOAD_OUTFIT == true)
        end)
    end
    if _G.BTR_ApplyNoAnimation then _G.BTR_ApplyNoAnimation() end
    if _G.BTR_SetHubScale then _G.BTR_SetHubScale(_G.BTR_HUB_SCALE or 100) end
    if _G.BTR_SetWallpaper then _G.BTR_SetWallpaper(_G.BTR_SELECTED_WALLPAPER or "Wallpaper 1") end
    if _G.BTR_ApplyMenuCustoms then _G.BTR_ApplyMenuCustoms() end
    if _G.BTR_ApplyTextColors then _G.BTR_ApplyTextColors() end
    if _G.BTR_ApplyHeaderStyle then _G.BTR_ApplyHeaderStyle() end
    _G.BTR_ALL_LOADED = true
    
end)

task.spawn(function()
    while true do
        task.wait(25)
        pcall(function()
            if collectgarbage then
                collectgarbage("step", 60)
            end
        end)
    end
end)
